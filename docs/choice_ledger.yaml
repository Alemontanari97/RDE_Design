# CHOICE LEDGER — typed index of the algorithmic choice-adjudication
# ledger (standing directive choice-adjudication-convergence,
# 2026-08-12; S-ORDINE T2 step S5a, census R25/R32).
#
# PROSE AUTHORITY (unchanged by this file): the ANNEX "6. ANNEX — THE
# CHOICE LEDGER" in validation/ADVISORY_S24_sota_gapmap_2026-08-12.md
# (rows C1-C45, table + reading paragraph). This YAML INDEXES that
# annex — it never replaces it. Per-row deltas that postdate the
# annex (executed twins/sweeps, closures, three S25-bis-declared new
# rows) are folded in as status/note updates CITING
# docs/rde_nozzle_PROGRESS.md (R25 census rows), never silently
# overwriting the annex text.
#
# SCHEMA (strict subset, mirrors docs/findings_registry.yaml
# discipline): each entry
#   id: C<n>                                   (C1..C48, unique)
#   choice: <one line>                          (what is being decided)
#   incumbent: <the code's current behavior>
#   alternatives: [<genuine SOTA alternative>, ...]   (may be [])
#   status: DECIDED | MIXED | SINGLE-AUTHOR | NEVER
#     DECIDED       = converged-panel / constructional / measured
#                      verdict of record (annex "CONVERGED*")
#     MIXED         = one branch DECIDED, a sibling branch NEVER
#                      (e.g. eq-path decided, IP-path never surveyed)
#     SINGLE-AUTHOR = one author's rationale stands, no survey/panel
#     NEVER         = registered open row, no comparison run yet
#   evidence: "<path#anchor>"   (resolvable: annex row anchor for
#                                 C1-C45; PROGRESS/session-log anchor
#                                 for rows carrying a post-annex delta
#                                 or for the three new C46-C48 rows)
#   owner: REQUIRED for every NEVER and SINGLE-AUTHOR row (verbatim
#          from the annex "Owner+placement if open" column, or the
#          census/session-log text for C46-C48; "F2 window
#          (unassigned)" only if the source truly names none)
#   note: optional — annex's own internal evidence-pointer text
#         (D6/param-F.../GAP-N/AC-N, informational, not necessarily
#         independently resolvable), annex status nuance verbatim,
#         and/or the PROGRESS delta citation for touched rows.
#
# LINT: none yet — no tests/test_choice_ledger.py exists at the time
# of this extraction (S-ORDINE T2/S5a is a READ-ONLY-elsewhere step,
# one new file only). Declared gap, owner = next S-ORDINE/S-CERT
# window (mirror tests/test_findings_registry.py's group (xix)
# pattern: schema/enum check, per-status required-field check,
# evidence-anchor resolution check, duplicate-id check).
#
# COUNTS (verified at extraction time, see verification note below):
#   annex rows of record (C1-C45): 45
#   this registry: 62 rows (45 indexed + C46-C48 newly declared
#     S25-bis rows + C49-C51 minted S-FOUNDATIONS 2026-08-17 — growth
#     enumerated, not silent: C49 Phase-B diff omission, C50/C51 the
#     blind-contract judge's C-4 deliveries — + C52-C54 minted
#     2026-08-19, S-FOUNDATIONS-C Blocco-2 landing: the remaining
#     C-4 agenda items D-1/D-2/D-4 of
#     validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md
#     Part A (iv), landed as ledger rows per the same condition
#     — + C55 minted 2026-08-19 at the S-FOUNDATIONS-C boundary
#     — + C56 minted 2026-08-19 at the C9/C11 wave-1 landing (count
#       regenerated by measured grep same window, SR-12)
#     — + C57 minted 2026-08-19 at the wave-2 landing (global
#       exploration tier; count regenerated same window)
#     — + C58 minted 2026-08-20 at the wave-3 landing (differentiable-
#       stack FOUNDATION row, user catch — third instance of the
#       no-row foundation class after C49/C56; dedup by measured grep
#       and count regenerated by measured command same window, SR-12)
#     — + C59/C60 minted 2026-08-21 at the C4 COVERAGE GATE
#       (FOUNDATION-CHOICE ENUMERATION category: C59 temporal form of
#       the design functional, canonical-inside-the-pin scoping per
#       BRIEF_blocco2_phaseD_addendum_c4.md (e); C60 NAND-vs-SAND
#       optimizer architecture, the FORK_LEDGER_141_adjudication.md
#       D-1/O15 rider executed — 4th and 5th instances of the no-row
#       foundation class; dedup by measured grep and count
#       regenerated by measured command same window, SR-12)
#     — + C61/C62 minted 2026-08-21 at the C4 COVERAGE GATE REPAIR
#       pass (critic findings CGC-4/CGC-5: C61 base-pressure closure
#       model p_b, the harvest-adjudicated axis with the practiced
#       legacy Veen incumbent NOT adopted by the program; C62 phase
#       quadrature over Xi, the cycle-average discretization axis —
#       6th and 7th instances of the no-row foundation class; dedup
#       by measured grep and count regenerated by measured command
#       same window, SR-12)
#     touchpoint: P_amb aggregation form, the user-ratified parked
#     decision (a), GATED-by-owner on first multi-point instantiation)
#   tallies by status (red-team-corrected recount, replacing the
#     document's own inconsistent "8/14/23" line — see the annex
#     reading paragraph and docs/rde_nozzle_PROGRESS.md R25):
#     DECIDED: 5 (C3, C4, C15, C23, C24)
#     MIXED: 3 (C29, C31, C39)
#     SINGLE-AUTHOR: 15 (C1, C6, C11, C12, C13, C17, C18, C19, C27,
#       C32, C44, C45, C46, C47 + C48 counted under DECIDED — see
#       C48 note; SINGLE-AUTHOR annex-only subtotal = 12, plus C46
#       and C47 = 14; recount below is authoritative)
#     NEVER: 25 (C2, C5, C7, C8, C9, C10, C14, C16, C20, C21, C22,
#       C25, C26, C28, C30, C33, C34, C35, C36, C37, C38, C40, C41,
#       C42, C43)
#   EXACT machine tally of this file (do not hand-eyeball the block
#   above further — count the `status:` field values instead;
#   post-WAVE-3 LANDING 2026-08-20, regenerated by measured
#   Select-String commands in the landing window (SR-12; supersedes
#   the stale 2026-08-13 post-reconciliation tally, which had not
#   been regenerated at the C55/C56/C57 mints):
#     DECIDED: 12 (C3, C4, C15, C19, C23, C24, C26, C30, C45, C46,
#       C47, C48)
#     MIXED: 36 (C1, C2, C5, C6, C7, C8, C9, C10, C11, C12, C13, C14,
#       C16, C20, C21, C22, C27, C28, C29, C31, C32, C33, C34, C35,
#       C36, C37, C39, C40, C41, C42, C43, C44, C49, C50, C56, C58)
#     SINGLE-AUTHOR: 2 (C17, C18)
#     NEVER: 12 (C25, C38, C51, C52, C53, C54, C55, C57, C59, C60,
#       C61, C62 — C59/C60 added at the 2026-08-21 C4 coverage-gate
#       mint, C61/C62 at the same-day coverage-gate REPAIR pass
#       (CGC-4/CGC-5); tally regenerated by measured grep same
#       window, SR-12)
#     TOTAL: 62 (12+36+2+12=62; C49 minted at S-FOUNDATIONS Phase B —
#     the fit-vs-capture solution-representation choice had NO ledger
#     row, caught by the de-novo diff). C48 (vmap-in-engine) is classed
#     DECIDED here despite the census shorthand "gate-rejected as
#     default" because it carries a full multi-probe converged
#     technical verdict (m6gate + three convergent root-cause
#     probes), NOT a single-author or un-surveyed choice — see its
#     note for the declared nuance (adoption path itself still open,
#     owner F2).
#
# RECONCILIATION (cross-reconciliation sweep, 2026-08-13, S-ORDINE
# tail; user challenge "abbiamo passato a setaccio le scelte
# algoritmiche piu' volte — sicuri che le fasi siano conciliate?"):
# every row with status NEVER (25) or SINGLE-AUTHOR (14) was swept
# against the adjudication corpus below for evidence that the choice
# WAS in fact put against alternatives. ARITHMETIC OF RECORD:
# 39 rows swept -> 1 upgraded SINGLE-AUTHOR -> DECIDED (C46, padding
# policy: Form-2 fused convergence B-F2 vs P_DUM + refuter
# RF-11(iii) + measured-DEAD ragged route), 0 upgraded to MIXED,
# 0 upgraded NEVER -> SINGLE-AUTHOR, 25 CONFIRMED NEVER,
# 13 CONFIRMED SINGLE-AUTHOR. Every swept row carries a dated
# "cross-reconciled 2026-08-13" note (upgrade anchors, or the
# nearest evidence found and why it does not adjudicate, or a plain
# confirm).
# CORPUS SWEPT (all committed):
#   validation/ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md
#     (+ the _math and _impl_fidelity one-pass raws),
#   validation/ADVISORY_S25bis_diff_convergence_2026-08-12.md,
#   validation/ADVISORY_S24_thermo_closure_survey_2026-08-12.md,
#   validation/ADVISORY_S24_DEbucket_panel_2026-08-12.md,
#   validation/ADVISORY_def_equivalence_panel_2026-08-07.md + _proof,
#   validation/ADVISORY_Sgauntlet_generality_ledger_2026-08-11.md,
#   validation/ADVISORY_engine_speed_audit_2026-08-12.md,
#   validation/ADVISORY_litreview_confrontation_2026-08-13.md
#     (ADOPTED/A-rows only, cited strictly as
#     adjudication-evidence-pending-ratification — the doc is CAVA
#     NON RATIFICATA, never cited as decided),
#   validation/ADVISORY_rde_choking_2026-08-11.md,
#   validation/ADVISORY_mean_swirl_panel_2026-08-11.md,
#   validation/sota_gapmap_raws_2026-08-12/ (finder/verifier/refuter
#     raws — the annex's own sources; confirmations, never upgrades),
#   docs/rde_nozzle_PROGRESS_ARCHIVE.md (historical census deltas),
#   grep of validation/PROGRESS_*.md session logs per row subject.
# STANDARD APPLIED (dual-proof, strict): expert sense-review alone
# is NOT choice adjudication (it asks does-this-make-sense, not
# does-it-beat-the-alternatives); upgrades only where a CONVERGED
# panel/refuter round EXPLICITLY weighed the incumbent against
# genuine alternatives. Confirmation of NEVER is a legitimate
# outcome; no upgrade was forced.
entries:

- id: C1
  choice: "Design basis class"
  incumbent: "clamped/natural interpolating cubic, heights-as-dofs"
  alternatives:
    - "B-spline control polygon (de Boor/Boehm)"
    - "CST (Kulfan)"
    - "Hicks-Henne"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.5 C1"
  owner: "F2 duty F2-C1-CONTROL-CHART-MIGRATION (items 0-6 + driver leg; GAP-21 = the duty findings carrier)"
  note: "annex status verbatim: 'single-author on the oscillation axis (D6 adjudicated conditioning+revalidation only)'; annex internal pointer: D6:915-926; param-F1 + Q2. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); gapmap raws re-verify the D6:915-926 rejection of the B-spline control-point switch (sota_gapmap_raws_2026-08-12/s24_gapv_constraints.md:173 'collides with D6 S20 rejection of record') but that is the same single-author D6 axis the annex already scoped; no panel/refuter comparison of basis classes found. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.5/4.5): direction converged — certified carrier migrates to the B-spline control-polygon chart of the SAME certified spline space (Bernstein-exact admissibility certificates = the new axis); incumbent chart verdict-bearing until the three migration falsifiers pass; the first migration falsifier re-pinned on the chart innocent-data response with the per-use conversion-map pin; duty gains the DRIVER LEG (chart-map conditioning bound derived OR in-chart walk re-baseline inside derived bands); the D6 S20 rejection untouched on its axis AND weighed on its driver-conditioning content. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19). Duty-text dof-budget prior: Masters 2017 (B-spline ~42 dv average / 28-72 range for one-count aero convergence; geometric-error/force-error linear correlation; Kulfan tolerance insufficient by >1 order) — consume as a PRIOR for the campaign's control-point-count check with declared transfer scope (2-D external-aero Euler), never as a program-side bound (retro-sweep 2026-08-20)."

- id: C2
  choice: "Right-end BC"
  incumbent: "natural y''(L)=0"
  alternatives:
    - "not-a-knot (de Boor/FITPACK)"
    - "free-end control points"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.6 C2"
  owner: "conditional: branch A rides F2-C1-CONTROL-CHART-MIGRATION; branch B = duty F2-C2-NOTAKNOT-ADOPTION"
  note: "annex evidence pointer: toc:148; param-F2. POST-ANNEX DELTA (S25-bis, docs/rde_nozzle_PROGRESS.md R25 delta S25-bis block; findings_registry.yaml:parametrization:natural-BC-lip-bias): notaknot-twin EXECUTED — corner mismatch 6.629e-2 -> 1.166e-2 (r=1, -82%), 6.637e-2 -> 9.775e-3 (r=2); lip-state (cd) shift 8.2% = mechanism datum; baseline reproduces S19 figure to 5 digits. Diagnostic executed, ADOPTION DECISION still open -> status stays NEVER, owner updated from the stale 'S25 notaknot-twin (GAP-5)' to the live F2 consequence-adjudication owner. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the notaknot twin remains a DIAGNOSTIC (mechanism datum), the adoption comparison itself was never adjudicated; no further evidence found. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-CONDITIONAL, enum MIXED (VERDICT_wave2 par.2.6/4.6): branch A (C1 migration lands) — the end-condition dof dissolves, C2 RESOLVES by structure to the second listed alternative free-end control points; the GAP-5 numbers (corner -82 percent, cd 8.2 percent) = measured priors, re-measured at a certified optimum inside the C1 campaign; branch B (migration refuted) — live F2 adoption question per the frozen criteria. The BC itself NOT adjudicated (twin remains a diagnostic of record; brief pin honored). Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C3
  choice: "Left-end BC"
  incumbent: "clamped tan(thB)"
  alternatives: []
  status: DECIDED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C3 |"
  note: "annex status verbatim: 'CONVERGED (constructional) [J]' — attachment tangency, forced by geometry, no genuine alternative exists; annex internal pointer: toc:146-147; annex owner column: '-'. WAVE-2 CONFIRMATION 2026-08-19 = DECIDED confirmed-rescoped (VERDICT_wave2 par.2.7/4.7): clamped tan(thB) STANDS — neither tree challenger (P-F21 rounded lip; H-F18 PM-fan internal data) touches this row's axis; gloss narrowed to this axis of record. Attachment-GEOMETRY axis = NAMED census-lemma territory (live anchors: lip-circles pin + diff par.4.8 cone-vs-sharp tension; F2-exit user pin respected); KT2015 hundredth-fractions materiality carried; sharp-vs-floor twin falsifier pinned for any future census-window amendment. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C4
  choice: "Knot placement law"
  incumbent: "uniform xi + [X-AKNO] goal-indicator insertion"
  alternatives:
    - "free knots (Jupp)"
    - "curvature-based abscissae"
  status: DECIDED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C4 |"
  note: "annex status verbatim: 'survey-decision of record [S25-REPAIR: was \"CONVERGED-panel\"; D6 item 9 records an adopt-or-declare SURVEY decision, not a Form-2 panel]'; annex internal pointer: D6 item 9 (free-knot rejection, Jupp lethargy); S20 [X-AKNO] record. POST-ANNEX MENTION (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta paragraph): 'C4-suite chiusa' is named in the same R25 delta sentence alongside C17/C18/C23/C24/C34/C2; referent recorded verbatim from PROGRESS (test-suite/gate-closure context, distinct from and not altering this knot-placement survey-decision itself) — no change to status or alternatives. WAVE-3 VERIFICATION 2026-08-20 = CONFIRMED-CONSISTENT + note-delta (VERDICT_wave3 par.3 / VERIF_CONV par.1), R3CONV-1 repair adopted — the note's silence parenthesis reads: (measured full-tree sweep of record: 9 'knot' hits across the four FULL trees, none a placement law — opt :367/:430/:432/:467/:480/:1653, var :972, prop :778/:786; zero hits for jupp|free knot|free-knot|curvature-based|abscissa; refute_CONV.md R3CONV-1 completion consumed as the measurement). Rest of the VERIF_CONV par.1 delta verbatim (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C5
  choice: "Knot anchoring across thB updates"
  incumbent: "normalized xi re-anchored to live xB"
  alternatives:
    - "physical-x anchored knots"
    - "arc-length anchored knots"
    - "curvature-anchored knots"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C5: ADJUDICATED-CONVERGED-KEEP"
  owner: "F2 duty F2-C5-KNOTDRIFT-MONITOR (GAP-34 home)"
  note: "annex internal pointer: param-F8 (corrected magnitudes Q8). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-CONVERGED-KEEP, enum MIXED (measured half = named F2 duty) (VERDICT_wave3 par.4): PANEL_REMMARCH par.4.1 verbatim — incumbent kept, monitor-armed; GAP-34 measured immateriality; F-C5-1 with derived threshold + S18 replay control; no sustained refuter attack (R3REMMARCH-9 NO-ATTACK accepted: code-verified nesting, honest materiality). Note text of record = PANEL_REMMARCH par.4.1 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C6
  choice: "Insertion degeneracy guard"
  incumbent: "dx_loc = median station spacing (mesh-derived)"
  alternatives:
    - "operator-derived max adjacent-knot ratio (de Boor; Schoenberg-Whitney)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C6: ADJUDICATED-SPLIT (guard-object flip"
  owner: "F2 duty F2-C6-GUARD-EDIT (GAP-23 home)"
  note: "annex status verbatim: 'single-author (rationale measured wrong: solve vs operator)'; annex internal pointer: adaptive_knot_optimize:209-213; P4' probe. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block), no panel/refuter adjudication found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT, enum MIXED (VERDICT_wave3 par.4): guard-object FLIP; single-author residue DISCHARGED by panel+refuter (= the owed dual-proof half — the flip's evidentiary base survived genuine attack). PANEL_REMMARCH par.4.2 WITH (R3REMMARCH-8 amendment): F-C6-1 gains the branch — if the incumbent guard ALSO rejects the synthetic control, instantiate the control at the GAP-23 trigger configuration (Nw>=240 / max_ins>=3) where the degradation law admits it, or declare the separation not reproduced; the O-F8 fallback names its object: de Casteljau certified subdivision = a Bernstein-form bound on cardinal amplitude over the knot interval. (Cross-slot: C6 deliberately uses its OWN K_amp — declared, keeps the C42 census clean, VERDICT_wave3 par.7 item 1.) Note text of record = PANEL_REMMARCH par.4.2 AS REPAIRED by VERDICT_wave3 par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C7
  choice: "Outer-loop dof policy"
  incumbent: "insertion-only ratchet"
  alternatives:
    - "insert+remove (Lyche-Morken)"
    - "AFEM coarsening (BDD04)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C7: ADJUDICATED-SPLIT (insert+remove direction"
  owner: "F2 duty F2-DUTY-C7-REMOVALTEST (F2/F3 loop upgrade home, GAP-25; measurement-gated)"
  note: "annex internal pointer: param-F5 (corrected attribution). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT (insert+remove direction, end-of-cycle sweep, measurement-gated), enum MIXED (VERDICT_wave3 par.6): PANEL_REMPOL par.4.1 WITH (R3REMPOL-1 repair, adopted verbatim): the phantom band binding is REPLACED — F-C7-2 band = the [D3] derived stopping floor tol_stop = K_RICH × |rel(r=1) − rel(r=2)| (adaptive_knot:50-54 — the goal-metric noise floor, already derived); F-C7-1 retire test carries its OWN derived deviation band = the measured dev_warm floor across the recorded S20 cycles (the quantity the loop already prints), declared as the removal counterpart of the measured-not-gated insertion deviation; the par.3.1 sentence 'every insertion is gated by derived-band machinery' is corrected (insertion is Doerfler-marked and budget-capped; the deviation is measured and printed — the indicator is a refinement driver, not a verdict row). Grep claim corrected per R3REMPOL-2 (zero hits, conservative direction). Direction, F-C7-3 economy carrier, duty F2-DUTY-C7-REMOVALTEST, AFEM rejection, scope clause: unchanged. Lit rider handed to the landing per registry policy: Lyche-Morken 1987 [TITLE, WANTED-class] (PANEL_REMPOL par.4.6). Note text of record = PANEL_REMPOL par.4.1 AS REPAIRED by VERDICT_wave3 par.6 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C8
  choice: "Warm-start deviation handling"
  incumbent: "print-only dev_warm"
  alternatives:
    - "derived-floor rejector (armed) + THEOREM row"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C8: ADJUDICATED-CONVERGED"
  owner: "F2 duty F2-C8-ARM-DEVWARM (GAP-24 home; the floor constant rides the C42 role-census outcome — derived per-role constant substitutes on landing)"
  note: "annex status verbatim: 'NEVER (armable invariant unarmed)'; annex internal pointer: param-F4; P3 = 6.7e-16. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-CONVERGED, enum MIXED (VERDICT_wave3 par.4): armed rejector adopted; PANEL_REMMARCH par.4.3 WITH (R3REMMARCH-6): (i) seam clause added — the floor constant rides the C42 role-census outcome; a derived per-role constant substitutes on landing; (ii) the V-F7 citation DEMOTED (fold margins do not bear on re-representation exactness; O-F32 + the findings-row theorem + measured P3 carry the arming case — no double-counted advocacy). K_RICH role notification to FAM/C42 CONSUMED (VERDICT_wave3 par.1.5 item 2: census 10 + 3 notified, C8 dev_warm arming floor = the third notified role, binding). Falsifier discipline: the F-C8-1 negative control not firing = rejector vacuous, not adopted (VERDICT_wave3 par.10). Note text of record = PANEL_REMMARCH par.4.3 AS REPAIRED by VERDICT_wave3 par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C9
  choice: "March mesh law"
  incumbent: "uniform NI/da/Nw/Ne per run"
  alternatives:
    - "column-local C- insertion at record time"
    - "goal-oriented AMR (Venditti-Darmofal)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.3 Row C9"
  owner: "F2 duty F2-C9-MESHLAW-CAMPAIGN (was R25/F2 spec)"
  note: "annex status verbatim: 'NEVER (registered open row)'; annex internal pointer: R25; mesh-F1/AC2. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); nearest evidence = litreview A36 singularity-aware derived spacing law (log-weighted DWR equidistribution near the sonic strip, ADVISORY_litreview_confrontation_2026-08-13.md A36) = adjudication-evidence-pending-ratification only (CAVA NON RATIFICATA, cited as pending, never as decided); the incumbent uniform law was never put against the listed alternatives. WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED (VERDICT_wave1.md par.2.3/4.3): goal-oriented characteristic-native adapted placement = TARGET production law, presumptive pending F9a; uniform-per-run retained as interim certificate law (no verdict of record moves); A36 static arm adopted now (refinement-only, IVL immobile); AC2 column-insertion = the adaptation primitive ([X-AKNO] mechanism transferred, hypothesis under test); falsifiers F9a (matched TOTAL unit-process count, BOTH counts reported, promotion-threshold derivation published before first use), F9b (frozen-topology attribution re-run), F9c (IVL immobility). SUPPLEMENT OF RECORD (directive-axis closure; PANEL_C9C11_SUPPLEMENT.md axis A + VERDICT_C9C11_supplement.md par.4.1 FINAL): moving-mesh/r-adaptive family CLOSED at census grade — A36 IS the family equidistribution principle in derived static form; clause (ii) generalizes to F9c (one-locus anti-motion, not family closure); front-fitting = the family alignment content already exact of record (C49/Lemma B par.4.3); MMPDE map-motion does not transfer (columns = characteristic intersections; free dofs = seeds + insertion). FOLDED: F9a arm R = fixed-count wall-station seed redistribution (IVL-line/fan/exit seed families frozen-by-declaration; fixed shape, no recompile); matched TOTAL count EXPECTED by construction and VERIFIED by the repaired-F9a rule (both counts reported per arm, no exemption); IVL seed pinned per F9c; F9b via frozen pre-motion seed set; falsifier F9a-R (dropped if not beating uniform+A36 outside joint bands on the pre-registered twin sites; promoted only under the same E_A <= E_U/2 rule). Three-arm composition of record: C9 closes on uniform+A36 only if BOTH adaptive arms fail their beat tests; drop rules per-arm; at parity (joint-band discipline, before the cost tiebreak) the declared cost tiebreak selects the r-arm as production mechanism (fixed shapes) with insertion in the between-runs role; every such outcome closes C9 converged, not re-gated. J-materiality of motion-vs-insertion declared BELOW bands; cost materiality (recompile) declared FOR the r-arm."

- id: C10
  choice: "Refinement operator"
  incumbent: "refine(r) joint global scaling"
  alternatives:
    - "per-direction refinement (multi-parameter GCI, Eca-Hoekstra)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C10: ADJUDICATED-SPLIT"
  owner: "F2 duty F2-C10-PERDIR-FLAG (GAP-35 home)"
  note: "annex internal pointer: o33_bench:172-176; mesh-F3(ii). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT, enum MIXED (VERDICT_wave3 par.4): PANEL_REMMARCH par.4.4 WITH (R3REMMARCH-5 amendment): F-C10-1 names the REAL fields — the three single-direction refinements at r=2: NI (IVL rows) / da_deg (arc angle) / Nw (contour-exit stations) — o33_bench cfg fields of record (no Ne field exists); census closure line added: Fidkowski-Darmofal 2011 (on disk): output-based adaptation review — not consumed here by stated reason: C9 mesh law and C11 estimator are landed authority; the diagnostic-vs-production split is not touched by it (materiality LOW). Anchor corrections carried (R3REMMARCH-10): wanted_eca_hoekstra :852-853, delta anchor :850 -> :852-853. Masking event = pre-registered promotion (VERDICT_wave3 par.10). Note text of record = PANEL_REMMARCH par.4.4 AS REPAIRED by VERDICT_wave3 par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C11
  choice: "Error estimator for J"
  incumbent: "two-level Richardson K=4"
  alternatives:
    - "three-mesh observed-p GCI (Roache/Celik)"
    - "DWR (Becker-Rannacher)"
    - "LSQ multigrid"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.4 Row C11"
  owner: "F2 duty F2-C11-ESTIMATOR-CAMPAIGN, leg (a) ORDERED before leg (b) (was S25/GAP-9 + R25 DWR)"
  note: "annex status verbatim: 'single-author at band sites ([X-O32] exists for O3.2 norms only)'; annex internal pointer: AC3; GAP-9. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); litreview par.3.25/A36 re-scope the DWR bars near the sonic throat (adjudication-evidence-pending-ratification, CAVA NON RATIFICATA) — that touches the DWR alternative's validity DOMAIN, not the estimator choice at band sites; no panel/refuter comparison of estimators found. WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED, burden INVERTED honored (VERDICT_wave1.md par.2.4/4.4): DWR = target primary (characteristic-native; consumes HELD ancourt_peter_atinault_2023 + the HELD lozano_ponsin_2025 row (registry :122-128, read-partial, PDF on disk — rider corrected per the sustained supplement-refutation note: the row EXISTS, no new mint) + the space-time-DWR template line); Richardson/GCI = permanent referee; h^p-asymptotics premise RETIRED as epistemic premise (standing banded verdicts STAND, flag on findings row mesh-amr:observed-order-not-at-band-sites); interim bands = observed-p GCI via [X-O32]/GAP-9, K=4 in-loop only at measured p_obs >= 0.415 per site; LSQ = degraded-p checkpoint rule; D-49 lands in the F2 window in BOTH F11a branches; DWR-failure path closes C11 GCI/LSQ-primary (converged, not re-gated); C43 adjudicated-with-C11 (same window edit). SUPPLEMENT OF RECORD (axis B + VERDICT_C9C11_supplement.md par.4.2 FINAL): the DWR WEIGHT fork is CLOSED — weight of record = DISCRETE AD-adjoint of the marched scheme (custom_vjp, one-lowering per C48; exact at computed J, O3.1 [X-A1IM]; captured-smear trap bypassed on the fitted class, P2 Lemma B par.4.4); continuous/characteristic-native line (Ancourt 2023 + Lozano-Ponsin 2025) = formulation frame + REFEREE only (compatibility residuals; never the weight — a weight cannot referee itself); estimator form = two-level fine-space residual (Venditti-Darmofal class) on the [X-O32] ladder; same-level pairing signal-free at the certified Newton floor ([X-A1IM] per-cell z-space certification) so enrichment MANDATORY, and the enrichment operator must not smooth across characteristic-borne adjoint discontinuities. NEW PIN F11d, two legs (split per the sustained supplement-refutation amendment): leg 1 oracle convergence (AD weight -> analytic adjoint on the Giles-Pierce quasi-1D oracle at pre-registered O3.2/O3.3 order/norms; leg-1-only = oracle-only label; quasi-1D->axisym rides the declared OPEN R27 bridge); leg 2 band sites (ACE/Lozano-Ponsin Eqs. (30)/(31) compatibility residuals at production band sites, no analytic adjoint needed). EITHER leg firing blocks F11a promotion and re-opens the WEIGHT realization only (architecture stands: DWR-target + referee permanence). Dual consistency of the march = P2 Lemma B par.4.5 SCHEMA — F11d is its execution at estimator sites. F2-C11-ESTIMATOR-CAMPAIGN leg (b) anchor: Becker-Rannacher par.5.1 pp.40-41 = the published enrichment-operator menu with measured effectivities (global higher-order dual solve / patch-wise higher-order interpolation / difference-quotient weights), and Becker-Rannacher p.41's vanishing-estimator sentence is the published same-level-signal-free anchor (Galerkin-orthogonality mechanism there, certified-Newton-floor mechanism here — analog DECLARED, not identified); admissibility of each menu item still governed by the pinned no-smoothing-across-characteristic-discontinuities constraint (retro-sweep 2026-08-20). Estimator-form anchor: Venditti-Darmofal p.208 hyperbolic-prolongation warning (isodirectional interpolation oscillatory, alternative reconstruction needed) = class-source anchor, at the estimator-form source, for the pinned no-smoothing enrichment constraint (retro-sweep 2026-08-20)."

- id: C12
  choice: "Start line"
  incumbent: "Sauer first-order, gamma=const, GENO-mirrored (+1e-6 offset)"
  alternatives:
    - "Kliegel-Levine higher-order"
    - "Hall"
    - "displaced-IVL rejector"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C12: ADJUDICATED-PROTOCOL"
  owner: "F2 duty F2-C12-STARTLINE-REJECTOR; F4b/F5 higher-order scope unchanged (the mdot->Me common mode covered only at F4b/F5 if the band-term evaluation proves non-cheap)"
  note: "annex status verbatim: 'single-author (twin DATA CONTRACT - fidelity adjudicated, accuracy NEVER)'; annex internal pointer: a1:719-727, :37-40; GAP-6/AC14. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); litreview A7 registers the Hoffman-1987 LENGTH oracle whose throat model is Kliegel-Levine (pending ratification) — oracle-band context around the alternative, not an accuracy adjudication of the start line; accuracy axis still never adjudicated. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-PROTOCOL, enum MIXED (VERDICT_wave3 par.4): PANEL_REMMARCH par.4.5 WITH (R3REMMARCH-2 repair, adopted; option (a) preferred with (b) as declared floor): the composed band CARRIES a declared mdot-channel term — the Kliegel-Levine second-order mass-flow correction EVALUATED as a band term (formula evaluation feeding a bar; NOT an adoption of the higher-order start — F4b/F5 scope clause untouched). If the evaluation proves non-cheap at the duty window, the declared MINIMUM is the named exclusion: 'F-C12-1 certifies start-line invariance of the NET; the mdot->Me common mode is covered only at F4b/F5' — the certificate never silently over-claims either way. PLUS (R3REMMARCH-3): displaced start = the marched data on column +3 taken as the new IVL; the fan is rebuilt from it. PLUS (R3REMMARCH-11): 'census evidence recorded (Q3 2026-08-20)' replaces the census-ranking/presumptive-first-candidate wording (the F4b/F5 owner inherits evidence, not a verdict). F-C12-2 vacuousness control stands (it covers the net channel; the mdot term/exclusion covers the other — stated in the delta). Common-mode witness of record: a1:853-862 Simpson mdot on the Sauer IVL + :864-879 Me from leggeAree(mdot, eps). Note text of record = PANEL_REMMARCH par.4.5 AS REPAIRED by VERDICT_wave3 par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C13
  choice: "Axis unit process"
  incumbent: "GENO foot-ratio guard v1/y1"
  alternatives:
    - "L'Hopital dv/dy carried (Zucrow-Hoffman)"
    - "MMS verification"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C13: ADJUDICATED-PROTOCOL (single-author residue"
  owner: "F2 duty F2-C13-AXIS-ORACLE (gconst axis oracle home; + folded F2-C14-EXITBAND)"
  note: "annex status verbatim: 'single-author (GENO mirror; march-level verification NEVER)'; annex internal pointer: a1:488-499, :566; GAP-8. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block), no panel/refuter adjudication found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-PROTOCOL, enum MIXED (single-author residue DISCHARGED as for C6) (VERDICT_wave3 par.4): PANEL_REMMARCH par.4.6 WITH (R3REMMARCH-4 amendment) the F-C13-3 operand pinned: (dv/dy)_axis estimated by two-NI Richardson of v1/y1 (the same two-NI data F-C13-1 orders) or by the H-F19 one-term series coefficient fitted on recorded off-axis cells (v = a1·y + O(y³)) — either estimator declared before first use. Swirl obstruction stays NAMED-not-decided with correct homes. Any of F-C13-1/2/3 firing = pre-registered promotion path (VERDICT_wave3 par.10). Note text of record = PANEL_REMMARCH par.4.6 AS REPAIRED by VERDICT_wave3 par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C14
  choice: "Exit-condition read point"
  incumbent: "axis-cell Me only"
  alternatives:
    - "multi-point exit read + band"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C14: ADJUDICATED-CONVERGED"
  owner: "folded duty F2-C14-EXITBAND (rides F2-C13-AXIS-ORACLE; was the GAP-8 oracle fold)"
  note: "annex status verbatim: 'NEVER [J]'; annex internal pointer: a1:853, :889. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-CONVERGED, enum MIXED (VERDICT_wave3 par.4): multi-point exit read + band adopted; PANEL_REMMARCH par.4.7 WITH (R3REMMARCH-1 repair, adopted verbatim): the sentence claiming the exit-line row j2 cells are already computed is STRUCK (false premise — those cells carry the imposed uniform state qq, judge-verified at a1:1082-1085; vacuous-rejector class D-47/R5); F-C14-1 RE-ANCHORED: primary metric = max_j |M_j − Me_ach| over the FINAL MARCHED COLUMN (= the final ARC column at exit fire, the a1:958 interior sweep — NOT the :1079 mach-extension columns, whose j2 cell is the imposed state) (interior cells G[(j, i_exit)], j ∈ (Nv, j2], computed via solver_int), with the mass-flow closure residual (mdot1+mdot2 vs mdot, a1:1086-1106, already accumulated) reported as the free companion residual; threshold-derivation and the mass-flux-weighted fallback clauses carry over unchanged. Control-surface identity rejector consumed from findings :1588 unchanged. CR-W3-1 landing splice APPLIED in this text (confirm_repairs_wave3.md par.1.2: the :958 arc-column disambiguation the judge restatement had dropped). Note text of record = PANEL_REMMARCH par.4.7 AS REPAIRED by VERDICT_wave3 par.4 + CR-W3-1 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C15
  choice: "m_stop exit floor"
  incumbent: "1e-5 GENO-mirrored (S19 knob)"
  alternatives:
    - "mesh-coupled floor m_stop ~ C*da^2"
  status: DECIDED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C15 |"
  note: "annex status verbatim: 'CONVERGED-measured (attribution diagnostic executed; STIM-1)'; annex internal pointer: AC4; annex owner column: '- (residual: coupling documentary)' — a documentary residual remains, not a reopened choice"

- id: C16
  choice: "Newton damping ladder"
  incumbent: "(1,1/2,1/4,1/16,1/64,0), bare argmin non-increase"
  alternatives:
    - "Deuflhard measured-contraction damping"
    - "Armijo sufficient decrease"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C16: ADJUDICATED-SPLIT"
  owner: "F2 duty F2-C16-DAMPAB (engine rebuild window)"
  note: "annex status verbatim: 'NEVER (no recorded rationale)'; annex internal pointer: a1:371, :397-416; GAP-29. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the executed GAP-29 halved-constants sweep exercised C18's roundoff-floor constants, NOT the damping ladder; no adjudication of the ladder vs Deuflhard/Armijo found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT, enum MIXED (VERDICT_wave3 par.5): PANEL_REMENG par.4.1 WITH: (R3REMENG-1) both rationale sentences corrected — the certificate consumes the UNDAMPED NEWTON STEP norm vs NEWTON_TOL_FACTOR·EPS·sc (a1:433-448), already error-oriented; the challenger's edge is confined to the ACCEPT rule (argmin residual non-increase), which is what Deuflhard (3.40) indicts; (R3REMENG-2 repair, adopted) F-C16-3 DEMOTED to an instrument of F-C16-1/2: NMT-reject/argmin-accept events are LOGGED as activation instances and A/B evidence; they force the challenger only if correlated with cert failures or excess implicit solves above the derived threshold — the forces-challenger-regardless-of-cost overturn clause is struck; (R3REMENG-3) F2-C16-DAMPAB gains the explicit WIN criterion: the challenger build must PRESERVE the documented NaN-safety property of the trailing 0.0 candidate (a1:418-423: stalled cell keeps its iterate and fails certification honestly) — else not apples-to-apples; the silent-outcome characterization corrected to documented honest-fail path. Note text of record = PANEL_REMENG par.4.1 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C17
  choice: "Trip cap N_NEWTON"
  incumbent: "30"
  alternatives:
    - "derived from contraction budget"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C17 |"
  owner: "F2 (owner delta of record, VERDICT_wave3 par.3: was 'AUDIT F5 execution (S25)'; C43/C21 bookkeeping-alignment precedent)"
  note: "annex status verbatim: 'single-author (KAT proved it bites, not derived)'; annex internal pointer: [X-CDKAT]; a1:145-148. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C17-C18 sweep = GAP-29'): the GAP-29 halved-constants sweep targeted C18's constants (NEWTON_TOL_FACTOR, C_FLOOR, C_OPS); N_NEWTON=30 itself was NOT among the swept constants and remains un-derived. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); the sweep-exclusion fact above stands, no derivation or panel adjudication of N_NEWTON found. WAVE-3 VERIFICATION 2026-08-20 = CONFIRMED on convergence; owner delta STANDS (VERDICT_wave3 par.3 / VERIF_CONV par.2): owner 'AUDIT F5 execution (S25)' -> F2 (C43/C21 bookkeeping-alignment precedent); (R3CONV-2) support clause reworded — the sibling C18 family is already F2-owned at findings :221, same alignment class (N_NEWTON explicitly excluded from that family); (R3CONV-3) the note's guard-cap clause labeled (panel-proposed composition, not tree content). Rest verbatim per VERIF_CONV par.2 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C18
  choice: "Roundoff-floor constants"
  incumbent: "NEWTON_TOL_FACTOR=100, C_FLOOR=8 (spike convention)"
  alternatives:
    - "Higham op-count gamma_n"
    - "More-Wild noise floors"
  status: SINGLE-AUTHOR
  evidence: "docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED"
  owner: "S25 halved-constants sweep (executed; derivation duty now F2-live)"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C18 |; annex status verbatim: 'single-author (AUDIT F5 registered; test never executed)'; annex internal pointer: AC9; GAP-29. POST-ANNEX DELTA (S25-bis, findings_registry.yaml:engine-core:F5-underived-factors, s25bis_gap29_sweep.json): GAP-29 sweep EXECUTED — 1 FLIP of record: NEWTON_TOL_FACTOR/2 flips cert_verdict (factor-2 margin load-bearing); C_FLOOR/2 and C_OPS/2 no-flip (>=2x measured headroom). Sweep executed, derivation itself still NEVER done -> status stays SINGLE-AUTHOR. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); the executed sweep is measurement evidence on the incumbent (1 flip of record), not a Higham/More-Wild comparison; derivation duty F2-live unchanged. WAVE-2 NOTE 2026-08-19 (VERDICT_wave2 par.4.11, named-not-adjudicated): C20's Tier-1 shares this row's derivation window — the kappa-qualified band and the NEWTON_TOL_FACTOR derivation are one question (GAP-29 factor-2 cliff). WAVE-3 VERIFICATION 2026-08-20 = CONFIRMED-CONSISTENT + note-delta verbatim (VERDICT_wave3 par.3 / VERIF_CONV par.3): More-Wild blindly re-derived at O-F25(c); GAP-29 anchors verified; C20 Tier-1 seam consistent at all three anchors (authority chain; no duplication here by anti-entropy calibration 2026-08-20). Anchor-drift bookkeeping (VERDICT_blocco2 LB-13, 2026-08-20): row C18 sits at docs/choice_ledger.yaml:324-333 as of 2026-08-20 (VERDICT_wave2's :315-324 cite drifted; cite by id)."

- id: C19
  choice: "Certification metric scale"
  incumbent: "scalar sc = max(1, max|z|)"
  alternatives:
    - "componentwise scaling (Deuflhard NLEQ; Higham)"
  status: DECIDED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C19: ADJUDICATED-ON-COST"
  owner: "F2-C19-SCALECENSUS = the engine-core:F8-cert-scale-unit-mixing findings-row owner window (no new row)"
  note: "annex status verbatim: 'single-author (AUDIT F8 adjudicated LOW)'; annex internal pointer: AC8/Q1. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block), no panel/refuter adjudication of scalar-vs-componentwise scaling found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-ON-COST (retain scalar), enum DECIDED (VERDICT_wave3 par.5; gated:false — the census duty homes into the F8 findings row): PANEL_REMENG par.4.2 WITH (R3REMENG-4 repair, adopted verbatim): the steelman-against sentence REPLACED by the true record — component-scale disparity of record is ~3 decades (engine-core:F8-cert-scale-unit-mixing: position components certified only to ~100 eps|u|; no verdict compromised, contour bands Richardson-dominated); F-C19-1 REPLACED by the VERDICT-LAXITY census: per certification point, laxity_i = sc/max(1,|z_i|) per component, compared against the margin by which Richardson-dominated bands absorb it (threshold derived from the GAP-29 factor-2 headroom on the laxity-vs-band-dominance axis, NOT raw disparity); the duty homes into the F8 findings row (F2-C19-SCALECENSUS = that row's owner window; no new row). Outcome (retain on cost) survives on the F8 row's own no-verdict-compromised; the laxity census showing band dominance broken re-opens the row with burden inverted (VERDICT_wave3 par.10). Note text of record = PANEL_REMENG par.4.2 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C20
  choice: "Certificate qualification"
  incumbent: "unqualified one-extra-step ratio"
  alternatives:
    - "kappa(J)-aware derived bound"
    - "Kantorovich/alpha-theory"
    - "branch-consistency monitor"
    - "interval-Newton [X-IVXC]"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.9 C20"
  owner: "F2 (HIGH); duty F2-C20-CERTQUAL-CAMPAIGN (Tier-0 shared with C21; Tier-1 shared with the C18 window; M0 back-propagation R4)"
  note: "annex internal pointer: GAP-3. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.9/4.9): tiered qualification architecture of record — Tier-0 branch/fold monitors always-on + Tier-1 kappa-derived band composed with the retained incumbent metric + Tier-2 flagged-cell Kantorovich/Krawczyk referee + deflation diagnostic; solvability-region CLOSED-as-mechanism (O-F20 counted ONCE, shared-source declaration vs C28); P4 absolute, floor unmoved; four pinned falsifiers; dependencies C20 -> C28, C56/C11-estimator; C18/C19 interplay named-not-decided. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C21
  choice: "Seed validity policy"
  incumbent: "asserted O(h) predictor + stale replay seeds (C2-F2 mitigation)"
  alternatives:
    - "alpha-theory per-seed certificate"
    - "Kantorovich radius ([X-TBAK] pattern in z)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.10 C21"
  owner: "F2 duty F2-C21-SEEDCERT-CAMPAIGN (owner alignment S25 -> F2, C43 precedent)"
  note: "annex internal pointer: GAP-20. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the S25-bis in-trace traced-predictor seeds (predict_interior_t, declared ulp divergence) shipped GATE-verified under m5cgate — an implementation delta on the incumbent, not an alpha-theory/Kantorovich alternatives adjudication. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.10/4.10): incumbent seeds RETAINED as base layer; derived Kantorovich radius in z via the [X-TBAK] two-point pattern (new K_RICH role NOTIFIED to C42); V-F7 fold-margin monitor transferred with declared native context; replay-ball coverage falsifier with fresh re-record fallback; the asserted O(h) predictor RETIRED to measured-or-deleted; quiet-band derivation published before first use; F9b scope boundary per the landed C9. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C22
  choice: "Wall-foot search"
  incumbent: "linear index scan, full Newton per attempt, monotone-foot predicate"
  alternatives:
    - "Brent on chord parameter + Shewchuk pre-filter -> derived (N,Nv)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C22: ADJUDICATED-SPLIT (burden on the predicate)"
  owner: "F2 duty F2-C22-FOOTPROBE (rate lever; F-C22-3 hands the rate lever to C37's band — coupling declared both sides)"
  note: "annex internal pointer: GAP-28; toc:847-852. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the S25-bis M5c executor compiled the incumbent scan (gate-verified) without weighing the Brent/Shewchuk alternative; no adjudication found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT (burden on the predicate), enum MIXED (VERDICT_wave3 par.5): PANEL_REMENG par.4.3 WITH (R3REMENG-5 repair, adopted): F-C22-1 probe family BOUNDED to the admissible class (knot spacing >= the refinement floor of record; the [X-AKNO] instance family): IN-CLASS disagreement = predicate falsified, challenger becomes certificate-bearing reference; OUT-OF-CLASS disagreement = evidence routed to C10/C5 (REMMARCH), not a predicate verdict; anchor fix: the 'bitwise' wording is the GAP-28 findings DELTA text, the ledger note says 'compiled...gate-verified'. Cross-slot (VERDICT_wave3 par.7 item 4): churn axis C22 rate-at-source vs C37 materiality-band — coupling declared on both sides, no double-adjudication. Note text of record = PANEL_REMENG par.4.3 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C23
  choice: "Record-failure policy"
  incumbent: "full record then P4 gate"
  alternatives:
    - "fail-fast early-abort (opt-in, schema'd)"
  status: DECIDED
  evidence: "docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C23 |; annex status verbatim: 'CONVERGED-panel (speed audit H1 design of record)'; annex internal pointer: AC7; annex owner column: 'scheduled S25 item 8'. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C23 = H1/M5b atterrata'): the fail-fast/H1 record-failure design landed in the M-chain speed program (M5b)."

- id: C24
  choice: "Thermo closure"
  incumbent: "[X-THC1] quintic C1 tables (backend-1)"
  alternatives:
    - "NASA-direct (oracle only per C-A)"
    - "dCp-ingest (gated C-B)"
  status: DECIDED
  evidence: "docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C24 |; annex status verbatim: 'CONVERGED-panel (KEEP verdict fresh)'; annex internal pointer: ADVISORY_S24_thermo_closure_survey Sec.8; annex owner column: 'conditions C-A..C-D open as named'. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C24 = C-A scaricata'; findings_registry.yaml:thermotab:C-A-edge-stencil, status DISCHARGED): condition C-A (NASA-direct oracle) discharged in the [X-THC1] carrier, same M3 touch; C-B/C-C/C-D remain open, owner F2. WAVE-3 VERIFICATION 2026-08-20 = CONFIRMED-CONSISTENT + cross-ref note-delta verbatim (VERDICT_wave3 par.3 / VERIF_CONV par.4); dedup verdict CONFIRMED (diff par.2.6/par.2.7 already homed at findings :1615/:1624, nothing to mint) (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C25
  choice: "Table box"
  incumbent: "literals 1050/3900 K, N_TAB=8192"
  alternatives:
    - "envelope-derived box + declared margin + per-run certificate"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C25 |"
  owner: "F2 (with C-C/C-D)"
  note: "annex internal pointer: GAP-11. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); partial context found, NOT a comparison: thermo survey refuter attack A4 ('quintic + 8192-point table is over-engineering') judge-REJECTED and condition C-C makes no-joint-in-window a declared per-certificate hypothesis (ADVISORY_S24_thermo_closure_survey_2026-08-12.md par.7-8) — neither weighs the envelope-derived box alternative; the literals remain underived. WAVE-3 VERIFICATION 2026-08-20 = CONFIRMED-CONSISTENT + note-delta verbatim (VERDICT_wave3 par.3 / VERIF_CONV par.5), with the 1/4-tree core-of-alternative support scope recorded and anti-inflation noted exemplary; cross-slot consumption of record (VERDICT_wave3 par.7 item 6): the H-F6 reachable-set box is C25's object — C45's derived scan window and C26's edge margin CONSUME it by name, neither derives it (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C26
  choice: "Out-of-box behavior"
  incumbent: "silent clamp (linear) / silent quintic extrapolation (C1)"
  alternatives:
    - "traced flag + rejector covering BOTH modes"
  status: DECIDED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C26: ADJUDICATED (traced box-margin flag"
  owner: "C-D owner F2 (the standing C-D repair; the edge-band derivation lands in the same C-D repair window)"
  note: "annex status verbatim: 'NEVER (registered conditional C-D, spec corrected)'; annex internal pointer: AC6. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); thermo survey condition C-D re-affirms the traced box-exit rejector as required repair with owner F2 (ADVISORY_S24_thermo_closure_survey_2026-08-12.md par.8 C-D) — a repair mandate against the incumbent silence, not a comparison run; still unlanded. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED (traced box-margin flag + rejector, cert_worst pattern), enum DECIDED (VERDICT_wave3 par.6; gated:false — execution = the standing C-D repair window): PANEL_REMPOL par.4.2 WITH: (R3REMPOL-4 repair, adopted) F-C26-1 verdict threshold RE-DERIVED FOR THE EDGE: margin < 0 + the derived probe-excursion allowance (FD probes at P±h are the named near-edge consumers, findings :581 — anchor in its CR-W3-2(i) corrected form), OR an explicit one-sided edge mollification argument with its own falsifier — the interior-joint ~4ΔT band is NOT transplanted (it was derived for piece boundaries, S24 survey :250-252); the edge derivation lands in the same C-D repair window; (R3REMPOL-3) the incumbent premise sentence REPLACED: the box-exit mode is NOT hypothetical — CONFIRMED LIVE S21 of record (KAT-B first range left the box, lam_ad −0.216 vs −1.185 at M~4.7, findings :576); materiality = already-happened-once of record; (R3REMPOL-5) (i) the margin scalar IS the detector (the index clip at i = n−2 is also legitimate in-box — the clip-event equivalence is dropped), (ii) the valve's sufficiency hypothesis carries its one-line check (all table reads route through ht/T: state_q :403-405; invert_h :308), (iii) declared: pre-registered criterion (d) (demonstrably fires) is discharged AT BUILD TIME by F-C26-2 (acceptance rejector). Checkify closure (stated reason verified), F-C26-2/3, duty (the standing C-D repair): unchanged; the edge margin CONSUMES the C25 H-F6 box by name (VERDICT_wave3 par.7 item 6). Note text of record = PANEL_REMPOL par.4.2 AS REPAIRED by VERDICT_wave3 par.6 + CR-W3-2(i) (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C27
  choice: "Constraint aggregation"
  incumbent: "KS-min, rho = K_RICH ln(N)/mu0 (conservativeness-derived)"
  alternatives:
    - "adaptive-KS (Poon-Martins)"
    - "induced-exp/power (Kennedy-Hicken)"
    - "two-constant rule"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.2 Row C27"
  owner: "F2 (with GAP-1); duty F2-DUTY-C27-AGGCOND (subsumes the gapmap R3 pointer; window = findings row margin-governor:G1-three-jump-channels)"
  note: "annex status verbatim: 'single-author (survey answered accuracy axis; conditioning axis NEVER)'; annex internal pointer: GAP-27; AC12. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); DE-bucket panel independently re-verified the ladder/rho arithmetic at the S24 instance (ADVISORY_S24_DEbucket_panel_2026-08-12.md M3, E3+judge) and pipeline-sense R4 classes GAP-27 squarely on-axis — instance verification + classification, not a comparison vs adaptive-KS/induced aggregates. WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED (structure/protocol converged; conditioning axis = measured F2 duty open) (VERDICT_wave1.md par.2.2/4.2): converged half = two-constant rule (derivation repair + ex-ante trigger, arithmetic on derived constants; preprint theorem predicts conflict at depth) + vector near-binding band block via margin_factory as the DERIVED escalation route (panel-designed; tree advocacy 2/4 + diff divergence 3/4) + instance-covering over xi with mandatory post-hoc fine-phase sweep certificate semantics + per-lane multiplier atoms as (ii) carriers under the B-stationarity qualifier. Incumbent RETAINED as fence + G1 surrogate + sole operator where the two-constant check passes; theorems and adopt-or-declare untouched. Alternatives closed by stated reason incl. grouped/regional aggregation (per-group fencing = optional refinement). Protocol arity/cadence/band-width pins + completed falsifier partition land as repaired per the verdict's sustained list. Dependency of record: the C28 KS-max inherits the two-constant rule."

- id: C28
  choice: "Cert-frontier representation"
  incumbent: "binary outside gates only (quantifier bridge falsified)"
  alternatives:
    - "KS-max surrogate inside KKT, gates kept"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.1 Row C28"
  owner: "F2 (census row 10); duties [P-CERTKS]+[P-BSTAT] in the GAP-1/GAP-2 window, [P-IPADJ] same critical path"
  note: "annex status verbatim: 'NEVER (successor to falsified K_disc~A_0 absent)'; annex internal pointer: GAP-1. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); pipeline-sense converged R1 re-states of record that no successor quantifier exists after the falsified K_disc~A_0 bridge (ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md par.3.1). WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED (representation converged; adoption-of-record gated on F2 measured half) (VERDICT_wave1.md par.2.1/4.1, prior evidence pointer superseded there): hybrid priced-frontier representation = the M0 tier-ladder KKT-with-margin-multiplier of record + GAP-1 KS-max on traced per-cell certification ratios (derived rho, stratum-local, margin_factory slot; realized exclusion band <= 2 ln(N)/rho reported by the duty); P4/P3(ii) gates kept ABSOLUTE; incumbent binary-only SUPERSEDED as sole representation; solvability regions re-scoped to physical margins (wave-2 territory). The 'no successor' gloss of this row is RETIRED: pipeline-sense R1's full sentence names 'GAP-1's in-KKT certification surrogate' among the named exits (ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md:191-199) — the prior 'adjudicates no alternative representation' reading was imprecise against its own source. Protocol + reporting pins land as repaired per the verdict's sustained-amendment list; Probe B evidence = scratchpad-run toy reproduced by both S24 verifiers, numbers in the committed advisory (NOT a committed carrier). WAVE-2 RIDER (par.4.13, PANEL_C2021 par.3.4): the priced ratio field and the P4 gate consume the SAME unqualified ratio — the wrong-branch mode evades BOTH; the first C28-panel protocol pin wrong-branch discrimination WAITS on C20 Tier-0; [P-CERTKS] may pilot on the unqualified ratio (conditioning side covered by gate disagreement)."

- id: C29
  choice: "Mask freezing granularity"
  incumbent: "rung-frozen + C3 drift gate"
  alternatives:
    - "segment-level re-freeze (P2 rhythm)"
    - "smooth gate sigma(val/eps_gate)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C29: ADJUDICATED-SPLIT (decision rule converged"
  owner: "F2 duty F2-DUTY-C29-MASKGRAIN (re-adjudication executed wave-3; smooth gate = escalation branch only)"
  note: "annex status verbatim: 'CONVERGED-panel for rung-freeze, alternatives NEVER considered; C3 fired post-adjudication'; annex internal pointer: GAP-13; panel C3. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT (decision rule converged; default = segment-level re-freeze; smooth gate = escalation branch only), enum MIXED (VERDICT_wave3 par.6): PANEL_REMPOL par.4.3 WITH (R3REMPOL-6): F-C29-2 gains the cost carrier — measured cost report at the replay: N_seg × val_diag rebuild cost vs the recorded decisive-run-2-class repeat cost — the acceptance is priced, not asserted. F-C29-1/3/4 (incl. eps_gate-derived entry condition and C3 detector permanence), duty F2-DUTY-C29-MASKGRAIN, C28 gates-absolute consumption, O-F19 single-count declaration: unchanged. A jumpy [R1] outcome overturns the default BY DESIGN (smooth gate arms; GPB04 procurement conditional, VERDICT_wave3 par.9 item 7). Sequencing of record (par.7 item 5): C30's one-liner lands FIRST and instruments C29's [R1] replay for free. Note text of record = PANEL_REMPOL par.4.3 AS REPAIRED by VERDICT_wave3 par.6 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C30
  choice: "Mask crop policy"
  incumbent: "overlap crop + occurrence counter"
  alternatives:
    - "conservation-of-enforcement invariant (lost-lane count verdict-bearing)"
  status: DECIDED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C30: ADJUDICATED (conservation-of-enforcement invariant"
  owner: "one-liner build (was S25 one-liner); sequencing pinned: C30 lands FIRST and instruments C29's [R1] replay for free"
  note: "annex internal pointer: GAP-14. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED (conservation-of-enforcement invariant, one-liner), enum DECIDED (VERDICT_wave3 par.6; gated:false): PANEL_REMPOL par.4.4 WITH (R3REMPOL-8): F-C30-2 plant must GUARANTEE at least one True lane beyond the overlap (a shape change whose True lanes all sit inside the overlap yields lost_lanes = 0 HONESTLY and must not count as refutation); logging adopts the EXISTING per-margin_W-call rhythm under mismatch (:679) by name — no second event semantics minted. Sequencing note (C30 one-liner first — it instruments C29's replay for free) stands. Measured overhead on an int subtraction = nil, declared closed (VERDICT_wave3 par.10). Note text of record = PANEL_REMPOL par.4.4 AS REPAIRED by VERDICT_wave3 par.6 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C31
  choice: "Optimizer engine"
  incumbent: "scipy trust-constr (eq-path Sec.4bis-adjudicated; IP path in use since S22)"
  alternatives:
    - "IPOPT"
    - "filter SQP (Fletcher-Leyffer)"
    - "SLQP"
    - "Uno"
    - "proximal-bundle (D6:738, never built)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.1 C31"
  owner: "F2 [P-IPADJ]; adds F2-C31-ENGINE-AB"
  note: "annex status verbatim: 'CONVERGED for eq-path; IP path NEVER'; annex internal pointer: GAP-15; kickoff Sec.4bis. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.1/4.1, panel PANEL_C31TRIO par.4.1 as repaired): engine of record stays scipy trust-constr interior-point as-is under INFORMATION-ONLY multiplier discipline until [P-IPADJ] (first F2 engine act on C28's critical path); flip candidate = Uno (install = O5-class decision); A/B pinned matched-constraint-set with identical-certified-outcomes guard; engine falsifiers one/two/three as defined in VERDICT_wave2 par.1.1; confirm-lens finding 1 ORCHESTRATOR ADJUDICATION OF RECORD (strict option, confirm_repairs_wave2.md): falsifier-two certification is CONDITIONED on the falsifier-one semantics leg PASSING — A/B dominance discharges ONLY the overhead leg, never semantics; incumbent case completed with the V-F30 pro-IP multiplier clause + the 2023-2026 IP warm-start census line as [P-IPADJ] input; gradient-role rider consumed per par.2.10. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19). [P-IPADJ] BHN equation-level anchors now on disk: barrier update law = (mu, eps_mu) reduced by constant factor theta (p.879); 'optimality' source semantics = E(x,s;mu) Eq. (2.3); multiplier semantics = subordinate least-squares estimates Eq. (3.15) ('just barely ... primal-dual', p.884) — consume at equation level, not from memory (retro-sweep 2026-08-20). Arm-B wording rider: arm B = Uno filtersqp preset (published par.5.2: presets = filtersqp/ipopt only); any funnel-globalization configuration is a DECLARED options-selected variant, not a preset (retro-sweep 2026-08-20). [P-IPADJ] SPEC ENTRY CONTRACT (user order 2026-08-20, C4 injection (6)(e)): the 10 arm-B spec inputs of validation/sfoundations_raws_2026-08-13/blocco3/DOSSIER_uno_fullread.md section 3 ENTER the [P-IPADJ] spec (verbatim-consumed at that window; the dossier INFORMS the spec, adjudication stays at the [P-IPADJ] window per its own falsifiers)."

- id: C32
  choice: "Curvature policy"
  incumbent: "fresh full FD Hessian per segment, no carry (policy-bound)"
  alternatives:
    - "SR1 carry from certified pairs + stale-symptom re-measure + directional rejector"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.2 C32"
  owner: "G0/T2 review; F2 entry gains [P-QNCARRY] + [P-HESSREJ] (unconditional)"
  note: "annex status verbatim: 'single-author (R-3 cites no alternatives; survey NEVER)'; annex internal pointer: AC10. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); speed audit S1 declines quasi-Newton reuse 'OUT by policy' (defers to the same single-author R-3 policy, no fresh survey) and N5 keeps the exact-Hessian route adjudication-only (ADVISORY_engine_speed_audit_2026-08-12.md par.7.3 S1 + par.4 N5); the SR1-carry alternative was never panel-weighed. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.2/4.2): fresh-FD = interim policy of record; SR1-carry = pinned challenger via [P-QNCARRY] with the arm-B same-lowering pair pin applied AS QUOTED in VERDICT_wave2 par.1.1 IN FULL, incl. the carried-matrix-crosses-segment-boundaries-only-as-a-prior (facet G4) clause (confirm-lens finding 2 landing hygiene); the promotion package NAMES the DIR-RKG scope/falsifier amendment as a landing item; two-instance promotion criterion declared; HVP truncated-Newton = second challenger, N5-gated; chart-migration events = re-record-class boundaries for the carry; [P-HESSREJ] unconditional. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C33
  choice: "Constraint curvature"
  incumbent: "scipy default BFGS (no hess=)"
  alternatives:
    - "exact HVP (fwd-over-rev)"
    - "FD-of-exact-gradient per segment"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.3 C33"
  owner: "F2 duty F2-C33-CONSTRHESS (rides the R2 pilot flag + GAP-1 window)"
  note: "annex internal pointer: GAP-19. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); adjacent facts found, not an adjudication of THIS choice: speed audit S1/N5 declined HVP-as-default for the OBJECTIVE Hessian, and S25-bis diff convergence B-F11 source-verified the route facts (solve is custom_vjp only so jacfwd is blocked as written; central differences non-route) (ADVISORY_engine_speed_audit_2026-08-12.md par.7.3; ADVISORY_S25bis_diff_convergence_2026-08-12.md B-F11) — the constraint-curvature default-BFGS choice itself was never weighed. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.3/4.3): constraint curvature measured at segment base, frozen within segment (R-3 policy extended); default arm = FD-of-constraint-gradient; protocol arm = KS-structured panel-derived arithmetic (judge-re-derived exact); identity-seeded BFGS SUPERSEDED as policy (de-facto until the activity trigger); per-segment budget derivation published before first use; incumbent comeback branch kept. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C34
  choice: "TR floor/caps"
  incumbent: "TR_FLOOR=1e-3, tr0=0.05, tr_cap=0.25 (S18 clip)"
  alternatives:
    - "noise-derived floor K_RICH*tol_dp/||g|| (Cartis-Scheinberg; More-Wild)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#1.1 C34"
  owner: "F2 duty F2-C34-TRFLOOR-DERIVE (carries the R3FAM-3 clause: an engine swap at [P-IPADJ] re-prices the rho-histogram accept/reject semantics — re-check at that landing)"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C34 |; annex internal pointer: GAP-4. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C34 [P-TRFLOOR] = blocked-with-named-cause'): input forfeited by the pre-8761dce kill; trigger = first campaign artifact post-8761dce. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); [P-TRFLOOR] remains blocked-with-named-cause (forfeited inputs), no adjudication of the noise-derived floor found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT, enum MIXED (VERDICT_wave3 par.1.1): panel outcome STANDS with 2 adopted repairs/amendments — (R3FAM-1 repair) the immateriality branch reads: immateriality branch: no accepted step within M× of either literal, with M = 10 DECLARED sufficient-not-optimized (direction argument: larger M makes immateriality HARDER to claim = conservative; M = 10 > K_RICH = 4, the registered safety constant) — or M derived from the measured rho-histogram support width at duty time, superseding the declaration; (R3FAM-2 amendment) the protocol pin declares the embedded safety: NOTE: tol_dp is itself K_RICH-banded at source (a1_toc:2224), so TR_FLOOR_derived = K_RICH × tol_dp/||g||_seg carries K_RICH² ≈ 16× on the raw two-point spread — DECLARED as the intended compounded safety; the duty window may substitute the raw spread with a single declared K. This composition instance is NOTIFIED to C41 (a P-tag applies at composition time) and to the C42 role census; (R3FAM-3, adopted into duty text) the [P-IPADJ] re-check clause now in the owner field. Falsifier, duty, dependencies, what-would-overturn: as PANEL_FAM par.4.1. Note text of record = PANEL_FAM par.4.1 AS REPAIRED by VERDICT_wave3 par.1.1 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C35
  choice: "xtol"
  incumbent: "literal 1e-10 at every call site"
  alternatives:
    - "Newton-floor-derived on u scale (derivation exists as prose only)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#1.2 C35"
  owner: "F2 duty F2-C35-XTOL-DERIVE (arithmetic rides the findings :633-641 owner window, unchanged)"
  note: "annex internal pointer: GAP-33. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found (the S18 driver finding 'xtol with KKT open = stale-model collapse', PROGRESS archive, is a symptom record, not a derived-xtol comparison). WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT, enum MIXED (VERDICT_wave3 par.1.2): outcome STANDS; the pinned formula is REPLACED by the adopted repair (R3FAM-4, verbatim): xtol_u = floor_W/(K_RICH × max(Dv)): floor_W (the C20-window Newton floor) propagated component-wise through the driver's actual W = Dv∘u map, EXTREMAL (max-Dv) direction, safety factor DIVIDING (premature-stop is the declared material risk). Note of record: the driver normalizes Dv by its own median at construction (a1_toc:1741), so any median-based form is vacuous. Arithmetic executes in the existing findings-:640 owner window (unchanged). (R3FAM-5 amendment) the formula carries the label PANEL-DERIVED arithmetic (C33 precedent, VERDICT_wave2 par.2.3) — tree convergence is on the derivation DUTY (O-F25(e)/H-F40 gradient-side forms), not on this step-scale formula. Falsifier (negative control), duty (rides findings :633-641 owner window), dependencies (C20/C18 seam; C36 Dv-map link), and the FOUND-prose-refutes-label finding: as PANEL_FAM par.4.2 (all stand). Note text of record = PANEL_FAM par.4.2 AS REPAIRED by VERDICT_wave3 par.1.2 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C36
  choice: "Scaling policy"
  incumbent: "Jacobi Dv measured once/walk, objective-only"
  alternatives:
    - "per-segment refresh from diag(H) (free) + constraint-row equilibration (SNOPT)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#1.3 C36"
  owner: "F2 duty F2-C36-DVREFRESH ([P-DVREFRESH] home)"
  note: "annex internal pointer: GAP-17. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the speed audit's DEAD row killed the Jacobi-precond REMOVAL lever of record (ADVISORY_engine_speed_audit_2026-08-12.md par.4 DEAD) — an adjacent keep-vs-drop verdict, not an adjudication of the refresh/equilibration alternatives; [P-DVREFRESH] unchanged. WAVE-3 ADJUDICATION 2026-08-20 = CONVERGED-panel, enum MIXED (measured half = named F2 duty) (VERDICT_wave3 par.1.3): outcome STANDS; falsifier re-pinned (R3FAM-7 amendment): F-C36 falsifier comparison object PINNED — refreshed-vs-frozen twin on the S18 record case compares (a) certificate validity AT EACH WALK'S OWN endpoint, (b) replay-fidelity/determinism bit-check (findings :1261 rides), (c) same-base same-seed segment-level comparison for the scaling-sensitivity claim. A genuine REJECT = band violation or determinism break — NOT endpoint divergence (path-dependence of a nonconvex driver is not a refresh defect). H^s option-with-stated-precondition, duty F2-C36-DVREFRESH, C32 re-price clause: as PANEL_FAM par.4.3. (R3FAM-15 census hygiene: the SNOPT scaling identity RE-ANCHORED to findings :1261, source marked [TITLE].) Note text of record = PANEL_FAM par.4.3 AS REPAIRED by VERDICT_wave3 par.1.3 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C37
  choice: "Segmentation trigger"
  incumbent: "any (N,Nv) flip ends segment"
  alternatives:
    - "materiality-gated by replay-fidelity band"
    - "PC^1 active-set anticipation"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C37: ADJUDICATED-DEFAULT+GATED-LEVER"
  owner: "F2 duty F2-C37-FLIPMAT (F2 entry, G0/T2)"
  note: "annex internal pointer: GAP-18. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); speed audit N6 flags the benign-flip segment merge as touching constraint 7 and 'needs its own panel. Flagged, never traded' (ADVISORY_engine_speed_audit_2026-08-12.md par.4 N6) — an explicit refusal to adjudicate without a dedicated panel; alternatives never run. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-DEFAULT+GATED-LEVER (the N6 right-sized panel instance, executed), enum MIXED (VERDICT_wave3 par.5): incumbent stays default; materiality-gated merge = gated lever. PANEL_REMENG par.4.4 WITH (R3REMENG-6 repair, adopted verbatim): F-C37-1 gains a MERGE-FREE benign proxy, declared sufficient-not-optimized — benign = flips whose certified-class (S,xi) labels are unchanged across the boundary under the adopted O-F19/H-F11 loci logging, and/or flips whose discarded-Hessian columns are lip-only (the findings-magnitude case of record); F-C37-2 licenses the merge on the proxy-flagged subset ONLY; a proxy-vs-F-C37-2 disagreement on that subset RETIRES the proxy. The census is thereby executable merge-free; the F-C37-1/2 dependency cycle is broken. F-C37-3 composition rejector and the [P-QNCARRY] arm-B pin stand; F-C37-2 violation kills the lever (VERDICT_wave3 par.10). Note text of record = PANEL_REMENG par.4.4 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C38
  choice: "Outcome-II stationarity declaration"
  incumbent: "ratchet exhaustion + raw KKT-open number"
  alternatives:
    - "B-stationarity certificate (convex-hull / surrogate residual)"
    - "normalized reporting"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C38 |"
  owner: "F2"
  note: "annex internal pointer: GAP-2. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); pipeline-sense R4 partition classes GAP-2 'squarely on-axis' (ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md par.3.4) — a classification of the gap, not an adjudication of the B-stationarity-certificate alternative. WAVE-3 VERIFICATION 2026-08-20 = CONFIRMED-CONSISTENT + consumption-notice note-delta (VERDICT_wave3 par.3 / VERIF_CONV par.6), R3CONV-7 repair adopted: (a) the Q10 protocol row corrected to the measured census 8 hits / 4 registry files (ledger :421/:534/:539; claims :1350/:1874; findings :1136/:1947; glossary :1126 — judge re-measured 2026-08-20); (b) the consumption notice extended: qualifier semantics already live pre-wave-1 in committed claims (claims_registry :1350 S22 corner-metric corollary, :1874 S24 F1b twin, both 'until O1') — the declaration policy must compose with these carriers too, not only with [P-BSTAT]/[P-CERTKS]. Landing priority note (prevents a future re-mint) stands (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C39
  choice: "Multiplier provenance"
  incumbent: "res.v raw (convention closed S24-T1)"
  alternatives:
    - "complementarity band + LSQ re-estimate"
    - "AD post-optimality (Fiacco) via lip elimination"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C39: verification half INSTRUMENTS-CONVERGED"
  owner: "F2 duty F2-C39-MULTVER (instruments 1-2 land WITH [P-IPADJ]; instrument 3 LSQ referee licensed NOW on recorded artifacts)"
  note: "annex status verbatim: 'convention CONVERGED (T1); verification NEVER'; annex internal pointer: GAP-12, GAP-32. WAVE-3 ADJUDICATION 2026-08-20 = verification half INSTRUMENTS-CONVERGED, enum MIXED (VERDICT_wave3 par.5): PANEL_REMENG par.4.5 WITH: (R3REMENG-7, branch (a) adopted) ordering pin reworded — instruments 1-2 land WITH [P-IPADJ]; instrument 3 (LSQ referee) is licensed NOW on recorded artifacts (offline, solver-independent — the panel's own census point); F-C39-3 readings pre-[P-IPADJ] stand as demotions-until-explained, exactly as pinned; (R3REMENG-8) two preconditions NAMED: F-C39-2 has an EMPTY instance set today (margin inactive at every recorded instance — constraints:margin-constraint-no-hess); it becomes well-defined at the first margin-active verdict (= the GAP-12 trigger); and lambda_e coverage: instrument 1 is structurally vacuous on equalities and instrument 2 needs activity — lambda_e verification rests on instrument 3 ALONE, so the C40 conditional is materially likely to fire (stated honestly). C40 fold into F2-C39-MULTVER stands; multiplier discipline consumed identically with wave-2 C31 INFORMATION-ONLY constraint (VERDICT_wave3 par.7 item 7). Note text of record = PANEL_REMENG par.4.5 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C40
  choice: "Lip equality handling"
  incumbent: "LinearConstraint row"
  alternatives:
    - "variable elimination (N-W Sec.15.3)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C40: ADJUDICATED-CONDITIONAL"
  owner: "folded into F2-C39-MULTVER (VERDICT_wave3 par.5)"
  note: "annex internal pointer: GAP-32. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-CONDITIONAL, enum MIXED (VERDICT_wave3 par.5): PANEL_REMENG par.4.6 WITH (R3REMENG-9): F-C40-1 — disagreement with the eliminated twin's O3.1-style post-optimality check row PASSING = elimination WINS (provenance defect demonstrated); disagreement with the check FAILING = INVESTIGATE, no winner declared. Fold into F2-C39-MULTVER stands; the conditional is materially likely to fire given the lambda_e coverage honesty on C39 (instrument 3 alone). Note text of record = PANEL_REMENG par.4.6 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C41
  choice: "Band form/composition"
  incumbent: "K=4 pointwise two-level; ad-hoc sum/max/heterogeneous mixes"
  alternatives:
    - "declared GUM/RSS policy"
    - "three-level GCI"
    - "topology-conditioned (LB-c2)"
    - "running-max envelope (in-house)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#1.4 C41"
  owner: "F2 duty F2-C41-BAND-POLICY"
  note: "annex status verbatim: 'NEVER (no composition policy anywhere)'; annex internal pointer: GAP-7, GAP-9, GAP-10. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no composition-policy adjudication found. WAVE-3 ADJUDICATION 2026-08-20 = CONVERGED-panel, enum MIXED (VERDICT_wave3 par.1.4): P1-P4 policy STANDS with P1 tightened and P3 carrying its derived bound. (R3FAM-9 repair) P3 reads: max PERMITTED only with a recorded dominance certificate r = top/runner-up ≥ K_RICH, CARRYING its derived coverage bound K·r/(r+1) and the accepted erosion DECLARED (at the threshold r = 4: coverage 3.2× vs intended 4× = 20% erosion, sufficient-not-optimized, direction stated: higher threshold ⇒ less erosion; an erosion budget eps may replace the declaration via r ≥ (1−eps)/eps — the 19:1 D-prime datum sits at the 5% line). (R3FAM-10 amendment) P1 reads: components with MEASURED or STRUCTURALLY PROVEN independence → RSS (GUM); a merely stated argument routes to P2 (conservative direction; this honors the pre-registered criterion — the O-F25 'per correlation argument' advocacy is noted, not adopted, as the weaker form). NOTIFICATION consumed: the C34 K² instance (VERDICT_wave3 par.1.1 item 2) is a live P-tag case for this policy's first pass. Falsifier, duty F2-C41-BAND-POLICY, dependencies: as PANEL_FAM par.4.4. Note text of record = PANEL_FAM par.4.4 AS REPAIRED by VERDICT_wave3 par.1.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C42
  choice: "Safety constant K_RICH role reuse"
  incumbent: "one numeral, 8 roles"
  alternatives:
    - "per-role derivations (GCI Fs on observed p; KS rho from Lipschitz scale; etc.)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#1.5 C42"
  owner: "F2 ordered duty F2-C42-KRICH-ROLE-AUDIT (role census 10 functional + 3 notified pending; census annex home unchanged)"
  note: "annex status verbatim: 'NEVER (registered open row)'; annex internal pointer: AC5 (R25). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the DE-bucket panel re-verified ONE instance arithmetic (rho = K_RICH ln(N)/mu0) — instance verification of a single role, not the per-role derivation audit; no adjudication found. WAVE-1 NOTIFICATION 2026-08-19 (VERDICT_wave1.md par.4.6, no role adjudicated): C27's repaired protocol adds K_RICH roles (near-binding band width; chatter red-line multiplier) to this row's role census; C11's estimator-campaign leg (a) produces the observed-p data this audit consumes. WAVE-2 DELTA (par.4.12): C21's [X-TBAK]-pattern omega surrogate in z = NEW K_RICH role (notification); C31TRIO consumes C27/C28-derived rho constants (no new role). WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT, enum MIXED (VERDICT_wave3 par.1.5): ordered per-role program STANDS; interim-validity hypothesis RESTATED per the adopted repair (R3FAM-11, verbatim): interim validity restated honestly: role-1 (two-resolution Richardson) sites stand under the wave-1 interim regime's OWN checkable mechanism (VERDICT_wave1 par.2.4: pre-F2 band-bearing verdicts run the already-built [X-O32] estimator at their own sites or declare the row trigger fired) — NOT under a blanket conservativeness hypothesis; roles 2/3/6/7/8/10 interim validity = the per-role structural declarations of the ordered program, pulled forward explicitly as DECLARED-INTERIM (one line each at the audit's first window); roles 4/5 (delta_inst = min_margin/K_RICH, thr = m_ref/K_RICH) have INVERTED direction (larger K = smaller floor) — 'K = 4 conservative' is not well-defined for them and no blanket claim is made. Role-census note delta CORRECTED (judge, cross-slot reconciliation): measured 2026-08-20: 107 .py sites / 17 files; 10 functional roles (9 certificate-bearing + timing declared out-of-scope) + 3 notified pending (C27 w/red-line, wave-1; C21 omega-in-z, wave-2; C8 dev_warm arming floor — wave-3 REMMARCH notification, PANEL_REMMARCH par.4.3, binding). Reverse conditionality (R3REMMARCH-6(i), judge-carried): the C8 arming floor consumes whatever per-role constant F2-C42-KRICH-ROLE-AUDIT lands for that role — derived constant substitutes the literal on landing. Ordered duty, falsifier (published-route Fs(p_obs) > 4 forces the derived constant), dependencies: as PANEL_FAM par.4.5. Note text of record = PANEL_FAM par.4.5 AS REPAIRED by VERDICT_wave3 par.1.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C43
  choice: "Asymptotic-range handling at band sites"
  incumbent: "presumed h^p at r=1,2"
  alternatives:
    - "per-site observed-order check (o32 estimator reuse) + NON-CONCLUSIVE propagation"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#4.5 C43"
  owner: "F2 (aligned with C11; was S25)"
  note: "annex internal pointer: GAP-9. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-1 BOOKKEEPING ALIGNMENT 2026-08-19 = adjudicated-with-C11, ledger-enum MIXED (VERDICT_wave1.md par.4.5, consequence of the cluster-C9C11 refutation round): content identity — C43's listed alternative ('per-site observed-order check (o32 estimator reuse) + NON-CONCLUSIVE propagation') IS the C11 interim regime adopted by the wave-1 verdict; measured half = F2-C11-ESTIMATOR-CAMPAIGN leg (a) (GAP-9, both rows' annex pointer). Not a fresh adjudication — a declared alignment; the landing window confirms. WAVE-3 CONFIRM 2026-08-20 = CONFIRM-ALIGNMENT PASS, unchanged (VERDICT_wave3 par.1.6): final delta = PANEL_FAM par.4.6 note-append verbatim, with the anchor corrected to findings :1182-1190 (row starts at :1182 — R3FAM-13 nit). No status change (MIXED stands), no duty minted, no escalation. Note text of record = PANEL_FAM par.4.6 AS REPAIRED by VERDICT_wave3 par.1.6 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C44
  choice: "FD steps (gradient/Hessian)"
  incumbent: "fixed sqrt(eps)*scale / eps^(1/3)*scale"
  alternatives:
    - "More-Wild noise-aware per-column steps"
    - "seam-aware stencils"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C44: ADJUDICATED-SPLIT"
  owner: "F2 duty F2-C44-FDSTEP (with [P-HESSREJ])"
  note: "annex status verbatim: 'single-author (derived-for-smooth; kink-aware NEVER)'; annex internal pointer: GAP-16. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); speed audit S1 DECLINEs approx_derivative's step machinery on constraint-7 grounds (protects the incumbent derived steps by POLICY, not by accuracy comparison) and N8/V7 registers the measured-pattern colored-FD mechanism (ADVISORY_engine_speed_audit_2026-08-12.md par.7.3 S1 + par.4 N8); More-Wild per-column steps never weighed. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-SPLIT, enum MIXED (VERDICT_wave3 par.5): PANEL_REMENG par.4.7 WITH: (R3REMENG-10) the Nocedal-Wright par.8.1 support re-marked at the depth actually held (standard-textbook error model, [P-HESSREJ] owner-text citation; par.8.1 [FULL] read folded into F2-C44-FDSTEP execution — the book is on disk); (R3REMENG-11) F-C44-3 pinned to ONE lowering: FD-on-twin vs CS-on-twin (numpy twin tier, h=1e-20 instrument of record); conclusion scoped to the shared step/stencil logic — the engine-tier transfer is by that logic, not by cross-lowering comparison; (R3REMENG-12) More-Wild 2011/2012 re-marked [TITLE]/of-record in the census (papers-needed already concedes the full text; ECNoise = ONE deduplicated FAM+REMENG procurement row, VERDICT_wave3 par.9). Rides the C18/C20 Tier-1 measured-noise seam (one owner, four riders — VERDICT_wave3 par.7 item 2). Note text of record = PANEL_REMENG par.4.7 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C45
  choice: "leggeAree bracket"
  incumbent: "hardcoded 400-point grid [1.01, 2.8]*as"
  alternatives:
    - "box-derived q_max window"
    - "vectorization"
  status: DECIDED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C45: ADJUDICATED-WITH-VALVE"
  owner: "F2-C45-BRACKETGUARD = the execution of engine-core:F7-legge-bracket-fallback's owner prescription (findings :599-607) — cited, not re-minted; no parallel object"
  note: "annex status verbatim: 'single-author (registered: speed audit H2 + AUDIT)'; annex internal pointer: AC16. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); the speed audit MEASURED the vectorization alternative's block gain (444x, H2 bracket-scan = the leggeAree scan per sota_gapmap_raws_2026-08-12/s24_gapv_cell-cert.md:191) but H2 never landed (M5c accepted first-pass) and the box-derived q_max window was never adjudicated (ADVISORY_engine_speed_audit_2026-08-12.md H2) — measurement of one alternative, no comparison verdict. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED-WITH-VALVE, enum DECIDED (VERDICT_wave3 par.5; gated:false — the duty is the F7 findings row's owner prescription, cited): PANEL_REMENG par.4.8 WITH (R3REMENG-13 repair, adopted verbatim): the rejector family REPLACES the single endpoint rejector — (a) EMPTY-BRACKET ⇒ LOUD REFUSAL: the silent q0 = 1.8·as default (a1:872) dies or is gated; (b) boundary-interval bracket ⇒ REFUSE; (c) post-solve supersonic-root rejector M(qe) > 1 + derived margin (the F7 audit's named check); the idx[-1] last-sign-change ordering hypothesis is LOGGED as a named, monitored hypothesis. The duty is HOMED: F2-C45-BRACKETGUARD = the execution of engine-core:F7-legge-bracket-fallback's owner prescription (findings :599-607: derive the scan window from the table box (q_max from h0 − h(T_TAB_LO)) + assert bracket found + supersonic-root rejector) — cited, not re-minted; no parallel object. (R3REMENG-14) the correctness half is carried as LOAD-BEARING (not bookkeeping); post-repair the valve treatment is legitimate. F-C45-2 derived-window containment and the dormant H2 vectorization conditional stand; the derived scan window CONSUMES the C25 H-F6 box by name (VERDICT_wave3 par.7 item 6). Note text of record = PANEL_REMENG par.4.8 AS REPAIRED by VERDICT_wave3 par.5 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

# --- NEW ROWS DECLARED S25-bis (ledger grows 45 -> 48; docs/rde_
# nozzle_PROGRESS.md R25 delta S25-bis block, 'regola R5-estesa':
# righe nuove dichiarate al commit) ---

- id: C46
  choice: "Padded-lane fill policy for the M5c per-column vmapped bucket executor"
  incumbent: "tail-repeat: padded lanes repeat the last real partner's data (data-only, never accounted, never in the plan)"
  alternatives:
    - "zero-padding with an explicit masked reduction"
    - "NaN-padding + isnan guard on every consumer"
    - "ragged/variable-shape scan (no padding, no fixed bucket bound)"
  status: DECIDED
  evidence: "validation/ADVISORY_S25bis_diff_convergence_2026-08-12.md#tail-repeat padding vs P_DUM: principled"
  owner: "F2 window (unassigned) — residual lever only: the named second-design-bucket lever if padding pressure returns (informational; owner no longer required at DECIDED)"
  note: "PROGRESS census (docs/rde_nozzle_PROGRESS.md R25 delta S25-bis): 'padding tail-repeat (single-author+gate)' — the m5cgate bitwise-identity gate covers correctness of the executor's output (dec-vector, z-band, doctored first-offender, near-seam pair). cross-reconciled 2026-08-13: UPGRADED SINGLE-AUTHOR -> DECIDED — the padding choice WAS put against genuine alternatives and converged: Form-2 fused convergence row B-F2 'tail-repeat padding vs P_DUM: principled' ADOPTED with the merged refuter attack RF-11(iii) FAILED (ADVISORY_S25bis_diff_convergence_2026-08-12.md par.2 convergence table; cost accepted with the named lever, second design bucket, if pressure returns) — the dummy-fill class (zero/NaN padding) adjudicated there; the ragged/no-padding route is separately measured-REJECTED of record (eager cell_scan pattern 0.8x slower + 803 s per-shape compile churn, ADVISORY_engine_speed_audit_2026-08-12.md M5/DEAD rows). Dual proof complete: gate half (m5cgate) + panel/refuter half (B-F2/RF-11(iii)). Prior single-author anchor kept for provenance: validation/PROGRESS_2026-08-12_S25bis_speed.md#tail-repeat the last real partner."

- id: C47
  choice: "Numeric-lint enforcement policy for legacy literals in validation/**/*.py (R28 scope-hole closure)"
  incumbent: "per-file frozen baseline ratchet (validation/numeric_lint_baseline_validation.json): any count INCREASE over baseline fails; any DECREASE also fails until the baseline is ratcheted down; an unbaselined file with literals fails outright"
  alternatives:
    - "full per-file classification into TRIVIAL/ALLOWLISTED/EXEMPTED (the src/ standard already enforced by tests/test_numeric_lint.py)"
    - "blanket exemption of validation/ (status quo ante — the scope hole itself)"
    - "hard fail on any legacy literal, no ratchet (immediate full remediation)"
  status: DECIDED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave3.md#C47: ADJUDICATED (ratchet retained + multiset-fingerprint"
  owner: "F2-entry hygiene (per-file classification into the src/ taxonomy; findings registry row test-suite:numeric-lint-scope-hole) — endpoint confirmed by the wave-3 verdict"
  note: "PROGRESS census (docs/rde_nozzle_PROGRESS.md R25 delta S25-bis): 'ratchet-baseline come pattern debt-lint (standard di settore, dichiarato)' — declared as an industry-standard debt-ratchet pattern by single-author decision, not a converged panel; DECLARED LIMIT (test_numeric_lint.py docstring): within-count literal swaps still pass, the ratchet is a channel guard not a classification. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); Form-2 fused convergence ratified the ratchet as the strict standard pattern WITH repairs (RF-9/B-F12 ADOPTED, three missing rejector directions added; ADVISORY_S25bis_diff_convergence_2026-08-12.md par.2) — refuter-HARDENED, but the listed policy alternatives were not comparatively weighed; full per-file classification remains the F2-entry owner. WAVE-3 ADJUDICATION 2026-08-20 = ADJUDICATED (ratchet retained + multiset-fingerprint upgrade), enum DECIDED (VERDICT_wave3 par.6; gated:false): PANEL_REMPOL par.4.5 WITH (R3REMPOL-9 repair, adopted verbatim — the closure claim REPLACED everywhere by the honest residual declaration): the multiset upgrade SHRINKS the declared limit from {any within-count literal swap} to {within-count PERMUTATIONS of the existing in-file value multiset} — value-swaps A→B now FAIL; role-permutations of values already present both PASS and are the DECLARED RESIDUAL (documented in the F-C47-2 rejector exactly as the current docstring declares the swap limit); positional/context fingerprints (SARIF partialFingerprints proper) = the NAMED upgrade path that would close it, not adopted (cost above need). F-C47-1/3, endpoint confirmation (per-file classification, F2-entry owner), closed alternatives: unchanged. Landing note recorded (R3REMPOL-10, carrier outside this ledger, handed to its owner window): reconcile the F2-entry owner-text '621' against the magnitude's re-measured 37/639 in the same window (SR-12). Lit rider handed per registry policy: SARIF v2.1.0 partialFingerprints [ABS, spec] (PANEL_REMPOL par.4.6). Note text of record = PANEL_REMPOL par.4.5 AS REPAIRED by VERDICT_wave3 par.6 (authority chain; no duplication here by anti-entropy calibration 2026-08-20)."

- id: C48
  choice: "Hessian/gradient batching strategy for engine M4: vmap-batched entry (M6) vs sequential per-segment FD, as the DEFAULT execution path"
  incumbent: "sequential per-segment FD Hessian (default, unchanged from the M-chain; nonfinite-lane guard + hess_lane counter shipped on this path)"
  alternatives:
    - "vmap value_and_grad batched entry inside the M4 engine (SOTA, plan as broadcast operands, zero per-segment recompile) — IMPLEMENTED and gate-tested, REJECTED as default"
  status: DECIDED
  evidence: "docs/rde_nozzle_PROGRESS.md#AGGIUDICAZIONE M6 DI RECORD"
  owner: "F2 (adoption = declared version change at a session boundary, m6gate re-formed on the corrected criterion, M3 precedent; candidate opt-in flag A1_VMAP_HESS=1 named in the interim)"
  note: "m6gate FIRED at first implementation (dH 18% of Hessian scale = 5-6x the FD scheme's own asymmetry) -> root cause ISOLATED via three convergent probes (adjoint cross-lowering gradient floor ~1e-8 rel over ~250 implicit solves, FD-amplified ~7 orders; even sequential eager-vs-jit diverges 2.9e-2; the defect was the mixed-lowering formulation, not the batching) -> CORRECTED FORM (base in-batch, one lowering; lane-permutation control bitwise) measured dH 1.68x scheme asymmetry, INSIDE the derived K_RICH bound, batched self-asymmetry better than sequential -> classed ADOPTION-READY. Classed DECIDED (not SINGLE-AUTHOR/NEVER) because the verdict rests on a full converged multi-probe technical adjudication (m6gate + consumer diagnostic + root-cause isolation), not an un-surveyed choice; the residual open item is WHEN to adopt (a session-boundary version-change decision), not WHETHER the current default is justified. [PLACEMENT REPAIR 2026-08-19, declared: this note sat as a stray SECOND note: line under C51 — a duplicate key that OVERWROTE C51's own note for any machine parser; content is C48's (the m6gate verdict), relocated here verbatim in the Blocco-2 landing window.]"

- id: C49
  choice: "Per-phase solution REPRESENTATION for design: fitted-front characteristic marching vs adjoint-consistent shock capturing vs two-tier (capturing explorer + fitted certifier)"
  incumbent: "fitted-front characteristic marching (the S1 piecewise-smooth class with fitted fronts is the certified class of record; capturing never used)"
  alternatives:
    - "adjoint-consistent shock capturing (WENO/DG/entropy-stable FV with smooth limiters) as the sole solver"
    - "two-tier: capturing as exploration/discovery solver + fitted marching as certification solver, |J_captured - J_fitted| promoted to a measured (v) model-residual carrier (the 4/4 de-novo convergence)"
    - "modern implicit shock tracking (HOIST/Zahr; Bonfiglioli-Paciorri unstructured fitting) as the high-order upgrade path"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.8 C49"
  owner: "S-FOUNDATIONS Phase C (Form-2 with a GENUINE captured-only advocate seeded from the four de-novo trees' option blocks; user question 2026-08-17 'perche si fitta una soluzione che potrebbe essere catturata' = the mandate); falls to F2 entry if the Phase C batch splits"
  note: "Minted 2026-08-17 at Phase B: the choice lives in M0 D2.5 (solution class) and was never a LEDGER row — the de-novo diff exposed the omission. The four blind trees each carry the captured option at its genuine best (V-F6/F27, H-F4/F8/F9/F28, O-F9, P-F12) and converge on the two-tier form with the fitted tier as the certificate bearer (adjoint front-motion term = the load-bearing argument, Giles-Pierce class). Nothing decided here: the row exists so the adjudication cannot close by omission. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.8/4.8; ZERO refuter findings on this row): two-tier architecture of record — fitted-front marching = SOLE certificate bearer (five anchor-verified reasons incl. the Giles-Ulbrich held anchor at declared 1-D scalar scope; the ANSWER OF RECORD to the user question 2026-08-17 'perche si fitta una soluzione che potrebbe essere catturata'); captured explorer = named build-gated exploration tier (duty F2/F5-C49-CAPTURE-EXPLORER, not scheduled) with the |J_capture - J_fitted| carrier protocol + four pinned falsifiers; implicit shock tracking = named upgrade path with standing entry gate; fit-vs-capture boundary = DWR-budget policy via the C11 estimator campaign leg (b). Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19). Entry-gate published audit items: HZ JCP 454 par.5.2-5.3 robustness checklist (edge-collapse element removal, sensor-triggered re-initialization, adaptive kappa re-balancing Eq. (71), shock-agnostic p=0 init) + Thakur JCP 523 p.26 open-issues list (partial-shock stability, robust mesh ops, R_u-preconditioner approximation) — the gate's robustness criterion is now checkable against the line's own published measures; lit-registry rider: thakur identity -> JCP 523:113633 journal version (retro-sweep 2026-08-20)."

- id: C50
  choice: "Datum-space metric for the interface-data uncertainty contract (acceptance + sensitivity semantics)"
  incumbent: "none — the record has no metric at the contract site (finding contract:datum-uncertainty-contract-missing)"
  alternatives:
    - "L1(A) (blind-PDE lens: TV-metrics ruled out since jump positions are never measured to TV accuracy; weaker-than-L1 ruled out by flux continuity)"
    - "thrust-calibrated flux-weighted L2 + separate L-inf realizability guard (blind-data/UQ lens)"
    - "both, split by role: acceptance metric vs sensitivity metric"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_C50_form2.md#4 EXACT LEDGER DELTA TEXT FOR C50"
  owner: "F2 duty F2-C50-CONTRACT-METRIC, superseding form VERDICT_C50_form2 par.7 (6 numbered clauses; armed at the EARLIER of findings :1668 / :2324; FS-3 at first U-c ensemble; F-ARCH at first campaign-scale comparison; sequenced with contract:datum-uncertainty-contract-missing + contract:phase-gauge-jitter-alignment-unpinned)"
  note: "Minted 2026-08-17 per the blind-contract judge's fourth closing condition; the two blind formalizers genuinely disagree and the record is silent — a textbook Form-2 case with non-incestuous advocates already on file. Cross-reference appended 2026-08-19 (Blocco-2 landing, no adjudication): the acceptance+sensitivity SEMANTICS this metric must serve got their carriers this window — the uncertainty-contract finding row (findings registry, contract:datum-uncertainty-contract-missing) and the contract-diff metric-dispute record it cites; the adjudication itself remains OWED, unchanged. C50 CLOSURE 2026-08-20 (VERDICT_C50_form2.md, Form-2 full escalation D1 — THE C50 authority of record; supersedes the wave-3 §2.1 delta where they differ, diff declared in that file §5): SINGLE-CONTRACT-METRIC of record, PRODUCT FORM. Contract tuple = [DATA] Def 7.1's six slots (Q̂, (N,Ω), χ, U, W, 𝒫) with d/ε/W semantics per [PDE] D7.1; the d slot carries ONE metric: d = L1(A) on phase-aligned fields ⊕ declared front positions/speeds as finite-dimensional components with their own bars (ε_field; ε_{s_k}; ε_Ω), inside the L∞(K) realizability guard (adopted in the [PDE] role: class side condition, converged-on-object with [DATA]'s norm-pair member), TV-bounded side condition. Acceptance = product-ball membership; excursion from W = NO DATUM (rejected, not error-barred). The thrust-calibrated flux-weighted L2 ⊕ front-parameter block is NOT a second contract metric: it is the U-slot grade-U-a certified sensitivity INSTRUMENT — per-design consumer instrument under C7.2, delivered by the C56 discrete AD-adjoint of record (a fired F11d re-opens its delivery route) — with mandatory arming and falsifiers FS-1 (probe family incl. near-sonic axis, probes at the quoted design)/FS-2/FS-3 (rank-correlation)/FS-4 (G validity-neighborhood stability); G carries a declared evaluation point (datum nominal + reference design/family, recorded in 𝒫) + declared validity neighborhood + recalibration trigger; U-b covariance / U-c sampling live in the same slot with sampling laws intersected with the L∞ guard. Sharpness discipline (S1′ discharged, text untouched): the certified sensitivity constant is the actual dual pairing against the CERTIFIED perturbation class — for the product ball, ‖Λ‖_L∞(A)·ε_field + Σ_k|∂J/∂s_k|·ε_{s_k} + |∂_Ω J|·ε_Ω, exact for that ball (sup-attainment, one-line duality), under the seam regularity declarations of record (Λ ∈ L∞(A) imposed dual regularity; G ∈ L∞ on the smooth complement; finite front sensitivities — verified at instantiation, FS-1/FS-2 live); richer certified grades sharpen through U, never through a second d. The wave-3 seam clause is replaced by the grade statement: coarser certified grade ⇒ coarser certified interval; grades ordered; ONE tie rule ([PDE] C7.2 ≡ [DATA] Claim 7.4; ties reported as ties). Front-block sensitivities at the record's declared rigor: shift-differentiability rigorous in 1-D, practice-without-theorem in multi-D (theorem_ledger :235; G12 R-G12.2 to-verify); FS-2 the live empirical guard. Fronts are a DELIVERABLE: declared front set (positions/speeds with bars) is a tuple component; the F-2 datum-side moving-RH/entropy audits (findings :1651-1659, same F2 window) read the DECLARED components and are continuous in the product metric; a family with demonstrably unidentifiable fronts falls back PER-FAMILY to the scoped form (A1 restricted to the flux-integral audit class + representation-level RH/entropy declaration) — declared, never silent (F-PROD). Alignment branch pinned by (N,Ω) + delivered front positions (N-fold hazard closed structurally); FA-2 predicted drop = Σ_k |[q]_k|·δ_k over the declared jump set (O(jitter×slope) only as the finite-slope-representation reading). ARCHITECTURE FLIP PRE-REGISTERED (F-ARCH — the S1′ dispute as experiment): at the first imported real-data family, IF the cheap product-form intervals tie a majority of compared design pairs that the U-a instrument (FS-1 passing, FS-2 non-vacuous) separates AND the sampled [min,max] route over the certified ball is priced infeasible at campaign scale by measured cost, THEN this row re-opens in favor of SPLIT-BY-ROLE (sharp metric into the d pair) as a ledger update, not a re-litigation. Wasserstein/OT family stays CLOSED (honest form of record: the only datum-space optimization in the contract is the 1-D phase alignment, exhaustively solvable on S¹; OT convexity targets high-dimensional misfit landscapes; signed multi-component data needs artifact-prone normalization) — re-enters only at any future upstream assimilation layer. Instantiation + ALL numeric constants = duty F2-C50-CONTRACT-METRIC (superseding form, VERDICT_C50_form2 §7), armed at the EARLIER of the two named triggers (findings :1668 F2-freeze/first-real-family; :2324 first unsteady-generator ingestion); sequenced with contract:datum-uncertainty-contract-missing and contract:phase-gauge-jitter-alignment-unpinned. Census of the escalation: Hoffman et al. 1995 (Mon. Wea. Rev. 123:2758-2770, distortion = displacement ⊕ amplitude ⊕ residual) VERIFIED [ABS] — upgrades the R3C50-11(3) [MEMORY] lead; Ben-Tal–Nemirovski + Bertsimas dual-norm/set-geometry anchors [ABS] ride this note (argument support; dedup vs docs/literature_registry.yaml owed at landing); Fidkowski-Darmofal AIAA J. 49(4) 2011 [ABS] = the output-weighted calibration principle's SOTA home (same line as the Giles-Pierce anchor, diff :288-293); Métivier arXiv:2101.00904 stays the optional non-blocking OT-closure enrichment. (Provenance of record, landing 2026-08-20: authority chain = VERDICT_wave3 par.2.1 C50 delta AS SUPERSEDED by VERDICT_C50_form2.md section 4 blockquote, applied verbatim per the depth-addendum D1 order; enum MIXED-equivalent per landing convention, instantiation half = named F2 duty; F-2 row anchor carried in its CR-W3-2(ii) corrected form :1651-1659.)"

- id: C51
  choice: "Rung-3a wave-frame solver: implicit BVP (freezing + Newton-Krylov) vs azimuthal MARCHING of the periodic orbit"
  incumbent: "implicit BVP (M0 rung-3a pin: freezing + Newton-Krylov)"
  alternatives:
    - "azimuthal marching in alpha: the blind-PDE formalizer proves the wave-frame problem symmetrizable-hyperbolic in alpha where |u_theta − Omega r| > a — the periodic orbit could be MARCHED (a genuine algorithmic alternative surfaced blind)"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-5 — Wave-frame azimuthal MARCHING"
  owner: "rung-3a implementation window (adjudication owed at implementation time, not before — the judge's own sequencing)"
  note: "Minted 2026-08-17 per the blind-contract judge's fourth closing condition. The hyperbolicity-in-alpha regime condition is the decision criterion's core; outside the regime the marching option dies structurally. INPUT APPENDED 2026-08-19 (S-FOUNDATIONS-C Blocco 2, swirl5f dispatch sec.6 rule 2): swirl5f claim 6 is available as DIRECT adjudication input — azimuthal-march spacelikeness == |w_rel| > c, and its degeneracy loci == the D.18 H-WR/H-NC kernel loci (one geometric object; verifier-CONFIRMED in-panel, status for THIS ledger = advisory input, panel grades never record grades; UPDATE 2026-08-19 close: the panel's §8 verifier verdict LANDED (converged, with three named edits applied to the panel report); the loci identification is verifier-confirmed at the phaseD source, so the input is no longer §8-gated); the dispatch's data-anchored-shadow reading (sec.3 row 13, user catch of record: the per-phase family reconstructs ONLY the data-anchored shadow of helical interior sheets, azimuthally-fed interior front segments are UNREACHABLE by the axial march) STRENGTHENS the marching route's case — route B treats helical sheets natively. The 'CJ locus' naming caveat travels with any consumption (findings row swirl5f:c8-cj-locus-naming-approximate)."

- id: C52
  choice: "Contract status of a MIXED / phase-crossing characteristic signature on Gamma_d (agenda item D-1 of the blind-contract diff): does a phase-migrating sonic line VOID the contract, or merely DOWNGRADE the Verdict?"
  incumbent: "record reading: subsonic patches are a declared case-class with monitors (D1 sec.4.3bis O1-O4); the class stays ADMISSIBLE, H3-cl expected to FAIL on migrating patterns (K-P Fig. 6) with documented Verdict downgrades"
  alternatives:
    - "blind-PDE reading ((P3)): signature must be CONSTANT in phase and position; mixed signature is INADMISSIBLE outright — the sonic set is a free boundary of the prescription problem"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-1 — Mixed/phase-crossing characteristic signature"
  owner: "F2 contract window (the judge's agenda: pin which reading governs; feeds the chi character-map finding row — findings registry, contract:chi-character-map-unknown-band-missing)"
  note: "Minted 2026-08-19 (S-FOUNDATIONS-C Blocco-2 landing) per the blind-contract verdict's fourth closing condition: agenda items to the ledger. Mostly mooted by the L4 default; load-bearing the day the declared subsonic case-class is used."

- id: C53
  choice: "Gamma_d PLACEMENT policy (agenda item D-2): the record fixes Gamma_d without recording the trade; the two blinds DISAGREE WITH EACH OTHER on the preference direction — both monotone pressures must be recorded with the emptiness verdict"
  incumbent: "record: Gamma_d fixed per case with no recorded placement trade"
  alternatives:
    - "blind-DATA: most UPSTREAM station with beta = 0 (transportability pressure: datum least contaminated by the design's own back-influence)"
    - "blind-PDE: admissible band [z_I^-, z_I^+], POSSIBLY EMPTY, with DOWNSTREAM preference (phase-family reduction pressure); the emptiness verdict is part of the deliverable"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-2 — Placement as a band"
  owner: "F2 contract window: a placement-band remark in M0 D2.4 (or this row's adjudication) naming the two monotone pressures and the emptiness verdict — the judge's own suggested carrier"
  note: "Minted 2026-08-19 (Blocco-2 landing) per the blind-contract verdict's fourth closing condition. Genuine blind-vs-blind disagreement: two non-incestuous advocates already on file."

- id: C54
  choice: "Normative status of the mean-state rung I4 (agenda item D-4): ladder rung (record) vs BANNED reduction (blind-data Jensen counterexample — the mean-datum single solve has O(Var) error with NO small parameter); the two are compatible only if I4 carries the O(Var) no-small-parameter warning at its definition site"
  incumbent: "M0 D2.4 keeps I4 as a ladder rung with NO warning at the site — VERIFIED 2026-08-19: the D2.4 ladder line reads 'I4 single mean state.' bare; the Blocco-2 M0 landing did NOT include the one-line annotation, so the agenda item is NOT consumed and this row stays OPEN"
  alternatives:
    - "blind-DATA Claim 3.4: BAN the mean-datum solve as a reduction (reject-only reading)"
    - "compatibilization (the judge's named resolution): one-line I4 annotation in D2.4 carrying the O(Var) no-small-parameter warning — I4 stays a declared ladder rung, never an approximation claim"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-4 — Normative status of the mean state I4"
  owner: "next M0 D2.4 touch (one-line I4 annotation; when the line lands this row flips DECIDED with the M0 anchor as evidence)"
  note: "Minted 2026-08-19 (Blocco-2 landing) per the blind-contract verdict's fourth closing condition; consumption-check against the same-window M0 edit performed and negative (see incumbent)."

- id: C55
  choice: "P_amb aggregation form: how the objective aggregates over a declared operating-ambient interval/measure (mission-weighted mean vs minimax vs CVaR)"
  incumbent: "single-point default P_amb = {Pa0} (slot minted, docs/rde_nozzle_problem_book.md P_amb SLOT OF RECORD 2026-08-19; no aggregation chosen — the default instantiation makes the question empty)"
  alternatives:
    - "nu-weighted mission mean: EXACT-equal to the single evaluation at <Pa>_nu wherever the full-flowing/ambient-blind affinity holds (H-T3.2, T3 Lemma-C class) — the classical trajectory-weighted design"
    - "minimax over [Pa_min, Pa_max]: binds through the admissibility constraints (g_sep, wake fence) at the monotone-worst member"
    - "CVaR_beta / Wasserstein-DRO over the ambient measure (PB-5 machinery extended from mu to the operating envelope)"
  status: NEVER
  evidence: "docs/rde_nozzle_problem_book.md#P_amb SLOT OF RECORD"
  owner: "first multi-point instantiation of P_amb (adjudication owed at instantiation time, not before — the slot's own sequencing clause; until then the single-point default keeps the question empty)"
  note: "Minted 2026-08-19 (S-FOUNDATIONS-C boundary touchpoint, user ratification of parked decision (a): explicit c-slot with single-point default). Structural facts the adjudication will consume: interior per-phase fields are Pa-INDEPENDENT on the certified class (ambient-blind, H-T3.2), F affine in Pa (so mean-aggregation collapses to a point evaluation wherever admissibility is uniform); the envelope binds ONLY through admissibility (separation/wake) and every non-affine regime is outside the certified class (H2' fence) — excluded and declared, never silently averaged. The ambient weight nu is a CONTRACT DATUM with provenance discipline identical to mu."

- id: C56
  choice: "Adjoint REALIZATION of record for the marched stack, per role (gradient / DWR weight / adaptation indicator): discrete AD-adjoint (custom_vjp, one-lowering) vs separately discretized continuous adjoint vs dual-consistent synthesis"
  incumbent: "discrete AD-adjoint everywhere (route fact since S8/S18; never adjudicated as a choice — directive-axis verification 2026-08-19)"
  alternatives:
    - "separately discretized continuous adjoint (characteristic-native, Ancourt/Lozano-Ponsin line)"
    - "dual-consistent synthesis (discrete weight + continuous-structure referee, Hicken-Zingg criteria)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_C9C11_supplement.md#4.3 Candidate NEW ledger row"
  owner: "weight role CLOSED (C11 supplement, F11d pinned); gradient role closed-consumed 2026-08-19 wave-2 (one consumption of record: PANEL_C31TRIO par.3.1-bis primary + PANEL_C1REP par.3.4-bis merged convergent, VERDICT_wave2 par.2.10 — realization = discrete AD-adjoint, continuous line = frame + referee); indicator role closed by transitivity (f2 = -lambda2, same object). All three roles CLOSED; F11d = the measured residue (owner F2-C11-ESTIMATOR-CAMPAIGN); a fired F11d re-opens this row"
  note: "Minted 2026-08-19 at the C9/C11 landing per VERDICT_C9C11_supplement par.4.3 (sustained-repair re-key of record: id C52->C56 by measured command, grep last = C55; content-dedup vs rows C52 through C55 NEGATIVE twice independently). The dual-consistency residue is the P2 Lemma B par.4.5 SCHEMA with F11d as its estimator-site execution; a fired F11d re-opens THIS row, not C11. Anchors: [X-A1IM]; C48 note (one-lowering); C49 (fitted class). Wave-2 premise guard (PANEL_C2021 par.3.4, VERDICT_wave2 par.4.4): all three roles linearize at the replayed root and inherit the wrong-branch blind spot until C20 Tier-0 lands; F11d verifies the weight GIVEN the root, not root identity — the C20 branch monitor guards F11d's premise at estimator sites. F11d published criterion set anchored: Hicken-Zingg Definition 1 (JCP 256 p.164: L*_h v + g = O(h^q), v = projected continuous dual) + the p.164 caveat that primal consistency does NOT imply dual consistency — F11d's leg-1 order bookkeeping cites the definition by page, and the caveat is the published reason the F11d check cannot be waived on primal-order evidence (retro-sweep 2026-08-20). Canon support anchored: Fidkowski-Darmofal 2011 p.676 (Duraisamy et al. comparison — discrete adjoint superior for fine-space output, continuous marginally better for analytic output when resolved) = published independent support for the weight-of-record vs referee role split (retro-sweep 2026-08-20)."

- id: C57
  choice: "Global exploration tier for the certified design loop: none (local-only of record) vs derivative-free/evolutionary/Bayesian global layer ABOVE the gradient-based certificate closer vs captured-explorer reuse (C49 tier) as the exploration vehicle"
  incumbent: "none — the record optimizes locally from continuation/warm starts; no global layer was ever adjudicated (directive-axis verification 2026-08-19)"
  alternatives:
    - "DFO/BO/evolutionary global layer, exploration-only, certificate closer untouched (trees quarantine advocacy V/O/P)"
    - "captured-explorer tier as the exploration vehicle (C49 two-tier synergy)"
    - "multi-start continuation on the existing branch machinery (V-F26/V-F7 line)"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.14 Candidate rows handed to the landing"
  owner: "F2-entry adjudication window (interacts with C31 A/B and C49 explorer; user directive axis 4 = adjoint-free at genuine best)"
  note: "Minted 2026-08-19 at the wave-2 landing per VERDICT_wave2 par.4.14 item 1 (PANEL_C31TRIO par.4.5; dedup grep reproduced by refuter and re-verified at mint). Candidates 3 (physical solvability-region derivation program) and 4 (exact-vs-sampled certificate pattern) FOLDED by declaration: item 3 rides the C28 re-scope note (F4b/O1-adjacent, not a C20/F2 blocker), item 4 lives inside the C1 migration duty item (3) — anti-entropy calibration of record. User-ratified candidate experiment (2026-08-20, registered on the user's order as this row's measured arm): one-shot rival-paradigm pilot — an uncertified exploration-first arm (BO/DRL/evolutionary at their genuine modern best, on cheap capturing solves; the C49 captured-explorer tier is the natural vehicle) vs the certified closer of record, on ONE pilot case; deliverables = J found per arm, solve/wall-clock budget per arm, and the downstream cost of CERTIFYING each arm's winner; PURPOSE OF RECORD: converts the constitution-bias residue (certificate-first paradigm = declared axiology, never head-to-head adjudicated — named in the C2 bias audit answer) into a measured comparison, per the dual-proof standard. Ratified as CANDIDATE only: execution and priority are decided at this row's adjudication window (F2-entry), jointly with the S-5F and C49-explorer build decisions."

- id: C58
  choice: "Differentiable-stack FOUNDATION: AD-exact-gradient stack (JAX custom_vjp/implicit-function rules) vs hand-coded adjoint vs AD-in-other-framework (Julia/Enzyme, Fortran+Tapenade, 2026 landscape) vs gradient-free foundation"
  incumbent: "JAX primary per G0 decision of record (docs/rde_nozzle_G0_decision.md, DIR-G0, 2026-07-17: executable evidence — 52/52 derived-tolerance Jacobian rejector, adjoint overhead 1.5 percent measured, X-GENOXC cross-code oracle 218/218 with negative controls; Julia+Enzyme = declared alternate; loop-speed falsifier ARMED and subsequently NOT FIRED by the S25/S25-bis measured MET program)"
  alternatives:
    - "hand-coded adjoint"
    - "AD-in-other-framework (Julia/Enzyme, Fortran+Tapenade, 2026 landscape)"
    - "gradient-free foundation"
  status: MIXED
  evidence: "docs/rde_nozzle_G0_decision.md"
  owner: "F2 entry engine window (same decision surface as C31 A/B [P-IPADJ]); adjudication = census+refuter panel as 9(e) delta-sweep of G0 vs the 2026 landscape"
  note: "Minted 2026-08-20 on the user catch (third instance of the no-row foundation class after C49/C56); dedup by measured grep in the mint window: grep -n C58 docs/choice_ledger.yaml = 0 hits pre-mint, and the keyword sweep (custom_vjp/JAX/Enzyme/Tapenade/gradient-free) hits only C11/C33/C56 — adjoint-realization, curvature and batching axes, no row covers the stack-foundation choice. ENTRY CONTRACT: the census weighs every framework alternative against the OBLIGATIONS created by the adjudicated corpus (C56 one-lowering discrete-AD weight + F11d execution duty; C31 A/B + information-only multiplier discipline; C32/C33 curvature pins; C57 adjoint-free tier; C9/C11 estimator architecture; engine:cross-lowering-gradient-floor discipline) — the question is re-anchored from G0's 'can we get exact gradients affordably' (answered, measured) to 'which foundation best realizes the ADJUDICATED architecture'. A C58 flip re-enters consuming rows ONLY via their pinned falsifiers."

- id: C59
  choice: "Temporal form of the design functional: cycle-average vs harmonic-balance adjoint vs time-spectral vs windowed unsteady adjoint"
  incumbent: "cycle-average — CANONICAL-INSIDE-THE-PIN: inside the periodic-rotating-wave pin the quotient structure makes the cycle-average a theorem-backed CONSEQUENCE, not a choice (BRIEF_blocco2_phaseD_addendum_c4.md section (e), BINDING scoping)"
  alternatives:
    - "harmonic-balance adjoint (live ONLY in weakened-pin regimes: multi-frequency)"
    - "time-spectral adjoint (live ONLY in weakened-pin regimes: multi-frequency / quasi-periodic)"
    - "windowed unsteady adjoint (live ONLY in weakened-pin regimes: aperiodic / finite-window transients)"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/BRIEF_blocco2_phaseD_addendum_c4.md#(e) TEMPORAL-FORM ROW REFINEMENT"
  owner: "F2-entry census window; trigger = any weakened-pin regime (multi-frequency / aperiodic / windowed) entering scope — inside the pin the question is EMPTY by the binding scoping, so no adjudication is owed before the trigger fires"
  note: "Minted 2026-08-21 at the S-FOUNDATIONS-C4 COVERAGE GATE, FOUNDATION-CHOICE ENUMERATION category. SCOPING IS BINDING per the addendum-c4 section (e) refinement: the three alternatives are NOT rivals of the cycle-average inside the periodic-rotating-wave pin (there the cycle-average is canonical, the quotient-structure consequence of record); they live ONLY where the pin is relaxed, so this row can never be read as re-opening the pinned-regime functional. Anchors of record: rubino_2018, schotthofer_2024, zahr_persson_2016 (literature registry, root D, READ-INTEGRAL). Dedup by measured grep in the mint window (SR-12): grep -niE 'harmonic|time-spectral|windowed unsteady|temporal form|cycle-average vs' docs/choice_ledger.yaml = 0 hits pre-mint; grep -niE 'rubino|schotthofer|zahr_persson' docs/choice_ledger.yaml = 0 hits pre-mint — no existing row covers the temporal-form axis."

- id: C60
  choice: "Optimizer architecture: NAND reduced-space (incumbent, practiced) vs SAND full-space (LNKS class) vs hybrids"
  incumbent: "NAND reduced-space, PRACTICED of record — INCUMBENT-DECLARED, never weighed vs SAND in this ledger: the practice rests on certificates evaluated on converged states, the measured segment speed (S25/S25-bis MET program), and the compiled-chain record"
  alternatives:
    - "SAND full-space one-shot (LNKS class: state + design + adjoint solved simultaneously in one KKT system)"
    - "hybrids (reduced-space outer loop with full-space inner corrections / one-shot warm-started terminal phases)"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/FORK_LEDGER_141_adjudication.md#D-1 (O15)"
  owner: "F2-entry engine window (the C31 A/B decision surface; adjudicated as a CLUSTER with C57/C58/[P-IPADJ]); trigger = the F2-entry engine act"
  note: "Minted 2026-08-21 at the S-FOUNDATIONS-C4 COVERAGE GATE, FOUNDATION-CHOICE ENUMERATION category — the FORK_LEDGER_141_adjudication.md deviation D-1 (O15) rider EXECUTED, 5th instance of the no-row foundation class (after C49/C56/C58 and the same-window C59). The question is WEIGHED of record OUTSIDE this ledger — docs/rde_nozzle_pipeline_audit.md:105 ('reduced TR-SQP / full-space one-shot (LNKS) ... two immature solvers composed — deferred, not dismissed') and docs/rde_nozzle_general_scheme_panel.md:29 ('full-space LNKS deferred — two immature solvers composed, adjoint-opt feasibility refutation') — but NO ledger row carried it until this mint, the same no-row class the user caught at C49/C56/C58. Status NEVER with the incumbent DECLARED: NAND is practiced, its supports are measured, but the head-to-head vs SAND was never run; a future flip re-enters consuming rows only via their pinned falsifiers (C58 discipline). Dedup by measured grep in the mint window (SR-12): grep -niE 'NAND|SAND|LNKS|full-space|one-shot|reduced-space' docs/choice_ledger.yaml pre-mint hits ONLY the C57 note ('one-shot rival-paradigm pilot' — the exploration-arm experiment, a DIFFERENT axis: uncertified exploration vs certified closer, not state-design coupling architecture); no row covers NAND-vs-SAND."

- id: C61
  choice: "Base-pressure closure model p_b for truncated plug/shrouded sectors: Veen 0.846p/M^1.3 (legacy-practiced, WG10-FAILED) vs WG10 empirical bracket vs Nasuti-Onofri transition model vs derived N2 closure vs measured (V1.4-class CTAP instrumentation)"
  incumbent: "INCUMBENT-DECLARED, no program adoption: the Veen constant closure p_b = 0.846 p/M^1.3 is practiced in the LEGACY chain only (GENO practice of record, lit row vander_veen_1974 summary '0.846/M^1.3 -> sec.7.5 unreliability row'); the program's own p_b is a DECLARED SLOT, not a model (docs/rde_nozzle_problem_book.md:348 sector (ii) PLUG: 'truncation length L_p and base-pressure closure p_b declared (N2)')"
  alternatives:
    - "WG10 empirical bracket [+19%,-15%] (best pure-empirical on cold data; Onofri 2002 RTO-TR-AVT-007 survey)"
    - "Nasuti-Onofri transition-PR model (open->closed wake transition; the only validated classical piece, [MODEL-VAL] per WG10 pp. 10-11)"
    - "derived N2 closure (the problem book's declared owner of the slot; WG10 verdict on Eq. (5.1): 'N2 MUST REPLACE IT')"
    - "measured closure (V1.4-class CTAP: the single existing hot-fire RDE base-pressure measurement, Purdue nozzleless, transition Pa/Pc ~ 0.15)"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/BASE_PRESSURE_HARVEST_c4.md"
  owner: "N2/F4b window (base-pressure closure adjudication rides the F4b external-expansion window; shares the diff par.2.9 agenda entry with findings rows litreview:residue-r8-r23-base-pressure-pb2-blocking (:2092, trigger 'PB-2 base-pressure closure window') and plume:free-boundary-solve-mechanics-missing (:2519, H20 — which carries the neighboring SOLVE mechanics, not this closure-model choice))"
  trigger: "first truncated-plug (value,delta) row entering the record, or F4b window entry — whichever fires first"
  note: "Evidence content (the anchor file's adjudication, quoted here because the evidence field carries the bare resolvable path per lint family (e)): Veen traced to a 1966 cold near-wake curve fit FAILED by WG10 + Humphreys 1971 exhibit — p_b model swap moves the design argmax x2.45 at +0.26% value + Purdue V1.4 CTAP datum, the only RDE base measurement of record. Minted 2026-08-21 at the S-FOUNDATIONS-C4 COVERAGE GATE REPAIR pass (critic finding CGC-4) — 6th instance of the no-row foundation class (C49/C56/C58/C59/C60 precedents; the C60 precedent shape verbatim: weighed of record OUTSIDE the ledger at record grade — Veen / WG10 bracket / Nasuti-Onofri / N2 mechanistic / Purdue-datum-anchored, BASE_PRESSURE_HARVEST_c4.md — with a practiced legacy incumbent and a mandated replacement, but NO C-row for a future flip to consume via a pinned falsifier until this mint). The Humphreys x2.45 argmax sensitivity is channel-(vi)-shaped, i.e. FOUNDATION-grade: the closure choice moves the DESIGN, not just the value. Dedup by measured grep in the mint window (SR-12): grep -niE 'base.pressure|p_b|Veen' docs/choice_ledger.yaml = 0 hits pre-mint (exit 1); the 60 pre-mint choice titles contain no p_b/base axis — 'Thermo closure' (C24) and 'leggeAree bracket' (C45) are different axes."

- id: C62
  choice: "Phase-quadrature over Xi: discretization of the cycle-average objective (uniform trapezoid practiced) vs Gauss vs adaptive/event-stratified vs QMC alternatives"
  incumbent: "INCUMBENT-DECLARED, never adjudicated: every averaged-functional evaluation of record uses a finite uniform phase set (uniform-grid trapezoid practice); the sole coverage anchor across the record is diff par.2.10 'phase-quadrature stratification at events' (phaseB_tree_diff.md), which is the anchor covering FOUR forks (V4, H33, O2, P10, FORK_LEDGER_141 rows) plus the par.4.3 FFT probe"
  alternatives:
    - "Gauss quadrature on Xi"
    - "adaptive / event-stratified quadrature (the diff par.2.10 increment: stratify at detected events — dF/dS discontinuous across events even where F is continuous; kinked-data composite rules)"
    - "QMC / randomized phase sampling"
    - "declared-canonicity resolution (the C59 pattern): the periodic pure-rotating-wave pin makes uniform-trapezoid/FFT spectrally optimal for smooth integrands — IF adopted, THAT argument becomes this row's incumbent text, with the event-stratification caveat as its named boundary"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/phaseB_tree_diff.md#Phase-quadrature stratification at events"
  owner: "F2 engine window (numerics cluster: quadrature choice adjudicated alongside the C44 FD-step / C43 band-site / C34 and C35 tolerance family)"
  trigger: "first accuracy budget of the F2 campaign (the budget cannot close without a named quadrature-error bar — findings :988 is the standing reminder)"
  note: "Secondary evidence (the evidence field carries the single resolvable diff anchor per lint family (e)): the diff par.2.10 entry is the sole coverage anchor of record and covers forks V4/H33/O2/P10; docs/findings_registry.yaml:988 phase-diagrams:eps-tolerance-omits-quadrature-bar is the bar-accounting residue riding the same axis. Minted 2026-08-21 at the S-FOUNDATIONS-C4 COVERAGE GATE REPAIR pass (critic finding CGC-5) — 7th instance of the no-row foundation class (after C49/C56/C58/C59/C60 and the same-repair C61). Honesty clause carried from the critic (zero inflation): unlike C61 no in-record adjudication AMONG alternatives has been performed — the axis is weighed once, in one place (diff par.2.10), with no alternatives table; the mint-or-canonicity decision is therefore INSIDE this row (alternative 4), never a silent default. Dedup by measured grep in the mint window (SR-12): grep -niE 'quadrat' docs/choice_ledger.yaml = 0 hits pre-mint (exit 1); C27 ('Constraint aggregation') is the CONSTRAINT aggregation axis, not the objective's quadrature rule."
