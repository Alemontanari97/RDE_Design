# CHOICE LEDGER — typed index of the algorithmic choice-adjudication
# ledger (standing directive choice-adjudication-convergence,
# 2026-08-12; S-ORDINE T2 step S5a, census R25/R32).
#
# PROSE AUTHORITY (unchanged by this file): the ANNEX "6. ANNEX — THE
# CHOICE LEDGER" in validation/ADVISORY_S24_sota_gapmap_2026-08-12.md
# (rows C1-C45, table + reading paragraph). This YAML INDEXES that
# annex — it never replaces it. Per-row deltas that postdate the
# annex (executed twins/sweeps, closures, three S25-bis-declared new
# rows) are folded in as status/note updates CITING
# docs/rde_nozzle_PROGRESS.md (R25 census rows), never silently
# overwriting the annex text.
#
# SCHEMA (strict subset, mirrors docs/findings_registry.yaml
# discipline): each entry
#   id: C<n>                                   (C1..C48, unique)
#   choice: <one line>                          (what is being decided)
#   incumbent: <the code's current behavior>
#   alternatives: [<genuine SOTA alternative>, ...]   (may be [])
#   status: DECIDED | MIXED | SINGLE-AUTHOR | NEVER
#     DECIDED       = converged-panel / constructional / measured
#                      verdict of record (annex "CONVERGED*")
#     MIXED         = one branch DECIDED, a sibling branch NEVER
#                      (e.g. eq-path decided, IP-path never surveyed)
#     SINGLE-AUTHOR = one author's rationale stands, no survey/panel
#     NEVER         = registered open row, no comparison run yet
#   evidence: "<path#anchor>"   (resolvable: annex row anchor for
#                                 C1-C45; PROGRESS/session-log anchor
#                                 for rows carrying a post-annex delta
#                                 or for the three new C46-C48 rows)
#   owner: REQUIRED for every NEVER and SINGLE-AUTHOR row (verbatim
#          from the annex "Owner+placement if open" column, or the
#          census/session-log text for C46-C48; "F2 window
#          (unassigned)" only if the source truly names none)
#   note: optional — annex's own internal evidence-pointer text
#         (D6/param-F.../GAP-N/AC-N, informational, not necessarily
#         independently resolvable), annex status nuance verbatim,
#         and/or the PROGRESS delta citation for touched rows.
#
# LINT: none yet — no tests/test_choice_ledger.py exists at the time
# of this extraction (S-ORDINE T2/S5a is a READ-ONLY-elsewhere step,
# one new file only). Declared gap, owner = next S-ORDINE/S-CERT
# window (mirror tests/test_findings_registry.py's group (xix)
# pattern: schema/enum check, per-status required-field check,
# evidence-anchor resolution check, duplicate-id check).
#
# COUNTS (verified at extraction time, see verification note below):
#   annex rows of record (C1-C45): 45
#   this registry: 57 rows (45 indexed + C46-C48 newly declared
#     S25-bis rows + C49-C51 minted S-FOUNDATIONS 2026-08-17 — growth
#     enumerated, not silent: C49 Phase-B diff omission, C50/C51 the
#     blind-contract judge's C-4 deliveries — + C52-C54 minted
#     2026-08-19, S-FOUNDATIONS-C Blocco-2 landing: the remaining
#     C-4 agenda items D-1/D-2/D-4 of
#     validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md
#     Part A (iv), landed as ledger rows per the same condition
#     — + C55 minted 2026-08-19 at the S-FOUNDATIONS-C boundary
#     — + C56 minted 2026-08-19 at the C9/C11 wave-1 landing (count
#       regenerated by measured grep same window, SR-12)
#     — + C57 minted 2026-08-19 at the wave-2 landing (global
#       exploration tier; count regenerated same window)
#     touchpoint: P_amb aggregation form, the user-ratified parked
#     decision (a), GATED-by-owner on first multi-point instantiation)
#   tallies by status (red-team-corrected recount, replacing the
#     document's own inconsistent "8/14/23" line — see the annex
#     reading paragraph and docs/rde_nozzle_PROGRESS.md R25):
#     DECIDED: 5 (C3, C4, C15, C23, C24)
#     MIXED: 3 (C29, C31, C39)
#     SINGLE-AUTHOR: 15 (C1, C6, C11, C12, C13, C17, C18, C19, C27,
#       C32, C44, C45, C46, C47 + C48 counted under DECIDED — see
#       C48 note; SINGLE-AUTHOR annex-only subtotal = 12, plus C46
#       and C47 = 14; recount below is authoritative)
#     NEVER: 25 (C2, C5, C7, C8, C9, C10, C14, C16, C20, C21, C22,
#       C25, C26, C28, C30, C33, C34, C35, C36, C37, C38, C40, C41,
#       C42, C43)
#   EXACT machine tally of this file (do not hand-eyeball the block
#   above further — count the `status:` field values instead;
#   post-reconciliation 2026-08-13, see RECONCILIATION block below):
#     DECIDED: 7 (C3, C4, C15, C23, C24, C46, C48)
#     MIXED: 3 (C29, C31, C39)
#     SINGLE-AUTHOR: 13 (C1, C6, C11, C12, C13, C17, C18, C19, C27,
#       C32, C44, C45, C47)
#     NEVER: 31 (C2, C5, C7, C8, C9, C10, C14, C16, C20, C21, C22,
#       C25, C26, C28, C30, C33, C34, C35, C36, C37, C38, C40, C41,
#       C42, C43, C49, C50, C51, C52, C53, C54)
#     TOTAL: 54 (7+3+13+31=54; C49 minted at S-FOUNDATIONS Phase B —
#     the fit-vs-capture solution-representation choice had NO ledger
#     row, caught by the de-novo diff). C48 (vmap-in-engine) is classed
#     DECIDED here despite the census shorthand "gate-rejected as
#     default" because it carries a full multi-probe converged
#     technical verdict (m6gate + three convergent root-cause
#     probes), NOT a single-author or un-surveyed choice — see its
#     note for the declared nuance (adoption path itself still open,
#     owner F2).
#
# RECONCILIATION (cross-reconciliation sweep, 2026-08-13, S-ORDINE
# tail; user challenge "abbiamo passato a setaccio le scelte
# algoritmiche piu' volte — sicuri che le fasi siano conciliate?"):
# every row with status NEVER (25) or SINGLE-AUTHOR (14) was swept
# against the adjudication corpus below for evidence that the choice
# WAS in fact put against alternatives. ARITHMETIC OF RECORD:
# 39 rows swept -> 1 upgraded SINGLE-AUTHOR -> DECIDED (C46, padding
# policy: Form-2 fused convergence B-F2 vs P_DUM + refuter
# RF-11(iii) + measured-DEAD ragged route), 0 upgraded to MIXED,
# 0 upgraded NEVER -> SINGLE-AUTHOR, 25 CONFIRMED NEVER,
# 13 CONFIRMED SINGLE-AUTHOR. Every swept row carries a dated
# "cross-reconciled 2026-08-13" note (upgrade anchors, or the
# nearest evidence found and why it does not adjudicate, or a plain
# confirm).
# CORPUS SWEPT (all committed):
#   validation/ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md
#     (+ the _math and _impl_fidelity one-pass raws),
#   validation/ADVISORY_S25bis_diff_convergence_2026-08-12.md,
#   validation/ADVISORY_S24_thermo_closure_survey_2026-08-12.md,
#   validation/ADVISORY_S24_DEbucket_panel_2026-08-12.md,
#   validation/ADVISORY_def_equivalence_panel_2026-08-07.md + _proof,
#   validation/ADVISORY_Sgauntlet_generality_ledger_2026-08-11.md,
#   validation/ADVISORY_engine_speed_audit_2026-08-12.md,
#   validation/ADVISORY_litreview_confrontation_2026-08-13.md
#     (ADOPTED/A-rows only, cited strictly as
#     adjudication-evidence-pending-ratification — the doc is CAVA
#     NON RATIFICATA, never cited as decided),
#   validation/ADVISORY_rde_choking_2026-08-11.md,
#   validation/ADVISORY_mean_swirl_panel_2026-08-11.md,
#   validation/sota_gapmap_raws_2026-08-12/ (finder/verifier/refuter
#     raws — the annex's own sources; confirmations, never upgrades),
#   docs/rde_nozzle_PROGRESS_ARCHIVE.md (historical census deltas),
#   grep of validation/PROGRESS_*.md session logs per row subject.
# STANDARD APPLIED (dual-proof, strict): expert sense-review alone
# is NOT choice adjudication (it asks does-this-make-sense, not
# does-it-beat-the-alternatives); upgrades only where a CONVERGED
# panel/refuter round EXPLICITLY weighed the incumbent against
# genuine alternatives. Confirmation of NEVER is a legitimate
# outcome; no upgrade was forced.
entries:

- id: C1
  choice: "Design basis class"
  incumbent: "clamped/natural interpolating cubic, heights-as-dofs"
  alternatives:
    - "B-spline control polygon (de Boor/Boehm)"
    - "CST (Kulfan)"
    - "Hicks-Henne"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.5 C1"
  owner: "F2 duty F2-C1-CONTROL-CHART-MIGRATION (items 0-6 + driver leg; GAP-21 = the duty findings carrier)"
  note: "annex status verbatim: 'single-author on the oscillation axis (D6 adjudicated conditioning+revalidation only)'; annex internal pointer: D6:915-926; param-F1 + Q2. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); gapmap raws re-verify the D6:915-926 rejection of the B-spline control-point switch (sota_gapmap_raws_2026-08-12/s24_gapv_constraints.md:173 'collides with D6 S20 rejection of record') but that is the same single-author D6 axis the annex already scoped; no panel/refuter comparison of basis classes found. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.5/4.5): direction converged — certified carrier migrates to the B-spline control-polygon chart of the SAME certified spline space (Bernstein-exact admissibility certificates = the new axis); incumbent chart verdict-bearing until the three migration falsifiers pass; the first migration falsifier re-pinned on the chart innocent-data response with the per-use conversion-map pin; duty gains the DRIVER LEG (chart-map conditioning bound derived OR in-chart walk re-baseline inside derived bands); the D6 S20 rejection untouched on its axis AND weighed on its driver-conditioning content. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C2
  choice: "Right-end BC"
  incumbent: "natural y''(L)=0"
  alternatives:
    - "not-a-knot (de Boor/FITPACK)"
    - "free-end control points"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.6 C2"
  owner: "conditional: branch A rides F2-C1-CONTROL-CHART-MIGRATION; branch B = duty F2-C2-NOTAKNOT-ADOPTION"
  note: "annex evidence pointer: toc:148; param-F2. POST-ANNEX DELTA (S25-bis, docs/rde_nozzle_PROGRESS.md R25 delta S25-bis block; findings_registry.yaml:parametrization:natural-BC-lip-bias): notaknot-twin EXECUTED — corner mismatch 6.629e-2 -> 1.166e-2 (r=1, -82%), 6.637e-2 -> 9.775e-3 (r=2); lip-state (cd) shift 8.2% = mechanism datum; baseline reproduces S19 figure to 5 digits. Diagnostic executed, ADOPTION DECISION still open -> status stays NEVER, owner updated from the stale 'S25 notaknot-twin (GAP-5)' to the live F2 consequence-adjudication owner. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the notaknot twin remains a DIAGNOSTIC (mechanism datum), the adoption comparison itself was never adjudicated; no further evidence found. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-CONDITIONAL, enum MIXED (VERDICT_wave2 par.2.6/4.6): branch A (C1 migration lands) — the end-condition dof dissolves, C2 RESOLVES by structure to the second listed alternative free-end control points; the GAP-5 numbers (corner -82 percent, cd 8.2 percent) = measured priors, re-measured at a certified optimum inside the C1 campaign; branch B (migration refuted) — live F2 adoption question per the frozen criteria. The BC itself NOT adjudicated (twin remains a diagnostic of record; brief pin honored). Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C3
  choice: "Left-end BC"
  incumbent: "clamped tan(thB)"
  alternatives: []
  status: DECIDED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C3 |"
  note: "annex status verbatim: 'CONVERGED (constructional) [J]' — attachment tangency, forced by geometry, no genuine alternative exists; annex internal pointer: toc:146-147; annex owner column: '-'. WAVE-2 CONFIRMATION 2026-08-19 = DECIDED confirmed-rescoped (VERDICT_wave2 par.2.7/4.7): clamped tan(thB) STANDS — neither tree challenger (P-F21 rounded lip; H-F18 PM-fan internal data) touches this row's axis; gloss narrowed to this axis of record. Attachment-GEOMETRY axis = NAMED census-lemma territory (live anchors: lip-circles pin + diff par.4.8 cone-vs-sharp tension; F2-exit user pin respected); KT2015 hundredth-fractions materiality carried; sharp-vs-floor twin falsifier pinned for any future census-window amendment. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C4
  choice: "Knot placement law"
  incumbent: "uniform xi + [X-AKNO] goal-indicator insertion"
  alternatives:
    - "free knots (Jupp)"
    - "curvature-based abscissae"
  status: DECIDED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C4 |"
  note: "annex status verbatim: 'survey-decision of record [S25-REPAIR: was \"CONVERGED-panel\"; D6 item 9 records an adopt-or-declare SURVEY decision, not a Form-2 panel]'; annex internal pointer: D6 item 9 (free-knot rejection, Jupp lethargy); S20 [X-AKNO] record. POST-ANNEX MENTION (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta paragraph): 'C4-suite chiusa' is named in the same R25 delta sentence alongside C17/C18/C23/C24/C34/C2; referent recorded verbatim from PROGRESS (test-suite/gate-closure context, distinct from and not altering this knot-placement survey-decision itself) — no change to status or alternatives."

- id: C5
  choice: "Knot anchoring across thB updates"
  incumbent: "normalized xi re-anchored to live xB"
  alternatives:
    - "physical-x anchored knots"
    - "arc-length anchored knots"
    - "curvature-anchored knots"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C5 |"
  owner: "S25 drift monitor (GAP-34)"
  note: "annex internal pointer: param-F8 (corrected magnitudes Q8). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found"

- id: C6
  choice: "Insertion degeneracy guard"
  incumbent: "dx_loc = median station spacing (mesh-derived)"
  alternatives:
    - "operator-derived max adjacent-knot ratio (de Boor; Schoenberg-Whitney)"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C6 |"
  owner: "S25 guard edit (GAP-23)"
  note: "annex status verbatim: 'single-author (rationale measured wrong: solve vs operator)'; annex internal pointer: adaptive_knot_optimize:209-213; P4' probe. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block), no panel/refuter adjudication found"

- id: C7
  choice: "Outer-loop dof policy"
  incumbent: "insertion-only ratchet"
  alternatives:
    - "insert+remove (Lyche-Morken)"
    - "AFEM coarsening (BDD04)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C7 |"
  owner: "F2/F3 loop upgrade (GAP-25)"
  note: "annex internal pointer: param-F5 (corrected attribution). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found"

- id: C8
  choice: "Warm-start deviation handling"
  incumbent: "print-only dev_warm"
  alternatives:
    - "derived-floor rejector (armed) + THEOREM row"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C8 |"
  owner: "S25 (GAP-24)"
  note: "annex status verbatim: 'NEVER (armable invariant unarmed)'; annex internal pointer: param-F4; P3 = 6.7e-16. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found"

- id: C9
  choice: "March mesh law"
  incumbent: "uniform NI/da/Nw/Ne per run"
  alternatives:
    - "column-local C- insertion at record time"
    - "goal-oriented AMR (Venditti-Darmofal)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.3 Row C9"
  owner: "F2 duty F2-C9-MESHLAW-CAMPAIGN (was R25/F2 spec)"
  note: "annex status verbatim: 'NEVER (registered open row)'; annex internal pointer: R25; mesh-F1/AC2. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); nearest evidence = litreview A36 singularity-aware derived spacing law (log-weighted DWR equidistribution near the sonic strip, ADVISORY_litreview_confrontation_2026-08-13.md A36) = adjudication-evidence-pending-ratification only (CAVA NON RATIFICATA, cited as pending, never as decided); the incumbent uniform law was never put against the listed alternatives. WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED (VERDICT_wave1.md par.2.3/4.3): goal-oriented characteristic-native adapted placement = TARGET production law, presumptive pending F9a; uniform-per-run retained as interim certificate law (no verdict of record moves); A36 static arm adopted now (refinement-only, IVL immobile); AC2 column-insertion = the adaptation primitive ([X-AKNO] mechanism transferred, hypothesis under test); falsifiers F9a (matched TOTAL unit-process count, BOTH counts reported, promotion-threshold derivation published before first use), F9b (frozen-topology attribution re-run), F9c (IVL immobility). SUPPLEMENT OF RECORD (directive-axis closure; PANEL_C9C11_SUPPLEMENT.md axis A + VERDICT_C9C11_supplement.md par.4.1 FINAL): moving-mesh/r-adaptive family CLOSED at census grade — A36 IS the family equidistribution principle in derived static form; clause (ii) generalizes to F9c (one-locus anti-motion, not family closure); front-fitting = the family alignment content already exact of record (C49/Lemma B par.4.3); MMPDE map-motion does not transfer (columns = characteristic intersections; free dofs = seeds + insertion). FOLDED: F9a arm R = fixed-count wall-station seed redistribution (IVL-line/fan/exit seed families frozen-by-declaration; fixed shape, no recompile); matched TOTAL count EXPECTED by construction and VERIFIED by the repaired-F9a rule (both counts reported per arm, no exemption); IVL seed pinned per F9c; F9b via frozen pre-motion seed set; falsifier F9a-R (dropped if not beating uniform+A36 outside joint bands on the pre-registered twin sites; promoted only under the same E_A <= E_U/2 rule). Three-arm composition of record: C9 closes on uniform+A36 only if BOTH adaptive arms fail their beat tests; drop rules per-arm; at parity (joint-band discipline, before the cost tiebreak) the declared cost tiebreak selects the r-arm as production mechanism (fixed shapes) with insertion in the between-runs role; every such outcome closes C9 converged, not re-gated. J-materiality of motion-vs-insertion declared BELOW bands; cost materiality (recompile) declared FOR the r-arm."

- id: C10
  choice: "Refinement operator"
  incumbent: "refine(r) joint global scaling"
  alternatives:
    - "per-direction refinement (multi-parameter GCI, Eca-Hoekstra)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C10 |"
  owner: "S25 flag (GAP-35)"
  note: "annex internal pointer: o33_bench:172-176; mesh-F3(ii). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found"

- id: C11
  choice: "Error estimator for J"
  incumbent: "two-level Richardson K=4"
  alternatives:
    - "three-mesh observed-p GCI (Roache/Celik)"
    - "DWR (Becker-Rannacher)"
    - "LSQ multigrid"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.4 Row C11"
  owner: "F2 duty F2-C11-ESTIMATOR-CAMPAIGN, leg (a) ORDERED before leg (b) (was S25/GAP-9 + R25 DWR)"
  note: "annex status verbatim: 'single-author at band sites ([X-O32] exists for O3.2 norms only)'; annex internal pointer: AC3; GAP-9. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); litreview par.3.25/A36 re-scope the DWR bars near the sonic throat (adjudication-evidence-pending-ratification, CAVA NON RATIFICATA) — that touches the DWR alternative's validity DOMAIN, not the estimator choice at band sites; no panel/refuter comparison of estimators found. WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED, burden INVERTED honored (VERDICT_wave1.md par.2.4/4.4): DWR = target primary (characteristic-native; consumes HELD ancourt_peter_atinault_2023 + the HELD lozano_ponsin_2025 row (registry :122-128, read-partial, PDF on disk — rider corrected per the sustained supplement-refutation note: the row EXISTS, no new mint) + the space-time-DWR template line); Richardson/GCI = permanent referee; h^p-asymptotics premise RETIRED as epistemic premise (standing banded verdicts STAND, flag on findings row mesh-amr:observed-order-not-at-band-sites); interim bands = observed-p GCI via [X-O32]/GAP-9, K=4 in-loop only at measured p_obs >= 0.415 per site; LSQ = degraded-p checkpoint rule; D-49 lands in the F2 window in BOTH F11a branches; DWR-failure path closes C11 GCI/LSQ-primary (converged, not re-gated); C43 adjudicated-with-C11 (same window edit). SUPPLEMENT OF RECORD (axis B + VERDICT_C9C11_supplement.md par.4.2 FINAL): the DWR WEIGHT fork is CLOSED — weight of record = DISCRETE AD-adjoint of the marched scheme (custom_vjp, one-lowering per C48; exact at computed J, O3.1 [X-A1IM]; captured-smear trap bypassed on the fitted class, P2 Lemma B par.4.4); continuous/characteristic-native line (Ancourt 2023 + Lozano-Ponsin 2025) = formulation frame + REFEREE only (compatibility residuals; never the weight — a weight cannot referee itself); estimator form = two-level fine-space residual (Venditti-Darmofal class) on the [X-O32] ladder; same-level pairing signal-free at the certified Newton floor ([X-A1IM] per-cell z-space certification) so enrichment MANDATORY, and the enrichment operator must not smooth across characteristic-borne adjoint discontinuities. NEW PIN F11d, two legs (split per the sustained supplement-refutation amendment): leg 1 oracle convergence (AD weight -> analytic adjoint on the Giles-Pierce quasi-1D oracle at pre-registered O3.2/O3.3 order/norms; leg-1-only = oracle-only label; quasi-1D->axisym rides the declared OPEN R27 bridge); leg 2 band sites (ACE/Lozano-Ponsin Eqs. (30)/(31) compatibility residuals at production band sites, no analytic adjoint needed). EITHER leg firing blocks F11a promotion and re-opens the WEIGHT realization only (architecture stands: DWR-target + referee permanence). Dual consistency of the march = P2 Lemma B par.4.5 SCHEMA — F11d is its execution at estimator sites."

- id: C12
  choice: "Start line"
  incumbent: "Sauer first-order, gamma=const, GENO-mirrored (+1e-6 offset)"
  alternatives:
    - "Kliegel-Levine higher-order"
    - "Hall"
    - "displaced-IVL rejector"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C12 |"
  owner: "S25 rejector; F4b/F5 higher-order if it fires"
  note: "annex status verbatim: 'single-author (twin DATA CONTRACT - fidelity adjudicated, accuracy NEVER)'; annex internal pointer: a1:719-727, :37-40; GAP-6/AC14. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); litreview A7 registers the Hoffman-1987 LENGTH oracle whose throat model is Kliegel-Levine (pending ratification) — oracle-band context around the alternative, not an accuracy adjudication of the start line; accuracy axis still never adjudicated"

- id: C13
  choice: "Axis unit process"
  incumbent: "GENO foot-ratio guard v1/y1"
  alternatives:
    - "L'Hopital dv/dy carried (Zucrow-Hoffman)"
    - "MMS verification"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C13 |"
  owner: "S25 gconst axis oracle"
  note: "annex status verbatim: 'single-author (GENO mirror; march-level verification NEVER)'; annex internal pointer: a1:488-499, :566; GAP-8. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block), no panel/refuter adjudication found"

- id: C14
  choice: "Exit-condition read point"
  incumbent: "axis-cell Me only"
  alternatives:
    - "multi-point exit read + band"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C14 |"
  owner: "folded into GAP-8 oracle"
  note: "annex status verbatim: 'NEVER [J]'; annex internal pointer: a1:853, :889. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found"

- id: C15
  choice: "m_stop exit floor"
  incumbent: "1e-5 GENO-mirrored (S19 knob)"
  alternatives:
    - "mesh-coupled floor m_stop ~ C*da^2"
  status: DECIDED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C15 |"
  note: "annex status verbatim: 'CONVERGED-measured (attribution diagnostic executed; STIM-1)'; annex internal pointer: AC4; annex owner column: '- (residual: coupling documentary)' — a documentary residual remains, not a reopened choice"

- id: C16
  choice: "Newton damping ladder"
  incumbent: "(1,1/2,1/4,1/16,1/64,0), bare argmin non-increase"
  alternatives:
    - "Deuflhard measured-contraction damping"
    - "Armijo sufficient decrease"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C16 |"
  owner: "F2 engine rebuild"
  note: "annex status verbatim: 'NEVER (no recorded rationale)'; annex internal pointer: a1:371, :397-416; GAP-29. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the executed GAP-29 halved-constants sweep exercised C18's roundoff-floor constants, NOT the damping ladder; no adjudication of the ladder vs Deuflhard/Armijo found"

- id: C17
  choice: "Trip cap N_NEWTON"
  incumbent: "30"
  alternatives:
    - "derived from contraction budget"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C17 |"
  owner: "AUDIT F5 execution (S25)"
  note: "annex status verbatim: 'single-author (KAT proved it bites, not derived)'; annex internal pointer: [X-CDKAT]; a1:145-148. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C17-C18 sweep = GAP-29'): the GAP-29 halved-constants sweep targeted C18's constants (NEWTON_TOL_FACTOR, C_FLOOR, C_OPS); N_NEWTON=30 itself was NOT among the swept constants and remains un-derived. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); the sweep-exclusion fact above stands, no derivation or panel adjudication of N_NEWTON found"

- id: C18
  choice: "Roundoff-floor constants"
  incumbent: "NEWTON_TOL_FACTOR=100, C_FLOOR=8 (spike convention)"
  alternatives:
    - "Higham op-count gamma_n"
    - "More-Wild noise floors"
  status: SINGLE-AUTHOR
  evidence: "docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED"
  owner: "S25 halved-constants sweep (executed; derivation duty now F2-live)"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C18 |; annex status verbatim: 'single-author (AUDIT F5 registered; test never executed)'; annex internal pointer: AC9; GAP-29. POST-ANNEX DELTA (S25-bis, findings_registry.yaml:engine-core:F5-underived-factors, s25bis_gap29_sweep.json): GAP-29 sweep EXECUTED — 1 FLIP of record: NEWTON_TOL_FACTOR/2 flips cert_verdict (factor-2 margin load-bearing); C_FLOOR/2 and C_OPS/2 no-flip (>=2x measured headroom). Sweep executed, derivation itself still NEVER done -> status stays SINGLE-AUTHOR. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); the executed sweep is measurement evidence on the incumbent (1 flip of record), not a Higham/More-Wild comparison; derivation duty F2-live unchanged. WAVE-2 NOTE 2026-08-19 (VERDICT_wave2 par.4.11, named-not-adjudicated): C20's Tier-1 shares this row's derivation window — the kappa-qualified band and the NEWTON_TOL_FACTOR derivation are one question (GAP-29 factor-2 cliff)."

- id: C19
  choice: "Certification metric scale"
  incumbent: "scalar sc = max(1, max|z|)"
  alternatives:
    - "componentwise scaling (Deuflhard NLEQ; Higham)"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C19 |"
  owner: "F2 hygiene with AUDIT F8"
  note: "annex status verbatim: 'single-author (AUDIT F8 adjudicated LOW)'; annex internal pointer: AC8/Q1. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block), no panel/refuter adjudication of scalar-vs-componentwise scaling found"

- id: C20
  choice: "Certificate qualification"
  incumbent: "unqualified one-extra-step ratio"
  alternatives:
    - "kappa(J)-aware derived bound"
    - "Kantorovich/alpha-theory"
    - "branch-consistency monitor"
    - "interval-Newton [X-IVXC]"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.9 C20"
  owner: "F2 (HIGH); duty F2-C20-CERTQUAL-CAMPAIGN (Tier-0 shared with C21; Tier-1 shared with the C18 window; M0 back-propagation R4)"
  note: "annex internal pointer: GAP-3. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.9/4.9): tiered qualification architecture of record — Tier-0 branch/fold monitors always-on + Tier-1 kappa-derived band composed with the retained incumbent metric + Tier-2 flagged-cell Kantorovich/Krawczyk referee + deflation diagnostic; solvability-region CLOSED-as-mechanism (O-F20 counted ONCE, shared-source declaration vs C28); P4 absolute, floor unmoved; four pinned falsifiers; dependencies C20 -> C28, C56/C11-estimator; C18/C19 interplay named-not-decided. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C21
  choice: "Seed validity policy"
  incumbent: "asserted O(h) predictor + stale replay seeds (C2-F2 mitigation)"
  alternatives:
    - "alpha-theory per-seed certificate"
    - "Kantorovich radius ([X-TBAK] pattern in z)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.10 C21"
  owner: "F2 duty F2-C21-SEEDCERT-CAMPAIGN (owner alignment S25 -> F2, C43 precedent)"
  note: "annex internal pointer: GAP-20. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the S25-bis in-trace traced-predictor seeds (predict_interior_t, declared ulp divergence) shipped GATE-verified under m5cgate — an implementation delta on the incumbent, not an alpha-theory/Kantorovich alternatives adjudication. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.10/4.10): incumbent seeds RETAINED as base layer; derived Kantorovich radius in z via the [X-TBAK] two-point pattern (new K_RICH role NOTIFIED to C42); V-F7 fold-margin monitor transferred with declared native context; replay-ball coverage falsifier with fresh re-record fallback; the asserted O(h) predictor RETIRED to measured-or-deleted; quiet-band derivation published before first use; F9b scope boundary per the landed C9. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C22
  choice: "Wall-foot search"
  incumbent: "linear index scan, full Newton per attempt, monotone-foot predicate"
  alternatives:
    - "Brent on chord parameter + Shewchuk pre-filter -> derived (N,Nv)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C22 |"
  owner: "F2 (rate lever)"
  note: "annex internal pointer: GAP-28; toc:847-852. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the S25-bis M5c executor compiled the incumbent scan (gate-verified) without weighing the Brent/Shewchuk alternative; no adjudication found"

- id: C23
  choice: "Record-failure policy"
  incumbent: "full record then P4 gate"
  alternatives:
    - "fail-fast early-abort (opt-in, schema'd)"
  status: DECIDED
  evidence: "docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C23 |; annex status verbatim: 'CONVERGED-panel (speed audit H1 design of record)'; annex internal pointer: AC7; annex owner column: 'scheduled S25 item 8'. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C23 = H1/M5b atterrata'): the fail-fast/H1 record-failure design landed in the M-chain speed program (M5b)."

- id: C24
  choice: "Thermo closure"
  incumbent: "[X-THC1] quintic C1 tables (backend-1)"
  alternatives:
    - "NASA-direct (oracle only per C-A)"
    - "dCp-ingest (gated C-B)"
  status: DECIDED
  evidence: "docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C24 |; annex status verbatim: 'CONVERGED-panel (KEEP verdict fresh)'; annex internal pointer: ADVISORY_S24_thermo_closure_survey Sec.8; annex owner column: 'conditions C-A..C-D open as named'. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C24 = C-A scaricata'; findings_registry.yaml:thermotab:C-A-edge-stencil, status DISCHARGED): condition C-A (NASA-direct oracle) discharged in the [X-THC1] carrier, same M3 touch; C-B/C-C/C-D remain open, owner F2."

- id: C25
  choice: "Table box"
  incumbent: "literals 1050/3900 K, N_TAB=8192"
  alternatives:
    - "envelope-derived box + declared margin + per-run certificate"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C25 |"
  owner: "F2 (with C-C/C-D)"
  note: "annex internal pointer: GAP-11. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); partial context found, NOT a comparison: thermo survey refuter attack A4 ('quintic + 8192-point table is over-engineering') judge-REJECTED and condition C-C makes no-joint-in-window a declared per-certificate hypothesis (ADVISORY_S24_thermo_closure_survey_2026-08-12.md par.7-8) — neither weighs the envelope-derived box alternative; the literals remain underived"

- id: C26
  choice: "Out-of-box behavior"
  incumbent: "silent clamp (linear) / silent quintic extrapolation (C1)"
  alternatives:
    - "traced flag + rejector covering BOTH modes"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C26 |"
  owner: "C-D owner F2"
  note: "annex status verbatim: 'NEVER (registered conditional C-D, spec corrected)'; annex internal pointer: AC6. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); thermo survey condition C-D re-affirms the traced box-exit rejector as required repair with owner F2 (ADVISORY_S24_thermo_closure_survey_2026-08-12.md par.8 C-D) — a repair mandate against the incumbent silence, not a comparison run; still unlanded"

- id: C27
  choice: "Constraint aggregation"
  incumbent: "KS-min, rho = K_RICH ln(N)/mu0 (conservativeness-derived)"
  alternatives:
    - "adaptive-KS (Poon-Martins)"
    - "induced-exp/power (Kennedy-Hicken)"
    - "two-constant rule"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.2 Row C27"
  owner: "F2 (with GAP-1); duty F2-DUTY-C27-AGGCOND (subsumes the gapmap R3 pointer; window = findings row margin-governor:G1-three-jump-channels)"
  note: "annex status verbatim: 'single-author (survey answered accuracy axis; conditioning axis NEVER)'; annex internal pointer: GAP-27; AC12. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); DE-bucket panel independently re-verified the ladder/rho arithmetic at the S24 instance (ADVISORY_S24_DEbucket_panel_2026-08-12.md M3, E3+judge) and pipeline-sense R4 classes GAP-27 squarely on-axis — instance verification + classification, not a comparison vs adaptive-KS/induced aggregates. WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED (structure/protocol converged; conditioning axis = measured F2 duty open) (VERDICT_wave1.md par.2.2/4.2): converged half = two-constant rule (derivation repair + ex-ante trigger, arithmetic on derived constants; preprint theorem predicts conflict at depth) + vector near-binding band block via margin_factory as the DERIVED escalation route (panel-designed; tree advocacy 2/4 + diff divergence 3/4) + instance-covering over xi with mandatory post-hoc fine-phase sweep certificate semantics + per-lane multiplier atoms as (ii) carriers under the B-stationarity qualifier. Incumbent RETAINED as fence + G1 surrogate + sole operator where the two-constant check passes; theorems and adopt-or-declare untouched. Alternatives closed by stated reason incl. grouped/regional aggregation (per-group fencing = optional refinement). Protocol arity/cadence/band-width pins + completed falsifier partition land as repaired per the verdict's sustained list. Dependency of record: the C28 KS-max inherits the two-constant rule."

- id: C28
  choice: "Cert-frontier representation"
  incumbent: "binary outside gates only (quantifier bridge falsified)"
  alternatives:
    - "KS-max surrogate inside KKT, gates kept"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#2.1 Row C28"
  owner: "F2 (census row 10); duties [P-CERTKS]+[P-BSTAT] in the GAP-1/GAP-2 window, [P-IPADJ] same critical path"
  note: "annex status verbatim: 'NEVER (successor to falsified K_disc~A_0 absent)'; annex internal pointer: GAP-1. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); pipeline-sense converged R1 re-states of record that no successor quantifier exists after the falsified K_disc~A_0 bridge (ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md par.3.1). WAVE-1 ADJUDICATION 2026-08-19 = adjudicated-split, ledger-enum MIXED (representation converged; adoption-of-record gated on F2 measured half) (VERDICT_wave1.md par.2.1/4.1, prior evidence pointer superseded there): hybrid priced-frontier representation = the M0 tier-ladder KKT-with-margin-multiplier of record + GAP-1 KS-max on traced per-cell certification ratios (derived rho, stratum-local, margin_factory slot; realized exclusion band <= 2 ln(N)/rho reported by the duty); P4/P3(ii) gates kept ABSOLUTE; incumbent binary-only SUPERSEDED as sole representation; solvability regions re-scoped to physical margins (wave-2 territory). The 'no successor' gloss of this row is RETIRED: pipeline-sense R1's full sentence names 'GAP-1's in-KKT certification surrogate' among the named exits (ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md:191-199) — the prior 'adjudicates no alternative representation' reading was imprecise against its own source. Protocol + reporting pins land as repaired per the verdict's sustained-amendment list; Probe B evidence = scratchpad-run toy reproduced by both S24 verifiers, numbers in the committed advisory (NOT a committed carrier). WAVE-2 RIDER (par.4.13, PANEL_C2021 par.3.4): the priced ratio field and the P4 gate consume the SAME unqualified ratio — the wrong-branch mode evades BOTH; the first C28-panel protocol pin wrong-branch discrimination WAITS on C20 Tier-0; [P-CERTKS] may pilot on the unqualified ratio (conditioning side covered by gate disagreement)."

- id: C29
  choice: "Mask freezing granularity"
  incumbent: "rung-frozen + C3 drift gate"
  alternatives:
    - "segment-level re-freeze (P2 rhythm)"
    - "smooth gate sigma(val/eps_gate)"
  status: MIXED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C29 |"
  owner: "F2 re-adjudication"
  note: "annex status verbatim: 'CONVERGED-panel for rung-freeze, alternatives NEVER considered; C3 fired post-adjudication'; annex internal pointer: GAP-13; panel C3"

- id: C30
  choice: "Mask crop policy"
  incumbent: "overlap crop + occurrence counter"
  alternatives:
    - "conservation-of-enforcement invariant (lost-lane count verdict-bearing)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C30 |"
  owner: "S25 one-liner"
  note: "annex internal pointer: GAP-14. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found"

- id: C31
  choice: "Optimizer engine"
  incumbent: "scipy trust-constr (eq-path Sec.4bis-adjudicated; IP path in use since S22)"
  alternatives:
    - "IPOPT"
    - "filter SQP (Fletcher-Leyffer)"
    - "SLQP"
    - "Uno"
    - "proximal-bundle (D6:738, never built)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.1 C31"
  owner: "F2 [P-IPADJ]; adds F2-C31-ENGINE-AB"
  note: "annex status verbatim: 'CONVERGED for eq-path; IP path NEVER'; annex internal pointer: GAP-15; kickoff Sec.4bis. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.1/4.1, panel PANEL_C31TRIO par.4.1 as repaired): engine of record stays scipy trust-constr interior-point as-is under INFORMATION-ONLY multiplier discipline until [P-IPADJ] (first F2 engine act on C28's critical path); flip candidate = Uno (install = O5-class decision); A/B pinned matched-constraint-set with identical-certified-outcomes guard; engine falsifiers one/two/three as defined in VERDICT_wave2 par.1.1; confirm-lens finding 1 ORCHESTRATOR ADJUDICATION OF RECORD (strict option, confirm_repairs_wave2.md): falsifier-two certification is CONDITIONED on the falsifier-one semantics leg PASSING — A/B dominance discharges ONLY the overhead leg, never semantics; incumbent case completed with the V-F30 pro-IP multiplier clause + the 2023-2026 IP warm-start census line as [P-IPADJ] input; gradient-role rider consumed per par.2.10. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C32
  choice: "Curvature policy"
  incumbent: "fresh full FD Hessian per segment, no carry (policy-bound)"
  alternatives:
    - "SR1 carry from certified pairs + stale-symptom re-measure + directional rejector"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.2 C32"
  owner: "G0/T2 review; F2 entry gains [P-QNCARRY] + [P-HESSREJ] (unconditional)"
  note: "annex status verbatim: 'single-author (R-3 cites no alternatives; survey NEVER)'; annex internal pointer: AC10. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); speed audit S1 declines quasi-Newton reuse 'OUT by policy' (defers to the same single-author R-3 policy, no fresh survey) and N5 keeps the exact-Hessian route adjudication-only (ADVISORY_engine_speed_audit_2026-08-12.md par.7.3 S1 + par.4 N5); the SR1-carry alternative was never panel-weighed. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.2/4.2): fresh-FD = interim policy of record; SR1-carry = pinned challenger via [P-QNCARRY] with the arm-B same-lowering pair pin applied AS QUOTED in VERDICT_wave2 par.1.1 IN FULL, incl. the carried-matrix-crosses-segment-boundaries-only-as-a-prior (facet G4) clause (confirm-lens finding 2 landing hygiene); the promotion package NAMES the DIR-RKG scope/falsifier amendment as a landing item; two-instance promotion criterion declared; HVP truncated-Newton = second challenger, N5-gated; chart-migration events = re-record-class boundaries for the carry; [P-HESSREJ] unconditional. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C33
  choice: "Constraint curvature"
  incumbent: "scipy default BFGS (no hess=)"
  alternatives:
    - "exact HVP (fwd-over-rev)"
    - "FD-of-exact-gradient per segment"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.3 C33"
  owner: "F2 duty F2-C33-CONSTRHESS (rides the R2 pilot flag + GAP-1 window)"
  note: "annex internal pointer: GAP-19. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); adjacent facts found, not an adjudication of THIS choice: speed audit S1/N5 declined HVP-as-default for the OBJECTIVE Hessian, and S25-bis diff convergence B-F11 source-verified the route facts (solve is custom_vjp only so jacfwd is blocked as written; central differences non-route) (ADVISORY_engine_speed_audit_2026-08-12.md par.7.3; ADVISORY_S25bis_diff_convergence_2026-08-12.md B-F11) — the constraint-curvature default-BFGS choice itself was never weighed. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.3/4.3): constraint curvature measured at segment base, frozen within segment (R-3 policy extended); default arm = FD-of-constraint-gradient; protocol arm = KS-structured panel-derived arithmetic (judge-re-derived exact); identity-seeded BFGS SUPERSEDED as policy (de-facto until the activity trigger); per-segment budget derivation published before first use; incumbent comeback branch kept. Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C34
  choice: "TR floor/caps"
  incumbent: "TR_FLOOR=1e-3, tr0=0.05, tr_cap=0.25 (S18 clip)"
  alternatives:
    - "noise-derived floor K_RICH*tol_dp/||g|| (Cartis-Scheinberg; More-Wild)"
  status: NEVER
  evidence: "docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED"
  owner: "S25 arithmetic + F2 adoption ([P-TRFLOOR] blocked-with-named-cause)"
  note: "annex row: validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C34 |; annex internal pointer: GAP-4. POST-ANNEX DELTA (docs/rde_nozzle_PROGRESS_ARCHIVE.md#LEDGER-CARRIED, S25 delta: 'C34 [P-TRFLOOR] = blocked-with-named-cause'): input forfeited by the pre-8761dce kill; trigger = first campaign artifact post-8761dce. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); [P-TRFLOOR] remains blocked-with-named-cause (forfeited inputs), no adjudication of the noise-derived floor found"

- id: C35
  choice: "xtol"
  incumbent: "literal 1e-10 at every call site"
  alternatives:
    - "Newton-floor-derived on u scale (derivation exists as prose only)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C35 |"
  owner: "F2 one-liner"
  note: "annex internal pointer: GAP-33. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found (the S18 driver finding 'xtol with KKT open = stale-model collapse', PROGRESS archive, is a symptom record, not a derived-xtol comparison)"

- id: C36
  choice: "Scaling policy"
  incumbent: "Jacobi Dv measured once/walk, objective-only"
  alternatives:
    - "per-segment refresh from diag(H) (free) + constraint-row equilibration (SNOPT)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C36 |"
  owner: "F2 [P-DVREFRESH]"
  note: "annex internal pointer: GAP-17. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the speed audit's DEAD row killed the Jacobi-precond REMOVAL lever of record (ADVISORY_engine_speed_audit_2026-08-12.md par.4 DEAD) — an adjacent keep-vs-drop verdict, not an adjudication of the refresh/equilibration alternatives; [P-DVREFRESH] unchanged"

- id: C37
  choice: "Segmentation trigger"
  incumbent: "any (N,Nv) flip ends segment"
  alternatives:
    - "materiality-gated by replay-fidelity band"
    - "PC^1 active-set anticipation"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C37 |"
  owner: "F2 entry (G0/T2)"
  note: "annex internal pointer: GAP-18. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); speed audit N6 flags the benign-flip segment merge as touching constraint 7 and 'needs its own panel. Flagged, never traded' (ADVISORY_engine_speed_audit_2026-08-12.md par.4 N6) — an explicit refusal to adjudicate without a dedicated panel; alternatives never run"

- id: C38
  choice: "Outcome-II stationarity declaration"
  incumbent: "ratchet exhaustion + raw KKT-open number"
  alternatives:
    - "B-stationarity certificate (convex-hull / surrogate residual)"
    - "normalized reporting"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C38 |"
  owner: "F2"
  note: "annex internal pointer: GAP-2. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); pipeline-sense R4 partition classes GAP-2 'squarely on-axis' (ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md par.3.4) — a classification of the gap, not an adjudication of the B-stationarity-certificate alternative"

- id: C39
  choice: "Multiplier provenance"
  incumbent: "res.v raw (convention closed S24-T1)"
  alternatives:
    - "complementarity band + LSQ re-estimate"
    - "AD post-optimality (Fiacco) via lip elimination"
  status: MIXED
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C39 |"
  owner: "S25 band/counter; F2 LSQ"
  note: "annex status verbatim: 'convention CONVERGED (T1); verification NEVER'; annex internal pointer: GAP-12, GAP-32"

- id: C40
  choice: "Lip equality handling"
  incumbent: "LinearConstraint row"
  alternatives:
    - "variable elimination (N-W Sec.15.3)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C40 |"
  owner: "F2"
  note: "annex internal pointer: GAP-32. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found"

- id: C41
  choice: "Band form/composition"
  incumbent: "K=4 pointwise two-level; ad-hoc sum/max/heterogeneous mixes"
  alternatives:
    - "declared GUM/RSS policy"
    - "three-level GCI"
    - "topology-conditioned (LB-c2)"
    - "running-max envelope (in-house)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C41 |"
  owner: "S25 policy + band pass"
  note: "annex status verbatim: 'NEVER (no composition policy anywhere)'; annex internal pointer: GAP-7, GAP-9, GAP-10. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no composition-policy adjudication found"

- id: C42
  choice: "Safety constant K_RICH role reuse"
  incumbent: "one numeral, 8 roles"
  alternatives:
    - "per-role derivations (GCI Fs on observed p; KS rho from Lipschitz scale; etc.)"
  status: NEVER
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C42 |"
  owner: "R25 F2 audit + census annex"
  note: "annex status verbatim: 'NEVER (registered open row)'; annex internal pointer: AC5 (R25). cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block); the DE-bucket panel re-verified ONE instance arithmetic (rho = K_RICH ln(N)/mu0) — instance verification of a single role, not the per-role derivation audit; no adjudication found. WAVE-1 NOTIFICATION 2026-08-19 (VERDICT_wave1.md par.4.6, no role adjudicated): C27's repaired protocol adds K_RICH roles (near-binding band width; chatter red-line multiplier) to this row's role census; C11's estimator-campaign leg (a) produces the observed-p data this audit consumes. WAVE-2 DELTA (par.4.12): C21's [X-TBAK]-pattern omega surrogate in z = NEW K_RICH role (notification); C31TRIO consumes C27/C28-derived rho constants (no new role)."

- id: C43
  choice: "Asymptotic-range handling at band sites"
  incumbent: "presumed h^p at r=1,2"
  alternatives:
    - "per-site observed-order check (o32 estimator reuse) + NON-CONCLUSIVE propagation"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md#4.5 C43"
  owner: "F2 (aligned with C11; was S25)"
  note: "annex internal pointer: GAP-9. cross-reconciled 2026-08-13: CONFIRMED NEVER — swept the RECONCILIATION corpus (header block), no adjudication of this choice found. WAVE-1 BOOKKEEPING ALIGNMENT 2026-08-19 = adjudicated-with-C11, ledger-enum MIXED (VERDICT_wave1.md par.4.5, consequence of the cluster-C9C11 refutation round): content identity — C43's listed alternative ('per-site observed-order check (o32 estimator reuse) + NON-CONCLUSIVE propagation') IS the C11 interim regime adopted by the wave-1 verdict; measured half = F2-C11-ESTIMATOR-CAMPAIGN leg (a) (GAP-9, both rows' annex pointer). Not a fresh adjudication — a declared alignment; the landing window confirms."

- id: C44
  choice: "FD steps (gradient/Hessian)"
  incumbent: "fixed sqrt(eps)*scale / eps^(1/3)*scale"
  alternatives:
    - "More-Wild noise-aware per-column steps"
    - "seam-aware stencils"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C44 |"
  owner: "F2 (with P-HESSREJ)"
  note: "annex status verbatim: 'single-author (derived-for-smooth; kink-aware NEVER)'; annex internal pointer: GAP-16. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); speed audit S1 DECLINEs approx_derivative's step machinery on constraint-7 grounds (protects the incumbent derived steps by POLICY, not by accuracy comparison) and N8/V7 registers the measured-pattern colored-FD mechanism (ADVISORY_engine_speed_audit_2026-08-12.md par.7.3 S1 + par.4 N8); More-Wild per-column steps never weighed"

- id: C45
  choice: "leggeAree bracket"
  incumbent: "hardcoded 400-point grid [1.01, 2.8]*as"
  alternatives:
    - "box-derived q_max window"
    - "vectorization"
  status: SINGLE-AUTHOR
  evidence: "validation/ADVISORY_S24_sota_gapmap_2026-08-12.md#| C45 |"
  owner: "S25 speed session"
  note: "annex status verbatim: 'single-author (registered: speed audit H2 + AUDIT)'; annex internal pointer: AC16. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); the speed audit MEASURED the vectorization alternative's block gain (444x, H2 bracket-scan = the leggeAree scan per sota_gapmap_raws_2026-08-12/s24_gapv_cell-cert.md:191) but H2 never landed (M5c accepted first-pass) and the box-derived q_max window was never adjudicated (ADVISORY_engine_speed_audit_2026-08-12.md H2) — measurement of one alternative, no comparison verdict"

# --- NEW ROWS DECLARED S25-bis (ledger grows 45 -> 48; docs/rde_
# nozzle_PROGRESS.md R25 delta S25-bis block, 'regola R5-estesa':
# righe nuove dichiarate al commit) ---

- id: C46
  choice: "Padded-lane fill policy for the M5c per-column vmapped bucket executor"
  incumbent: "tail-repeat: padded lanes repeat the last real partner's data (data-only, never accounted, never in the plan)"
  alternatives:
    - "zero-padding with an explicit masked reduction"
    - "NaN-padding + isnan guard on every consumer"
    - "ragged/variable-shape scan (no padding, no fixed bucket bound)"
  status: DECIDED
  evidence: "validation/ADVISORY_S25bis_diff_convergence_2026-08-12.md#tail-repeat padding vs P_DUM: principled"
  owner: "F2 window (unassigned) — residual lever only: the named second-design-bucket lever if padding pressure returns (informational; owner no longer required at DECIDED)"
  note: "PROGRESS census (docs/rde_nozzle_PROGRESS.md R25 delta S25-bis): 'padding tail-repeat (single-author+gate)' — the m5cgate bitwise-identity gate covers correctness of the executor's output (dec-vector, z-band, doctored first-offender, near-seam pair). cross-reconciled 2026-08-13: UPGRADED SINGLE-AUTHOR -> DECIDED — the padding choice WAS put against genuine alternatives and converged: Form-2 fused convergence row B-F2 'tail-repeat padding vs P_DUM: principled' ADOPTED with the merged refuter attack RF-11(iii) FAILED (ADVISORY_S25bis_diff_convergence_2026-08-12.md par.2 convergence table; cost accepted with the named lever, second design bucket, if pressure returns) — the dummy-fill class (zero/NaN padding) adjudicated there; the ragged/no-padding route is separately measured-REJECTED of record (eager cell_scan pattern 0.8x slower + 803 s per-shape compile churn, ADVISORY_engine_speed_audit_2026-08-12.md M5/DEAD rows). Dual proof complete: gate half (m5cgate) + panel/refuter half (B-F2/RF-11(iii)). Prior single-author anchor kept for provenance: validation/PROGRESS_2026-08-12_S25bis_speed.md#tail-repeat the last real partner."

- id: C47
  choice: "Numeric-lint enforcement policy for legacy literals in validation/**/*.py (R28 scope-hole closure)"
  incumbent: "per-file frozen baseline ratchet (validation/numeric_lint_baseline_validation.json): any count INCREASE over baseline fails; any DECREASE also fails until the baseline is ratcheted down; an unbaselined file with literals fails outright"
  alternatives:
    - "full per-file classification into TRIVIAL/ALLOWLISTED/EXEMPTED (the src/ standard already enforced by tests/test_numeric_lint.py)"
    - "blanket exemption of validation/ (status quo ante — the scope hole itself)"
    - "hard fail on any legacy literal, no ratchet (immediate full remediation)"
  status: SINGLE-AUTHOR
  evidence: "tests/test_numeric_lint.py#VALIDATION RATCHET TIER (R28, S25-bis"
  owner: "F2-entry hygiene (per-file classification into the src/ taxonomy; findings registry row test-suite:numeric-lint-scope-hole)"
  note: "PROGRESS census (docs/rde_nozzle_PROGRESS.md R25 delta S25-bis): 'ratchet-baseline come pattern debt-lint (standard di settore, dichiarato)' — declared as an industry-standard debt-ratchet pattern by single-author decision, not a converged panel; DECLARED LIMIT (test_numeric_lint.py docstring): within-count literal swaps still pass, the ratchet is a channel guard not a classification. cross-reconciled 2026-08-13: CONFIRMED SINGLE-AUTHOR — swept the RECONCILIATION corpus (header block); Form-2 fused convergence ratified the ratchet as the strict standard pattern WITH repairs (RF-9/B-F12 ADOPTED, three missing rejector directions added; ADVISORY_S25bis_diff_convergence_2026-08-12.md par.2) — refuter-HARDENED, but the listed policy alternatives were not comparatively weighed; full per-file classification remains the F2-entry owner"

- id: C48
  choice: "Hessian/gradient batching strategy for engine M4: vmap-batched entry (M6) vs sequential per-segment FD, as the DEFAULT execution path"
  incumbent: "sequential per-segment FD Hessian (default, unchanged from the M-chain; nonfinite-lane guard + hess_lane counter shipped on this path)"
  alternatives:
    - "vmap value_and_grad batched entry inside the M4 engine (SOTA, plan as broadcast operands, zero per-segment recompile) — IMPLEMENTED and gate-tested, REJECTED as default"
  status: DECIDED
  evidence: "docs/rde_nozzle_PROGRESS.md#AGGIUDICAZIONE M6 DI RECORD"
  owner: "F2 (adoption = declared version change at a session boundary, m6gate re-formed on the corrected criterion, M3 precedent; candidate opt-in flag A1_VMAP_HESS=1 named in the interim)"
  note: "m6gate FIRED at first implementation (dH 18% of Hessian scale = 5-6x the FD scheme's own asymmetry) -> root cause ISOLATED via three convergent probes (adjoint cross-lowering gradient floor ~1e-8 rel over ~250 implicit solves, FD-amplified ~7 orders; even sequential eager-vs-jit diverges 2.9e-2; the defect was the mixed-lowering formulation, not the batching) -> CORRECTED FORM (base in-batch, one lowering; lane-permutation control bitwise) measured dH 1.68x scheme asymmetry, INSIDE the derived K_RICH bound, batched self-asymmetry better than sequential -> classed ADOPTION-READY. Classed DECIDED (not SINGLE-AUTHOR/NEVER) because the verdict rests on a full converged multi-probe technical adjudication (m6gate + consumer diagnostic + root-cause isolation), not an un-surveyed choice; the residual open item is WHEN to adopt (a session-boundary version-change decision), not WHETHER the current default is justified. [PLACEMENT REPAIR 2026-08-19, declared: this note sat as a stray SECOND note: line under C51 — a duplicate key that OVERWROTE C51's own note for any machine parser; content is C48's (the m6gate verdict), relocated here verbatim in the Blocco-2 landing window.]"

- id: C49
  choice: "Per-phase solution REPRESENTATION for design: fitted-front characteristic marching vs adjoint-consistent shock capturing vs two-tier (capturing explorer + fitted certifier)"
  incumbent: "fitted-front characteristic marching (the S1 piecewise-smooth class with fitted fronts is the certified class of record; capturing never used)"
  alternatives:
    - "adjoint-consistent shock capturing (WENO/DG/entropy-stable FV with smooth limiters) as the sole solver"
    - "two-tier: capturing as exploration/discovery solver + fitted marching as certification solver, |J_captured - J_fitted| promoted to a measured (v) model-residual carrier (the 4/4 de-novo convergence)"
    - "modern implicit shock tracking (HOIST/Zahr; Bonfiglioli-Paciorri unstructured fitting) as the high-order upgrade path"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.8 C49"
  owner: "S-FOUNDATIONS Phase C (Form-2 with a GENUINE captured-only advocate seeded from the four de-novo trees' option blocks; user question 2026-08-17 'perche si fitta una soluzione che potrebbe essere catturata' = the mandate); falls to F2 entry if the Phase C batch splits"
  note: "Minted 2026-08-17 at Phase B: the choice lives in M0 D2.5 (solution class) and was never a LEDGER row — the de-novo diff exposed the omission. The four blind trees each carry the captured option at its genuine best (V-F6/F27, H-F4/F8/F9/F28, O-F9, P-F12) and converge on the two-tier form with the fitted tier as the certificate bearer (adjoint front-motion term = the load-bearing argument, Giles-Pierce class). Nothing decided here: the row exists so the adjudication cannot close by omission. WAVE-2 ADJUDICATION 2026-08-19 = adjudicated-split, enum MIXED (VERDICT_wave2 par.2.8/4.8; ZERO refuter findings on this row): two-tier architecture of record — fitted-front marching = SOLE certificate bearer (five anchor-verified reasons incl. the Giles-Ulbrich held anchor at declared 1-D scalar scope; the ANSWER OF RECORD to the user question 2026-08-17 'perche si fitta una soluzione che potrebbe essere catturata'); captured explorer = named build-gated exploration tier (duty F2/F5-C49-CAPTURE-EXPLORER, not scheduled) with the |J_capture - J_fitted| carrier protocol + four pinned falsifiers; implicit shock tracking = named upgrade path with standing entry gate; fit-vs-capture boundary = DWR-budget policy via the C11 estimator campaign leg (b). Note text of record = the named panel par.4 section AS REPAIRED by VERDICT_wave2 par.1/par.4 (authority chain; no duplication here by anti-entropy calibration 2026-08-19)."

- id: C50
  choice: "Datum-space metric for the interface-data uncertainty contract (acceptance + sensitivity semantics)"
  incumbent: "none — the record has no metric at the contract site (finding contract:datum-uncertainty-contract-missing)"
  alternatives:
    - "L1(A) (blind-PDE lens: TV-metrics ruled out since jump positions are never measured to TV accuracy; weaker-than-L1 ruled out by flux continuity)"
    - "thrust-calibrated flux-weighted L2 + separate L-inf realizability guard (blind-data/UQ lens)"
    - "both, split by role: acceptance metric vs sensitivity metric"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-3 — Datum-space metric"
  owner: "F2 contract window (with finding contract:datum-uncertainty-contract-missing); genuine two-lens disagreement surfaced blind — Form-2 with each lens as its own advocate"
  note: "Minted 2026-08-17 per the blind-contract judge's fourth closing condition; the two blind formalizers genuinely disagree and the record is silent — a textbook Form-2 case with non-incestuous advocates already on file. Cross-reference appended 2026-08-19 (Blocco-2 landing, no adjudication): the acceptance+sensitivity SEMANTICS this metric must serve got their carriers this window — the uncertainty-contract finding row (findings registry, contract:datum-uncertainty-contract-missing) and the contract-diff metric-dispute record it cites; the adjudication itself remains OWED, unchanged."

- id: C51
  choice: "Rung-3a wave-frame solver: implicit BVP (freezing + Newton-Krylov) vs azimuthal MARCHING of the periodic orbit"
  incumbent: "implicit BVP (M0 rung-3a pin: freezing + Newton-Krylov)"
  alternatives:
    - "azimuthal marching in alpha: the blind-PDE formalizer proves the wave-frame problem symmetrizable-hyperbolic in alpha where |u_theta − Omega r| > a — the periodic orbit could be MARCHED (a genuine algorithmic alternative surfaced blind)"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-5 — Wave-frame azimuthal MARCHING"
  owner: "rung-3a implementation window (adjudication owed at implementation time, not before — the judge's own sequencing)"
  note: "Minted 2026-08-17 per the blind-contract judge's fourth closing condition. The hyperbolicity-in-alpha regime condition is the decision criterion's core; outside the regime the marching option dies structurally. INPUT APPENDED 2026-08-19 (S-FOUNDATIONS-C Blocco 2, swirl5f dispatch sec.6 rule 2): swirl5f claim 6 is available as DIRECT adjudication input — azimuthal-march spacelikeness == |w_rel| > c, and its degeneracy loci == the D.18 H-WR/H-NC kernel loci (one geometric object; verifier-CONFIRMED in-panel, status for THIS ledger = advisory input, panel grades never record grades; UPDATE 2026-08-19 close: the panel's §8 verifier verdict LANDED (converged, with three named edits applied to the panel report); the loci identification is verifier-confirmed at the phaseD source, so the input is no longer §8-gated); the dispatch's data-anchored-shadow reading (sec.3 row 13, user catch of record: the per-phase family reconstructs ONLY the data-anchored shadow of helical interior sheets, azimuthally-fed interior front segments are UNREACHABLE by the axial march) STRENGTHENS the marching route's case — route B treats helical sheets natively. The 'CJ locus' naming caveat travels with any consumption (findings row swirl5f:c8-cj-locus-naming-approximate)."

- id: C52
  choice: "Contract status of a MIXED / phase-crossing characteristic signature on Gamma_d (agenda item D-1 of the blind-contract diff): does a phase-migrating sonic line VOID the contract, or merely DOWNGRADE the Verdict?"
  incumbent: "record reading: subsonic patches are a declared case-class with monitors (D1 sec.4.3bis O1-O4); the class stays ADMISSIBLE, H3-cl expected to FAIL on migrating patterns (K-P Fig. 6) with documented Verdict downgrades"
  alternatives:
    - "blind-PDE reading ((P3)): signature must be CONSTANT in phase and position; mixed signature is INADMISSIBLE outright — the sonic set is a free boundary of the prescription problem"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-1 — Mixed/phase-crossing characteristic signature"
  owner: "F2 contract window (the judge's agenda: pin which reading governs; feeds the chi character-map finding row — findings registry, contract:chi-character-map-unknown-band-missing)"
  note: "Minted 2026-08-19 (S-FOUNDATIONS-C Blocco-2 landing) per the blind-contract verdict's fourth closing condition: agenda items to the ledger. Mostly mooted by the L4 default; load-bearing the day the declared subsonic case-class is used."

- id: C53
  choice: "Gamma_d PLACEMENT policy (agenda item D-2): the record fixes Gamma_d without recording the trade; the two blinds DISAGREE WITH EACH OTHER on the preference direction — both monotone pressures must be recorded with the emptiness verdict"
  incumbent: "record: Gamma_d fixed per case with no recorded placement trade"
  alternatives:
    - "blind-DATA: most UPSTREAM station with beta = 0 (transportability pressure: datum least contaminated by the design's own back-influence)"
    - "blind-PDE: admissible band [z_I^-, z_I^+], POSSIBLY EMPTY, with DOWNSTREAM preference (phase-family reduction pressure); the emptiness verdict is part of the deliverable"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-2 — Placement as a band"
  owner: "F2 contract window: a placement-band remark in M0 D2.4 (or this row's adjudication) naming the two monotone pressures and the emptiness verdict — the judge's own suggested carrier"
  note: "Minted 2026-08-19 (Blocco-2 landing) per the blind-contract verdict's fourth closing condition. Genuine blind-vs-blind disagreement: two non-incestuous advocates already on file."

- id: C54
  choice: "Normative status of the mean-state rung I4 (agenda item D-4): ladder rung (record) vs BANNED reduction (blind-data Jensen counterexample — the mean-datum single solve has O(Var) error with NO small parameter); the two are compatible only if I4 carries the O(Var) no-small-parameter warning at its definition site"
  incumbent: "M0 D2.4 keeps I4 as a ladder rung with NO warning at the site — VERIFIED 2026-08-19: the D2.4 ladder line reads 'I4 single mean state.' bare; the Blocco-2 M0 landing did NOT include the one-line annotation, so the agenda item is NOT consumed and this row stays OPEN"
  alternatives:
    - "blind-DATA Claim 3.4: BAN the mean-datum solve as a reduction (reject-only reading)"
    - "compatibilization (the judge's named resolution): one-line I4 annotation in D2.4 carrying the O(Var) no-small-parameter warning — I4 stays a declared ladder rung, never an approximation claim"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md#D-4 — Normative status of the mean state I4"
  owner: "next M0 D2.4 touch (one-line I4 annotation; when the line lands this row flips DECIDED with the M0 anchor as evidence)"
  note: "Minted 2026-08-19 (Blocco-2 landing) per the blind-contract verdict's fourth closing condition; consumption-check against the same-window M0 edit performed and negative (see incumbent)."

- id: C55
  choice: "P_amb aggregation form: how the objective aggregates over a declared operating-ambient interval/measure (mission-weighted mean vs minimax vs CVaR)"
  incumbent: "single-point default P_amb = {Pa0} (slot minted, docs/rde_nozzle_problem_book.md P_amb SLOT OF RECORD 2026-08-19; no aggregation chosen — the default instantiation makes the question empty)"
  alternatives:
    - "nu-weighted mission mean: EXACT-equal to the single evaluation at <Pa>_nu wherever the full-flowing/ambient-blind affinity holds (H-T3.2, T3 Lemma-C class) — the classical trajectory-weighted design"
    - "minimax over [Pa_min, Pa_max]: binds through the admissibility constraints (g_sep, wake fence) at the monotone-worst member"
    - "CVaR_beta / Wasserstein-DRO over the ambient measure (PB-5 machinery extended from mu to the operating envelope)"
  status: NEVER
  evidence: "docs/rde_nozzle_problem_book.md#P_amb SLOT OF RECORD"
  owner: "first multi-point instantiation of P_amb (adjudication owed at instantiation time, not before — the slot's own sequencing clause; until then the single-point default keeps the question empty)"
  note: "Minted 2026-08-19 (S-FOUNDATIONS-C boundary touchpoint, user ratification of parked decision (a): explicit c-slot with single-point default). Structural facts the adjudication will consume: interior per-phase fields are Pa-INDEPENDENT on the certified class (ambient-blind, H-T3.2), F affine in Pa (so mean-aggregation collapses to a point evaluation wherever admissibility is uniform); the envelope binds ONLY through admissibility (separation/wake) and every non-affine regime is outside the certified class (H2' fence) — excluded and declared, never silently averaged. The ambient weight nu is a CONTRACT DATUM with provenance discipline identical to mu."

- id: C56
  choice: "Adjoint REALIZATION of record for the marched stack, per role (gradient / DWR weight / adaptation indicator): discrete AD-adjoint (custom_vjp, one-lowering) vs separately discretized continuous adjoint vs dual-consistent synthesis"
  incumbent: "discrete AD-adjoint everywhere (route fact since S8/S18; never adjudicated as a choice — directive-axis verification 2026-08-19)"
  alternatives:
    - "separately discretized continuous adjoint (characteristic-native, Ancourt/Lozano-Ponsin line)"
    - "dual-consistent synthesis (discrete weight + continuous-structure referee, Hicken-Zingg criteria)"
  status: MIXED
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_C9C11_supplement.md#4.3 Candidate NEW ledger row"
  owner: "weight role CLOSED (C11 supplement, F11d pinned); gradient role closed-consumed 2026-08-19 wave-2 (one consumption of record: PANEL_C31TRIO par.3.1-bis primary + PANEL_C1REP par.3.4-bis merged convergent, VERDICT_wave2 par.2.10 — realization = discrete AD-adjoint, continuous line = frame + referee); indicator role closed by transitivity (f2 = -lambda2, same object). All three roles CLOSED; F11d = the measured residue (owner F2-C11-ESTIMATOR-CAMPAIGN); a fired F11d re-opens this row"
  note: "Minted 2026-08-19 at the C9/C11 landing per VERDICT_C9C11_supplement par.4.3 (sustained-repair re-key of record: id C52->C56 by measured command, grep last = C55; content-dedup vs rows C52 through C55 NEGATIVE twice independently). The dual-consistency residue is the P2 Lemma B par.4.5 SCHEMA with F11d as its estimator-site execution; a fired F11d re-opens THIS row, not C11. Anchors: [X-A1IM]; C48 note (one-lowering); C49 (fitted class). Wave-2 premise guard (PANEL_C2021 par.3.4, VERDICT_wave2 par.4.4): all three roles linearize at the replayed root and inherit the wrong-branch blind spot until C20 Tier-0 lands; F11d verifies the weight GIVEN the root, not root identity — the C20 branch monitor guards F11d's premise at estimator sites."

- id: C57
  choice: "Global exploration tier for the certified design loop: none (local-only of record) vs derivative-free/evolutionary/Bayesian global layer ABOVE the gradient-based certificate closer vs captured-explorer reuse (C49 tier) as the exploration vehicle"
  incumbent: "none — the record optimizes locally from continuation/warm starts; no global layer was ever adjudicated (directive-axis verification 2026-08-19)"
  alternatives:
    - "DFO/BO/evolutionary global layer, exploration-only, certificate closer untouched (trees quarantine advocacy V/O/P)"
    - "captured-explorer tier as the exploration vehicle (C49 two-tier synergy)"
    - "multi-start continuation on the existing branch machinery (V-F26/V-F7 line)"
  status: NEVER
  evidence: "validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave2.md#4.14 Candidate rows handed to the landing"
  owner: "F2-entry adjudication window (interacts with C31 A/B and C49 explorer; user directive axis 4 = adjoint-free at genuine best)"
  note: "Minted 2026-08-19 at the wave-2 landing per VERDICT_wave2 par.4.14 item 1 (PANEL_C31TRIO par.4.5; dedup grep reproduced by refuter and re-verified at mint). Candidates 3 (physical solvability-region derivation program) and 4 (exact-vs-sampled certificate pattern) FOLDED by declaration: item 3 rides the C28 re-scope note (F4b/O1-adjacent, not a C20/F2 blocker), item 4 lives inside the C1 migration duty item (3) — anti-entropy calibration of record. User-ratified candidate experiment (2026-08-20, registered on the user's order as this row's measured arm): one-shot rival-paradigm pilot — an uncertified exploration-first arm (BO/DRL/evolutionary at their genuine modern best, on cheap capturing solves; the C49 captured-explorer tier is the natural vehicle) vs the certified closer of record, on ONE pilot case; deliverables = J found per arm, solve/wall-clock budget per arm, and the downstream cost of CERTIFYING each arm's winner; PURPOSE OF RECORD: converts the constitution-bias residue (certificate-first paradigm = declared axiology, never head-to-head adjudicated — named in the C2 bias audit answer) into a measured comparison, per the dual-proof standard. Ratified as CANDIDATE only: execution and priority are decided at this row's adjudication window (F2-entry), jointly with the S-5F and C49-explorer build decisions."
