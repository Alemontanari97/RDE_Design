__pycache__/
*.pyc
# ad-hoc console redirects (reproducible ephemera; S9 T7 rule)
scratch_*.txt
# GENO/ is an INDEPENDENT git repo (never committed from here; S-ORDINE S1)
GENO/
# third-party PDF binaries: never committed (S-ORDINE S2 declared exclusion; handoff duty 2026-08-13)
literature/*.pdf
literature_review/*.pdf

# Uno/ is an INDEPENDENT git repo clone (C31 A/B flip candidate; never committed from here — same rule as GENO/, 2026-08-20)
Uno/
# [F2/A1 brick 2] environment fixture: GENO lives outside the tree and
# is symlinked in (the march reads its thermo tables and reference
# fields from there).
/GENO

# [F2/A1 brick 2] run caches. These are OUR march's outputs, replayed
# to make re-runs cheap; every one of them is regenerated by the
# carrier that owns it. They are results, not sources.
validation/_config_cmp/
validation/_cycle_ckpt.json
validation/_driver_eps_ckpt.json
validation/_plug_cycle_ckpt/
validation/_plug_spline/

# [F2/A1 brick 2] cached field from the independent Fortran referee
# (8.8 MB). An INPUT rather than a result, but a large binary one:
# regenerate by running the GENO gamma123 case (see the twin carrier's
# docstring) rather than carrying it in the history.
validation/_geno_twin/

# [F2/A1 brick 2] document build products and figure intermediates.
# The figure sources are make_figures.py plus the run caches above.
docs/brick2_doc_src/main.pdf
docs/brick2_doc_src/figs/_*.np[yz]
docs/brick2_doc_src/figs/_build.log
