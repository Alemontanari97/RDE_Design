# PANEL — F2-B0 ENGINE CLUSTER — GENUINE ADVOCATE OF THE ALTERNATIVES

Window: F2-B0, 2026-08-31. Form-2 role: strongest HONEST case per
challenger, so the judge never decides against a strawman. Anchors are
labeled [RECORD file:line] (repo record read this window) vs
[KNOWLEDGE] (2026 landscape, my own knowledge, no repo anchor).
Rules honored: read-only outside this file; no installs; no engine
runs; greps/reads only. Every section ends with the falsifier I
propose AGAINST my own position and the duty (with cost) to pin if my
side wins.

Cluster of record consumed: engine_cluster_ledger_rows.md (C31/C32/
C37/C57/C58/C60 verbatim); decision map SURFACE A E1-E6
[RECORD docs/rde_nozzle_pipeline_decision_map.md:199-205]; G0 decision
[RECORD docs/rde_nozzle_G0_decision.md]; VERDICT_wave2 §1.1/§2.1-2.2/
§4.1-4.2; DOSSIER_uno_fullread §3 (10 arm-B inputs); pipeline_audit:105
+ general_scheme_panel:29; FOUNDATION_ENUM_census_c4 §5.

---

## A1 — C57 (global exploration tier, status NEVER): the case for the tier and for the EARLY pilot

**Position.** Run the user-ratified one-shot rival-paradigm pilot
EARLY in F2 (immediately after the [P-IPADJ]/A-B acts), and let the
standing exploration-tier decision ride its measured outcome. The
motion is not "adopt BO/EA of record today" — it is that the measured
arm the user already ratified must not sit at the back of F2.

**The case.**

1. *The cost objection is STALE by the record's own numbers.*
   Local-only was never adjudicated — the incumbent is literally
   "none — no global layer was ever adjudicated"
   [RECORD validation/f2b0_raws_2026-08-31/engine_cluster_ledger_rows.md:36].
   It congealed when solves were expensive. Post-S25/S25-bis the
   measured clean-host solve is t_solve = 0.116 s and the defnoz
   record fell 100.84 s -> 25.49 s
   [RECORD docs/rde_nozzle_G0_decision.md:167, :188-189]. At 0.1-0.3
   s/solve, a 10^3-10^4-evaluation CMA-ES or trust-region-BO campaign
   is minutes-to-an-hour of compute [KNOWLEDGE: CMA-ES budgets at
   n~10-40; TuRBO/BoTorch batch BO routinely handles n<=100 with
   10^2-10^3 evals]. A competent 2026 DFO practitioner's opening line:
   "your closer is now so fast that refusing to explore is no longer a
   resource decision — it is a prior."

2. *The record already concedes multimodality everywhere except this
   row.* The audit's optimizer row ADOPTS "deflation" and "certified
   B&B at 2-4 DOF" [RECORD docs/rde_nozzle_pipeline_audit.md:105];
   deflated continuation exists precisely to find MULTIPLE stationary
   points [KNOWLEDGE: Farrell-Birkisson-Funke deflation]. Step 7 of
   the scheme is an outer layer over a finite enumerated branch table
   with census re-runs [RECORD docs/rde_nozzle_general_scheme_panel.md:37],
   and the shape-calculus row runs a sector TOURNAMENT for topology
   [RECORD docs/rde_nozzle_pipeline_audit.md:102]. Sector topology +
   deflation-worthy stationary structure + branch tables is the
   textbook profile of a design space that local-from-warm-starts
   under-covers.

3. *The record has a MEASURED local-crawl pathology.* The S20 [C-O33]
   obstruction: the certified driver crawling the certifiability
   boundary, cause adjudicated = CLASS OF DESIGN, certdiag 8/8 genuine
   [RECORD memory s20-adaptive-obstruction; memory s19-o33-campaign].
   A global layer maps certifiable design classes from outside instead
   of crawling one boundary from inside. This is the honest reframe:
   exploration is not only about better J — it is about not letting
   the warm-start lineage silently decide which design CLASS the
   program ever sees. The row itself names this: the pilot "converts
   the constitution-bias residue (certificate-first paradigm =
   declared axiology, never head-to-head adjudicated) into a measured
   comparison, per the dual-proof standard"
   [RECORD engine_cluster_ledger_rows.md:41].

4. *The vehicle needs NO new solver build.* The C49 captured-explorer
   tier is the natural vehicle named in the row [RECORD :41], but the
   pilot is not hostage to the S-5F/C49 build decisions: capturing was
   never used ("capturing never used", C49 incumbent
   [RECORD docs/choice_ledger.yaml:684]) while the FITTED solver run
   in solve-only mode (certificates disabled per step) IS the cheap
   capturing-grade solve at 0.116 s. The exploration arm can run on
   existing machinery today (post-engine-window), decoupling the
   pilot from the C49 build fork.

5. *Priority is the only open question and early wins it.* The pilot
   is ALREADY user-ratified (2026-08-20) as this row's measured arm
   with deliverables pinned: J per arm, solve/wall-clock budget per
   arm, downstream cost of CERTIFYING each arm's winner
   [RECORD engine_cluster_ledger_rows.md:41]. The user directive axis
   4 on this row is "adjoint-free at genuine best" [RECORD :40] — a
   half-hearted late pilot violates the directive twice (not genuine
   best, not decision-relevant). Run early: outcome (a) validates
   local-only cheaply for the whole F2 campaign; outcome (b) re-prices
   the campaign while re-planning is still cheap. Every F2 campaign
   run before the pilot inherits unexplored-basin risk silently.

**Honest costs and risks.** One focused session (spec freeze + runs +
verdict), compute minutes at measured rates, ~2-3 agents. Genuine
risk: the exploration arm's winner may be UNCERTIFIABLE (lands outside
the certified class — the C-O33 lesson), making deliverable 3
unbounded; mitigation: time-box deliverable 3 and count
"uncertifiable at box expiry" as a measured outcome of record, not a
pilot failure. Second risk: a one-case pilot can only ever be
candidate-order evidence (one case, one seed family) — the advocate
does not claim more.

**My falsifier (measurable, proposed).** Pre-register the exploration
budget (N_evals, wall-clock cap). If the exploration arm at its
genuine best finds NO design with J outside the incumbent champion's
certified error bar, AND the incumbent's own multi-start machinery
(C57 alternative 3, the V-F26/V-F7 line [RECORD :37]) reaches the same
basins within the same budget, C57 closes LOCAL-ONLY
CONFIRMED-BY-MEASUREMENT and the tier dies with a number.

**Duty if my side wins.** F2-C57-PILOT, ordered immediately AFTER
[P-IPADJ]/F2-C31-ENGINE-AB (its certified arm consumes the engine
adjudication). Cost: 1 session + minutes of compute. Deliverables =
the ratified row text verbatim.

---

## A2 — C60 (NAND vs SAND/LNKS, status NEVER): the strongest 2026 full-space case

**Position.** A competent full-space practitioner would NOT argue SAND
for the production fitted-marching loop — and the judge should not let
the incumbent win against that strawman. The genuine motion:
adjudicate C60 as a SCOPE PARTITION (NAND structurally forced where
the state solve is a direct march; SAND structurally natural at the
anchor tiers the scheme has ALREADY adopted), plus one cheap hybrid
experiment.

**The case.**

1. *The "two immature solvers composed" objection is stale as
   written.* It dates to the 2026-07-16-era audit
   [RECORD docs/rde_nozzle_pipeline_audit.md:105 "one-shot LNKS now
   (two immature solvers composed — deferred, not dismissed)"] and
   scheme panel [RECORD docs/rde_nozzle_general_scheme_panel.md:29].
   Today one of the two composed solvers is measurably MATURE:
   certified forward march with production gate OPEN
   [RECORD docs/rde_nozzle_G0_decision.md:160-171], exact adjoint with
   a 52/52 derived-tolerance rejector and 1.5% overhead [RECORD
   :32-45, :53], measured curvature machinery (vmapped Hessian M6,
   VJP constraint curvature per C33 [RECORD memory
   s25bis-speed-complete; VERDICT_wave2.md:271-282]). If NAND is
   retained, it must be retained on the TRUE reason (point 3), not on
   a deferral whose stated premise no longer holds — otherwise C60 is
   an unexamined incumbency, the exact no-row-class disease this
   cluster exists to cure.

2. *Where full-space genuinely wins in 2026* [KNOWLEDGE]. When the
   state solve is an expensive ITERATIVE process (pseudo-time steady
   RANS, large implicit systems), one-shot/SAND amortizes state
   convergence across design steps — the classical 3-8x wall-clock
   gains of the one-shot line (Ta'asan; Jameson; Hazra-Schulz
   simultaneous pseudo-timestepping; Biros-Ghattas LNKS I/II), with
   modern full-space KKT solvers (composite-step SQP in ROL;
   inexact-Newton-Krylov with reduced-space preconditioning) making
   NAND literally the PRECONDITIONER of SAND — so the marginal
   implementation cost, given a working NAND stack with exact adjoint
   and Hessian blocks, is genuinely modest, not "immature solver #2".
   Warm-started design sequences (continuation walks) are exactly
   where one-shot shines: the coupled iterate carries over.

3. *Where SAND structurally CANNOT win here — the true retention
   reason the judge should adopt.* (a) The incumbent state solve is a
   DIRECT SPACE-MARCH (C49 incumbent: fitted-front characteristic
   marching [RECORD docs/choice_ledger.yaml:684]): one O(N) sweep with
   embedded per-cell Newton solves. There is no outer state iteration
   to truncate, so SAND's central economy — partial state convergence
   reused across design steps — has ZERO purchase; the
   state-constraint Jacobian of a march is block-triangular, and the
   full-space Newton step on a triangular state block REDUCES to
   forward substitution (the march) + backward substitution (the
   adjoint sweep), i.e. the NAND step. SAND and NAND coincide up to
   ordering on this structure. (b) Certificates are evaluated on
   CONVERGED states — the C60 row's own support ("the practice rests
   on certificates evaluated on converged states"
   [RECORD engine_cluster_ledger_rows.md:56]); SAND intermediates
   carry non-converged states with NO certificate semantics, so a
   full-space trajectory is uncertifiable until its endpoint anyway.
   (c) Segmentation and chart migration (C37: any (N,Nv) flip ends the
   segment; migrations = re-record-class boundaries [RECORD :27,
   :21]) break the "one KKT system" premise mid-iteration.

4. *But the record already stepped INTO full-space territory at the
   anchor tiers — without a row.* Step 2 uses Zahr-Persson implicit
   shock tracking for the wave-frame anchor and Step 4 is space-time
   ISHT (Naudet-Zahr) with unknown (Omega,T) and doubly bordered
   adjoint [RECORD docs/rde_nozzle_general_scheme_panel.md:27, :31];
   HOIST/ISHT-class solvers ARE full-space formulations by
   construction — state and mesh solved as ONE optimization
   [KNOWLEDGE: the HOIST line poses the solver itself as full-space
   optimization]. And C49 names HOIST as the high-order upgrade path
   [RECORD docs/choice_ledger.yaml:688]. So the honest description of
   the program is: NAND in the production loop, SAND-shaped at the
   anchors, boundary undeclared. "NEVER weighed" understates the
   problem — the weighing already happened by adoption, silently.

5. *The cheap live experiment: hybrid terminal polish* (C60
   alternative 2 verbatim: "one-shot warm-started terminal phases"
   [RECORD engine_cluster_ledger_rows.md:57]). At the end of a NAND
   run, a few FULL-SPACE Newton steps on the coupled KKT system —
   every block exists (exact adjoint, M6 Hessian, C33 constraint
   curvature) — to drive the KKT residual below the 7.7e-2
   transversality of record [RECORD memory s18-brick2-closed]; then
   ONE re-march (0.116 s) re-converges the state and the certificate
   is evaluated on a converged state. Doctrine intact at the cost of
   one solve; certificate quality improves by construction.

6. *Forward-looking honesty, cutting FOR the challenger:* the H20 free
   plume boundary p = Pa solve has "no home in the record"
   [RECORD docs/rde_nozzle_pipeline_decision_map.md:183]; a free-
   boundary outer iteration in F4b would be the FIRST genuinely
   iterative state solve in the program — the first place SAND economy
   gains purchase. The partition verdict should name this trigger
   instead of letting F4b re-litigate from scratch.

**My falsifier.** (i) Partition leg: exhibit by construction the
block-triangularity of the assembled march's state Jacobian (cheap,
structural). If it FAILS — e.g. the plug free boundary couples
downstream-to-upstream so the march is not triangular — the "SAND
cannot win on marching" leg falls and a genuine head-to-head becomes
MANDATORY, not optional. (ii) Hybrid leg: if terminal full-space
Newton on one pilot case fails to reduce the KKT residual versus an
equal-eval-budget continuation of plain NAND, the hybrid closes
measured.

**Duty if my side wins.** C60 -> ADJUDICATED-PARTITION: NAND of record
for the fitted-marching production loop WITH the structural reason
recorded (not the stale immaturity line); SAND conditionally-live,
keyed to (a) the C49-HOIST branch, (b) the Step-4 RPO/space-time tier,
(c) the H20 free-boundary trigger — each re-entry via pinned
falsifiers per C58 discipline. Optional F2-C60-HYBRID terminal-polish
spike: half session, reuses existing blocks, ordered after [P-IPADJ]
(KKT residual semantics inherit the multiplier adjudication).

---

## A3 — C58 (stack foundation delta-sweep, status MIXED): each alternative at its strongest, weighed against the entry contract

**Entry contract of record** (the bar every challenger must carry):
the census weighs alternatives against the ADJUDICATED OBLIGATIONS —
C56 one-lowering discrete-AD weight + F11d; C31 A/B +
information-only multiplier discipline; C32/C33 curvature pins; C57
adjoint-free tier; C9/C11 estimator architecture; cross-lowering
gradient-floor discipline — "which foundation best realizes the
ADJUDICATED architecture", not G0's already-answered "can we get exact
gradients affordably" [RECORD engine_cluster_ledger_rows.md:51].
I argue each challenger at its genuine best and CONCEDE where the
obligations kill it.

**(a) Julia/Enzyme (+ 2026 Reactant landscape) — the strongest
challenger.** The honest 2026 case: every major engineering cost the
JAX record PAID is a tracing-JIT pathology. The record itself is the
exhibit: 39.8 s per-evaluation Python re-trace + XLA module blowup
CRASH (S17 [RECORD docs/rde_nozzle_G0_decision.md:144-159]); the
single-bucket-per-phase padded-scan remediation required to make the
outer jit compilable at all (S18 [RECORD :160-171]); per-segment
engine recompile deleted only by the M4 plan-as-args cache, duplicated
compiled executions deleted by M2, record-cost tail M5c (S25 [RECORD
:172-198]). Enzyme differentiates mutating, non-traced native code at
LLVM level — that entire cost class does not exist there; and the 2026
Reactant.jl path compiles Julia to XLA/MLIR with Enzyme gradients,
recovering frozen-executable discipline comparable to jit
[KNOWLEDGE: Enzyme AD; Reactant/EnzymeJAX landscape 2024-2026].
Obligations check, honestly: one-lowering (C48 B-shape, "one frozen
plan / one compiled executable" [RECORD VERDICT_wave2.md:216-218]) —
carryable via Reactant executables or sysimage pinning, but the
executable-identity INSTRUMENTATION (bitwise gates, compiled-execution
counters) must be REBUILT; multiplier discipline — optimizer-level,
framework-neutral; curvature pins (same-lowering secant pairs) —
expressible; cross-lowering floor — instrumentation rebuild again.
Nothing in the obligations list is INEXPRESSIBLE in Julia. The honest
blocker is elsewhere: the pinned flip falsifier (structural T2a
failure) is NOT FIRED — S25 verdict of record: "the flip clause does
NOT trigger and the G0 language decision STANDS" [RECORD
docs/rde_nozzle_G0_decision.md:172-198] — and a flip forfeits a
measured, gate-green, MET stack for un-benchmarked promises
("Julia/Enzyme NOT benchmarked on this host", the standing honesty row
[RECORD :199-201]). Therefore the genuine best ask is NOT a flip:
(i) author the FLIP-EXECUTION PROTOCOL NOW — benchmark spec reusing
the X-LSG0 T1/T2a machinery, obligations-port checklist, lowering-
identity instrumentation list — so that if the armed falsifier ever
fires structurally (the XLA-blowup class is DEMONSTRATED-REAL in this
record), execution takes days, not a crisis re-derivation; (ii) any
install stays an O5-class user-gated env decision — schedule only,
nothing runs today.

**(b) Fortran+Tapenade — concede: fatally wounded by the record's own
epistemology.** Its one genuine attraction is proximity to GENO
semantics. But differentiating GENO (or a GENO-derived twin) COLLAPSES
dual-code independence: GENO is never modified and never assumed
bug-free [RECORD docs/rde_nozzle_G0_decision.md:72-74; memory
moc-critical-independent-invariants: "accordo cross-code != verita"],
and an engine whose adjoint descends from GENO semantics cannot then
be cross-checked against GENO as an INDEPENDENT oracle — X-GENOXC's
entire value dies. A fresh non-GENO Fortran twin is the worst of both
worlds: full rebuild plus the same instrumentation debt as (a) with
none of (a)'s compilation-model advantage. Honest disposition: RETIRE
as foundation, retain only as a possible third differentiation
cross-check route — a role G0 §3 already assigns to Julia
[RECORD :128-130].

**(c) Hand-coded full adjoint — the endpoint argued honestly.** The
record HALF-practices it already: every custom_vjp implicit-function
rule is a hand-derived adjoint at unit-process level [RECORD
docs/rde_nozzle_G0_decision.md:32-45] — the full endpoint hand-codes
the march-level reverse sweep (the Lemma B discrete adjoint) with no
framework at all. Genuine advantages: a line-auditable adjoint
(certification-culture fit — the repo certifies everything else at
source level), zero framework-evolution risk [KNOWLEDGE: JAX API/
ecosystem churn 2024-2026 is nonzero], trivial portability. Honest
costs: forfeits the MEASURED fusion economics (M-chain gains, vmapped
Hessian M6, the (n+1)-VJP constraint-curvature route of C33) for zero
performance motive — the adjoint already costs 1.5% [RECORD :47-58];
every architecture change re-pays hand-derivation; the 52/52 rejector
becomes hand-maintained. Best honest form: NOT a build — an
EXIT-STRATEGY note: verify the hand-derived rule layer is documented
framework-independently in M0 (largely true via Lemma B), so the
endpoint stays reachable at bounded cost if the framework ever forces
an exit. Cost: a documentation-completeness check, ~1 hour, folded
into the same act as (a).

**(d) Gradient-free foundation — concede by construction.** The
obligations list is UNSATISFIABLE for a gradient-free foundation:
curvature pins, cross-lowering-gradient floor, and the multiplier
discipline are gradient-object obligations, and the certificate
closer's KKT transversality IS a gradient statement. Gradient-free's
live content is a TIER above the closer, not a foundation under it —
that case is A1/C57, argued there at full strength. Disposition:
close on the entry contract with the pointer. This is the honest
demonstration that the entry-contract re-anchoring WORKS: it kills
exactly one challenger outright, and that verdict should be recorded
as contract-driven, not taste-driven.

**My falsifier (for the (a) ask, my strongest position).** If, once
the user ever grants the O5 install, a TIME-BOXED Reactant/Enzyme port
of ONE unit process + one segment walk cannot meet T1 (grad/solve <=
4, the X-LSG0 criterion) and cannot reproduce the one-lowering
executable-identity discipline within the box, the Julia case dies and
C58 closes JAX-CONFIRMED, with the shelf protocol as its only residue.
Conversely my no-benchmark-now restraint is falsified by events if a
structural XLA failure arrives WITHOUT a shelf protocol and recovery
costs more than one session — the measurable regret event the shelf
protocol exists to cap.

**Duty if my side wins.** F2-C58-SHELF: flip-execution protocol
authored + obligations-port checklist + the (c) exit-strategy
documentation check. Half a session, zero installs, zero benchmarks —
those stay behind the armed falsifier and the O5 gate. C58 status
moves MIXED -> ADJUDICATED (JAX-primary confirmed against the 2026
landscape, alternates priced and shelved with named triggers).

---

## A4 — C31 [P-IPADJ] + A/B (status MIXED, adjudicated-split): the pro-flip case and the early schedule

**Position.** The flip case (arm B = Uno filtersqp PRESET — preset
wording of record: presets are filtersqp/ipopt only; any funnel
configuration is a DECLARED options-selected variant, not a preset
[RECORD engine_cluster_ledger_rows.md:11;
DOSSIER_uno_fullread.md:182-205]) is genuinely strong on the axes the
record itself declared decision-bearing; and the A/B belongs
IMMEDIATELY after [P-IPADJ] in the same F2 block — not because arm B
is presumed to win, but because every downstream duty consumes
multiplier semantics only this pair of acts can certify.

**The pro-flip case at its strongest (built from the 10 spec inputs
of DOSSIER §3, now contract-entered into the [P-IPADJ] spec
[RECORD engine_cluster_ledger_rows.md:11]).**

1. *Eval-count economics on the RIGHT axis.* Published benchmark:
   filtersqp preset 18.08 vs ipopt preset 48.45 shifted-geometric-mean
   objective evaluations over 429 CUTE problems, sizes NEAR our n~10
   dense per-segment NLPs, logs public; eval-count is exactly the axis
   for our expensive integrator-backed evaluations (panel W4
   vocabulary), and ONLY the eval-count/robustness pattern transfers —
   no timing claim [RECORD DOSSIER_uno_fullread.md:284-295, input 7].
   A ~2.7x published eval-count edge at our size class is real
   candidate-order evidence [KNOWLEDGE: filter-SQP accepts on a
   feasibility/objective filter without a barrier-subproblem
   continuation — structurally fewer evaluations on small dense NLPs].

2. *Certification-culture alignment — the under-argued pro-B axis.*
   Uno terminates in DECLARED CLASSES — feasible KKT / feasible FJ /
   infeasible-stationary / TR-collapse / loose-tolerance — "we check
   for termination, rather than for optimality"
   [RECORD DOSSIER_uno_fullread.md:231-243, input 1], and the
   pi*-scaled FJ formulation makes CQ status EXPLICIT with the full
   primal-dual iterate returned [RECORD :244-253, input 2]. The
   incumbent's multipliers sit under INFORMATION-ONLY discipline
   precisely because their semantics are uncertified pending [P-IPADJ]
   [RECORD engine_cluster_ledger_rows.md:11]. An engine whose exit
   status is a certified class is the repo's native epistemic
   language: the Verdict format wants contour + certificates, and
   termination-class IS a certificate surface.

3. *Warm-start honesty lands NEUTRAL, killing a classic anti-B myth
   AND an anti-A myth simultaneously.* scipy tr_interior_point exposes
   NO warm-start interface — the 19-21 cold restarts are an
   implementation fact of record (RC31T-3
   [RECORD VERDICT_wave2.md:224-238]); Uno documents NO warm-start API
   either, the filter is flushed on mu updates, BQPD's active-set
   estimate lives inside one solve — the arm-B pin FORBIDS assuming
   one [RECORD DOSSIER_uno_fullread.md:254-262, input 3]. So W3 is
   MEASURED on both arms (total evals + restart churn across the RK-G
   segment walk), with the 2023-2026 IP warm-start repair line entering
   as arm-A design input, not as a promise [RECORD
   VERDICT_wave2.md:230-238]. No side gets a mythology.

4. *Concessions into the record (the incumbent's genuine case, which
   I carry so the judge sees it whole).* V-F30's pro-IP multiplier
   clause is genuine and now part of the incumbent case
   [RECORD VERDICT_wave2.md:254-258; engine_cluster_ledger_rows.md:11];
   the preset is NOT filterSQP — 12 CUTE instances lost across the
   mimicry boundary, no transfer claims [RECORD
   DOSSIER_uno_fullread.md:278-283, input 6]; no wall-clock evidence
   transfers [input 7]; 2.2.0 lacks least-squares multiplier
   refinement and dual inertia regularization perturbs the multiplier
   system — arm-B multipliers enter under the SAME information-only
   discipline until measured [RECORD :244-253].

5. *What makes the A/B DECISIVE (I accept all of it as the price of a
   clean flip):* matched constraint set 1:1 realizable at the
   interface (input 4 [RECORD :263-269]); Hessian path ASSERTED with
   degrade-warning runs REJECTED — the silent zero-Hessian degrade
   would fake the A/B (input 5 [RECORD :270-277]); the
   identical-certified-outcomes guard compares TERMINATION CLASS, not
   objective values (input 1 + F-C31-3
   [RECORD VERDICT_wave2.md:194-196]); falsifier-two certification is
   CONDITIONED on the falsifier-one semantics leg PASSING — A/B
   dominance discharges ONLY the overhead leg, never semantics (the
   strict orchestrator adjudication of record
   [RECORD engine_cluster_ledger_rows.md:11]); installed version
   PINNED and presets re-verified at that version — paper 2.2.0 vs
   clone v2.8.0 differ materially (input 10 [RECORD
   DOSSIER_uno_fullread.md:305-309]); decision rule F-C31-2: arm B
   wins W2+W3 at non-worse W1/W4 => flip of record; arm A wins => IP
   path CERTIFIED and the candidate CLOSES
   [RECORD VERDICT_wave2.md:190-194].

6. *The schedule motion (the advocate's real ask at THIS window).*
   [P-IPADJ] is already ordered FIRST — "without it neither curvature
   A/B can interpret its multiplier metrics"
   [RECORD VERDICT_wave2.md:517-519] — and sits on C28's critical path
   [RECORD :500-501]. Order F2-C31-ENGINE-AB immediately after it, in
   the same block, because: (i) an engine swap at [P-IPADJ] re-prices
   C34's rho-histogram accept/reject semantics (edge E6
   [RECORD docs/rde_nozzle_pipeline_decision_map.md:205]) — the later
   the flip, the longer the tail of results to re-price; (ii)
   [P-QNCARRY]/[P-HESSREJ] are ordered after [P-IPADJ] and inherit the
   engine decision [RECORD VERDICT_wave2.md:550-551]; (iii) the Uno
   INSTALL is an O5-class session-boundary user decision — it CANNOT
   run today and I do not pretend otherwise; the motion is to present
   the decision CARD (install, version pin per input 10) at the NEXT
   session boundary so the env decision is taken before the A/B window
   arrives and never sits serial on the critical path.

**Costs, honest.** [P-IPADJ]: ~1 session (source adjudication; the
BHN equation-level anchors are already on disk — barrier law
theta-reduction p.879, E(x,s;mu) Eq.(2.3), least-squares multipliers
Eq.(3.15) [RECORD engine_cluster_ledger_rows.md:11]). A/B: ~1 session
+ compute minutes at measured rates, ONCE installed. Decision card:
minutes. If the user declines the install at the boundary: the A/B
degrades to incumbent-only [P-IPADJ] certification — still worth the
session, and F-C31-1 still armed (semantics fail or out-of-band
barrier-restart overhead => A/B becomes MANDATORY and the install
question returns with teeth [RECORD VERDICT_wave2.md:186-190]).

**My falsifier.** F-C31-2 itself under the pinned guards: arm A wins
W2+W3 at non-worse W1/W4 => the flip candidate CLOSES of record and I
lose cleanly. F-C31-3 guard violation => protocol red — neither arm's
numbers verdict-bearing; I do not keep a tainted win.

**Duty if my side wins.** F2-C31-ENGINE-AB scheduled in-block
immediately after [P-IPADJ]; O5 decision card (Uno install,
version-pinned, presets re-verified at installed version) presented at
the next session boundary; arm-B spec consumes DOSSIER §3 inputs 1-10
verbatim per the entry contract already of record.

---

## A5 — C32/C37 ([P-QNCARRY] + [P-HESSREJ] unconditional): the SR1-carry challenger and the rejector armed for real

**Position.** Make the promotion criteria FIREABLE now
(pre-registration of both instances at spec time), and arm
[P-HESSREJ] with a DEMONSTRATED seeded failure — otherwise the
adjudicated-split quietly hardens into incumbent-forever without a
measurement, the exact starvation pattern the ledger and the
never-postpone directive exist to prevent
[RECORD memory never-postpone-resolvables].

**The challenger's case at its strongest.**

1. *The cost asymmetry is structural, not speculative.* Fresh full FD
   Hessian per segment costs an (n+1)-class evaluation budget per
   segment base — the record derives and publishes exactly this
   per-segment budget form for the constraint-curvature sibling
   (RC31T-8: "(n+1) VJPs PER SEGMENT BASE ... publishes the derived
   per-segment budget" [RECORD VERDICT_wave2.md:271-282]) — and the
   speed program named the vmapped Hessian a first-class cost item
   (M6 [RECORD memory s25bis-speed-complete;
   docs/rde_nozzle_G0_decision.md:196-198]). SR1 pairs from
   already-certified same-lowering gradient pairs cost ZERO additional
   evaluations — secant pairs are byproducts of accepted steps, and
   SR1 is the quasi-Newton update of choice for indefinite
   trust-region curvature [KNOWLEDGE]. The verdict's own words:
   the incumbent "loses only the cost axis and only at matched
   certified outcomes" [RECORD VERDICT_wave2.md:526-528]. A challenger
   that wins the only contested axis BY CONSTRUCTION deserves a
   fireable test, not an indefinite split.

2. *The two-instance criterion fires only if the instances exist on
   paper.* F-C32-2 = eval-count reduction + identical certified
   outcomes + zero rejector trips on BOTH pre-registered instances;
   the two-instance widening is declared-with-reason — "the frontier
   instance is where C28 makes the choice material" (RC31T-7
   [RECORD VERDICT_wave2.md:263-270, :542-547]). Motion: pre-register
   BOTH at the [P-QNCARRY] spec window — instance 1 = the recorded S18
   walk (the frozen WIN-RULE object), instance 2 = the C28-frontier
   instance per RC31T-7. Unnamed instances = a criterion that can
   never fire = challenger starvation dressed as caution.

3. *The challenger accepts the incumbent's full safety envelope — and
   should say so loudly.* Same-lowering pairs ONLY (one frozen plan /
   one compiled executable, C48 B-shape); flip-touched columns
   invalidated; cross-lowering pairs REJECTED at update time (the M6
   incident, dH 18% gate-rejected, is the recorded failure mechanism
   of the class); the carried matrix crosses segment boundaries ONLY
   as a prior (facet G4); chart-migration events = re-record-class
   boundaries; and the promotion package NAMES the DIR-RKG
   scope/falsifier amendment as a landing item — a carry promotion
   cannot land while "re-measured fresh, never carried" stands in a
   registered directive [RECORD VERDICT_wave2.md:198-223, :528-537;
   engine_cluster_ledger_rows.md:21]. The challenger wins, if it wins,
   INSIDE the incumbent's own discipline. That is what makes the split
   resolvable rather than theological.

4. *C37 coupling — run the census early because the carry consumes
   it.* The wave-3 adjudication gives F-C37-1 a MERGE-FREE benign
   proxy (benign = flips whose certified-class (S,xi) labels are
   unchanged across the boundary and/or whose discarded-Hessian
   columns are lip-only), F-C37-2 licenses the merge on the
   proxy-flagged subset ONLY, and a proxy-vs-census disagreement
   RETIRES the proxy [RECORD engine_cluster_ledger_rows.md:31]. Benign
   boundaries are exactly where the carried-prior mechanism (facet G4)
   is safest: motion — order F2-C37-FLIPMAT early enough that its
   measured benign/malign labels feed [P-QNCARRY]'s boundary-crossing
   prior instead of leaving it on assumptions. The census is
   executable merge-free by construction of record; there is no
   dependency excuse left [RECORD :31 "the F-C37-1/2 dependency cycle
   is broken"].

5. *[P-HESSREJ] must be a rejector, not a declaration.* R5 of the
   codebase protocol: tests must be able to REJECT. The record's own
   negative-control culture is the standard — the corrupted-vjp
   control in X-G0, the 1%-corrupted child and wrong-pairing controls
   in X-GENOXC (100% -> 0%, 84x
   [RECORD docs/rde_nozzle_G0_decision.md:36-38, :100-104]). An
   UNFIRED rejector guarding the "zero trips" promotion leg is
   epistemically vacuous: zero trips is evidence only if a trip was
   demonstrably POSSIBLE. Motion: the [P-HESSREJ] acceptance gate
   includes a SEEDED-FAILURE demonstration — inject a deliberately
   poisoned carry (stale curvature carried ACROSS a
   chart-migration/re-record boundary, the declared invalidation
   class; and/or a corrupted secant pair) and demonstrate the trip. A
   rejector that cannot be made to fire on the seeded case FAILS its
   own acceptance, and no carry instance can be evaluated until it is
   repaired. This is armed-not-declared, at the cost of one synthetic
   case.

**My falsifier.** The pinned set fires against me cleanly: F-C32-1
(carry degrades acceptance or trips the armed rejector => C32 CLOSES
on fresh-FD, converged), F-C32-3 (bit-compare fails => carry REJECTED
outright), F-C32-2 unmet on either pre-registered instance
[RECORD VERDICT_wave2.md:542-549]. Note the asymmetry I accept: if the
SEEDED failure cannot be made to trip, my rejector motion has proven
the spec vacuous and the CARRY side loses its evaluability — my own
arming demand can kill my challenger. That is the honest shape.

**Duty if my side wins.** [P-QNCARRY] spec gains: (i) both instances
pre-registered AT SPEC TIME (paperwork, minutes); (ii) the
seeded-failure demonstration as an ACCEPTANCE GATE of [P-HESSREJ]
(half session inside the build; poison injection reuses the existing
record/replay machinery); (iii) F2-C37-FLIPMAT ordered early so its
benign labels are available to the carry prior. Order preserved:
after [P-IPADJ] per the verdict [RECORD VERDICT_wave2.md:550-551].

---

## A6 — SDP-CAND-8 (mint-or-retire): adjudicated on consumers, argued honestly

**Position: MINT-AS-DORMANT** — with the consumer finding stated
inside the row itself. Retire-by-omission is the one outcome the
census forbids; a plain retire is defensible ONLY if the panel also
records where the user decision of record then lives.

**The consumer audit (measured this window — the brief's question
answered plainly).**
- The S17 decision is a USER decision of record: "DECISIONE UTENTE:
  solver SDP = Clarabel primario, MOSEK accademico fallback (lock
  BLOCCATO sciolto)"
  [RECORD docs/rde_nozzle_PROGRESS_ARCHIVE.md:956-962].
- Its ONLY ever-named consumer is the head-to-head SOS experiment,
  queued in the S16/S17-era CODA text
  [RECORD docs/rde_nozzle_PROGRESS_ARCHIVE.md:1849-1852, :1899,
  :1935 "head-to-head SOS (gate env: serve un solver SDP)"].
- That consumer did NOT survive into the living plan: current
  PROGRESS carries SDP only inside the F2-B0 engine-window listing —
  the mint-or-retire act itself
  [RECORD docs/rde_nozzle_PROGRESS.md:33-34]; greps for
  "head-to-head", "bound ladder", "SOS" in current PROGRESS = 0 hits
  (measured this window, SR-12). The ROADMAP's only SDP hits are the
  SDP-CAND-8 node rows [RECORD docs/ROADMAP_critical_path.md:39,
  :108].
- The audit REJECTS moment-SOS twice with cause — "non-polynomial
  data, SDP scale explosion" and the global-optimality row's "SOS (as
  above)" [RECORD docs/rde_nozzle_pipeline_audit.md:105, :107]; the
  adopted global machinery (M1-M5 ladder; certified B&B at 2-4 DOF)
  states no SDP dependence anywhere.

**So, said plainly per the brief: NOTHING in the living F2-F3 plan
consumes an SDP solve.** The object is consumer-extinct as of this
window.

**Why mint anyway — the genuine case.** The no-row class discipline is
about DECISIONS, not consumers: "the ledger's function is that the
question cannot close by omission"
[RECORD validation/sfoundations_raws_2026-08-13/blocco3/FORK_LEDGER_141_adjudication.md:16-18],
and the census records this very candidacy "so the question CANNOT
close by omission" [RECORD FOUNDATION_ENUM_census_c4.md §5 (:102), §6
(:108-113); seven instances of the class caught to date]. A user decision of record that exists
only in an append-only archive and a memory file IS the class instance
— the same state C49/C56/C58/C60 were caught in, and this would be the
8th catch per the census's own counting [RECORD :102 "if the F2-entry
census confirms the practiced-incumbent + live-alternatives shape, it
mints (8th instance); if the SDP layer is retired from the F2 stack,
it dies with a named reason"]. Note the census's mint condition is
actually NOT met in its first branch — the incumbent is DECIDED but
NOT PRACTICED (no SDP solve exists in the repo) — which is exactly why
the row must be minted DORMANT rather than active, or retired WITH the
named reason. Mint costs minutes; a silent retire costs some future
session the same archaeology performed here.

**The row I would write** (id = next free at the mint window, verified
by measured grep):
- choice: "Certificate-layer SDP backend (SOS/semidefinite certificate
  solves)"
- incumbent: "Clarabel primary + MOSEK academic fallback — user
  decision of record S17 2026-08-06 by SOTA survey
  (docs/rde_nozzle_PROGRESS_ARCHIVE.md:956-962); NEVER EXERCISED: no
  SDP solve exists in the repo; only living reference = the F2-B0
  mint-or-retire node (measured this window)"
- alternatives: ['SCS (first-order conic, warm-startable, scales past
  IP-accuracy limits)', 'COSMO / chordal-decomposition class for
  structured SDPs', 'MOSEK commercial license (beyond academic)',
  'retire the layer (moment-SOS rejected-with-cause,
  docs/rde_nozzle_pipeline_audit.md:105/:107)']
  [KNOWLEDGE: 2026 conic landscape — Clarabel (Rust interior-point) is
  the current OSS accuracy default at small-medium scale; SCS for
  scale; chordal decomposition for structured problems]
- status: DORMANT-DECIDED (decision of record; consumer extinct in
  the living plan — search-proven, greps quoted in the note)
- falsifier / revival trigger: any duty that POSES an SDP — a
  bound-ladder rung, a C20-class certificate form, a B&B bound — 
  re-arms the row at that duty's window; if NO F2-F3-scope duty names
  an SDP solve by the F3.TOURNAMENT window, the row RETIRES with
  reason "consumer extinct" and no conic stack is ever installed for
  it
- owner: F2-B0 (this window) for the mint; the revival trigger owns
  the rest.

**My falsifier.** The retire side beats me if it exhibits a POSITIVE
scoped exclusion of record — e.g. a C20 adjudication enumerating the
admissible certificate forms exhaustively and excluding semidefinite
ones — rather than mere absence. Then the honest outcome is
retire-with-named-reason, and the S17 user decision gets its
of-record annotation in the retirement note instead of a row. Absence
alone (what I found and reported) does not clear the no-row-class bar,
because absence is exactly what every caught instance looked like
before it was caught.

**Duty if my side wins.** Mint at this window (minutes; row text
above, id and greps re-measured in-window per SR-12). NO install, NO
benchmark, NO SDP work of any kind rides the mint — DORMANT means
dormant.

---

## CLOSING NOTE (advocate's self-limits)

Every [KNOWLEDGE] item above is 2026-landscape argumentation offered
for the judge to weigh, not evidence of record; where a [KNOWLEDGE]
claim would become load-bearing in a verdict, it must be demoted to a
census line with its own procurement/verification duty (litreview
protocol tiering [RECORD memory litreview-verification-protocol]).
No position above requires installing anything today, running the
engine, or touching the pinned environment; every schedule motion
routes O5-class decisions to the user at a session boundary, as
required.

