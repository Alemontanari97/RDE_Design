# VERDICT — F2-entry ENGINE CLUSTER (SURFACE A), F2-B0 2026-09-05

Form-2 panel of record: genuine advocate (PANEL_engine_ADVOCATE.md) +
refuter (PANEL_engine_REFUTER.md: 1 KILL / 11 WEAKEN / 13 NOTE), judge =
the F2-B0 orchestrator (this file). Inputs: ledger rows C31/C32/C37/C57/
C58/C60 (engine_cluster_ledger_rows.md, verbatim), decision-map SURFACE A
(E1-E6), G0 decision doc, VERDICT_wave2/wave3, DOSSIER_uno_fullread §3,
FOUNDATION_ENUM_census_c4 §5. Authority chain: the ledger notes cite THIS
file; panel texts are positions, not record.

CROSS-CUTTING CLAUSE (REF-6/REF-10, binding on every row below): the
derived roadmap re-consumes C58/C60 inside F2.REPR ("stack C58/C60 ...
decided here"). Every SURFACE-A verdict below is therefore
**REPRESENTATION-CONDITIONAL**: a 2.5-D/3-D outcome at F2.REPR re-enters
the affected row through its own pinned falsifiers — the re-entry
trigger is DECLARED here once and inherited by all six rows.

## V-C58 (was MIXED) — JAX-primary REAFFIRMED at the 9(e) delta level; ROW REPAIRED

1. **REF-1 (KILL) accepted — the row's central support was FALSE as
   written.** "loop-speed falsifier ARMED and subsequently NOT FIRED" is
   contradicted at three anchors (claims X-TOCV; glossary "T2 fired";
   M0:3280). History of record: T2a FAILED-as-implemented at S17
   (production gate closed) → T2 FIRED at S18 (121.6 s vs 35.9 s;
   decomposition = curvature + re-record cost, NOT language throughput;
   T1 = 1.593 and T2a = 0.116 s both PASS) → root-caused → remediated
   (M-chain, record 100.84 → 5.58 s) → G0/T2 review CONSUMED at S25
   with NO FLIP and T2 re-priced. The falsifier **stays ARMED** (T1 > 4,
   or a structural T2a failure on the production path). The repaired
   support is STRONGER than the false one — fired-and-survived beats
   never-tested — but the row must say it. REF-2 accepted with it: the
   honest overhead figure is grad/solve = 1.593 at engine scale (the
   1.5% figure is the unit-process microbenchmark with its own declared
   caveat).
2. **Verdict**: incumbent JAX-primary REAFFIRMED at this window on the
   corrected support base. Status stays MIXED (adjudicated-split:
   Julia/Enzyme = protocol-on-the-shelf challenger behind the armed
   falsifier — advocate A3 and refuter agree it has never been
   benchmarked on this host, G0's own honesty row).
3. **The 9(e) delta-sweep contract** (what the census must cover beyond
   G0, REF-5 verbatim, now the duty's content): (a) Julia/Enzyme
   never-benchmarked leg; (b) hand-coded-adjoint and gradient-free
   never weighed by G0 (gradient-free adjudicates only via the
   re-anchored census / C57); (c) post-G0 obligations realization check
   per framework (C56 one-lowering, C31 multiplier discipline, C32/C33
   pins, C48, CLG floor); (d) float64/mixed-precision sub-axis (owned
   by C58 per census §3, never adjudicated by G0); (e) JAX-specific
   structural-cost class (XLA re-trace, padded buckets, plan-as-args,
   vmapped-Hessian M6) weighed cross-framework; (f) env currency
   (jaxlib cp313 GIL wheel N7, numpy O5); (g) GENO interop leg
   re-verification post-B-GENO (quarantine DONE this session, build
   green — the leg is now re-verifiable). Owner: the C58 census duty,
   executed at/after F2.REPR (representation-conditional clause).
4. REF-3 noted of record: X-G0/X-GENOXC are dated July-2026 on-demand
   passes, not standing CI; the staleness gate guarding them was
   P0-defective and is FIXED this session (closure-aware + envfp).

## V-C60 (was NEVER) — OUT OF NEVER to MIXED: ADJUDICATED-PARTITION

1. The stale deferral line "two immature solvers composed" is RETIRED
   as support (REF-7: dated refereed deferral, premise half-expired —
   the reduced stack is now mature/certified). The incumbent's real
   support is STRUCTURAL, and the partition (advocate A2, surviving
   REF-8) is adopted:
   - **NAND of record for the fitted-marching production loop**, with
     the structural reason recorded: the assembled march's state
     Jacobian is block-triangular in x (x-as-time causality), and the
     ENTIRE certificate stack is defined on CONVERGED forward states
     (per-cell undamped-step certification at
     validation/a1_toc_variational_jax.py:941, :950, :364, :1000 —
     REF-9's pin; the wave-3 anchor "a1:433-448" has DRIFTED to M5c
     code and must not be cited). Under SAND, intermediate iterates
     are not converged states: per-iterate certificate semantics would
     need re-derivation. Terminal-phase certification survives.
   - **SAND conditionally-live** with three NAMED re-entry triggers,
     each with an existing hook: (i) the C49-HOIST captured/implicit
     branch (a genuinely coupled solve class); (ii) any RPO/space-time
     tier; (iii) the H20 free-plume boundary solve — the FIRST
     genuinely iterative outer state solve in the program (advocate
     item 6; decision-map :183 "no home in the record") — F3.PLUG must
     weigh SAND economy THERE instead of re-litigating from scratch.
   - **Optional spike F2-C60-HYBRID** (terminal full-space polish:
     a few coupled-KKT Newton steps at the end of a NAND run + ONE
     re-march to restore converged-state certificate semantics),
     half-session class, ordered AFTER [P-IPADJ] (KKT residual
     semantics inherit the multiplier adjudication). Not scheduled —
     a named lever.
2. **Partition falsifier** (advocate's own, adopted): exhibit the
   block-triangularity of the assembled march's state Jacobian by
   construction (cheap, structural). If it FAILS at any tier (e.g.
   the plug free boundary couples downstream-to-upstream), the
   "SAND cannot win on marching" leg falls and a head-to-head becomes
   MANDATORY. Hybrid leg falsifier: terminal polish must beat an
   equal-eval-budget NAND continuation on the pilot case, else the
   hybrid closes measured.
3. REF-10 honored: no head-to-head at the current n~10 axial scale
   (it would measure nothing decision-relevant); the question
   re-prices at the F2.REPR representation outcome (cross-cutting
   clause).

## V-C57 (was NEVER) — OUT OF NEVER to MIXED; INCUMBENT STATEMENT REPAIRED; PILOT PINNED

1. **REF-11 accepted — incumbent statement repaired**: "no global layer
   was ever adjudicated" is false as written. Of record: D7 (refereed,
   2026-07-16) WEIGHED deflation / DIRECT-B&B / moment-SOS and ADOPTED
   "deflation; certified B&B at 2-4 DOF" (moment-SOS rejected with
   cause); M0:3000 = M4 deflated-continuation enumeration + bound gap.
   The TRUE gap: no LEDGER row, and never a head-to-head vs the modern
   DFO/BO/evolutionary tier; alternative 3 (multi-start continuation)
   is essentially the already-adopted M4 posture.
2. **Verdict**: local-only + M4/tournament line REAFFIRMED as the
   incumbent APPARATUS (declared, SCHEMA-level — REF-12: it may be
   cited as the incumbent's declared machinery, NEVER as executable
   evidence; nothing global is built).
3. **The user-ratified one-shot rival-paradigm pilot**: scheduling
   DECIDED here per its own ratification text (REF-13): execution +
   priority are decided at F2.REPR, JOINTLY with the S-5F and
   C49-explorer build decisions (the ratification's joint clause
   structurally forces that table; deciding at F2-B0 alone would
   BREACH it, as would pushing past F2.REPR). Advocate's cost update
   stands: post-S25 solve cost (0.116 s) kills the old expense
   objection; the C49 captured-explorer tier is the natural vehicle.
4. Status: NEVER → MIXED (incumbent apparatus + pinned measured arm
   with decision table at F2.REPR).

## V-C31 / [P-IPADJ] (stays MIXED) — SPEC EMENDED AT ITS OWN WINDOW

1. **REF-14 accepted (spec repair)**: F-C31-1 leg 2 referenced
   "existing derived bands" for barrier-restart overhead that DO NOT
   EXIST. Re-worded of record: "outside a band DERIVED IN-WINDOW from
   the S18/S24 walk records at the [P-IPADJ] act" — deriving that band
   is [P-IPADJ]'s own first sub-duty.
2. **REF-16 material catch — landed as a dated DELTA on the existing
   critical row constraints:multiplier-rejector-missing** (same code
   span; the anti-re-mint rule of lint xix routed it there, no new row):
   margin_governor.py:620-622 reads mu_est = +vlist[-1][0] (OPPOSITE
   sign to the S24 res.v convention of record mu(M0) = −res.v[-1][0])
   and :645-649 feeds mu_dual_clause whose result GATES the carrier
   verdict; plus REF-17: the :638 mu_use = 0.0 forcing makes the
   inactive-margin dual check a tautology at the call site — the S24
   +4.86e4 smearing symptom can never fire it. The "information-only
   multiplier discipline" is 2/3 practiced on disk and NOT enforced by
   any assert/lint. [P-IPADJ] consumes this finding as input; the
   repair is its first code touch.
3. REF-15 honored: F-C31-2 not fireable until Uno lands — **O5-class
   user card at the next session boundary** (install decision), A/B
   in-block AFTER [P-IPADJ]. REF-18 of record: the BHN equation-level
   anchors are ON DISK and EXACT (p.879 theta-law; Eq. 2.3; Eq. 3.15 +
   "just barely" sentence) — leg-A source adjudication is executable
   today; paper-on-disk ≠ scipy-conformance is [P-IPADJ]'s first act.
4. Verdict: C31 stays MIXED (adjudicated-split unchanged); [P-IPADJ]
   spec EMENDED as above, remains the first F2 engine act on C28's
   critical path.

## V-C32/C37 — [P-QNCARRY] + [P-HESSREJ] + F2-C37-FLIPMAT CONFIRMED UNCONDITIONAL, with teeth

1. Seeded failures NAMED (REF-20, adopted as the arming spec):
   [P-HESSREJ] must flag (i) an FD column straddling a knot/plan seam
   and (ii) an injected pre-symmetrization asymmetry — if the built
   rejector passes either, it fails its own negative control.
   [P-QNCARRY] must REJECT a cross-lowering secant pair at update time
   (acceptance = spec violated). F-C32-3: two-run bit-compare of the
   carried matrix (nondeterminism fires outright).
2. **REF-21 asymmetry DECLARED of record**: the SR1-carry can LOSE
   today (negative branches fire on the S18 instance alone) but cannot
   WIN today — promotion requires the C28 frontier instance, which
   does not exist yet (F2.ENGINE window). The advocate's ask
   (pre-register both instances at spec time) is adopted.
3. **REF-22 landing line adopted**: the FLIPMAT merge-free census is
   executable-after-instrumentation, not today: leg 1 needs (S,xi)
   loci logging that exists in NO committed .py; leg 2 (lip-only
   discarded columns) carries the census ONLY IF segment records
   retain per-column Hessian data across re-record events (to verify
   at first execution). The census duty line now names which leg
   carries it and the instrumentation gate. Cycle break REAL (REF-23).
4. REF-19 disambiguation: the "G0/T2" token in both owners = the
   F2-entry review window of THIS session chain (the queued S18 review
   was consumed at S25); the duties execute at F2.ENGINE after
   [P-IPADJ], their specs confirmed HERE.

## V-SDP-CAND-8 — RETIRED WITH NAMED REASON (mint declined)

Judge's call between advocate MINT-AS-DORMANT and refuter RETIRE:
**RETIRE.** Grounds: (i) consumer extinct SEARCH-PROVEN in the living
plan (refuter greps: 0 ledger hits, no imports, certificate stack =
Lanczos/KP not SDP, premium_bound = theorem device not SDP, SOS
head-to-head survives only in PROGRESS_ARCHIVE); (ii) the advocate's
own bar ("absence alone does not clear the no-row-class bar") is met
by MORE than absence: moment-SOS was POSITIVELY rejected-with-cause
(D7 :105 "SDP scale explosion") — a scoped exclusion of the one
in-plan consumer class; (iii) the no-row-class analogy fails: the five
caught instances (C49/C56/C58/C59/C60) were PRACTICED foundations
without rows — SDP is practiced nowhere. The S17 user decision
(Clarabel primary + MOSEK academic fallback) is hereby ANNOTATED of
record in this retirement: it stands as the pre-decided solver choice
IF the layer ever revives. **Revival triggers** (REF-25, any one
re-arms as a fresh mint at that duty's window): (a) the SOS
head-to-head re-queued and executed; (b) moment-SOS/premium-bound
revival at F3.TOURNAMENT (needs the D7 rejection overturned first);
(c) a certificate re-formulation adopting LMI/SDP relaxations (e.g.
if the Lanczos/KP form fails its [P-BSTAT]-window falsifiers). Tool
consequence: the SDP-CAND-8 node override flips to
OUT:retired-with-named-reason citing this verdict. NO install, NO
benchmark, NO conic stack.

## Duties emitted by this verdict (owners named)

| duty | owner window |
|---|---|
| C58 row repair (REF-1/REF-2 text) + 9(e) delta-list (a)-(g) as census contract | ledger edit NOW; census executes at/after F2.REPR |
| C60 partition entry + structural pin (a1 lines) + 3 SAND triggers + hybrid lever | ledger edit NOW; block-triangularity falsifier = first C60 citation |
| C57 incumbent repair + pilot decision table pinned to F2.REPR | ledger edit NOW; F2.REPR agenda |
| [P-IPADJ] spec emendation (restart band derived in-window; mgov finding as input; O5 Uno card to user at next session boundary) | ledger note NOW; act = first F2 engine act |
| mgov sign/tautology delta | landed NOW on constraints:multiplier-rejector-missing (findings registry), consumed by [P-IPADJ] |
| [P-QNCARRY]/[P-HESSREJ]/FLIPMAT arming spec (seeded failures; win-asymmetry; instrumentation gate) | ledger notes NOW; execute F2.ENGINE |
| SDP-CAND-8 retirement (tool override + this verdict) | NOW |

Every verdict above is representation-conditional per the cross-cutting
clause. Nothing here installs packages, runs the engine, or touches the
pinned env.
