# SESSION LOG — F-SERVICE RATIFICATION WINDOW (Part 1 of the
# F-SERVICE + S-CERT double session; contract =
# validation/ADVISORY_Fservice_Scert_prompt_2026-08-13.md, executed
# integrally; census row R35; chain of record: commits 1e188a9 +
# e3cfab3, user-ratified 2026-08-13)

Date: 2026-08-13. Opening HEAD: e3cfab3 (verified, tags
[F-SERVICE/S-ORDINE], no foreign commits; untracked debris = the S11
adjudicated set, untouched). Branch rde-nozzle-program.

## (0) OPENING (R2) + pre-execution gate

Pre-reads executed in the handoff order: memory (6 named memories) ->
PROGRESS ORA + census (R32 = CONSUMED-with-declared-split VERIFIED —
precondition met) -> ASSESSMENT_methodology_position_2026-08-13.md
INTEGRAL -> ADVISORY_litreview_confrontation_2026-08-13.md (§3.1,
§3.6, §3.11, §3.15, §3.17, §3.20, §5 ADOPTED A1-A39, §6 corrections,
§8 duties) -> literature_review/reports/REFUTE_C_cone_form.md
(the 5-carrier table with line-verified evidence) ->
docs/findings_registry.yaml + docs/choice_ledger.yaml (dedup
authority).

PRE-EXECUTION GATE VERDICT (logged): plan-adherence PASS (window =
the user-ratified F-SERVICE insertion between S-ORDINE and S-CERT);
upstream-rigor PASS with declared limit — C30 gate R28-lit VERIFIED:
Shmyglevskii PMM 26(1) 1962 = WANTED (literature registry row
wanted_shmyglevskii_1962, unread), so C30 executable ONLY in the
gated "classical structure" form.

## (1a) USER RATIFICATIONS (all three ratified 2026-08-13, executed
## immediately, registry rows same window)

- **D-01 RATIFIED + EXECUTED**: the gloss identifying the classical
  optimality residual with the adjoint gradient DELETED of record at
  docs/rde_nozzle_literature_map.md (item 1 of the in-house corpus
  block + G14 row) and docs/rde_nozzle_theorem_ledger.md §8; claim 1
  (P2/G14) rewritten with the §3.1 record block (three claimed forms;
  generic equivalence + in-school Route B -> Route A chain
  EXPLICITLY CONCEDED, KT2015 (2.2)->(2.9)->(2.10)->(2.12); residual
  novelty = articulation + operationalization). Class unchanged
  (query-bounded). Findings row:
  litreview:glossa-residual-adjoint-gradient (DISCHARGED).
- **C31-minimale RATIFIED + EXECUTED**: T7(c) rewritten in the
  refuter-surviving MINIMAL cone form (ONE VI-statement
  D in N_K(s_E*), K declared per statement/instrument; mass flow
  stays an EQUALITY outside the cone) in M0 Part III, with: the CONE
  TRANSFER LEMMA [T-T7CN] (THEOREM; converse false, two-phase
  counterexample of record), the axial-vacuous/radial-substantial
  content split, the twin boxed warning ("the weight does not deform
  the cone"), the Route-A graft lambda_L = Int(-lambda3/q) dmu (the
  [T-P3] length twin), and the C32 falsifier regime-qualified (signs,
  not roots). Mirrored: M0 VI.3 CSTR_PA/PB cone re-statement (regime
  declared in every printed (**') residual), M0 fixed-eps bookkeeping
  NAMED regime 2 (the refuter's point: an embryonic regime
  declaration already existed at M0's FIXED-EPS block — extended, not
  contradicted), docs/rde_nozzle_P1_sections_5_7.md §5.1(c) + boxed
  warning twin. Registry rows: T-T7CN (claims, THEOREM), findings
  litreview:t7c-equality-wrong-regime (DISCHARGED).
- **C30 RATIFIED GATED + EXECUTED**: the classical attribution
  formula of record + the DECLARED ASYMMETRY (they change class, we
  price staying in tier 0) landed at the M0 ladder block (GENERAL
  FORMALIZATION OF RECORD); upgrade "structure" -> "system" stays
  GATED on reading Shmyglevskii 1962 (R28; P0 procurement row).
  Findings row: litreview:tier-ladder-attribution-c30 (DISCHARGED).

## (1b) R4 BATCH (ADOPTED REV-3 rows only; rigor classes declared)

1. **Propagation lemma [S-T0P]** (SCHEMA, the "lemma to write" duty):
   written next to T-T0 in M0 with the proof route NAMED
   (equivariance + uniqueness + L4 finite domain of dependence), the
   honest TWO-STAGE claim of record (stage 1 exact quotient — the
   naive periodic-BVP adjoint is degenerate for autonomous rotating
   waves; stage 2 = declared rung 2), the three priced boundaries
   (R20 pin provenance; O(St) distinct threat, R22 decides; out-of-pin
   route ZP/Rubino + LSS/NILSS), falsifier named. Full write-up = F2
   duty (findings row theory:s-t0p-proof-writeup-pending, OPEN).
2. **PB-2 locked formulation** (D-06): "first genuinely averaged and
   NON-COLLAPSING shape problem of the program (a CYCLE instance)" +
   the dead-phrase rider (Efremov-Kraiko 2004 Eq. 1.7) + the 7-item
   extended caveat list, at the M0 PB-2 block. Findings row
   litreview:pb2-first-averaged-phrase-dead (DISCHARGED).
3. **E4 attribution** (D-07): EOS-generality of Route-A first
   integrals = classical 1957 (Sternin), our audit = RE-DERIVATION;
   full lineage with the "according to the account of Kraiko et al.
   2001, a party in the dispute" rider at
   docs/rde_nozzle_P2_lemmaA.md §3.0 + M0 N4-ladder note; forbidden
   phrase registered. Findings row (DISCHARGED).
4. **H-EXO on T7** (D-02): named hypothesis added to the T-T7FS
   block in M0 (dmu/dSigma = 0, ds/dSigma = 0; guaranteed on L4 by
   [T-NSW]; the Rubino/Krakos residue prices its failure; refuter
   precision: the object is mu, not the physical period; falsifier =
   measured dmu/dSigma != 0; load-bearing at F5). Claims row C-HEXO
   (conditional/SCHEMA).
5. **Three non-containments (h)/(i)/(j)** (D-03): added BY NAME to
   ADVISORY_generality_litmap_2026-08-12.md §6 (elliptic-region
   design variable / endogenous-cycle family with the Zahr-Persson
   STATE-constraint signature / vectorial-thrust Pareto with the
   scalarization defense) + claim 18 re-enunciated + "no coupling"
   corrected to "no STATE coupling". Findings row (DISCHARGED).

Claims registry: +3 rows (S-T0P, T-T7CN, C-HEXO), lint (xv) PASS
first run (139 entries). Findings registry: +10 rows this window
(7 executed-item rows + 3 exclusion/decision rows), lint (xix) PASS.

## (1c) THE 5 DUAL-BLIND CARRIER FIXES (REFUTE_C rows, SR-11: no file
## moves; repairs S1-S3 = A37-A39 WITH the refuter's emendations)

Fixes applied (all REGIME-AWARE per [T-T7CN] — the refuter's binding
emendation: in regime 2 the multiplier sign is FREE, so a bare sign
rejector "fires on nothing"; the dual clause is armed only at
declared unilateral caps):
1. `validation/o33_bench.py` — NEW ROWS gating the exit code
   (VERSION CHANGE DECLARED in the module docstring):
   * **R8** (repair S1/A37): dual-feasibility clause on lambda_e —
     live instance declares regime 2/pinned (sign FREE); positive
     control at a declared upper cap; SEED REJECTOR: wrong-side
     lambda at a declared upper cap FIRES. Lexicon of record fixed:
     R3 is an identity on the SIGNED value without a dual clause
     (never "a modulus identity").
   * **R9** (repair S2/A38): the axial lambda_L row — f3* = -lambda3
     gains the DERIVED two-resolution band gate it never had
     (identity half of lambda_L = Int(f3*/q) dmu); activity report
     (theta_E = 7.9055 deg != 0 => length cap ACTIVE, A13(ii) cycle
     form); SEED REJECTOR ON THE IDENTITY, never the sign (f3* >= 0
     identically — REFUTE_C/R5).
   * **R10** (repair S3/A39): cone-scan transfer certificate —
     all-in-cone family certified at cycle level; CONVERSE REJECTOR:
     the one-phase-out/mean-in family (D = [-1,+3], equal weights)
     must be ACCEPTED and is.
2. `validation/margin_governor.py` — `mu_dual_clause` added (the
   margin constraint m >= mu_0 IS unilateral => mu is genuinely
   regime-3: required mu >= 0 under B-stationarity + complementarity
   mu = 0 at inactive margin), campaign call site prints + gates it,
   self-test with SEED REJECTOR proven in every stage (derive
   included); rel_c lexicon corrected in-docstring. VERSION CHANGE
   DECLARED.
3. `validation/a1_toc_variational_jax.py` — the O3 multiplier
   reading (res.v, abs(lam) magnitude) now DECLARES ITS REGIME in
   the print (regime 2/pinned, sign FREE; print-only, no computation
   change).
(4./5. of the REFUTE_C five = the o33 R7 pa/p_E concordance row and
the f3*-drift row — both covered by R8/R9 above;
src/thrust/phase_diagram_real.py left untouched of record: its sign
is right by monotonicity, "saved by structural accident" stays a
REGISTERED reading, repair owner F2 with the quasi-1D reduction.)

**HONEST CATCH of record**: the first R9 seed rejector used R1's
literal 5% corruption and FAILED TO FIRE (f3* drifts ~1.2e-1, an
order of magnitude above f2's ~9e-3 — a fixed-percent bump on an
interior node need not create a new max beyond the band). Repaired
by DERIVING the corruption from the band itself (argmax node +
2 x band_f3 relative => firing guaranteed if the detector works; no
magic percent). The failure mode is exactly the class the R5
discipline predicts for copied literals.

TRIPLE PROOF per touched algorithmic piece: gate re-run (below) +
the pre-existing adversarial refuter (REFUTE_C, line-verified) +
perimetered sense-review (the regime-awareness emendation applied
from REFUTE_C lines 1/3; the identity-not-sign emendation from line
4; no new algorithmic surface beyond the three rows).

GATE RE-RUNS (SR-11, same-day stamps; redirect-only, EXIT-gated):
- [X-MGOV] derive stage: VERDICT PASS, EXIT 0 (dual-clause self-test
  green, seed rejector FIRES). pass= re-stamped 2026-08-13.
- [X-O33B] full run: VERDICT PASS, EXIT 0 (all rows incl. R8/R9/R10
  + seed rejectors; R1/R5/R7 unchanged-green). pass= re-stamped
  2026-08-13.
- [X-TOCV] full run INCLUDING the opt stage (A1_TOCV_OPT=1, so the
  touched print is exercised): VERDICT PASS, EXIT 0. pass=
  re-stamped 2026-08-13.
- H4 derive artifact (RF-1 intra-commit staleness channel):
  re-minted by re-running def_twin stage_derive on the final tree
  (the carrier edits change the record-path code identity by
  construction); channel FRESH at close.

## (1d) NAMED EXCLUSIONS (declared, never silent)

- **D-49** (sonic-strip probe + Mach-margin locus): NOT executed —
  EXCLUSION CONFIRMED BY USER 2026-08-13; S-CERT registers it as a
  DECLARED LIMIT of the sample; the MOC-08 economic falsifier is
  enrolled in the S-CERT MC1/MC2 sample independently (hook 2a).
  Findings row oracles:sonic-strip-probe-missing-d49 (OPEN, owner
  F-SERVICE+F2).
- **D-20/[X-GP01]** (quasi-1D analytic adjoint oracle): NOT built
  (contract 2e) — F2-prioritized; findings row
  oracles:a1-gp01-quasi1d-not-built (OPEN).
- **MOC-*/GENO-owned items** (incl. the P0 wall-thrust double-count):
  GENO window, untouched here (R13/R32 rows).
- **Seeding tranches (c)/(d)/(e)**: remain the NAMED queue of the
  next seeding window (R31/R32 rows) — not this window.

## (1e) BOUNDARY DECISIONS (user, 2026-08-13 — taken early in-window
## at the user's request, before their unavailability)

- **A2 = (a)**: O3.1-cs BUILT AT S-CERT OPENING as an AUDIT
  INSTRUMENT (double implementation + negative control that MUST be
  detected; strengthens MC1/MC7; never an in-window patch). Findings
  row oracles:o31-common-mode-hole carries the pinned decision.
- **D-49 = excluded** (above).

## (1f) MINI-R3 OF THE BOUNDARY

- Census: row R35 added IN PLACE (SR-7); BLOCCATO 10 CONSUMED (see
  PROGRESS edit of this window).
- This log = the window log; ADVISORY_INDEX row added same window
  (R7/SR-1).
- Commit: [F-SERVICE/RATIFICHE][PIANO/R-chain] (the boundary
  commit); full suite EXIT 0 verified at the boundary (measured in
  THIS window — see the commit message for the group count/time).
- SR-9 orchestration weight: ZERO subagents in Part 1 — direct
  execution (docs+code edits with line-verified REFUTE_C evidence
  already in hand; an orchestration would have added weight without
  coverage). Token weight = single-context session.
- SR-12: every count/claim above cites its own in-window
  measurement (gate logs in the scratchpad of this session; lint
  runs re-executed after every registry append).

## HARD BOUNDARY declared

From the boundary commit: TOTAL FREEZE of tree edits; Part 2 =
S-CERT executes validation/ADVISORY_Scert_prompt_2026-08-12.md
INTEGRALLY with hooks 2a-2e (MOC-08 in the MC1/MC2 sample; A2 built
as audit instrument; MC6 over the S-ORDINE deliverables; MC8 over
the ASSESSMENT chain; D-49/D-20 declared limits). No Part-1 artifact
is presented to the auditors as authority.
