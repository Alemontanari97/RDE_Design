# ADVISORY — Agnostic full-codebase audit of record (2026-08-07,
# inter-session window after S20 closure; find->verify adversarial
# workflows wf_2dcde0d8-806 + wf_ebd2be38-96b, 24 agents, 12
# subsystems, every finding adversarially verified with
# default-REFUTED semantics; to be ingested at S21 opening)

STATUS: advisory (untracked, ADR/PANEL pattern). TOTALS (CORRECTED
2026-08-11 after the red-team caught the original header miscount —
ADVISORY_redteam_2026-08-11.md, issue judge-totals-do-not-match-body;
counts recomputed programmatically from the verified digest):
94 findings: 82 CONFIRMED (13 high), 12 REFUTED, 0 PLAUSIBLE;
seeds: 4 CONFIRMED, 3 REFUTED. The prior header read
"86 CONFIRMED (8 high), 8 REFUTED" — wrong on every number but the
total; the triage below was built on the correct body, but any
downstream text quoting the old header numbers must be corrected. Seeds from the
session lead were verified under the same standard: A1 (certdiag has
no KAT), A3 (Sauer/gammamedio correlated data error unmeasured), A4
(NASA-poly extrapolation unmeasured), A5 (tol_stop class-independence
undeclared) CONFIRMED; A2, A6, A7 REFUTED.

SCOPE STATEMENT: subsystems audited = theory core (M0 I-V, SCAFFOLD),
foundations U-series + ledgers + LBML, P-2 line, engine core, TOC
variational driver, oracle benches, registry + legacy carriers,
problem statement (D1 + (P) + superseded notes), cycle-averaged src/
machinery, phase diagrams + CJ/ZND, D3/D4/P-1 ledger coherence, test
suite. NOT audited: GENO internals (own repo/register), pptx/lecture
material, literature PDFs' content beyond citation status.

## CLUSTER MAP (deduplicated themes; fix once, closes several rows)

C1 CLOSED-FORM SCOPE LEAK (the audit's central theme, HIGH): the
   f2/Rao closed-form family is asserted on all of S1 by (P)(ii),
   T7(a), T-P3, while its derivation layer (T-A3) is scoped to
   irrotational homentropic flow; M0 VI.4bis(iv) itself demotes the
   closed form to oracle/initializer for rotational data. One fix:
   scope qualifiers at the three sites + a lint rule + the
   field-level rejector test. [theory-core:F1,
   problem-statement:closed-form-scope-overreach,
   ledger-coherence:T-LEMA-ii-class-contradiction; related
   ledger-coherence:P1-f2-locus-stale]
C2 CERTIFICATION-STACK REJECTOR GAPS (HIGH): NaN paths that DROP
   cert accounting, certification enforced on the record path only,
   nonfinite gradients silently zeroed in the driver, iterative-
   refinement caps exiting silently. [engine-core:F1,F2,F4;
   variational-driver:gnp-zeroes-nonfinite-gradient]
C3 UNDERIVED TOLERANCES (one recurring 1e3 prefactor counted once):
   [p2-line:f2-drift-tolerance-underived (HIGH); engine-core:F5;
   benches:3; foundations-U:U3BD-1e3 = registry-legacy:1E3 (same
   constant); cycle-avg:F3,F7; phase-diagrams:ka-self-inflating,
   znd-tuned, cj-fd]
C4 SUITE COVERAGE HOLES (HIGH lead): on-demand carriers excluded
   from every CI verdict; numeric-lint scope hole (validation/ not
   scanned); golden-table filter; orphan-lint prefixes.
   [test-suite:*]
C5 CYCLE-AVG MACHINERY (HIGH leads): dual-route check tautological
   on one channel; gamma-ladder staleness vs current thermo.
   [cycle-avg-machinery:F1,F2,F4]
C6 FOUNDATIONS: U3/U4 C0-bootstrap circularity (HIGH), L4 statement
   not amended after execution, LBML gradient-rate rejector cannot
   fire (ladder cannot hold topology - known, now a named rejector
   gap). [foundations-U:*]
C7 O3.2/O3.3 BENCH SPECIFICS: corner-row rejector claimed but its
   refutation recorded (HIGH pair with C3's tolerance row);
   lip-exclusion not applied to the headline drift; Prop-A3 planar
   case not machine-verified. [p2-line:*, benches:*]
C8 LEDGER/PROCESS HYGIENE: C-O33 rows stale in two places, D4
   novelty query vs Kraiko-Osipov 1970 unresolved, label
   collisions, GENO line refs unpinned, St numbers uncarried.
   [ledger-coherence:*, problem-statement:*, registry-legacy:*]

## VERDICT-BEARING PRIORITY (for S21 Fase 0 triage)
P0 (before any new decisive run): C2 (cert-stack gaps), seed A1
   (certdiag KAT), C7 corner-row rejector row.
P1 (before P-2 draft freeze): C1 scope qualifiers, C7 lip/drift
   wording, C8 C-O33 staleness, C3 f2-drift tolerance derivation.
P2 (scheduled): C4 suite holes, C5, C6, remaining C3, C8.

---

# FULL FINDINGS (generated from the verified digest)


## theory-core (candidates dropped by finder cap: 5)

### [CONFIRMED | high | hidden-hypothesis] theory-core:F1-f2-closed-form-rotational-leak

- FILE/LOC: docs/rde_nozzle_P3_multipliers.md — P3 doc §1-§2 (lines 17-46, 67-71); M0 lines 286-289 ((P)(ii)), 856-859 (T7(a)), 892-896; claims_registry.yaml lines 456-467 (T-P3) vs 378-389 (T-A3)

- CLAIM OF RECORD: T-P3 (THEOREM*): in the shock-free S1 class the averaged mass multiplier satisfies lambda2(xi) = -f2(lip data) with f2 = V cos(theta -/+ alpha)/cos(alpha); M0 T7(a): 'in S1 it reduces to the classical closed form'; (P)(ii) requires S* to satisfy 'per-phase closed-form adjoint (Rao/Kraiko) conditions'.

- FINDING: The closed form's own derivation layer is scoped strictly narrower than the class in which T-P3/T7(a)/(P)(ii) assert it. The registry states T-A3 ('f2 = transported adjoint invariant') holds for 'irrotational homentropic, EOS-general; rotational extension = Hoffman four-field route', yet T-P3's statement and proof step (ii) invoke exactly that invariant for the full shock-free S1 class with no irrotationality/homentropy hypothesis — grep confirms the words 'irrotational' and 'homentropic' appear NOWHERE in the P3 document. S1 (D2.5) admits rotational piecewise-smooth flows, the interface contract VI.1 explicitly carries per-phase entropy profiles s(y;xi) and '[vorticity]', and M0's own VI.4bis(iv) contradicts the theorem by demoting 'the two-field closed form (Prop. A3)' to ORACLE/INITIALIZER only for rotational data. On the actual RDE data class (rotational per-phase profiles from the sweep) f2 is not a transported invariant, so the THEOREM*-of-record claim is false on part of its declared scope, and the anchor problem's (P)(ii) stationarity system as worded imposes closed-form conditions that do not characterize S* there.

- EVIDENCE: P3 doc line 33-35: 'THEOREM P3-S1 (averaged multiplier regularity, shock-free S1 class...)' — hypotheses list margins and measurability only; line 69-71: 'unique solution for lambda2 is Rao's first integral... exactly Lemma A step 5 ((L.12))'. Registry T-A3 scope (line 381): 'irrotational homentropic, EOS-general; rotational extension = Hoffman four-field route'. M0 line 1045-1047: 'ROTATIONAL DATA (unconditional): per-phase adjoint at the FIELD level...; the two-field closed form (Prop. A3) serves as ORACLE/INITIALIZER only'. M0 line 287-288: '(ii) S* satisfies the averaged system T7: per-phase closed-form adjoint (Rao/Kraiko) conditions'. The registry falsifier for T-P3 ('computed lambda2(xi) drifting from -f2(lip) beyond margins on certified data') would actually fire inside the declared scope on rotational certified data.

- SUGGESTED TEST: Symbolic/numeric rejector: build one S1 phase with nonzero inflow entropy gradient (data class the contract already admits), integrate f2 along the terminal characteristic with the existing X-PA1 machinery and compare -f2(lip) against the field-level multiplier from the O3.1-certified adjoint march; a drift beyond derived tolerance confirms the mis-scoping. Fix: add 'irrotational homentropic' to T-P3/T7(a) statements and reword (P)(ii) to 'per-phase adjoint conditions (closed-form in the irrotational homentropic subclass; field-level otherwise)', or prove the lip-endpoint identity via the Hoffman four-field route.

- VERIFIER NOTE: Refutation failed on every axis. docs/rde_nozzle_P2_lemmaA.md §3.0 (line 23) pins Lemma A's scope: 'steady, inviscid, HOMENTROPIC (uniform h0, s), frozen gamma'; Prop. A3 (lines 411-412) is 'THEOREM within the irrotational homentropic scope', and its ROTATIONAL SCOPE NOTE (lines 434-443) states explicitly 'the two-field closed form is irrotational-homentropic; for ROTATIONAL... the identification survives at the FIELD level, the two-constant reduction does not'. Yet THEOREM P3-S1 (docs/rde_nozzle_P3_multipliers.md lines 33-46) asserts (P3.1) lambda2 = -f2(lip) for the whole shock-free S1 class with hypotheses limited to margins/measurability — grep confirms zero occurrences of irrotational/homentropic/rotational/vorticity in the P3 doc — and proof step (ii) (lines 67-71) invokes 'Lemma A step 5 ((L.12))' outside that lemma's declared scope. The class of record is rotational: D2.5 S1 has no irrotationality clause, VI.1 (M0 line 994) carries 's(y;xi), [vorticity]', VI.2 mandates rotational MOC, and Prop. A3's own note says 'M0 VI.2 mandates rotational MOC'. M0 T7(a) (lines 856-859) and (P)(ii) (lines 287-289) impose the closed form on S1 without qualifier; registry T-P3 scope (docs/claims_registry.yaml line 459) names single-wall and shock-free but not irrotational/homentropic, while T-A3 (line 381) is scoped 'irrotational homentropic'. M0 VI.4bis(iv) (lines 1045-1051) internally contradicts T-P3 by demoting the closed form to ORACLE/INITIALIZER on rotational data while citing '(P3 theorem)'. The declared residues R-P3.1/2/3 name shocks and reacting gas but not the rotational inert boundary, which sits INSIDE the declared shock-free S1 scope. The THEOREM*-of-record is therefore asserted on a class where its own derivation layer disclaims it.

### [CONFIRMED | medium | gamma-leak] theory-core:F2-lemmaA-ideal-gas-pin-undeclared

- FILE/LOC: docs/rde_nozzle_MASTER.md — M0 lines 515-534 (T-T3 Lemma A/B) vs claims_registry.yaml lines 261-272 (T-T3 scope)

- CLAIM OF RECORD: T-T3 Lemma A: 'pressure-scaling similarity; holds for gamma(T) and ACROSS transversal shocks'; registry scope inflates this to 'Lemma T3-A EOS-general across shocks'.

- FINDING: Lemma A's k-scaling proof is load-bearing on the thermally-perfect ideal-gas EOS and this hypothesis is never named as a boundary. The proof itself pins it — 'EOS rho = p/(RT) preserved' (line 524) and the entropy shift '-R ln k' (line 525) both require rho degree-1 homogeneous in p at fixed T, i.e. rho = p·f(T): the similarity fails for any non-ideal EOS (co-volume/virial/real-gas tables, relevant at RDE chamber pressures). M0 correctly says 'gamma(T)' but only names the CALORIC boundary (Lemma B, calorically perfect); the THERMAL boundary (ideal-gas EOS) is undeclared, and the registry — the machine-readable index of record — widens the claim to 'EOS-general', the same vocabulary used elsewhere for genuinely abstract-EOS results (T-XWALL 'abstract EOS'). Since T-T3 is globality mechanism M2 ('valid for EVERY S in the sector', (P)(iv)), an application on a future non-ideal tabulated backend would silently import a false collapse. Violates the standing gamma-variable-generality directive ('gamma=const boundaries named') at the EOS level.

- EVIDENCE: M0 line 520: 'Lemma A (pressure-scaling similarity; holds for gamma(T) and ACROSS transversal shocks)'; line 523-524: 'momentum contains p only through grad(p)/rho, invariant; EOS rho = p/(RT) preserved'; line 529-530: 'Lemma B ... REQUIRES calorically perfect gas' — only Lemma B is flagged as 'THE boundary' (registry line 264). Registry line 264 scope: 'Lemma T3-A EOS-general across shocks' — strictly stronger than what the proof establishes; the program elsewhere distinguishes 'abstract EOS' (T-XWALL/T-XSON, registry ~1463-1488) from Cantera-class, so 'EOS-general' here is an inflation, not jargon.

- SUGGESTED TEST: Known-answer rejector: run the Lemma-A scaling check on a Noble-Abel (co-volume b>0) gas — scale (p,rho) by k at fixed (u,T) and verify the EOS residual rho - p/(R T (1 - b rho)...) is NOT k-invariant, so the collapse fails; then amend the T-T3 statement and registry scope to name 'thermally perfect ideal-gas EOS' as an explicit hypothesis alongside H1.

- VERIFIER NOTE: The Lemma A proof is load-bearing on the thermally-perfect ideal-gas EOS: M0 lines 523-525 use 'EOS rho = p/(RT) preserved' and the entropy shift '-R ln k', both requiring rho degree-1 homogeneous in p at fixed T (rho = p·f(T)) — the scaling fails for any co-volume/virial/real-gas EOS. The T-T3 H-list (M0 lines 507-511) does not name this hypothesis; only the CALORIC boundary is flagged ('Lemma B... REQUIRES calorically perfect gas', line 529; registry line 264 'Lemma T3-B calorically perfect = THE boundary'). The registry scope (docs/claims_registry.yaml line 264) asserts 'Lemma T3-A EOS-general across shocks', where the registry's own usage of EOS-general elsewhere means genuinely EOS-independent (T-LEMA-CL line 355: 'EOS-general (only Gibbs along the isentrope + c^2 definition used)'; T-XWALL line 1448: 'ABSTRACT EOS') — so the machine index of record asserts a generality the proof does not establish. Mitigations found (why medium, not high): the entry's gamma field is 'perfect-gas-oracle' (line 272), honestly flagging T-T3 overall, and the proof itself displays the ideal-gas EOS inline; M0's prose says 'holds for gamma(T)' (line 520), which is accurate for thermally-perfect. The defect is the registry scope string's inflation plus the absence of the thermal-EOS boundary from the named hypothesis set — a real gamma-directive-class naming failure at the EOS level, but visible-in-proof.

### [CONFIRMED | medium | rejector-weakness] theory-core:F3-TGB-class-inflation-rejector-scope

- FILE/LOC: docs/rde_nozzle_MASTER.md — M0 lines 602-638 (T-GB statement + proof) vs claims_registry.yaml lines 300-311

- CLAIM OF RECORD: T-GB: 'for ANY solid set S in ANY topology, under choked frozen feed: J[S] <= J_ideal' — registry class THEOREM; M0 class label 'THEOREM-grade / mechanism M1'; proof header 'PROOF (sketch, rigorous)'.

- FINDING: The bound that feeds the headline certificate delta = B - J[S*] ((P)(iv), M0 lines 292-298) carries three rigor-hygiene defects: (1) M0's class label 'THEOREM-grade' is not in the rigor legend (THEOREM/THEOREM*/SCHEMA/CONJECTURE/PRACTICE) — a non-legend class on a verdict-bearing object; (2) the registry lists it as full THEOREM while the proof layer is a self-declared 'sketch' that glosses the streamtube-decomposition validity for arbitrary topologies (closed recirculation regions behind truncated-plug bases in steady inviscid flow, exit-pressure nonuniformity handled only by the quasi-1D dF/dA_e sign argument); (3) the registered falsifier ('any admissible exit scan beating the capped formula') sweeps only the per-phase 1-D exit-state family, so it structurally CANNOT fire on the multi-D channels the sketch glosses — a rejector that cannot reject on the claim's full 'ANY topology' scope. History shows this exact claim class can be wrong in-scope (the pre-cap subcritical counterexample, lines 623-638, was found and fixed) — the same discipline has not been applied to the topology-generality hypothesis set.

- EVIDENCE: M0 line 602-603: '[T-GB] PROPOSITION 7 ... THEOREM-grade / mechanism M1' (not a legend class, cf. lines 10-13); line 608: 'PROOF (sketch, rigorous)'; lines 613-620 give the per-streamtube argument with no lemma covering exit-surface area not fed by interface streamtubes. Registry line 302-310: class THEOREM, scope 'ANY topology', falsifier = exit scan only. Lines 626-638 document that the uncapped form of this same proposition was strictly false on subcritical phases until the sonic cap was added.

- SUGGESTED TEST: Either de-rate the registry entry to THEOREM* with a named hypothesis ID (steady per-phase S1, all exit flux interface-originated, far-field pressure relaxation to Pa), or write the missing far-field control-surface lemma (pressure term -> 0, momentum flux = interface streamtube sum) at statement level; add one shape-level falsifier — a certified 2-D truncated-plug instance with a base recirculation region checked against the capped ceiling — so the rejector can actually probe the 'ANY topology' quantifier.

- VERIFIER NOTE: All three sub-defects verified with no rescuing material found. (1) M0 line 603 labels T-GB 'THEOREM-grade / mechanism M1' — not a class in the rigor legend (lines 10-13: THEOREM/THEOREM*/SCHEMA/CONJECTURE/PRACTICE); the theorem ledger repeats the non-legend label (docs/rde_nozzle_theorem_ledger.md line 772: 'THEOREM-grade'). (2) The registry (docs/claims_registry.yaml lines 300-311) classes it THEOREM ('proof here, verified line-by-line' per the legend) while the sole proof of record is self-declared 'PROOF (sketch, rigorous)' (M0 line 608): the per-streamtube argument (lines 610-621) has no lemma covering exit flux not originating at the interface (recirculation/backflow in 'ANY topology'), and expansion completeness rests on the quasi-1D dF/dA_e sign argument; registry doc/proof pointers both resolve to this sketch, and no fuller proof exists in the ledger. (3) The registered falsifier (line 310: 'any admissible exit scan beating the capped formula') sweeps only the per-phase 1-D exit-state family, so it structurally cannot fire on the multi-D 'ANY topology' quantifier — a rejector that cannot reject on the claim's full scope (R5 violation), on the object that feeds the headline certificate delta = B - J[S*] ((P)(iv), lines 292-299). The in-scope precedent (subcritical counterexample and sonic cap, lines 626-638) shows this claim class has already been false once, and only the 1-D branch was subsequently instrumented. Severity stays medium (not high) because the bound direction and the capped 1-D content are heavily instrumented (X-GRP08/11/12) and the physical argument is likely repairable, but class-of-record vs proof-of-record and rejector-vs-scope mismatches stand.

### [CONFIRMED | medium | doc-code-mismatch] theory-core:F4-CXBVP-ledger-dangling-pointer

- FILE/LOC: docs/rde_nozzle_MASTER.md — M0 lines 14-22 (legend) and 156-170 (T-XWS 'ledger §1bis') vs docs/rde_nozzle_conditionals.md (whole file) and SCAFFOLD §5 lines 200-213

- CLAIM OF RECORD: M0 legend: 'the named ANALYTIC conditionals (C-D25U/C-MAJDA); the closure-conditional vs analysis-conditional distinction lives in the L4 ledger (docs/rde_nozzle_conditionals.md)'; M0 D2.5: T-XWS is 'THEOREM* inheriting [C-XBVP](a,b), ledger §1bis'; SCAFFOLD §5: 'Read "nothing else" as "no other ANALYTIC conditional"'.

- FINDING: The single-statement L4 architecture is broken for C-XBVP: the file M0 names as THE L4 ledger (docs/rde_nozzle_conditionals.md) contains ONLY C-D25U and C-MAJDA — no §1bis, no C-XBVP (grep-verified) — while M0's T-XWS cites '[C-XBVP](a,b), ledger §1bis'. C-XBVP actually lives in docs/rde_nozzle_remaining_conditionals.md §1bis (per the registry doc pointer, claims_registry.yaml lines 1484-1495), a different file. Moreover C-XBVP is plainly an ANALYSIS conditional (component (a): x-entropy convexity open at abstract EOS; component (b): weak-side trace technicalities — not a model closure), so both the M0 legend's two-item analytic inventory (C-D25U/C-MAJDA) and SCAFFOLD §5's 'no other ANALYTIC conditional' assertion are stale since S15 (2026-08-05). The at-a-glance residual-honesty property SCAFFOLD §5 advertises ('the whole theory hangs on C-D25U + C-MAJDA... stays VISIBLE structurally') is currently false as written.

- EVIDENCE: M0 line 18-20: 'the named ANALYTIC conditionals (C-D25U/C-MAJDA)... lives in the L4 ledger (docs/rde_nozzle_conditionals.md)'; M0 line 166-167: '[T-XWS] (THEOREM* inheriting [C-XBVP](a,b), ledger §1bis)'. Grep of docs/rde_nozzle_conditionals.md for '1bis|XBVP|P4RZ': zero hits; its only sections are C-D25U (line 18) and C-MAJDA (line 77). claims_registry.yaml line 1489: C-XBVP doc = 'docs/rde_nozzle_remaining_conditionals.md#§1bis C-XBVP'. SCAFFOLD lines 207-213 (S14 note) enumerate C-D25U, C-MAJDA + closures + C-P4RZ but predate/omit C-XBVP.

- SUGGESTED TEST: Extend tests/test_claims_lint.py with a rule: every ID in any `inherits` field must resolve to a section anchor in the single declared L4 ledger file; seeding the current tree should REJECT on C-XBVP. Fix by minting C-XBVP (and classifying C-P4RZ) in docs/rde_nozzle_conditionals.md and updating the M0 legend + SCAFFOLD §5 analytic inventory in the same session (R4).

- VERIFIER NOTE: Grep-verified: docs/rde_nozzle_conditionals.md contains zero hits for '1bis', 'XBVP', 'P4RZ'; its only conditional sections are C-D25U (line 18) and C-MAJDA (line 77), and it declares itself the single statement location ('This file states the TWO analytic conditionals', lines 9-11; Reading rule lines 131-137: 'THIS file is the single statement location'; 'Adding a NEW conditional requires justifying why it is not one of these two'). M0's legend (lines 18-20) names that file as THE L4 ledger, and M0's T-XWS citation (line 167) reads '[C-XBVP](a,b), ledger §1bis' — a pointer that does not resolve in the named ledger. C-XBVP actually lives in docs/rde_nozzle_remaining_conditionals.md §1bis (line 127; registry doc pointer line 1489 confirms), and its components are analysis-grade, not model closures: (a) abstract-EOS convexity residue (§1bis lines 140-147, 'the abstract-EOS statement only (schema)'), (b) 'x-slice normal traces and wall traces... Dafermos-class bookkeeping' (lines 148-151) — so it fits none of the M0 legend's three buckets (closures C-HT4/C-IGMIX/C-O33, numeric residuals, or the two named analytic conditionals). SCAFFOLD §5 (lines 203-213) still enumerates C-D25U + C-MAJDA + closures + C-P4RZ with 'Read "nothing else" as "no other ANALYTIC conditional"' — stale since C-XBVP was minted S15 (2026-08-05). The advertised at-a-glance residual-honesty property is currently false as written. No mathematical content is wrong (the statement exists and is registered), hence medium: an architecture/consistency defect in the corpus of record, exactly the drift the lint layer claims to machine-reject.

### [REFUTED | medium | hidden-hypothesis] theory-core:F5-canonicity-region-local-overreach

- FILE/LOC: docs/rde_nozzle_MASTER.md — M0 lines 142-155 (D2.5 canonicity) with the S15 upgrade lines 156-170

- REFUTED BY VERIFIER: Two independent grounds. (1) The finding's central technical premise — that Dafermos relative entropy and its measure-valued version are 'domain-global statements' that cannot localize — is incorrect for the cited TIME formulation: the relative-entropy comparison is standard on cones of determinacy (the strong solution need be Lipschitz only on the comparison cone; the cone-boundary flux term is controlled by choosing the cone slope above the maximal characteristic speed — Dafermos' local comparison theorems), so 'in SHOCK-FREE (locally Lipschitz) regions and regimes' has a theorem-backed reading for regions whose domains of dependence are shock-free. (2) The feared false license ('the smooth core is canonical even with a fitted front present') is explicitly blocked in the SAME D2.5 passage: M0 lines 148-155 state 'ACROSS transversal shocks... the general multi-D weak-strong property is OPEN... S1 canonicity WITH shocks is therefore a DECLARED conditional [C-MAJDA], not a theorem' — i.e., any flow containing fronts sits wholly in the conditional half, and D2.6's STATE clause (line 281) repeats 'canonicity per D2.5 (exact shock-free, declared conditional across fronts)'. The time-vs-BVP formulation gap the finding cites as a concession is self-flagged by the corpus in the same block (lines 156-170) and routed through T-XWS with C-XBVP inherited — residual honesty is present at the site. What remains is at most a wording preference ('regions' unglossed by 'domain of dependence'), not a defect that licenses any unconditional claim the text elsewhere forbids.

### [REFUTED | low | hidden-hypothesis] theory-core:F6-T4-attainment-compactness

- FILE/LOC: docs/rde_nozzle_MASTER.md — M0 lines 578-583 (T-T4 attainment/measurability step), cf. lines 416-427 (T-O2 scope note)

- REFUTED BY VERIFIER: The claimed defect — attainment 'holds only under the undeclared closure convention Pc(0) = P_CJ' — does not stand: on the domain of record xi in [0,1) (M0 line 417, T-O2), the canonical blowdown Pc(xi) = P_CJ · PR^(-xi) is strictly decreasing and attains its maximum AT xi = 0, which is an element of [0,1); Pc(0) = P_CJ is the formula's value at an interior-domain point, not a convention, and the 'wave-passage phase' of the proof (M0 lines 581-583) is exactly xi = 0. No circle-compactification or upper-semicontinuity argument is needed for attainment — the sawtooth discontinuity lives only at the identified endpoint under the circle reading, which the proof does not require. For general imported mu, the T-O2 S14 scope note (lines 420-427) already declares the mu-hypothesis re-verification duty, and the finding itself concedes the conclusion survives (F constant for l >= l(xi), so the peak design attains Int max regardless) and that 'no downstream number is wrong'. What remains is that the parenthetical justification '(compact) cycle' is loose wording (the [0,1) parametrization is not compact; monotone attainment at the included endpoint is the honest one-line reason) — a phrasing nit below finding threshold once the asserted invalidity and the asserted undeclared convention are both refuted.


## foundations-U (candidates dropped by finder cap: 5)

### [CONFIRMED | high | hidden-hypothesis] foundations-U:U34-C0-bootstrap-circularity

- FILE/LOC: docs/rde_nozzle_D25U_U3U4.md — §2.3 lines 141-155 (K_delta definition), §3(c) lines 214-221, §4.1 lines 264-273, §4.3 lines 306-333

- CLAIM OF RECORD: Component -a of C-D25U is COMPLETE at class level with LIP_shocked explicit in the five constants (delta, L_x, C_geo, C_dat, h_min); U3-L1 floors and the front count N_F^max are uniform over the shocked certified class.

- FINDING: The shocked-class constant chain is circular as written. K_delta is defined with the state box |V_+-| <= C_0 where C_0 = the U1 §3 SMOOTH-region C^0-BOUND (line 148), and every uniform floor (s_*, g_*, l_*, s_j) plus L_RH is a function of (delta, C_0). But in a shocked solution the traces are bounded only by the composed bound V(x) <= L_RH^{N_X} R_0^{N_b}(...)e^{K_1 x} (§3(c), line 220-221), which exceeds the smooth C_0 — so states downstream of fronts are never proven to lie inside the K_delta box on which L_RH's uniformity rests. Likewise U4-A asserts 'the wall-trace entropy stays within the U1 C^0 bound' (lines 270-272) for a wall trace that has crossed front feet, and N_F^max = 4 C_0/s_j(delta, C_0) uses the same smooth C_0. No bootstrap/induction layer (region-wise C_0^(k) along causal order with a closing fixed point) is written; the §4.3 induction invokes U3(e) per front without re-deriving the box membership.

- EVIDENCE: docs/rde_nozzle_D25U_U3U4.md line 148: 'with C_0 = C^0-BOUND(delta, L_x, C_geo, C_dat, h_min) from U1 §3'; line 208 'L_RH = L_RH(delta, C_0) uniform over the class'; lines 220-221 the composed bound with L_RH^{N_X} factor > C_0; lines 270-272 the wall-trace entropy claim; docs/rde_nozzle_D25U_U1.md lines 140-143 define C^0-BOUND for the smooth region only. The implicit hypothesis 'shocked-solution states remain in the smooth-C_0 box' is nowhere stated or priced in the §5 inventory c1-c4.

- SUGGESTED TEST: Write the region-wise bootstrap: define C_0^(k) recursively along the causal order (each region's inflow = prior front traces), redefine K_delta with the final composed C_0*, and verify the fixed point closes — in particular that N_F^max evaluated at C_0* does not exceed the front count used to build C_0* (s_j decreases as C_0 grows). If it does not close, C-D25U-a is not complete and the §5 inventory needs a fifth clause.

- VERIFIER NOTE: Refutation attempted and failed. K_delta is defined with the box |V_+-| <= C_0 where line 148 of docs/rde_nozzle_D25U_U3U4.md fixes 'C_0 = C^0-BOUND(delta, L_x, C_geo, C_dat, h_min) from U1 §3', and U1 §3 (docs/rde_nozzle_D25U_U1.md lines 140-143) derives that bound for the SMOOTH region only (R_0^{N_b} factor, no front factor). The shocked-solution traces are bounded only by the composed bound V(x) <= L_RH^{N_X} R_0^{N_b}(...)e^{K_1 x} (U3U4 lines 219-221), which exceeds the smooth C_0 whenever L_RH > 1; nothing in the doc proves shocked states lie in the K_delta box on which every uniform floor s_*, g_*, l_*, s_j and L_RH itself (lines 157-165, 'L_RH = L_RH(delta, C_0)' line 208) rests. U4-A's proof asserts 'The wall-trace entropy stays within the U1 C^0 bound' (lines 270-271) for a wall trace downstream of front feet — asserted, not derived — and N_F^max = 4 C_0/s_j (lines 265-266) uses the same smooth C_0, so the front count and the box that defines it are mutually dependent with no written fixed point (s_j shrinks as the box grows, N_F^max grows, the composed bound grows). The certified-set definition in §1 (lines 49-57) contains no state-box clause that could substitute for the derivation, the §4.3 induction (lines 325-333) invokes U3(e) per front without re-establishing box membership, and the §5 inventory c1-c4 (lines 347-375) prices topology, origination, Lopatinskii and genuine nonlinearity but NOT this implicit hypothesis. The self-closing note at §3(d2) (lines 235-238) covers only the straightening loop, not C_0. The 'COMPLETE at class level with every clause it costs NAMED' verdict (lines 25-30) therefore carries an unnamed hypothesis; high because the missing bootstrap is not obviously closable (the fixed point could fail in principle) and the completeness claim is the doc's headline.

### [CONFIRMED | medium | doc-code-mismatch] foundations-U:L4-statement-falsifier-unamended

- FILE/LOC: docs/rde_nozzle_conditionals.md — C-D25U STATEMENT lines 20-30 and FALSIFIER lines 66-67

- CLAIM OF RECORD: L4 is 'the single statement location' from which every THEOREM* inherits its conditionals BY ID; its C-D25U statement is a faithful transcription of the source of record.

- FINDING: The L4 statement block still reads 'Let D(delta, L_x, C_geo, C_dat) be the certified set ... ALL constants depending ONLY on (delta, L_x, C_geo, C_dat)' — the exact four-constant claim that U1 §7 proved 'FALSE as literally written' (a certified family with h_min -> 0 and all four constants fixed breaks the uniform Lipschitz bound). The source of record was amended (remaining_conditionals.md line 30 names h_min), but the L4 STATEMENT and FALSIFIER blocks were not: the recorded falsifier 'a certified family in D with solution-map Lipschitz constant blowing up while all margins stay >= delta' is satisfied by U1's own §7 counterexample, i.e. per the ledger of record C-D25U stands falsified. The h_min amendment appears only inside the bracketed DISCHARGER STATUS note (lines 53-64), while the reading rule (lines 131-137) directs heirs to the statement block.

- EVIDENCE: docs/rde_nozzle_conditionals.md lines 21-22 and 29-30 (four-constant set and dependence claim), lines 66-67 (falsifier without h_min); docs/rde_nozzle_D25U_U1.md lines 217-234 (§7: statement 'FALSE as literally written', 'AMENDMENT OF RECORD ... ledger §1 statement annotated'); docs/rde_nozzle_remaining_conditionals.md lines 30-34 (source amended). The L4 CONTENT RULE (lines 10-14) says statements are transcriptions of the sources — the transcription is now stale against its own source.

- SUGGESTED TEST: Grep-lint (suite group xv candidate): assert the L4 C-D25U statement and falsifier strings contain 'h_min' whenever remaining_conditionals §1 does; amend the STATEMENT and FALSIFIER blocks with the dated five-constant text.

- VERIFIER NOTE: Verified against both files. docs/rde_nozzle_conditionals.md lines 21-22 still read 'Let D(delta, L_x, C_geo, C_dat) be the certified set' and lines 29-30 'ALL constants depending ONLY on (delta, L_x, C_geo, C_dat)'; the FALSIFIER block (lines 66-67) has no h_min scope. docs/rde_nozzle_D25U_U1.md §7 (lines 217-234) proves that exact four-constant claim 'FALSE as literally written' with an explicit certified family (h_min -> 0, R > 1) that satisfies the recorded falsifier verbatim, and the source of record was amended inline with a dated AMENDMENT OF RECORD paragraph (docs/rde_nozzle_remaining_conditionals.md lines 28-35: 'the uniform-constants claim below is FALSE as written... every C-D25U-derived statement carries the fifth constant from now on'). L4's CONTENT RULE (lines 11-14) declares its statements TRANSCRIPTIONS of that source, and the reading rule (lines 131-137) makes L4 the single statement location for 8 heirs — yet neither the STATEMENT nor the FALSIFIER block carries the amendment. Severity mitigated to medium, not high: the five-constant amendment IS present in the same C-D25U section via the dated DISCHARGER STATUS note (lines 53-64: 'h_min amendment', 'LIP_shocked explicit in the five-constant certificate'), so a reader of the full section is not actually misled and the correction is dated and co-located; the defect is a real staleness of the designated statement/falsifier text, grep-lintable and one-paragraph to fix.

### [REFUTED | high | rejector-weakness] foundations-U:certified-constants-symbols-only

- FILE/LOC: docs/rde_nozzle_D25U_U3U4.md — U1 §3 line 141-143; U3U4 §2.3 lines 176-179 and §4.3 lines 317-325; conditionals.md lines 66-67

- REFUTED BY VERIFIER: The factual observations hold (Grep confirms: 'h_min' has zero matches under validation/*.py; 'C_geo' appears only in comments at a1_toc_variational_jax.py:15 and adaptive_knot_optimize.py:13; no carrier computes ||w||_C2, C_dat or a width floor) but the defect as framed — an R5 violation, 'a rejector that cannot reject' — does not survive the declarations and the record. (1) The absence of an executable carrier for the function-space conditional is DECLARED, not silent: docs/rde_nozzle_remaining_conditionals.md lines 6-9 ('none admits a symbolic carrier — declared per the sufficiency discipline'), U1 audit line (docs/rde_nozzle_D25U_U1.md lines 13-20: 'Carrier: none (function space)... declared'). (2) The C-D25U falsifier is an ANALYTIC falsifier and it demonstrably CAN fire — it already FIRED: U1 §7's h_min counterexample is exactly 'a certified family in D with blowing Lipschitz constant while all margins stay >= delta', and its firing forced a dated amendment of record (lines 217-234). A rejector that has rejected is not a rejector that cannot fire. (3) An executable symptom channel is additionally declared: U3U4 §6(iv) (lines 402-406) names the A1 front-solve conditioning blowup as 'an EXECUTABLE symptom the march already monitors through its Newton solves'. (4) The 'EXPLICIT' wording is qualified in the same doc by the honesty note (U3U4 lines 176-179: 'explicit as NAMED FUNCTIONS... via compactness, not closed forms'), so no overstatement is hidden from the ledger adjudication, which cites that doc. The residual kernel (no run-level five-constant membership certificate) is a legitimate improvement suggestion, not a violation of the declared standard.

### [CONFIRMED | low | process] foundations-U:pass2-discharges-are-rescopes

- FILE/LOC: docs/rde_nozzle_hypothesis_ledger.md — §4.1 lines 112-117 (H2, H-I2, H-Pa rows); vocabulary lines 15-18

- CLAIM OF RECORD: Pass 2: 3 DISCHARGED (H2, H-I2 on the L4-default class; H-Pa by instance reclassification), where DISCHARGED is defined as 'proven or removed — no longer an assumption'.

- FINDING: All three pass-2 DISCHARGED verdicts are scope moves, not proofs or removals. H2 and H-I2 are discharged 'on the L4-default class' — i.e. by DECLARING axially-supersonic interfaces the certified default (an election, dated M0 D2.4 note); the hypothesis survives verbatim on subsonic-patch interfaces, relabeled 'a case-class closure ... not a theory hypothesis'. H-Pa is discharged by 'instance reclassification' with the (P) text explicitly UNTOUCHED — constant Pa is renamed an instance of the case-D form; nothing was proven this pass (the T3 Corollary-2 duality predates it). Under the ledger's own vocabulary these are WEAKENED-to-named-subset outcomes (H2/H-I2 even carry the qualifying suffix 'on the L4-default class' inside the DISCHARGED verdict). The headline 'PASS-2 TOTALS: 3 DISCHARGED' therefore overstates the theory's self-sufficiency, which is exactly what this governance deliverable exists to measure.

- EVIDENCE: docs/rde_nozzle_hypothesis_ledger.md lines 16-18 ('DISCHARGED: proven or removed — no longer an assumption'; 'WEAKENED: ... confined to a named subset'); line 43 H2 'survives only as a CASE-CLASS closure assumption inside D1 §4.3bis options O2/O3'; line 44 H-I2 'the assumption survives ONLY on declared subsonic patches'; line 117 H-Pa '(P) text UNTOUCHED (anchor discipline)'; lines 29-33 pass-2 totals.

- SUGGESTED TEST: Re-adjudicate the three rows against the ledger's own vocabulary: either rename the verdicts 'WEAKENED (confined to the L4-default / case-D instance)' or amend the DISCHARGED definition to admit class-election discharges explicitly — and restate the pass-2 totals accordingly.

- VERIFIER NOTE: The vocabulary mismatch is real and internal to the doc. docs/rde_nozzle_hypothesis_ledger.md lines 16-18 define DISCHARGED as 'proven or removed — no longer an assumption' and WEAKENED as 'replaced by a strictly weaker NAMED hypothesis (or confined to a named subset...)'. Post-pass-2, H2 'survives only as a CASE-CLASS closure assumption inside D1 §4.3bis options O2/O3' (line 43) and H-I2 'survives ONLY on declared subsonic patches' (line 44) — i.e. both are literally 'confined to a named subset', the WEAKENED definition verbatim; H-Pa is discharged with '(P) text UNTOUCHED (anchor discipline)' (line 117), i.e. by reclassification with no proof delta this pass (the T3 Corollary-2 duality predates pass 2). The pass-2 delta for H2/H-I2 is purely the L4 default ELECTION (M0 D2.4 dated note) — the theorem content ([T-TH0]/[T-NSW]) was already the pass-1 WEAKENED grounds — and pass 1's own text concedes 'discharging takes proofs, not adjudication' (lines 27-28) while §4 is again 'verdicts from the existing record only'. Refutation attempted: the corpus doctrine that case-class closures are not analytic hypotheses (conditionals.md lines 5-9) and the inline qualifiers in the totals ('H2, H-I2 on the L4-default class', lines 30-31) genuinely mitigate — no reader is misled about the mechanism — but they do not erase the fact that the doc's own definitions file these outcomes under WEAKENED, and the doc's own falsifier is 'any row whose cited grounds do not support its verdict'. Survives at low severity: a labeling/self-consistency defect in a transparent governance doc, no mathematical content affected.

### [CONFIRMED | low | numerics] foundations-U:U4B-stated-bound-unproven

- FILE/LOC: docs/rde_nozzle_D25U_U3U4.md — §4.2 lines 283-302 (Lemma U4-B)

- CLAIM OF RECORD: LEMMA U4-B: any backward characteristic path crosses fronts at most N_X <= N_F^max (N_b + 2) times.

- FINDING: The lemma as STATED is not what the proof establishes. The proof's first argument dead-ends in circularity ('legs <= N_b + N_X + 1...' — bounding N_X by a quantity containing N_X, abandoned mid-sentence with an ellipsis), and the doc then 'RECORD[s] the cruder but simply-proved bound N_X <= 2 N_F^max (N_b + 1)', which for N_b > 0 is strictly WEAKER than the stated lemma bound N_F^max (N_b + 2). So the displayed lemma statement is unproven as written, and the §4.3 exponent (N_b + N_X + 1 in LIP_shocked) inherits whichever bound one picks — a factor-2 ambiguity inside a claimed-explicit exponent. The doc declares the crudeness honestly, but a LEMMA header carrying a bound its own proof does not reach is a statement/proof mismatch in a chain marketed as 'every factor a named function of the five constants'.

- EVIDENCE: docs/rde_nozzle_D25U_U3U4.md lines 285-287 (statement: 'N_X <= N_F^max (N_b + 2)'), lines 296-301 (proof: circular attempt with '...' then 'we RECORD the cruder but simply-proved bound N_X <= 2 N_F^max (N_b + 1) and use it'). 2 N_F^max (N_b + 1) > N_F^max (N_b + 2) whenever N_b > 0.

- SUGGESTED TEST: Replace the lemma statement with the bound the proof actually derives (N_X <= 2 N_F^max (N_b + 1)), or complete the sharper count; propagate the chosen bound into the §4.3 LIP_shocked exponent and into any doc citing U4-B.

- VERIFIER NOTE: Statement/proof mismatch verified verbatim. docs/rde_nozzle_D25U_U3U4.md lines 285-286 state LEMMA U4-B as 'crosses fronts at most N_X <= N_F^max (N_b + 2) times'; the proof's first count dead-ends circularly ('legs <= N_b + N_X + 1...' line 295-296, N_X bounded via N_X, abandoned with an ellipsis) and the doc then explicitly records 'the cruder but simply-proved bound N_X <= 2 N_F^max (N_b + 1) and use[s] it' (lines 298-299). Since 2N_F^max(N_b+1) = N_F^max(2N_b+2) > N_F^max(N_b+2) for all N_b > 0, the displayed lemma bound is strictly sharper than anything the proof reaches — the header is unproven as stated. Refutation attempted and failed: no other derivation of the (N_b+2) coefficient exists in the doc, and the abandoned sharper count is genuinely circular as written. Severity low, not medium: the doc declares the crudeness inline ('QED (with declared crudeness)', line 302), explicitly declares the combinatorial constant 'NOT load-bearing' (lines 300-301), and the downstream §4.3 exponent uses N_X symbolically with the RECORDED (proved) bound ('and use it'), so the composed constant chain is internally consistent — only the lemma header needs the one-line correction to the proved bound.

### [CONFIRMED | medium | rejector-weakness] foundations-U:LBML-gradient-rate-rejector-cannot-fire

- FILE/LOC: docs/rde_nozzle_LBML.md — audit line lines 20-27 vs §5 S19 addendum lines 161-194; registry S-LBML falsifier

- CLAIM OF RECORD: S-LBML: the discrete gradient converges in the dual pairing at order h^2; declared falsifier: 'O3.2 (measured order < 2 - tol on smooth certified references kills the rate claim)' (also the registry falsifier field).

- FINDING: The S19 execution documented in this same file shows the declared rejector for the GRADIENT half of the claim structurally cannot fire: 'The ADJOINT exponent is NOT MEASURABLE ON A REFINEMENT LADDER AT ALL' because refinement cannot hold the march topology fixed (LB-c2) — so O3.2 can neither confirm nor kill the gradient-rate claim, on any run. The doc is honest about the mechanism and names the owed replacement experiment (fixed-topology family or truncation-insensitive functional), but neither the audit-line falsifier list (lines 21-24) nor the registry falsifier field ('O3.2 order check, O3.3 term match, F2') was amended: the claim of record still advertises a falsifier that is demonstrated non-operative for half of the claim. Until the replacement experiment exists, the gradient h^2 rate is a written claim with no executable rejector — the exact R5 configuration the culture forbids.

- EVIDENCE: docs/rde_nozzle_LBML.md lines 21-24 (audit-line falsifiers), lines 166-179 ('the ADJOINT exponent is NOT MEASURABLE ... the h^2 rate for the GRADIENT stays a written claim with its executable half deferred, and the paper must say so'); docs/claims_registry.yaml S-LBML entry line 624 falsifier unchanged ('O3.2 order check, O3.3 term match, F2 FD-vs-AD at fitted shocks'); X-O32 entry (registry line ~1196) confirms 'the ADJOINT rows are NOT USABLE on a refinement ladder'.

- SUGGESTED TEST: Amend the S-LBML audit line and registry falsifier to scope O3.2 to the TANGENT/primal half, and register the owed gradient-order experiment (fixed-topology ladder per D6 item 9 S19 annotation) as the gradient-half falsifier with a named carrier ID before the claim is cited in the paper.

- VERIFIER NOTE: Verified in both the doc and the registry. The claim of record (docs/rde_nozzle_LBML.md §1 line 59 and registry S-LBML statement, claims_registry.yaml line 618) asserts 'the discrete gradient converges in the dual pairing at the same order' AND the registry statement affirmatively adds 'the O3.2 oracle measures the actual order, making LB-c1 self-monitoring' — while the registry's own X-O32 carrier entry (line 1196) records 'the ADJOINT rows are NOT USABLE on a refinement ladder because refinement cannot hold the march topology fixed', and LBML §5 (lines 163-179) documents the mechanism ('the ADJOINT exponent is NOT MEASURABLE ON A REFINEMENT LADDER AT ALL... the h^2 rate for the GRADIENT stays a written claim with its executable half deferred, and the paper must say so'). Neither the audit-line falsifier list (lines 21-24) nor the registry falsifier field (line 624: 'O3.2 order check, O3.3 term match, F2 FD-vs-AD at fitted shocks') was re-scoped, and the owed replacement experiment (fixed-topology family, D6 item 9 S19 annotation) is named but not registered as a carrier. Refutation attempted: O3.3/F2 do touch the gradient but test fixed-h correctness, not the RATE, so they cannot kill the h^2 claim; the exact-transpose argument (step iv) cannot rescue measurability because the tangent on a ladder suffers the same topology transitions. So the gradient-half rate currently has no operative executable falsifier while its registry entry advertises one and asserts self-monitoring — the honesty of §5 mitigates but the machine-read claim of record is stale/contradicted by its own carrier row. Medium.

### [CONFIRMED | low | process] foundations-U:ledger-totals-miscount-CMAJDA

- FILE/LOC: docs/rde_nozzle_hypothesis_ledger.md — lines 23-28 (pass-1 totals), line 64 (C-MAJDA row), lines 133-135 (pass-2 totals)

- CLAIM OF RECORD: PASS-1 TOTALS: 0 DISCHARGED / 6 WEAKENED / 5 NECESSARY / 13 PRICED; PASS-2: .../ 5 NECESSARY (unchanged) / 17 PRICED; 'zero rows unadjudicated'.

- FINDING: The totals do not match the row verdicts. Hand count of the 24 rows: NECESSARY = 6 (H-A1, H-A2, H-T3.1, H-T3.3, H-E4 in §1, plus C-MAJDA whose §2 verdict cell reads 'NECESSARY(cex)') and PRICED = 12 at pass 1 — the headline books C-MAJDA under PRICED (5 NECESSARY / 13 PRICED), contradicting its own verdict column; the pass-1 parenthetical enumerating the NECESSARY counterexamples lists only the five §1 rows. The same off-by-one propagates to the pass-2 totals (5 NECESSARY / 17 PRICED, should be 6 / 16 by the rows). The doc's own falsifier is 'any row whose cited grounds do not support its verdict' — here the summary contradicts the rows, in the governance artifact other docs cite for 'the theory's hypothesis surface, adjudicated'.

- EVIDENCE: docs/rde_nozzle_hypothesis_ledger.md line 64: C-MAJDA verdict cell 'NECESSARY(cex)'; lines 23-27: '5 NECESSARY (every one with a cited counterexample: 2 machine-exhibited ..., 1 executable ..., 1 published ..., 1 first-order breaker)' — five items, all from §1; row-by-row count of §1+§2 yields 6 NECESSARY / 12 PRICED vs the stated 5/13 (both sum to 24, so exactly C-MAJDA is misfiled).

- SUGGESTED TEST: Add a totals lint to the suite: parse the two verdict tables, recompute the tallies, and fail on mismatch with the headline totals (pass 1, pass 2, and the living-ledger 29-row count).

- VERIFIER NOTE: Hand count reproduces the miscount exactly. docs/rde_nozzle_hypothesis_ledger.md line 64: C-MAJDA's verdict cell reads 'NECESSARY(cex)' (the vocabulary mandates 'one primary verdict per row', line 12). Pass-1 row count: §1 gives 5 NECESSARY (H-A1, H-A2, H-T3.1, H-T3.3, H-E4), 5 WEAKENED, 7 PRICED; §2 gives C-D25U WEAKENED, C-MAJDA NECESSARY, 5 PRICED — totals by rows 0/6/6/12, headline (lines 23-28) says 0/6/5/13; both sum to 24, so exactly one row (C-MAJDA) is booked under PRICED against its own verdict column, and the parenthetical enumerating the NECESSARY counterexamples lists only the five §1 rows. Pass-2 rows: 3 DISCHARGED (H2, H-I2, H-Pa), 4 WEAKENED (D3, H-T3.2, C-D25U, C-XBVP), 6 NECESSARY (the five §1 + C-MAJDA, whose S16 note explicitly says 'the NECESSARY verdict (wild-class cex) is untouched', line 64), 11 PRICED — headline (lines 133-135) says 5 NECESSARY / 17 PRICED (12+5), should be 6 / 16 (11+5). The doc's own falsifier ('any row whose cited grounds do not support its verdict') covers a summary contradicting its rows. Refutation attempted: the C-MAJDA grounds cell contains a 'Priced:' sub-note, but the verdict COLUMN is unambiguous and primary. Low: an arithmetic bookkeeping error in a governance headline, trivially lint-fixable, no downstream claim consumes the tallies.

### [CONFIRMED | low | underived-constant] foundations-U:U3BD-1e3-safety-multiplier

- FILE/LOC: validation/u3_bordered_front_solve.py — lines 26, 166, 199, 213, 231

- CLAIM OF RECORD: Carrier header: 'derived SVD/FD tolerances, no magic numbers'; doc quotes 'bordered s_min = 1.984e-01 >> derived floor 2.652e-07' (U3U4 §2.2).

- FINDING: The floor and tolerances all carry a bare 1e3 safety multiplier that is not derived anywhere in the file: floor = 1e3 * EPS**(2/3) * sv[0] (line 213), tolH = 1e3 * EPS * scaleH (line 166), eigenvector residual bound 1e3 * EPS (line 199), det-identity tolerance 1e3 * EPS**(2/3) (line 231). The EPS**(2/3) scale is legitimately derived from the central-difference step h = EPS**(1/3)*scale (line 104), but the three-orders-of-magnitude prefactor is a convention, contradicting the header's 'no magic numbers' claim — the same prefactor sets both the PASS floors and the R1 singularity-detection threshold, so its value is verdict-adjacent even though the current margins (0.198 vs 2.65e-07) dwarf it.

- EVIDENCE: validation/u3_bordered_front_solve.py line 26 ('derived SVD/FD tolerances, no magic numbers'), line 104 (h = EPS**(1/3)*scale, the derived part), lines 166/199/213/231 (the underived 1e3 prefactor in every tolerance and floor); docs/rde_nozzle_D25U_U3U4.md line 126 cites the resulting 2.652e-07 as 'derived floor'.

- SUGGESTED TEST: Derive the prefactor: propagate the FD Jacobian error bound (condition-number times EPS**(2/3) per entry, Weyl for the s_min perturbation) into an explicit floor, or declare 1e3 as a named safety factor with its rationale in the header — then re-run X-U3BD to confirm P2/P3/R1 verdicts are insensitive over a decade of prefactor variation.

- VERIFIER NOTE: Verified in validation/u3_bordered_front_solve.py: header line 26 claims 'derived SVD/FD tolerances, no magic numbers', yet every tolerance and floor carries a bare 1e3 prefactor with no derivation or named-safety-factor declaration anywhere in the file — line 166 (tolH = 1e3*EPS*scaleH), line 199 (eigenvector residual < 1e3*EPS), line 213 (floor = 1e3*EPS**(2/3)*sv[0]), line 231 (det-identity tol 1e3*EPS**(2/3)). The EPS**(1/3) FD step (line 104) legitimately grounds the EPS**(2/3) scale, but not the three-orders-of-magnitude multiplier. Refutation attempted: the comment 'same derived floors as X-G12' (line 164) was chased — validation/g12_shock_linearization.py lines 155 and 171 contain the identical bare 1e3 with no derivation either, so the pointer resolves to the same underived convention. Mitigations keep it low: the prefactor is verdict-insensitive at the current margins (P2: s_min 1.984e-01 vs floor 2.652e-07, ~6 orders; R1 degeneration is detected far below the floor), it sits on the conservative side for the PASS checks, and the U3U4 doc quotes the floor only as an instance datum. The defect is the header's 'no magic numbers' claim being contradicted by an undeclared 1e3 convention that also sets the R1 singularity-detection threshold.

### [SEED REFUTED] SEED-A7
- VERIFIER: The gap is real but its status is DECLARED at every relevant site, and the S20-operative constraint is a different object from the deferred constant chain. (1) The h_0(delta, C_0) deferral is explicit with a named trigger: docs/rde_nozzle_LBML.md §5 lines 197-201 — 'explicit in principle from the O(h) perturbation bound + the U3-L1 floors; writing its constant chain is bookkeeping deferred until a carrier needs the number (no number minted here without one)'. (2) The trigger has not fired: what S20 made operative is the DISCRETE certifiable set K (per-cell Newton certification + axial/causality margin + P4 floor — validation/PROGRESS_2026-08-07_S20_adaptive.md steps 7-9), not the continuum five-constant certificate; no S20 carrier consumes h_0 (the A1 TOC walk is shock-free — h_0 is the discrete FRONT-solve threshold) and the tier-0 discharge path uses a fold margin with its own derived floor. (3) The relation between the operative K and the class membership is itself DECLARED with a rigor class and falsifier, not left implicit: S20 step 9(c) writes the ladder A_t(mu_0) to M0 Part VI as SCHEMA and adjudicates ''K_disc approximates A_0' = CONJECTURE with falsifier = the Eq.(4)/Sternin monitor evaluated along the walk'; the formulation gap ('certifiable-set membership was implicit') was itself found, named and adjudicated in-session (Le Digabel-Wild classification, hypothesis_ledger C-O33 row). (4) The numeric-C_geo absence falls under the declared named-functions/sufficiency-discipline status (U3U4 honesty note lines 176-179; U1 audit line). So the seed's premise — a deferred constant chain whose deferral trigger fired undeclared — does not hold: every layer of the gap carries a dated declaration, a trigger, or a falsifier.


## p2-line (candidates dropped by finder cap: 4)

### [CONFIRMED | high | underived-constant] p2-line:f2-drift-tolerance-underived

- FILE/LOC: validation/o33_bench.py — o33_bench.py:88-91 (docstring), :599-601 (R1), :622-624 (R7), :611-612; vs docs/rde_nozzle_P2_lemmaA.md §3.7(b) lines 553-561 and docs/rde_nozzle_MASTER.md lines 1193-1198

- CLAIM OF RECORD: "TOLERANCES — DERIVED (R5): every band is a two-resolution Richardson estimate times the repo's reused K_RICH = 4, plus the accumulated Newton certification floor... No band is adjusted after a number is seen"; and (lemmaA §3.7(b)/M0) "the residual is the instance's discretization, identical for both designs".

- FINDING: The verdict-bearing R1/R7 rows for identification (ii) gate the f2 drift against a hardcoded 0.03 ("drift < 3%"), plus 1e-4 for the wall-representation check — none Richardson-derived, contradicting the carrier's own blanket derived-tolerance declaration. No two-resolution Richardson band for the f2 drift is ever computed anywhere in the bench; the doc-of-record attribution "residual = the instance's discretization" rests solely on the cross-design constant agreement (2.4e-04), an inference, not a refinement measurement. The 3% gate is 3x looser than the measured 9.5e-03, so a tripled residual would still PASS the row.

- EVIDENCE: o33_bench.py line 600-601: `ok &= check("R1 f2 constant ... (drift < 3%)", D_f2 < 0.03)`; line 624: `rep_geno["d2"] < 0.03`; line 612: `rep_err < 1e-4`. The Richardson machinery (refine(), K_RICH band) is applied only to R3 (lines 697-713). Docstring lines 88-91 claims every band is Richardson-derived.

- SUGGESTED TEST: Compute the f2 drift at r=1 and r=2 on the same design and derive the R1 band as K_RICH x |drift(r=1) - drift(r=2)| + Newton floor, replacing the 0.03 literal; the row must still PASS at 9.5e-03 and must FAIL a synthetically corrupted state table (existing negative-control pattern).

- VERIFIER NOTE: o33_bench.py:88-91 declares 'TOLERANCES — DERIVED (R5): every band is a two-resolution Richardson estimate times ... K_RICH = 4, plus the accumulated Newton certification floor', but the verdict-bearing gates are literals: :599-601 'R1 f2 constant ... (drift < 3%)' gated D_f2 < 0.03; :622-624 rep_geno['d2'] < 0.03; :611-612 rep_err < 1e-4 ('better than the march's contour band' — the S18 band of record is the measured max|dy|=1.861e-03, not 1e-4). The refine()/K_RICH Richardson machinery is applied only to the R3 band (:697-713); no f2-drift Richardson band is computed anywhere, and no derivation of 0.03 exists in the S19 log (grep '0.03|3%' over PROGRESS_2026-08-06_S19_o33.md: no matches) or any doc. The S19 gate declaration [D3] ('field bands from two-level Richardson x K_RICH = 4') is likewise contradicted by the coded gate. Measured drift 9.5e-03 means a tripled residual still passes. Mitigation checked and insufficient: only the R7 cross-design comparison rows (:619-631) use self-referencing derived bands; the constancy rows themselves are magic-gated. The doc attribution 'residual = instance's discretization' (lemmaA :557-561, M0 :1196-1198) rests on the cross-design agreement inference, not a refinement measurement — as claimed.

### [CONFIRMED | high | rejector-weakness] p2-line:corner-row-rejector-refuted-but-still-claimed

- FILE/LOC: validation/o33_bench.py — o33_bench.py:54-56 (R3 CONTROL claim), :539-545 ([D1] refusal premise), :837-850 (refutation note of record); docs/rde_nozzle_MASTER.md lines 1254-1262 (outcome-I licensing premise)

- CLAIM OF RECORD: R3 docstring: "CONTROL: the identity is DERIVED FROM OPTIMALITY..., so at a NON-optimal feasible design it must BREAK — that is the row's rejector"; [D1] gate: "the corner identity must BREAK at non-optimal designs".

- FINDING: The carrier's own R6 measurement refutes the premise the R3 rejector rests on: across the perturbed family the corner mismatch varies smoothly and monotonically (7.8e-2 -> 5.7e-2), the same order as at the optimum (6.6e-2), and the note of record withdraws the stationarity expectation — yet the docstring lines 54-56 still advertise the break-at-non-optimum control as the row's rejector, and the [D1] eligibility refusal (lines 539-545) plus M0's outcome-I licensing argument still cite the refuted premise. As instrumented, at the current design-class accuracy the corner row cannot distinguish the optimum from a 3%-perturbed design, i.e. identification (iii)'s declared falsifier cannot fire on this instance.

- EVIDENCE: o33_bench.py lines 847-850 print "corner mismatch across the family: %.3e -> %.3e (monotone, NOT stationary at the optimum — the expectation that it would be is refuted and withdrawn)" while lines 54-56 and 539-545 retain the break-at-non-optimum claim verbatim. No R3 check tests a non-optimal design against a break threshold.

- SUGGESTED TEST: Add an executable R3 control at a strongly non-optimal design (e.g. the 1.5% perturbed S18 start): quantify how far the corner mismatch must move to be distinguishable from the design-class floor, and either restore a firing rejector at that amplitude or rewrite the R3/[D1] docstrings to the two-knob-convergence falsifier only.

- VERIFIER NOTE: o33_bench.py:54-56 still advertises 'at a NON-optimal feasible design it must BREAK — that is the row's rejector' and :539-545 refuses non-[D1] designs on the same premise, while the same carrier's note of record (:837-850) reports the corner mismatch varying smoothly and monotonically across the ±3% family (7.847e-02 -> 5.690e-02, S19 log step 5) versus 6.6e-02 at the optimum — indistinguishable at the design-class floor — and withdraws the stationarity expectation. No R3 check tests a non-optimal design against a break threshold (the coded checks :768-776 are the two-knob convergence and the [D1] adaptive-vs-baseline comparison only), so the advertised rejector cannot fire. Worse, M0:1254-1262 (S20 block, written AFTER the S19 refutation) grounds the [D1] outcome-I licensing on '[X-O33B]'s own control says the identity must BREAK at a non-optimal design' — citing the refuted control as if established. Partial mitigation verified: the falsifier of record was re-scoped in-session to the two-knob convergence test (declared, o33:720-728, outline addendum (iv)), so identification (iii) is not falsifier-less — but the stale rejector claim and its propagation into the S20 [D1] eligibility logic stand as written.

### [REFUTED | medium | process] p2-line:preregistered-coordinate-substitution

- FILE/LOC: validation/o33_bench.py — o33_bench.py:5-6, :57-64 (R4) vs docs/rde_nozzle_P2_outline.md:189-194 (§5(b) pre-registration)

- REFUTED BY VERIFIER: The substitution was declared, not silent. Committed S19 session log (validation/PROGRESS_2026-08-06_S19_o33.md), step 2, gate declaration [D1], made BEFORE the decisive runs: 'The pre-registration NAMES the coordinate class as "L-P 2025 Eqs. (30)/(31)" ... the printed algebraic FORM of (30)/(31) is not transcribed in this repo ... Therefore the bench is built on the EQUATION-NUMBERED, PAGE-VERIFIED IN-HOUSE CORPUS for the same coordinate class (JOTA 1972 Eqs. (21)-(23) / HTH 1971 / Hoffman 1967 Eq. (54)) plus in-house derivation, and the alignment of our compatibility rows with L-P's equation numbers is recorded as a CITATION-level item that stays PENDING page re-verification. No content is claimed from an unverified page; the protocol's substance (WHICH coordinates: compatibility residuals + f2 drift; which are FORBIDDEN: (32)-(34) off uniform patches) is honoured exactly.' The gate step was committed (2664f95) before the bench commit (ceae2ae), and M0:1181-1182 designates the S19 log as part of the record. Residual cosmetic nit (lemmaA §3.7(c) calling the JOTA relation 'the coordinate the protocol registers' without repeating the pending-alignment caveat) does not sustain the finding's core claim of an undeclared deviation weakening the pre-registration.

### [REFUTED | medium | rejector-weakness] p2-line:primary-kill-pass-without-numbers

- FILE/LOC: docs/rde_nozzle_P2_lemmaA.md — rde_nozzle_P2_lemmaA.md §3.7(e) lines 592-596; docs/rde_nozzle_MASTER.md lines 1202-1204; validation/o33_bench.py:806-833 (bar construction)

- REFUTED BY VERIFIER: The R6 numbers ARE of record in two committed places. S19 log step 5: 'dJ/dt crosses zero at t = -5.061918e-04 and the Rao residual's V-vertex sits at t = -7.308913e-03, so |t_grad - t_Rao| = 6.8027e-03 <= the derived bar 3.5612e-02; the family discriminates (dJ/dt changes sign, +1.34e+06 to -1.40e+06)' plus the seven f2-drift V values. docs/rde_nozzle_PROGRESS.md:109-113 repeats |t_grad - t_Rao| = 6.80e-03 vs bar 3.56e-02. M0's campaign block header (:1181-1182) explicitly delegates 'full numbers and the honest residues in P2_lemmaA §3.7 and the S19 log', so the qualitative sentence in §3.7(e)/M0 is a summary with its numeric record named, not an unauditable pass. The bar-width concern is also bounded by the record: bar 3.5612e-02 < family half-width 3*t0amp ≈ 4.8e-02 (t0amp = 0.01*mean(W_STAR[1:-1]), W* :126-128), so the test as run had rejection power. The structural observation that bar_r = floor/V-slope carries no coded guard against a flat V is a fair improvement, but the finding's factual core ('no numbers at all ... never written down') is false.

### [REFUTED | medium | doc-code-mismatch] p2-line:o32-primal-exponent-verdict-omitted

- FILE/LOC: docs/rde_nozzle_P2_lemmaA.md — rde_nozzle_P2_lemmaA.md §3.7(f) lines 592-603 vs validation/o32_mesh_convergence.py:51-72 (pre-registered verdict rule)

- REFUTED BY VERIFIER: The finder applied the wrong rule. The verdict rule of record is NOT the module docstring's two-sided rule (o32:63-72, the step-3 text) but the OLDER binding falsifier committed in S16 with [S-LBML] itself (docs/rde_nozzle_LBML.md:22-23: 'measured order < 2 - tol on smooth certified references kills the rate claim' — ONE-SIDED), which predates the carrier. The carrier's verdict_row docstring (o32:224-243) declares the two-texts conflict openly ('the difference must not be able to move a verdict silently'), the code implements the binding rule (:273 'killed = p_f < 2.0 - dpt # the BINDING S16 falsifier') with the conclusiveness cap at the value its own derivation yields (1.0, :272), and prints both readings (:281-287). Under it, p_fine = 2.5347 with dp_tot = 0.6704 is conclusive and not killed -> PASS; the adjudication was declared in the S19 log step 4 BEFORE reading any four-level rate ('A RULE CORRECTION I OWE BEFORE ANY NUMBER'), and the row's full verdict with dp_noise = 4.48e-03, dp_model = 0.6659 is recorded there ('-> PASS, conclusive'). §3.7(f)'s supportive presentation therefore matches the verdict of record. Residual nit only: the module docstring :51-72 retains the superseded two-sided text while verdict_row supersedes it — an internal cosmetic inconsistency, not an unsupported claim.

### [CONFIRMED | medium | doc-code-mismatch] p2-line:lip-exclusion-not-applied-to-headline-drift

- FILE/LOC: validation/o33_bench.py — o33_bench.py:462-478 (surface_report inner mask) vs :260-272 (registered_mask) and :793-796 (R6 usage); docs/rde_nozzle_P2_outline.md addendum (ii) lines 231-233

- CLAIM OF RECORD: Outline §5 addendum (ii): "NORMS. The pre-registered exclusions (lip/corner, sonic/Sauer, axis) are unchanged and were applied".

- FINDING: The headline drift numbers of record (f2 9.5e-03 / 8.0e-03, f3* drift, wrong-family drift — lemmaA §3.7(b), M0 campaign block) come from surface_report, whose mask excludes STENCIL_RADIUS nodes only at the kernel-boundary end of the control surface; the lip end is retained up to the chain's O(h) single-node offset, i.e. inside the pre-registered lip/corner exclusion neighborhood (2 nodes). The registered_mask that drops both ends exists and is applied only in the R6 family loop. Since the inviscid adjoint is generically singular at the lip (the stated reason for the registration), including the lip-adjacent node can bias exactly the number the row reports, and the addendum's "were applied" is not accurate for the headline rows.

- EVIDENCE: o33_bench.py lines 469-471: `inner = cs_m.copy(); inner[idx[:STENCIL_RADIUS]] = False` — start end only; chain start at (K,1) is one node from the lip (lines 200-202, "an O(h) offset, declared"), less than STENCIL_RADIUS = 2. registered_mask (lines 260-272) drops both ends but is only used at line 795.

- SUGGESTED TEST: Recompute the R1/R2/R7 drifts with the lip-end STENCIL_RADIUS exclusion applied (registered_mask & locus_split) and record both numbers; if the drift moves materially, the 9.5e-03/8.0e-03 numbers of record must be re-issued under the registered norm.

- VERIFIER NOTE: surface_report (o33_bench.py:462-478) builds its mask as inner = cs_m.copy(); inner[idx[:STENCIL_RADIUS]] = False (:469-470) — dropping STENCIL_RADIUS=2 nodes at the kernel-boundary end only; the chain is ordered axis-ward->lipward (:218) so the lip end retains a node one grid-node from the lip (:199-202, the declared O(h) offset), inside the 2-node lip exclusion that the carrier's own registered_mask (:260-272, explicitly citing 'the lip end (lip/corner) — per P2_outline §5(a)') implements and which is applied only in the R6 family loop (:795). The headline numbers of record (f2 drift 9.5e-03/8.0e-03, f3* and wrong-family drifts — lemmaA §3.7(b), M0:1193-1196) all come from surface_report's laxer mask, while the bench docstring (:12-13, 'field-level rows are evaluated in the registered norm (lip/corner/sonic/axis excluded)') and outline addendum (ii) (:231-233, 'were applied') claim otherwise. The registered reason (inviscid adjoint generically singular at the lip, §5(a); the S19 lip-point probe measured degraded order p=0.763 there) makes the retained lip-adjacent node a real bias risk for exactly the reported quantity. No counter-evidence found: no doc declares the asymmetric mask.

### [CONFIRMED | medium | doc-code-mismatch] p2-line:propA3-planar-case-not-machine-verified

- FILE/LOC: validation/pa1_symbolic_lemmaA.py — pa1_symbolic_lemmaA.py:190-291 (part3, axisymmetric only) with the coverage claim at :233-236; docs/rde_nozzle_P2_lemmaA.md §3.4 Prop. A3 lines 409-433

- CLAIM OF RECORD: Prop. A3 (THEOREM, "machine-verified: validation/pa1_symbolic_lemmaA.py Part 3"): the closed-form family (lambda1, lambda2) = a (y^d rho v, u) + b (0,1) solves the multiplier PDEs — stated for planar (delta=0) AND axisymmetric (delta=1); the carrier comment says the planar case "is covered by the same check with y -> 1 at the end".

- FINDING: No such check exists: part3 verifies only the d=1 system (lam1 = y rho v, continuity with the y-weight) and never re-runs with the planar continuity/multiplier system. Because y is an active coordinate (its y-derivatives enter both the continuity substitution and lam1.diff(y)), setting y -> 1 is a genuinely different verification, not a corollary of the executed one. The planar half of the machine-verified label on a THEOREM-class claim is therefore carried by a comment describing a check that was never coded. Additionally, the derivation-of-record chain has a correlated-error exposure: the multiplier PDE system itself (lines 234-245) is transcribed from an off-carrier by-parts derivation, and the (3c) rejector only shows a corrupted PAIR fails the SAME system — nothing machine-checks the system against an independent anchor.

- EVIDENCE: pa1_symbolic_lemmaA.py lines 233-236: "(axisymmetric d=1 shown; the planar case replaces y by 1 and is covered by the same check with y -> 1 at the end)" — grep shows no second resid_system call with the planar system anywhere in the file; part3 runs resid_system exactly twice (lines 260, 285), both with the y-weighted forms.

- SUGGESTED TEST: Add the planar branch to part3 (continuity d_x(rho u) + d_y(rho v) = 0, lam1 = rho v) and a second anchor check that the transcribed multiplier PDE system reduces to JOTA 1972 Eqs. (21)-(23) in their published variables, with its own corrupted-system rejector.

- VERIFIER NOTE: pa1_symbolic_lemmaA.py:233-235 claims the planar case 'replaces y by 1 and is covered by the same check with y -> 1 at the end', but no such check exists: resid_system is invoked exactly twice (:260 for the HTH pair, :285 for the corrupted-pair rejector), both with the y-weighted axisymmetric system, and no y->1 substitution or planar continuity (d_x(rho u) + d_y(rho v) = 0, lam1 = rho v) appears anywhere in the file. The coverage claim is mathematically wrong as stated: y is an active coordinate — reduce_flow eliminates v_y via the y-WEIGHTED continuity (cont at :251-253 contains the extra rho*v term from d_y(y rho v)), so substituting y=1 into the already-reduced (identically zero) residuals verifies nothing about the planar system, which is a genuinely different PDE set. LemmaA §3.4 Prop. A3 (:410-419) states the closed-form family with y^d (d in {0,1}) as THEOREM 'machine-verified: Part 3'; the planar half of that label is carried by a comment describing an uncoded check. The correlated-error half also stands: the multiplier PDE system (:239-246) is hand-transcribed from an off-carrier by-parts derivation and the (3c) rejector only shows a corrupted PAIR fails the SAME transcribed system. Mitigations verified: the published anchor (HTH 1971 p. 1583) is page-verified, and every bench instance is axisymmetric — the verified case is the operative one; hence medium, not high.

### [CONFIRMED | low | doc-code-mismatch] p2-line:r4-order-claim-untested-dead-code

- FILE/LOC: validation/o33_bench.py — o33_bench.py:66-70 (O(h^2) claim), :358-367 (order_of_decay, dead code), :649-656 (0.05 separation factor)

- CLAIM OF RECORD: R4 docstring: the cancellation fraction is "O(h^2) when the sign is the right one for that family"; sign separation gated as min < 0.05 * max.

- FINDING: The claimed O(h^2) decay of the R4 compatibility residual is never measured: order_of_decay is defined and never called (confirmed by grep — single occurrence), and R4 runs at one resolution only. The 0.05 sign-separation factor is underived (the measured separation is ~1e-4 vs 1.0, so the gate is very slack). The R4 rejector itself (wrong sign saturates) is real and sharp; the defect is that the order property stated in the row's specification is unverified and its gate constant is magic.

- EVIDENCE: Grep over validation/: `order_of_decay` appears only at its definition (o33_bench.py:358). R4 checks at lines 649-656 use the literal 0.05 with no derivation; no refine() call feeds compat_fraction.

- SUGGESTED TEST: Run compat_fraction on the r=1 and r=2 records and feed order_of_decay; gate the C+/C- correct-sign fractions on a measured order in [2 - dp, 2 + dp] with dp from the two-triple drift, and derive the sign-separation gate from the measured saturation level.

- VERIFIER NOTE: order_of_decay is defined at o33_bench.py:358-367 and never called (grep over validation/: single occurrence, the definition); R4 evaluates compat_fraction at one resolution only (:639-648), so the docstring's 'O(h^2) when the sign is the right one' (:66-70) is asserted, never measured. The 0.05 sign-separation factor (:649-656) is a bare literal with no derivation in the carrier, the S19 log, or any doc — very slack against the measured separation (1.6e-04 vs saturation at exactly 1.0). The rejector direction itself is real and sharp (wrong sign saturates at 1.0, the S19 log calls it the sharpest rejector in the bench), which bounds the severity: the defect is an unverified order property in the row's specification plus a magic gate constant, not a rejector that cannot fire.

### [SEED REFUTED] SEED-A2
- VERIFIER: The gamma status of the Sternin/Rao-Beck Eq. (4) boundary IS declared in the corpus of record, and the monitor the program owns is not the perfect-gas closed form. D3 theorem_ledger (rde_nozzle_theorem_ledger.md:622-628): 'the valid/invalid-region boundary of Sternin 1962 (closed form in Rao-Beck 1994 Eq. 4) ... is ALREADY IMPLEMENTED, γ-free, as boundaryfunction_solve (GENO Rao_m.f90:30-56)'; rde_nozzle_claims_verdict.md:126: 'verified; implemented γ-free'; GENO/docs/variable_gamma_generalization.md §2bis: gamma-free by construction ('val=[Λ·B·(A+B)-(A-B)]/[1+Λ·(A+B)], A/B kinematic; Λ=V·dα/dV via 2 td%solve. Zero formula gamma=cost' — DIMOSTRATO-algebrico). The code confirms: Rao_m.f90:41-47 computes Λ numerically through the black-box thermo backend, 'no thermodynamic assumptions'. The M0 S20 block adopts Eq. (4) only as the CLASSICAL cross-check beside the EOS-general fold margin ('distance from same-family characteristic coalescence', AD gradient, KS-aggregated), flags the characteristic-naming translation, and D6 item 12 declares the Sternin/Shmyglevskii citation status (via Rao-Beck only, acquisition queued). Second half of the seed: lemmaA §3.0 (:54-75) carries the full gamma audit naming the actual gamma=const boundaries (corner<->eps bijection E4, Lemma B, S-H closed forms); Props. A2/A3 carriers keep c symbolic (EOS-general, machine-verified); the bench thermo is the table backend; the gammamedio Sauer start is a declared data contract. No silent perfect-gas relation found in the P-2 line. Residuals, both already declared in-repo: the M0 S20 block does not locally restate the γ status (covered by D3 P7), and the γ-free form's numeric validation against the constant-γ Eq. (4) is declared pending in GENO/docs/deep_crosscheck.md:129 — neither sustains the seed as a defect.


## engine-core (candidates dropped by finder cap: 10)

### [CONFIRMED | high | rejector-weakness] engine-core:F1-nan-cert-drop

- FILE/LOC: validation/a1_ideal_march_jax.py — lines 397, 399-403 (newton body), 678-681 (certify)

- CLAIM OF RECORD: "certified per cell, rejector by construction" (line 32); "A non-certifying cell FAILS the run — rejector by construction" (lines 86-87); check "all cells Newton-certified" (line 1159).

- FINDING: A cell whose Jacobian solve returns NaN (singular/near-singular Jz) passes certification silently. In the Newton body, `dz = jnp.where(jnp.isnan(dz), 0.0, dz)` zeroes the step, so the reported step metric `jnp.max(jnp.abs(dz))` becomes 0.0 and the while_loop cond exits on the very next trip believing it is at the floor. Post-hoc, `certify` computes `step = float(stepfn(z, p))` which is NaN for the same singular Jz, and then `cert["worst"] = max(cert["worst"], nan)` — Python's builtin max returns its FIRST argument when the second is NaN (`nan > x` is False), so the NaN metric is silently discarded, cert_worst stays unchanged, and the run PASSES. The declared rejector structurally cannot fire on exactly the failure mode (degenerate cell) it exists for.

- EVIDENCE: Line 397: `dz = jnp.where(jnp.isnan(dz), 0.0, dz)`; line 402-403 returns `jnp.max(jnp.abs(dz))` of the already-zeroed dz as the termination metric; line 680-681: `cert["worst"] = max(cert["worst"], step / (NEWTON_TOL_FACTOR * EPS * sc))` with `step` a raw Python float that can be NaN (jnp.linalg.solve raises nothing on singular input). CPython max(0.0, float('nan')) == 0.0 because comparison order puts the accumulator first.

- SUGGESTED TEST: Unit test: build an implicit solver whose residual has a rank-deficient Jacobian at the root (e.g. r(z)=[(z0-z1)^2, (z0-z1)^2]) or monkeypatch step_norm to return NaN for one cell; run a march containing it and assert the run FAILS certification. Today it passes with cert_worst unchanged. Fix: replace the max with a NaN-rejecting form, e.g. `if not (step <= bound): fail`, so NaN falls on the reject side.

- VERIFIER NOTE: a1_ideal_march_jax.py:397 zeroes NaN dz, so the loop metric jnp.max(|dz|)=0.0 and cond (386-390) exits as if converged; certify (678-681) computes step=float(stepfn(z,p)) which is NaN for the same singular/NaN-residual cell, and CPython max(cert_worst, nan) returns the first arg (nan>x is False), silently discarding the metric. No other NaN guard exists: _norm's NaN->inf (371) only steers candidate selection; the declared honest-stall path (362-365) covers finite stalls only; no negative control (N1-N4) exercises a singular-Jacobian/NaN-residual cell. Concrete trigger: a seed with M<1 makes arcsin(1/M) NaN -> r NaN -> dz NaN -> immediate exit with the unsolved seed, cert PASS. The 'rejector by construction' (lines 32, 86-87) structurally cannot fire on the degenerate-cell mode.

### [CONFIRMED | high | rejector-weakness] engine-core:F2-cert-record-only

- FILE/LOC: validation/a1_ideal_march_jax.py — a1_ideal_march_jax.py lines 677-682, 389-390, 1157; a1_march_scan.py lines 500-784 (make_run_scan_jit), 855-857/888-890 (O3.1 FD probes)

- CLAIM OF RECORD: Docstring: Newton "to ~machine (certified per cell, rejector by construction)" (line 32); printed as "[Newton certification] ... worst resid/tol" (line 1157); the scan/jit engines are presented as the production replay path.

- FINDING: Per-cell certification exists ONLY in record mode: `certify` returns immediately when `S.mode != "rec"` (line 677-678), and the while_loop cond silently caps at N_NEWTON=30 trips with no failure flag (lines 389-390). Every replay evaluation — including the verdict-bearing O3.1 finite-difference probes at P±h·v (a1_march_scan.py lines 855-857, 888-890, and A1 lines 1260-1263) and the entire jitted production path `make_run_scan_jit` (whose only certification is one build-time dummy cell, lines 567-572) — solves cells from stale seeds at a perturbed P with NO certification and NO rejector if a cell exhausts N_NEWTON unconverged. The numbers entering O3.1's lhs (FD side) can therefore come from uncertified solves while the verdict text implies floor-level solves everywhere. Additionally line 1157 mislabels the metric: it prints "worst resid/tol" but the quantity is a Newton STEP norm, not a residual — the honest-labeling question of the lens fails at exactly this citation.

- EVIDENCE: Line 677-678: `if S.mode != "rec": return`; line 389-390: cond = `it < N_NEWTON AND step > tol` (cap exit indistinguishable from convergence exit); make_run_scan_jit's `run` (a1_march_scan.py 582-782) contains no step_norm call; only the safe-where dummy is certified at build time (567-572). Line 1157: `"[Newton certification] %d cells, worst resid/tol = %.3e"`.

- SUGGESTED TEST: Replay the recorded schedule at P' = P + h·v with h large enough that some cell needs >30 trips from its stale seed (or set N_NEWTON=2 in a test) and assert the replay REFUSES or flags; currently it returns a contour silently. Minimal fix: have the replay/jit paths return a traced worst-step diagnostic (step_norm is already jitted) checked against the same bound, and rename the printed label to step norm.

- VERIFIER NOTE: a1_ideal_march_jax.py:676-677 'if S.mode != "rec": return' — certification exists only in record mode; while_loop cond (389-390) caps at N_NEWTON=30 with no failure flag (cap-exit indistinguishable from convergence). a1_march_scan.py run_scan and make_run_scan_jit contain no step_norm call on live cells; only the build-time dummy is certified (567-572). The verdict-bearing O3.1 FD probes evaluate f(P±h·v) via uncertified replay (A1:1260-1263; scan:853-857, 888-890). Mitigations checked and insufficient: base-point equivalence (scan:837-845) does not cover perturbed P; O3.1's tolerance is self-scaling, not a certification. Mislabel confirmed: line 1157 prints 'worst resid/tol' for a z-space step-norm ratio, contradicting the code's own comment (669-675) that the metric is deliberately NOT a residual.

### [CONFIRMED | medium | hidden-hypothesis] engine-core:F3-table-clamp-silent

- FILE/LOC: validation/a1_ideal_march_jax.py — a1_ideal_march_jax.py lines 344-351 (state_q); thermotab_c1_jax.py lines 143-147 (_locate), 189-197 (invert_h); thermotab C7 claim lines 31-35

- CLAIM OF RECORD: thermotab_c1_jax.py docstring: G>0 "Certified on the declared box = the full table T range (contains every march-realized state)" (lines 31-33); state_q presented as the EOS-general closure of record for every march state.

- FINDING: The closure has no domain rejector. `jnp.interp` in state_q silently CLAMPS when ht = h0 - q²/2 leaves the tabulated h range (T pinned to 1050 K or 3900 K) and its gradient outside the range is exactly zero — so an out-of-box march state (or an FD probe at P±h that pushes q past the box) produces a wrong-physics state with silently zeroed sensitivities, and Newton then certifies the WRONG-but-consistent clamped system to the floor, so cert_worst cannot catch it. The quintic path is worse in the opposite direction: `_locate` clips the interval index, so outside the grid the quintic EXTRAPOLATES polynomially without bound and invert_h iterates on it. The hypothesis "the box contains every march-realized state" is asserted in the C7 audit but never enforced by any in-march check in any of the three carriers.

- EVIDENCE: Line 346: `T = jnp.interp(ht, hg, Tg)` (jnp.interp clamps to endpoints, zero out-of-range derivative); thermotab line 144: `i = jnp.clip(jnp.searchsorted(...) - 1, 0, n - 2)` with no range guard; grep confirms no assert on T/ht vs [T_TAB_LO, T_TAB_HI] anywhere in run_march / run_scan / make_run_scan_jit.

- SUGGESTED TEST: Run state_q with q such that ht < h(T_TAB_LO) and assert a rejection; currently it returns T = T_TAB_LO with dT/dq = 0 and the march proceeds. Add a traced min/max-T (or ht-margin) diagnostic to the out-dict, checked like cert_worst; assert it fires on a case with eps large enough to exit the box.

- VERIFIER NOTE: a1_ideal_march_jax.py:345-346: jnp.interp clamps out-of-range ht/T to table endpoints with exactly zero exterior derivative — Newton then certifies the wrong-but-consistent clamped system, so cert_worst cannot fire. thermotab_c1_jax.py:144: _locate clips the interval index, so the quintic extrapolates polynomially without bound and invert_h (189-197) iterates on it. Grep confirms no assert/diagnostic on T or ht vs [T_TAB_LO, T_TAB_HI] in run_march, run_scan, or make_run_scan_jit. The C7 hypothesis 'the declared box contains every march-realized state' (thermotab:31-32) is asserted in the docstring and never enforced by any carrier — a hypothesis without a rejector, per the repo's own R5 standard.

### [CONFIRMED | medium | rejector-weakness] engine-core:F4-itref-cap-silent-exit

- FILE/LOC: validation/a1_ideal_march_jax.py — lines 763, 848-850 (exit decision), docstring lines 46-48, m_stop comment lines 838-847

- CLAIM OF RECORD: Docstring: "STOP at axis M = Me via GENO's linear wall-angle interpolation to |M - Me| < 1e-5 (GENO algorithmic constant, mirrored), then Me := achieved M" (lines 46-48); the S19 m_stop comment declares m_stop as THE accuracy floor of record.

- FINDING: The exit decision is `"exit" if (abs(float(Max - Me)) < m_stop or it_ref > 30)`: after 30 wall-angle interpolation refinements the march declares exit REGARDLESS of |Max - Me|, silently redefines Me_ach := Max, and raises no flag, so a run whose achieved exit Mach misses the target by far more than the declared 1e-5 floor still passes every check (downstream is self-consistently built on the achieved Max). The 30-cap is an undeclared magic constant: the docstring and the long m_stop provenance comment declare only the |M-Me|<1e-5 mirror, never the iteration-cap escape, and the S19 O3.2 diagnostic ("tighten m_stop, re-measure the exponent") can be silently defeated by the cap without any observable difference in the verdict output.

- EVIDENCE: Line 848-850: `code = S.dec(lambda: ("exit" if (abs(float(Max - Me)) < m_stop or it_ref > 30) else ...))`; it_ref incremented only in the interp branch (line 860); no print/flag/out-dict field distinguishes cap-exit from converged exit; docstring lines 46-48 and comment lines 838-847 mention only the 1e-5 constant.

- SUGGESTED TEST: Run with m_stop tightened (e.g. 1e-9, the pre-declared O3.2 diagnostic) and assert the carrier either converges or REJECTS; currently a cap-exit returns Me_ach with |Me_ach - Me| >> m_stop and reports success. Minimal fix: record the cap-exit as a distinct decision value and fail (or at least surface) `|Me_ach - Me| >= m_stop` in the out-dict.

- VERIFIER NOTE: a1_ideal_march_jax.py:848-851: code = 'exit' if (abs(float(Max-Me)) < m_stop or it_ref > 30) — after 30 interp refinements the march exits regardless of |Max-Me|, sets Me_ach := Max, and no out-dict field, flag, or print distinguishes cap-exit from converged exit. it_ref incremented only in the interp branch (860). The docstring (46-48) and the long m_stop provenance comment (836-847) declare only the |M-Me|<1e-5 GENO mirror, never the 30-cap escape. Line 1189-1191 even prints the claim '|.-Me_target| < 1e-5 each' without checking it. The pre-declared S19 O3.2 diagnostic (tighten m_stop) can be silently defeated by the cap with no observable difference — verified: nothing downstream tests |Me_ach - Me| < m_stop.

### [CONFIRMED | medium | underived-constant] engine-core:F5-underived-factors

- FILE/LOC: validation/a1_ideal_march_jax.py — lines 147 (NEWTON_TOL_FACTOR), 1228-1232 (S5 bound), 1256-1258 (replay-fidelity ×10); a1_march_scan.py line 840 (tol_eq ×10); thermotab_c1_jax.py lines 84-87 (C_OPS, N_NEWT_INV)

- CLAIM OF RECORD: "TOLERANCES — ALL DERIVED (R5, no magic numbers)" (line 81); R5 in CLAUDE.md: "tolleranze derivate, non magiche".

- FINDING: Several verdict-bearing tolerances contain asserted, not derived, factors: (a) NEWTON_TOL_FACTOR = 100 is justified only as "spike factor convention" — an inherited assertion, not an operation-count bound for THESE residuals; (b) the S5 primary-vs-interop bound uses `100.0 * budget * max(wy)` where the comment derives ~9 (isentrope exponent) and hand-waves the remaining ~11x as "march accumulation" with no derivation — this factor decides the PASS of the primary-route consistency check; (c) the equivalence tolerance `tol_eq = NEWTON_TOL_FACTOR * EPS * scale * 10.0` (a1_march_scan.py:840) and the replay-fidelity tolerance (A1:1258) carry a bare ×10 headroom with no stated origin; (d) C_OPS = 100 is again "repo convention". Each of these is a knob that could be (or has been) sized to make its check pass, which is exactly what R5 forbids.

- EVIDENCE: Line 147: `NEWTON_TOL_FACTOR = 100.0` with only "spike factor convention" (line 86); line 1231: `bound = 100.0 * budget * float(np.max(wy)) + ...` with comment "covers the isentrope exponent ~ gam/(gam-1) ~ 9 and march accumulation"; a1_march_scan.py:840: `tol_eq = A1.NEWTON_TOL_FACTOR * EPS * scale * 10.0`; thermotab line 85: `C_OPS = 100.0  # operation-count roundoff factor (repo convention)`.

- SUGGESTED TEST: Sensitivity audit: rerun each carrier with the asserted factor halved (50 instead of 100, ×5 instead of ×10); any check that flips PASS→FAIL identifies a tolerance whose margin is load-bearing and must be derived (op-count/condition-number bound for NEWTON_TOL_FACTOR; a per-cell sensitivity accumulation bound for the S5 factor) or the number reclassified.

- VERIFIER NOTE: Header claims 'TOLERANCES — ALL DERIVED (R5, no magic numbers)' (line 81). Confirmed underived: (a) NEWTON_TOL_FACTOR=100 justified only as 'spike factor convention' (85-86, 147); (b) verdict-bearing S5 bound = 100.0*budget*max(wy) (1231) where the comment derives ~9 and asserts the remaining ~11x as 'march accumulation' with no derivation; (c) bare ×10 headroom in tol_eq (a1_march_scan.py:840) and replay-fidelity (A1:1257-1258) with no stated origin. Partially refuted component: C_OPS=100 does carry an op-count sketch ('quintic evaluation, ~64 flops -> C = 100', thermotab:38-40), so (d) is weakly derived. The surviving core — the load-bearing S5 factor and the two bare ×10s — stands with line evidence.

### [CONFIRMED | low | doc-code-mismatch] engine-core:F6-node-check-vacuous

- FILE/LOC: validation/thermotab_c1_jax.py — lines 87 (N_PROBE comment), 282-289 (C3 node subset)

- CLAIM OF RECORD: Comment line 87: "N_PROBE = 20001  # dense probe grid (odd, hits nodes and midpoints)"; C3 comment line 282: "nodes: exact by construction (verify at the node subset)" with the printed "node-set max" figure.

- FINDING: The node-exactness verification is vacuous. With N_TAB = 8192 nodes (8191 intervals) and a 20001-point probe (20000 intervals) over the same span, nodes fall at probe-index spacing 20000/8191 ≈ 2.442 — the probe grid does NOT hit the nodes, so the docstring premise is false. Worse, `node_idx = np.arange(0, N_PROBE, (N_PROBE-1)//(len(Tg)-1))[:len(Tg)]` computes stride (20000//8191) = 2, i.e. it samples every second probe point from the START of the range — an arbitrary subset of the first ~40% of the interval, not the nodes. The printed "node-set max" therefore verifies nothing about node exactness of s0' = cp/T; the declared invariant "exact at nodes" is never actually checked (the overall gap check on the full probe is unaffected, but the specific node-exactness claim rests on a broken index computation).

- EVIDENCE: Line 283: `node_idx = np.arange(0, N_PROBE, (N_PROBE - 1) // (len(Tg) - 1))[: len(Tg)]` — (20001-1)//(8192-1) = 2, so node_idx = [0, 2, 4, ..., 16382], covering only T in [Tg[0], Tg[0] + 0.82·span·...] at points that are not nodes; nodes would require fractional probe indices (incommensurate grids).

- SUGGESTED TEST: Evaluate s0p directly AT jnp.asarray(Tg) (the actual nodes) and assert |s0'(Tg_i) - cp_i/Tg_i| <= C_OPS·EPS·scale; also fix or drop the "hits nodes and midpoints" comment (true only if N_PROBE-1 is a multiple of N_TAB-1, e.g. N_PROBE = 2·(N_TAB-1)+1 = 16383).

- VERIFIER NOTE: thermotab_c1_jax.py:283: (N_PROBE-1)//(len(Tg)-1) = 20000//8191 = 2, so node_idx = [0,2,...,16382] — even probe indices covering only ~82% of the span; true nodes fall at fractional probe indices 20000·i/8191 ≈ 2.442·i (incommensurate grids), so the printed 'node-set max' samples non-node points and the comment 'odd, hits nodes and midpoints' (line 87) is false. Severity capped at low after refutation attempt: the check that gates PASS is gap.max() <= floor on the full probe (line 290) — the node subset is print-only — and node exactness of s0' = cp/T is structurally guaranteed by the Hermite construction (s0_d = cpg/Tg node data, basis derivative values). The defect that stands is a declared verification that verifies nothing plus a misleading printed figure.

### [CONFIRMED | low | underived-constant] engine-core:F7-legge-bracket-fallback

- FILE/LOC: validation/a1_ideal_march_jax.py — lines 720-729 (exit-Mach root bracketing)

- CLAIM OF RECORD: Docstring: "exit Mach from eps (leggeAree twin ... supersonic root, implicit-solver wrapped)" (lines 41-42); R5: no silent fallbacks, procedures general not case-tuned.

- FINDING: The supersonic-root seed for the leggeAree solve is found by scanning q in the magic window [1.01·as, 2.8·as] with 400 samples, and when NO sign change is found the code silently falls back to `q0 = as_ * 1.8` (line 726) — a guess with no rejection or even a warning. For a design point whose supersonic root lies above 2.8·as (large eps) or an eps near 1 the bracket fails, the fallback seed is used, and Newton on rho(q)·q = target can converge to the SUBSONIC root of the mass-flux relation; nothing at this stage checks M(qe) > 1. The march would then fail only indirectly (NaN mu downstream) or, worse, near-sonic could complete plausibly. All three constants (1.01, 2.8, 400) are case-tuned to the reduced twin case, against the declared generality directive.

- EVIDENCE: Lines 722-726: `qs = np.linspace(float(as_) * 1.01, float(as_) * 2.8, 400)` ... `q0 = qs[idx[-1]] if idx.size else float(as_) * 1.8` — the else-branch is a silent fallback; no `assert idx.size` and no post-solve `M(qe) > 1` check before qe is consumed at line 733.

- SUGGESTED TEST: Call run_march with eps large enough that the supersonic root exceeds 2.8·as (or mock state_q so the flux window misses the target) and assert a REJECTION; currently the fallback seed is taken silently. Minimal fix: `assert idx.size` plus a post-solve rejector `state_q(qe)[5] > 1 + margin`, with the scan window derived from the table box (q_max from h0 - h(T_TAB_LO)).

- VERIFIER NOTE: a1_ideal_march_jax.py:722-726: qs = linspace(1.01·as, 2.8·as, 400); q0 = qs[idx[-1]] if idx.size else float(as_)*1.8 — the else-branch is a silent guess with no assert or warning, and no post-solve M(qe)>1 check exists before qe is consumed (733). Partial mitigation found in refutation: idx[-1] selects the LAST sign change, i.e. the supersonic root when the bracket succeeds, and the window is pinned to the declared reduced CASE — but the silent fallback violates the no-silent-fallback rule regardless, and a subsonic-root convergence would surface only as downstream NaN (which F1 shows can itself pass certification silently). Record-mode only, declared fixed case: low.

### [CONFIRMED | low | numerics] engine-core:F8-cert-scale-unit-mixing

- FILE/LOC: validation/a1_ideal_march_jax.py — lines 386-390, 669-681 (certification scale)

- CLAIM OF RECORD: Comment lines 669-676: "UNIT-CONSISTENT certification: the residual rows mix units ... so certify in z-space instead: one extra Newton step ... less than NEWTON_TOL_FACTOR * eps * scale(z) (Newton at the roundoff floor)".

- FINDING: The certification bound uses a single scalar scale `sc = max(1.0, max|z|)` applied to the max-norm of the whole step, but z itself mixes units: for interior cells z = [x4, y4, u4, v4] with u ~ O(10^3) m/s and x, y ~ O(1) m, so sc is dominated by u and the POSITION components are certified only to 100·eps·|u| — roughly 3 orders of magnitude looser than their own roundoff floor 100·eps·|x|. The "unit-consistent" fix moves the unit-mixing problem from residual space into z-space rather than eliminating it; the per-component guarantee on the contour coordinates (the quantity actually compared to GENO and returned as wall_x/wall_y) is weaker than the "Newton at the roundoff floor" phrasing states. Same structure propagates into tol_eq (a1_march_scan.py:839-840) where a single global max|output| scales all components including small-y early-wall points.

- EVIDENCE: Line 388: `sc = jnp.maximum(1.0, jnp.max(jnp.abs(z)))` and line 679: `sc = max(1.0, float(jnp.max(jnp.abs(z))))` — one scalar for a mixed-units vector; step is `jnp.max(jnp.abs(...))` over all components (line 422), so a position step of 1e-11 m passes when u4 ~ 2.5e3.

- SUGGESTED TEST: Recompute cert_worst with a per-component scale vector (position rows scaled by max(|x|,|y|,yt), velocity rows by max(|u|,|v|,as)) on the existing recorded march and report the new worst ratio; if it exceeds 1, the current PASS is an artifact of the scalar scale and the bound must be per-component.

- VERIFIER NOTE: a1_ideal_march_jax.py:388 and 679: sc = max(1, max|z|), one scalar over z = [x4,y4,u4,v4] with u ~ O(10^3) m/s (as_ ≈ 1300+ m/s from the real tables) vs x,y ~ O(1) m — position components are certified only to ~100·eps·|u|, roughly 3 decades looser than their own roundoff floor, while the comment (669-676) claims the z-space metric is 'UNIT-CONSISTENT' and 'Newton at the roundoff floor'. The premise is internally inconsistent: z mixes units exactly as the residual does. Severity low after refutation: the declared metric matches the implementation, the max-norm bound is still a valid (if loose) guarantee, and no verdict-bearing check is compromised — the contour bands are dominated by the Richardson estimate and tol_eq passes on actual (much tighter) convergence.

### [SEED CONFIRMED] SEED-A3
- VERIFIER: The Sauer IVL is gamma=const on gammamedio (a1_ideal_march_jax.py:694-701, alpha/c1/c2/eshift all in (gm+1)) and is declared a shared data contract (37-40), but its error is never measured: both table routes carry the identical plt['gammamedio'] (263, 316), the GENO twin uses the same start, and the two-resolution Richardson re-runs the same start at both resolutions — every declared cross-check (S3, S4, S5, O3.1) is exactly correlated with it and structurally blind. Grep over validation/ confirms no carrier quantifies the start-model error: o32_mesh_convergence.py EXCLUDES the Sauer-IVL neighborhood from its field norms (lines 25-31, 328-331) and a1_toc_variational_jax.py reuses the same IVL (249-252, 430-433). Absolute contour/Me numbers of record therefore inherit an unquantified correlated start error; the declaration exists, the quantification does not.

### [SEED CONFIRMED] SEED-A4
- VERIFIER: a1_ideal_march_jax.py:283-288 declares the extrapolation and deliberately mirrors it into the PRIMARY Cantera route (spc.thermo = ct.NasaPoly2(Tlow, max(Thigh, T_TAB_HI), ...)), making the dual-route rejector structurally unable to see it; nasa_funcs (236-244) evaluates the blended polynomial at any T with no Thigh guard. The region is exercised by real states: tables span to 3900 K and stagnation ts ≈ 3600 K exceeds H's Thigh = 3500 K. Grep for 'Thigh'/'extrapolat' across the repo's own code finds only the declaration comments (A1:283-286, PROGRESS S11 note) — no executable check quantifies the extrapolation error against valid-range thermo. Status remains suspected-not-measured, exactly as the critical list holds it, with no measurement carrier.


## variational-driver (candidates dropped by finder cap: 9)

### [REFUTED | high | rejector-weakness] variational-driver:certlimited-stale-res-unconsumed-flag

- FILE/LOC: validation/a1_toc_variational_jax.py — run_trsqp lines 848-872 (certifiability-limited return), line 1017 (result=res), main() lines 1339-1404

- REFUTED BY VERIFIER: The headline scenario is control-flow impossible. The in-loop break (a1_toc_variational_jax.py:1040-1043) returns NORMALLY (certifiability_limited=False, line 1046-1049) whenever res.status==1, or status==2 with optimality<=10*gtol, or stalled; a status-2 segment whose constr_violation>gtol raises at 1028-1031. Therefore any `result` carried into the exhaustion return (line 864-867)必 comes from a segment that CONTINUED the loop, i.e. has status in {0,3,4} or status 2 with optimality>10*gtol. main() then necessarily fails at least one wired check: status∉(1,2) fails the check at 1349-1352, and status-2 with optimality>10*gtol fails the O3 check at 1356-1358 — ok becomes False and the bench exits 1 (line 1543). So 'O3 transversality PASS for a KKT-OPEN run' cannot occur and no outcome-II run can exit 0. The flag IS consumed by the outcome-II consumer of record: the S20 [X-AKNO] carrier excludes certifiability-limited cycles from best/[D1] and writes d1_eligible=false which the [X-O33B] loader refuses (PROGRESS_2026-08-07_S20_adaptive.md steps 7[D] and 8). Residual blemish only: one individual check line (1349) can print PASS and the multiplier print at 1395-1404 can use the stale res while the overall verdict FAILs — a reporting cosmetic, not the claimed unenforced restriction.

### [CONFIRMED | high | rejector-weakness] variational-driver:gnp-zeroes-nonfinite-gradient

- FILE/LOC: validation/a1_toc_variational_jax.py — run_trsqp g_np lines 952-955, f_np line 951; O3 verdict at lines 1345-1358

- CLAIM OF RECORD: O3.1 is 'the safe-where NaN-leak detector' for the jit path (lines 508-510, 1291-1294) and res.optimality is the executable transversality statement (O3, lines 58-64).

- FINDING: g_np returns np.where(np.isfinite(g), g, 0.0): any NaN/Inf component of the adjoint gradient is SILENTLY replaced by 0 in the gradient scipy uses to compute res.optimality — the exact quantity the O3 transversality check and the in-loop break test consume. The O3.1 leak detector runs ONCE, at W0 on plan0's jit function; every later segment builds a NEW jitted replay from a new plan (new padded lanes, new dummies) that is never leak-checked, so a NaN leak arising mid-walk manifests as an artificially small KKT residual and a spurious O3 PASS with no rejector able to fire. The paired f_np fallback 'v if np.isfinite(v) else 1e30' is an underived magic constant on the same silent path (comment admits 'monitor-lite').

- EVIDENCE: Lines 952-955: 'g = np.asarray(g) * Dv; return np.where(np.isfinite(g), g, 0.0)'. Line 951: 'return v if np.isfinite(v) else 1e30  # monitor-lite NaN guard'. The only O3.1 execution is at lines 1269-1294 on runj0 (plan0); make_run_toc_scan_jit is re-invoked per segment at line 878 with no per-segment leak check.

- SUGGESTED TEST: Inject a NaN into one gradient component inside g_np for the final segment (test shim): the run must FAIL loudly, not report res.optimality below tol. Minimal fix-test: assert run_trsqp raises (or flags) when any g_np output component was non-finite at the returned iterate; add a per-segment O3.1 spot-check or a finiteness counter carried into the Verdict.

- VERIFIER NOTE: Line 955: g_np returns np.where(np.isfinite(g), g, 0.0) — non-finite adjoint-gradient components are silently replaced by 0 in the jac scipy uses; res.optimality (the O3 verdict quantity at 1356-1358 and the in-loop break at 1041) is computed from exactly this zeroed gradient, so a NaN leak REDUCES the KKT residual instead of failing. No refuting guard exists: the O3.1 leak detector runs once, on plan0's runj0 only (1269-1294); make_run_toc_scan_jit is rebuilt per segment (878) and its build-time certification covers only the safe-where DUMMY cells (562-578), not the real lanes of the new plan; the base fidelity monitor cannot fire on NaN either, because line 885 tests `dev > tol` which is False when dev is NaN. f_np's fallback 1e30 (line 951) is an underived constant on the same silent path, admitted 'monitor-lite'. This is a rejector that structurally cannot fire on the exact failure mode (masked-branch NaN through the transpose) that the S18 record says O3.1 exists to catch.

### [REFUTED | medium | doc-code-mismatch] variational-driver:ratchet-caps-only-segment-initial-radius

- FILE/LOC: validation/a1_toc_variational_jax.py — run_trsqp lines 1018-1022 (carry clip), docstring note (ii) lines 736-743, S20 sticky-shrink comment lines 832-838

- REFUTED BY VERIFIER: The declared guarantee is carry/boundary-scoped, matching the mechanism exactly. Code comment 1019-1021 claims only that 'the CARRY is clipped by the RATCHETED cap'; the S20 fix declaration of record says the same ('tr_cap only ever decreases on a certification rejection and clips every later carry', PROGRESS_2026-08-07_S20_adaptive.md step 6 fix (i)). The DIR-RKG semantics of record define the excursion bound as the CURRENT trust radius (rde_nozzle_brick2_kickoff.md:41-43 'The trust radius is the excursion bound…'; S18 log step 6: 'the RK-G excursion bound is the CURRENT radius'), and the adoption decision explicitly declares the trade-off that scipy's inner iterates are not under driver control, with guarantees enforced at the evaluation boundary (kickoff 403-408). Within-segment radius growth is therefore inside the declared contract; every within-segment ACCEPTED iterate is still P3(ii)-certified by the callback (987-994), so an over-grown step cannot silently survive. The livelock claim holds as stated: the measured livelock was the CARRY re-capturing scipy's grown radius (832-838); with the ratchet, tr_cap strictly decreases per rejection to the floor and terminates in declared outcome II — and the decisive S20 run measured exactly that ('Ratchet monotone 0.10 -> … -> 1.000e-03 (floor) with NO livelock', step 8). The suggested test would show tr_radius>tr_cap within a segment, but no claim of record asserts otherwise.

### [CONFIRMED | medium | correlated-error] variational-driver:objective-omits-throat-panel

- FILE/LOC: validation/a1_toc_variational_jax.py — thrust_J lines 705-712; record stations lines 300-309; objective declaration lines 22-27

- CLAIM OF RECORD: 'J = 2 pi * sum_trapz p * y * dy over the DESIGN wall (arc sector theta in (0, theta_B] + contoured section to the lip)' with Pa dropping out because 'the ambient term Pa*(A_lip-A_0) is constant on the feasible set'.

- FINDING: The wall array's first point is the arc station at theta_1 = thB/n_B (stations start at k=1, line 301), so the trapezoid actually covers [theta_1, L]: the throat panel from y_t to y(theta_1) = yt + rtd*(1 - cos(thB/n_B)) is omitted, and its extent DEPENDS ON THE DESIGN DOF thB (n_B is frozen per record). dJ/dthB therefore carries a systematic bias ~ p_throat*yt*rtd*sin(thB/n_B)/n_B relative to the declared integral, an error INVISIBLE to O3.1 because AD and FD both differentiate the same truncated discrete J (correlated by construction). The transversality instance (O3) is thus stationarity of the truncated functional; whether the bias is below 10*gtol is never measured, and the Pa-drop argument (A_0 = throat area constant) presumes the integral starts at the throat, which it does not.

- EVIDENCE: Lines 300-304: 'for k in range(1, n_B + 1): th = thB * k / n_B' — no theta=0 station; thrust_J lines 711-712 sums trapz only over supplied points; docstring lines 23-24 declares the (0, theta_B] sector. O3.1 (lines 1280-1294) compares AD vs FD of scalar_J built from the SAME wall array.

- SUGGESTED TEST: Add the analytic throat-panel term int_{yt}^{y(th1)} p y dy (arc geometry is exact) to J and re-run the OPT stage: assert |dJ/dthB(with panel) - dJ/dthB(without)| < gtol at W*; if it exceeds gtol the recorded transversality verdict moves — the bias must be either included or bounded into the derived KKT tolerance.

- VERIFIER NOTE: Facts verified: stations start at k=1 (lines 300-304: th = thB*k/n_B), the wall array (400) has no theta=0 throat point, and thrust_J (705-712) trapezoids only supplied points, so the panel [y_t, y_t+rtd(1-cos(thB/n_B))] is omitted with extent depending on the design dof thB (n_B frozen per record, line 209). No refutation found: no doc adjudicates the omission (grep across kickoff/S18/S20 progress finds no throat-panel or A_0-anchor discussion), and the Pa-drop declaration (lines 25-27) presumes the integral anchored at the constant throat area A_0, whereas the truncated integral's anchor A(theta_1) varies with thB. Magnitude: d(missing panel)/dthB ≈ 2π·p_throat·y·rtd·sin(thB/n_B)/n_B ≈ O(10^3) in J-units (p~5e6 Pa, rtd=0.45, th1≈da=0.0087, n_B≈23) vs the O3 acceptance 10·gtol ≈ 9.3 (S20 gtol=9.338e-01 of record) — orders of magnitude above the declared KKT tolerance, and invisible to O3.1 since AD and FD differentiate the same truncated discrete J (1280-1294). The O3 verdict is thus stationarity of a functional that measurably differs from the declared 'DESIGN wall' pressure-thrust integral in its thB row, with the bias never measured. Kept medium, not high: the discrete carrier is internally consistent, the contour-level optimum shift is far inside the O2 band, and the continuum term-match is explicitly deferred to the pre-registered O3.3 campaign (58-64).

### [CONFIRMED | medium | underived-constant] variational-driver:cross-unit-and-slack-tolerances

- FILE/LOC: validation/a1_toc_variational_jax.py — lines 1027-1031 (status-4 rejector), 1321-1323 (gtol/xtol), 1349-1358 (verdict checks), 885/1254 (tol_rep x10)

- CLAIM OF RECORD: R5 / header: 'TOLERANCES — ALL DERIVED... KKT residual bound = trust-constr's own gtol x a measured gradient scale' (lines 72-76); 'tolerances derived, not magic' (CLAUDE.md R5).

- FINDING: Verdict-bearing tolerances mix underived constants and incompatible units: (a) the status-4-class infeasibility rejector fires on res.constr_violation > gtol, comparing a lip-height violation (length units, O(1) scale) against gtol derived in gradient units (max(tol_dp, 1e-8*gscale)) — with gscale reported ~1e5, gtol ~1e-3 in J/W units accidentally polices a length; the rejector's firing threshold is dimensional coincidence, not derivation; (b) the 1e-8 multiplier on gscale, xtol=1e-10 ('Newton floor on W scale' asserted, not derived), the 10x slack in 'optimality <= 10.0*gtol' (both loop break and O3 check), and the 10.0 in the replay-fidelity tol (NEWTON_TOL_FACTOR*EPS*scale*10.0) are all magic factors inside PASS/FAIL gates. Additionally res.optimality is the norm of the Dv-SCALED Lagrangian gradient (components rescaled by up to sqrt(kappa)~6) while gtol was derived from the PHYSICAL gradient noise scale tol_dp — the compared quantity and its tolerance live in different coordinates.

- EVIDENCE: Line 1028: 'if float(res.constr_violation) > gtol'; line 1322: 'gtol = max(tol_dp, 1e-8 * gscale)'; line 1323: 'xtol = 1e-10'; lines 1041/1358: '<= 10.0 * gtol'; line 885: 'A1.NEWTON_TOL_FACTOR * EPS * scale * 10.0'. Scaled-space optimality: scipy computes the Lagrangian gradient in u-coordinates (x0 = W/Dv, jac = g*Dv, lines 954, 1008), while tol_dp (lines 1287-1289) is measured on the physical dJ/dW.

- SUGGESTED TEST: Dimensional audit test: run with lip height perturbed so A W - yL = 1e-4 (physically meaningful infeasibility) and gtol forced to 1e-3 — the status-4 rejector must fire; derive a separate feasibility tolerance in length units (e.g. Newton-floor on y scale x K_RICH) and a scaled-space gtol via the same Dv map, replacing 1e-8/10x with derived factors.

- VERIFIER NOTE: Surviving parts, line-verified: (a) line 1028 compares res.constr_violation — which in the u-coordinates of the constraint row A*Dv (916) equals |y_m - yL|, a LENGTH — against gtol derived in gradient units; with the S20 run's gtol = 9.338e-01 of record this 'status-4-class' rejector would tolerate a lip infeasibility of ~0.9 length units on a y~2 wall, i.e. it is dimensionally incoherent and near-vacuous (same cross-unit reuse in main's check at 1352); (b) xtol=1e-10 (1323) is asserted as 'the Newton floor on W scale' (1319-1320) but the actual Newton floor is NEWTON_TOL_FACTOR*EPS*scale ≈ 100·2.2e-16·O(1) ≈ 4e-14 (a1_ideal_march_jax.py:147) — the constant is not the quantity named; (c) the 10.0 slack in optimality<=10*gtol (1041, 1358) and in the replay tol NEWTON_TOL_FACTOR*EPS*scale*10.0 (885, 1254) are undischarged factors inside PASS/FAIL gates; (d) scipy's optimality is the norm of the Dv-scaled Lagrangian gradient (jac=g*Dv, 954; x0=W/Dv, 1008) while tol_dp entering gtol was measured on the physical dJ/dW (1287-1289) — compared quantity and tolerance live in different coordinates, off by up to the Dv spread. REFUTED component: the 1e-8 in gtol=max(tol_dp, 1e-8*gscale) (1322) is scipy trust-constr's own default gtol, which is exactly the header's declared derivation 'KKT residual bound = trust-constr's own gtol x a measured gradient scale' (74-76) — that factor is derived-by-declaration, not magic.

### [CONFIRMED | medium | doc-code-mismatch] variational-driver:negative-control-N2-missing

- FILE/LOC: validation/a1_toc_variational_jax.py — header lines 65-71 and 78-80 (contract); main() lines 1295-1299 (N1), 1524-1538 (N3); N2 absent

- CLAIM OF RECORD: 'NEGATIVE CONTROLS: ... (N2) the optimizer restarted from a perturbed feasible point must return to the same optimum within derived bars (local uniqueness probe)... Exit code 0 iff ALL checks pass INCLUDING the negative controls.'

- FINDING: N2 is never implemented: main() runs N1 (corrupted gradient) and N3 (shrunken design vs oracle) but contains no second optimization from a different perturbed start and no derived-bars comparison of two optima. The single perturbed-start OPT run is compared to the GENO band (O2), not to another independently-reached optimum, so the declared local-uniqueness rejector cannot fire and the header's exit-code contract ('ALL checks INCLUDING the negative controls') is satisfiable with N2 permanently unexecuted. The local-uniqueness property matters for the verdict: the O2 band is wide enough (representation + resampling + cross-res envelope terms) that two distinct local optima inside the band would be indistinguishable, which is exactly what N2 was declared to exclude.

- EVIDENCE: Grep for 'N2' in the file matches only the header (line 66-68); main() checks at lines 1298 ('N1 corrupted gradient rejected') and 1534/1537 ('N3 oracle discriminates') are the only negative controls wired into ok; the OPT stage runs run_trsqp exactly once (line 1336).

- SUGGESTED TEST: Implement N2: second run_trsqp from an independently-seeded feasible perturbation; assert max|W*_a - W*_b| <= a DERIVED bar (e.g. K_RICH x the xtol-implied step floor x conditioning) and that the check is wired into ok. Until then, delete the N2 line from the header or mark it UNIMPLEMENTED so the exit-code contract is honest.

- VERIFIER NOTE: The header declares N2 (perturbed-restart local-uniqueness probe, lines 66-68) and the exit-code contract 'Exit code 0 iff ALL checks pass INCLUDING the negative controls' (79-80), but main() wires only N1 (1295-1299) and N3 (1524-1538); run_trsqp is invoked exactly once (1336). Refutation hunt came up empty: 'local uniqueness' appears NOWHERE else in the repo (repo-wide grep matches only line 68 of this file); the S17/S18/S20 progress logs and docs/rde_nozzle_PROGRESS.md contain no declared deferral or UNIMPLEMENTED marker for the TOC carrier's N2 (the 'N2 corrupted vjp DETECTED' entries in the S17/S18 logs belong to the DIFFERENT carrier [X-SCANM], a1_march_scan.py). The single perturbed-start OPT run is the O2 discriminating test against the GENO band, not a second independently-reached optimum with derived bars, so the declared local-uniqueness rejector cannot fire — and the O2 band (cross-res + representation + resampling envelope, 1505-1510) is indeed wide enough that two distinct in-band optima would be indistinguishable, which is what N2 was declared to exclude. Undeclared dead control + dishonest exit-code contract in a rigor culture that forbids exactly this.

### [CONFIRMED | medium | numerics] variational-driver:dv-roundtrip-defeats-stall-floor-and-base-skip

- FILE/LOC: validation/a1_toc_variational_jax.py — lines 1007-1016 and 1039 (stalled test), 964-976 (callback skip), 973-1004 (re-record + flip detection)

- CLAIM OF RECORD: 'Continue with a FRESH segment ... unless the segment made no progress at all (zero accepted steps = the honest floor)' (lines 1035-1038); callback 'skip the re-record when x has not moved since the last one' (lines 969-972).

- FINDING: Both mechanisms compare floating round-trips with np.array_equal: minimize receives x0 = W/Dv and the driver reconstructs physical points as state.x*Dv, but (W/Dv)*Dv differs from W at the ulp level for generic Dv. Consequences: (a) stalled = np.array_equal(W, W_start) is essentially never True, so the declared 'honest floor' break for a zero-progress status-2 segment cannot fire — the driver instead re-records and re-measures a fresh (n+1)-gradient-eval Hessian every segment until max_segments=100 (the loop is bounded, and main's status check would then FAIL, but the declared termination semantics is dead code); (b) on the first callback of every segment xk_now = roundtrip(base) != last_rec(=base), forcing one spurious full adaptive re-record per segment at an eps-perturbed base, where the admittedly FRAGILE (N, Nv) chord-foot decisions (docstring: 'a decision flip fires at almost every accepted step') can flip on roundoff alone and end the segment with zero accepted steps — inflating re_record_events and segment counts with pure-noise 'topology moved' verdicts reported in the Verdict.

- EVIDENCE: Line 1008: 'minimize(f_np, W / Dv, ...)'; line 975: 'xk_now = np.asarray(state.x) * Dv' vs seg_state['last_rec'] = np.asarray(W).copy() (line 957); line 1039: 'stalled = np.array_equal(W, W_start)' with W = np.asarray(res.x) * Dv (line 1016). No tolerance-based comparison anywhere on these paths.

- SUGGESTED TEST: Unit test: for the measured Dv of the twin case, assert np.array_equal((W0/Dv)*Dv, W0) — expected FAIL, demonstrating both dead paths; fix by comparing in u-space (state.x vs stored u) or with an ulp-scaled tolerance, then assert one segment with zero accepted steps actually triggers the honest-floor break and that segment-0 performs exactly one base record.

- VERIFIER NOTE: Facts verified: minimize receives x0=W/Dv (1008), W is reconstructed as res.x*Dv (1016), stalled=np.array_equal(W, W_start) (1039); the callback compares xk_now=state.x*Dv (974-975) against last_rec initialized to the exact physical W (957). Dv is a generic measured vector (1/sqrt|h| median-normalized, 908-909), so the double-rounded roundtrip (w/d)*d == w fails for generic components; with 9 components (one exact via the median normalization) all-equal is essentially never true. Consequences hold: (a) a genuinely zero-progress status-2 segment yields res.x bit-equal to x0, but W=roundtrip(W_start)≠W_start, so the declared 'honest floor' break (1035-1038 comment, 1042) is dead code — the driver instead re-records and re-measures an (n+1)-gradient-eval Hessian per segment until max_segments=100; the final verdict still FAILs (optimality>10*gtol), so no false PASS, but the declared termination semantics cannot execute; (b) on any segment whose first iteration is rejected, the callback sees state.x==x0, xk_now≠last_rec at the ulp level, and pays one spurious full adaptive re-record of the base — inflating n_rec, which the Verdict MUST report per the P2 policy of record (kickoff line 53). No tolerance-based comparison exists on either path. Medium: no verdict can flip, but a declared rejector is unfirable and Verdict-reported record counts are contaminated.

### [CONFIRMED | low | rejector-weakness] variational-driver:t2-indicator-undercounts-evals

- FILE/LOC: validation/a1_toc_variational_jax.py — lines 939 (n_eval += n+1), 946-951 (f_np counts), 952-955 (g_np uncounted), 897-906 (precond uncounted), 1418-1424 (T2 bound)

- CLAIM OF RECORD: T2 whole-loop practicality bound: 'N_eval x (t_solve + t_grad) <= K_prac x t_GENO_type2', a governance falsifier whose FIRING queues a G0 re-decision with 'the measured decomposition' (lines 1406-1440).

- FINDING: n_eval undercounts the true evaluation work: every scipy jac call invokes g_np -> val_grad (a full solve+gradient) with NO counter; the one-time preconditioner costs 2n additional val_grad calls, also uncounted; f_np and g_np each call val_grad separately for the same point (no memoization), so the actual number of solve+grad evaluations is roughly 2x n_eval plus 2n. The T2 left-hand side is therefore systematically biased LOW — the falsifier is weaker than declared and the 'measured decomposition' carried to the G0 review understates the true cost. (T2 already FIRED of record per S18, so the verdict direction did not flip on the recorded run, but the margin of the firing — the governance-relevant number — is wrong, and a future near-threshold run could spuriously PASS.)

- EVIDENCE: g_np (lines 952-955) has no 'nonlocal n_eval'; the precond loop (lines 899-906) calls val_grad 2n times before any counting; f_np increments once per objective call only (line 948); lhs_T2 = opt['n_eval'] * (t_solve + t_grad) at line 1418.

- SUGGESTED TEST: Wrap val_grad with a call counter and re-run the OPT stage: assert counter == opt['n_eval']; expected FAIL by ~2x. Fix by counting inside val_grad (the single choke point) and re-evaluate the T2 line with the corrected N_eval.

- VERIFIER NOTE: Verified: g_np (952-955) has no n_eval counter; the one-time preconditioner pays 2n uncounted val_grad calls (899-906); f_np and g_np each invoke the full jitted value_and_grad at the same point with no memoization (949, 953), and scipy trust-constr calls fun and jac separately (~1:1 per iterate), so true solve+grad work ≈ 2x the counted n_eval (939, 948) plus 2n; lhs_T2=opt['n_eval']*(t_solve+t_grad) (1418) is systematically biased LOW. Refutation attempts fail: no declared definition restricts N_eval to objective-only calls — the printed formula models each counted eval as one solve+grad pair while each ACTUAL pair costs ~2x that. Severity low, not medium: T2 is declared an indicator, not a bench exit-fail (1425-1440), it FIRED of record anyway (S18) so the governance consequence (G0 re-decision review) is already queued and the verdict direction of record is unaffected — the defect is an understated firing margin/decomposition carried to the review, plus the possibility that a future near-threshold run spuriously PASSes the indicator at half-measured cost.

### [SEED CONFIRMED] SEED-A1
- VERIFIER: Binding chain source-verified as CORRECT: certdiag (810-831) sets A1.N_NEWTON=10x, builds solvers under a NEW cache key 'certdiag_%d' (815, so a1_march_scan._SOLVER_CACHE:298-307 cannot serve stale closures), and cached_solvers -> make_solvers -> mk = A1.make_implicit_solver (a1_march_scan.py:159), whose @jax.jit newton reads the module global N_NEWTON inside cond (a1_ideal_march_jax.py:390) — a Python global resolved at TRACE time, and the fresh closures are first traced inside run_toc_record(…solvers=solv_d) (817-819) BEFORE the finally-restore (831), so 300 is baked in and the knob bites. The missing-rejector half stands: repo-wide grep finds NO test that varies N_NEWTON (no KAT with a lowered cap proving cert_worst rises), and the S20 numbers of record make this non-academic — 'CERTDIAG EXECUTED 8/8, ALL GENUINE: every rejected design re-recorded with N_NEWTON 30->300 … returns a BIT-IDENTICAL cert_worst … the trip-cap hypothesis (K_budget) is DEAD, eight times over' (PROGRESS_2026-08-07_S20_adaptive.md step 8). Bit-identical output under the raised cap is exactly what a NON-biting knob would produce; the adjudication distinguishes the two only via the un-instrumented damped-Newton-freeze narrative (t=0 trial, a1_ideal_march_jax.py:360-366), not via an executable rejector. An instrument whose discriminating power is unproven underwrote a 'hypothesis DEAD' adjudication of record — an R5 violation (the test must be able to reject); severity medium since the binding is in fact sound and the diagnostic is env-gated and reporting-only.


## benches (candidates dropped by finder cap: 11)

### [CONFIRMED | high | doc-code-mismatch] benches:1

- FILE/LOC: validation/o33_bench.py — march_design, lines 441-458 (restore at line 458); consumed by wall_geometry a1_toc_variational_jax.py lines 165-166 via make_run_toc_scan_jit line 593

- CLAIM OF RECORD: march_design docstring: 'M_NODES/KNOT_XI are module constants of [X-TOCV]; they are set and RESTORED around the call so no other caller can see a mutated class'; the [D1] 'verdict of record is the [X-O33B] re-run (T3)' (adaptive_knot_optimize.py lines 69-71).

- FINDING: The finally clause executes `TV.M_NODES = old` where `old = (TV.M_NODES, TV.KNOT_XI)` — it stuffs the whole tuple into M_NODES and never restores KNOT_XI (contrast the correct unpacking in geno_design lines 428-429 and design_class.__exit__ in adaptive_knot_optimize.py line 134). After the first march_design call (R7, line 613), TV.M_NODES is a tuple and TV.KNOT_XI is clobbered to None. The next trace of the replay — R3's jax.grad at line 679 — calls wall_geometry, which with KNOT_XI None evaluates `jnp.arange(1, M_NODES + 1)` on a tuple: TypeError. The committed bench therefore cannot execute R3, the two-knob test, or R6 (the PRIMARY kill criterion) in any stage past R7, in BOTH default and design-loaded modes; in design-loaded mode the adaptive knot vector is additionally lost before perturbation_direction (line 512) would use it. The pre-declared S20 [D1] kill re-run is unproducible with this file.

- EVIDENCE: o33_bench.py line 441 `old = (TV.M_NODES, TV.KNOT_XI)`, lines 442-443 set both, line 458 `finally: TV.M_NODES = old` (KNOT_XI absent); a1_toc_variational_jax.py lines 165-168 read the globals inside wall_geometry, which is called inside the jitted replay (line 593) traced at o33_bench.py line 679. o33_bench.py is not in the modified list of git status, so this is the committed S20 state (cd0e204).

- SUGGESTED TEST: Run the bench with A1_O33_STAGE=rows (or all): it raises TypeError at line 679 instead of producing a verdict. Fix-verifying test: after any march_design call assert isinstance(TV.M_NODES, int) and TV.KNOT_XI is restored to the pre-call object.

- VERIFIER NOTE: o33_bench.py line 441 `old = (TV.M_NODES, TV.KNOT_XI)`, lines 442-443 set both, line 458 `finally: TV.M_NODES = old` — the tuple is stuffed into M_NODES and KNOT_XI is never restored; contrast the correct unpacking `TV.M_NODES, TV.KNOT_XI = old` in geno_design line 429 and design_class.__exit__ (adaptive_knot_optimize.py line 134). a1_toc_variational_jax.py wall_geometry lines 165-168 read the module globals at trace time (`jnp.arange(1, M_NODES + 1) / M_NODES` when KNOT_XI is None), and the jitted replay of make_run_toc_scan_jit calls wall_geometry inside the traced body (line 593). After the first march_design call (R7, o33_bench.py line 613) TV.M_NODES == (8, None) [or (m, xi) in design-loaded mode] and TV.KNOT_XI is None; the next fresh trace — R3's jax.grad at line 679 through the runj built at 672 — evaluates `(8, None) + 1`: TypeError. Every stage past 'field' (rows/family/all) is unexecutable, in both default and design-loaded modes; in design-loaded mode the adaptive knot vector is additionally clobbered before perturbation_direction (line 512) reads it. Provenance verified by git: the S19 PASS of record (ceae2ae) had the correct scalar restore `old = TV.M_NODES ... TV.M_NODES = old`; the S20 KNOT_XI extension (1d93719) widened `old` to a tuple in both functions but fixed only geno_design's restore. Since the S20 [D1] run was obstructed (no d1-eligible design, PROGRESS lines 11-12), the broken path has never executed — the pre-declared [D1] kill re-run (T3, adaptive_knot_optimize.py lines 69-71) is unproducible with the committed file. Doc-code mismatch is literal: the march_design docstring (437-440) promises both constants 'set and RESTORED around the call'.

### [CONFIRMED | medium | rejector-weakness] benches:2

- FILE/LOC: validation/o33_bench.py — R6 block, lines 804-836 (bars at 819-830, check at 831-833)

- CLAIM OF RECORD: R6 is 'THE PRE-REGISTERED GRADIENT-LEVEL TEST... This is the primary kill criterion' (docstring lines 76-83) with 'derived bars'.

- FINDING: The acceptance is |t_grad - t_rao| <= bar_g + bar_r, where bar_r = |floor|/|V-slope| of the fitted V (line 824). The floor is the Rao-residual value at the vertex, which the file itself elsewhere says is dominated by the design-class representation gap (note lines 837-846) — a quantity that does not shrink with the family span. Nothing checks that bar_g + bar_r is smaller than the scanned range 6*t0amp (or any fraction of it): if the floor/slope ratio reaches the half-span, the check is satisfied for ANY relative placement of the two zeros and the primary kill criterion cannot fire. Moreover neither bar is a derived uncertainty of the fitted zero: bar_g omits the linear-fit model error of dJ/dt (curvature bias of -ic/sl over a span of 3% design amplitude) and least-squares residuals; bar_r is a heuristic floor-to-slope ratio, not a vertex-location bound. The 'DISCRIMINATES' control (line 834-836) only checks dJ/dt changes sign — it does not bound the bar width.

- EVIDENCE: Lines 807-810 (linear fit, t_grad = -ic/sl), 812-817 (V-fit), 821-824 (bar_g = Newton floor/|sl|; bar_r = |coef[1]|/|coef[0]|), 831-833 (verdict). t0amp = 0.01*mean(W) at line 782; no comparison of bar_g+bar_r against ts span anywhere.

- SUGGESTED TEST: Synthetic rejection probe: feed the R6 machinery a Drao family with floor = 0.5*|V-slope|*span and t_rao displaced from t_grad by half the span — the current check PASSes. Add a conclusiveness guard bar_g + bar_r < (ts[-1]-ts[0])/2 (declared NON-CONCLUSIVE otherwise) and propagate fit residuals into both bars.

- VERIFIER NOTE: Core defect stands with one correction to the finder's supporting citation. Verified: lines 807-809 linear fit and t_grad = -ic/sl; 811-817 V-fit of Drao; 821-824 bar_g = NEWTON_TOL_FACTOR*EPS*cert_n*max|dJdt|/|sl| and bar_r = |coef[1]|/|coef[0]| (floor/V-slope); 831-833 verdict |t_grad - t_rao| <= bar_g + bar_r. No conclusiveness guard anywhere compares bar_g+bar_r to the scanned half-span 3*t0amp (t0amp = 0.01*mean(W), line 781; ts at 782): if floor/slope reaches the half-span the PRIMARY kill criterion (docstring lines 75-83) passes for any relative placement of the two zeros, and nothing detects it — the o32 twin carrier in the same campaign implements exactly the required NON-CONCLUSIVE branch (o32 verdict_row), so the omission is against the campaign's own standard. The bars also omit the linear-fit model error and least-squares residuals, and the module's blanket claim (lines 88-91: 'every band is a two-resolution Richardson estimate times K_RICH plus the Newton floor') describes neither bar. The DISCRIMINATES control (834-836) only checks dJ/dt sign change, not bar width. Correction: the note at 837-850 about design-class dominance concerns Dcorner, not the Drao floor — but this is immaterial: bar_r's floor is t-independent by the fit model and does not shrink with the family span regardless. Severity medium, not high: both bars ARE derived from measured quantities (Newton floor; fitted floor/slope, which is a legitimate vertex-resolution bound for the model |t-t*|*slope + floor), the bar value is printed (828-830) so a fat bar is visible in the log, and the check is not structurally unable to fire — it is unguarded against losing its power.

### [CONFIRMED | medium | underived-constant] benches:3

- FILE/LOC: validation/o33_bench.py — docstring lines 88-91 vs checks at lines 599-601, 619-631, 649-656; dead function order_of_decay lines 358-367

- CLAIM OF RECORD: 'TOLERANCES — DERIVED (R5): every band is a two-resolution Richardson estimate times the repo's reused K_RICH = 4, plus the accumulated Newton certification floor... No band is adjusted after a number is seen.' Also R4 docstring (lines 65-70): the right-sign cancellation fraction 'is O(h^2)'.

- FINDING: Only R3 gets a Richardson band (lines 699-713). The other verdict-bearing rows use hardcoded, underived thresholds: R1 'drift < 3%' (0.03, line 601), R7 GENO-drift < 0.03 (line 624), R4 sign-separation factor 0.05 (lines 651-656), and the R3 two-knob checks use a bare factor 0.5 (relN[60] < 0.5*rel, line 774) and an unbanded single-pair monotonicity rel_g2 < relN[60] (line 776). None of 0.03/0.05/0.5 is derived from a measured quantity, contradicting the module's own tolerance claim. Additionally, the O(h^2) decay that R4's docstring predicts is never measured: order_of_decay is defined at line 358 and never called (grep: single occurrence in the repo), so R4 tests only a factor-20 separation at one resolution — a much weaker guarantee than the stated one.

- EVIDENCE: Lines 88-91 (blanket derived-band claim); line 601 `D_f2 < 0.03`; line 624 `rep_geno["d2"] < 0.03`; lines 650-656 `min < 0.05 * max`; line 774 `relN[60] < 0.5 * rel`; line 776 `rel_g2 < relN[60]` with no band. Grep for order_of_decay returns only its definition (o33_bench.py:358).

- SUGGESTED TEST: Perturb the instance so D_f2 = 0.031: the R1 verdict flips on a constant with no carrier. Derive the drift band per the docstring (two-resolution Richardson of the drift itself x K_RICH) and actually call order_of_decay on the R4 fractions across refine(cfg, r), asserting p ≈ 2 within a derived band.

- VERIFIER NOTE: All cited facts verified in the committed file: line 601 `D_f2 < 0.03` (R1 verdict), line 624 `rep_geno["d2"] < 0.03` (R7), lines 649-656 factor 0.05 sign-separation (R4), line 774 `relN[60] < 0.5 * rel`, line 776 `rel_g2 < relN[60]` unbanded single-pair monotonicity. None of 0.03/0.05/0.5 has a carrier or derivation — grep of docs/ finds no derivation of these constants — while the module's own binding text (lines 88-91) claims 'every band is a two-resolution Richardson estimate times... K_RICH, plus the accumulated Newton certification floor'. Only R3 actually gets that band (697-713). order_of_decay (line 358) is dead code: repo-wide grep returns only its definition, so the O(h^2) decay the R4 docstring predicts (lines 66-70) is never measured; R4 tests only a factor-20 separation at one resolution. Partial mitigation keeping this at medium rather than high: several R7 rows DO use measured comparisons (df2 < max of the measured drifts, 619-621; dpa < max drift, 629-631), the magic constants act as loose caps far from the measured values (drift printed sub-percent vs the 3% cap), so a verdict flip requires a large real change; the defect is a genuine R5 discipline violation plus a false blanket claim, not a demonstrably wrong verdict.

### [CONFIRMED | low | doc-code-mismatch] benches:4

- FILE/LOC: validation/o32_mesh_convergence.py — module docstring lines 51-72 vs verdict_row lines 269-288

- CLAIM OF RECORD: 'Verdict rule (fixed before any number was produced): CONCLUSIVE iff dp_tot < 1/2... PASS iff CONCLUSIVE and |p_hat_fine - 2| <= dp_tot' (lines 63-68).

- FINDING: The deciding code implements a strictly weaker rule: conclusiveness cap dpt < 1.0 (line 272, `2.0 * SEP_ADJACENT`) and a ONE-SIDED kill p_f < 2 - dpt (line 273). A run with p_fine = 1.15 and dp_tot = 0.9 PASSes under the code but is NON-CONCLUSIVE (and would fail two-sided) under the rule the module header still declares as fixed-before-any-number. The swap is declared inside verdict_row's docstring (lines 222-243) by appeal to the older S16 one-sided falsifier, but two mutually inconsistent 'binding' texts now coexist in the same file, the header was not corrected, and the weaker one decides — exactly the silent-verdict-motion the file says it exists to prevent. The relabeled cap also inherits a new gap: with the one-sided rule, discrimination from order 3 is abandoned entirely (an order-3 or spuriously superconvergent sequence always passes), which the original two-sided 0.5 rule excluded.

- EVIDENCE: Line 137 `SEP_ADJACENT = 0.5  # derived: half-separation from orders 1 and 3`; line 272 `concl = dpt < 2.0 * SEP_ADJACENT`; line 273 `killed = p_f < 2.0 - dpt`; header lines 63-68 state the 0.5 two-sided rule as the pre-registered one.

- SUGGESTED TEST: Unit test verdict_row on synthetic f with p = 1.2 and injected dp_tot = 0.85: assert which rule is of record. Then make the header and the code state the same rule, with the chosen cap's derivation written once.

- VERIFIER NOTE: The factual mismatch is real but the 'silent-verdict-motion' characterization is refuted. Verified: header lines 63-68 still declare 'Verdict rule (fixed before any number was produced): CONCLUSIVE iff dp_tot < 1/2... PASS iff CONCLUSIVE and |p_hat_fine - 2| <= dp_tot'; the deciding code uses `concl = dpt < 2.0 * SEP_ADJACENT` (= 1.0, line 272) and the ONE-SIDED `killed = p_f < 2.0 - dpt` (line 273). However, verdict_row's docstring (lines 220-243) declares the discrepancy at length, names the binding text (the S16 [S-LBML] one-sided falsifier, which predates the file), explicitly confesses the 0.5-vs-1.0 cap inconsistency ('an inconsistency of mine, declared here rather than silently resolved'), and lines 281-287 PRINT all four readings (one-sided, two-sided, cap 1.0, cap 0.5) on every row — nothing is silent, and the abandonment of order-3 discrimination is declared ('an order ABOVE 2 does not falsify a claim of order h^2'). What survives: the module header was never corrected and still asserts pre-registration for the two-sided rule, so two mutually contradictory 'binding' texts coexist in one file and the weaker one drives the exit code — a real but declared and fully printed doc-hygiene defect. Severity low.

### [CONFIRMED | low | rejector-weakness] benches:5

- FILE/LOC: validation/o32_mesh_convergence.py — negative control lines 645-656 (with_grad=False at 648-649); adjoint noise model line 254

- CLAIM OF RECORD: 'the two rows are separately falsifiable, which is the point' (lines 20-22); the negative control 'must be REJECTED by the same band that accepts the production march' (lines 74-81).

- FINDING: The first-order negative control is run only on the PRIMAL (toc_ladder called with with_grad=False, so G is identically 0.0 and no adjoint verdict_row is possible). The ADJOINT band — the row that alone falsifies claim (iv) — is never demonstrated able to fire on a known-wrong-order input. Additionally, the adjoint row's noise floor reuses the primal model eps_f = NEWTON_TOL_FACTOR*EPS*N_cells*scale(f) (line 254) with scale = max|<g,v>|; no derivation is given for why the reverse-mode (custom_vjp implicit-transpose) error accumulates with the same constant and the same linear-in-N law as the primal Newton certification — a hidden hypothesis inside a 'both contributions MEASURED, none tuned' claim (lines 52-57).

- EVIDENCE: Line 648-649 `rf = toc_ladder(..., with_grad=False)`; only okQ (primal) is checked at 650-653. Line 521 sets G = 0.0 when with_grad is False. Line 254 applies the primal floor formula to both rows via verdict_row.

- SUGGESTED TEST: Run the foot-solver ladder once with with_grad=True (even at reduced rmax) and assert the adjoint row is REJECTED by the same band; derive (or empirically bound, via repeated-perturbation scatter) the adjoint-specific floor instead of borrowing the primal's.

- VERIFIER NOTE: Facts verified: line 648-649 `rf = toc_ladder(..., with_grad=False)`; line 521 `G = ... if with_grad else 0.0`; only the primal row okQ is checked (650-653). The adjoint band — the sole falsifier of claim (iv), per lines 20-22 'the two rows are separately falsifiable, which is the point' — is never demonstrated to fire on a known-wrong-order physics input, and no declaration explains the with_grad=False choice. The eps_f reuse is also verified: line 254 applies the primal accumulation law NEWTON_TOL_FACTOR*EPS*N*scale to the G row via verdict_row with scale = max|f|, with no in-file derivation for reverse-mode error obeying the same constant and linear-in-N law. Mitigations that cap severity at low: (a) the estimator selftest (lines 212-216) machine-checks that the SAME estimator/band machinery rejects a first-order sequence, so the adjoint row's band can fire if fed first-order numbers — the missing piece is only the end-to-end demonstration that the foot-solver adjoint produces such numbers; (b) the eps_f transfer has colorable support from the file's own claim chain ([T-LEMB]: the gradient is the EXACT finite-dimensional transpose verified by O3.1 at machine precision, line 13-14), though that argument is nowhere written down as the floor's justification.

### [CONFIRMED | low | rejector-weakness] benches:6

- FILE/LOC: validation/o32_mesh_convergence.py — topology monitor lines 534-547 and 661-663; window construction lines 326-334 / 469-481

- CLAIM OF RECORD: 'If the fraction of columns carrying an axis point does not vary smoothly with r... the gradient sequence carries stratum-transition terms that no order estimator can absorb. Asserted mechanisms are worth nothing; this is the number that supports or refutes it' (lines 538-543). Registered window 'h-INDEPENDENT by construction' (lines 33-35).

- FINDING: Two monitors that the file itself declares verdict-relevant are print-only and cannot fire: (a) res['topo'] is printed at lines 661-663 but no check consumes it — a ladder whose axis-column fraction jumps between strata still yields PASS, despite the docstring saying such a run's exponents are uninterpretable; (b) the registered window [a,b] is frozen from the r=1 run, but no per-level assertion checks that every level's wall span still covers [a,b] — the mirrored GENO stop |M-Me|<1e-5 makes the march's end station a non-smooth function of h (Me drift is printed at lines 617-621, again ungated), so a level ending inside [a,b] would silently truncate the trapezoid and contaminate the measured order while every declared check passes.

- EVIDENCE: Lines 544-547 store topo, lines 661-663 only print it; lines 330-334 fix (a,b) from wx1 of r=1; the r-loop (342-384) contains no coverage assertion on wall_x vs [a,b]; the mstop stage (584-604) is diagnostic-only and 'the BAND IS NOT AMENDED'.

- SUGGESTED TEST: Assert per level: wall_x[0] <= a and wall_x[-1] >= b (fail the row otherwise), and gate the topo fraction to change monotonically/smoothly (e.g. |Delta fraction| below a derived bound) instead of printing it.

- VERIFIER NOTE: Both facts verified: res['topo'] is stored (544-547) and consumed only by the print at 661-663 — no check gates it, despite the docstring's own words ('Asserted mechanisms are worth nothing; this is the number that supports or refutes it', 538-543); and neither toc_ladder nor ideal_ladder asserts per-level that wall_x covers the frozen r=1 window [a,b] (windows fixed at 326-334 and 469-481; the r-loops contain no coverage check). The ideal march's end station is genuinely h-dependent via the mirrored m_stop exit (lines 317-321), and the mstop stage (584-604) is explicitly diagnostic-only. Mitigations that cap severity at low: (a) the TOC twin's lip sits at x = L exactly at every level (lines 528-533), so window coverage is structurally guaranteed on the TOC ladder — the exposure is the ideal ladder only, where |dMe| is at least printed (617-621); (b) a truncation or stratum crossing perturbs the functional sequence and would typically inflate dp_model, tripping the NON-CONCLUSIVE branch (verdict_row) rather than yielding a clean PASS — so 'every declared check passes' is not guaranteed, only possible; (c) both quantities are labeled monitors, though the file's own philosophy (executable guards over discipline) argues they should gate. A real rejector-weakness, but with partial structural and statistical backstops.

### [REFUTED | medium | correlated-error] benches:7

- FILE/LOC: validation/o33_bench.py — cplus_chain lines 193-218; guard at lines 576-577

- REFUTED BY VERIFIER: The claimed failure channel — ULP-level discrepancy between the two stored copies breaking the float-keyed walk — cannot occur, by object provenance in the exporter. In a1_toc_variational_jax.py run_toc_record, the cplus arrays are built from the SAME in-memory point objects that populate the previous column's cline: wall columns, lines 355-372, `cplus.append(pt2)` with `pt2 = prev[j-1]`, where prev is the previous column's newcol whose solved members are exactly the objects stacked into that column's cline (394-398); fan columns likewise, `cplus_f.append(prev[k])` (280) vs `cline=np.stack(newcol)` (289-292). Both the index keys (o33_bench.py 194-197) and the lookup keys (211-213) are float() of np.asarray() conversions of the same objects — np.stack/np.asarray are bit-exact, there is no recomputation and hence no ULP channel. The `break` at a missing key is the structural terminator at the fan/IVL boundary, deliberately so per the exporter's comment ('the chain must be recorded as it is built, never reconstructed from newcol by index arithmetic', 350-354). The suggested test (perturb one cplus entry by 1 ulp) probes a state the program cannot reach: the two copies share provenance, so no independent rounding can separate them. Residue: the chain-length guard `>= 8` (line 577) is weak in isolation, and the wall-angle corroboration (586-593) is print-only — but with the truncation mechanism refuted, these do not add up to a standing defect; the guard's role is to catch a structurally broken export, which it would.

### [CONFIRMED | low | rejector-weakness] benches:8

- FILE/LOC: validation/adaptive_knot_optimize.py — [D1] checks: adaptive_knot_optimize.py lines 575-586 and o33_bench.py lines 761-770; measured floor at adaptive_knot_optimize.py lines 376-383

- CLAIM OF RECORD: KILL LINK [D1]: 'the final goal must fall BELOW the S19 baseline..., else the design-class diagnosis is FALSIFIED' (lines 66-71); stopping rule built on the metric's own measured noise floor tol_stop = K_RICH*|rel(r=1)-rel(r=2)| (lines 50-56).

- FINDING: Both [D1] kill comparisons are raw sign tests with no uncertainty band: `best["goal"] < rel1` (adaptive_knot_optimize.py line 579) and `rel < rel8` (o33_bench.py line 770). The carrier itself measures the goal metric's noise floor (tol_stop) and uses it for the STOPPING rule, but not for the VERDICT: an adaptive optimum that lands within tol_stop of the baseline gets its confirmed/FALSIFIED verdict decided by noise the run has already quantified. The declared falsifier is thus formally two-sided-sharp while the measurement is only floor-resolved — a verdict-bearing comparison weaker than its own available error model.

- EVIDENCE: adaptive_knot_optimize.py line 380 computes tol_stop; line 579 compares best['goal'] < rel1 with no reference to it; o33_bench.py lines 768-770 compare rel < rel8 raw, with no band despite the R3 band machinery existing in the same function (lines 697-713).

- SUGGESTED TEST: Set up (or simulate) best['goal'] = rel1 - 0.5*tol_stop: the check PASSes although the improvement is below the metric's own measured floor. Require improvement > tol_stop for a confirming verdict and declare the in-floor region NON-CONCLUSIVE.

- VERIFIER NOTE: Facts verified: adaptive_knot_optimize.py line 380 computes tol_stop = K_RICH*|rel1 - rel2| and uses it only for the stopping rule (line 557); the [D1] verdict comparisons are raw sign tests — `best["goal"] < rel1` (line 579) and o33_bench.py `rel < rel8` (line 770) — with no band and no NON-CONCLUSIVE branch, despite the o33 comment at 750-760 promising 'same band machinery' and the R3 band code existing in the same function (697-713). A goal landing within tol_stop of the baseline gets its confirmed/FALSIFIED verdict decided by noise the run has itself quantified — a verdict-bearing comparison weaker than its available error model, in both directions (false confirm AND false falsify). Severity capped at low because: (a) the raw binary form faithfully implements the PRE-DECLARED [D1] kill link ('the final goal must fall BELOW the S19 baseline..., else FALSIFIED', docstring 66-71, S20 gate fixed before any adaptive run) — adding a band post hoc would itself be verdict motion against the pre-registration; (b) the measured floor is computed and printed alongside (381-383), so an in-floor verdict is auditable from the log; (c) the expected effect size (discharge of a 6.6e-2 class-dominated residual) dwarfs the floor the file itself declares 'small and honest' (lines 53-56). The right fix is a declared NON-CONCLUSIVE region in the NEXT pre-registration, not a silent edit of this one.

### [SEED CONFIRMED] SEED-A5
- VERIFIER: Verified: tol_stop is measured ONCE at the 8-node uniform baseline (lines 374-383: goal_metric at r=1 and r=2 on W8 with xi=None) and then applied unchanged to every enriched-class cycle's improvement test (line 557 `impr < tol_stop`). The mesh-dependence of the corner-mismatch metric is a property of the instance (design + knot class); nothing re-measures rel(r=2) on enriched designs, and neither the docstring (50-59, which declares only WHERE the floor is measured, calling it 'the metric's own mesh dependence') nor the code declares the class-independence hypothesis being relied on. Under the repo standard (hypotheses explicit; memory gate-pre-esecuzione) an undeclared transfer assumption inside a declared 'measured quantities only' stopping rule is a genuine defect. Severity low because the blast radius is the STOPPING rule only, not any verdict: a wrong floor stops the loop early (best design kept, line 542-544) or late (budget-capped, lines 336-338/400), while the [D1] comparisons are separate raw tests (see benches:8) and every per-cycle goal and improvement is printed for audit (538-540).

### [SEED REFUTED] SEED-A6
- VERIFIER: The declaration exists and the mirrored-truth risk runs in the fail-safe direction. (1) Declared: the module header's GENO-INDEPENDENCE paragraph (o33_bench.py 15-21) and geno_design's docstring (371-380) explicitly state GENO's role — 'a CROSS-DESIGN check, not a cross-code oracle... GENO supplies only a second, independently obtained TOC wall geometry', with both sides of every row computed by our march on our field. The two-knob rows conform: relN and rel_g2 (730-749) compare OUR AD gradient g_n[-1] against OUR corner density cd_n on OUR march of the GENO wall — GENO output enters no side of the comparison, so it cannot manufacture agreement between them. (2) Wrong-wall direction: the corner identity is derived FROM optimality and must BREAK at non-optimal designs (the row's own declared control, lines 54-56); a wrong (non-Rao) GENO wall therefore INFLATES relN[60], driving `relN[60] < 0.5*rel` (774) and `rel_g2 < relN[60]` (776) toward FAIL — a false kill, not a false pass. For the check to pass with a 'wrong' wall, the wall's non-stationarity contribution would have to be under ~3e-2 in this metric, i.e. the wall would have to be nearly Rao-optimal anyway. (3) In-run corroboration independent of GENO's construction: rep_err < 1e-4 representation gate (611-612), and R7 verifies the wall's Rao necessary conditions on OUR field — f2 constancy on GENO's contour, cross-design agreement of the Rao constant within the measured drifts, and transversality-reading agreement (614-631). Residue not rising to a finding: R7's f2-constancy cap is the underived 3% of benches:3, and the near-continuum-proxy role could be named more explicitly in the header — but the reliance is declared in substance, cross-checked, and fail-safe.


## registry-legacy (candidates dropped by finder cap: 9)

### [CONFIRMED | medium | doc-code-mismatch] registry-legacy:G12-2B-PHANTOM-CHECK

- FILE/LOC: validation/g12_shock_linearization.py — docstring lines 22-23 vs code lines 183-192; claims_registry.yaml lines 684-692 (X-G12) and 146-152 (C-MAJDA)

- CLAIM OF RECORD: X-G12 statement: 'Machine verification of the G12-L1 eigenstructure and the G12-L2 linearized-RH nonsingularity/degeneration law'; module docstring: '(2b) det dH/dV_plus tracks u_n^2 (u_n^2 - c^2) (the Prop. A2 kernel law) within derived tolerance'.

- FINDING: Check (2b) is never executed as a check: the code prints the determinant value ('law checked by R1 limit... record the value for the log') and does not fold anything into `ok`. Only two ENDPOINTS are verified (nonsingular at one oracle state, singular at the characteristic-front limit); the quantitative tracking law det ~ u_n^2(u_n^2-c^2) is verified nowhere. Yet the registry statement of record claims the 'degeneration law' is machine-verified, and C-MAJDA's falsifier ('degenerating linearized RH would contradict the G12-L2 margin law') references that law as established content of the sharpened front conditional.

- EVIDENCE: Line 189-191: `detv = float(np.linalg.det(Jv)); print('  (2b) det dH/dV+ recorded: %.6e (law checked by R1 limit)' % detv)` — no comparison, no tolerance, no `ok &=`. The inline comment (lines 184-189) openly abandons the advertised check ('sign depends on variable ordering/weights, so check the LAW via the characteristic-limit rejector R1 instead'), but neither the module docstring (which still promises 'within derived tolerance') nor registry X-G12 ('nonsingularity/degeneration law' machine-verified, 'PASS 9/9') was corrected.

- SUGGESTED TEST: Either implement (2b) as a real check — evaluate det(dH/dV+) along a one-parameter family of shock strengths and fit the vanishing order/prefactor against u_n^2(u_n^2-c^2) with a derived band — or amend the docstring and registry X-G12/C-MAJDA wording to claim only the two endpoint bricks. A rejector: corrupt the law (e.g., scale one RH row by u_n) and require the fitted exponent check to fire.

- VERIFIER NOTE: g12_shock_linearization.py lines 22-23 promise '(2b) det dH/dV_plus tracks u_n^2(u_n^2-c^2)... within derived tolerance' but lines 183-192 only compute detv and print 'recorded... (law checked by R1 limit)' — no tolerance, no fold into ok; the inline comment abandons the check without correcting the header. Registry X-G12 (line 685) claims machine verification of the 'nonsingularity/degeneration law'. Partial refutation limits severity: 'PASS 9/9' honestly counts the 9 executed checks ((1a),(1b)x3,(2a)x3,R1,R2 — (2b) excluded), and the content C-MAJDA actually rests on (line 146: 'G12-L2 blocks... + U3-H1') IS verified — nonsingularity by (2a) lines 170-175, degeneration endpoint by R1 lines 194-210, plus independently by X-U3BD (s_L=1.8685, own R1). Defect stands as doc-of-record vs code mismatch in a carrier where doc==proof==file, but the quantitative tracking law is not load-bearing for C-MAJDA beyond the verified endpoints, so medium, not high.

### [CONFIRMED | medium | process] registry-legacy:C-O33-STALE-CONDITIONAL

- FILE/LOC: docs/claims_registry.yaml — lines 181-192 (C-O33) vs lines 1205-1216 (X-O33B) and 1218-1229 (X-AKNO)

- CLAIM OF RECORD: C-O33 statement: 'Term-by-term match of the closed-form multiplier components against the AD adjoint field on a TOC case (oracle O3.3 / P-A3) awaits the A1 engine.' carrier: [], falsifier: 'O3.3 run with component mismatch beyond derived tolerance'.

- FINDING: The conditional's statement is stale and its falsifier has in fact been RUN without the entry recording the outcome: X-O33B executed the O3.3 bench in S19 (f2 drift 9.5e-03 on the classical surface, R1-R7 rows) and X-AKNO (S20) records 'C-O33 neither discharged nor falsified' plus a design-class diagnosis and a queued margin-constrained reformulation — all of it living only in OTHER entries' scope prose. C-O33 itself still says the oracle 'awaits the A1 engine' and lists no carrier, while heirs T-LEMA-i (line 398) and T-LEMA-iii (line 424) inherit this stale entry as their THEOREM* conditional. An auditor resolving the inherits chain from T-LEMA-i lands on a statement that misrepresents the current adjudication state of the program's own primary kill criterion (R6).

- EVIDENCE: C-O33 (line 185): 'awaits the A1 engine'; A1 engine landed S18 (X-TOCV, line 1182 'FULL staged+opt VERDICT PASS 2026-08-06 S18') and the bench ran S19 (X-O33B line 1208 'S19 execution addendum'); memory of record: 'C-O33 aperta ma QUANTIFICATA (causa = classe di design)'. None of this is reflected in the C-O33 entry, violating the registry's own index discipline (one entry per citable object, current status).

- SUGGESTED TEST: Registry lint rule: a conditional inherited by any entry must not be older (by dated scope note) than a carrier that declares `inherits: [that-conditional]`; concretely, update C-O33 scope/statement with the S19/S20 adjudication and add X-O33B/X-AKNO to its carrier list, then grep-assert the 'awaits' wording is gone.

- VERIFIER NOTE: claims_registry.yaml line 185: C-O33 statement still reads 'awaits the A1 engine' with carrier: [] (line 189), while line 1182 (X-TOCV) records 'FULL staged+opt VERDICT PASS 2026-08-06 S18', line 1208 (X-O33B) records the executed S19 bench ('S19 execution addendum', f2 drift 9.5e-03 on the classical surface) and line 1221 (X-AKNO) records 'C-O33 neither discharged nor falsified' with the design-class diagnosis. T-LEMA-i (line 398) and T-LEMA-iii (line 424) inherit the stale node. No convention shields it: the registry's own practice appends dated status notes to conditionals (C-XBVP scope 'DISCHARGED AT INSTANCE LEVEL', S-XCONV 'now BOX-CERTIFIED', C-HT4 dated IOU), so C-O33 violates the registry's demonstrated currency discipline on the program's primary kill criterion.

### [CONFIRMED | medium | hidden-hypothesis] registry-legacy:IVXC-SIGN-BRANCH-UNGUARDED

- FILE/LOC: validation/ivxc_interval_certificate.py — certify(), lines 460-471; claims_registry.yaml lines 1458-1469 (S-XCONV), 1575-1586 (X-IVXC)

- CLAIM OF RECORD: S-XCONV: 'eta is STRICTLY CONVEX in m on the axially supersonic branch... ideal-gas instance CERTIFIED of record: strict definiteness over (M,V) in [1.15,3]x[-0.8,0.8] for EVERY (rho,S)'.

- FINDING: The certifier accepts a box when EITHER +G or -G passes interval LDL^T, choosing the sign per box from the sign of the interval det J (`if det[0] > 0: sign = 1 elif det[1] < 0: sign = -1`), and counts both toward the same coverage total. Certifying -G PD certifies CONCAVITY of eta, not the convexity T-XWS's Dafermos machinery needs; and nothing checks that the chosen sign is the SAME across all 43199 boxes — a mixed-sign union would still return (True, used, '') and be recorded as one strict-convexity certificate. Soundness currently rests on the unstated, unchecked hypothesis that det J is single-signed over the supersonic slice (true for this instance, but nowhere asserted or tested), and on an undeclared det(J)-sign-to-definiteness-sign link that has no stated mathematical basis (congruence preserves definiteness regardless of det J's sign).

- EVIDENCE: Lines 464-470: sign selected from `det` (of J) per box; line 470: `if sign != 0 and ldl_psd(G, sign): covered += area(box)` — no global sign accumulator, no assertion `sign == +1`, no post-hoc report of which sign was used. The registry statement reports the result as 'strict convexity' unconditionally.

- SUGGESTED TEST: Track the certified sign per box and FAIL the certificate if more than one sign occurs (or hard-assert sign == +1 for the convexity claim); add a rejector: run certify() on a function whose Hessian is PD on half the box and ND on the other half and require NOT-CERTIFIED.

- VERIFIER NOTE: ivxc_interval_certificate.py certify() lines 464-470: sign is chosen per box from the interval sign of det J ('if det[0] > 0: sign = 1; elif det[1] < 0: sign = -1') and 'if sign != 0 and ldl_psd(G, sign): covered += area(box)' — no global sign accumulator, no assertion sign == +1, no report of which sign was used. The module docstring's own criterion (line 34-35) is 'H_m > 0 iff G > 0 (J invertible)': congruence H_m = J^{-T} G J^{-1} preserves definiteness regardless of det J's sign, so a box certified via sign = -1 (G negative definite) certifies CONCAVITY yet counts toward the strict-convexity coverage. Soundness rests on the unstated, untested hypothesis that det J is single-signed on the supersonic slice (true for this instance via the sonic-degeneracy structure, but nowhere asserted), and the registry S-XCONV/X-IVXC statements (lines 1462, 1579) report unconditional strict convexity. Refutation failed: no guard or declaration found anywhere in the file.

### [CONFIRMED | medium | underived-constant] registry-legacy:1E3-PREFACTOR-UNDERIVED

- FILE/LOC: validation/u3_bordered_front_solve.py — u3_bordered_front_solve.py lines 166, 199, 213, 231; g12_shock_linearization.py lines 155, 171; gbe_ergodic_envelope.py lines 185, 215, 230; xbvp_entropy_transfer.py line 168

- CLAIM OF RECORD: All five carriers declare 'derived SVD/FD tolerances, no magic numbers' (u3 docstring line 26, gbe line 35, g12 line 16).

- FINDING: Every 'derived' tolerance in these carriers is the product of a genuinely derived exponent (EPS, EPS^(2/3), EPS^(1/2)) and an ASSERTED safety prefactor 1e3 (once 4*4*1e3) that is derived nowhere — no error-propagation constant, condition-number bound, or operation count justifies the factor, and it is copy-pasted across unrelated check types (RH residual floors, SVD singularity floors, eigenvector residuals, FD-Hessian concavity floors). The prefactor is verdict-bearing at least once: X-GBE S3's recorded max scaled eigenvalue 8.7e-06 sits at 58% of the 1e3*EPS^0.5 = 1.49e-05 floor, so the concavity PASS/FAIL flips if the asserted 1e3 were 5e2. This is exactly the 'tolerances derived, not magic' rule the docstrings claim to satisfy.

- EVIDENCE: u3 line 166: `tolH = 1e3 * EPS * scaleH`; line 213: `floor = 1e3 * EPS ** (2.0/3.0) * sv[0]`; line 199: `resid < 1e3 * EPS`; gbe line 185: `if ev > 1e3 * EPS ** 0.5`; xbvp line 168: `floor = 4 * 4 * np.finfo(float).eps * 1.0e3  # solve+matmul roundoff` — the comment names the mechanism but 4*4 covers the matmul while 1e3 is unexplained.

- SUGGESTED TEST: Derive per-check prefactors (operation-count times condition number, or a measured-noise route like X-O32's) and record them; alternatively run each carrier with the prefactor swept over [1e1, 1e4] and record which verdicts flip — any flip identifies a tolerance whose magnitude, not just exponent, is load-bearing and must be derived.

- VERIFIER NOTE: All cited lines verified: u3_bordered_front_solve.py 166 (tolH = 1e3*EPS*scaleH), 199 (resid < 1e3*EPS), 213 (floor = 1e3*EPS**(2/3)*sv[0]), 231; g12_shock_linearization.py 155, 171; gbe_ergodic_envelope.py 185, 215, 230 (1e3*EPS**0.5); xbvp_entropy_transfer.py 168 ('4 * 4 * np.finfo(float).eps * 1.0e3  # solve+matmul roundoff' — the 4*4 is named, the 1e3 is not). Docstrings claim 'derived SVD/FD tolerances, no magic numbers' (u3 line 26, gbe line 35, g12 line 16); only the EXPONENTS are derived, the 1e3 safety prefactor has no derivation anywhere in the five carriers. It is verdict-bearing: X-GBE registry line 1682 records max scaled eig 8.7e-06 vs floor 1.5e-05 — the S3 PASS flips if the asserted prefactor were ~5.8e2. Direct violation of the program's own R5 tolerance discipline.

### [CONFIRMED | medium | rejector-weakness] registry-legacy:GBE-CONCAVITY-NEAR-FLOOR

- FILE/LOC: validation/gbe_ergodic_envelope.py — s3_scan(), lines 162-194; claims_registry.yaml lines 1666-1690 (S-GBE, X-GBE)

- CLAIM OF RECORD: X-GBE scope: 'S3 composite perspective concave 400/400 across the seam, max scaled eig 8.7e-06 vs floor 1.5e-05'; S-GBE rests the ergodic ceiling J_exact^+ <= F_env on Jensen, which REQUIRES this concavity.

- FINDING: The load-bearing concavity check operates within a factor ~1.7 of its own detection floor: any genuine convexity violation of scaled magnitude below 1.5e-05 (plausible precisely at the branch seam, which only a random subset of 400 points approaches within the crude |NPR-NPR*|<0.5 window) is structurally invisible, and the recorded 8.7e-06 maximum is not adjudicated as signal vs noise. Unlike X-O32, which declares a conclusiveness cap from its own derivation, neither the carrier nor the registry entry declares the resolution limit of this rejector — the ceiling claim is presented as scan-verified without stating the smallest violation the scan could have seen. A rejector that can only fire above 1.5e-05 scaled curvature is materially weaker than 'composite concavity verified'.

- EVIDENCE: Line 185: `if ev > 1e3 * EPS ** 0.5: bad += 1` (floor 1.49e-05); registry line 1682 records 'max scaled eig 8.7e-06 vs floor 1.5e-05' with no conclusiveness statement; seam sampling is incidental (line 187: seam counter only counts, does not target seam points). R1's corruption (line 245: 1e-4 quadratic) is orders above the floor, so the demonstrated rejector firing does not establish sensitivity near the floor.

- SUGGESTED TEST: Add a seam-targeted deterministic scan (points placed at NPR = NPR*(1 +/- delta) for a delta ladder) plus a calibration rejector: inject convex corruptions of decreasing scaled amplitude (1e-3, 1e-4, 2e-5, 1e-5) and record the smallest detected — declare that number as the check's conclusiveness cap in the registry entry.

- VERIFIER NOTE: gbe_ergodic_envelope.py line 185: detection threshold 1e3*EPS**0.5 = 1.49e-05; registry X-GBE (line 1682) records max scaled eig 8.7e-06 = 58% of the floor with no conclusiveness statement (contrast X-O32, line 1195, which declares 'conclusiveness cap at the constant its own derivation yields'). Structure strengthens the finding: F_env(m,e,sig) = m*V(e/m,sig/m) is positively 1-homogeneous, so its exact Hessian has a zero eigenvalue along the ray — the scan necessarily measures FD noise about an exact zero and operates AT its resolution floor by construction; any genuine seam violation below ~1.5e-05 scaled is structurally invisible. Seam sampling incidental (line 187: counter only counts |P0/Pa - NPR*| < 0.5 among random points); R1's corruption amplitude 1e-4 (line 245) is ~7x the floor, so the demonstrated rejector firing does not establish sensitivity near the floor. S-GBE's Jensen step (registry line 1670) requires exactly this concavity; the numeric scan is the only brick covering the composite/seam.

### [CONFIRMED | low | rejector-weakness] registry-legacy:FALSIFIERS-WITHOUT-CARRIERS

- FILE/LOC: docs/claims_registry.yaml — lines 235-246 (T-T0), 459-467 (T-P3), 248-259 (T-NSW)

- CLAIM OF RECORD: T-T0 (class THEOREM) falsifier: 'thrust-trace flatness diagnostic failing on certified single-mode data'; T-P3 (THEOREM*) falsifier: 'computed lambda2(xi) drifting from -f2(lip) beyond margins on certified data' with carrier: [].

- FINDING: Verdict-bearing THEOREM/THEOREM* entries carry falsifiers that name no executable test existing in the tree: (a) the only 'flatness' code in the repo is the SK plateau/Fig-8b diagnostic in src/thrust/sk_models.py and stechmann_nozzle.py — unrelated to a rotating-mode thrust-trace monitor — so T-T0's falsifier (and DIR-PERIODIC's 'monitor = T0 flatness', line 1053) currently cannot fire; (b) T-P3's falsifier is now executable in substance — X-O33B measures exactly f2 = -lambda2 constancy and its lip value — but the registry link is absent (T-P3 carrier still empty, X-O33B not referenced), so the entry presents an unfalsifiable claim while its de-facto falsifier lives unregistered elsewhere. T-NSW at least declares its missing carrier ('L6 candidate, none exists today').

- EVIDENCE: Repo-wide grep for 'flatness' in *.py returns only src/thrust/sk_models.py (Fig. 8b T/Mdot window diagnostic, 'used in no verdict') and src/thrust/stechmann_nozzle.py (plateau flatness) — no thrust-trace-vs-time monitor; T-P3 lines 464: `carrier: []` while X-O33B statement (line 1209) reports 'the Rao first integral f2 = -lambda2 is constant along the control surface to 9.5e-03'.

- SUGGESTED TEST: Extend the claims lint with a rule: any THEOREM/THEOREM* falsifier that names a diagnostic must either resolve to a committed script (grep the named object) or carry an explicit dated IOU tag like C-HT4's; for T-P3, register X-O33B in its carrier list so the falsifier becomes executable-of-record.

- VERIFIER NOTE: Main (T-T0) prong substantially REFUTED as an undeclared defect: grep confirms no thrust-trace flatness monitor exists ('flatness' only in src/thrust/sk_models.py and stechmann_nozzle.py), BUT the gap is a panel-adjudicated dated IOU of record — PAN-S14 finding F-FLAT with the D6 §6 item 5-bis arbiter decision (rde_nozzle_development_plan.md lines 398-403: 'T0 FLATNESS MONITOR carrier... T-T0/DIR-PERIODIC carrier fields pointed at it WHEN the script lands (not before — lint truthfulness)'), M0 line 998 names the certificate field in the contract, and P1 skeleton row C5 declares 'diagnostic = PRACTICE (declared pending)'. Surviving residue, at low severity: (i) the registry entries T-T0 (line 245) and DIR-PERIODIC (line 1053) omit the inline IOU tag the registry's own convention uses (T-O2 line 232 'named IOU', C-HT4 line 165 'named IOU, dated'), so an auditor resolving only the index sees an unexecutable falsifier with no pointer; (ii) T-P3 (line 464) carrier: [] while X-O33B (line 1209) measures precisely f2 = -lambda2 constancy — the de-facto falsifier instrument exists unregistered, same currency-defect family as C-O33.

### [CONFIRMED | low | doc-code-mismatch] registry-legacy:IVXC-DOCSTRING-STALE-METHOD

- FILE/LOC: validation/ivxc_interval_certificate.py — module docstring lines 36-39 vs i_gauss_solve() lines 244-252

- CLAIM OF RECORD: Module docstring METHOD section: 'lam is enclosed by INTERVAL GAUSSIAN ELIMINATION on J^T lam = grad eta (pivot sign-definiteness required, else split)'.

- FINDING: The implemented linear-solve enclosure is the Krawczyk method with midpoint-inverse preconditioning (the function's own docstring even records plain interval Gaussian elimination as a measured ~15x-over-wide NO-GO), so the carrier's method-of-record description in its module docstring contradicts its implementation. The registry X-IVXC entry (line 1578) correctly says 'Krawczyk verified linear solve', so this is intra-file staleness — but the carrier IS the proof document (doc == proof == this file), so the method section of the proof misstates the certified algorithm.

- EVIDENCE: Docstring lines 37-38 name interval Gaussian elimination as the enclosure method; i_gauss_solve docstring (lines 245-252): 'Verified enclosure of A x = b (4x4) by the KRAWCZYK method... [Method note: plain interval Gaussian elimination measured ~15x over-wide on this system]'.

- SUGGESTED TEST: Update the module docstring METHOD paragraph to name Krawczyk (keeping the interval-Gauss NO-GO in the method record); optionally rename i_gauss_solve to i_krawczyk_solve to prevent recurrence.

- VERIFIER NOTE: ivxc_interval_certificate.py module docstring lines 36-39: 'lam is enclosed by INTERVAL GAUSSIAN ELIMINATION on J^T lam = grad eta (pivot sign-definiteness required, else split)' — but i_gauss_solve (lines 244-252) implements the Krawczyk method with midpoint-inverse preconditioning and its own docstring records plain interval Gaussian elimination as a measured ~15x over-wide NO-GO. Intra-file contradiction in a carrier where doc == proof == the file; the registry X-IVXC entry (line 1578, 'Krawczyk verified linear solve') is correct, confining the defect to the module-docstring METHOD paragraph. No refutation found: the METHOD paragraph is unambiguous and stale.

### [CONFIRMED | low | numerics] registry-legacy:U3-R1-FLOOR-CROSSMATRIX

- FILE/LOC: validation/u3_bordered_front_solve.py — lines 213 (floor derivation) vs 262-266 (R1 use); line 199 (P1b)

- CLAIM OF RECORD: R1: 'characteristic-front degeneration: the bordered matrix must be DETECTED singular' (docstring lines 42-44), with 'derived SVD/FD tolerances'.

- FINDING: The singularity-detection threshold used by rejector R1 is `floor = 1e3 * EPS**(2/3) * sv[0]` computed from the HEALTHY main-case matrix Meq, then reused unchanged to test the degenerate matrix M0eq (line 263: `deg = sv0[-1] < floor`), which is built with different row/column equilibrations (rs0, cs0 from w0, sig_c). The detection band is thus calibrated on a different matrix's largest singular value; if the degenerate configuration's scale differed materially from the oracle case's, R1 could mis-classify (declare singular what is merely differently scaled, or vice versa). The declared 'derived' status of this rejector threshold does not hold across the matrix swap. Same asserted-prefactor issue on P1b (line 199: eigenvector residual `< 1e3 * EPS` with no derivation for an SVD-based residual ratio).

- EVIDENCE: Line 213: floor built from `sv[0]` of the main Meq; lines 259-263: M0eq gets its own rs0/cs0 scaling but is compared against the main-case floor — no `sv0[0]`-relative floor is formed for the degenerate test.

- SUGGESTED TEST: Form the R1 floor from M0eq's own largest singular value (`1e3 * EPS**(2/3) * sv0[0]`) so the criterion is scale-invariant per matrix; verify R1 still fires and that a NON-degenerate front (small but finite strength) is NOT flagged singular under the corrected floor — the second half is the missing negative control.

- VERIFIER NOTE: u3_bordered_front_solve.py line 213: floor = 1e3*EPS**(2/3)*sv[0] built from the healthy main-case Meq; lines 259-263: the degenerate M0eq is equilibrated with its own rs0/cs0 yet tested against that main-case floor (deg = sv0[-1] < floor), with no sv0[0]-relative floor formed. Mitigations weigh against high severity but do not refute: both matrices are equilibrated to O(1) entries so a material scale discrepancy is excluded by construction, and the sibling carrier g12 (lines 203-205) explicitly DECLARES the same-floor choice ('degeneracy = s_min below the same floor that (2a) certified ABOVE') — but the u3 file carries no such declaration, so its 'derived tolerances, no magic numbers' claim (line 26) does not cover the cross-matrix reuse; and the missing negative control (a weak-but-finite-strength front NOT flagged singular) is real — R1's specificity is untested in both carriers.


## problem-statement (candidates dropped by finder cap: 5)

### [CONFIRMED | high | hidden-hypothesis] problem-statement:closed-form-scope-overreach

- FILE/LOC: docs/rde_nozzle_MASTER.md — D2.6 (P)(ii) lines 287-291; T-T7FS(a) lines 856-859; also docs/rde_nozzle_theorem_ledger.md §4(a) lines 204-207

- CLAIM OF RECORD: (P)(ii): 'S* satisfies the averaged system T7: per-phase closed-form adjoint (Rao/Kraiko) conditions...'; T7(a): 'per a.e. xi: the per-phase adjoint Euler system; in S1 it reduces to the classical closed form ... f2 = V cos(theta -/+ alpha)/cos(alpha) = -lambda2(xi)'.

- FINDING: The closed-form reduction is claimed for the whole S1 class, but the derivation of record is scoped to IRROTATIONAL HOMENTHALPIC+HOMENTROPIC per-phase flow, a strict subclass of S1 (D2.5/D1 §6 S1 requires only piecewise-C1 MOC-regularity; rotational stratified members are in-class). The GIVEN interface class I2 explicitly admits '[entropy/vorticity profiles]' (D1 line 199-200), i.e. RDE-realistic stratified data for which the f2 first integral and Rao corner conditions are NOT the stationarity conditions (registry: 'rotational extension = Hoffman four-field route'); M0 VI.4bis(iv) itself demotes the closed form to 'ORACLE/INITIALIZER only' for rotational data, contradicting the (P)(ii)/T7(a) wording at the sites of record. A future (P)(ii)-conformance verdict on stratified data evaluated via the closed forms would be wrong; today's optima (S18/S20) happen to sit on the homentropic subclass, so the leak is latent, not yet number-corrupting.

- EVIDENCE: docs/claims_registry.yaml:381 scope: 'irrotational homentropic, EOS-general; rotational extension = Hoffman four-field route'; docs/rde_nozzle_theorem_ledger.md:519-521 'irrotational homentropic scope (EOS-general); rotational extension = Hoffman'; docs/rde_nozzle_MASTER.md:1045-1051 VI.4bis(iv) 'ROTATIONAL DATA (unconditional): per-phase adjoint at the FIELD level... the two-field closed form (Prop. A3) serves as ORACLE/INITIALIZER only'; validation/a1_march_scan.py:257-261 'EXACT only on HOMENTHALPIC + HOMENTROPIC data... by Crocco... exactly the irrotationality of the S1 class. Stratified data (RDE-realistic per-phase interface profiles) carries per-streamline s/h0'; docs/rde_nozzle_problem_book.md:199-200 I2 data class includes '[entropy/vorticity profiles]'.

- SUGGESTED TEST: Add the irrotational-homentropic qualifier at the three sites (or restate (P)(ii) as 'field-level adjoint stationarity, reducing to closed form on homentropic phases') and extend tests/test_claims_lint.py to require it; executable rejector: a two-streamline entropy-jump per-phase datum through the field-level adjoint (n6_fivefield_adjoint machinery) showing f2 is not transported (closed-form corner residual nonzero at the true stationary point).

- VERIFIER NOTE: Refutation failed: grep 'irrotational|homentropic' over docs/rde_nozzle_MASTER.md yields zero qualifiers at the claim sites (only line 1003 'rotational MOC', confirming rotational members are in the evaluator class). D2.5 (M0:139-141) defines S1 as piecewise-smooth MOC-regular with no vorticity restriction; D1 I2 (problem_book:199-200) admits '[entropy/vorticity profiles]'. Yet (P)(ii) (M0:287-290) and T-T7FS(a) (M0:856-859; ledger:204-207) assert the closed-form f2 reduction for all of S1 unqualified, while the derivation of record is explicitly scoped: claims_registry.yaml:381 T-A3 scope 'irrotational homentropic, EOS-general; rotational extension = Hoffman four-field route'; theorem_ledger:519-521 same; and M0 VI.4bis(iv) (1045-1051) in the SAME document demotes the closed form to 'ORACLE/INITIALIZER only' on rotational data — an internal contradiction at the definition of record. validation/a1_march_scan.py:254-264 confirms stratified (rotational) data is RDE-realistic and in-scope (named engine extension slot). Latent (current optima homentropic) but a false-as-stated conformance criterion in the problem of record.

### [CONFIRMED | medium | rejector-weakness] problem-statement:flatness-monitor-unarmed

- FILE/LOC: docs/rde_nozzle_MASTER.md — D2.3 scope note lines 108-114; VI.4bis(v) lines 1052-1056; docs/rde_nozzle_development_plan.md lines 395-405

- CLAIM OF RECORD: 'mu and the rung-2 integrand are declared WITHIN the standing pure-periodic single-mode scope (fixed wave count n; monitor = T0 flatness, VI.4bis)... never silently averaged'; VI.4bis(v): 'The flatness monitor itself is mandatory in every data contract.'

- FINDING: The single monitor that fences the whole averaged objective's scope (pure periodic rotating-wave data, standing memory assumption) has NO committed carrier: plan item 5-bis says the T0-flatness script, its derived threshold, and its impure-mode rejector land later ('X-carrier ID registered ... WHEN the script lands (not before)'), and the CycleFamily module it would guard is 'NOT STARTED as a module'. The scope guarantee 'never silently averaged' is thus currently unfalsifiable in-repo: no rejector can fire on impure-mode or mode-transition data. Honestly declared as pending in the plan, so a declared-but-weaker-than-stated guarantee rather than a silent hole.

- EVIDENCE: docs/rde_nozzle_development_plan.md:395-397 'CycleFamily v0 ... [NOT STARTED as a module]'; :398-405 '5-bis ... T0 FLATNESS MONITOR carrier — committed script + threshold DERIVED ... WHEN the script lands (not before — lint truthfulness)'; grep 'CycleFamily' hits only docs, zero matches in src/ or validation/*.py; docs/rde_nozzle_MASTER.md:1056 'mandatory in every data contract' with no data-contract module existing.

- SUGGESTED TEST: Land the flatness-monitor carrier (harmonic decomposition of a synthetic rotating-pattern trace; threshold derived from the pattern-rotation tolerance) with a rejector that fails on a two-mode admixture and on an n-transition trace; until then, add a lint asserting D2.3's scope note carries an explicit 'monitor carrier PENDING' flag.

- VERIFIER NOTE: Refutation failed: the only 'flatness' hits in code are unrelated (src/thrust/sk_models.py:303-334 SK T/Mdot flatness diagnostic; stechmann_nozzle.py plateau flatness); zero CycleFamily hits in any .py. Dev plan :395-397 'CycleFamily v0 ... [NOT STARTED as a module]' and :398-405 item 5-bis defers the T0-flatness carrier ('WHEN the script lands (not before)'). Meanwhile M0 D2.3 scope note (:108-114) asserts 'monitor = T0 flatness ... never silently averaged' and VI.4bis(v) (:1056) 'mandatory in every data contract' — a present-tense fence with no in-repo rejector able to fire on impure-mode or mode-transition data. Honestly declared pending in the plan, but not at the M0 sites; the guarantee is currently unfalsifiable.

### [CONFIRMED | medium | rejector-weakness] problem-statement:stage-a-audits-premised-not-implemented

- FILE/LOC: docs/rde_nozzle_MASTER.md — D2.6 GIVEN lines 201-206; docs/rde_nozzle_development_plan.md lines 61-63, 372-374, 395-397

- CLAIM OF RECORD: (P) GIVEN: 'interface contract (Gamma_d, D, mu) with the stage-A admission audits passed (characteristic completeness on axially supersonic patches per Lemma 4; Crocco compatibility; per-phase spacelikeness margin; declared closure O1/O2/O3 on subsonic patches)'; plan G6: 'any CycleFamily failing stage-A audits ... is rejected loud'.

- FINDING: The problem of record premises admission audits that mostly cannot be run: grep finds no Crocco-compatibility or characteristic-completeness audit anywhere in src/ or validation/*.py — only the axial spacelikeness margin exists as a certificate (a1_toc_variational_jax margin_floor rejector). Gate G6 ('rejected loud') therefore cannot fire on an inconsistent data family today. Since (P) is the enunciato every downstream verdict conforms to, its GIVEN currently names a checked precondition that is checkable only 1-of-3; declared NOT STARTED in the plan, but neither D2.6 nor D1 §4.3bis flags at the site that two of the three named audits have no carrier.

- EVIDENCE: Grep 'spacelike|Crocco|stage[- ]A|characteristic completeness' over src/: no matches; validation/a1_toc_variational_jax.py:184-185, 242, 1210-1224 implement only the axial-margin (u_x - c) rejector with derived floor; docs/rde_nozzle_development_plan.md:395-397 'CycleFamily v0 with the stage-A data contract + loud rejects ... [NOT STARTED as a module]'; :372-374 gate G6 text.

- SUGGESTED TEST: Implement stage-A v0 as a standalone auditor over a tabulated per-phase profile: Crocco residual u x omega - (grad h0 - T grad s) against a derived discretization band, characteristic-completeness count on each patch, axial-margin map; loud-reject test on a deliberately Crocco-inconsistent synthetic family and on an axially-subsonic patch without declared closure.

- VERIFIER NOTE: Survives with one evidence correction: besides the axial-margin rejector (a1_toc_variational_jax.py:180-245, 1210-1224, derived floor min_margin/K_RICH), validation/a1_march_scan.py:254-274 inlet_admissibility IS a committed admission guard (h0/s spread vs derived Richardson band) — the finder's 'only the axial margin exists' overstates. However that guard enforces homentropic UNIFORMITY for engine exactness; it is not the Crocco-compatibility audit of stratified profiles nor the characteristic-completeness audit named in (P)'s GIVEN (M0:202-205): grep 'Crocco|characteristic completeness' over src/ and validation/*.py finds only that comment, no audit code. Gate G6 (dev plan :372-374 'rejected loud') cannot fire as specified; CycleFamily NOT STARTED (:395-397); neither D2.6 nor D1 §4.3bis flags at the site that two of the three named audits have no carrier.

### [CONFIRMED | medium | underived-constant] problem-statement:st-marginal-numbers-uncarried

- FILE/LOC: docs/rde_nozzle_problem_book.md — §8 lines 471-478 (and ledger row D1, line 493)

- CLAIM OF RECORD: 'Honest numbers (rocket RDEs): f = 3-30 kHz, t_c = 33-330 μs, τ_n = 20-150 μs ⇒ St = O(0.1-1): MARGINAL. Therefore the averaged theory (rung 2) is NOT self-licensing.'

- FINDING: The numbers that set the program's central error-budget verdict (St MARGINAL → P4 corrector mandatory, (P)(v) St bar, 'any hardware conclusion from rung 2 without an St error bar is out of contract') carry no citation and no committed carrier or test — an R5 violation inside a normative (non-superseded) document. τ_n = ∫dx/u is computable in-repo on the certified march for the Table-1 anchor cycles, yet was never computed; the frequency range is uncited literature folklore. If the true τ_n for the anchor class were, e.g., 5 μs, St would be small and the P4 obligation oversized; nothing can currently reject the stated range.

- EVIDENCE: docs/rde_nozzle_problem_book.md:471-478 gives the ranges with no source and no script reference (contrast: every other number in D1/M0 cites a carrier, e.g. bounds.py, phase_diagram_real.py); grep '3-30|33-330|20-150' over src/, tests/, validation/*.py, examples/: no matches — no carrier computes τ_n or St anywhere in the repo.

- SUGGESTED TEST: Commit an St carrier: τ_n = ∫dx/u along the mean streamline of the certified TOC march for the blessed CH4/O2 anchor at the Table-1 states, t_c from the matched-cycle machinery, St with derived bars; test asserts the D1 range brackets the computed values and rejects on a corrupted u-field; add the literature citation for the f range.

- VERIFIER NOTE: Refutation failed: problem_book:471-478 gives f = 3-30 kHz, t_c = 33-330 us, tau_n = 20-150 us => St = O(0.1-1) MARGINAL with no citation and no carrier; the only other occurrence of the f-range is in the SUPERSEDED note (cycle_averaged_variational_nozzle.md:204, equally uncited). Grep 'Strouhal|tau_n|St_n' over all repo .py: zero matches — no script computes tau_n or St, though tau_n is computable on the certified march. The ledger row D1 (:493) repeats St = O(0.1-1) with falsifiers for the quasi-steady HYPOTHESIS (P4/O5), none for the numbers. These uncarried numbers drive the program-structuring verdict ('rung 2 NOT self-licensing', P4 mandatory, (P)(v) St bar, hardware-conclusion contract clause) inside a normative document — an R5 violation as charged.

### [CONFIRMED | medium | gamma-leak] problem-statement:t4-gamma-status-undeclared

- FILE/LOC: docs/rde_nozzle_MASTER.md — [T-T4] THEOREM 6, lines 563-600; docs/rde_nozzle_theorem_ledger.md §6 lines 340-361

- CLAIM OF RECORD: 'THEOREM 6 (T4 — plug simultaneous optimizability). THEOREM* (under the ideal-adaptation closure [C-HT4]: wall pressure clamps to Pa downstream of each phase's full-expansion point; upstream, T3-class scaling).'

- FINDING: T4's statement declares only the C-HT4 closure and names no variable-gamma status, violating the standing gamma directive ('every theory piece states its variable-gamma status; gamma=const boundaries named'). The hidden boundary is real: 'upstream, T3-class scaling' delivers a phase-independent field Π(x) with p_ξ(x) = Pc(ξ)Π(x) only via Lemma A (gamma(T)-safe, fixed T0) PLUS Lemma B (declared 'REQUIRES calorically perfect gas') whenever T0(ξ) varies across phases — the generic RDE cycle has T0(ξ) varying (D3 §5.2: T0 = 3727 K → 2228 K). For gamma(T) with varying T0, Π becomes phase-dependent and the proof's ordering l(ξ) increasing in Pc(ξ) (hence the nesting and the peak-design conclusion) is underived. Contrast: T3 names the Lemma-B boundary explicitly, and T-GB was gamma-purged with a measured delta; T4 was not.

- EVIDENCE: docs/rde_nozzle_MASTER.md:563-566 (no gamma clause in the T-T4 header or proof); :529-534 Lemma B 'REQUIRES calorically perfect gas ... Dies for gamma(T)'; docs/rde_nozzle_theorem_ledger.md:340-361 (same absence); :312 measured per-cycle gamma_s excursion 1.1537→1.2093 with T0 3727→2228 K showing the varying-T0 case is the physical one; ledger row H-T4 (docs/rde_nozzle_problem_book.md:499) lists only the adaptation closure, no gamma hypothesis.

- SUGGESTED TEST: Declare T4's status (gamma=const, or gamma(T) at fixed T0) at both statement sites; executable probe in the style of the D3 §5.2 two-gamma counterexample: two-phase family (Pc1,T01),(Pc2,T02) on the Cantera gamma(T) route, compute the full-expansion lengths l(ξ) and test whether the Pc-ordering (nesting) survives — rejector fires if l-ordering inverts while Pc-ordering holds.

- VERIFIER NOTE: Best refutation — 'upstream, T3-class scaling' (M0:565; ledger:344) declares the gamma boundary by reference to T3's H1/Lemma B — is undercut by two verified facts: (1) claims_registry.yaml:287-298 declares T-T4 gamma: 'EOS-general' outright, while the peak-design identification (l(xi) increasing in Pc, M0:574-583) rests on a phase-independent field Pi that for varying T0(xi) — the physical case, D3 gamma_s 1.1537->1.2093 / T0 3727->2228 K per ledger — requires Lemma B, declared 'REQUIRES calorically perfect gas ... Dies for gamma(T)' (M0:529-534); Lemma A (M0:516-528) is gamma(T)-safe only at FIXED T0. (2) The registry's C-HT4 statement (yaml:155-159) omits the upstream-scaling clause entirely, so inherits:[C-HT4] does not carry the gamma content. D1 ledger row H-T4 (:499) lists only the adaptation closure. An 'EOS-general' label over a Lemma-B-dependent ordering is a gamma-leak per the standing directive; contrast T3 which names the boundary explicitly.

### [CONFIRMED | medium | doc-code-mismatch] problem-statement:pv-bars-no-evaluator

- FILE/LOC: docs/rde_nozzle_MASTER.md — D2.6 (P)(v) + BAR-CLASS NOTE, lines 300-306; docs/rde_nozzle_hypothesis_ledger.md line 46

- CLAIM OF RECORD: '(v) declared bars: |J_exact - J[S*]| <= St|J1| + D2 residual + DWR' (with the BAR-CLASS NOTE downgrading them to 'ESTIMATED/ASYMPTOTIC indicators ... NOT certified bounds').

- FINDING: Clause (v) is part of the definition of the deliverable pair (S*, delta), yet no committed carrier can evaluate either physical bar term for a delivered design: t3qs_sweep_protection.py is a structural lemma (J1 concentrates at the jump on ray families), not a J1 evaluator for a given (S, cycle); the D2-residual meter is declared to be the 3-D helical wave-frame march whose 'full anchor = A5' — not built. So even as estimates the (v) bars are uncomputable today, and conformance of any delivered optimum to (P)(v) is unfalsifiable — weaker than the BAR-CLASS NOTE's own downgrade admits (it concedes non-certification, not non-computability). The certified fallback [J_exact^-, J_exact^+] plus the S-GBE upper wall is the only shippable bracket, which the docs do state.

- EVIDENCE: Grep 'J_1|J-CT1|corrector' over src/, validation/*.py, tests/: only a1_ideal_march_jax.py (unrelated hit) and t3qs_sweep_protection.py (lines 5-8, 34, 238: symbolic jump-localization identity, no design-level evaluator); docs/rde_nozzle_hypothesis_ledger.md:46 'D2 azimuthal decoupling | PRICED | ... EXACT meter of the sweep/D2 residual at marching cost ... full anchor = A5' (A5 not present in validation/ or src/); docs/rde_nozzle_MASTER.md:1035-1040 VI.4bis(ii) describes the sweep-perturbation J1 route as pipeline design, not as an existing carrier.

- SUGGESTED TEST: Either land the licensed cheap route (one linearized sweep-perturbation solve on the certified march → numeric St|J1| with derived bars) for the anchor case, or amend (P)(v)'s BAR-CLASS NOTE to state 'evaluators pending (J1: VI.4bis(ii) route; D2: A5 anchor)'; add a Verdict lint requiring every (v) bar term to name its carrier or its declared absence.

- VERIFIER NOTE: Refutation failed: validation/t3qs_sweep_protection.py docstring (lines 1-45) confirms it is a symbolic structural carrier (J1 concentrates at the data jump on single-parameter ray cycles; declared scope boundary excludes two-parameter cycles) — not a J1 evaluator for a delivered (S, cycle). hypothesis_ledger:45 'J1 corrector = the A4 deliverable' and :46 'EXACT meter of the sweep/D2 residual ... full anchor = A5' — both pending, no A4/A5 evaluator anywhere in src/ or validation/. The BAR-CLASS NOTE (M0:301-306) concedes only non-certification ('ESTIMATED/ASYMPTOTIC indicators'), not non-computability, so (P)(v) conformance of any delivered pair (S*, delta) is unfalsifiable today; only the [J_exact^-, J_exact^+] fallback bracket is shippable, as the docs state.

### [CONFIRMED | low | process] problem-statement:label-namespace-collisions

- FILE/LOC: docs/rde_nozzle_problem_book.md — D1 §3 (Prop O1 line 94, Lemma O2 line 117, missing Def 3.3), §4.3bis options O1-O4 lines 307-317; docs/rde_nozzle_MASTER.md D2.6 line 206 vs line 710

- CLAIM OF RECORD: D2.6 GIVEN: 'declared closure O1/O2/O3 on subsonic patches'; same definition later: 'the unpinned census question O2'; D1 §3: 'PROPOSITION O1', 'LEMMA O2'; VI.6: 'ORACLES (O1: T3-family... O2: ideal plug...)'.

- FINDING: One letter-namespace carries at least five meanings in the normative corpus: O1/O2 as theorem labels (Prop O1, Lemma O2), O1-O4 as the §4.3bis mixed-interface options (D2.6's 'closure O1/O2/O3' — which also miscounts: §4.3bis defines four options, and its O1 is an interface move, not a closure), O1-O5 as Verdict oracles (VI.6), O3.1-O3.3 as campaign oracles, O1/O2/O9-O11 as census pin questions (D2.6's own M1-witness note). Likewise R1/R2 are simultaneously interface-contract requirements (D2.4), T3 proof remarks (R1)/(R2), and the Schmucker 'criterion class R2' in D-GSEP; and D1's definition numbering skips 3.3 while the notation table cites 'Def. 3.5' for a section that carries no Def label. In a corpus whose conflict rule is textual ('M0+D1-D7 win'), collisions inside the very definition D2.6 are a misreading hazard for future sessions and reviewers.

- EVIDENCE: docs/rde_nozzle_MASTER.md:206 'declared closure O1/O2/O3 on subsonic patches' vs docs/rde_nozzle_problem_book.md:307-317 options O1-O4 (O1 = 'MOVE Gamma_d', O4 = anchor); docs/rde_nozzle_MASTER.md:710 'census question O2' inside the same D2.6/T-GB complex; :1068-1071 VI.6 oracle O1/O2; docs/rde_nozzle_problem_book.md:94, 117 (Prop O1/Lemma O2), 171-177 (R2 requirement) vs docs/rde_nozzle_MASTER.md:225-229 ('criterion class R2') and :541-544 (T3 remarks (R1)/(R2)); D1 §3 has Def 3.1, 3.2, 3.4, 3.5 — no 3.3.

- SUGGESTED TEST: Namespace rename pass (e.g. IF-O1..IF-O4 for interface options, ORC-O1..O5 for Verdict oracles, CEN-O* already used for census pins — apply it at D2.6 line 710); restore or tombstone Def 3.3; add a doc-lint (extend tests/test_claims_lint.py) rejecting a bare 'O\d' token that matches two registered label spaces.

- VERIFIER NOTE: All instances verified line-level: problem_book:94 PROPOSITION O1, :117 LEMMA O2 (theorem labels); :302-317 §4.3bis options O1-O4 with O1 = 'MOVE Gamma_d' (interface move, four options) vs M0:205 'declared closure O1/O2/O3 on subsonic patches' (miscount + namespace clash); M0:710 'unpinned census question O2' in the T-GB complex of the same normative doc; M0:1068-1071 Verdict oracles O1-O5. R1/R2 triple use: D1:169-177 interface requirements, M0:541-544 T3 remarks (R1)/(R2), M0:225 'criterion class R2' (D-GSEP/Schmucker). D1 §3 numbering defect confirmed: §3.3 heading contains Def 3.4; no Def 3.3 exists; §3.4 heading missing (3.1, 3.2, 3.3, 3.5). Genuine misreading hazard under the textual conflict rule, no wrong content found downstream — low.

### [CONFIRMED | low | citation] problem-statement:geno-line-refs-unpinned

- FILE/LOC: docs/rde_nozzle_problem_book.md — §4.3 I2, lines 204-205 (also §4.3bis line 290)

- CLAIM OF RECORD: 'This is rung 2's native interface, and GENO's: `read_ivl_from_file` (IO_m.f90:620) + annular inlet (InitialValues_m.f90:146).'

- FINDING: The frozen problem book anchors part of the interface-contract narrative to source line numbers in GENO, which by standing instruction is an independent, never-committed repo ('GENO/ is an independent git repo: never add it to this repo's commits'). These anchors carry no commit hash or version stamp, so they are unverifiable from this repo's history and will silently rot if GENO moves — an unpinnable citation inside a document whose header declares 'Frozen definitions'. The same pattern appears in the superseded note, where it is harmless; in D1 it is normative.

- EVIDENCE: docs/rde_nozzle_problem_book.md:204-205 and :290 cite IO_m.f90:620 / InitialValues_m.f90:146 with no GENO commit/version identifier; CLAUDE.md 'Preferenze': GENO/ never committed to this repo; git status shows GENO/ untracked.

- SUGGESTED TEST: Stamp every GENO code citation in D1/M0 with the GENO commit hash used by the cross-code oracle ([X-GENOXC]/G0 dossier already fixes a build), and add a lint that rejects a GENO file:line citation lacking a hash; alternatively route D1's references through docs/rde_nozzle_G0_decision.md where the pinned build is documented.

- VERIFIER NOTE: Refutation failed: problem_book:204 cites 'IO_m.f90:620' + 'InitialValues_m.f90:146' bare; grep over docs/ for GENO commit hashes / version stamps finds none — docs/rde_nozzle_G0_decision.md documents the build ('The GENO build reproduces the contour of record', :93) but pins no commit hash, so routing the citation there today would not fix it either. GENO/ is untracked and by standing instruction never committed (CLAUDE.md Preferenze; git status confirms), so the line references are unverifiable from this repo's history and will rot silently inside a document whose definitions are frozen and normative. Same pattern in the superseded note (cycle_averaged_variational_nozzle.md:35-36) is harmless there. Minor citation-hygiene defect — low.


## cycle-avg-machinery (candidates dropped by finder cap: 10)

### [CONFIRMED | high | correlated-error] cycle-avg-machinery:F1-dual-route-tautology

- FILE/LOC: src/thrust/bounds.py — docstring lines 30-34 and cf_intmax docstring lines 154-159 vs formulas at lines 111-120, 130-138, 141-167; check_chain line 236-237; tests/test_bounds.py lines 140-150; docs/rde_nozzle_MASTER.md line 632-633

- CLAIM OF RECORD: "Isp_intmax == Isp_ideal is the quasi-1D exhaustiveness theorem ... and is ASSERTED by the dual-route agreement, not assumed"; cf_intmax is "Numerically independent of cf_ideal (which never touches Eq. 9)"; M0: "int-max = ceiling, dual-route verified".

- FINDING: The two 'routes' are the same algebra. On interior phases cf_intmax's momentum term sqrt(cf_base*(1-NPR^-(g-1)/g)) with NPR=Pc/Pa is character-for-character cf_ideal_naive, and the eps term eps*(1/NPR - Pa/Pc) is identically zero at the substituted analytic argmax; on the sonic boundary both reduce to cf_bell(g,1,...). The agreement check (check_chain 'dual-route' row and test B2+) therefore compares one expression against its own rearrangement and can NEVER fire against a wrong exhaustiveness theorem (e.g. a missed second per-phase DOF): the check's floating-point residual is pure roundoff of shared code regardless of the theorem's truth. The exhaustiveness claim is actually carried by Theorem 1 of validation/bell_optimality_proof.md, not by this instrument; the code and M0 present the numerical agreement as the certificate.

- EVIDENCE: bounds.py line 119-120: cf_ideal_naive x = clip(1-(Pa/Pc)^((g-1)/g)); line 164-167: cf_intmax NPR=maximum(Pc/Pa,NPRs), mom=sqrt(cf_base*(1-NPR^-(g-1)/g)), eps term = eps*(1/NPR - Pa/Pc) which is 0 to roundoff when NPR=Pc/Pa. Negative control B5 'dual-route breach REJECTED' (test_bounds.py line 121-122) only corrupts the stored JSON number, never the algebra.

- SUGGESTED TEST: Add a genuinely independent int-max route: per-phase numerical maximization of Eq. 9 over eps (golden section, no substitution of the analytic argmax) and, separately, a perturbed-closure control (e.g. inject a second exit DOF or a small non-isentropic loss) for which the exhaustiveness identity must BREAK and the check must fire. Reword bounds.py/M0 to attribute exhaustiveness to Theorem 1, demoting the numerical agreement to a consistency check.

- VERIFIER NOTE: bounds.py:164-167 — cf_intmax sets NPR = np.maximum(Pc/Pa, npr_sonic(g)) directly and never calls npr_of_eps (the actual Eq. 9 area-Mach inversion, st_core.py:109-116); eps* enters only the term eps*(1/NPR - Pa/Pc), which is ~1 ulp on interior phases where NPR=Pc/Pa, so the mom term sqrt(cf_base*(1-NPR^-(g-1)/g)) is a floating-point rearrangement of cf_ideal_naive (bounds.py:119-120). On sonic phases eps_star_of_pc returns 1 and cf_ideal itself goes through cf_sonic -> cf_bell (Eq. 9, st_core.py:119-123 with closed-form NPR at eps<=1) — falsifying the docstring's 'Numerically independent of cf_ideal (which never touches Eq. 9)' (bounds.py:154-159) on both counts. The check (check_chain line 236-237, test_bounds.py B2+ lines 140-150) therefore measures roundoff of shared algebra and cannot fire against a wrong exhaustiveness theorem or a broken eps_star_of_pc; B5's 'dual-route breach REJECTED' (test_bounds.py:121-122) corrupts only the stored dict. M0 line 633 states 'int-max = ceiling, dual-route verified'. The exhaustiveness content is genuinely carried by Theorem 1 of validation/bell_optimality_proof.md; the module and M0 misattribute the certificate to a rejector that can never reject — a direct violation of rigor item (1)/(2) on a THEOREM-grade claim of record.

### [CONFIRMED | high | rejector-weakness] cycle-avg-machinery:F2-gamma-ladder-staleness

- FILE/LOC: tests/test_bounds_gamma.py — docstring line 13 ('row invariants re-run live via check_row') and G2/G3 blocks lines 55-75 vs src/thrust/bounds_gamma.py real_row/check_row (lines 290-413); contrast tests/test_bounds.py B1 lines 53-61; M0 lines 654-656

- CLAIM OF RECORD: M0 gamma-purge instance of record: "the real gamma(T) ceiling sits 4.4-7.9% BELOW the frozen-gamma_s closed-form oracle (bars ~0.002%, all significant) — now a number with a rejector instead of a hypothesis"; test docstring: invariants "re-run live ... (not the persisted flag)".

- FINDING: Unlike test_bounds.py (whose B1 recomputes every ladder_row live so 'the JSON cannot drift from the code that claims it'), test_bounds_gamma never recomputes any real-route row: check_row only re-evaluates inequality logic on the numbers already stored in data/bounds_ladder_real.json. The only live computation is the synthetic constant-cp known-answer gas. Hence the verdict-bearing numbers of record quoted in M0 (delta 4.4-7.9%, row bars, cap-probe margins) have no staleness rejector: a change in bounds_gamma.py (or in the blessed states) after the JSON was persisted would leave every test green while the recorded delta is wrong. 'Re-run live via check_row' overstates what the suite gates.

- EVIDENCE: test_bounds_gamma.py imports only REAL_JSON, TOL_KA, check_row, known_answer (line 35-36); rows come from json.load(REAL_JSON) (line 56-57); no call to real_row or evaluate_all anywhere in the test. check_row (bounds_gamma.py 383-413) takes only the persisted dict.

- SUGGESTED TEST: Add a B1-equivalent guard: recompute at least one finite-Pa subcritical row and one vacuum row live via real_row(s, Pa) from the blessed states and assert agreement with the persisted Isp/delta within tol_row (budget-bounded, e.g. reduced N_REAL with a correspondingly widened Richardson bar).

- VERIFIER NOTE: test_bounds_gamma.py:35-36 imports only REAL_JSON, TOL_KA, check_row, known_answer; grep of tests/ shows no call to real_row or evaluate_all anywhere (test_phase_diagram_real.py:49 also only reads REAL_JSON). G3 re-runs check_row (bounds_gamma.py:383-413), which takes only the persisted dict — inequality logic on stored numbers, never a recomputation. Best refutation attempt: G1 runs known_answer() live, but that exercises the route on a synthetic constant-cp gas only — a change to bounds_gamma.py parameters (M_TAB/N_REAL), products_gas anchoring, or the blessed states in data/st_nozzle_opt.json would alter the real rows while every test stays green with the stale JSON. This is an explicit asymmetry with the sibling suite: test_bounds.py B1 (lines 53-61) recomputes all 18 rows live precisely so 'the JSON cannot drift from the code that claims it'. No declared budget exemption found in the test docstring or PROGRESS. The unguarded numbers are exactly M0's verdict-bearing record (lines 654-656: 'real gamma(T) ceiling sits 4.4-7.9% BELOW ... now a number with a rejector instead of a hypothesis').

### [CONFIRMED | low | underived-constant] cycle-avg-machinery:F3-bars-not-all-derived

- FILE/LOC: src/thrust/bounds_gamma.py — docstring line 51 ('NUMERICS (all bars derived, no magic numbers)') vs constants lines 92-98 (M_TAB, N_SPOT=12, S_BAR=4.0, TOL_KA=1e-6) and bar_interp construction lines 326-332

- CLAIM OF RECORD: "NUMERICS (all bars derived, no magic numbers)"; TOL_KA "derived from the Cantera flash convergence (rel ~1e-9 on the state) times a sensitivity factor <= 1e2, times safety 10"; M0 line 653: "derived tol 1e-6".

- FINDING: Three of the bar ingredients are chosen, not derived: (1) the interpolation bar is S_BAR times the MAX deviation over N_SPOT=12 exact-flash probes out of N_REAL=801 phases — a sampled estimate whose validity rests on an undeclared smoothness/coverage assumption in xi, not an error bound (the max over 12 samples can undershoot the true max at unsampled phases); (2) S_BAR=4.0 is a bare safety factor justified only by analogy ('same role as the g0-spike K_RICH'); (3) the TOL_KA sensitivity factor '<= 1e2' is asserted, never measured. The bars enter tol_row, which decides the SIG/in-bar verdict per row and hence the M0 statement 'all significant'. (Mitigation: the recorded deltas are 4.4-7.9% vs bars ~0.002%, so the conclusion is robust to a large bar error — the defect is the 'derived, no magic' claim, not the verdicts.)

- EVIDENCE: bounds_gamma.py line 94-97: 'N_SPOT = 12  # exact-flash spot checks per row (bar measurement)'; 'S_BAR = 4.0  # safety on measured/Richardson bars'; line 326-331: dev = max over idx = linspace(0, n-1, N_SPOT) probes; bar_interp = S_BAR * dev * isp_real.

- SUGGESTED TEST: One-off dense probe: run real_phase_exact on all 801 phases for one subcritical row and record max_dense/max_12 (validates or corrects N_SPOT coverage); measure the KA sensitivity factor by finite-differencing the four KA checks against a 1e-9 relative flash perturbation; persist both so the bar derivation chain is closed.

- VERIFIER NOTE: bounds_gamma.py:51 claims 'NUMERICS (all bars derived, no magic numbers)' while lines 94-97 declare N_SPOT=12, S_BAR=4.0, TOL_KA=1e-6 and lines 326-331 build bar_interp as S_BAR times the max deviation over 12 sampled probes of 801 phases — a sampled estimate with an undeclared smoothness/coverage assumption, not an error bound; the TOL_KA 'sensitivity <= 1e2' is asserted, never measured. Partial refutation found and credited: validation/numeric_allowlist.json (src/thrust/bounds_gamma.py block) classifies all six constants with rationale notes and records the KA rejector firing at 8.7e-5 vs the 4.7e-7 pass value (M0 line 653) — so the constants are declared, lint-gated and rejector-demonstrated, refuting any 'undeclared' charge. But the notes are classifications ('NUMERIC: safety ... same role as K_RICH'), not derivations, so the blanket 'all bars derived' remains an overstatement of record. Severity lowered from medium: the recorded deltas (4.4-7.9%) exceed the bars (~0.002%) by 3+ orders, so every SIG verdict is robust to a large bar error; the defect is confined to the wording of the derivation claim.

### [CONFIRMED | medium | hidden-hypothesis] cycle-avg-machinery:F4-matched-fixedpoint-sensitivity

- FILE/LOC: src/thrust/stechmann_nozzle.py — matched() lines 305-335 (tol=0.01; det evaluated at the pre-update Pinit while the returned record carries the post-update Pinit); docstring Step 2 lines 37-38

- CLAIM OF RECORD: "Fixed-point iteration on Pinit = P_CJ/PR to |dPinit/Pinit| < 1e-2 (<= 5 CJ evaluations; each shifts P_CJ far less than the tolerance)."

- FINDING: The load-bearing sensitivity claim ('each shifts P_CJ far less than the tolerance') has no measurement and no rejector anywhere in the pipeline: nothing measures dP_CJ/dPinit (or dcstar0/dPinit). Moreover the blessed state of record is internally mixed: after the loop, PR/TCJ/gamma/cstar0 come from det_state at the PREVIOUS Pinit while the returned Pinit/P0 are the updated values (up to 1% apart at the tol=1e-2 exit). These blessed states are the sole physical input to the entire OP-0 ladder (bounds.py) and the gamma-purge record (bounds_gamma.py), so an unexpectedly strong CJ sensitivity (e.g. a dissociation-sensitive propellant added via the PROPS extension point) would silently bias every downstream number of record within a tolerance that was asserted, not derived.

- EVIDENCE: stechmann_nozzle.py lines 311-319: det = det_state(..., Pin) then Pnew = P0/PR; Pin = Pnew; break on conv < tol — the returned dict(..., Pinit=Pin, **det) pairs the new Pinit with the old-fill CJ state. No test in tests/ evaluates det_state at the converged Pinit for comparison.

- SUGGESTED TEST: After convergence, run one extra det_state at the final Pinit and persist |dP_CJ|/P_CJ, |dcstar0|/cstar0, |dPR|/PR; assert they are below a derived fraction (e.g. 1/10) of the downstream PASS_ISP budget, turning the docstring's sensitivity claim into a measured, rejectable number.

- VERIFIER NOTE: stechmann_nozzle.py:311-319 — the loop computes det = det_state(..., Pin) at the OLD Pin, then Pnew = P0/det['PR']; Pin = Pnew; break on conv < tol=0.01; the return (line 333-335) is dict(..., P0=P0, Pinit=Pin, **det): P0 = Pinit*PR is internally consistent, but TCJ/gamma/cstar0/PR come from the fill at the pre-update Pinit, up to 1% away from the returned Pinit at the tol exit. The docstring's load-bearing claim (line 38: 'each shifts P_CJ far less than the tolerance') has no carrier: grep of tests/ finds only test_cj_coherence.py, which compares det_state to the canonical cj_core solver at a FIXED Pinit (1 atm, line 70) — cross-solver coherence, not dPCJ/dPinit sensitivity; the allowlist merely classifies tol 0.01 as NUMERIC. These blessed states are the sole physical input to bounds.py ladder_row and bounds_gamma.py products_gas (which anchors at state['Pinit']*state['PR'] and state['TCJ']), so an unexpectedly strong CJ sensitivity through the PROPS extension point would bias every downstream number of record inside an asserted, unmeasured tolerance. The 5% Table-1 end-to-end validation cannot resolve a sub-1% mixing bias and covers only shipped propellants.

### [CONFIRMED | low | process] cycle-avg-machinery:F5-qmapping-uncommitted-datestamp

- FILE/LOC: data/q_mapping.json — working-tree diff vs HEAD: q_mapping.json line 4 ('updated': 2026-07-16 -> 2026-08-11) and q_mapping.md line 3 (generation date); generator src/cycles/q_mapping.py lines 166 and 196 (datetime.date.today())

- CLAIM OF RECORD: Mandate item: report what the uncommitted data/q_mapping.* edits change and whether anything of record depends on the uncommitted version. PROGRESS (line 723-725, 1494-1496, 1554): 'q_mapping.* datestamp rigenerati dagli esempi ... contenuto identico, dichiarato'.

- FINDING: VERDICT ON THE MANDATED ITEM: the uncommitted diff is datestamp-only (one line per file, 2026-07-16 -> 2026-08-11); every number, table row and claim is byte-identical to the committed state, so NOTHING of record depends on the uncommitted version. The underlying defect is that the generator embeds datetime.date.today() into artifacts of record, so any example/suite run dirties the tree; PROGRESS documents this being manually restored at least twice and 'never staged' during concurrent sessions — a recurring manual-restore burden and a standing risk that a future content-bearing regeneration hides inside an 'expected datestamp diff' and gets reverted or committed unreviewed.

- EVIDENCE: git diff --stat: 2 files, 1 line each; both hunks touch only the 'updated'/'Generated ... on' date. q_mapping.py line 166: updated=datetime.date.today().isoformat(); line 196: f-string with datetime.date.today().

- SUGGESTED TEST: Make the artifacts deterministic: stamp with the git commit of the inputs (or the hash of data/cycles_ws.json) instead of today's date, or move the date to a non-diffed sidecar; then a clean tree after a full example run becomes an assertable invariant (CI check: git diff --exit-code data/).

- VERIFIER NOTE: git diff verified: 2 files, 1 line each — q_mapping.json 'updated': 2026-07-16 -> 2026-08-11 and q_mapping.md generation date; every number, table row and claim byte-identical to HEAD, so nothing of record depends on the uncommitted version (the mandated verdict holds). Root cause confirmed: q_mapping.py line 166 (updated=datetime.date.today().isoformat()) and line 196 (f-string with datetime.date.today()) embed the run date into artifacts of record, so any example/suite run dirties the tree. PROGRESS lines 723, 1494 and 1554 document the datestamp being regenerated/declared and 'MAI toccati né staged' across sessions — the recurring manual-restore burden and the risk of a content-bearing regeneration hiding inside an 'expected datestamp diff' are real, as stated. Process-grade defect, correctly scoped low.

### [CONFIRMED | low | numerics] cycle-avg-machinery:F6-silent-clamp-unchoked

- FILE/LOC: src/thrust/st_core.py — cf_spike lines 126-131 (np.minimum(Pa/Pc, 1.0), np.clip(..., 0, None)); same construct in src/thrust/bounds.py cf_ideal_naive line 119

- CLAIM OF RECORD: bounds.py docstring lines 74-76: "the chain is pointwise in xi for rungs 0-2 ... so it holds row by row up to roundoff"; stechmann_nozzle docstring line 50: channel choking "is verified and reported for every case".

- FINDING: When a phase has Pc <= Pa (chamber below ambient — the choked-feed premise is void), cf_ideal_naive and the free branch of cf_spike silently return CF = 0 via the min/clip pair instead of raising or flagging: the ladder would keep evaluating and ordering physically meaningless rungs pointwise on such phases with no rejector able to notice. Currently dormant (the shipped 18 rows have min Pc/Pa above 1: choke_margin < 1 rows are below the SONIC threshold, not below ambient), but ladder_row/real_row accept arbitrary new states through the same code path, and the clamp converts a model-domain violation into a plausible number.

- EVIDENCE: st_core.py line 130: np.clip(1 - np.minimum(Pa / Pc, 1.0) ** ((g - 1) / g), 0, None) — for Pc < Pa the ratio clamps to 1, the bracket to 0, CF_free to 0 with no warning. No Pc > Pa guard exists in ladder_row (bounds.py lines 171-223) or real_phase_cf (bounds_gamma.py lines 193-225).

- SUGGESTED TEST: Add an explicit domain guard in ladder_row/real_row: assert Pc.min() > Pa (or persist a 'clamp_active' flag that check_chain/check_row rejects), plus a negative-control test feeding a PR large enough to drive Pc below Pa and requiring the guard to fire.

- VERIFIER NOTE: st_core.py:129-131 — cf_spike's free branch uses np.clip(1 - np.minimum(Pa/Pc, 1.0)**((g-1)/g), 0, None): for Pc < Pa the ratio clamps to 1, the bracket to 0, CF to 0 with no warning; identical construct in bounds.py:119 (cf_ideal_naive). No Pc > Pa guard exists in ladder_row (bounds.py:171-223), real_phase_cf (bounds_gamma.py:193-225 — where the sonic branch mdot*ustar + (Pstar - Pa) would silently go through as well), check_chain, or check_row (crit_ratio_real > 1 tests the sonic ratio Pc/P*, not chamber-vs-ambient). The stechmann docstring's 'channel choking ... verified and reported for every case' (line 49-50) covers reporting choke_margin against the SONIC threshold only; below-ambient chamber pressure is a different domain violation with no rejector. Dormant on the 18 shipped rows (subcritical rows have choke_margin < 1 but Pc/Pa > 1), and both ladder_row and real_row accept arbitrary new states through this path — the clamp converts a model-domain violation into a plausible number, exactly as claimed.

### [CONFIRMED | low | underived-constant] cycle-avg-machinery:F7-qformal-magic-tolerances

- FILE/LOC: src/cycles/q_formal.py — line 98 (assert abs(q_std_sum - q_std) < 1.0) and lines 117-118 (assert abs(I_quad - I_ident) < max(2.0, 1e-4 * abs(I_ident)))

- CLAIM OF RECORD: R5 rigor culture: "tolleranze derivate, non magiche"; this script is the numerical backing of validation/q_formal.md and the standard-state anchor cited by q_mapping.py/md and the D-docs ('q_c(300K) - q_std = +0.0076%/-0.0023%').

- FINDING: The two assert thresholds are magic: 1.0 J/kg, 2.0 J/kg and 1e-4 relative are stated nowhere as derived. The 2.0 J/kg floor for the 201-node trapezoid cross-check is derivable (composite-trapezoid error (b-a) h^2/12 max|d2(dcp)/dT2| plus Cantera cp evaluation noise) but the derivation is absent, so the rejector's firing threshold is a guess: too loose and it cannot reject a subtly wrong sensible-shift integral; the sensible shift is exactly the quantity promoted to the docs as the -0.0076%/+0.0023% anchor of record.

- EVIDENCE: q_formal.py line 117: 'assert abs(I_quad - I_ident) < max(2.0, 1e-4 * abs(I_ident))' with only the comment 'independent check'; no derivation in the module docstring (lines 9-37) or validation/q_formal.md is referenced for the numbers 1.0/2.0/1e-4.

- SUGGESTED TEST: Derive the trapezoid bound from the sampled dcp array itself (second-difference estimate of max|d2 dcp/dT2| on the 201-node grid) and assert against that computed bound times a declared safety, replacing the literals; add a corrupt-control (scale dcp by 1+1e-3) that must trip the assert.

- VERIFIER NOTE: q_formal.py:98 'assert abs(q_std_sum - q_std) < 1.0' and :117 'assert abs(I_quad - I_ident) < max(2.0, 1e-4 * abs(I_ident))' — the literals 1.0/2.0/1e-4 carry no derivation in the module docstring (lines 9-37) and none in the allowlist: validation/numeric_allowlist.json's q_formal.py entries restate them ('checked vs closed enthalpy identity, 2 J/kg'; 'relative floor of the quadrature-vs-identity assert') without deriving the trapezoid bound, so the classification does not close the R5 'tolleranze derivate' requirement. Mitigation verified, keeping severity low: the rejector can fire (2 J/kg is ~0.8% of the ~260 J/kg 300 K sensible shift, ~1e-4 relative at 700 K), so a grossly wrong sensible-shift integral would trip it; the defect is that the firing threshold is a guess, and the guarded quantity is the -0.0076%/+0.0023% standard-state anchor quoted in q_mapping.py/md and M0-adjacent docs.

### [CONFIRMED | low | gamma-leak] cycle-avg-machinery:F8-inline-gamma12-constants

- FILE/LOC: src/cycles/q_mapping.py — lines 132 (qt_cp12 = 2.0 * H / 2.2), 140 (qtilde_R_eff_g12 = 6.0 * qt_cp12), 142 (m_cj(1.1 * qt_R_true / 6.0))

- CLAIM OF RECORD: Module computes the 'Fig. A22 family (gamma = 1.2)' traceability of record for fig_cycle_family (cycles.py stage_plot reads data/q_mapping.json); the 5%-family-fit claim is printed into data/q_mapping.md and asserted at line 151-153.

- FINDING: The gamma=1.2 family constants are hardcoded as bare numerics — 2.2 (= g+1), 6.0 (= g/(g-1)), 1.1 (= (g+1)/2) — with no symbolic derivation from a single gamma variable, in a module whose other formulas are written gamma-symbolically. Declared context (the A22 family is explicitly a gamma=1.2 model, and the md states 'the family is a fuel-air-class model'), so this is not a leak into a gamma-general claim; but the three literals encode the same gamma three independent ways, so a future edit changing the family gamma would desynchronize them silently — exactly the mirrored-constant failure mode the gamma-variable directive exists to prevent.

- EVIDENCE: q_mapping.py line 132: 'qt_cp12 = 2.0 * H / 2.2  # gamma = 1.2 inversion'; line 140: 'qtilde_R_eff_g12=6.0 * qt_cp12'; line 142: 'M_model_g12_from_qtildeR=float(m_cj(1.1 * qt_R_true / 6.0))'. cycles.py repeats the conversion independently at line 318 ('qh = q * (1.2 - 1.0) / 1.2') and line 928 ('qh = q * 0.2 / 1.2').

- SUGGESTED TEST: Introduce G_FAM = 1.2 once and express the three factors symbolically ((G_FAM+1)/2, G_FAM/(G_FAM-1)); add a one-line self-check that the q_mapping and cycles.py family conversions agree at one q value (cross-module mirrored-constant rejector).

- VERIFIER NOTE: Literals verified: q_mapping.py:132 'qt_cp12 = 2.0 * H / 2.2', :140 '6.0 * qt_cp12', :142 'm_cj(1.1 * qt_R_true / 6.0)'; cycles.py:318 'qh = q * (1.2 - 1.0) / 1.2' and :928 'qh = q * 0.2 / 1.2' duplicate the same conversion in two spellings with no cross-module rejector. Refutation partially succeeds and narrows the finding: each literal IS declared — inline comments plus validation/numeric_allowlist.json SPEC entries naming the symbolic identity ('1.1: (gamma+1)/2 at gamma=1.2 - family-model algebra', '2.2: gamma+1 at gamma=1.2'), the family gamma is a paper-pinned SPEC (Fig. A22) rather than a tunable, and the dev_air < 5% assert (q_mapping.py:151-153) weakly binds the 1.1/6.0 pair to the physical M_CJ data. What survives is exactly the mirrored-constant residue: three independent encodings of one gamma (plus two more in cycles.py), with the only live cross-check (5% width) too wide to catch a single-constant typo of a few percent and nothing at all asserting on qtilde_R_eff_g12 (the 2.2/6.0 chain). Not a gamma-leak (declared context), a low-severity maintenance defect of the named mirrored-constant class.


## phase-diagrams (candidates dropped by finder cap: 8)

### [CONFIRMED | medium | rejector-weakness] phase-diagrams:oracle-primary-never-confronted

- FILE/LOC: src/thrust/phase_diagram_real.py — phase_diagram_real.py:8-15 (oracle declaration); tests/test_phase_diagram.py:44-48 and tests/test_phase_diagram_real.py:50-54 (each test imports only its own record)

- CLAIM OF RECORD: phase_diagram_real.py docstring: after the S7 purge the gamma=const closed forms of phase_diagram.py 'are DECLARED ORACLES, so the diagram itself needed the primary-route re-derivation. This module supplies it. The eps-level module REMAINS of record as the closed-form oracle instrument; this one is the PRIMARY-route instance.'

- FINDING: The declared oracle/primary pair is never confronted by any executable check. test_phase_diagram.py gates only the eps record (data/phase_diagram.json, the source of the P-1 figure per phase_diagram.py:96-99); test_phase_diagram_real.py gates only the real record; no test compares winner maps, m1_gap_zero flags, eps*/knee values, or premium_bound between the two JSONs, even band-aware. A divergence between the gamma=const diagram of record and its EOS-general primary re-derivation (the exact failure class the oracle relationship exists to catch) would fire nothing. The verdict-bearing artifact (eps winners map feeding the paper figure) is thus unfalsifiable against its own declared primary route.

- EVIDENCE: Grep over the repo: only run_all.py, the two test files, the two modules, and examples/example_phase_diagram.py reference 'phase_diagram'; test_phase_diagram.py imports DIAG_JSON only, test_phase_diagram_real.py imports REAL_DIAG_JSON + REAL_JSON only; cross-anchor checks D7 (test_phase_diagram.py:187-195) and E8 (test_phase_diagram_real.py:185-192) each compare against their own ladder record, never against each other.

- SUGGESTED TEST: Add a cross-record oracle test: for every (PR, eps_max) cell, winner labels must agree up to ties resolvable within the two cells' summed tol_abs; |premium_bound_eps - premium_bound_real| and |m1_gap_eps - m1_gap_real| within summed derived bars plus a declared model-form term; corrupt one record's winner to prove the comparison can reject.

- VERIFIER NOTE: No file loads both records: test_phase_diagram.py imports only DIAG_JSON+LADDER_JSON (lines 42-49, 60, 188), test_phase_diagram_real.py only REAL_DIAG_JSON+REAL_JSON (lines 49-51, 66, 186); D7/E8 each go to their own ladder. phase_diagram_real.py:33-35 even declares 'the closed form NPR(eps*) = <Pc>/Pa is its gamma = const oracle' for eps_star_real, yet no test compares the persisted eps_star/knee/winner/premium_bound of the two records, and known_answer_eq (lines 359-394) covers only the eq-bracket route, not the diagram machinery. Severity downgraded from high: structural oracles (T3/T4/M1, dominance) are enforced independently on both routes so structural divergence fires within-suite, and the blessed-PR ceiling is transitively confronted via D7 + E8 + bounds_gamma's per-row delta-vs-closed-oracle with bars (test_bounds_gamma.py G4, lines 85-91). The uncovered class is quantitative/winner-boundary divergence across the synthetic PR sweep and the diagram-specific quantities.

### [CONFIRMED | medium | hidden-hypothesis] phase-diagrams:gauge-hypothesis-no-rejector

- FILE/LOC: src/thrust/phase_diagram.py — phase_diagram.py:89-92 (hypothesis), 124-128 (PR_GRID comment deriving 90 by hand from the current anchor); src/thrust/bounds.py:207-215 (choke_margin definition)

- CLAIM OF RECORD: Docstring: 'PR grid capped so that min-cycle Pc >= Pa (choked-feed edge + gauge validity of the (p - Pa) closed forms; choke_margin persisted per cell regardless)'; grid comment: '90 = largest octave-ish rung keeping min-cycle Pc >= Pa at <Pc> ~ 20.9 atm (Pc_min/Pa = ... 1.05 there; 128 would cross 1)'.

- FINDING: The hypothesis min-cycle Pc >= Pa is enforced only by a hand-derived comment tied to the CURRENT anchor's <Pc>. No code asserts Pc.min() >= Pa: check_cell (lines 234-271) has no gauge item, and the persisted choke_margin is Pc_min/(Pa*NPR_sonic) (bounds.py:208) — the SONIC margin, already < 1 on the declared subcritical strip — so it cannot signal a Pc < Pa breach as the docstring's 'persisted regardless' implies. Changing ANCHOR or appending a PR grid point silently evaluates cells outside the gauge validity of the (p - Pa) closed forms and outside the choked-feed hypothesis, and every rejector still passes. phase_diagram_real.py reuses the same PR_GRID (line 104, 435) with no guard either. The 90.0 endpoint is an anchor-dependent constant with no executable tie to its derivation.

- EVIDENCE: phase_diagram.py:128 hardcodes PR_GRID=[...,90.0]; cell_row (178-208) and check_cell (234-271) contain no comparison of Pc.min() against Pa; bounds.py:208 'margin = float(Pc.min() / Pa / NPRs)' shows choke_margin measures supercriticality, not Pc>=Pa; phase_diagram_real.py:435 'PRs = sorted(set(PR_GRID) | ...)' inherits the grid unguarded.

- SUGGESTED TEST: Persist gauge_margin = Pc.min()/Pa per cell and make check_cell/check_cell_real reject gauge_margin < 1; negative control: evaluate a synthetic cell at PR=128 on the current anchor and assert it is rejected.

- VERIFIER NOTE: Hypothesis 'PR grid capped so that min-cycle Pc >= Pa' lives only in phase_diagram.py:89-92 and the hand-derived grid comment (lines 124-128, PR_GRID endpoint 90.0 tied to the CURRENT anchor's <Pc> ~ 20.9 atm). check_cell (234-271) contains no Pc.min() vs Pa item; nothing in cell_row or the tests asserts it. bounds.py:208 confirms choke_margin = Pc.min()/Pa/NPR_sonic — the sonic margin, already < 1 on the declared subcritical strip (data/phase_diagram.md:35), so it flags Pc < Pa only below ~1/NPR_sonic(1.64) ≈ 0.49 and no code checks that threshold either. phase_diagram_real.py:435 inherits PR_GRID unguarded (its only RuntimeError, eps_star_real lines 219-221, guards table depth, not the gauge). Changing ANCHOR or extending the grid would evaluate cells outside the declared hypothesis with every rejector still passing.

### [CONFIRMED | medium | rejector-weakness] phase-diagrams:ka-self-inflating-tolerance

- FILE/LOC: src/thrust/phase_diagram_real.py — phase_diagram_real.py:389-394 (tol = TOL_KA + S_BAR * rel_c), 382-384 (corrupt mode scales only the h column)

- CLAIM OF RECORD: Docstring lines 62-64: the equilibrium route carries 'a known-answer rejector (constant-cp gas: ... the eq route must reproduce the closed forms within bounds_gamma.TOL_KA; corrupted route rejected)'.

- FINDING: The known-answer acceptance bar is partly measured from the route under test: tol = TOL_KA + S_BAR*rel_c, where rel_c = |ustar_eq/ustar_frozen - 1| is computed from the same eq table being validated. Any defect that enters through the eq sound-speed channel (e.g. a bug in the c^2 = np.gradient(P, rho) derivative at build_eq_table:156, or a corrupted c column) inflates rel_c and therefore widens its own tolerance — that defect class cannot be rejected by construction. The shipped negative control (corrupt=True, line 382) perturbs only the h column at one magnitude (1e-4), so the rejector is demonstrated only on the channel that does NOT feed the tolerance term.

- EVIDENCE: Lines 392-394: 'rel_c = abs(float(pe[\'ustar\'][0] / pf[\'ustar\'][0]) - 1.0); tol = TOL_KA + S_BAR * rel_c'; line 382: corruption is 'h[::-1] * ((1.0 + 1e-4) if corrupt else 1.0)' — h only; tests/test_phase_diagram_real.py:182-183 exercises only that h-corruption.

- SUGGESTED TEST: Replace the measured rel_c term with an a priori nested-grid (M_EQ vs half) bound on the table-derivative sound-speed error, and add a negative control that scales the eq c column (not h) by 1e-3 and asserts rejection.

- VERIFIER NOTE: phase_diagram_real.py:392-394: rel_c = |pe['ustar']/pf['ustar'] - 1| is computed from the eq table under test and tol = TOL_KA + S_BAR*rel_c (S_BAR = 4.0, bounds_gamma.py:95). A defect entering through the eq sound-speed channel (c^2 = np.gradient(P, rho), build_eq_table line 156; only guard is positivity, lines 157-159) shifts ustar and cf_capped by the same order, so the tolerance widens commensurately with the defect — that channel cannot be rejected by construction. The shipped negative control perturbs only the h column at 1e-4 (line 382; test_phase_diagram_real.py:182-183), i.e. the one channel that does NOT feed the tolerance. The eq route feeds the E6 bracket rows, whose frozen<=eq check is one-sided, so a c-channel bias could hide inside the percent-level recombination gain.

### [CONFIRMED | low | numerics] phase-diagrams:eps-tolerance-omits-quadrature-bar

- FILE/LOC: src/thrust/phase_diagram.py — phase_diagram.py:48-50 and 91-92 (tolerance claim), cell_row:191-207 (m1_gap, premium_bound persisted); src/thrust/bounds.py:100,211 (TOL_REL = NQ*EPS_MACH, tol_abs = TOL_REL*Isp_ideal); contrast phase_diagram_real.py:249-252,287 (bar_quad added on the real route)

- CLAIM OF RECORD: Docstring: 'tie if |plug - bell| <= tol_abs (derived roundoff tolerance, NQ*eps_mach — no magic numbers)'; premium_bound is 'the tournament device' certifying 'the MAXIMUM any non-bell sector can earn there' (data/phase_diagram.md scope block, _write_md:400-413).

- FINDING: tol_abs covers ONLY roundoff (~4001*eps_mach ≈ 9e-13 relative), but the persisted numbers of record m1_gap and premium_bound carry trapezoid discretization error on kinked integrands (release-point and cap kinks) that is orders of magnitude larger than tol_abs and is nowhere quantified per cell. The winner SIGN is protected by pointwise dominance on a shared grid, but premium_bound is advertised as a certified upper bound for the future sector tournament, and its numeric band excludes the dominant error term — a bell-vs-sector adjudication using it at face value could flip inside the unbarred quadrature error. The real-route module derives and adds bar_quad by nested Richardson (line 287), an implicit acknowledgment that the eps-level tolerance is missing this term; the 'no magic numbers' claim is therefore narrower than stated: the tolerance is derived, but it is not a bar on the quantity it accompanies.

- EVIDENCE: bounds.py:100 'TOL_REL = NQ * EPS_MACH # roundoff bound for an NQ-term quadrature sum'; stechmann_nozzle.py:172 'NQ = 4001 # ... convergence checked vs 16001' (a one-time global check, not a per-cell bar); phase_diagram.py cell_row persists m1_gap (203) and premium_bound (207) with only row['tol_abs'] from ladder_row; phase_diagram_real.py:249-252 computes bar_quad = S_BAR*|Isp(N)-Isp(N/2)|/3 and folds it into tol_abs at 287 — absent from the eps module.

- SUGGESTED TEST: Per eps-level cell, recompute cand/ideal on the nested NQ//2+1 grid and persist a Richardson bar; assert |premium_bound(NQ) - premium_bound(nested)| <= bar and quote premium_bound ± bar in the JSON/MD; rejector: inflate the persisted bar-free premium_bound by 2x the measured bar and require rejection.

- VERIFIER NOTE: Confirmed factually: bounds.py:100/211 gives tol_abs = NQ*EPS_MACH*Isp_ideal (~9e-13 rel, roundoff only); trapezoid error on the kinked integrands at NQ=4001 (h=2.5e-4) is ~1e-7-1e-8 rel, 4-5 orders larger and nowhere barred per cell; phase_diagram_real.py:249-252/287 derives and folds in bar_quad, absent from the eps module, while premium_bound is persisted (cell_row:207) and advertised as certifying 'the MAXIMUM any non-bell sector can earn' (data/phase_diagram.md:16-17). Severity reduced to low because refutation narrowed the exposure: winner sign is exact (pointwise dominance on a shared grid makes trapezoid ordering exact), m1_gap_zero cancels identically at attainment (same integrand), a one-time NQ convergence check vs 16001 exists (stechmann_nozzle.py:172), and any realistic tournament band dwarfs 1e-8 rel — the residual defect is a bar-of-record that omits the dominant error term of the number it accompanies.

### [CONFIRMED | medium | correlated-error] phase-diagrams:interp-bar-covers-only-ceiling-channel

- FILE/LOC: src/thrust/phase_diagram_real.py — phase_diagram_real.py:449-458 (bar_interp_rel from real_phase_exact spot flashes on cf_capped), 165-205 (phase_branches / pe_of_eps / bell_cf_at: extra interpolation layer), 287 (same bar reused in the candidates' tie tolerance)

- CLAIM OF RECORD: Docstring NUMERICS block: 'interpolation bar per PR by bounds_gamma.real_phase_exact spot flashes; tie tolerance = the cell's summed derived bars' (i.e. all interpolation error in the compared quantities is claimed covered by derived bars).

- FINDING: The spot-flash interpolation bar is measured on the CEILING route (cf_capped) only, but the bell and plug candidates pass through an additional interpolation channel the flashes never exercise: the per-phase area-ratio inversion (branch arrays built from the M_TAB table, np.interp of eps->lnP in pe_of_eps, then h/rho re-interpolation in bell_cf_at). The tie tolerance at line 287 applies the ceiling-route bar to comparisons dominated by branch-route quantities, so the 'summed derived bars' guarantee is weaker than declared: the branch-inversion error is analogized from a different channel, not derived. Winner/tie labels near the tolerance edge inherit an uncontrolled term.

- EVIDENCE: Lines 452-457: dev is max over |real_phase_exact - ph['cf_capped'][i]|/|cf_ex| — cf_capped only; cell_row_real:276-287 computes cf_bell_v and cf_plug_branch via bell_cf_at (branch interpolation) and sets tol_abs = bar_quad + bar_interp_rel*isp_ideal + roundoff, with no bell-branch exact-flash probe anywhere in the module or its test.

- SUGGESTED TEST: Extend the spot-flash probe to the bell branch: at N_SPOT phases, solve the exact area-ratio inversion by direct SP flashes + bisection at eps_bell and eps_plug, measure the deviation from bell_cf_at, and take the max of the two channels as bar_interp_rel; corrupt the branch interpolation (e.g. coarsen the branch grid 8x) and assert the enlarged bar is detected.

- VERIFIER NOTE: phase_diagram_real.py:452-458: dev is the max over |real_phase_exact - ph['cf_capped'][i]|/|cf_ex| — the ceiling channel only. The bell/plug candidates pass through an additional interpolation layer the flashes never exercise: phase_branches (165-187), pe_of_eps np.interp of eps->lnP (190-195), and bell_cf_at h-re-interpolation (198-205); cell_row_real:287 applies the ceiling-measured bar_interp_rel to comparisons dominated by those branch quantities, so the docstring claim 'tie tolerance = the cell's summed derived bars' (line 80) is analogized, not derived, for the branch channel. Mitigations found (exact ties when eps_bell == eps_plug cancel identically; the exact sonic point is prepended at lines 183-184; S_BAR=4 safety) reduce but do not remove the defect: near the eps_star boundary the tie-vs-plug-capped label of the record is decided inside a tolerance with an unmeasured term, and no negative control (e.g. coarsened branch grid) exists in module or test.

### [CONFIRMED | low | underived-constant] phase-diagrams:znd-tuned-tolerances-nonatomic-write

- FILE/LOC: src/detonation/znd_profiles.py — znd_profiles.py:43-46 (per-case t_end/max_step 'tuned per mixture stiffness'), 58 (relTol=1e-5, absTol=1e-9), 80 (json.dump(out, open(OUT, 'w')) non-atomic)

- CLAIM OF RECORD: Docstring: 'The shipped file is the validated build artifact; re-running reproduces it (energy invariant h + U^2/2 conserved to 6e-6 %)'; repo rule of record: every tolerance derived, never magic.

- FINDING: The ZND ODE tolerances (relTol=1e-5, absTol=1e-9) and the four per-mixture (t_end, max_step) windows are tuned constants with no derivation or convergence bar tied to the shipped induction/exothermic lengths (numbers of record in data/znd_sdt.json). The completeness assertions (lines 62-68) are genuine rejectors for truncated windows, but nothing bounds the discretization error of ind_mm/exo_mm themselves (no tolerance-halving reproducibility check). Separately, the output JSON is written with a bare json.dump on an open handle (line 80) — a crash mid-write corrupts the shipped artifact, unlike sk_models.py's tmp+os.replace pattern (sk_models.py:66-69).

- EVIDENCE: Line 58: 'zndsolve(gas, gas1, cj, relTol=1e-5, absTol=1e-9, t_end=tend, max_step=mstep, ...)'; CASES tuple comments at 42-46 give no derivation; line 80: 'json.dump(out, open(OUT, \'w\'))' with no temp-file swap.

- SUGGESTED TEST: Re-run one case at relTol/10 and max_step/2 and assert ind_mm/exo_mm move by less than a declared reproducibility bar (persist that bar in the JSON); switch the writer to tmp+os.replace.

- VERIFIER NOTE: znd_profiles.py:58: relTol=1e-5, absTol=1e-9 and the four per-mixture (t_end, max_step) windows (lines 43-46, comment 'tuned per mixture stiffness') carry no derivation and no tolerance-halving/step-halving reproducibility bar on the shipped ind_mm/exo_mm; the completeness asserts (62-68) reject only truncated windows, not discretization error. Grep over tests/ finds NO test referencing znd at all, so the shipped data/znd_sdt.json numbers have no test carrier beyond the in-script asserts. Line 80: json.dump(out, open(OUT, 'w')) — non-atomic, vs the tmp+os.replace pattern of sk_models.py:66-69. Severity low: the module is the lecture/pedagogical detonation-structure artifact, outside the phase-diagram verdict chain.

### [CONFIRMED | low | underived-constant] phase-diagrams:cj-fd-soundspeed-no-truncation-bar

- FILE/LOC: src/detonation/cj_states.py — cj_states.py:44-59 (soundspeed_eq: one-sided FD, rho1 = rho0*(1+1e-4)), 117 (gamma_eq derived from it)

- CLAIM OF RECORD: Docstring: gamma_eq = rho2*a_eq^2/P2 is 'the equilibrium isentropic exponent, ~1.13-1.17 ... the gamma_e of Shepherd & Kasahara Eq. 19-22'; validated against Caltech DB 'U_CJ within 0.1%'.

- FINDING: soundspeed_eq uses a one-sided finite difference with a fixed relative density step of 1e-4 and no truncation/cancellation bar; a_eq and the derived gamma_eq (line 117) therefore carry an unquantified O(drho) bias plus equilibrate-solver noise. Mitigation exists — this solver is the declared pedagogical cross-check, and tests/test_cj_coherence.py holds it to the canonical cj_core chain at a declared 2e-3 cross-solver tolerance — so the exposure is bounded, but the FD step itself is a magic constant and the one-sided scheme is first-order where the sdtoolbox reference (and sk_models.aeq fallback, sk_models.py:113-116) uses a central difference.

- EVIDENCE: Lines 52-56: 'rho1 = rho0*(1.0 + 1e-4); gas.SV = s0, 1.0/rho1; ... a_eq = float(np.sqrt((P1 - P0)/(rho1 - rho0)))' — single forward difference, no step-halving check; contrast sk_models.py:112-116 central difference over (P0-dP, P0+dP).

- SUGGESTED TEST: Add a step-halving check inside soundspeed_eq (evaluate at 1e-4 and 5e-5, require agreement within a declared bar) or switch to the central-difference form and assert agreement with sdtoolbox soundspeed_eq to a derived tolerance on the 5 demo mixtures.

- VERIFIER NOTE: cj_states.py:52-56: single one-sided forward difference with fixed rho1 = rho0*(1+1e-4), no step-halving or truncation bar; gamma_eq derived from it at line 117. Contrast: sk_models.py:112-116 central difference over (P0-dP, P0+dP). Mitigation confirmed exactly as the finding declares: cj_states is the declared pedagogical cross-check (docstring lines 11-15) and tests/test_cj_coherence.py holds its UCJ and gamma_eq to the canonical cj_core chain at the declared 2e-3 cross-solver tolerance (lines 8, 61-66), bounding the exposure. The residual defect — a magic FD step and first-order scheme inside a validated-against-0.1% claim — stands at low severity.

### [CONFIRMED | low | gamma-leak] phase-diagrams:eps-md-lacks-gamma-status

- FILE/LOC: src/thrust/phase_diagram.py — phase_diagram.py:377-437 (_write_md — the human table data/phase_diagram.md), contrast phase_diagram_real.py _write_md:525-534 which names the EOS-general route

- CLAIM OF RECORD: Standing gamma directive (memory 'gamma-variable-generality'): every theory piece states its variable-gamma status, with gamma=const boundaries named; phase_diagram_real.py:10-13 records that the eps diagram's closed forms are gamma=const DECLARED ORACLES after the S7 purge.

- FINDING: The human-facing eps-level record data/phase_diagram.md (generated by _write_md) carries the winner-semantics scope block but NO statement of its gamma=const / declared-oracle status and no pointer to the EOS-general primary re-derivation (phase_diagram_real). The frozen-gamma hypothesis lives only in the module docstring (line 87 'frozen gamma') and in the OTHER module's header. Since this MD (and the JSON it mirrors) is the figure source for P-1, a reader of the artifact alone cannot see that every number in it is a gamma=const oracle with a primary-route counterpart — precisely the leak class the standing directive targets.

- EVIDENCE: _write_md lines 382-435: the L list contains scope/oracle text about winner semantics, T3/T4 limits, and the subcritical strip, but no occurrence of 'gamma', 'frozen', 'oracle vs primary', or a reference to phase_diagram_real; phase_diagram_real.py:10-15 is the only place the eps diagram's oracle status is written.

- SUGGESTED TEST: Add a gamma-status banner line to _write_md ('all cells gamma=const closed forms — DECLARED ORACLE of the EOS-general primary record data/phase_diagram_real.md') and a doc-consistency check in test_phase_diagram.py asserting the banner is present in the shipped MD.

- VERIFIER NOTE: Read data/phase_diagram.md in full: no occurrence of 'gamma', 'frozen', or any oracle-vs-primary status, and no pointer to phase_diagram_real — the scope block (lines 11-17) covers winner semantics only, matching _write_md (phase_diagram.py:377-437) which contains no gamma-status line. The frozen-gamma hypothesis lives only in phase_diagram.py:86-92 and in the OTHER module's header (phase_diagram_real.py:9-15), while phase_diagram_real.md's generated header names its EOS-general route explicitly (_write_md lines 525-534). Under the standing gamma-variable directive, the human-facing eps record does not disclose that all its numbers are gamma=const declared oracles with a primary-route counterpart. Low severity: it is a generated artifact of a module whose docstring does declare the hypothesis, and the P-1 figure pipeline consumes the JSON via examples, not the MD.


## ledger-coherence (candidates dropped by finder cap: 7)

### [CONFIRMED | high | doc-code-mismatch] ledger-coherence:P1-f2-locus-stale

- FILE/LOC: docs/rde_nozzle_P1_sections_5_7.md — §5.1(a), lines 47-57

- CLAIM OF RECORD: "the optimal control surface is the phase's terminal characteristic, and along it the first integral f2 = V cos(theta -/+ alpha)/cos(alpha) = -lambda2(xi)" (paper text of record, S10).

- FINDING: The S19 LOCUS CORRECTION OF RECORD (theorem ledger D3 lines 465-467: the control surface is the C+ traced back from the lip and STOPPED at the kernel boundary; on the full C+ f2 drifts 2.9e-01 and 'the row looks falsified') was propagated to D3, P2_lemmaA (line 545), P2_outline (lines 217-232) and the X-O33B registry entry, but NOT to the P-1 paper text of record. Read literally ('terminal characteristic' = full C+ to the axis), the paper's central §5 identity is numerically falsified by the program's own O3.3 carrier; the claimed constancy holds only on the kernel-stopped surface (9.5e-03 / 8.0e-03). This is an R4 retro-propagation violation in the paper source itself.

- EVIDENCE: docs/rde_nozzle_P1_sections_5_7.md:48-53 (no kernel-boundary qualifier anywhere in the file; grep for 'kernel' returns nothing); docs/rde_nozzle_theorem_ledger.md:465-467 ('LOCUS CORRECTION OF RECORD (S19) ... on the full C+ to the axis f2 drifts 2.9e-01 and the row looks falsified'); docs/claims_registry.yaml:1205-1217 (X-O33B scope repeats the correction and the 2.9e-01 drift).

- SUGGESTED TEST: Add the kernel-boundary locus to §5.1(a) and its audit line, then add a lint rule (extend test_claims_lint (e) or a new doc-lint) requiring every doc sentence asserting f2 constancy to co-cite the kernel-stopped locus; seed the uncorrected phrasing as a rejected violation.

- VERIFIER NOTE: docs/rde_nozzle_P1_sections_5_7.md:47-57 asserts the f2 first integral 'along the phase's terminal characteristic' with no kernel-boundary qualifier (grep 'kernel' in the file hits only line 171, the unrelated 'kernel/transport propositions'); the §5.1 audit falsifier (lines 113-115, 'computed lambda2(xi) drifting from -f2(lip) beyond margins') is exactly what measures 2.9e-01 on the full C+ per D3:465-467 ('LOCUS CORRECTION OF RECORD (S19)... on the full C+ to the axis f2 drifts 2.9e-01 and the row looks falsified'). The correction was propagated to M0:1187, P2_lemmaA:545, P2_outline:211-233 (which records that the campaign itself initially misread the locus — defeating the refutation that 'terminal characteristic' unambiguously means the kernel-stopped piece) and registry X-O33B:1208, but never to the P-1 text of record (S10, 2026-07-17, predates S19). R4 retro-propagation violation in the paper source confirmed.

### [CONFIRMED | medium | citation] ledger-coherence:D4-novelty-zero-hits-vs-KO1970

- FILE/LOC: docs/rde_nozzle_claims_verdict.md — lines 10-14 (b2 gate), line 44 (T3-novelty row), vs lines 100-107 (S13 addendum)

- CLAIM OF RECORD: "b2 found ZERO hits for family-averaged classical contouring (C1), averaged Rao-type wall/corner conditions (C2)" and "T3 novelty ... SURVIVES query-bounded ... b2 confirms zero hits on averaged optimality conditions".

- FINDING: The same document's S13 ADJUDICATION ADDENDUM confirms Kraiko-Osipov PMM 34(6) 1970: 'trajectory-averaged weighted variational contouring with characteristic multiplier fields and an averaged-weight collapse case CONFIRMED'. The C1/C2 zero-hit paragraph and the T3-novelty row carry no supersession marker pointing to the addendum, although D4 demonstrably uses in-row supersession markers elsewhere (the Rao 'Eq. 14' row, lines 25-29). Downstream, the P-1 skeleton still describes the contingency as 'armed' (line 15) and claim C26 as 'QUERY-BOUNDED (2026-07-16)' (line 377) although its falsifier ('any surfaced citation') fired and was adjudicated 2026-07-22; only sections_2_4 was reworded. The novelty ledger of record is internally contradictory without markers.

- EVIDENCE: docs/rde_nozzle_claims_verdict.md:10-14, 44, 100-107 read directly; supersession-marker practice at lines 25-29; docs/rde_nozzle_P1_skeleton.md:15 ('D4 §3 contingency armed'), :377 (C26 row unchanged); docs/rde_nozzle_P1_sections_2_4.md:27,491 confirm adjudication was recorded only there.

- SUGGESTED TEST: Mirror the Rao-row practice: add [SUPERSEDED/QUALIFIED — see §3 ADJUDICATION ADDENDUM] markers to the b2-gate paragraph and the T3-novelty row; refresh skeleton line 15 and the C26 row to 'ADJUDICATED (containment, K-O 1970 mandatory citation)'; add a claims-lint orphan-style check that 'zero hits' phrasings co-cite the adjudication row.

- VERIFIER NOTE: docs/rde_nozzle_claims_verdict.md:10-14 ('b2 found ZERO hits' for C1/C2) and :44 (T3-novelty row) carry no marker pointing to the same doc's S13 addendum (:100-107, K-O PMM 34(6) 1970 'trajectory-averaged weighted variational contouring with characteristic multiplier fields and an averaged-weight collapse case CONFIRMED', resolved in containment with mandatory citation), although in-row supersession is this doc's demonstrated practice (Rao 'Eq. 14' row, :25-28, marker added S15). Downstream staleness verified: P1_skeleton:14-16 still says 'D4 §3 contingency armed' and the C26 row (:377) still 'QUERY-BOUNDED (2026-07-16)' though its falsifier ('any surfaced citation') fired 2026-07-22. One evidence correction: the S14 sync also updated the sections_5_7 header (:29-34, 'ADJUDICATED'), not only sections_2_4 — which confirms the sync existed and skipped D4's rows and the skeleton. Defect stands.

### [CONFIRMED | medium | doc-code-mismatch] ledger-coherence:T-LEMA-ii-class-contradiction

- FILE/LOC: docs/rde_nozzle_theorem_ledger.md — §8, lines 462-464 vs lines 513-522

- CLAIM OF RECORD: S19 status (2026-08-06, the latest entry): "Classes of (i)-(iii) therefore UNCHANGED (THEOREM*)".

- FINDING: This contradicts both the same section's earlier P-A1'-discharge paragraph ('Lemma A identification (ii) is now THEOREM within the irrotational homentropic scope (EOS-general)', lines 518-520) and the registry of record (T-LEMA-ii: class THEOREM, inherits [], suffices_symbolic yes, carriers X-PA1/X-P2A1). P-1 §5.3 (sections_5_7 lines 170-173) also lumps all 'component identifications' as 'THEOREM*/pending the O3.3 numeric residual [C-O33]', silently demoting (ii). The class of a verdict-bearing identification is stated three different ways across D3, the registry, and the paper text; a reader of the latest D3 entry gets the wrong class.

- EVIDENCE: docs/rde_nozzle_theorem_ledger.md:462-464 ('(i)-(iii) ... UNCHANGED (THEOREM*)') vs :513-522 ('identification (ii) is now THEOREM'); docs/claims_registry.yaml:404-416 (T-LEMA-ii class THEOREM, empty inherits); docs/rde_nozzle_P1_sections_5_7.md:170-173.

- SUGGESTED TEST: Correct the S19 sentence to '(i)/(iii) UNCHANGED (THEOREM*); (ii) already THEOREM per P-A1'' and split §5.3's sentence accordingly; extend test_claims_lint with a doc-vs-registry class-consistency check for spine IDs cited with an explicit class word within N characters.

- VERIFIER NOTE: docs/rde_nozzle_theorem_ledger.md:462-463, the LATEST dated entry (S19, 2026-08-06): 'Classes of (i)-(iii) therefore UNCHANGED (THEOREM*)' — directly contradicted by the same section's P-A1'-discharge paragraph (:519-520, 'Lemma A identification (ii) is now THEOREM within the irrotational homentropic scope (EOS-general)', 2026-07-16) and by the registry of record (claims_registry.yaml:404-415: T-LEMA-ii class THEOREM, inherits [], suffices_symbolic yes, carriers X-PA1/X-P2A1), while T-LEMA-i/iii are THEOREM* inheriting C-O33 (:391-402, :417-428). P1_sections_5_7.md:170-173 additionally lumps 'component identifications' as 'THEOREM*/pending the O3.3 numeric residual [C-O33]' without excepting (ii). Three inconsistent statements of a verdict-bearing class; the charitable reading ('UNCHANGED' = no class changed in S19) leaves the parenthetical '(THEOREM*)' wrong for (ii). Confirmed.

### [CONFIRMED | medium | rejector-weakness] ledger-coherence:coherence-grep-uncommitted

- FILE/LOC: docs/rde_nozzle_P1_skeleton.md — lines 40-49 and 379-382 (acceptance rule (c)); sections_5_7 §7.1 audit line, lines 327-329

- CLAIM OF RECORD: "enforced by coherence grep at every draft pass"; audit line: "Falsifier: any sentence violating the grep | Carrier: the coherence grep itself (acceptance rule (c))".

- FINDING: No committed coherence-grep artifact exists anywhere in the repo (searched tests/, validation/, scripts, all .py): the only executable doc lint is test_claims_lint (anchors/suite/schema/orphans), which does NOT implement the winner-as-hardware-verdict check. The 'coherence grep' is also the declared falsifier of five PAP-* registry entries (claims_registry.yaml lines 1243-1321). This is precisely the guard against the documented past drift risk (diagram winners read as hardware verdicts), and it exists only as narrative — a rejector that cannot fire mechanically, named as a Carrier in an audit line of record.

- EVIDENCE: grep -rn 'coherence' over tests/ and validation/*.py returns only test_cj_coherence (unrelated); docs/claims_registry.yaml:1243,1256,1269,1282,1321 cite 'coherence grep' as falsifier with no carrier entry; tests/test_claims_lint.py:1-64 shows its checks do not include the semantic grep.

- SUGGESTED TEST: Commit the grep as a runnable carrier (e.g., tests/test_doc_coherence.py: pattern list for hardware-verdict phrasings near §7 winner sentences, unweighted-(**) outside T3 mentions, unlabeled gamma-const objects) and register it; seed one violating sentence in-memory each run to prove it can reject.

- VERIFIER NOTE: No committed executable implements the coherence grep: grep over all repo .py files for 'coherence' returns only the unrelated CJ-coherence chain; grep for 'grep' in .py returns nothing; tests/test_claims_lint.py (read in full, checks (a)-(e): parse/references/suite-membership/schema/orphans) contains no winner-as-hardware-verdict or novelty-wording check. The grep exists only as a manual per-session procedure logged in PROGRESS notes (validation/PROGRESS_2026-07-16_fase1_P1.md:111-117 logs the pattern). Yet it is named as the Carrier in an audit line of record (P1_sections_5_7.md:327-329) and as the falsifier of PAP-* registry entries (claims_registry.yaml:1243,1256,1269,1282,1321,1611), all with carrier: [] — while the skeleton's own rule (P1_skeleton.md:34-36) demands 'an EXECUTABLE IN-REPO CARRIER' for every paper claim. The refutation (manual execution is logged) fails the repo's own rejector-that-can-fire bar; finding F1 of this batch is a live demonstration of the drift this guard was supposed to catch.

### [CONFIRMED | low | doc-code-mismatch] ledger-coherence:D25U-statement-four-constants

- FILE/LOC: docs/rde_nozzle_remaining_conditionals.md — §1, lines 35-41 (CLAIM clause) and line 84 (U4)

- CLAIM OF RECORD: "with ALL constants depending ONLY on (delta, L_x, C_geo, C_dat) — never on the individual solution" (and U4: "total constant explicit in (delta, L_x, C_geo, C_dat)").

- FINDING: The S15 amendment prepended into the same section (lines 28-35) declares this exact clause 'FALSE as written' without the duct-width floor h_min (unbounded acoustic bounce count with reflection factors > 1) and mandates the fifth constant 'from now on' — yet the CLAIM clause and the U4 architecture line were never edited and still assert the four-constant form. The canonical statement of the program's single shared conditional is false-as-written by its own doc, correct only via a marginal note; the registry (C-D25U) already carries five constants, so the doc lags the registry.

- EVIDENCE: docs/rde_nozzle_remaining_conditionals.md:28-35 ('Without it ... the uniform-constants claim below is FALSE as written'), :39-41 (unedited four-constant claim), :84 (U4 four-constant line); docs/claims_registry.yaml:129-141 (C-D25U statement with (delta, L_x, C_geo, C_dat, h_min)).

- SUGGESTED TEST: Edit the CLAIM and U4 lines to the five-constant form (keeping the amendment as historical note); add a grep-lint asserting no D2.5-U constant tuple appears without h_min outside dated-amendment context.

- VERIFIER NOTE: docs/rde_nozzle_remaining_conditionals.md: the CLAIM clause (:35-41) still reads 'ALL constants depending ONLY on (delta, L_x, C_geo, C_dat)' and U4 (:82-84) 'total constant explicit in (delta, L_x, C_geo, C_dat)', while the S15 amendment embedded in the same STATEMENT (:28-35) declares the four-constant claim 'FALSE as written' without h_min and mandates the fifth constant 'from now on'; claims_registry.yaml:132-133 (C-D25U) carries the five-constant form. Defect factually stands (canonical claim text never edited; doc lags registry). Severity reduced to low because the correction is not marginal: the amendment sits immediately before the CLAIM inside the statement block with an explicit 'FALSE as written', and the S16 status note directly under U4 (:85-92) states 'LIP_shocked explicit in the five constants' — no reading path reaches the stale tuples without passing the correction.

### [CONFIRMED | low | process] ledger-coherence:D3-52-heading-unverified-stale

- FILE/LOC: docs/rde_nozzle_theorem_ledger.md — §5.2 heading, lines 298-300

- CLAIM OF RECORD: Heading: "measured magnitude DOWNGRADED to UNVERIFIED-IN-REPO".

- FINDING: The body of the same section (line 309 onward) records 'MEASURED IN-REPO (2026-07-16, A0.3 closed: examples/gamma_cycle_probe.py + tests/test_gamma_probe.py + data/gamma_cycle_probe.json)' with the corrected numbers of record (−0.56%, −0.00028%). The heading reflects the pre-A0.3 state and was never updated; a heading-level reader concludes the gamma-channel magnitudes are unverified when they are the carrier-backed numbers D4 §1 instructs to 'cite only'.

- EVIDENCE: docs/rde_nozzle_theorem_ledger.md:298-300 (heading) vs :309-327 (MEASURED IN-REPO block with carriers and corrected numbers); docs/rde_nozzle_claims_verdict.md:45 (RESOLVED row pointing at D3 §5.2 as numbers of record).

- SUGGESTED TEST: Change the heading to 'measured magnitude RE-DERIVED IN-REPO (A0.3)'; no carrier change needed.

- VERIFIER NOTE: docs/rde_nozzle_theorem_ledger.md:298-299 heading still reads 'measured magnitude DOWNGRADED to UNVERIFIED-IN-REPO' while the body (:309-327) records 'MEASURED IN-REPO (2026-07-16, A0.3 closed: examples/gamma_cycle_probe.py + tests/test_gamma_probe.py + data/gamma_cycle_probe.json)' with the corrected numbers of record (-0.56%, -0.00028%, stale -1.9% STRUCK). D4 §1 (claims_verdict.md:45) designates D3 §5.2 as 'numbers of record now in D3 §5.2; cite only those'. The heading reflects the pre-A0.3 state; no refutation found (it is a status label, not a historical-trajectory title). Stale heading confirmed, low impact since the body and D4 both carry the correction.

### [CONFIRMED | low | process] ledger-coherence:C-O33-registry-statement-stale

- FILE/LOC: docs/claims_registry.yaml — C-O33 entry, lines 181-194

- CLAIM OF RECORD: "Term-by-term match of the closed-form multiplier components against the AD adjoint field on a TOC case (oracle O3.3 / P-A3) awaits the A1 engine."

- FINDING: The A1 engine exists and the pre-registered O3.3 campaign RAN (S19, carriers X-O32/X-O33B, both in the registry and inheriting C-O33), quantifying the residual (6.6e-02 design-class-dominated, mesh-independent) — and the S20 X-AKNO attempt ended certifiability-limited with C-O33 'neither discharged nor falsified'. The conditional's own statement field still says it awaits an engine that has since produced the numbers. Class SCHEMA is correct; the statement text is stale and misdescribes what remains open (design-class residual + margin-constrained reformulation, not engine availability).

- EVIDENCE: docs/claims_registry.yaml:181-194 (C-O33 'awaits the A1 engine') vs :1205-1217 (X-O33B executed results) and :1218-1231 (X-AKNO 'C-O33 neither discharged nor falsified'); docs/rde_nozzle_theorem_ledger.md:447-464 ('[C-O33] stays OPEN, residual quantified, dominant term identified as the DESIGN CLASS').

- SUGGESTED TEST: Update the C-O33 statement to the S19/S20 residual formulation; add a lint rule that a conditional inherited by an executed carrier whose scope declares results must not contain 'awaits' language referencing that carrier's prerequisite.

- VERIFIER NOTE: docs/claims_registry.yaml:185 (C-O33 statement): '...awaits the A1 engine.' — verified stale against the same registry's executed carriers inheriting C-O33: X-O33B (:1205-1216, S19 bench executed on the S18 engine, f2 constant to 9.5e-03/8.0e-03, R6 primary criterion passed) and X-AKNO (:1218-1229, S20 attempt 'C-O33 neither discharged nor falsified', margin-constrained reformulation adjudicated), plus D3:447-464 ('[C-O33] stays OPEN, residual quantified, dominant term identified as the DESIGN CLASS'). Class SCHEMA remains correct; the statement text misdescribes what is open (design-class residual, not engine availability). The registry's own convention updates statement fields with session status (C-D25U statement, :133, carries S15/S16 status inline), so this is staleness against its own practice. Confirmed, low.

### [CONFIRMED | low | process] ledger-coherence:d3-claims-field-misnomer

- FILE/LOC: data/gamma_cycle_probe.json — top-level field 'd3_claims' (written by examples/gamma_cycle_probe.py:154-155)

- CLAIM OF RECORD: Persisted data of record contains d3_claims = {gamma_s: '1.154 -> 1.210', eps_shift: '-1.9%', isp_penalty: '-0.001%'}.

- FINDING: The field named 'd3_claims' in the data of record holds the STRUCK stale internal-note numbers (−1.9%, −0.001%) — exactly the values D3 §5.2 and D4 §1 declare corrected/struck — not D3's numbers of record (−0.56%, −0.00028%). The test correctly rejects the stale class, but the persisted label invites mis-citation: anything harvesting 'd3_claims' from the JSON would quote the rejected numbers as D3's. Naming defect only; the computed fields (eps_shift −0.005559, isp_penalty 2.83e-6) match the record.

- EVIDENCE: data/gamma_cycle_probe.json d3_claims block (read); examples/gamma_cycle_probe.py:154-155 writes it; tests/test_gamma_probe.py:102-105 checks the corrected values against the stale ones; docs/rde_nozzle_theorem_ledger.md:315-321 ('the stale note numbers are hereby CORRECTED ... STRUCK').

- SUGGESTED TEST: Rename the field to 'stale_note_claims_struck' (with a schema-bump note) and add a golden-test assertion that no field named *claims* in data of record carries struck values without a 'struck'/'stale' marker in its key.

- VERIFIER NOTE: data/gamma_cycle_probe.json (end of file) contains 'd3_claims': {gamma_s: '1.154 -> 1.210', eps_shift: '-1.9%', isp_penalty: '-0.001%'}, written by examples/gamma_cycle_probe.py:154-155; the -1.9% and -0.001% are exactly the values D3 §5.2 (:314-321) declares STRUCK/corrected and D4 §1 (:45) says never to cite. tests/test_gamma_probe.py:99-107 gates the corrected values (G3 rows explicitly 'strikes the stale -1.9%'), so the rejector culture holds; the residual defect is the persisted label: after D3's correction, a key named 'd3_claims' in data of record attributes struck numbers to D3 with no struck/stale marker. Best refutation (historical provenance: these were the claims-under-test at A0.3 time) explains the origin but does not cure the present mis-citation hazard. Confirmed as a naming/process defect only; computed fields match the record (eps_shift -0.005559, isp_penalty 2.83e-6).


## test-suite (candidates dropped by finder cap: 5)

### [CONFIRMED | high | rejector-weakness] test-suite:ondemand-carrier-exclusion

- FILE/LOC: tests/test_claims_lint.py — tests/test_claims_lint.py:59,184-185 (ONDEMAND marker + bypass) with docs/claims_registry.yaml:723,736,749,762,1143,1156,1169,1182,1195,1208,1221 and tests/run_all.py:54-75

- CLAIM OF RECORD: Claims-lint docstring (c): "every kind: carrier entry is IN THE SUITE"; run_all.py presents itself as "the package coherence & non-regression suite" whose full run is the closure verdict of record.

- FINDING: The entire verdict-bearing chain of the current nozzle program — X-A1IM, X-THC1, X-SCANM, X-LSG0, X-TOCV (KKT 7.745e-02, J* 2.7761688e+07, oracle 91/91), X-O32, X-O33B (the O3.3 bench PASS), X-AKNO, plus X-G0/X-G0AX/X-GENOXC — is outside BOTH run_all tiers via the on-demand exemption. The exemption is a pure substring test (`ONDEMAND in e['scope']`, line 184-185): the lint verifies no environment gate, no staleness date, no exit-0 re-execution, so its suite-membership rejector structurally CANNOT fire for exactly the 11 carriers most exposed to drift (jax/gfortran env). Every "PASS of record" number embedded in those registry scopes rests on a single historical run that nothing re-executes; a regression (or a wrong original run) is invisible to every closure verdict the suite can emit.

- EVIDENCE: test_claims_lint.py:59 `ONDEMAND = 'on-demand carrier (env: '`; :184-185 `if ONDEMAND in e['scope']: pass  # declared exemption, of record`. Registry lines 1140-1229 (X-THC1/X-SCANM/X-LSG0/X-TOCV/X-O32/X-O33B/X-AKNO) all carry the marker and carry numbers-of-record (e.g. line 1182: "KKT 7.745e-02 ... ORACLE 91/91") in scope strings. run_all.py FAST/RIGOR/SLOW lists (54-75) contain none of these scripts.

- SUGGESTED TEST: Add an env-conditional CI tier: when `import jax` succeeds (and the GENO binary is absent-tolerant), run each on-demand carrier as a subprocess requiring exit 0 + final VERDICT: PASS; in the lint, replace the substring bypass with a typed field (ondemand_env) plus a staleness check that FAILS when the recorded PASS date predates the last commit touching the carrier script.

- VERIFIER NOTE: All factual elements verified. tests/test_claims_lint.py:59 defines ONDEMAND = 'on-demand carrier (env: ' and :184-185 bypass suite-membership on a bare substring match in scope, with no env gate, no staleness check, no re-execution. Grep of docs/claims_registry.yaml confirms the marker on exactly the 11 verdict-bearing carriers (lines 723, 736, 749, 762 = X-G0/X-G0AX/X-GENOXC/X-A1IM; 1143, 1156, 1169, 1182, 1195, 1208, 1221 = X-THC1/X-SCANM/X-LSG0/X-TOCV/X-O32/X-O33B/X-AKNO), whose scope strings carry the program's numbers of record (line 1182: KKT 7.745e-02, J* 2.7761688e+07, ORACLE 91/91; line 1208: the O3.3 bench values). run_all.py FAST/RIGOR/SLOW lists (:54-75) contain none of these scripts, and test_rigor_carriers.py CARRIERS (:31-42) covers a disjoint set; test_examples.py runs only examples/. Best refutation attempted and rejected: the exemption IS declared (lint docstring :15-19 'Declared exemption (syntax-enforced)' and each scope says 'outside CI tiers by declaration'), so nothing is hidden — but the defect as stated stands: no automated channel exists that can fire on a regression (even a syntax error) in any of these scripts, any future entry can self-exempt by scope wording alone, and the recorded PASS numbers rest on single manual session runs with no staleness link to the carrier files. Against culture rule (1) (a rejector that CAN reject) this is a structural inability of every closure verdict the suite emits to see the current program's entire verdict chain — severity high.

### [CONFIRMED | medium | underived-constant] test-suite:numeric-lint-scope-hole

- FILE/LOC: tests/test_numeric_lint.py — tests/test_numeric_lint.py:36-40 (SCOPE_DIR = src/ only; TRIVIAL_FLOAT); magic tolerances at tests/test_bell_optimality.py:51,69,71-72 and tests/test_axial_bound.py:30

- CLAIM OF RECORD: Rigor culture of record: "every tolerance DERIVED, never magic"; run_all.py:21-22 sells (vii) as "the no-magic-number invariant".

- FINDING: The no-magic-number invariant is enforced ONLY on src/**/*.py. tests/ and validation/ — where the verdict-bearing thresholds actually live — are never scanned. Concretely: test_bell_optimality gates T1a stationarity with `abs(d0) <= 0.02 * max(dm, -dp)` (0.02 underived; loosening it 10x would still print PASS), the Euler-lemma check with `worst < 1e-5`, dominance probes at hand-picked deltas (-3.0..3.0) clamped at 1.05; test_axial_bound hard-codes march resolution vstep=500, vmax=20.0 that set the accuracy of the "independent" bound. Additionally, inside src/ the blanket TRIVIAL_FLOAT set {100, 1000, 1e6} exempts any physics literal that happens to equal a "display scaler" (e.g. 1e6 Pa), and exempt_functions accepts arbitrary fnmatch patterns with no breadth guard.

- EVIDENCE: test_numeric_lint.py:36 `SCOPE_DIR = os.path.join(ROOT, 'src')`; :40 `TRIVIAL_FLOAT = {0.25, 0.5, 0.75, 100.0, 1000.0, 1e6}`; :65 fnmatch on exempt_functions. test_bell_optimality.py:69 `abs(d0) <= 0.02 * max(dm, -dp)`; :51 `worst < 1e-5`; :71-72 delta set. test_axial_bound.py:30 `vstep=500, vmax=20.0`.

- SUGGESTED TEST: Extend the lint scope to tests/**/*.py and validation/**/*.py with their own allowlist sections (class + one-line derivation note per threshold), and remove 100/1000/1e6 from TRIVIAL_FLOAT, forcing per-site classification as display scalers.

- VERIFIER NOTE: Verified: test_numeric_lint.py:36 SCOPE_DIR = src/ only; :40 TRIVIAL_FLOAT = {0.25, 0.5, 0.75, 100.0, 1000.0, 1e6} blanket-exempts any src/ literal equal to a 'display scaler' (a bare 1e6 Pa would pass unlisted); :65 exempt_functions accepted via fnmatch with no breadth guard. tests/ and validation/ are never scanned, and that is where verdict thresholds live: test_bell_optimality.py:69 gates T1a stationarity with the underived factor 0.02 (`abs(d0) <= 0.02 * max(dm, -dp)`), :52 `worst < 1e-5`, :71-72 hand-picked delta set with 1.05 clamp; grep of validation/bell_optimality_proof.md finds NO derivation of 0.02 (no '0.02'/'derived'/'threshold' anywhere), so the constant is genuinely magic in a verdict-bearing test. Partial refutations noted and absorbed: the lint's own docstring (:19 'Scope: src/**/*.py') and run_all.py:21 honestly scope the invariant to src/, so there is no doc-vs-code overclaim; and test_axial_bound's 2e-3 comes from TOL with a documented cross-run rationale (docstring :14-15), weakening that sub-example (vstep=500/vmax=20.0 at :30 remain underived resolution knobs). The surviving defect is against the repo-wide culture rule 'every tolerance DERIVED, never magic', which the enforcement mechanism structurally cannot police where it matters most. Medium.

### [CONFIRMED | medium | rejector-weakness] test-suite:golden-tables-verdict-filter

- FILE/LOC: tests/test_golden.py — tests/test_golden.py:86-92

- CLAIM OF RECORD: Docstring line 11: "tables (V&V verdicts) -> 8/8 PASS" — the shipped tables module's V&V is fully gated by the golden group.

- FINDING: The 8/8 verdict count is reconstructed by filtering stdout lines whose first token starts with a HARDCODED ID tuple ('1','2','3a','3b','3c','4','5','6') and requiring exactly len(verd)==8. Any NEW verdict row added to tables.main() numbered 7, 8, 9 (or renamed) is silently excluded from the filter: a future "7 ... FAIL" line leaves npass==8 and len(verd)==8 and the golden group still prints PASS. The rejector only fires in the over-count direction (a stray line starting with '1' such as an '18/18' summary would push len(verd) to 9 and fail safe); the under-count direction — the one a wrong regeneration would exploit — cannot fire.

- EVIDENCE: test_golden.py:87-89 `ln.split()[0].rstrip('*').replace('_','').startswith(('1','2','3a','3b','3c','4','5','6'))`; :91 `npass == 8 and len(verd) == 8`. Same self-report pattern for the other regenerated goldens: :75-78 accepts the module's own printed "18/18 rows PASS", :99-100 accepts the printed "0.00%" string.

- SUGGESTED TEST: Have tables.main() emit a machine-readable terminal line (e.g. "VERDICT ROWS: n_pass/n_total") and assert n_pass == n_total AND that the ID set equals a checked-in expected list, so an added or renamed FAIL row is rejected.

- VERIFIER NOTE: Verified at test_golden.py:86-92: verd filters stdout lines whose first token starts with the hardcoded tuple ('1','2','3a','3b','3c','4','5','6') and the check requires npass == 8 and len(verd) == 8, so a future verdict row keyed '7...' or '8...' printing FAIL is excluded from verd and the group still passes — the under-count rejector cannot fire. The strongest refutation FAILED: src/thrust/tables.py:300 does return `0 if all(v[0] in ('PASS','PASS*') ...) else 1`, i.e. a machine-readable self-gate exists, but test_golden.py:83-84 calls `tables.main()` bare inside redirect_stdout and DISCARDS the return value; and test_examples.py (slow tier, read in full) runs only the seven examples/ scripts, never tables, so no other suite path checks that exit status. Same self-report pattern confirmed at :75-78 (accepts the module's own '18/18 rows PASS' string) and :99-100 ('0.00%' string). The one-line fix (use main()'s return) existing unused in the same call makes this a clean rejector-weakness. Medium.

### [CONFIRMED | medium | rejector-weakness] test-suite:t3-lattice-flag-readback

- FILE/LOC: tests/test_bell_optimality.py — tests/test_bell_optimality.py:94-104

- CLAIM OF RECORD: run_all.py:19-20 and the module docstring: group (vi) = "executable eps-optimality proofs"; test printout line 102: "T3 phi: strict lattice certificate, all 36 optima".

- FINDING: The T3 "strict lattice certificate" check is a readback of build-time booleans persisted in data/st_nozzle_opt.json: `okc &= bool(m['lattice_certified']) and m['lattice_gap'] > 0`. The certificate itself is never re-derived by the test; only unimodality is recomputed from the persisted grid (line 97-101, explicitly commented as the reproducible part). A wrong regeneration of st_nozzle_opt.json in which the build's certificate logic is broken (always writing cert=True, or writing a gap computed against the wrong neighbor set) is blessed silently — the golden-update discipline hole the audit lens names: the test gates the artifact's self-assessment, not the proof, while its printed label claims a certificate.

- EVIDENCE: test_bell_optimality.py:96 `okc &= bool(m['lattice_certified']) and m['lattice_gap'] > 0`; comment at :97 "re-derive unimodality from the PERSISTED grid (reproducible)" — the lattice certificate has no such re-derivation. Producer at src/thrust/stechmann_nozzle.py:536-537 writes the flags the test reads.

- SUGGESTED TEST: Re-derive the lattice certificate in-test from the persisted grid: recompute the objective at the certified argmax and its lattice neighbors and assert the stored lattice_gap equals the recomputed gap within a derived floor; seed one corrupted in-memory row (cert=True with a negative recomputed gap) and require rejection.

- VERIFIER NOTE: Verified: test_bell_optimality.py:94 `okc &= bool(m['lattice_certified']) and m['lattice_gap'] > 0` is a pure readback of build-time flags; the producer computes them at src/thrust/stechmann_nozzle.py:525-527 (`cert = interior and all(ev(p) < F[cur] ...)`, `gap = F[cur] - max(...)`) and persists them at :535-539 alongside the full evaluated grid. The test re-derives ONLY unimodality from the persisted grid (:97-101, comment explicitly marks that as 'the reproducible part'); the certificate and gap are never recomputed even though the persisted grid contains every evaluated (phi, F) pair and the recomputation is trivial. Refutation attempted: the oks unimodality re-derivation does narrow the corruption space (a grid-level corruption or non-unimodal regeneration is caught), and cert=False is caught by okc — but the named failure mode stands: a regenerated artifact whose certifier logic is broken (always-True cert, or gap computed against wrong neighbors) passes okc while the printed label at :102 claims 'strict lattice certificate, all 36 optima' inside run_all group (vi) 'executable eps-optimality proofs'. The check gates the artifact's self-assessment, not the proof. Medium.

### [REFUTED | medium | correlated-error] test-suite:axial-bound-correlated-routes

- FILE/LOC: tests/test_axial_bound.py — tests/test_axial_bound.py:1-16 (docstring claim), 30-62 (sv_march_bound), 67-74

- REFUTED BY VERIFIER: The factual substrate is accurate (test_axial_bound.py:36-41 rebuilds the state from the same cj dict the live route consumes at :71; both routes use cantera + sdtoolbox soundspeed_eq — sv_march_bound at :33-34/:48 and axial_calc via src/thrust/sk_models.py:33-36) but the claimed defect — that the declaration overstates independence — does not survive line-level reading. The docstring (:3-8) says 'recomputed here by a second, independent algorithm — the official SDT demo_PrandtlMeyerDetn / demo_quasi1d_eq march': independence is explicitly scoped to the ALGORITHM, and the second route is explicitly named as coming from the SAME SDT toolbox, so no reader is told the thermo library or the CJ input is independently checked. The determinism check (:11-12) is declared separately, and the shared CJ input is covered by the suite's own independent-solver leg (run_all.py:13-14, test_cj_coherence 'independent solvers within 2e-3'), which the finder concedes. The residual (no library-independent leg on the equilibrium sound-speed channel) is a coverage limitation nowhere claimed to be covered — a hardening suggestion, not a defect against a claim of record. Default-to-refuted applies.

### [CONFIRMED | low | rejector-weakness] test-suite:orphan-lint-prefix-and-scope

- FILE/LOC: tests/test_claims_lint.py — tests/test_claims_lint.py:60 (ID_RX), 200-211 (check_orphans), 186-193 (suite-membership substring)

- CLAIM OF RECORD: Docstring (e): "every bracketed registry-shaped ID cited in docs/*.md ... must exist in the registry"; (c): tests modules "must be registered in run_all.py".

- FINDING: Three narrowings weaken the lint below its stated guarantee: (1) ID_RX whitelists prefixes (T|D|C|S|J|X|O|DIR|PAP)- only, so spine IDs of other families (e.g. the U1-U4 conditional bricks cited in docs/rde_nozzle_remaining_conditionals.md, or a cited [L-DoD] for the S17-promoted lemma) can dangle forever unflagged; (2) check_orphans walks only top-level docs/*.md — validation/*.md dossiers and panel docs (e.g. validation/PANEL_topology_census_2026-07-22.md) are never scanned; (3) suite membership is `"'%s'" % mod in run_src` — any quoted module name anywhere in run_all.py's text (including a comment) satisfies registration.

- EVIDENCE: test_claims_lint.py:60 `ID_RX = re.compile(r'\[((?:T|D|C|S|J|X|O|DIR|PAP)-...')`; :203-205 `os.listdir(DOCS_DIR)` non-recursive, docs/ only; :187-189 substring registration check. docs/rde_nozzle_remaining_conditionals.md:56,67,85 shows U-family spine tags outside the prefix set.

- SUGGESTED TEST: Derive ID_RX from the registry's actual ID population plus a catch-all bracketed-tag pattern with a declared ignore-list; extend the scan to validation/**/*.md; check registration against the parsed FAST/RIGOR/SLOW lists (ast) instead of raw source substring.

- VERIFIER NOTE: Confirmed with CORRECTED evidence — the finder's own headline example is wrong, but the mechanism is real and live. (1) The finder's cited U-family tags in docs/rde_nozzle_remaining_conditionals.md are actually [S-D25U-U1], [T-U2RG], [S-D25U-U34], [X-U3BD] (lines 28, 57, 67, 86, 129, 236) — ALL match ID_RX's S-/T-/X- prefixes, so that example refutes itself. However, the registry ID population itself exceeds the whitelist: PAN-S14 is a registry entry (claims_registry.yaml:1601) cited as [PAN-S14] in docs/rde_nozzle_MASTER.md:1428 and docs/rde_nozzle_panel_2026-07-22.md:4,299 — the PAN- prefix is outside ID_RX (test_claims_lint.py:60), so those citations are structurally invisible to check_orphans and a misspelled [PAN-*] can dangle forever; likewise the S17-minted lemma L-DoD (named in X-AKNO scope, registry:1221) has no coverable prefix. (2) check_orphans walks os.listdir(DOCS_DIR) non-recursively (:203-205) — matches the docstring's letter ('docs/*.md') but leaves validation/*.md dossiers unguarded. (3) Suite registration is the raw substring `"'%s'" % mod in run_src` (:187-189) — any quoted test module name anywhere in run_all.py's text satisfies it. Docstring (e) claims 'every bracketed registry-shaped ID' where 'registry-shaped' is silently self-defined by the regex, which is narrower than the registry's actual IDs — the guarantee is weaker than a reader of the registry would infer. Low.

### [REFUTED | low | rejector-weakness] test-suite:rejector-demos-untargeted

- FILE/LOC: tests/test_claims_lint.py — tests/test_claims_lint.py:231-256 (seeded_rejector_demo); tests/test_rigor_carriers.py:47-62 (harness has no negative control)

- REFUTED BY VERIFIER: The untargeted-acceptance concern collapses under the run structure: run() (test_claims_lint.py:259-284) computes the REAL registry's violations first and PASS requires `not violations` (:280), so in any passing run the baseline is zero-violation and `rejected = bool(check(mutated, suite))` (:252) can only be True via the seeded mutation; each of the three mutations touches exactly one rule's precondition (strip inherits on a THEOREM* -> only rule :157; strip carrier on a symbolic-sufficient theorem -> only rule :159-161; dangling anchor on entry 0 = D-P, kind definition, registry:49, so no membership-rule crosstalk -> only rule :167-168), making the bool effectively targeted; deleting the seeded rule from check() makes the demo print ACCEPTED (FAIL). Coverage: the docstring (:28-32) promises exactly three seeded violations and delivers exactly those three — no overclaim to demonstrate 'each rule family'. On the harness: test_rigor_carriers.py's docstring declares its acceptance contract explicitly (:13-15 'exit code 0 AND the literal terminal line') and disclaims proof status (:16-18 'this module is their CI harness, not their proof'); its rejection channels (missing script, nonzero exit, missing verdict line, :50-58) CAN fire, and the any()-over-stdout hole is future-hypothetical — the finder itself verified current carriers tie exit code to the verdict. What remains are hardening suggestions, not standing defects with a claim they contradict.

### [REFUTED | low | doc-code-mismatch] test-suite:stale-suite-bookkeeping

- FILE/LOC: tests/run_all.py — tests/run_all.py:12-40 (docstring: no group (xvii); (xv)/(xvi) ordering differs from the FAST list at :69-70); docs/claims_registry.yaml:775 (X-GRP01 scope "suite-green of record 13/13 (S8-op)")

- REFUTED BY VERIFIER: Both cited facts are real but neither contradicts a claim of record. (1) The (xvii) gap: run_all.py's docstring lists (xiv), (xviii), (xv), (xvi) with no (xvii) (lines 32-39), and the SAME labels appear verbatim in the FAST/RIGOR tier entries (:69-70, :71-74) — docstring and code are mutually consistent, and no document asserts that group numerals are consecutive; a numbering gap that is consistently propagated is a cosmetic oddity, not a doc-vs-code mismatch, and the runner's verdict counts groups from the lists (:96-101), not from numerals, so a dropped group cannot hide in the hole. (2) X-GRP01's '13/13' (claims_registry.yaml:775) is explicitly vintage-stamped '(S8-op)' in the same scope string — it is a dated record of the S8 suite-green event, exactly the registry's declared convention for historical PASSes (the same pattern used throughout, e.g. 'PASS 2026-08-06 S17' on X-THC1), not a current-count claim any run is supposed to reproduce; the entry's statement field concerns only group (i) itself. The suggested group-count self-check is a reasonable hardening, but no line-level defect stands after refutation.
