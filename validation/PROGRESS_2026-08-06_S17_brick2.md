# S17 — Total-order session log (2026-08-06; BRICK-2 RE-ADJUDICATION
# + BRICK 2 KICKOFF — variational TOC on the assembled march)

Session: S17, per the S16 step-8 handoff and PROGRESS NEXT-1 of
2026-08-06. Mandate (user order, this session's header): (1) S10+
opening; (2) pre-execution GATE; (3) RE-ADJUDICATION of the brick-2
deferral at the S15 waiver terms (S15 log step 2: deferral #4, RK-A
cited, compensating controls i-iii; foundations campaign COMPLETE in
S15+S16 — re-adjudication DUE NOW; expected verdict: BRICK 2 STARTS;
any further deferral requires a NEW explicit user order with RK-A
re-weighed); then BRICK 2 (D6 item 9, F2/A1): variational TOC on the
[X-A1IM] engine — thrust objective + constraints {eps, L, lip},
transversality (**')/corner reached VIA the GRADIENT dJ/dSigma
(reverse-AD of the march = Lemma B, O3.1 certified) inside TR-SQP;
NEVER a hard-coded outer loop (GENO type-2 Mrao/eps loop = cross-code
reference only). Oracle: GENO tocnoz contour (1e-10) + X-GENOXC
regression; derived tolerances (Richardson). KICKOFF DUTIES BINDING
(D6 item 9, BEFORE optimizing): (a) derived loop-speed threshold
(clean-host protocol) with the G0 falsifier armed; (b) scan/vmap
column architecture; (c) RK-G policy (fixed topology in trust region
+ re-record on acceptance + kink detection, coherent with
[S-D25U-U34] c2 and [S-LBML] LB-c2); (d) THERMOTAB C^1 duty
(Hermite-monotone interpolant + invariants cp = dh/dT, s0' = cp/T
within derived floors + rejector) + S16 ADDITION: EOS G > 0 audit on
the state box (ledger channel c4). Then O3.3 unlock with the
PRE-REGISTERED protocol (P2_outline §5). Bench items (B1 interval
s_L, C-XBVP(b), g-scan B3, GBE global concavity) ONLY if budget
survives the brick; census amendments (D2.1/D2.6) NOT executed here
(census-lemma session, queued AFTER brick 2).

| # | Step |
|---|------|
| 1 | SESSION OPEN: HEAD = 324e167 (S16 closed at total order 1-8), branch rde-nozzle-program, no foreign commits, single-session rule verified (S16 terminated at step 8). Tree state declared: known untracked unchanged (GENO/, literature/, ADR + census advisories, user mailmap artifacts, stray user files); data/q_mapping.{json,md} found datestamp-regenerated (2-line diff, content identical — the S10 precedent) and RESTORED via git checkout, declared here. S10+ opening executed in full: project memory (MANDATORY three read: s15-foundations-campaign — campaign COMPLETE both tranches + do-not-redo list honored (U1-U4, U2, T2 27/27, substrate, G-B, S-LBML, ledger p1+p2 all DONE, none re-executed); topology-census-pins — census pins ALL DECIDED 2026-08-02, census-lemma session queued AFTER brick 2, advisory doc to be read before touching topology/sector/BC-branch work; s14-panel-verdict-canonical-loop — 16/16 verdicts not re-litigated, canonical workflow form if agents are used; plus python-env-cantera for the live env); L0 = SCAFFOLD §1 (six layers + registry; anchor = (P) of D2.6); claims_registry.yaml lint-verified live this session: 117 entries parsed, 0 violations, 3 rejectors REJECTED (PASS) — matches the S16 closure count; D6 read (item 9 = the binding kickoff duties + waiver annotation of record; item 12 acquisitions; RK-A text); PROGRESS ORA (S16 block) + NEXT (NEXT-1 = this re-adjudication) + BLOCCATO (user locks unchanged); S16 log read complete (1-8); S15 log step 2 (the waiver terms being re-adjudicated) read verbatim. TERMS OF REFERENCE BOUND: anchor (P) of D2.6; winner = closures NEVER hardware; novelty query-bounded; the 16 S14 verdicts + census §7pin pins + 27/27 T2-S15 + S16 adjudications (ledger p2, L4-default, U3-H1, S-GBE) NOT re-litigated; L-X-H 2022 method attribution IN QUARANTINE (never "MOC" for that paper until page-verified); no numbers without committed carrier + rejector; R4 same session; agents = verbatim quotes; multi-agent only canonical form. USER LOCKS untouched and not blocking: ADR + census ratifications; G5 send (time-boxed); literature uploads; SDP solver decision; GV email; RDE dataset; preprint decision (RK-A — step 3 re-weighs and re-presents it). |
| 2 | GATE (pre-execution, standing directive gate-pre-esecuzione) — (A) PLAN ADHERENCE: step 3 = the re-adjudication BOUND by the S15 waiver itself (S15 log step 2, compensating control iii: "the deferral is RE-ADJUDICATED at the end of the foundations campaign") and by PROGRESS NEXT-1 of record; campaign end VERIFIED reached (s15-foundations-campaign memory + S16 log step 8: the named [RIGOR/A] queue is EXHAUSTED — U1-U4, a-contraction, ledger p1+p2, G-B ergodic, S-LBML all of record); the brick-2 work itself = D6 item 9 (A1 brick 2, F2/A1 in the phase plan), tags [F2/A1][PIANO/S17]; kickoff duties (a)-(d) = the same item 9 bundle, S16 c4/G>0 addition included (ledger §4.2 c4 channel joins the THERMOTAB duty); O3.3 unlock = D6 item 9 "UNLOCKS O3.3 = P-2's numeric half". Bench items and census amendments EXCLUDED from this session's scope unless budget survives (bench) / unconditionally (census — different session by decided sequencing). (B) UPSTREAM RIGOR AUDIT (the points the task rests on, re-read this session): (i) [X-A1IM] carrier present (validation/a1_ideal_march_jax.py, 1130 lines) with the S11 verdict of record (62/62 contour vs GENO in derived Richardson band, O3.1 whole-march 2.7e-10 vs tol 5.1e-8, unit-consistent Newton certification, negative controls) — the march the brick differentiates EXISTS and is certified; (ii) O3.3 protocol PRE-REGISTERED and intact (P2_outline §5: oracle coordinates = L-P 2025 compatibility residuals (30)/(31) + Prop. A3 f2 drift, NEVER (32)-(34) off uniform patches; norms lip/corner/sonic-excluded; O3.3 = PRIMARY kill criterion; capturing control per X1) — compensating control (i) of the waiver held; (iii) Lemma B chain: T-LEMB exact transpose + [S-LBML] now written (S16) with LB-c1/LB-c2 declared — the gradient the brick uses has its mesh-limit statement; (iv) RK-G policy sources: [S-D25U-U34] discovery D2 (within-stratum estimate, c2) + LB-c2 (fixed march topology) — the duty-(c) policy must cite exactly these; (v) THERMOTAB: DIR-THERMOTAB pin (S11) declares accuracy floors only; the S14 two-lens finding stands (live carrier interpolates cp INDEPENDENTLY of h via piecewise-linear jnp.interp => cp != dh/dT between knots, Jacobian jumps at knots) — duty (d) is the fix, plus the S16 c4 channel (G > 0 on the state box, BZT-class violation named); (vi) DIR-G0: loop-speed falsifier ARMED but UNQUANTIFIED as written (S15 T2 fix d1: "derived threshold = D6 item-9 kickoff duty") — duty (a) closes exactly that; (vii) (P) of D2.6 = the anchor: the brick's output is (S*, delta)-class machinery, constraint set {eps, L, lip} declared at instance, never hardware claims; (viii) no QUARANTINE touches the brick (L-X-H 2022 is literature-side; GENO cross-code reference is code, not that paper). Suite state at open: lint (xv) PASS live (117, rejectors fire); full-suite state inherited from S16 closure 15/15 --fast 91 s. (C) VERDICT: gate pre-esecuzione PASS — proceed to step 3 (re-adjudication) before ANY brick work. |
| 3 | RE-ADJUDICATION OF THE BRICK-2 DEFERRAL (the act the S15 waiver bound to campaign end — due NOW, executed as its own numbered step). TERMS RE-READ VERBATIM (S15 log step 2): deferral #4 exercised by explicit user order citing RK-A, with compensating controls (i) O3.3 protocol stays pre-registered, (ii) preprint decision stays armed with the user, (iii) re-adjudication at the end of the foundations campaign. FINDINGS AGAINST THE TERMS: (1) CAMPAIGN END REACHED — the S15+S16 named [RIGOR/A] queue is EXHAUSTED of record (S16 log step 8; memory s15-foundations-campaign): U1-U4 written (C-D25U -a complete at class level, U3-H1 minted), T-SLRW, Cauchy->BVP transfer, X-IVXC substrate, a-contraction route adopted-as-named, ledger passes 1+2 (29 rows, zero unadjudicated), S-GBE, S-LBML. (2) COMPENSATING CONTROLS HELD: (i) O3.3 protocol verified intact this session (P2_outline §5, gate step 2(ii)); (ii) preprint decision untouched, still with the user; (iii) this step IS control (iii). (3) THE DEFERRAL PAID: the brick lands RICHER than at S11 — corrected O3.3 bench coordinates, pre-registered norms, U1-U4/S-LBML now give the gradient its stability + mesh-limit statements, RK-G policy has named sources (c2, LB-c2), THERMOTAB C^1 defect diagnosed with its fix specified, U3-H1 gives the front-solve a named conditioning monitor (ledger §4.2 channel). (4) NO GATE FAILURE is on record that would block the start (G1 oracles certified; G0 decided S10; the loop-speed falsifier is armed-unquantified and duty (a) quantifies it AT kickoff — it is a kickoff duty by D6's own text, not a precondition). VERDICT OF RECORD: the waiver's deferral #4 EXPIRES — **BRICK 2 STARTS THIS SESSION** with the D6 item-9 kickoff duties (a)-(d) binding BEFORE optimization, plus the S16 c4/G>0 EOS-audit addition to duty (d). Any FURTHER deferral would require a NEW explicit user order with RK-A re-weighed (rule restated, unchanged). RK-A RE-WEIGHED (duty of this step, decision re-presented to the user at closure): risk INCREASED during the waiver window — four user-ordered deferrals since S11, the characteristic-compatibility bank (Peter-Desideri 2022, Ancourt 2023, L-P 2023) now mandatory-cite, Lozano-Ponsin 2025 published the 2-D adjoint bank, Breitkopf-Ulbrich arXiv:2509.22076 active on the -c front — the mitigation of record ("fast-track P-2") RESUMES with this verdict since brick 2 unlocks O3.3 = P-2's numeric half; the PREPRINT-ARMING decision remains a USER lock and is re-presented at R3 closure with this re-weigh attached (recommendation recorded: once O3.3 numbers land, the scoop-window argument for arming the arXiv trigger strengthens materially; the decision itself stays with the user). R4 executed this step: D6 item 9 dated re-adjudication annotation; PROGRESS NEXT-1 marked executed with pointer to this step. |
| 4 | DUTY (c) [F2/A1] RK-G POLICY OF RECORD WRITTEN — new doc docs/rde_nozzle_brick2_kickoff.md (§1) + registry [DIR-RKG] (directive, PRACTICE; 118 entries, lint PASS after the lint correctly REJECTED a missing proof field + three not-yet-minted carrier IDs cited in brackets — fixed by minting-with-artifact discipline: future carrier IDs cited unbracketed until their commit). Policy content of record: P1 fixed topology in trust region (frozen Sched = the march DAG; gradients exact for the frozen DAG by T-LEMB and stratum-valid by S-D25U-U34 D2 + S-LBML LB-c2); P2 re-record on acceptance (decision-vector change = logged re-record event; TR model rebuilt; events = the LB-c2 statement boundaries; Verdict MUST report re-record count); P3 four kink monitors (replay fidelity vs Newton floor; per-cell certification on trial replays; decision-flip probe on stall with bisect-or-accept-with-re-record, no silent crossing; model/actual kink at small radius); P4 certification gate on acceptance (uncertified final iterate = not a result). THERMOTAB note of record: duty (d)'s C^1 closure DISSOLVES the "node crossings under one-sided policy" bookkeeping — knot crossings are not topology events once the closure is C^1 (the reason d precedes optimization). The kickoff doc also carries the NORMATIVE specs for duties (a)/(b)/(d) (threshold derivation T1 = grad/solve <= 4 from the cheap-gradient theorem constant + T2 = whole-optimization cost <= K_prac x GENO type-2 outer-loop cost with K_prac = 4 reused from the repo's two-level safety convention; scan-column architecture with equivalence-regression rejector; Hermite-monotone closure with structural cp = dh/dT + G>0 audit) — numbers land ONLY with the carriers (R5). |
| 5 | DUTY (d) [F2/A1] THERMOTAB C^1 EXECUTED — new carrier validation/thermotab_c1_jax.py [X-THC1] VERDICT PASS (14/14 incl. 4 negative controls; on-demand env jax, like X-A1IM). KICKOFF DISCOVERY declared in the spec (§2): cubic Hermite would leave cp = h' only C^0 (slope jumps at knots) — the C^1-COEFFICIENTS requirement forces the QUINTIC Hermite class: h from node data (h_i, cp_i, cp'_i), s0 from (s0_i, cp_i/T_i, (cp/T)'_i), cp'_i by 4th-order central differences; cp(T) := d/dT h-interpolant = the EXACT derivative polynomial, so cp = dh/dT is STRUCTURAL — machine-verified vs AD at 3.5e-07 against the DERIVED intermediate-magnitude tolerance 2.9e-05 (first tolerance attempt used the cp scale and was WRONG — the correct roundoff scale is the worst intermediate 60|h|/dT; deviation declared, fixed before commit, the check then passes with two decades of margin). Numbers of record: monotone-verified min cp = 1786.46 J/(kg K) with derived dip margin 0.24; s0' = cp/T at 5.2e-11 (floor 1.0e-06); gconst known-answers PASS incl. the G reduction to (gamma+1)/2 at 4.9e-09; dual-route vs the certified X-A1IM linear closure: max|dT| = 9.1e-07 K inside the derived interpolation band 1.5e-05 K (the C^1 closure changes NOTHING beyond interpolation class error — X-A1IM stays untouched and of record); inverse roundtrip 2.8e-09 vs derived floor 1.7e-07. EOS G > 0 AUDIT (ledger c4, S16 addition) EXECUTED: G(T) = (gamma+1)/2 + (gamma-1) T gamma'/(2 gamma) derived in-doc from the ideal-gas isentrope; on the full table box gamma in [1.1854, 1.2557] and G certified >= 1.089444 (Lipschitz-margin floor, grid audit declared not-interval) on BOTH nasa and cantera routes — genuine nonlinearity holds with wide margin for the working mixture; BZT-doctored rejector R3 DETECTS (min G -411.8). Registry +X-THC1 (119 entries, lint PASS). R4: kickoff doc §2 updated to the executed quintic class with the discovery + G formula. |
| 6 | RECONCILIATION (declared, user order mid-S17): two NON-MINE working-tree modifications found and verified against the user's declaration — (1) docs/rde_nozzle_PROGRESS.md NEXT-3: the [PAP-RIM] roads-x-insertions matrix census session (11 insertion points x classical+ML+exotic road families, 4 mandatory fields per cell, DIR-SUFF reaffirmed) QUEUED AFTER BRICK 2 by user pin (AskUserQuestion of record in the S16 post-closure conversation); (2) validation/PROGRESS_2026-08-06_S16_fondazioni2.md: post-closure step 9 appended (governance pointer only, log re-closed at 1-9). Both read in full; memory roads-insertion-matrix-directive read; content = governance/sequencing only, NO technical deltas, lint (xv) green with both in tree. ABSORBED as-is by this dedicated path-limited commit; the sequence pin (PAP-RIM after brick 2, relative order vs census-lemma decided then; no road verdicts minted in chat meanwhile) is TAKEN AS BINDING, not re-litigated. S17 scope unchanged: brick 2 proceeds. Note for this session's own conduct: the SOTA-library survey for the TR-SQP engine (user directive, memory sota-library-survey-directive) is an IMPLEMENTATION-STANDARD choice inside the already-adopted I5 slot machinery (D6 item 9 binds dJ/dSigma + TR-SQP), not a new road adjudication — it does not touch the PAP-RIM no-minting rule. |
| 7 | SOTA LIBRARY SURVEY OF RECORD (standing user directive of this session, memory sota-library-survey-directive written + MEMORY.md line) — kickoff doc §4bis: TR-SQP open implementations surveyed with current web sources (SciPy trust-constr = Byrd-Omojokun TR-SQP + TR interior point, mature/maintained; OpenSQP 2025 young; Uno/Argonot C++ heavier; IPOPT = interior point not TR; jaxopt maintenance-mode / optimistix no constrained SQP). DECISION: ADOPT scipy trust-constr wrapped in an RK-G SEGMENTATION DRIVER (frozen-replay callables = P1; per-stratum segments with fresh solver instance on re-record = P2 literally, no curvature/multiplier carry-over; monitors in the wrapper = P3(i)/(ii); P4 gates acceptance); declared trade-off (inner iterates not ours — guarantees enforced at the evaluation boundary where they are load-bearing); hand-rolled fallback licensed only as a declared deviation. Rationale: TR semantics = exactly the RK-G excursion bound; hand-rolling a constrained SQP would be the rigor risk the directive exists to prevent. |
| 8 | DUTY (b) [F2/A1] SCAN COLUMN ARCHITECTURE EXECUTED — new module validation/a1_march_scan.py [X-SCANM] VERDICT PASS (on-demand env jax). Content of record: the brick-2 REPLAY engine — record mode stays the adaptive [X-A1IM] Python march (RK-G P2 site); replay = per-column jax.lax.scan over the SAME implicit custom_vjp cells (Lemma B correspondence untouched); march plan reconstructed from the recorded schedule by walking the exact control flow on the decision stream (column-array representation derived in-session: fan column i's pt2 sequence = EXACTLY column i-1; arc columns = concat[wall, void copy, cells, axis]; straightening = downward scan to the recorded crossing row with traced crossing interpolation). NUMBERS OF RECORD: equivalence scan-vs-python max delta 8.9e-16 vs Newton-floor tol 1.6e-12 (bit-level: the restructure moves NOTHING); O3.1 on the scan path 7.2e-09 vs derived tol 4.1e-08; plan = 20 fan + 25 arc attempts (2 interp) + 40 straightening columns, 2684 interior cells; negative controls: N1 mis-indexed plan DETECTED (deviation 1.6e-03 vs 1.6e-12 floor), N2 corrupted vjp DETECTED. DEVIATION DECLARED: first run spent tens of minutes at multi-GB — root cause found and fixed IN-CARRIER (run_scan rebuilt jitted solvers per call => full XLA recompilation of every scan per evaluation; fix = load-bearing solver cache, documented in code + spec §3; the stale first run was killed by me, exit 255 in the task log = my kill, not a crash of the carrier). Registry +X-SCANM (120 entries, lint PASS). ADDENDUM absorbed in this commit: kickoff doc §4bis extended with the GENERAL-METHOD clarification (user: survey duty applies to EVERY complex algorithmic step) + retroactive declarations (PCHIP not adopted — reasons declared; lax.scan = the library idiom) + TOOLCHAIN CURRENCY note (user directive: old versions inadmissible; Python 3.13/jax 0.11/scipy 1.18 verified current; numpy 2.2.6 pin = DECLARED DEBT with discharge plan np.trapezoid + unpin + suite revalidation, queued this session budget permitting); memory sota-library-survey-directive updated with both halves. |
| 9 | DUTY (a) [F2/A1] LOOP-SPEED THRESHOLDS DERIVED AND MEASURED — carrier validation/a1_loopspeed_bench.py [X-LSG0] VERDICT PASS (clean host verified: 0 concurrent processes, rejector armed). NUMBERS OF RECORD (reduced twin case, scan engine as-implemented): t_record = 18.0 s (the RK-G P2 re-record cost, paid once per ACCEPTED step — t_record/t_solve = 0.5); t_solve = 39.8 s; t_grad = 119.6 s; **grad/solve = 3.004 — inside the [3,4] cheap-gradient theorem window: T1 PASS** (the reverse-AD implementation is theorem-conformant); t_GENO = 0.286 s (EPOCHREALTIME median of 3; the bash `time`-keyword redirection trap found and fixed, deviation declared); per-cell replay cost 1.42e-02 s (declared projection). T2a per-evaluation anchor: 39.8 s vs 4 x 0.286 s = **FAIL-AS-IMPLEMENTED** — root cause MEASURED, not guessed: each evaluation pays a Python re-trace over cached compiled kernels; the whole-march outer jit is NOT compilable today (XLA module blowup with ~100 inlined Newton scan bodies: >3 min/module slow-compile alarms then process death, exit 253 — both prior failed runs declared: run 1 = unjitted timings + broken GENO timing, run 2 = mega-jit crash). ADJUDICATION OF RECORD: **PRODUCTION GATE CLOSED** — the named remediation (single-bucket-per-phase padded scans => 3 XLA modules total, outer jit compilable, RK-G re-records stop generating new shapes) is BINDING before any production-mesh use of the engine, then T2a re-runs; T2 whole-loop bound ARMED with the measured constants for the brick Verdict; G0 FLIP CLAUSE NOT TRIGGERED (failure is dispatch-overhead class; the theorem-relevant T1 passes; flip re-arms if T1 or post-remediation T2a fails clean-host). General-vision note (user directive of this session, memory general-vision-nondivergence): the mega-jit fight was deliberately NOT pursued — honest as-is measurement + named lever serves the program; the direction stays on the brick. R4: DIR-G0 registry scope updated (falsifier now machine-rejectable), G0_decision.md §4 dated adjudication note, kickoff doc §4 status. Registry +X-LSG0 (121 entries, lint PASS). JAX persistent compilation cache enabled in the carriers (populated this run — the brick inherits the compiled kernels). **ALL FOUR D6 ITEM-9 KICKOFF DUTIES ARE NOW EXECUTED** (c step 4, d step 5, b step 8, a this step) + the S16 c4 addition: the "BEFORE optimizing" binding is satisfied — the brick may optimize. |
