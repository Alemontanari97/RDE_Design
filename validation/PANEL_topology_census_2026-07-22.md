# PANEL — Topology-sector census of the admissible class (D2.1/D2.6)
# Convergence-panel synthesis — ADVISORY, AWAITING USER RATIFICATION

Status: PANEL DELIVERABLE (2026-07-22, post-S11 addendum; UNTRACKED by
discipline, ADR precedent). REVISED Round 1 (adversarial pass, header
date as recorded by the applying session) and Round 2 (2026-08-02,
TERMINAL orchestrator revision applying the COMPLETE adjudicated
inventory of the 8-lens attack wave — 233/233 referee verdicts mapped;
deltas in section 9). Produced by a 5-lens convergence panel + an
8-lens adversarial attack wave with 3-referee arbitration (runs
wf_7dd292d7-e4d, wf_8d868096-274); the formal-convergence criterion
(unanimous clean 3-critic panel) was NOT met before infrastructure
truncation — see sections 8/8bis/9. Nothing here is of record until
ratified and promoted through the census-lemma rigor session (S15:
proofs + registry IDs + symbolic carriers + rejectors). Anchors: M0
D2.1 [D-DOM], D2.6 [D-P], b0bis (Veen/Migdal/JTH/Rao-Beck/Rao-1961),
GENO theory docs and conventions.

CONVENTIONS. Axisymmetric meridian analysis; the interface Gamma_d is
an annulus with inner/outer lips (Lambda). TOPOLOGY OF RECORD for
every clopenness/discreteness claim (declared Round 2, was silent):
Hausdorff-complementary convergence on the admissible class — the
Chenais convergence, consistent with M0's P7 contract; "discrete" =
locally constant in that topology; "continuous path" = in-class
continuum in that topology. NONBLOCK (Omega = E \ S connects Gamma_d
to the farfield) is NOT a clause of D2.1/D2.6 as written: it is a
DERIVED-candidate/pin — see CEN-O10 — and every use below is tagged.

GENERALITY STATUS (added Round 2; standing directive): the SECTOR
census (sections 2-3) is GEOMETRY-ONLY — data-, gamma- and phase-free
by construction. The STRATUM/REGIME machinery (section 4b) consists
of per-phase functionals of the state, EOS-GENERAL structurally
(rotational MoC class, tabulated closure per [DIR-THERMOTAB]); the
classical anchors cited in the map (Rao/Veen/Migdal/JTH closed forms)
are gamma-const ORACLE INSTANCES, never load-bearing. Per-phase
quantifiers are written explicitly wherever a quantity depends on xi.

## 1. Headline results (tags per the declared taxonomy; Round-2 form)

(1) MIGDAL CORRECTION [DECLARED; falsifier: a fully-internal two-wall
design whose termination-station ordering changes under a pure
characteristic-inclination change would be needed to RESTORE the
ordering as an invariant — none exists]: "which wall terminates
first" is NOT an invariant — in the fully-internal two-wall family
both walls end on the same INCLINED terminal characteristic, so
termination stations differ by pure Mach-line geometry. The correct
per-phase regime coordinate is the LIP FAN OPENING Delta-nu_lip >= 0
(REDEFINED Round 2, was "attachment locus": in the attached class the
ambient jet boundary always leaves from the outer TE/lip, so the
locus does not discriminate; the discriminant is the centered-fan
strength at the lip — Delta-nu_lip = 0 iff fully internal;
equivalently the lip pressure ratio; GENO RaoPlug's theta_E
parametrization is the same coordinate).

(2) ONE-SECTOR COLLAPSE [THEOREM-sketch; CONDITIONAL on CEN-O2 (tip
carrier), CEN-O9 (lip carrier), CEN-O3 (cone reading — the contact
barriers and gap floors of clause (b) ride on it), CEN-O4
(path-connectedness), CEN-O5 (value continuity), CEN-O1
(attachment-existence parse for the bare-annulus corner)]: the
inner-termination dichotomy "closes on axis vs base" is a STRATUM
BOUNDARY of the continuous coordinate r_b -> 0 (under CEN-O2); "wall
presence at a lip" is continuous ONLY under the S-union-chamber
carrier (CEN-O9): under S-only every attached component holds its own
cone, the out-of-class band is 0 < e < ~h0 (cone-HEIGHT floor,
corrected Round 2 from ~2 r0), and S0 fragments into wall-presence
sub-sectors. CONSEQUENCE (under those pins): bell, spike (full and
truncated), shrouded plug, Migdal annular, ED and the bare annulus
are ALL strata of ONE clopen sector S0, conjecturally connected (hub
argument owed, CEN-O4). The genuinely DISCRETE jumps are only: adding
a DETACHED component (D-family; CEN-O1/CEN-O6-conditional
admission). On the strong/Chenais branch of CEN-O3 the collapse
machinery is unconditional; on the weak-pointwise branch it is OPEN.
Falsifier: two S0 strata not connectable by an in-class continuous
path under generous c.

## 2. Discrete invariants and admissibility lemmas

 I1 Per-component ANCHOR CLASS: inner-lip / outer-lip / detached.
    ("Bridged" is not a value: A1.) [Discrete: contact/pinch events
    are barred by the w_min gap floors — Omega-side, CONDITIONAL on
    CEN-O3-strong (clause (b)). Members contacting dE\Lambda (E-face
    anchored flanges, base-resting bodies) are CEN-O6-conditional
    (their "anchor" is an envelope contact, not a lip). Falsifier: an
    in-class continuous path changing an anchor class.]
 I2 DETACHED counts by type: N_torus (off-axis solid torus), N_ball
    (axis-touching body). [Discrete by the same (CEN-O3-conditional)
    contact barrier; family admission CEN-O1-conditional (and
    CEN-O6-conditional for E-face-contacting members). Falsifier: an
    in-class continuous path changing N_torus or N_ball.]
 S1d STRUCTURAL DATUM (added Round 2, replaces the false "at most one
    attached continuation per lip" premise): each attached component
    carries a meridian BOUNDARY-ARC DECOMPOSITION — number of
    trailing edges, branch count (Y-forked/branched walls are
    constructible in-class: crotch filleted within the curvature
    bound, both branches above the cone scale), blind-pocket count.
    Whether Lambda admits MORE THAN ONE attached component per lip
    circle is pinned by CEN-O11 (Lambda = lip circles => one per lip
    derivable, components sharing a circle merge; Lambda = band =>
    open). Channel multiplicity from branching is a COORDINATE datum
    (one Delta-nu/termination pair per channel mouth), not a new
    sector, under the CEN-O3-strong barriers.
 L1 CAVITY-FREE LEMMA [CONDITIONAL on CEN-O10 — restated Round 2; the
    Round-1 "derived" tag was CIRCULAR (it cited this document's own
    section 6 list, which has no D2.1 clause): every admissible S has
    all per-component meridian first-Betti numbers = 0, PROVIDED the
    class carries a no-sealed-cavity / NONBLOCK+ clause (CEN-O10) or
    the D2.6 state-clause derivation (S1 solution required on every
    component of Omega) is written out — both queued for S15. Under
    CEN-O10: a bounded meridian complementary component revolves to a
    sealed cavity (excluded); the only axisymmetric through-passage
    is an annular slot = component SEVERANCE (anchored segment +
    detached successor, D-family); discrete perforations are
    non-axisymmetric (excluded). Falsifier: an admissible S with a
    meridian component of b1 >= 1 under the pinned clause.]
 (I4 RETIRED, Round 2: as defined it was either VACUOUS — under L1 +
    connectivity the meridian configuration of disjoint closed disks
    in a connected planar region admits exactly the labels already
    counted — or NOT an invariant: a detached torus can slide out of
    an open channel mouth with no contact event. A pose refinement
    exists only as the in-class-isotopy class WITH an obstruction
    (mouth clearance < component size), i.e. a DERIVED regime label,
    not a sector coordinate. D-family finiteness rests on ball
    counting alone.)
 A1 BRIDGE SEALING [THEOREM-sketch, CONDITIONAL on CEN-O10 for the
    NONBLOCK premise and scope-tagged per CEN-O3 (Round 2, from
    proofs-5): class-wide under CEN-O3-strong (Chenais => Lipschitz
    boundaries => meridian path-connectedness); C^{1,1}-spline-class
    conditional under weak]: any component whose closure contains
    both lips seals the duct for EVERY hole vector (meridian Jordan
    curve of two lip-to-lip arcs; piercings cannot rescue: b1 hole =
    sealed cavity (L1), annular slot UN-bridges, grids are
    non-axisymmetric). Fusion of two attached walls into a bridge is
    blocked AT FIRST CONTACT (meridian tangency = full contact ring =
    sealed annular channel — reworded Round 2: NONBLOCK provides NO
    pre-contact gap floor; the pre-contact barrier, where asserted,
    is clause (b)'s and is CEN-O3-conditional). BR is EMPTY; the
    axis-touching separation case and the simple-arc hypothesis go to
    the S15 lemma.

DEMOTED TO CONTINUOUS (stratum/regime coordinates, NOT sectors; tags
per entry): r_b (CEN-O2; plus CEN-O8 if a validity floor joins c);
attached extents e_i, e_o (CEN-O9; band 0 < e < ~h0 under S-only);
termination ordering (the Migdal trap — never an invariant);
Delta-nu_lip (per phase xi); fitted-sheet multiplicity [Round-2
retag: PER-PHASE INTEGER REGIME LABEL with NO barrier argument of
record — sheet-birth continuity is OPEN under CEN-O5 (face added);
DEF operation sits at/beyond the certified S1 margin, C-MAJDA-
conditional if fitted — NOT a silent demotion]; trailing-edge type;
flow-role labels.

## 3. The sector lattice (finite by the counting theorem)

 S0            attached-only: attached components per lip governed by
               CEN-O11 (one per lip circle derivable under the
               circle reading); hole-free per L1 (CEN-O10-
               conditional). THE PRIMARY SECTOR — the whole classical
               taxonomy lives here as strata/regimes (map, section
               5). [Under CEN-O9 S-only: wall-presence sub-sectors.
               Under CEN-O1 mandatory-existence parse: the bare-
               annulus stratum (attachment at zero lips) is OUT of
               class — see CEN-O1, third reading.]
 D(m,n)        S0 + m detached tori + n detached axis-bodies (no hole
               counts — L1; no nesting label — I4 retired; pose
               refinements only as obstructed-isotopy regime labels).
               Ejector ring = D(1,0); k-slotted/ventilated wall =
               anchored segment + k toroidal successors (D(k,0)).
               Admission CEN-O1-conditional (+ CEN-O6 for E-face
               members; strut idealization declared).

 DELETED: P(h) (hole vector identically 0 — L1) and BR(h>=1) (A1).
 Perforated-diaphragm/grid families exist only in a non-axisymmetric
 extension, out of class with struts/clusters.

 COUNTING THEOREM [THEOREM-sketch; BRANCHED Round 2 on CEN-O3 — the
 Round-1 two-clause split understated the branch]:
 - CEN-O3-STRONG (ball-uniform cone property; the Chenais 1975
   hypothesis, and what M0's P7 "Chenais compactness" already
   requires): every component (attached or detached) contains an
   r0(h0,omega)-inball; disjoint balls in bounded E => N <=
   N(r0,|E|); gap/channel floors w_min; contact barriers of I1/I2;
   clopenness of the partition in the Hausdorff-complementary
   topology; Chenais compactness — ALL UNCONDITIONAL on this branch.
 - CEN-O3-WEAK (pointwise cone): the rib/neck floor FAILS
   (constructive counterexamples: smoothed hourglass waist with
   flared flanks; thin plane-faced slot with cones leaning into the
   thick material) — finiteness of severance counts must rest on the
   detached-successor inballs instead; gap floors and contact
   barriers are NOT available; clopenness of D-jumps OPEN.
 => the partition {S0, D(m,n)} is FINITE and EXHAUSTIVE of the
 admissible class on the strong branch; on the weak branch the
 finiteness of D survives (successor inballs) but the barrier
 structure is open. Falsifier: an admissible S outside every label.

## 4a. Design-space coordinates proper (partition S0)

 R1 attached extents (e_i, e_o) [CEN-O9; band ~h0 under S-only].
 R2 inner termination (x_b, r_b) + base-closure channel (p_b model
    declared; r_b -> 0 stratum boundary, CEN-O2; validity floor
    CEN-O8).
 R3 outer termination (x_TE, r_TE) + TE type; per-channel data where
    S1d branching is present (one termination pair per mouth).
 R7 per-stratum spline DOF charts; detached poses in D(...).

## 4b. Per-phase state/regime coordinates (on S0 x Xi — Round-2 split;
##     these are functionals of the per-phase solution, NOT design DOF)

 P1 Delta-nu_lip(xi) >= 0 — lip fan opening (internal iff 0); a
    design under a cycle mu generally STRADDLES regimes across xi
    (phase-mixed regions are the norm, not the exception).
 P2 sigma_in(xi) — wake free-boundary edge state.
 P3 fitted-sheet multiplicity(xi) — DEF/deliberate-shock regime
    label; sheet-birth face OPEN under CEN-O5; C-MAJDA-conditional
    when fitted; delta_S1-margin interplay flagged.
 FACE-INDEXED p_a/p_b ENTRY (R4 REWRITTEN Round 2 — the old
 configuration-indexed table contradicted the GENO conventions of
 record): p_a always enters the OBJECTIVE gauge; in the
 BC/optimality structure it enters PER FACE of the optimizing
 control surface — outer/shroud C+ corner endpoint: CSTR_PA (sign -,
 carries p_a); inner/plug C- corner endpoint: CSTR_PB (sign +,
 carries p_b — NOT p_a); ambient free-boundary continuum wherever a
 jet edge at p_a bounds the surface; base closure p_b wherever a
 wake face does. Internal-vs-external does NOT align with
 corner-vs-continuum (Rao's pure-external 1961 spike takes p_a at
 the LIP CORNER; Veen's internal-external pair uses two corner
 conditions on surfaces of opposite families).
 ENGINE CONSEQUENCE: BC branches required = {interface data
 (supersonic patches); SUBSONIC-PATCH closures per the D1 decision
 tree (incoming invariants + impedance/choking surrogate) when
 admitted data carry non-primary subsonic patches (added Round 2);
 wall analysis/design; axis; jet at p_a; base at p_b; terminal-
 characteristic corner evaluation; fitted sheets} — and the
 optimizer MOVES Delta-nu_lip; no branch choice is an a-priori type
 selection.

## 5. Canonical-family map (regime labels are AT THE ANCHOR PHASE;
##    per-phase straddling per section 4b)

 conical / TIC / TOC / TOP / dual-bell(HIGH mode): outer-dominant
   strata of S0 (dual-bell kink = shape feature; LOW separated mode
   excluded as regime).
 DEF (Rao-Beck): geometry in S0 + sheet regime P3; deliberate-shock
   OPERATION at/beyond the certified S1 margin (b0bis A6),
   C-MAJDA-conditional if fitted (tag added Round 2).
 ideal/perfect full plug (JTH Eqs. (33)-(36); Angelino full
   contour): the r_b = 0 stratum boundary (in-class iff CEN-O2;
   else ceiling object). [RE-ANCHORED Round 2.]
 Rao 1961 optimum spike: FINITE-base stratum — free lip R_E and free
   base R_D with only L fixed; base radius is a variational OUTPUT
   (R_D/R_E = 0.137 oracle of record); p_b enters via the base
   corner. [MOVED Round 2 from the r_b = 0 row — corpus conflict.]
 truncated plug (JTH): S0 inner stratum with p_b closure.
 Migdal annular (two-wall): the IDEAL design of the two-wall strata
   — MoC march to UNIFORM EXIT state (GENO type-9 kernel: focus K at
   M = Me, theta = 0), the annular analogue of the ideal bell
   (nozzle_type 0). Delta-nu_lip = 0 is a CONSEQUENCE of the
   uniform-exit construction, not the defining property (corrected
   Round 2bis, user catch: ideal-vs-optimized is a DESIGN-PRINCIPLE
   label, orthogonal to the census). Max-area-ratio = feasibility
   frontier inside S0.
 Veen shrouded-plug catalogue: the thrust-OPTIMIZED counterpart of
   the same two-wall strata. VERIFIED optimality structure (page +
   GENO conventions of record): TWO control surfaces of opposite
   families ending at the two tips, with PRESSURE corner conditions
   at BOTH — shroud tip carries p_a (Veen Eq. 4 = CSTR_PA, sign -),
   plug tip carries p_b (Eq. 8 = CSTR_PB, sign +; base model Eq. 9).
   The internal/external expansion SPLIT is NOT imposed and NOT
   asserted here (corrected Round 2ter, user catch): the lip-fan
   strength at the shroud TE is an OUTPUT — zero iff the optimal
   shroud completes the expansion internally, positive for
   length-constrained short-shroud members where the exposed plug
   carries residual expansion (a pressure-matching consequence, not
   a design condition). Shroud length is a variational output;
   shroud->0 degeneration = classical echo of topology-as-output.
   [Round 2 split from Migdal re-founded on IDEAL vs OPTIMIZED;
   Round 2ter strips the unverified regime attribution.]
 NOTE (Round 2bis): the census classifies GEOMETRIES; "ideal" and
   "thrust-optimal" label the DESIGN PROBLEM a contour solves
   (uniform-exit direct march vs variational optimum under
   constraints) — the same distinction as ideal bell vs TOC in the
   single-wall rows. Both label subfamilies WITHIN the same strata,
   never sectors.
 FAMILY-GROUPING NOTE (Round 2quater, USER clarification of record):
   the two-wall strata with both extents substantial are PURELY
   INTERNAL-EXPANSION devices — "annular bells": Migdal (ideal) and
   Veen (optimal) belong HERE, with ambient entering ONLY through
   the exit corner/boundary conditions (p_a at the shroud tip, p_b
   at the plug base), exactly as a bell's ambient enters only at the
   lip. Despite the name, the Veen "shrouded plug" is NOT an
   aerospike: "plug" there names the inner wall of an internal
   annular channel. The PLUG/AEROSPIKE family proper (Rao 1961,
   Angelino, RaoPlug: inner wall exposed to ambient over most of the
   expansion, lip fan, jet boundary at p_a) is a DIFFERENT design
   formulation living toward the short-/zero-shroud end of the
   stratum space. GENO's type system already encodes this: types
   7/9 (Veen/Migdal) = two-wall internal; types 5/8
   (Angelino/RaoPlug) = external plug family. ENGINE DETAIL (how the
   code will realize it): each family is a COMPOSITION of the shared
   BC branches of section 4b — two-wall internal = {two wall
   branches + axis-free annular interior + BOTH corner evaluations +
   terminal characteristics}; plug/aerospike = {inner wall + lip
   centered fan + jet boundary at p_a + base closure p_b}; the
   branches are shared modules, so the optimizer/tournament can
   traverse between formulations without any hard-coded type switch
   (formulation level, never kernel — DIR-G0 discipline).
 ED: two-wall stratum, internal-dominant short inner body.
 bell-on-annulus: outer stratum with wake edge P2.
 bare annulus: e_i = e_o = 0 stratum [CEN-O9-conditional path;
   CEN-O1 third-reading conditional EXISTENCE].
 slotted/ventilated shroud AND ejector ring: D-family
   [CEN-O1 + CEN-O6 + strut idealization]. true ejector AUGMENTOR
   (secondary stream): EXCLUDED (interface physics). linear
   aerospike, clusters, strutted hardware: EXCLUDED
   (non-axisymmetric). extendible: deployed state in S0; transient
   excluded.

## 6. Declared exclusions (each tied to a clause OR a named pin)

 non-axisymmetric features (struts/clusters/lobes/linear aerospike;
 perforation grids); sub-cone-scale features (zero-thickness shells;
 GENO thin-cowl H8 = class-boundary object); porous/blowing/film
 walls (impermeable contract); time-varying S; solids upstream of
 Gamma_d or covering interface patches; sealing configurations and
 sealed internal cavities [CEN-O10 — pin, NOT yet a clause of
 record]; deliberately separated operating modes and reattachment/
 ejector-pumping regimes (flow-model boundary; per-phase [D-GSEP]
 quantification); subsonic-patch interface as PRIMARY (non-primary
 patches stay IN the contract via their closures — section 4b).

## 7pin. USER PIN RECORD (2026-08-02, per-pin discussion of record —
##    decisions on this ADVISORY; the D2.1/D2.6 amendments they imply
##    are EXECUTED only in the census-lemma rigor session, R4 duty)

 CARRIER DECISION (structural, reshapes CEN-O2/O3/O9; user-driven,
   fifth user catch of the campaign): the uniform cone condition
   moves to the FLUID SIDE — Omega = E \ S carries the (h0, omega)
   uniform cone property (the Chenais hypothesis proper), with S
   compact and S = closure(int S) (only exactly-zero thickness
   excluded). CONSEQUENCES: tangential tips IN CLASS (the ideal
   spike's tangent closure is a member); sharp trailing edges IN;
   thin walls (t > 0) IN — manufacturing floors, if wanted, enter
   the constraint vector c (t_min with its multiplier), not the
   class; sub-scale fluid gaps/channels OUT (exactly where the
   marching degenerates); gap floors w_min and the contact barriers
   become UNCONDITIONAL; component counting by GAP packing.
 CEN-O1 PINNED: PERMISSIVE reading (ii), bare annulus included.
   Detached-family physics clarified of record: (a) vehicle-anchored
   members (ejector shrouds on the vehicle base) belong to a FOURTH
   anchor class via the E-contract (CEN-O6), not to "floating";
   (b) strut-mounted members carry a DECLARED strut-penalty band in
   any verdict; (c) truly floating members are mathematical only —
   filtered by per-phase S1 certification and the tournament, never
   by the class definition.
 CEN-O2 CLOSED by the carrier decision (tips beta >= 0 admissible,
   including tangent closure).
 CEN-O3 PINNED: uniform Omega-side cone, Chenais form (h0, omega
   class-wide); formalization = S15.
 CEN-O9 DISSOLVED by the carrier decision (e -> 0 continuous; no
   S-side inball requirement; the ~h0 band objection evaporates).
 CEN-O10 PINNED: NONBLOCK+ — EVERY connected component of Omega
   meets the farfield ("una cavita sigillata non e nulla" — flow-
   irrelevant multiplicity quotiented away; bases NOT affected:
   geometrically open to the plume; near-sealed pockets already
   killed by the Omega-cone). S15 bonus target: derive it from the
   D2.6 state clause (pin -> lemma).
 CEN-O11 PINNED: option A — Lambda = the two lip CIRCLES; attachment
   = closure(S) contains the circle (C0 wetted continuation, slope
   FREE: lip corners/fans preserved); recessed/offset walls are
   D-family or vehicle-anchored; one-attached-per-lip becomes a
   THEOREM. Options B (band: magic width, loses the theorem) and C
   (tangency: kills lip fans) REJECTED of record.
 GENERALITY GUARANTEE (user condition on the pins, checked): no
   physical geometry is removed by the pin set — every excluded
   object is either flow-irrelevant (cavities), exactly-degenerate
   (t = 0), sub-scale pathology (narrow gaps), or reclassified
   honestly (recessed walls -> D/vehicle-anchored). Lip fans,
   tangent tips, thin cowls, mixed internal/external members,
   branched walls and the bare annulus are ALL in class.
 NET RESIDUAL after the pins: CEN-O4 (hub connectedness) and CEN-O5
   (value continuity at faces) — ANALYTIC obligations, S15 proofs,
   not choices; CEN-O6 (E-contract: farfield = downstream face,
   vehicle-anchor class, lateral faces as active constraints) —
   direction agreed, formalization S15; CEN-O8 (r_b validity floor)
   — joins c as a model-validity constraint when the p_b closure
   demands it.

## 7. Open points as originally consolidated (superseded by 7pin
##    where pinned; kept for the audit trail; RENAMED Round 2:
##    CEN-O* — M0's O1-O5 label subsonic closures, the decision
##    tree, and the VI.6 oracles, all distinct)

 CEN-O1 LIP-ATTACHMENT CLAUSE — now THREE readings (third added
    Round 2 from the surviving attachment-existence objection):
    (i) strict-anchored (every component attached => D excluded);
    (ii) permissive (only lip-touching components constrained =>
    D admissible); (iii) MANDATORY-EXISTENCE (S must attach at
    one/both lips => the bare annulus, e -> 0 limits and the
    Veen shroud->0 echo are OUT of class). Undecidable from the
    D2.1/D2.6 text; needs an amendment; USER pin.
 CEN-O2 tip carrier (cone condition at an axis-closing tip).
 CEN-O3 cone reading STRONG (ball-uniform / Chenais) vs WEAK
    (pointwise) — the counting theorem, all contact barriers, gap
    floors and A1's class-wide scope ride on it; M0's P7 already
    cites Chenais => STRONG is the reading consistent with the
    existing record (recommendation, not a resolution).
 CEN-O4 stratum path-connectedness (hub argument owed).
 CEN-O5 value continuity at stratum/regime faces — face list NOW
    INCLUDES sheet birth (added Round 2); map each face to its
    [T-P7S1]/[C-D25U] continuity argument.
 CEN-O6 boundary contract of E (envelope-contact members; farfield
    definition; upstream face beyond the outer lip).
 CEN-O7 CLOSED-as-derived (BR empty) — scope now CEN-O3-tagged
    (class-wide on strong; spline-conditional on weak); separation
    lemma to S15.
 CEN-O8 base-closure validity floor r_b >= r_min joining c.
 CEN-O9 cone carrier at the attachment (S-only vs S-union-chamber);
    S-only band corrected to 0 < e < ~h0.
 CEN-O10 NONBLOCK/cavity clause (NEW Round 2; the nod that 7
    independent objections converged on): weak (some path) vs
    NONBLOCK+ (every component of Omega meets the farfield — closes
    cavities and sealing in one clause; per-patch variant for
    interface pockets), vs DERIVATION from the D2.6 state clause.
    D2.1 amendment duty; USER pin; L1/A1 conditional on it.
 CEN-O11 definition of Lambda (lip circles vs band) — decides the
    one-attached-per-lip premise (NEW Round 2).
 R4 OBLIGATION: everything above reaches M0 ONLY through the S15
    census lemma (registry IDs + carriers + rejectors).

## 8bis. Adversarial-convergence attempt record (honest, cumulative)

 Attempts 1-3 as previously recorded (session/monthly limits; token
 stop with redacted transcripts). Attempt 4 (run wf_8d868096-274,
 six passes across limit windows): 8/8 attack lenses COMPLETED
 against the Round-1 document; 3-referee arbitration COMPLETED
 (233/233 verdicts mapped Round 2, zero unmapped); TWO 3-critic
 panels ran (R1: 3+7+4 issues -> adjudicated and applied Round 1;
 R2: 5+5+5 issues against a superseded agent-revision draft —
 arbitration NOT completed before the limits; that draft is
 SUPERSEDED by this Round-2 revision, and the R2 issues' general
 content is subsumed by the lens survivors where checked, DECLARED
 residual otherwise). The unanimous-clean-panel criterion was NEVER
 met: formal convergence at panel level remains UNACHIEVED and is
 routed to S15's machine-checked carriers (the stronger standard).

## 8. Dissent/independence log (honest)

 - Adjudication tallies of record (Round 2): 43 lens objections
   raised -> 39 CONFIRMED -> 31 SURVIVED triple refutation, 7
   REFUTED, 1 unarbitrated (variational-7 — folded: its fix is
   implied by the refutation logic of its twin variational-5, and
   adopted as the Delta-nu_lip redefinition); 8 PLAUSIBLE carried
   as dissent (ids in the run journal). Critic-R1: 14 issues -> 9
   survived and applied (Round 1), 3 refuted, 2 incomplete whose
   substance is covered by clean-vote survivors. Orchestrator
   adjudications (declared): r1-topo-3 attachment-existence
   (single lean-survive vote; adopted as CEN-O1 reading (iii));
   variational-7 (as above).
 - Script-internal tally (26) vs Round-2 recount (31): the script
   computed survivors at round-0 with the referee subset then
   completed; the recount uses the full 233-verdict set. The
   recount governs.
 - Classifier-unavailable flags: ref-record:generality-3 (one of
   six votes; discarding it does not change the majority — outcome
   robust) and ref-quote:r1-topo-2 (sole vote on an incomplete item
   whose substance is covered by completeness-1/discreteness-4 with
   clean votes). Both neutralized by redundancy, DECLARED.
 - The point-set lens completed in attempt 4 (its Round-1 absence
   is healed); its surviving objections are applied above
   (topology declaration, I4 retirement, floor corrections).

## 9. Formal convergence record

 Round 1: as previously recorded (9 critic-issue fixes applied:
 BR deleted; hole machinery retired -> L1; counting theorem split;
 O9 declared; falsifiers and headline tags added).

 Round 2 (2026-08-02, terminal orchestrator revision; complete
 lens-wave inventory): 31 surviving objections applied —
 (a) FOUNDATIONS: CEN-O10 declared (NONBLOCK/cavity pin; L1 and A1
     retagged conditional — their Round-1 "derived" tags were
     circular); topology of record declared (Hausdorff-
     complementary/Chenais); counting theorem re-branched on CEN-O3
     with the weak-branch failures stated (neck/slot
     counterexamples); one-per-lip premise replaced by S1d +
     CEN-O11; attachment-existence third reading added to CEN-O1.
 (b) CORPUS: Rao 1961 spike moved to the finite-base stratum
     (R_D/R_E = 0.137 oracle); ideal-plug row re-anchored on
     JTH/Angelino; Veen split from Migdal (internal-external,
     opposite-family control surfaces, page-verified); R4 rewritten
     FACE-INDEXED (CSTR_PB carries p_b — three verbatim conflicts
     with GENO conventions fixed).
 (c) STRUCTURE: section 4 split into design-space (4a) vs per-phase
     regime coordinates on S0 x Xi (4b); Delta-nu_lip replaces the
     attachment-locus coordinate; I4 retired; sheet-multiplicity
     demotion tagged OPEN with the sheet-birth face added to CEN-O5
     and the DEF C-MAJDA tag added; generality-status block added;
     subsonic-closure BC branches added; namespace renamed CEN-O*;
     O9 band corrected to ~h0; A1 "before contact" reworded to "at
     first contact"; E-face conditionality (CEN-O6) propagated into
     I1/I2/D.
 REFUTED attacks of record (7, robustness evidence): NONBLOCK-
 compactness leak; envelope-capping (absorbed into CEN-O6/CEN-O10
 scope); r_b/O8 hidden barrier; dual-bell per-phase misclassification
 claim; S-union inball leak; O5-face-list-only variant; a_amb
 collapse (its constructive content adopted via the Delta-nu_lip
 redefinition).
 Round 2bis (2026-08-02, USER CORRECTION of record): the Migdal/Veen
 rows re-founded — Migdal = IDEAL annular (MoC to uniform exit,
 GENO type-9 focus K at M = Me, theta = 0; the two-wall analogue of
 nozzle_type 0), Veen = the thrust-OPTIMIZED two-wall counterpart;
 the regime values (Delta-nu_lip = 0 vs > 0) are CONSEQUENCES of the
 design principle, not definitions. A design-principle axis
 (ideal / thrust-optimal) is noted as orthogonal to the census in
 section 5. Third user catch of the campaign (after the Migdal
 termination-ordering trap and the two-sector claim).
 Round 2ter (2026-08-02, USER CORRECTION of record): the Veen row's
 "internal-external" attribution STRIPPED as unverified inference —
 Veen's verified optimality structure is the PAIR of pressure corner
 conditions at the two tips (p_a at the shroud tip via Eq. 4/CSTR_PA;
 p_b at the plug tip via Eq. 8/CSTR_PB with the Eq. 9 base model);
 the expansion split is an optimization OUTPUT, not a boundary
 condition. Fourth user catch. The recurring lesson is now a
 DISCIPLINE NOTE for S15: regime attributions to classical designs
 enter the census ONLY as page-verified boundary/optimality
 structure; every inferred flow-regime consequence must be tagged
 INFERENCE with its mechanism stated, or omitted.
 Cumulative status: ADVISORY, awaiting user ratification; pins
 CEN-O1/O2/O3/O9/O10/O11 (+O6 contract) are USER/S15 decisions; the
 BINDING convergence instrument is the S15 census lemma with
 machine-checked carriers. The panel-level unanimity criterion
 remains honestly UNMET (section 8bis).
