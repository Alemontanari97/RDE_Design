# S22 — Total-order session log (2026-08-11; F1 "GOVERNOR + P-2
# CAPTURE" of plan v3 — T0 user decision on P-2 decoupling, [X-A1IM]
# hygiene, S20 retro-diagnosis (F1-entry row), pre-run derivations,
# decisive campaign A')

Session: S22, per the user's order opening this session (which RESOLVES
PROGRESS NEXT-1 of S21: phase F1 of the ratified plan v3, D6 §0-pre;
budget 2-3 sessions, max 2 decisive campaigns, anchor ~1046 s/campaign;
counter "F1 campaign k/2, session m/3" MANDATORY in PROGRESS ORA at
every close). Mandate (user order, this session's header): (1) S10+
opening; (2) pre-execution GATE; then duties in order: T0 [USER
DECISION FIRST — present and WAIT] P-2 decoupling (ISS-2): (a) freeze
the P-2 draft at F0+1 THIS session at fallback quality (S19 two-knob
numbers, o32 objective row declared NON-CONCLUSIVE, C1 blocker
verified closed), decoupled from the governor; or (b) ratified default
= dated trigger at F1 close; if (a), the freeze is a THIS-session
deliverable (date + artifact + recorder); locks re-presented (G5
time-boxed, acquisitions, preprint). T1 [HYGIENE, short] on-demand
[X-A1IM] main run (the new S2 exit_cap and S6 CERT_PLAY rows are
built but not yet exercised end-to-end — S21 declared) + verify the
rows CAN reject (low N_NEWTON or tight m_stop as stimulus). T2
[S20 RETRO-DIAGNOSIS, F1-entry row — the cheapest decisive
measurement, BEFORE any decisive run]: regenerate the S20 walk
deterministically ([X-AKNO] attempt-3 path) with A1_CERT_ARGMAX=1 +
A1_REJ_SAVE; on the persisted rejected designs run the C-1 THREE-WAY
locus test (claims_to_code): (a) argmin-margin locus on the terminal
C+ -> DEF reading confirmed; (b) INTERIOR locus -> DEF reading for
S20 FALSIFIED, tier-1; (c) healthy val but Newton stalls -> class
construction defect. EVERY branch is a verdict (O4 discharged for the
S20 instance); R4 same session. T3 [PRE-RUN DERIVATIONS, R5 — ALL
before the decisive run]: (i) margin constraint m(W) = KS aggregation
of the val field ([X-VMON], AD, EOS-general) with rho DERIVED and
floor mu_0 DERIVED (never tuned), GENO implementation-magic bands
(dV_pert, den-guard, PM window) derived for the cross-check leg;
(ii) G1-SURROGATE implemented REJECTOR-GATED (deliberate
beyond-frontier trial -> finite negative surrogate from the partial
march + recovered SQP step; stall/crash = FAIL — D6 tier-invariant
clause); (iii) the [D1]-CONSTRAINED variant derived and committed
BEFORE the run; (iv) short SOTA survey on adaptive KS aggregation if
needed (adopt-or-declare). Dual-proof standard on every new claim.
T4 [DECISIVE CAMPAIGN A']: margin-constrained re-optimization on the
bell tier-0, [X-VMON] monitor along the walk + mandatory logs
(argmin-margin + active-cusp census at termination); outcome
BRANCH-ADJUDICATED (margin-active ADMITTED in RT-1 consistent-with
wording; certifiability-limited-under-constraint = valid declared
exit; interior binding = H2-kill -> tier-1; multi-cusp = H1-kill);
mu >= 0 on a pre-registered floor sequence with a declared
extrapolation protocol (RT-4: mu is defined only downstream of O1 —
report B-stationarity until discharged; the deeper-jump test stays a
SIGN TEST). C-O33 closed or falsified as byproduct if outcome-I. Max
2 campaigns, then fallback AUTO (P-2 two-knob BY RULE). T5 [CLOSURE]:
P-2 freeze per the T0 rule; budget counter in ORA; full R3.

TERMS OF REFERENCE (binding, from the session header): S14-S21
verdicts NOT re-litigated (incl. o32 NON-CONCLUSIVE and the certdiag
retro-validation); NO driver surgery (the A' constraint is
FORMULATION, not surgery — S20 pre-named boundary); R5 numbers only
from committed carriers; R4 same session; query-bounded novelty; env
numpy 2.5.1 / jax 0.11 / scipy 1.18, persistent XLA cache;
reinforced commit protocol on EVERY commit (git log -3 + status
before, path-limited commits, hunk audit after; lint (xv) green
GATED ON THE EXIT CODE; NEVER pipe an exit code). DECLARED FALLBACK:
fallback B (P-2 on the S19 two-knob numbers) stands at every gate; a
SECOND governor obstruction goes to adjudication with an alternative
frame, NEVER to surgery. QUEUED AFTER (untouched this session): F1b
(DEF twin; leg 1 under the GENO external conditional — flagdef KAT
dispatch in BLOCCATO 5), U3' [F2a], S19 residues (b)/(c)/(d),
census-lemma, PAP-RIM, G0/T2 review. Suite-green readings inherit
the C4 carrier-exclusive annotation of record (run_all.py).

| # | Step |
|---|------|
| 1 | SESSION OPEN (S10+). HEAD = 1d9609a (S21 closed, step 8), branch `rde-nozzle-program`, no foreign commits, single-session rule verified (git log -3 + status at open). Tree state declared: data/q_mapping.* modified (inherited, pre-S15, untouched); untracked set unchanged from S21 close = GENO/ (independent repo, NEVER committed from here), literature/, user shell artifacts, the standing advisories (ADR pattern) incl. ADVISORY_rde_choking_2026-08-11 (U3' survey base, owner F2a — NOT this session) and the S21-prompt/addendum advisories. OPENING PROTOCOL EXECUTED IN ORDER: (a) project memory — the EIGHT mandatory notes read in full (s21-f0-order-instrumentation; s20-adaptive-obstruction; claim-dual-proof-standard; general-vision-nondivergence; generality-nonhardcoded-procedures; sota-library-survey-directive; moc-critical-independent-invariants; agentic-orchestration-forms) + gate-pre-esecuzione (the gate's own text). (b) M0 Part VI read: the S20 GENERAL FORMALIZATION block (lines ~1248-1416: the formal problem max J s.t. W in K; outcome I/II semantics; tier ladder (P_t), A_t(mu_0), margin vector, margin-multiplier KKT; K_disc~A_0 CONJECTURE with the Eq.(4)/Sternin monitor as named falsifier; KS = the standard aggregation, adaptive variants = current refinement; Rao-vs-Zucrow naming inversion) and the [S21 REGISTRATION BLOCK] (lines ~1418-1504: EQ-v2 at red-team footing; (G)/Lambda-form with BOTH bounds — bound (b) NAMES the derived implementation-magic bands as an F1-ENTRY DUTY; lambda_e; obligations ledger O1->F4b, O4->F1 [instrumentation ARMED], O5->F1b, G1->F1 [tier-invariant]; RT-4 sign-test demotion of record). (c) D6 §0-pre read WHOLE (F1 phase text: entry gates, BRANCH-ADJUDICATED exit, freeze checkability ISS-5, decoupling option ISS-2; F1b external conditional; tier-invariant clause; G-gate map; cross-cutting). (d) S21 log validation/PROGRESS_2026-08-11_S21_order.md read WHOLE (8 steps), incl. the T5 budget call (S20 retro-diagnosis moved to F1 ENTRY with phase tag — THIS session's T2), the declared residual "S2 exit_cap + S6 CERT_PLAY rows built but not exercised end-to-end" (THIS session's T1), and the [X-VMON] KAT numbers (2.7e-13 vs band 1.1e-12; F3 out-of-box clamp finding, P2 row). (e) PROGRESS ORA/NEXT/BLOCCATO read: NEXT-1 = user decision on P-2 decoupling THEN F1 — RESOLVED by this session's order; BLOCCATO rows 1-5 (G5, acquisitions, preprint, P-2 decoupling, GENO dispatch) ingested for the T0 re-presentation. (f) ADVISORY_claims_to_code read (G1 obligation text + the C-1 THREE-WAY DECISION TABLE, adopted VERBATIM as the T2 verdict form; C-5 sign-test demotion; C-6 progress half = requires A' + G1). (g) ADVISORY_redteam RT-4 read (evidence + cheapest check: the one-page dJ/d(depth) derivation + mu-estimator band are PRECONDITIONS for any magnitude claim). DECLARED RESTART POINT: phase F1 of plan v3 (commit tag `[F1/GOVERNOR][PIANO/S22]`), first session of the F1 budget (counter at close: "F1 campaign k/2, session 1/3"). |
| 2 | GATE (pre-execution, standing directive `gate-pre-esecuzione`) — **(A) PLAN ADHERENCE.** This session is NOT an orphan step (R1): it is phase F1 of the RATIFIED plan v3 (D6 §0-pre), and PROGRESS NEXT-1 names exactly this opening (user decision on P-2 decoupling, then F1). Task mapping, every duty placed: T0 = BLOCCATO row 4 (ISS-2 decoupling option, D6 F1 text: "USER DECISION re-presented, not defaulted") — presented FIRST and AWAITED per the order; T1 = the S21 declared residual (S2 exit_cap / S6 CERT_PLAY rows not yet exercised end-to-end) — F1-window hygiene, rejector-verification duty per R5; T2 = the S20 retro-diagnosis F1-ENTRY row (S21 step 7 budget call; consumes the ARMED O4 instrumentation; verdict form = C-1 three-way table VERBATIM); T3 = the F1 ENTRY gates as written in D6 (derived implementation-magic bands = M0 bound (b) F1-entry duty; G1-surrogate = tier-invariant clause owner F1; [D1]-constrained variant BEFORE the decisive run; KS survey per sota-library-survey-directive, adopt-or-declare); T4 = the F1 decisive campaign (max 2, anchor ~1046 s, BRANCH-ADJUDICATED exit per D6); T5 = R3 + the ISS-5 counter + the T0 freeze rule. EXCLUDED by declaration, no work done on them: F1b (twin), U3'/F2a, S19 residues (b)/(c)/(d), census-lemma, PAP-RIM, G0/T2 review, ANY driver surgery (the A' margin constraint enters as a scipy trust-constr NonlinearConstraint through the EXISTING driver interface — formulation, not surgery; if the driver interface itself proves insufficient, that is a SECOND OBSTRUCTION and goes to adjudication with an alternative frame, never to surgery, per the session TERMS). **(B) UPSTREAM RIGOR AUDIT** (each point this session stands on, re-verified NOW at HEAD 1d9609a, not assumed): (i) INSTRUMENTATION PRESENT at HEAD: A1_CERT_ARGMAX / A1_REJ_SAVE / CERT_PLAY / exit_cap present in the committed carriers (a1_toc_variational_jax.py, a1_ideal_march_jax.py, a1_march_scan.py — grep-verified); [X-VMON] carrier validation/validity_monitor.py present with surface_min_val + the C-1 three-way table as documented verdict form. (ii) ENV verified LIVE: Python 3.13.14, numpy 2.5.1, scipy 1.18.0, jax 0.11.0 — matches the session TERMS and the S21 record env. (iii) RIGOR FOOTING of the objects consumed: the (G)/Lambda-form margin is EOS-general but NOT data-general (bound (a)) — the F1 walk is homentropic-homoenergetic tier-0 bell, INSIDE scope; bound (b) makes the derived magic bands an ENTRY duty — T3(i) is that duty, not an optional refinement; O1 UNDISCHARGED -> every multiplier statement this session emits is B-STATIONARITY wording until O1 discharges (RT-4 of record, M0 ledger); the K_disc~A_0 bridge stays CONJECTURE — the T4 monitor-along-the-walk IS its named falsifier, so BOTH monitor outcomes are verdicts, neither is failure; Rao-vs-Zucrow C+/C- naming inversion noted for every T2/T4 locus reading (Rao's "left Mach line DE" = OUR C+ traced from the lip). (iv) VERDICTS NOT RE-LITIGATED: S20 8/8 GENUINE stands RETRO-VALIDATED ([X-CDKAT]); o32 objective row stays NON-CONCLUSIVE under the pre-registered 0.5 cap; the S20 standoff reading stays CONSISTENT-WITH (RT-1) — T2 does not re-litigate it, it runs the pre-registered O4 discrimination the wording itself awaits ("until O4 localization runs"). (v) NUMBERS DISCIPLINE: the anchor ~1046 s/campaign and all S19/S20/S21 numbers cited in this log come from committed carriers/logs of record; advisory numbers stay advisory (R5). **(C) PRE-DECLARATIONS (R5 pre-registration integrity, fixed BEFORE any run):** [P1-T1] The T1 hygiene verdict semantics: the S2 exit_cap row must FIRE under a forced cap stimulus (N_NEWTON low or m_stop tightened) and the S6 CERT_PLAY row must FIRE under a perturbed-replay stimulus; a row that cannot fire under its stimulus = instrument NON-DISCRIMINATING, recorded as a FINDING (not silently accepted), fix-or-declare before T4. [P2-T2] The T2 verdict form is the C-1 three-way table VERBATIM (no new branches invented mid-run); the walk regeneration must be DETERMINISTIC (same seeds/path as [X-AKNO] attempt 3); if regeneration does NOT reproduce the S20 rejection set bit-compatibly, that mismatch is itself a FINDING of record (determinism defect) and the locus test runs on the REGENERATED set with the mismatch declared. [P3-T3] rho and mu_0 enter ONLY by derivation (rho from a stated conservativeness bound on the KS overshoot vs the true min; mu_0 from measured record quantities, e.g. the certification-floor/margin statistics of the S18/S20 record) — a tuned constant anywhere in the margin definition = R5 violation, blocked at commit; the G1 surrogate lands WITH its firing rejector or T4 does not start (tier-invariant clause is binding text). [P4-T4] Campaign counting: a campaign = one decisive A' optimization run consuming wall-clock at the anchor scale; T2 (retro-diagnosis) and T1 (hygiene) are NOT campaigns; the counter line lands in ORA at close regardless of outcome. [P5-SCOPE] No number produced this session becomes record except through committed carriers run under the declared env; if T0 selects option (a), the P-2 freeze artifact cites ONLY committed-carrier numbers (two-knob S19 + S21 registered norm/band) with the o32 NON-CONCLUSIVE row declared. **(D) VERDICT: gate pre-esecuzione PASS** — proceed to T0 (user decision, present and WAIT). |
| 3 | **T0 [USER DECISION, of record, dated 2026-08-11]: P-2 DECOUPLING (ISS-2) — the user selects OPTION (b), the RATIFIED DEFAULT**: P-2 draft freeze stays COUPLED to the F1 close on the DATED trigger (ISS-5 rule: the F1 budget expires at the close of the session containing the 2nd decisive campaign, max 3 sessions; counter "F1 campaign k/2, session m/3" in PROGRESS ORA at every close; freeze date = that session's calendar date; content = S19 two-knob numbers BY RULE at fallback, upgradable by an outcome-I A' result). The decoupled-freeze option (a) is DECLINED for this session. LOCKS re-presented dated 2026-08-11, unchanged, all still PENDING on the user: G5 (blocks submissions only — noted: even a frozen P-2 is not submittable before G5), acquisitions (residual fetch AIAA 2019-0197; Sternin/Shmyglevskii = O3 hard gate; Giles-Ulbrich x2 + Lozano = F4b entry; Moretti/Salas conditional), preprint/arXiv trigger. BLOCCATO row 4 will be CLOSED in PROGRESS at R3 (decision taken; the freeze trigger row remains as the ISS-5 rule). Proceed to T1. |
| 4 | **T1 [HYGIENE] EXECUTED — with a FINDING of record, fixed and re-proven.** **(a) FINDING (the exact gap T1 existed to close): the committed [X-A1IM] could not complete its own S6 row.** First end-to-end main run since the S21 rows landed: exit 1 — the S21 C2-F4 bookkeeping line `me_gap = abs(float(Max - Me))` (a1_ideal_march_jax.py) runs UNCONDITIONALLY on the exit branch, where the S.dec record/replay shield protects only the DECISION, not the reporting; under the S6 O3.1 vjp the play path is AD-traced, float() on a tracer raises ConcretizationTypeError, and the whole-march O3.1 identity was UNVERIFIABLE with the committed file (the S21 closure suite could not see it: suite green = carrier-exclusive, C4 annotation of record — this is that annotation biting live). FIX (minimal, idiom-conformant): the gap is tracer-guarded exactly like certify() (`jnp.abs` on the traced path, `abs(float())` unchanged on concrete paths — bit-identical record/replay behaviour). RE-RUN OF RECORD: **[X-A1IM] main FULL PASS, exit 0** — all S1-S6 rows including BOTH new rows exercised end-to-end for the first time: S2 "exit-Mach refinement converged (cap exit surfaced, C2-F4)" PASS, S6 "O3.1 probe replays Newton-certified (C2-F2)" PASS with the dot-product identity green. **(b) REJECTION CAPABILITY VERIFIED (gate [P1-T1] honored; stimulus runs declared, qualitative fires/holds only, no number of record):** STIM-1 (S2 exit_cap row) — attempt 1 with m_stop = 1e-14 DECLARED INSUFFICIENT STIMULUS: the wall-angle refinement CONVERGED to me_gap = 3.553e-15 BELOW the tightened stop (honest datum: the refinement is ~9 orders more accurate than its GENO-mirrored 1e-5 stop; the cap never engaged); hardened to m_stop = 0.0 (unreachable by construction; the S21 KAT-3 hardening pattern, no band moved) -> **exit_capped = True, the row FIRES**. STIM-2 (S6 CERT_PLAY row) — perturbed-P replay (the S6 probe's own h = eps^(1/3) step) under N_NEWTON = 0 with fresh solver compiles (A1._SOLVERS cleared — the module cache is keyed on cell kind only, N_NEWTON bakes at trace time; same binding-chain care as [X-CDKAT]): **cert_worst = 5.440e+07 > 1, the row FIRES**; CONTROL-A same perturbed replay at the default cap certifies (1.558e-02 — the firing is the starvation, not the perturbation); CONTROL-B same starved replay with CERT_PLAY off performs NO certification (cert_n = 0 — the pre-S21 silence the row closes). STIMULUS VERDICT PASS (exit 0). Both S21-built rows are now exercised end-to-end AND rejector-verified. TREE-STATE NOTE (declared at first commit, [P5-SCOPE] hygiene): three NEW untracked advisories appeared in validation/ relative to the session-open snapshot — ADVISORY_Scollapse_prompt_2026-08-11.md, ADVISORY_Sgauntlet_prompt_2026-08-11.md, ADVISORY_mean_swirl_panel_2026-08-11.md (ADR pattern, user-landed in the inter-session window); they are NOT in this session's mandate and are NOT ingested here — declared for the record, ownership to be assigned by the user/next order. Proceed to T2. |
| 5 | **T2 [S20 RETRO-DIAGNOSIS] EXECUTED — O4 DISCHARGED for the S20 instance: THE THREE-WAY TEST RETURNS BRANCH (c) UNANIMOUSLY (5/5). BOTH standing readings of the S20 standoff are FALSIFIED for the instance, and the K_disc ~ A_0 bridge falsifier FIRES.** **(a) DETERMINISTIC REGENERATION ([X-AKNO] attempt-3 path, instrumentation armed: A1_REJ_SAVE + A1_AKN_CERTLIM_SAVE + A1_CERT_ARGMAX=1):** the walk reproduced S20 EXACTLY — same knot insertions (x = 0.3146, 0.7026; 8 -> 10 dofs; warm-start deviation 4.4e-16), same certified-prefix numbers (J to 2.7775368e+07 at seg 5, KKT 5.056e+04), same crawl (ratchet monotone to the 1e-3 floor), same certifiability-limited exit (returned base J = 2.7775702e+07; corner mismatch 6.6826e-02 information-only), same rejection signature: 8 events over 5 DISTINCT designs, cert_worst {1.170, 2.458 x4, 1.060, 1.455, 1.698} in the S20 order, and the re-written record artifact s20_adaptive_design.json is BIT-IDENTICAL to the committed one (git diff empty). Exit 1 BY CONSTRUCTION (the S20-documented [D1]-untestable outcome — expected, declared). Artifacts of record: validation/s22_rejected_designs.json (8 events WITH the class payload — the T5-persistence completeness fix of this session: m_nodes + knot_xi now persisted, a design without its class was not re-recordable), validation/s22_certlim_base.json (the outcome-II returned base + class). INFRASTRUCTURE NOTE (declared, hygiene): two early regeneration attempts were killed and cleaned after a monitoring defect (git-bash kill -0 checks MSYS pids, not Windows pids — false EXITED events; two concurrent walks briefly ran and were terminated; the clean run 3 is the only one whose artifacts exist). **(b) [X-LOCD] THE C-1 THREE-WAY LOCUS TEST (new committed carrier, registry 130; KAT-CLS classifier known-answer 5/5 PASS; BASE-CTRL healthy: baseline W* field 1675 pts, m_ref = min val = 6.8837e-01, median 7.7147e-01, all positive; determinism row PASS; every re-record reproduces its rejection):** per-design results, pre-registered rules (fold-implicated iff val_min < m_ref/K_RICH = 1.7209e-01; on-chain iff dist <= STENCIL_RADIUS x local chain spacing): rej-1 (worst 1.170): argmax cell INTERIOR (3.9272, 0.1375) design_col 23, val(fail) = 7.133e-01 HEALTHY, field val_min 6.123e-01 INTERIOR and far off-chain (d 1.52 vs r_st 0.082) -> (c). rej-2 (2.458): argmax (3.9601, 0.0525) col 30, val(fail) 8.574e-01, val_min 6.190e-01 interior -> (c). rej-3 (1.060): argmax (3.9501, 0.0549) col 30, val(fail) 8.534e-01 -> (c). rej-4 (1.455): argmax (4.2491, 0.1418) col 26, val(fail) 6.512e-01 -> (c). rej-5 (1.698): argmax (3.9593, 0.0591) col 30, val(fail) 8.452e-01 -> (c). The outcome-II returned base re-certifies (worst 4.851e-01) with val_min/m_ref = 0.899 — NO depression at the frontier base. **VERDICT OF RECORD (O4 discharge, determinism OK): unanimous branch (c) — NON-FOLD mechanism.** (i) The C-1(a) DEF-signature reading for S20 is FALSIFIED (the argmin-val locus is never on the terminal C+, the failing cells are never on it, and no depression exists: min val 0.612-0.620 across all five designs vs threshold 0.172). (ii) The C-1(b) interior-caustic reading is EQUALLY falsified (no low-margin locus anywhere — 'interior caustic' requires depression; there is none). (iii) The mechanism is CLASS CONSTRUCTION, not physics: Newton stalls at HEALTHY-margin cells. HONEST LOCALIZATION DATUM beyond the S20 candidates: the stalling cells sit in the NEAR-AXIS region of design columns 23-30 (x ~ 3.93-4.25, y ~ 0.05-0.14 with y_t = 1) — NOT adjacent to the inserted knots on the wall; the S20 mechanism candidates (spline conditioning / knot-station mismatch) remain candidates but the localization adds the near-axis locus as the primary datum; mechanism identification is an F2 (engine) item, NOT this session's (no driver/engine surgery — S20 boundary). (iv) **THE K_disc ~ A_0 BRIDGE FALSIFIER FIRES for this instance** (M0 Part VI: 'val must approach its boundary where certification degrades, else the bridge is dead'): certification degrades at val 0.61-0.86, nowhere near 0 — for the S20 instance the certifiable-set boundary is NOT the validity boundary; K's binding constraint here is numerical-class, not physical-fold. CONSEQUENCE FOR THE GOVERNOR (recorded at R4): the margin governor keeps its role as the EXCLUSION guard for genuinely fold-approaching designs (front taxonomy (c), REQ-NONSTALL steering) but CANNOT capture the S20-instance certifiability frontier — the two boundaries are distinct objects on this instance. RT-1 wording resolution: the S20 standoff is no longer 'consistent with a DEF-sector optimum OR an interior-binding tier-1 optimum' — O4 has run and BOTH limbs are falsified for the instance; the standoff was a class-construction artifact. **(c) T4 PRE-REGISTRATION (R5, fixed NOW, before the campaign):** derived prediction — the margin constraint will be INACTIVE along the whole A' walk on this instance: the tightest ladder rung is mu_0_1 = m_ref/2 = 3.4051e-01 + gap 1.064e-02 (the GOVERNOR's m_ref = 6.810298e-01 over the design-wall BUCKET lanes, N = 3498 — a wider scope than [X-LOCD]'s design-wall-region m_ref 6.8837e-01; both declared, each carrier uses its own), while the measured min val at every design the S20 walk visited (baseline, five rejected, returned base) is >= 6.12e-01 >> that; FALSIFIER: the constraint activating anywhere in the campaign kills this prediction. Campaign protocol (declared in the carrier BEFORE the run): ladder runs TIGHTEST rung first with the MONOTONICITY STOP (activity is monotone in mu_0: inactive at the tightest rung => every looser rung vacuous; remaining rungs not run, mu = 0 identically, the RT-4 sign statement trivially mu = 0). Expected branch by derivation: certifiability-limited-under-constraint with margin inactive — a VALID DECLARED F1 exit (D6), with the localization evidence = (b) above; the campaign MEASURES it (dual-proof: the derivation is the theory half, the executed walk the evidence half). |
| 6 | **T3 [PRE-RUN DERIVATIONS] EXECUTED IN FULL — [X-MGOV] margin governor committed and green (stage derive FULL PASS, exit 0), every F1 entry gate discharged BEFORE the decisive run.** **(i) THE MARGIN, all constants derived or measured:** traced (G)/Lambda-form val field over EVERY W-dependent lane of the march (new ADDITIVE val_diag output of the jitted TOC replay — the exact cert_diag S21 pattern; the fan bucket is W-independent by construction, excluded with the reason stated); KS-min in shifted overflow-free form; MEASURED governor reference (design-wall BUCKET scope): N = 3498 lanes, m_ref = 6.810298e-01, median val 0.95301, q range [1415.5, 2897.1] m/s; ladder mu_0 = [3.4051e-01, 1.7026e-01, 8.5129e-02, 4.2564e-02] (= m_ref/2^k, pre-registered); rho = K_RICH ln(N)/mu_0_min = 766.83, gap ln(N)/rho = 1.0641e-02 = mu_0_min/K_RICH BY CONSTRUCTION; R-KS bounds verified on the measured field (KS = 0.6803943 vs vmin = 0.6810298, gap 6.36e-04 inside ln(N)/rho). R-GRAD: AD margin gradient vs two-step-Richardson FD directional derivative = 3.287e-06 inside the derived band 5.920e-05, corrupted-gradient control FIRES. **(ii) G1-SURROGATE REJECTOR-PROVEN (tier-invariant clause, owner F1 — DISCHARGED):** at a deliberate gross non-monotone wall, m = -3.964e+02 FINITE NEGATIVE with FINITE gradient (one component zeroed AND COUNTED — the REQ-NONSTALL survive+report contract); baseline rung-1 margin positive control m = 3.3988e-01; RECOVERY LEG: a short margin-constrained walk (run_trsqp WITH the margin NonlinearConstraint through the new margin_factory formulation entry — additive parameter, policy stack untouched) completes with steps taken (1 segment, nit 4, 14 evals, 79 s, KKT 1.427e+03, counters gm_nonfinite = 2 counted, NO stall/crash) — R-G1d PASS. **(iii) [D1]-CONSTRAINED VARIANT derived and committed BEFORE the run:** rel_c = |gJ_lip + mu gm_lip - cd|/|cd| as COROLLARY of the S20-registered constrained-KKT THEOREM (lip-component projection; reduces bit-for-bit to the unconstrained [D1] at mu = 0; outcome-I validity condition unchanged; mu under the B-stationarity qualifier until O1 discharges — RT-4 honored). **(iv) GENO IMPLEMENTATION-MAGIC BANDS derived (M0 bound (b) F1-entry duty DISCHARGED, recipe SOURCE-VERIFIED in GENO Rao_m.f90 boundaryfunction_solve: CENTRAL FD, dV_pert = 1.0 absolute, alpha = asin(1/M) with M_FLOOR; GENO returns val = 0 'on boundary' at |den| < 1e-10 where our monitor reports den — semantic difference recorded for the F1b cross-check):** derived band = K_RICH |Lam_FD(1.0) - Lam_FD(0.5)| + 100 eps |Lam_AD| per grid point (range [3.50e-07, 2.77e-05] over the baseline q-range); GENO-recipe FD sits INSIDE it (worst 0.371 of band) — the F1b cross-check leg band is armed; min |den| over the baseline field = 6.3277e-01 vs the 1e-10 guard -> the GENO den-guard CANNOT bite in-range (measured statement); PM landing window = F1b consumption, declared. **(v) SURVEY (adopt-or-declare, SOTA search of record):** standard KS with derived rho ADOPTED; adaptive-rho lineage (Poon-Martins SMO 2007; interior-point adaptive aggregation, Computers & Structures 2015; SAKS, ASME JMD 2018) NOT adopted with the reason recorded (tuning surface without accuracy gain when rho is derived and gradients are exact AD). R4: M0 Part VI [S22 REGISTRATION BLOCK] written (O4 discharge + bridge falsification + governor re-scoping + all derivations above); registry 129 -> 131 (+[X-LOCD], +[X-MGOV]), lint (xv) green on the exit code. |
| 7 | **T4 [DECISIVE CAMPAIGN A', campaign 1/2] EXECUTED — VERDICT PASS (exit 0), THE PRE-REGISTERED PREDICTION HELD, BRANCH ADJUDICATED = CERTIFIABILITY-LIMITED-UNDER-CONSTRAINT (a VALID DECLARED F1 exit, D6) with the margin INACTIVE.** Run of record ([X-MGOV] stage campaign; derive re-run green in-process first): start = the outcome-II certified base (m = 10 dofs, regenerated class), rung 1/4 = the TIGHTEST floor mu_0_1 = 3.405149e-01. THE WALK: 14 segments, 498 s (inside the ~1046 s anchor), SEVEN rejections ALL at cert_worst 1.531e+00 (ONE new design adjacent to the base — a sixth distinct frontier design, consistent with the S20 crawl geometry), ratchet monotone 5.0e-02 -> 1.0e-03 floor, reject-and-shrink EXHAUSTED -> returned base BIT-IDENTICAL to the start (J = 2.7775702e+07, KKT OPEN 2.072e+04), certifiability_limited = True; REQ-NONSTALL clean (nonfinite counters obj/grad 0/0, margin m/gm 0/0 — the constrained machinery steered, survived and reported at every trial point; NO stall, NO crash: the G1 tier-invariant duty holds on the decisive run too). MANDATORY O4 LOGS (of record): min val 6.185541e-01 at (4.2641, 0.1429) INTERIOR (d 1.641 vs r_st 8.05e-02) — the same healthy interior locus as T2; ACTIVE-CUSP CENSUS AT TERMINATION: 0 lanes active (0 on-chain, 0 interior, 0 clusters). MARGIN INACTIVE THROUGHOUT: the pre-registered T4 prediction (step 5c) HELD — its falsifier (constraint activation) did NOT fire. MONOTONICITY STOP fired as pre-declared: rungs 2-4 vacuous (activity monotone in mu_0), mu = 0 identically on the ladder — the RT-4 sign statement is trivially mu = 0 and B-stationarity reduces to the unconstrained KKT; [D1]-constrained metric NOT licensed (no outcome-I; validity condition honored), so [C-O33] stays OPEN-QUANTIFIED (unchanged — the byproduct closure required outcome-I). HONEST DATUM, declared: the printed 'multiplier estimate' -156.05 is a SUSPECT extraction (likely the lip-equality multiplier in scaled coordinates at a NON-converged point; ordering/sign convention of scipy res.v not verified) — information-only, consumed by NO claim; margin activity is measured by the census (the load-bearing quantity). **BRANCH ADJUDICATION (D6 F1 exit, of record):** (i) margin-active outcome-I — NOT the outcome (margin never active); (ii) H2-kill (interior fold binding) — NOT the outcome (no fold binds: census 0, min val healthy); (iii) H1-kill (multi-cusp) — NOT the outcome (0 clusters); (iv) **CERTIFIABILITY-LIMITED-UNDER-CONSTRAINT — THE OUTCOME**, declared WITH localization evidence (T2 branch (c) + this run's O4 log: the binding obstruction is the NON-FOLD near-axis class-construction frontier, owner F2). DUAL-PROOF COMPLETE on the session's central claim ('the margin governor cannot capture the S20-instance frontier'): theory half = the inactivity derivation (step 5c, from measured margins vs the pre-registered ladder), evidence half = this executed campaign with its census logs. F1 EXIT CONDITIONS: the D6 F1 exit gate names this branch a VALID EXIT ('certifiability-limited-under-constraint declared with localization evidence; binding locus CLASSIFIED via the mandatory logs' — both delivered). A SECOND campaign is measurably VACUOUS (monotonicity + the branch already measured); the F1 budget rule (ISS-5) allows up to session 3/3 since no 2nd campaign ran — the F1 EARLY-CLOSE decision (which fires the dated P-2 freeze BY RULE and requires adjudicating the standing C1 P-2-FREEZE-BLOCKER row: the named field-level rejector, carrier unbuilt, owner F2 — S21 step 6) is a USER DECISION, placed on BLOCCATO for the S23 opening. NO unilateral freeze executed (T0 rule (b) honored: trigger at F1 close, and F1 close is the user's call given the blocker row). |
| 8 | **T5 [SESSION CLOSURE, R3].** Commits: apertura+gate+T0+T1 = aec2c9f, T2+T3 = 0174bb9, T4+chiusura = (this). Lint (xv) green ON THE EXIT CODE at every commit (registry 129 -> 131: +[X-LOCD], +[X-MGOV]). **CLOSURE SUITE OF RECORD: 15/15 groups PASS in 127 s (exit 0)** — carrier-exclusive per the C4 annotation of record; on-demand carriers exercised THIS session by direct runs: [X-A1IM] main exit 0 (post-fix), [X-LOCD] exit 0, [X-MGOV] derive exit 0 AND campaign exit 0; the [X-AKNO] regeneration exit 1 BY CONSTRUCTION (the S20-documented [D1]-untestable outcome, expected and declared). **BUDGET COUNTER (ISS-5, mandatory): F1 campaign 1/2, session 1/3** — recorded in PROGRESS ORA. **SESSION VERDICT, honest.** The session delivered MORE than its mandate's minimum and one big verdict NOBODY ordered in advance: (1) T0 decided (b); (2) T1 found and fixed a real carrier-breaking defect the carrier-exclusive suite could not see, and rejector-verified both new rows; (3) T2 DISCHARGED O4 for the S20 instance with a unanimous branch (c) — both standing readings of the S20 standoff falsified, the mechanism is class construction with a NEW honest locus (near-axis, cols 23-30), and the K_disc ~ A_0 bridge conjecture is FALSIFIED by its own named falsifier; (4) T3 discharged every F1 entry duty with derived-only constants and rejector-proven G1; (5) T4 ran campaign 1/2 and measured the D6-valid exit branch (certifiability-limited-under-constraint, margin inactive, census 0) with the pre-registered prediction holding and the monotonicity stop firing as declared. DEVIATIONS FROM THE ORDER, declared: NONE of substance — the T4 ladder stopped at rung 1 by the PRE-DECLARED monotonicity rule (not a silent cap); the P-2 freeze was NOT executed because T0 chose (b) (trigger at F1 close) and F1 close is a NEW USER DECISION (BLOCCATO 6: early close at campaign 1/2 + the standing C1 freeze-blocker adjudication). ERRORS OF MINE IN THE RECORD, not buried: (a) the T1 STIM-1 attempt 1 was an insufficient stimulus (declared, hardened — the S21 KAT-3 pattern); (b) two early T2 regeneration attempts were killed after a monitoring defect of mine (git-bash kill -0 on Windows pids = false EXITED; briefly two concurrent walks — terminated, cleaned, declared; run 3 is the only artifact source); (c) the campaign's multiplier print is a suspect extraction (declared information-only, consumed by no claim); (d) an early log-ordering slip (T0 row inserted before the gate row, fixed in-session). DECLARED RESIDUALS with owners: near-axis class-construction mechanism identification [F2]; field-level rejector carrier (P-2 freeze blocker row) [F2]; X-SCANM replay probes [F2, inherited]; C4 mechanical closure [F1 window, inherited]; multiplier-extraction convention read [F1b window, before any mu-consuming test]. **NEXT-1 (S23): USER DECISION on F1 EARLY CLOSE** (fires the dated P-2 freeze BY RULE + C1 blocker adjudication; see BLOCCATO 6), then F1b (twin DEF, GENO external conditional) / F2 queue as ordered. The session ends with the F1 exit conditions REACHED at half the campaign budget. |
