# S37 — F3 opened: the entry ratified, T-GB de-rated, Rao's Table 1 from our own march (2026-09-24 evening, [F3/A1])

Owner's direction of the day, after the S36 closure: "ok penso che dovremmo riallinearci con
il piano e completare le richieste di F3. Su F2 abbiamo nulla di già pronto?", then, on the
proposal of this session (F3 before F2, the counter instituted, S37 = ratification + T-GB +
the Table-1 oracle), "ok procediamo con la tua proposta". Session mose-a1, main tree
`rde-nozzle-program`. Communication in Italian; this log in English (CLAUDE.md).

## 1. The decisions of record (BLOCCATO 20 (g), DECIDED)
- **F3 entry ratified.** The D6 F3 entry reads "RaoPlug S1/S2 fix in GENO landed OR
  single-oracle status (Rao 1961 spike Table-1) declared; plug/C- mirror Lambda-form margin
  PROVEN or declared PRACTICE conditional". Declared: the single-oracle status, measured since
  S26 ([X-RAOTB] tables usable, [X-RAOCS] his optimum from his first integrals, [X-RAOFN] our
  functional on his surface, [X-RAOTW] the dual-code twin, the plug O3.3 [X-RAOO3] /
  [X-RAOSQ]); the C- mirror margin declared PRACTICE conditional on the plug's net-cell fold
  margin [X-PMRG] (family-agnostic, the class measured in Humphreys' region R since S34).
- **The counter.** D6 ISS-4 "F3 session m/4" instituted: S37 = 1/4 (cap 3 h of decisive runs
  per session, at most 2 decisive optimisation campaigns per instance; at exhaustion the
  fallback "spike-only capture" fires by rule). The nine sessions tagged F3 before today were
  the authorized parallel de-risk ("spike-level plug-march AUTHORIZED parallel to F2") and do
  NOT consume it. Measured in-window (SR-12): `git log --format=%s | grep -o
  '\[F3/A1\]\[S[0-9S-]*\]' | sort -u` = S26, S28, S29, S30, S31, S32, S33, S34-S35, S36 (9
  tags); 76 commits carry `[F3/A1]` before this session's.
- **The order.** F3 before F2: F3's exit duties close on the present (homentropic, EOS-general)
  engine with the variable-gamma instance, while F2 is new construction (section 2). F4b stays
  order-interchangeable with F3 given F2 exit (ISS-6), unchanged.
- Open and still the owner's: BLOCCATO 20 (i), the Panov-Shvets member (not needed by S37: the
  T-GB exit leg took the de-rating branch, not the truncated-plug falsifier).

## 2. What exists for F2 (SR-9: one Explore agent, one round, 246k tokens, 99 tool uses)
F2 has not been opened (no "F2 session m/6" counter; the ORA header of the main chain is the
S-FOUNDATIONS-C4 closure of 2026-08-21). What exists came from the plug line:
- a rotational (s, h0) plug march [X-RMAR] (`a1_rot_march.py`, the three-family cell with a
  streamline foot; plug topology only, smooth stratification only, PRACTICE) and its adjoint
  test [X-O31R] (reverse vs FD, one design);
- the thermo tables 1-D in T (NASA, Cantera, gamma-const), frozen composition; the (q; s, h0)
  closure runs on the linear-interpolation class, not the C^1 class [X-THC1];
- the U3' literature base (`ADVISORY_rde_choking_2026-08-11.md`, five sources read in full);
- the S-FOUNDATIONS theorems ([T-DISC], [T-RED], [T-DCRX], NTF, CLG), all on the homentropic
  4-wide engine.
Missing entirely: the generalized data guard and G6 loud-reject, the stage-A audits (Crocco,
completeness, H-I2), the T0 flatness monitor, the contact/slip adjudication, the U3' monitor,
the GENO moc_gen twin (and GENO's rotational branch is unvalidated, MOC-27), the extended
(q; s, h0) fold margin, the methods freeze. Record hygiene found (not touched here, F2 text):
[X-RMAR]'s registry statement still says W-5 open while commit 4bbc374 closed it; the plan's
F2 paragraph cites "Stechmann 2018" (the record is JSR 56(3) 2019, `stechmann_2019`) and
"page-verify in course" for Paxson-Miki (done in the choking advisory); 47 commits carry
`[F2/A1]` without the phase being open.

## 3. The F3 exit ledger after S37

| D6 F3 exit duty | state |
|---|---|
| certified plug optimum | value and motion halves on our world (S28 [X-OWO3] / [X-OWS3]); the re-obtentions S34-S35 ([X-TPOS], [X-RAOHM]); OWED: the A/B at the nominal ambient from OUR throat posing |
| spike Table-1 oracle (M_E 2.4, gamma 1.23) in derived bands | CLOSED S37 [X-RAOPO] (sections 4-5) |
| at least one var-gamma or stratified plug instance | our world IS variable-gamma (NASA tables, [X-OWTW]/[X-OWO3]/[X-OWS3]): to be declared at exit |
| T-GB de-rated to THEOREM* with named hypothesis OR the 2-D truncated-plug falsifier | the de-rating branch taken S37 (section 6); the falsifier stays owed to its findings row |
| two-wall Veen (conditional stretch) | the march S36 ([X-TWMU], [X-MGDL]); the optimiser OWED |

## 4. [X-RAOPO] stage derive: Rao's Table 1 by our inverse march from his conditions, 9/9
`validation/rao1961_posing.py` + `rao1961_posing_cases.json` (new). Every Rao-world instrument
of record started from GENO's field; what was ours was ED ([X-RAOCS], [X-RAOFN]). The
contour upstream of ED -- his Table 1 -- had never come from our method.

**The construction.** His optimum is characterised by ED, the C- from the lip carrying his
Eqs. (6)-(7) down to his published terminus. Between the lip's centred fan (the corner
relation, exact at E) and ED the field is a Goursat problem between two characteristics: the
inverse march of [X-AFAN] with HIS ED as the terminal ray (resampled at uniform arc length),
the wall the streamline traced backward from D. Two measured changes to [X-AFAN]'s cell:
- the fan's C- rays turn through the vertical (theta - mu from -124 deg at M_i 1.05 to -33 deg
  at M_E): in the record frame the slope form is singular there (a stalled cell at C- -90.4
  deg, cond(J) 2e14, cert 4e8 at 241 rays); marched in the frame of the leading ray's flow
  angle (-51.4 deg, [X-FRMR]'s throat frame, origin at E) both families stay off the vertical;
- the compatibility rows scaled by q^3 ([X-FRMR]'s top-down cell; same root): the unscaled
  cell's damped Newton cannot reduce a norm dominated by the O(1e6) rows (cert 1e2-4e8 at every
  rung before the change).

| rung (rays, levels) | cert | m_lead / m_ED - 1 | eps | C_F (vacuum) | F_lead + push vs F_ED |
|---|---|---|---|---|---|
| (61, 61) | 0.035 | 2.3e-4 | 3.80949 | 1.58017 | 1.6e-4 |
| (121, 121) | 0.022 | 6.0e-5 | 3.80945 | 1.58034 | 4.3e-5 |
| (241, 241) | 0.025 | 1.5e-5 | 3.80944 | 1.58038 | 1.1e-5 |

His numbers: eps 3.81, C_F 1.58 (Table 3: 1.5804). The traced wall converges (2.7e-4 -> 8.0e-5
R_E between rungs; band_W 3.2e-4).

**The oracle.** At his 15 rows downstream of the leading ray's crossing (x >= -0.092; his first
row, T, lies upstream of it) our wall minus his radius reads +6.5e-4, -7.6e-5, +3.1e-4,
+8.6e-4, -2.4e-4, +1.7e-4, -2.5e-5, +9.4e-4, +7.5e-4, -6.7e-5, +1.5e-4, +7.3e-4, +4.4e-4,
+8.8e-4, 0 (D): max 9.4e-4, mean +3.6e-4. The band is his table's OWN class measured at T:
his Eq. (12) with his eps 3.81 puts T on the straight sonic line at (-0.10785, 0.91547); he
prints (-0.109, 0.9165) -- 1.15e-3 / 1.03e-3 off his own equation. Graded:
- ID-4 max |dr| 9.4e-4 <= 1.15e-3 (his class at T) + band_W = 1.47e-3;
- ID-4b mean 3.6e-4 <= his printing 5e-4 + band_W.
Upstream of the leading ray (declared: straight at its flow angle to the sonic line) our T is
(-0.10819, 0.91520): between his print and his Eq. (12).

**Rejectors.** R-1 the planar march (delta 0) on the same data cannot build the wall (no
crossing on the first upstream ray: the construction fails, declared as an infinite miss);
R-2 the same construction for theta_E moved by +0.25 deg lands 5.2e-3 R_E from his table
(3.5x the band) -- the check discriminates a neighbouring optimum.

For scale: GENO's RaoPlug run of record reproduces Table 1 at mean 2.44e-3 ([X-RAOTW]); our
construction at mean 3.6e-4.

## 5. [X-RAOPO] stage walk: the TR-SQP re-obtention from our start
The [X-RAOSQ] v3 instrument verbatim (its stations, spline, frozen near-cut zone 35 percent,
tip pinned at his D, J_replay, segmented TR-SQP, the AD-Hessian bands per eigen-direction),
the constants asserted equal to the record's at run time, with ONE change: the start line at
x0 = 0.30 is the vertical cut through OUR inverse field (241, 241) -- 61 rows from the traced
wall (y 0.52449) to ED (y 0.80240) -- and the reference wall is HIS Table 1 (the spline
through his points) instead of GENO's contour. No GENO field is read anywhere.

Bands (his contour under this instrument, 241 x 241 field): e_rep 7.0e-5 R_E; J_fit
4.56012553e6 N (K 81) vs 4.56812491e6 (K 161); gradient floor 2.9e4 -> gtol 1.15e5; the AD
Hessian's seven directions carry bands 7.3e-3 (softest) .. 6.6e-4 R_E; band_J 3.26e4 N.

| check | perturbed start (1.5 %, [X-RAOSQ]'s) | generic chord start |
|---|---|---|
| start: distance to his wall; directions outside their band | 5.1e-3; 3 of 7 (worst 8.0x) | 7.4e-3; 1 of 7 (worst 1.1x) |
| every accepted base certified | PASS (0.065) | PASS (0.065) |
| convergence |grad J|inf <= gtol | PASS (94) | PASS (2.3e4) |
| RETURN: every eigen-direction inside its band | PASS (worst 0.20) | PASS (worst 0.48) |
| VALUE |J - J_fit| <= band_J | PASS (+15 N) | PASS (-21 N) |

RE-0 his contour marched from our start certifies (0.065): PASS. RE-3 the two landings
coincide (worst 0.38 of band, sup 1.7e-3 R_E): PASS. Walk of record: 10/11, 3104 s
(`_rao1961_posing/run_walk_2026-09-24.log`, record `walk_2026-09-24_1732.json`).

**R-3, the sign-flipped rejector: FAIL in the walk of record -- it never moved.** Every
trial of the minimising driver from the perturbed start was an uncertified record (cert
8e15 .. 2e18), so the driver reverted and halved its radius fourteen times, the record's
calibrated budget, and ended where it began (5.054e-3 = 5.054e-3). The same instrument in
Rao's world from GENO's start shows the same sequence and took its first certified step
only at segment 11 of 14 (`_rao1961_twin/run_of_record_sqpret_2026-08-27.log`): the
budget calibrated there is not enough from our start. ATTRIBUTION (stage rejector, the
driver alone with the budget raised, declared knob RAOPO_SEG_R1): with the budget 30 the minimising driver keeps
rejecting uncertified trials until its radius falls to ~2.5e-3 R_E (segment 18), takes its
first certified step at segment 19 (J 4.55292e6 -> 4.54116e6 N, -11,762 N) and ends 7.58e-3
R_E from his contour against the start's 5.05e-3 -- it WALKS AWAY: R-3 PASS at budget 30
(`run_rejector_budget30_2026-09-24.log`, `rejector_2026-09-24_budget30.json`, 1664 s). The
cause is the radius the calibrated budget reaches (6.7e-3), not the objective: from our start
the thrust-DEcreasing direction breaks the march at every larger step. Verdict of record: walk
10/11 at the record's budget, R-3 attributed and passing at budget 30 (declared knob).

## 6. T-GB de-rated to THEOREM* on a named closure
The finding theory-core:F3-TGB-class-inflation-rejector-scope (CONFIRMED since the 2026-08-07
audit) named three defects: the non-legend class "THEOREM-grade" in M0; a registry THEOREM over
a self-declared proof sketch with no lemma for non-interface-originated exit flux in "ANY
topology"; a falsifier that sweeps only the 1-D exit family. What the sketch needs, read against
the ergodic note's pointwise lemma GBE-L1 [S-GBE]: every streamline crossing the exhaust surface
must issue from the interface (an ejector-type topology expels ambient fluid whose thrust flux
the interface never supplied -- GBE-L1 bounds such a point by V_env of the AMBIENT state, which
is positive on the sonic branch) and the exhaust surface must carry the axial margin u_x >= c
(E3). That is a flow-class model closure, neither C-D25U nor C-MAJDA (SCAFFOLD §3 rule 4).
- Registry: new conditional [C-GBCS] (kind conditional, class SCHEMA, model closure, falsifier
  the shape-level test); [T-GB] class THEOREM -> THEOREM*, inherits [C-GBCS], scope restricted
  to the class, proof anchor the DE-RATING OF RECORD, falsifier restated (the executable scan =
  the per-streamtube rejector; the full-quantifier falsifier named, open).
- M0 Prop. 7: the header carries the class of record; the dated DE-RATING OF RECORD note after
  the proof (the corollary's "all topologies" reads "all topologies in the class").
- P-1 draft sections 5-7 (the finding's trigger document): the two class labels and the
  statement's scope follow the registry. SCAFFOLD §1 supersession note: C-GBCS listed among
  the model closures.
- Findings row -> DOWNGRADED: defects 1-2 discharged, defect 3 restated; RESIDUE the shape-level
  falsifier (a certified 2-D truncated plug with base recirculation, inside the class, against
  the capped ceiling), owner the truncated-plug instance.

## 7. Declared
- Three FORWARD posings of his sonic start were built and measured before the inverse
  construction, and abandoned: (i) his straight sonic line carried to M_i > 1 as a transverse
  start line -- no axisymmetric solution (the IVL triangle found subsonic roots, cert 3e12,
  the fan's first rays crossed the marched leading ray); (ii) a straight uniform leading C- to
  the wall -- the fan's first ray decelerated to M 1 along it (cert 6e10); (iii) his sonic line
  as the fan's leading ray (M_s 1.05, fan from M 1.15) -- certified only at 31 rays (61 rays:
  the free-jet triangle folds at the edge, cert 5e14) and read theta on ED 2-4 deg off at that
  resolution. The inverse construction has the corner exact at E and his ED exact as data.
- His published terminus (R_D/R_E 0.137) is used as D; it leaves his own Eq. (9) at a residual
  ([X-RAOCS]); his first row T is upstream of our leading ray and read, not graded.
- The class of his table at T is measured from his own Eq. (12) and his printed eps -- a
  property of the reference, not a chosen tolerance; the mean is graded against his printing.
- R-1 fires by the construction failing, not by a finite miss (declared).

## 8. HANDOFF
- Files of record: `validation/rao1961_posing.py` + `rao1961_posing_cases.json` (new); this
  log; registry [X-RAOPO], [C-GBCS], [T-GB] re-printed; M0 Prop. 7 note + Part VI addendum S37;
  P-1 sections 5-7 labels; SCAFFOLD note; findings row; PROGRESS (R20, R37, R38, BLOCCATO 20 g,
  ORA/NEXT-F3) and its archive; D6 F3 status paragraph; the index row.
- NEXT in F3 (counter 1/4 used): (1) the plug optimum at the NOMINAL ambient from OUR throat
  posing, margin-active, with RE-2/RE-3 (the exit duty (a)); (2) declare the variable-gamma
  instance; (3) the two-wall TR-SQP and Veen 1974 Table 3 (the conditional stretch); the
  truncated-plug falsifier with the base closure when BLOCCATO 20 (i) is decided.

## 9. Conformity
**The owner's condition** ("riallinearci con il piano e completare le richieste di F3";
"procediamo con la tua proposta"): F3 opened with its entry declared and its counter; this
session's exit legs:

| leg | carrier / edit | verdict |
|---|---|---|
| spike Table-1 oracle in derived bands | [X-RAOPO] stage derive | 9/9 PASS |
| the TR-SQP re-obtention from our start | [X-RAOPO] stage walk | 10/11 at the record's rejector budget; R-3 attributed, PASS at budget 30 |
| T-GB de-rated with a named hypothesis | [T-GB] THEOREM*, [C-GBCS] | done; the shape falsifier owed |

The owner's check page: `validation/_rao1961_posing/figs/17_rao_table1_ours.png` (generator
RDE/handoff/f3_2026-09-24/raopo_fig.py, presentation only). On the owner's request the
untracked figures of failed or ill-conditioned runs were deleted (list in
RDE/handoff/F3_HANDOFF_2026-09-24.md, section 5); the committed figures were not touched.

**Conformity** (CLAUDE.md R1-R7, SR-1..SR-12):
- Branch `rde-nozzle-program`, main tree; identity AlexFalco5; explicit pathspecs; GENO never
  added. Push on the owner's standing word of 2026-09-24, after the commits.
- R1: `[F3/A1][S37]`, F3 session 1/4. R3: this log; PROGRESS ORA-F3 / NEXT-F3 in place of the
  parallel blocks, the main NEXT carrying the order F3 -> F2; the outgoing blocks, rows R20 /
  R37 and BLOCCATO 20 (g) verbatim in PROGRESS_ARCHIVE under the 2026-09-24 notte banner with
  the DELTA CENSIMENTO note; R38 new; D6 F3 status paragraph; HANDOFF (section 8,
  RDE/handoff/F3_HANDOFF_2026-09-24.md). SR-12: the F3 session count measured in-window
  (section 1).
- R4: M0 Prop. 7 DE-RATING OF RECORD; M0 Part VI LINE ADDENDUM S37, three items with classes.
- R5 / SR-2: registry rows [X-RAOPO] (kind carrier, ondemand env=jax, pass 2026-09-24, suite
  none, falsifiers named), [C-GBCS] (model closure, SCAFFOLD §3 rule 4 justified in M0),
  [T-GB] re-printed (class THEOREM*, inherits, falsifier); findings row DOWNGRADED; P-1
  sections 5-7 labels; SCAFFOLD supersession note. SR-1: the ADVISORY_INDEX row. SR-5: no A1_*
  flag (RAOPO_* are instance knobs of the carrier). SR-9: section 2. SR-4: no new glossary
  token.
- SR-6: files of record committed: `validation/rao1961_posing.py`,
  `validation/rao1961_posing_cases.json` (new; numeric ratchet clean); this log; the index;
  PROGRESS + archive; D6; M0; the registries; P-1; SCAFFOLD. Not committed, by design: the
  figure and its generator, the stage JSONs and logs in `validation/_rao1961_posing/`
  (artefact directory). Declared: stage rejector was added after the walk of record (a new
  function; derive and walk unchanged).
- Lints on the tree of the first commit (measured 2026-09-24 ~18:05): numeric PASS (126 files,
  0 ratchet violations); advisory index PASS (129 rows, 0 violations); claims lint 4
  violations = PROGRESS and D6 citing [X-RAOPO] and [C-GBCS], which land in the second commit
  (the ordering rule). Full suite on the same tree: 15/23 = the seven environmental reds
  (xiii, xiv, xvi, xviii, xix, xxii, xxiii) + (xv) for the ordering reason, 85 s; (xvii) PASS
  (52 carriers accounted); data/phase_diagram.*, data/q_mapping.* and
  figs/phase_diagram_op11.png restored with git checkout before the commit.
- Two commits, as the line's practice: (1) code + data + this log + index + PROGRESS/archive
  + D6; (2) registry + findings + M0 + P-1 + SCAFFOLD + this log's measurement line.
