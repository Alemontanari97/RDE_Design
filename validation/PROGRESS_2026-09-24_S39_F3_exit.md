# S39 — F3 CLOSED: the exit review, the entry duties the S37 ratification missed, the residuals with owners (2026-09-24 night, [F3/A1])

Owner's call: "chiudiamo F3", given the status of the phase after S38 (value half of the plug
optimum certified, the finder campaign posable but not launched, two F3 sessions of four used).
Read as: close the phase now, carrying what is open as named residuals -- not as a request to
spend the last decisive campaign of the plug instance. This session runs no decisive
computation; it is the closure of record. Session mose-a1, main tree `rde-nozzle-program`.
Communication in Italian; this log in English (CLAUDE.md).

## 1. The exit review
D6 F3 EXIT (the plan's words) against the record:

| exit item | evidence | verdict |
|---|---|---|
| certified plug optimum ... in derived bands | VALUE: [X-OWNM] (S38) -- the ideal spike of our world, built by our own inverse march at the NOMINAL ambient, attains [T-GB]'s ceiling mdot V_id (Richardson limit +2.2e-5, band 3.5e-4), in class in region R, so no design of the class beats it beyond the band; the optimiser's re-obtention certified on literature optima from their own starts ([X-TPOS] Humphreys Tables 2 and 4, [X-RAOHM], [X-RAOSQ]) and on Rao's Table 1 from OUR start ([X-RAOPO] walk, RE-1 from a generic chord, RE-3); the motion half in our world at the member's ambient ([X-OWS3], basin) | MET in value; the finder at the nominal ambient from a far start is residual R-F3-1 |
| spike Table-1 oracle (M_E 2.4, gamma 1.23) in derived bands | [X-RAOPO] (S37): Rao's Table 1 by our inverse march from his optimality conditions, within his table's own class; eps and C_F at his printed digits | MET |
| at least ONE var-gamma or stratified plug instance | every our-world plug row marches the CH4/O2 frozen NASA tables (gamma(T) variable): [X-OWTW], [X-OWO3], [X-OWS3], [X-OWNM] | MET (declared S38) |
| T-GB de-rated to THEOREM* with named hypothesis OR the 2-D truncated-plug falsifier executed before the first certified plug optimum ships | [T-GB] THEOREM* inheriting [C-GBCS] (S37), before the S38 certification | MET by the de-rating branch; the shape falsifier is residual R-F3-3 |
| two-wall Veen = conditional stretch | the two-wall march certified ([X-TWMU], [X-MGDL], S36); no two-wall optimisation exercised | NOT TAKEN (conditional); residual R-F3-2 |

The F3 fallback ("spike-only capture") did not fire: the counter was not exhausted and the
exit items are met or carried with owners.

## 2. The F3 ENTRY duty rows the S37 ratification missed (declared)
D6 §0-pre, S-GAUNTLET ABSORPTION ADDENDUM, RATIFIED DUTY ROWS: "F3 ENTRY : DUTY-14 a-B2
separation-margin carrier-leg assignment (agenda row); DUTY-11 two-wall translation-nullspace
duty (CONDITIONAL on the two-wall stretch); DUTY-1(b) ranking-stability check (thermal
q-drivers vs optimizer DOFs)", with the ledger's rule "silent drops void this verdict"
(`validation/ADVISORY_Sgauntlet_generality_ledger_2026-08-11.md`). The S37 ratification read
only the F3 paragraph's ENTRY line (single-oracle status; the C- mirror margin) and did not
dispose of these three rows: a PROCESS omission, found at this closure and corrected here, in
the open, before the exit is recorded. Dispositions:
- **DUTY-14 (a-B2): ASSIGNED -- owner F5a entry adjudication.** The row is the Campaign A
  advisory's (III)(a-B2): constraint activity governed by the low-k tail of the phase measure
  (a separation-margin state constraint g_sep <= 0 for mu-a.e. xi can reintroduce measure
  dependence), CARRIER-A K3. F3's certified plug optimum is posed at ONE steady state (the
  nominal ambient, no phase measure), where a tail-governed constraint cannot be active; the
  measure mu, CARRIER-A's conventions (K4, owner F5a) and the separation monitor (a-B3, F2a G6)
  live downstream. The assignment is the owner's to ratify at the F5a entry adjudication; until
  then the census row R12 reads ASSIGNED, not SCHED.
- **DUTY-11: NOT FIRED.** Conditional on the two-wall stretch being exercised; F3 built and
  certified the two-wall MARCH only, no two-wall optimisation ran. It travels with residual
  R-F3-2 as that stretch's entry duty (near-nullspace analysis of the two-wall reduced Hessian +
  identifiability declaration).
- **DUTY-1(b): NOT EXECUTABLE in F3's model class -- disposed by DUTY-1's own declaration
  branch.** The plug line is adiabatic and inviscid: no heat-flux driver exists on it, so the
  ranking of plug designs against thermal q-drivers cannot be checked here. DUTY-1's accepted
  disposition offers "the RK-E-pattern 'thermal constraint unmodeled = named industrial gap in
  EVERY claim'": every F3 claim is hereby scoped THRUST-ONLY, THERMAL SURVIVABILITY UNMODELED
  (named industrial gap). The ranking-stability check fires at F5 entry, its second firing per
  the ledger ("F3/F5-entry"), where the per-phase peak-flux surrogate row of DUTY-1(a) lives.

## 3. Residuals of the phase (named, owned, triggered)
- **R-F3-1 the finder at the nominal ambient.** The tournament [X-PTRN]'s finder campaign from
  its far start C (0.84 m from the member, IN CLASS in region R, S38 reading) with the class
  constraint in region R -- multi-hour; the second and last decisive campaign D6 ISS-4 allows the
  plug instance, not spent. Owner: the next plug-optimiser campaign (any phase). Trigger: a claim
  that the optimiser finds the plug optimum from a far start in our world, or F5 entry.
- **R-F3-2 the two-wall stretch.** The TR-SQP with the shroud as a design variable (its push +dy
  in J_replay), RE-1 on Migdal's pair from generic starts, Veen 1974 Table 3 from our own posing;
  DUTY-11 fires at its entry. Owner: the first session that puts a shrouded plug in the design
  space. Trigger: a two-wall design claim.
- **R-F3-3 the T-GB shape falsifier.** A certified 2-D truncated plug with base recirculation,
  inside [C-GBCS], against the capped ceiling; its base closure waits for BLOCCATO 20 (i)
  (Panov-Shvets: ambient or state at D). Owner: the findings row
  theory-core:F3-TGB-class-inflation-rejector-scope (DOWNGRADED, S37).
- **R-F3-4 [X-PTRN]'s registry row.** Never written (S38 finding); to be coined with its derive
  re-run. Owner: the session that runs a1_plug_tournament.py next.
- **R-F3-5 DUTY-1(b)** at F5 entry (section 2).

## 4. Budget of record
- F3 sessions: 3 of 4 (S37 entry + Table-1 oracle + T-GB; S38 value certificate; S39 this
  closure, no decisive runs). Measured in-window: `git log --format=%s | grep -c
  '\[F3/A1\]\[S3[789]\]'` = 4 commits before this session's.
- Decisive campaigns on the plug instance: 1 of 2 (S30's overnight tournament leg).
- Decisive runs: S37 ~1.5 h, S38 ~50 min, S39 none -- inside the 3 h per-session cap.

## 5. What F3 hands on
The certified plug machinery (the march with its fold margin and region-R class, the TR-SQP
driver with backtracking and restoration, the throat posing, the inverse fan, the two-wall
march), its oracles (Rao 1961 Table 1 from our march; Humphreys 1971 Tables 2 and 4; Migdal's
perfect annular nozzle; the source-flow channel), the value certificate of the plug optimum at
the nominal ambient, and the scope qualifier (thrust-only; thermal survivability unmodeled).
Next in the main chain (PROGRESS NEXT): S-PRES, then F2 block 0 (counter 0/6), per the owner's
order of 2026-09-24 (F3 before F2).

## 6. Conformity
- Branch `rde-nozzle-program`, main tree; identity AlexFalco5; explicit pathspecs; GENO never
  added; push on the owner's standing word.
- R1: `[F3/A1][S39]`, the F3 closure (session 3/4, no decisive runs). R3: this log; PROGRESS:
  the F3 block replaced by the closure block, the residuals block, the main NEXT back to S-PRES
  then F2 block 0; R38 CONSUMED, R12 ASSIGNED, BLOCCATO 20 (j) CLOSED -- in place, the outgoing
  text verbatim in PROGRESS_ARCHIVE under the 2026-09-24 notte (S39) banner with the DELTA
  CENSIMENTO note; D6 F3 CLOSED paragraph and the duty table's dated disposition note; the
  handoff RDE/handoff/F3_HANDOFF_2026-09-24.md (section 7).
- R4: M0 Part VI LINE ADDENDUM S39, two items with classes (the thrust-only scope of the phase;
  the phase gate reads its duty rows).
- R5 / SR-2: no claims-registry row (a phase exit is not a claim); four findings rows minted
  (one DISCHARGED process omission, three open residuals with owners and triggers). SR-1: the
  index row. SR-12: the F3 commit count measured in-window (section 4).
- SR-6: files committed: this log, the index, PROGRESS + archive, D6, M0, the findings registry.
- Lints and suite: see the measurement line below.
- Measured on the tree of the commit (2026-09-24 ~22:40): numeric lint PASS (127 files, 0
  ratchet violations); claims lint PASS (0 violations); advisory index PASS (131 rows, 0
  violations); findings lint 0 violations on the findings rows (254 entries, 211 open; the 78
  are the dead literature paths of record); FULL suite 16/23 = the seven environmental reds
  (xiii, xiv, xvi, xviii, xix, xxii, xxiii), 85 s; data/phase_diagram.*, data/q_mapping.* and
  figs/phase_diagram_op11.png restored with git checkout before the commit. One commit: no
  claims-registry row lands, so the ordering rule does not apply.
