# ADVISORY — DEF-equivalence panel verdict of record (2026-08-07,
# workflow wf_706f7901-32d: 3 experts + dedicated refuter + judge,
# CONVERGED round 1/3; to be absorbed at S21 opening)

STATUS: advisory (untracked, ADR/PANEL pattern). The session lead's
unqualified conjecture (EQ) is REFUTED AS STATED; the converged
replacement is the split statement EQ-v2 below, overall rigor class
CONJECTURE with one THEOREM component (S4) and a pre-registered
falsifier. KEY CONSEQUENCE FOR S20's RECORD (REWORDED 2026-08-11
per red-team RT-1, ADVISORY_redteam_2026-08-11.md — the original
"PREDICTED SIGNATURE of a DEF-sector optimum" was judge-added
synthesis no panel position supports): at any FIXED certification
floor mu_0 > 0 the exact DEF wall is REJECTED at fine mesh — the
S20 frontier standoff is CONSISTENT WITH a DEF-sector optimum AND
EQUALLY with an interior-binding tier-1 optimum until O4
(localization) runs; the 41.8% residual concentration at the
ATTACHMENT is itself suggestive of interior binding. The standoff
is not a refutation of the DEF picture; neither does it
discriminate for it.
RED-TEAM NOTE (2026-08-11, RT-6): "CONVERGED round 1" below means
JUDGE-ADJUDICATED IN ONE ROUND — the three expert-vs-refuter splits
were resolved by judge reinterpretation, never adversarially
reviewed; the corrections in this header and inline below restore
the raw positions.

## AGREED STATEMENT (EQ-v2, verbatim from the judge)

EQ-v2 (split form; supersedes the session lead's unqualified EQ, which is REFUTED as stated). SCOPE: fixed (eps,L) in the DEF regime (the boundary-valid smooth Rao construction cannot attain them), bell single-wall family, fixed throat/kernel data, homentropic-homoenergetic core (td%solve(q) one-parameter state map, per the periodic-wave scoping), fold margin m = the EOS-general Lambda-form (zero set Lambda*tan(alpha)*sin(theta) = sin(theta-2alpha)), which for a perfect gas reduces EXACTLY to Rao-Beck Eq.(4) [THEOREM — algebra judge-verified against Rao_m.f90 L38-55 and the page-verified Eq.(4); nonvanishing connecting factor, zero sets identical]. DIRECTION A [CONJECTURE on SCHEMA footing]: the classical Rao-Beck DEF design — single PM jump at D' on the terminal C+ characteristic landing exactly on the validity boundary, E fixed by DE-BD mass equality — is, in the joint continuum limit (h->0, KS rho->inf, certification floor mu_0->0; limit order unestablished), a margin-active KKT point of the direct problem max J s.t. {eps,L}, m>=0, with a single active cusp at D' and mu>=0 pricing shock-freeness; its fold touches the computed domain only at D' and the interior certifies pointwise (under interior-fold-freeness of the inverse net, H-int). At any FIXED floor mu_0>0 the exact DEF wall is REJECTED at fine mesh — the S20 frontier standoff is the predicted signature, not a refutation. DIRECTION B [CONJECTURE, weaker]: conversely, a margin-active KKT point of the direct problem coincides at first order with the classical DEF construction ONLY under H1 (single active cusp, nondegenerate Danskin derivative), H2 (cusp on the OPEN terminal characteristic strictly between axis and lip, same-family fold = classical D'; reflected-family/axis and lip-corner bindings and interior caustics excluded), H3 (MFCQ via the jump-depth direction + strict complementarity mu>0), H4 (matched admissible classes: throat data fixed as classical, wall class dense incl. curvature-jump points, wall-dof variations spanning surface-flow variations), H5 (genuine-margin protocol, certdiag-style, excluding budget/table artefacts), H6 (local uniqueness of the classical DEF at the given (eps,L) — 2 classical dofs theta_B + s_D' pinned by 2 constraints, generically isolated, unproven), H7-SEL (ADDED 2026-08-11 per red-team RT-3: selection/global-max among margin-active KKT points of the DIRECT problem — the refuter's mechanism M5, which NO prior hypothesis covered; H6's "generically isolated" permits multiple classical roots, so without H7-SEL two direct KKT points could match different classical roots and "coincides" is ill-posed. DECLARED WEAKENING, same fix: Direction B as originally adjudicated said classical DEF OPTIMUM; the judge's text silently weakened it to "construction" — the weakening is now DECLARED: without H7-SEL and O2/O3, Direction B speaks of the classical DEF CONSTRUCTION, not its optimality). FIRST-ORDER DICTIONARY [SCHEMA]: tangential stationarity <-> f2 = V*cos(theta-alpha)/cos(alpha) = const on D'E (Rao-Beck Eq.(1) = Rao 1958 Eqs.(11)-(13), fixed-eps replacing Eq.(14) transversality by lambda_e = the measured dJ/dy_lip corner identity); m=0 at the cusp <-> jump-to-boundary Eq.(4); mass matching <-> marched-field conservation identity (Profile_m A4 block — costs the direct side nothing); mu>=0 <-> jump-depth minimality dJ/d(depth)<=0 at the boundary — a direct-side SURPLUS PREDICTION absent from the page-verified classical corpus (classical DEF optimality rests on Shmyglevskii 1981, cited via Rao-Beck only, citation-forbidden beyond their text; Rao-Beck themselves assert superiority only by computed comparison, Table 2). OVERALL RIGOR: CONJECTURE, resting on one THEOREM (Lambda-form/Eq.(4) reduction), one THEOREM* (Rao 1958 control-surface duality, classical, not re-proved), SCHEMA correspondences (S1 containment, S2 dictionary), with a pre-registered decisive twin falsifier.

## SUB-QUESTION VERDICTS

### S1
SPLIT VERDICT (REWORDED 2026-08-11 per red-team RT-2 — the original glued two limbs and manufactured unanimity):
LIMB-2 (fold exactly on the domain boundary at D', interior certifies pointwise): HOLDS, unanimous including the refuter's explicit concession of the continuum fold-on-boundary claim; SCHEMA, conditional on interior-fold-freeness of the inverse net (H-int, asserted computationally by Rao-Beck, not proven).
LIMB-1 (closure membership: does the DEF wall exist in cl(K)?): OPEN pending approximant existence — the refuter's verdict-carrying gap (strictly-margin-positive approximants must pass through the Eq.(4)-invalid strip; no page-verified theorem pins either limb); the original "settled as containment in bd(A_0) = cl{m>=0}" presumed exactly the disputed limb equivalence and is WITHDRAWN as a settlement.
Fixed-floor exclusion, attribution corrected: the REFUTER argues DEF is outside cl(K(mu_0)) at any fixed floor; the experts are CONSISTENT WITH but do not all state it (Expert 2: "NOT in K(mu_0)", no closure; Expert 1: DEF IS in closure(K_h); Expert 3: joint-limit distance only). Residual obligations: approximant existence (limb-1) and the h->0/mu_0->0 limit order.

### S2
(RED-TEAM ANNOTATION 2026-08-11, RT-5: the refuter's raw position was an adjudication-REFUSAL — "neither side possesses a derived first-order condition at the needed generality, so the claim CANNOT CURRENTLY BE ADJUDICATED; verdict OPEN" — not a concession; "no term is disputed" is true only because the refuter disputed the DERIVABILITY of the terms, not their identity. The HOLDS-at-SCHEMA label below is the judge's one-round rendering; quote it only with this annotation.)
HOLDS at SCHEMA level under H1-H5: the term-by-term dictionary is settled — tangential stationarity <-> Eq.(1)/f2-invariant (Rao 1958 Eqs.11-13, with fixed-eps lambda_e replacing Eq.14 transversality = the measured dJ/dy_lip identity); m=0 at cusp <-> jump-to-boundary Eq.(4); mass matching automatic (A4 marched identity); mu>=0 <-> jump-depth minimality, a direct-side surplus prediction with no page-verified classical counterpart. Classical side at fixed (eps,L) is constraint-pinned (0 residual dofs), so its optimality content is exactly these conditions. The refuter's OPEN targets theorem-grade derivation only (boundary-touching cusp adjoint via Danskin, J_wall<->C_Fv momentum identity, Rao duality in the certified class) — conceded by all as obligations O1-O3; no term of the dictionary is disputed. Theorem status OPEN.

### S3
HOLDS — the hunt succeeds: extra hypotheses are NECESSARY and the unqualified (EQ) is refuted as stated (all four agree in substance; the refuter's FAILS is the same content under the opposite labeling convention). Unexcluded mechanisms absorbed as H1-H6 PLUS H7-SEL (CORRECTED 2026-08-11 per red-team RT-3: the original "absorbed as H1-H6" was INCOMPLETE — mechanism M5 / KKT nonuniqueness mapped to NO hypothesis; it is now covered by the added H7-SEL, and Expert 3's well-posedness hypothesis survives only as the narrower H-int, declared): interior caustic from wall over-concavity (incipient interior shock, tier-1, not DEF), lip-corner binding, reflected-family/axis-end binding, multi-cusp Clarke case, KKT nonuniqueness (-> H7-SEL), kernel-dof/class mismatch, non-physical (budget/table) binding. Near-axis adjudicated: a same-family cusp strictly interior on the terminal characteristic is a legitimate classical D'; the exact axis (Guderley) limit and reflected-family folds are excluded by H2. No actual non-classical-binding KKT instance is exhibited — its existence is OPEN, with the cheapest discriminator being to log the cert-failing cell coordinates (S20/S21 record lacks them).

### S4
HOLDS, THEOREM (unanimous; judge independently re-derived and verified against sources): Lambda_pg = -(1+(gamma-1)M^2/2)/sqrt(M^2-1) = -(gamma-cos2alpha)/sin2alpha; A+B = sin(theta)/(cos(theta-alpha)cos(alpha)); Rao-Beck Eq.(4) rearranges exactly to Lambda*B*(A+B)-(A-B)=0, the zero set of GENO's val numerator (Rao_m.f90 L54), with nonvanishing connecting factor away from the fold denominator. The Lambda-form IS the correct EOS-general margin for the repo monitor, with two adopted bounds: (a) DATA-generality is conditional on the homentropic/homoenergetic (periodic-wave) scoping — entropy/h0-gradient flows need an extended margin; (b) implementation magics undischarged (dV_pert=1.0 m/s FD step; |den|<1e-10 guard conflating the benign d-theta=0 locus with the fold; PM landing window (0,1e-6)) — PRACTICE, tracked, must enter derived bands.

### S5
HOLDS as assembly: all four positions produce compatible assemblies converging on the split statement EQ-v2 (Direction A: DEF => margin-active KKT point, CONJECTURE on SCHEMA footing; Direction B: converse only under H1-H6, CONJECTURE; unqualified EQ REFUTED). Rigor CONJECTURE overall with one THEOREM component (S4) and one THEOREM* (Rao 1958 duality). Proof obligations merged and ranked by cost (see open_items); twin falsifier pre-registered with Richardson-derived bands, the two discriminating logs the current record lacks (argmin-margin cell location along the walk; active-cusp count/location at termination), and the mu-prediction test (GENO deeper-jump family). A correct CONJECTURE-with-falsifier is the converged outcome; no clause is promoted above its footing.

## DISAGREEMENTS RESOLVED

- S1 verdict split (3x HOLDS vs refuter OPEN) — RESOLVED as a formalization mismatch, not a substantive conflict: the refuter concedes fold-on-boundary + interior pointwise certification in the continuum; all four (experts included) state DEF is outside cl(K(mu_0)) at any fixed floor. Verdict fixed to the sub-question's own sense (bd(A_0)); the refuter's approximant-existence and limit-order gaps adopted as open obligations.

- S2 verdict split (3x HOLDS-at-SCHEMA vs refuter OPEN) — RESOLVED: no term of the first-order dictionary is disputed; the refuter's objection is that neither side has a theorem-grade derivation (cusp adjoint, J_wall<->C_Fv identity, classical jump-depth condition), which every expert already concedes as obligations O1-O3. Settled as HOLDS(SCHEMA)/theorem-OPEN.

- S3 verdict split (3x HOLDS-hunt-succeeds vs refuter FAILS-unqualified-EQ) — RESOLVED as pure labeling convention: identical content (hypotheses necessary, unqualified EQ dead, no counterexample instance exhibited).

- Near-axis cusp status (expert 2: benign, inside the DEF family; expert 3/refuter: excluded site) — RESOLVED: same-family cusp strictly interior on the terminal characteristic is a legitimate classical D' (Rao-Beck let D' range over the Mach line); reflected-family folds near the axis end C and the exact axis/Guderley limit are excluded by H2 as phrased.

- Classical-side optimality content (does the DEF construction carry intrinsic first-order conditions at fixed (eps,L)?) — RESOLVED with all four agreeing: it is constraint-pinned (theta_B, s_D' consumed by eps,L), its optimality content is exactly Eq.(1)+Eq.(4)-activity+the SIGN of dJ/d(jump depth); the last is a direct-side prediction absent from the page-verified corpus, recorded as surplus, not mismatch.

## OPEN ITEMS / PROOF OBLIGATIONS

- O1/P2: one-sided Danskin/Clarke derivative of the min-over-cells fold margin at a boundary-touching degenerate cusp (moving domain, argmin on the boundary, indicator degenerate) — the direct cusp-KKT (Dirac-supported grad m, adjoint point source at D') is asserted, not derived; until discharged the KKT is B-stationarity only.

- O2/P1: Rao 1958 wall<->surface variational duality (f2 = adjoint of Prop. A3) has no modern proof in the certified class D(...); extension of the f2-adjoint identification to the margin-active case open.

- O3: classical jump-depth optimality dJ/d(depth)<=0 is NOT in the page-verified corpus; Sternin 1962 and Shmyglevskii 1981 acquisition + page-verification is a hard gate for any classical-optimality claim (Direction B's converse and P4); until then it stands as a direct-side prediction with the GENO one-parameter deeper-jump numeric test.

- O4/P4: interior-binding KKT points neither excluded by theorem nor exhibited numerically; the S20/S21 record does NOT localize cert-failing cells — logging argmin-margin cell coordinates along the walk is the cheapest decisive measurement for H2 (first item to execute).

- O5/P5: K_h -> A_0 discrete-to-continuum consistency remains the named M0 CONJECTURE (Eq.(4)-monitor falsifier not yet run); the h->0 vs mu_0->0 double-limit order (Gamma-convergence of fixed-floor certified problems) unestablished and could confound the twin experiment.

- Refuter S1 gap (adopted): existence of strictly-margin-positive approximating designs converging to the DEF wall — the de-focused-fan approximant passes surface states through the Eq.(4)-invalid strip; whether the surface can advance there (val>0, dR>0) is unproven; alternatively prove closure via direct under-turned-wall approximants.

- Refuter S2 gap (adopted): objective identity J (direct wall pressure integral) <-> Rao-Beck C_Fv (control-surface thrust) modulo fixed throat momentum flux — standard momentum balance but unstated and unproven in the corpus.

- Implementation magics to derive or close (N-71 + O7/O8): dV_pert=1.0 m/s absolute FD step on tabulated thermo (closed-form Lambda exists for gamma=const), |den|<1e-10 guard conflating the benign d-theta=0 locus with the fold (spurious-boundary risk in sign scans), PM landing window (0,1e-6) and dthetapm=-0.001 — their induced band on D' must be derived for the twin test; planar-vs-axisymmetric role of val=0 to be documented when adopting the monitor.

- Scope limits to record: Lambda-form is EOS-general but NOT data-general (homentropic/homoenergetic td%solve(q) only — invalid under entropy/h0 gradients, i.e. beyond the periodic-wave scoping); plug/C- mirror (boundaryfunction_cminus_solve) unproven; axisymmetric centered-fan exactness away from the focal point THEOREM* not THEOREM (P6); axis-limit Guderley junction outside the corpus; interior certifiability of the full DEF field along DE is PRACTICE (O6).

- Second-order/uniqueness: no sufficiency, no local-optimality statement on either side; H6 (classical local uniqueness) and H3 (MFCQ/submersion of the classical parameter map) plausible, unproven; multi-KKT landscape (S19/S20) means 'coincides' needs the selection hypothesis wherever EQ-v2 is quoted.

- R4 retro-propagation due THIS session (session lead's duty): EQ-v2 with rigor tags into M0/D-docs; fixed-eps transversality bookkeeping (Rao Eq.(14) -> lambda_e = dJ/dy_lip corner identity); the S4 THEOREM cross-reference to GENO boundary_function_reformulation.md 9.1; the Rao-vs-Zucrow/GENO characteristic-family naming translation wherever D' is discussed.

- Execute the twin falsifier (see falsifier field) — EQ-v2 cannot rise above CONJECTURE before it runs; each rejection branch names which hypothesis dies.

## PRE-REGISTERED FALSIFIER
Pre-registered twin test (perfect gas gamma=1.4 so Eq.(4) is exact; Rao-Beck Table 1 throat r_up=1.0 R_T, r_down=0.1 R_T; DEF-regime target = Table-2 short-DEF twin, A_E/A_T=500, X_E=87 R_T). Leg 1: GENO type-2 flagdef=1 -> wall contour, D' seam state (qmonte/thetamonte vs qd/thetad), boundary-function residual at landing (val in (0,1e-6)), C_Fv. Leg 2: direct engine, margin-CONSTRAINED (KS-aggregated Lambda-form fold margin, floor->0 extrapolation), same (eps,L), run to KKT closure, with two MANDATORY logs the current record lacks: (a) argmin-margin cell coordinates along the entire walk, (b) count and location of active cusps at termination. REJECT EQ-v2 if ANY of: F1 wall-contour gap fails to shrink within the Richardson-derived two-code band under joint mesh+knot refinement (band = sum of measured extrapolation errors + the derived dV_pert/landing-window band on D'; no magic tolerances); F2 the direct active-cusp location does not converge to GENO's D' on the terminal characteristic; F3 f2 = V*cos(theta-alpha)/cos(alpha) drifts along the D'E segment beyond the S19-derived bar on either design; F4 the Lambda-form BF at the direct active cell does not -> 0 under refinement; F5 the margin binds persistently at an interior/lip/axis-side cell with KKT closed (kills H2 specifically: demotes EQ-v2 to Direction A only and moves the design into the interior-shock tier-1 regime); F6 multiple cusps simultaneously active at the optimum (kills H1); F7 direct certified J exceeds the DEF J beyond the combined band (kills classical optimality). Companion mu-prediction test (DEMOTED 2026-08-11 per red-team RT-4): a one-parameter deeper-jump GENO family at fixed (eps,L) must show dJ/d(depth)<0 at the boundary — a SIGN TEST for Direction A, NOT decisive for O3: under the panel's own record the cusp-KKT is B-stationarity only (O1 undischarged — no mu exists to "measure"), the sign link needs H3 submersion (unproved), the magnitude comparison crosses the ADOPTED-UNPROVEN J<->C_Fv identity, and no mu-estimator protocol (KS-rho bias + floor->0 extrapolation band) is specified. "Decisive for O3 without Shmyglevskii" is WITHDRAWN; preconditions for any magnitude claim: (i) the one-page derivation dJ/d(depth) = -mu*dm/d(depth) under H3, (ii) the mu-estimator definition with its derived band.
