# S19 — Total-order session log (2026-08-06; CAMPAIGN O3.2/O3.3 —
# P-2's NUMERIC HALF, unlocked by the S18 brick-2 closure)

Session: S19, per the S18 step-9 handoff and PROGRESS NEXT-1 of
2026-08-06 ("CAMPAGNA O3.2/O3.3, protocollo PRE-REGISTRATO
P2_outline §5, intatto"). Mandate (user order, this session's
header): (1) S10+ opening; (2) pre-execution GATE; then

T1 [O3.2] — MESH CONVERGENCE of the primal/adjoint PAIR on the twin
cases (ideal [X-A1IM] + TOC [X-TOCV]): expected rate h^2 (S-LBML,
clause LB-c1 self-monitoring VIA O3.2), double/triple resolutions,
PRE-REGISTERED norms (lip/corner/sonic exclusion — S14, never
re-litigated); rejector: rate outside the derived band; committed
carrier.

T2 [O3.3] — THE PRE-REGISTERED BENCH (P2_outline §5, INTACT since
S14: read WHOLE before writing code): L-P 2025 compatibility
residuals Eqs. (30)/(31) + the Prop. A3 f2 drift on OUR adjoint
field; NEVER the invariants (32)-(34) off constant-flow patches
(exception: R1^psi along streamlines); O3.3 = PRIMARY KILL
CRITERION — if it fails the verdict is a verdict, the band is not
adjusted; capturing control per X1; derived tolerances, negative
controls, GENO-independent oracles (memory moc-critical: the bench
IS by construction the GENO-independent oracle).

T3 — campaign verdict + R4 SAME SESSION (M0/D-doc delta, declared
rigor classes) + registry; with the O3.3 numbers landed, RE-PRESENT
the preprint decision (user lock; standing S17/S18 recommendation:
arm the arXiv trigger — the decision stays the user's).

TERMS OF REFERENCE (binding, from the session header): anchor (P) of
D2.6; the S14-S18 verdicts are NOT re-litigated (incl. T2 FIRED with
the G0 review QUEUED — NOT executed this session absent a user
order; envelope oracle band; TR-Newton driver at measured curvature
of record); no numbers without a committed carrier + a rejector that
can reject; R5: a pre-registered protocol admits NO post-hoc band
amendment without an independently declared diagnosis BEFORE the
decisive run (S18 precedent); R4 same session; query-bounded
novelty; L-X-H 2022 in method quarantine; environment numpy 2.5.1 /
jax 0.11 / scipy 1.18, persistent XLA cache, on-demand jax + GENO
WSL env. QUEUED AFTER (not executed): census-lemma session; PAP-RIM;
G0/T2 review; rigor bench (B1 interval s_L, C-XBVP(b), g-scan B3,
GBE concavity).

| # | Step |
|---|------|
| 1 | SESSION OPEN: HEAD = 1f3e0de (S18 closed at total order 1-9bis, log re-closed by the 9-bis repair addendum), branch `rde-nozzle-program`, no foreign commits, single-session rule verified. Tree state declared: the known untracked set unchanged (GENO/ = independent repo, NEVER committed here; literature/; validation/ADR_panel_2026-07-16.md + validation/PANEL_topology_census_2026-07-22.md = advisories by decision; user shell artifacts `er.name`, `mailmap.txt`, `current_commit_messages.txt`, `t --count HEAD:q`) plus the two modified data/q_mapping.* files inherited unchanged from before S15 — none of them is touched by this session and none enters any commit (path-limited commits per the reinforced protocol). OPENING PROTOCOL S10+ EXECUTED IN ORDER: (a) project memory — the five MANDATORY notes read in full: `s18-brick2-closed` (brick CLOSED of record; do-not-redo P1-P4; T2 FIRED with the G0 review queued; named scale levers), `s17-brick2-kickoff` (the four kickoff duties DONE; SDP decided Clarabel/MOSEK; numpy unpinned), `moc-critical-independent-invariants` (never assume GENO bug-free; the O3.2/O3.3 bench IS by construction the GENO-independent oracle; the CRITICAL LIST of deliberately mirrored GENO quirks), `sota-library-survey-directive` (survey + read-the-source + toolchain-currency, per complex algorithmic step), `general-vision-nondivergence` (decide every fork from the program picture); plus `python-env-cantera` (env of record) and `s15-foundations-campaign` for the inherited [RIGOR/A] state. (b) L0 = SCAFFOLD §1 (six layers + registry; the ANCHOR is (P) of M0 D2.6 — the pair (S*, delta) — and this campaign's output is an ORACLE layer artifact, L6->L2 traffic, not a new formulation). (c) `docs/claims_registry.yaml` — 122 entries; the rows this session can touch identified up front: [C-O33] (the open numeric conditional, inherited by T-LEMA-i and T-LEMA-iii), [T-LEMA-iv] (falsifier = O3.2 E-residual), [S-LBML] (statement says in as many words: "the O3.2 oracle measures the actual order, making LB-c1 self-monitoring"), [T-A3]/[X-PA1] (Prop. A3 closed pair, machine-verified symbolically), [T-LEMB]/[X-A1IM]/[X-TOCV]. (d) D6 — item 9 CLOSED annotation read (brick closed; "**O3.3 UNLOCKED** (next session: the pre-registered O3.2/O3.3 campaign = P-2's numeric half)"; governance residue = the G0 re-decision review QUEUED, explicitly NOT this session's work). (e) PROGRESS ORA (S18 closure) + NEXT-1 = exactly this campaign. (f) the S18 log read whole (steps 1-9bis), including the 9-bis lesson now binding on every commit here: NEVER pipe the lint inside a commit chain — gate on the exit code. (g) `docs/rde_nozzle_P2_outline.md` §5 READ WHOLE (the pre-registered protocol IS the plan of T2), together with §§1-4/6-8 for the claim classes it serves; and the two upstream theory sources the bench consumes: `docs/rde_nozzle_P2_lemmaA.md` §§3.3-3.6 (Props. A1/A2/A3, the component map, the PENDING register P-A3 = "O3.2 E-residual evaluation on the AD adjoint field — A1 engine") and `docs/rde_nozzle_LBML.md` (the five-step mesh-limit argument whose executable half is exactly T1). DECLARED RESTART POINT: PROGRESS NEXT-1, phase [F1/P-2] (paper P-2, numeric half) executed ON the [F2/A1] engine closed in S18 — commit tag `[F1/P-2][F2/A1][PIANO/S19]`. |
| 2 | GATE (pre-execution, standing directive `gate-pre-esecuzione`) — **(A) PLAN ADHERENCE.** This session is NOT an orphan step (R1): it is PROGRESS NEXT-1 verbatim, and D6 item 9's own closing annotation names it ("**O3.3 UNLOCKED** (next session: the pre-registered O3.2/O3.3 campaign = P-2's numeric half)"). Phase binding: the WORK is [F1/P-2] (publication stream P-2, its §5 numerics section) executed on the [F2/A1] engine; both tags carried. The two tasks map one-to-one onto pre-registered objects that predate the engine: T1 = oracle O3.2 (P2_outline §5, and the SELF-MONITORING channel that [S-LBML]'s own statement assigns to it for clause LB-c1); T2 = oracle O3.3 (P2_outline §5 (b)-(d), the PRIMARY kill criterion, whose open conditional [C-O33] is inherited by [T-LEMA-i] and [T-LEMA-iii]). EXCLUDED by declaration, with no work done on them: the G0/T2 re-decision review (queued by D6's flip clause; the session header forbids it absent a user order), the census-lemma session, [PAP-RIM], the rigor bench (B1/C-XBVP(b)/g-scan B3/GBE concavity). **(B) UPSTREAM RIGOR AUDIT** (every point this task stands on, re-read this session, not assumed): (i) THE ENGINE EXISTS AND IS CERTIFIED — [X-A1IM] (ideal march, 62/62 contour in the derived Richardson band, whole-march O3.1 2.7e-10, per-cell unit-consistent Newton certification) and [X-TOCV] (S18 run of record: KKT 7.745e-02 <= derived 1.156e-01, oracle 91/91, per-cell certification + axial-margin rejector + P4 instance floor 0.1202 m/s); the O3.2/O3.3 campaign differentiates and post-processes THESE, it does not re-open them (do-not-redo, memory `s18-brick2-closed`). (ii) THE PROTOCOL IS PRE-REGISTERED AND WAS READ WHOLE BEFORE ANY CODE: P2_outline §5 pins (a) norms excluding shrinking lip/corner/sonic-Sauer/axis neighborhoods for every FIELD-level check (O3.2 remains a rejector UNDER the registered norm — a max-norm-to-the-lip check could FALSE-KILL while gradients stay accurate), (b) O3.3 is the GRADIENT-level PRIMARY kill criterion with oracle coordinates = compatibility residuals + the Prop. A3 f2 drift, and NOT the constant-flow invariants (33)-(34), R1^psi retained as streamline-valid, (c) the wrong-family rejector (the r+ combination must NOT be conserved), (d) the wall-adjacent adjoint convergence audit with a declared lip-exponent probe. These pins are S14 verdicts and are NOT re-litigated here; they are IMPLEMENTED. (iii) THE CLASSICAL SIDE OF THE BENCH IS ALREADY THEOREM-CLASS AND MACHINE-VERIFIED: Prop. A3 ([T-A3], carrier [X-PA1] Part 3, EOS-general, irrotational homentropic) gives the closed multiplier pair (lambda1, lambda2) = a (y^d rho v, u) + b (0, 1) and the terminal-characteristic transversality E = lambda1 -/+ lambda2 y^d rho cot(alpha) = 0 => f2 = V cos(theta -/+ alpha)/cos(alpha) = -b/a = const; the compatibility form along Mach lines is page-verified in the in-house corpus (Hoffman-Scofield-Thompson, JOTA 10(3):133 (1972), Eqs. (21)-(23); HTH AIAA J 9(8):1581 (1971) p. 1583; Hoffman 1967 Eq. (54)). (iv) THE MESH-LIMIT STATEMENT EXISTS IN WRITING: [S-LBML] §2 (i)-(v) — consistency h^2 under LB-c1, stability = the D2.5-U five-constant machinery, Lax equivalence on the tangent, EXACT finite-dim transpose ([T-LEMB]) so the gradient converges in the dual pairing at the SAME order, front-shift convergence. T1 measures precisely the exponent that argument predicts, on a reference where LB-c2 (fixed march topology) holds by construction (plan frozen per resolution). (v) THE OPEN CONDITIONAL IS CORRECTLY CLASSED: [C-O33] is SCHEMA and DECLARED-NUMERIC (SCAFFOLD §3 rule 4), so discharging it is a measurement, not a proof — the session cannot upgrade [T-LEMA-i]/[T-LEMA-iii] beyond what the measured residuals support. (vi) GENO-INDEPENDENCE (memory `moc-critical-independent-invariants`): the bench is by construction the GENO-independent oracle — GENO does not enter T1 or T2 at any point (no cross-code row, no GENO reference contour, no shared constant); the CRITICAL LIST of deliberately mirrored GENO quirks (NASA-poly extrapolation, y2==0 source guard, |M-Me|<1e-5 stop, gammamedio Sauer start) is re-declared as DATA CONTRACT of the twin, and none of the four is load-bearing for either oracle. (vii) ANCHOR (P) of D2.6: the campaign's product is certificate-grade evidence for the (S*, delta) machinery's gradient — no hardware claim, no design claim beyond the declared instance. (viii) QUARANTINE: L-X-H 2022 is method-quarantined and is not consulted; no quarantined source enters. Suite state at open: lint (xv) PASS live, exit code 0, 122 entries, rejectors fire (0 violations); full-suite state inherited from the S18 closure 15/15 in 100 s. **(C) DECLARATIONS MADE BEFORE THE DECISIVE RUNS (R5 pre-registration integrity — stated HERE so that no band can be argued post-hoc):** [D1] SOURCE OF THE COMPATIBILITY COORDINATES. The pre-registration NAMES the coordinate class as "L-P 2025 Eqs. (30)/(31)". The in-repo status of that source is: ACQUIRED + PAGE-VERIFIED through §2 with the p. 7 facts of record (the (33)-(34) constant-flow restriction, the "agree with [26,27]" line) — but the printed algebraic FORM of (30)/(31) is not transcribed in this repo, and the literature upload is a standing user lock. Therefore the bench is built on the EQUATION-NUMBERED, PAGE-VERIFIED IN-HOUSE CORPUS for the same coordinate class (JOTA 1972 Eqs. (21)-(23) / HTH 1971 / Hoffman 1967 Eq. (54)) plus in-house derivation, and the alignment of our compatibility rows with L-P's equation numbers is recorded as a CITATION-level item that stays PENDING page re-verification. No content is claimed from an unverified page; the protocol's substance (WHICH coordinates: compatibility residuals + f2 drift; which are FORBIDDEN: (32)-(34) off uniform patches) is honoured exactly, and the forbidden rows are enforced by a coded guard, not by discipline. [D2] CAPTURING CONTROL (X1). On the twin instances of this campaign the fitted-vs-capturing control is VACUOUS BY CONSTRUCTION: both are shock-free (the TOC/ideal class boundary is exactly shock-freeness — S17 finding), so the march contains ZERO fitted fronts and the control's discriminating object does not exist. What IS executed from pre-registration item (d) is the other half: the wall/lip-adjacent adjoint convergence probe with the declared lip-exponent measurement, which is what justifies (or falsifies) the registered norm exclusion empirically. The capturing control proper is DEFERRED, declared, to the shock-bearing instance (G12/F2 line) — deferred, not dropped. [D3] BANDS. Every tolerance is derived before the run and written into the carrier: rate bands from the propagated roundoff floor of the difference estimator plus the measured asymptotic-range indicator; field bands from two-level Richardson x the repo's reused K_RICH = 4; no band is touched after seeing a decisive number (S18 precedent binding). [D4] KILL SEMANTICS. O3.3 is the PRIMARY kill criterion: if the term match fails outside the derived bars, the session records a FAIL verdict and the affected claims are DOWNGRADED — the band is not adjusted and the run is not repeated until it passes. **(D) VERDICT: gate pre-esecuzione PASS** — proceed to T1 (O3.2). |
| 3 | T1 [O3.2] CARRIER WRITTEN + PRE-DECLARATIONS, **committed BEFORE the decisive run** (R5: the protocol is pre-registered, so every band, every verdict rule and every diagnosis this session may invoke must exist in the record before the numbers do). NEW CARRIER `validation/o32_mesh_convergence.py`, registry ID **[X-O32]**. WHAT IT MEASURES: the two exponents that [S-LBML] §2 predicts — PRIMAL (a registered-norm functional of the computed field) and ADJOINT (a fixed-direction projection of the AD gradient of that same functional, i.e. literally the dual pairing of S-LBML step (iv)) — on one refinement ladder, so that (i)/(iii) and (iv) are SEPARATELY falsifiable: a degraded primal rate kills consistency/Lax-equivalence; a degraded adjoint rate WITH the primal in band kills the "exact transpose loses nothing" step. ESTIMATOR (derived, not a two-level log2): three levels h1 > h2 > h3 with f_k = f* + C h_k^p give (f1-f2)/(f2-f3) = R(p) := (h1^p - h2^p)/(h2^p - h3^p); R is strictly increasing (machine-asserted on the bracket, both triples) so p is recovered by bisection — exact for the model and valid for the non-integer ratios 1, 1/2, 1/3, 1/4 the ladder uses. KNOWN-ANSWER TEST executed at every invocation (§S1): synthetic sequences with p0 in {1, 1.5, 2, 3} recovered to worst |p_hat - p0| = **1.67e-13**, monotonicity PASS on both triples, and the estimator REJECTS a first-order sequence as h^2 — a broken estimator cannot silently pass a physics row. UNCERTAINTY, both terms MEASURED: dp_noise propagates the accumulated per-cell Newton certification floor eps_f = NEWTON_TOL_FACTOR * eps * N_cells * scale(f) (N_cells = the count the run itself certifies) through the estimator at the extremal admissible differences; dp_model = |p(coarse triple) - p(fine triple)| = the asymptotic-range indicator. VERDICT RULE FIXED NOW: CONCLUSIVE iff dp_tot = dp_noise + dp_model < 1/2, the 1/2 DERIVED as the half-separation from the adjacent integer orders 1 and 3 (a band touching 1 or 3 discriminates nothing); PASS iff CONCLUSIVE and |p_fine - 2| <= dp_tot; fewer than four levels is NON-CONCLUSIVE by construction (dp_model undefined) and never a PASS. There is no third branch: a conclusive rate outside the band is a FAIL of the h^2 claim and LB-c1 is then reported VIOLATED on that instance — the clause's own declared self-monitoring channel firing, which is a result, not an error. REGISTERED NORMS IMPLEMENTED (P2_outline §5(a), never re-litigated): a C^infinity bump weight supported strictly inside [x_start + d, x_end - d] with d = 2 x (coarsest-level station spacing), the factor 2 being the unit process's STENCIL RADIUS — a derived cell count, and an h-INDEPENDENT window (a functional whose definition moved with h would have no order at all). The excluded lip neighborhood is measured SEPARATELY from the same certified records (pre-registration item (d), the half of the X1 audit executable on a shock-free twin — see gate [D2]). NEGATIVE CONTROL (§S4), a rejector that can reject: `make_solvers_foot` = the SAME unit processes with the characteristic coefficients frozen at the FOOT state instead of the segment MIDPOINT — the explicit-Euler form of the MoC cell instead of the average-coefficient (modified-Euler) form: consistent, PROVABLY first order, identical code path and identical certification. The band that accepts the production march must reject it. PATH OF RECORD: the differentiated replay runs on the PRODUCTION bucketed-jit path ([X-SCANM] + the S18 P2 lever), not the eager Python replay whose graph churn killed the S17 run; measured this session, the jit path is ~3x faster at equal numbers (r=1: 29.9 s vs 80.1 s; r=2: 82.0 s vs 260.2 s, Q identical to all printed digits). RK-G MONITOR (i) PER LEVEL: the differentiated replay is checked against the certified record at the Newton floor at every level, so the exponent is measured on the sequence that was certified (smoke-test values: |replay - record| = 1.4e-14 and 4.4e-15 against floors 2.7e-10 and 1.0e-09). INSTANCE PROVENANCE, DECLARED: the TOC ladder is run at W* of the S18 run of record (thB = 15.5527 deg, nodes as printed in the S18 log step 7); W* is the converged design of an optimization whose STARTING POINT was GENO-seeded, so GENO supplies the provenance of the INSTANCE and nothing else — no row of this carrier compares anything to GENO output, hence no cross-code agreement can manufacture a rate; the design is re-certified (per-cell Newton + axial margin) at every level rather than trusted. **PRE-DECLARED DIAGNOSTIC (the one this session is allowed to invoke, declared here, before the decisive run):** IF the ideal twin's exponent degrades, the FIRST hypothesis to test is item 3 of the CRITICAL LIST of deliberately mirrored GENO quirks (memory `moc-critical-independent-invariants`) — the exit constant |M - Me| < 1e-5, which caps the accuracy of the ACHIEVED exit Mach and therefore lets the case itself drift with h. Honest provenance of this pre-declaration: the rmax=2 smoke test already showed |dMe| = **5.242e-06** between the two coarsest levels, i.e. sitting at the mirrored constant's own scale — the hypothesis is informed by that observation and is being registered BEFORE any four-level rate exists. TEST, executable without editing anything mid-verdict: the constant is now a parameter of [X-A1IM] (`cfg["m_stop"]`, default 1e-5 = the mirrored GENO value, so record behaviour is bit-identical) and the diagnostic re-runs the ladder with it tightened 100x (stage `mstop`). THE DIAGNOSTIC AMENDS NO BAND: it can only attribute a measured degradation to a named mechanism, or fail to. |
| 4 | T1 [O3.2] MESH CONVERGENCE OF THE PRIMAL/ADJOINT PAIR — carrier [X-O32] executed on the ladder r = 1..4 (h = 1/r) on both twins, at fixed design. **A RULE CORRECTION I OWE BEFORE ANY NUMBER.** My step-3 text fixed a TWO-SIDED verdict rule (|p - 2| <= dp_tot). The rule that BINDS is older and one-sided: the falsifier committed with [S-LBML] in S16, `docs/rde_nozzle_LBML.md` audit line — "O3.2 (measured order < 2 - tol on smooth certified references kills the rate claim)". An order ABOVE 2 does not falsify a claim of order h^2, and my step-3 rule was stricter than the text I am bound by. Second, the conclusiveness cap: I DERIVED it in words as "the band must not touch the adjacent integer orders 1 and 3" and then CODED 0.5, but a band 2 +/- dp touches 1 only at dp = 1, so the constant and its own stated derivation contradict each other — my error. Resolution, adopted before reading any four-level rate: the verdict uses the BINDING one-sided falsifier with tol := dp_tot (the run's own measured uncertainty, no free constant), gated on conclusiveness at the constant the derivation actually yields (1.0); the carrier PRINTS every row under BOTH rules and BOTH caps, so no reader depends on my choice. **APPARATUS FIRST, RESULTS SECOND.** Estimator: known-answer recovery over p0 in {1, 1.5, 2, 3} to 1.67e-13 with machine-asserted monotonicity on both triples. Per-level RK-G monitor (i): the differentiated replay reproduces the certified record at |replay - record| between 4.4e-15 and 3.7e-14 (ideal) and 4.6e-13 and 1.8e-12 (TOC) against Newton floors 2.7e-10..4.3e-06 — every level PASS, so the exponents are measured on the sequence that was certified. **NEGATIVE CONTROL, and it is what licenses reading the rest**: the foot-coefficient march (identical unit processes with the characteristic coefficients frozen at the FOOT state instead of the segment midpoint — the explicit-Euler form, provably first order) measures **p(coarse) = 0.9991, p(fine) = 0.9997, dp_tot = 6.0e-04** and is duly KILLED by the h^2 claim. The apparatus therefore resolves a clean order to three decimals; whatever the production march's exponents do, it is not the measurement that is doing it. **RESULTS.** TOC twin at W* (cells 3964/15227/33796/59662): the OBJECTIVE J = 2.776168784978e+07, 2.776068095041e+07, 2.776047926429e+07, 2.776042108731e+07 gives p(coarse) = 1.8688, **p(fine) = 2.5347**, dp_noise = 4.48e-03, dp_model = 0.6659, dp_tot = 0.6704 -> **PASS, conclusive** (the h^2 rate claim is not killed; it is also inside the two-sided reading). The registered-norm wall-speed functional Q_u is NON-CONCLUSIVE for an honest reason: it is already converged to ~2e-09 relative by r=2, so its differences sit a factor ~8 above the accumulated Newton floor and dp_noise alone is 5.5e-02 with dp_model 1.94 — a noise-limited row, reported as such. **THE ADJOINT ROW IS NOT USABLE ON A REFINEMENT LADDER, AND THAT IS THE SESSION'S HONEST NEGATIVE RESULT.** <dJ/dW, v> = -2.653147580975e+06, -2.631122104545e+06, -2.628790966486e+06, -2.626852188325e+06: the successive differences are -2.203e+04, -2.331e+03, -1.939e+03 — the fine pair barely shrinks and the ratio falls outside the estimator's bracket. Mechanism, and it is a clause we already own rather than an excuse: [S-LBML] takes its limit AT FIXED MARCH TOPOLOGY (clause LB-c2), and a refinement ladder cannot hold the topology fixed. MEASURED, not asserted — the topology monitor added for exactly this reports columns carrying an axis point over design-wall columns as **33/92, 66/183, 99/274, 131/365**, whose fraction runs 0.35870, 0.36066, 0.36131, 0.35890: non-monotone, with the reversal at r = 4, the same level at which the gradient sequence stalls. That is a correlation and it is named as one; the clean experiment (a fixed-topology family, or a functional insensitive to the truncation count) is OWED and queued. What is NOT in question is step (iv) of S-LBML: the transpose is exact by construction and O3.1-verified — this is a statement about which EXPERIMENT can measure the gradient's order, not about whether the gradient is the transpose. **IDEAL TWIN**: primal p(coarse) = 2.6524, p(fine) = 0.5294, dp_model = 2.1229 -> NON-CONCLUSIVE; adjoint NOT USABLE (non-monotone at r = 4). The achieved exit Mach is **non-monotone at the 1e-05 level** (|dMe| = 5.242e-06, 1.495e-05, 4.356e-06) — exactly the scale of the mirrored GENO exit constant, i.e. the PRE-DECLARED diagnostic of step 3 is pointed where it was aimed. **LIP PROBE (pre-registration item (d)), and it FIRES**: the lip-POINT wall speed q(x = L) — a genuine pointwise field value, the lip station existing at every level — converges at **p(fine) = 0.7630 with dp_tot = 0.5688, conclusive, i.e. KILLED against 2**, while the registered-norm functional does not. The pre-registered lip exclusion (S14 F-LIP) is therefore EMPIRICALLY SUPPORTED on this instance: an un-excluded wall-adjacent neighbourhood does converge at a degraded order. The lip-WINDOW functional is unusable as predicted (its coarsest level is supported on ~2 stations, so it conflates quadrature with physics) and is reported, not used. RECORD RUNS: the ideal half from the full-carrier run; the TOC half re-run under `A1_O32_STAGE=toc` after one correction to a SUMMARY SENTENCE ONLY (the conclusion line had been keyed to the unusable lip-WINDOW row instead of the lip-POINT row) — no number, band or verdict is touched by that edit, and staging is a declared feature of the carrier. |
| 5 | T2 [O3.3] THE PRE-REGISTERED BENCH — new carrier `validation/o33_bench.py`, registry ID **[X-O33B]**. **THE SESSION'S ONE REAL ERROR, AND ITS CORRECTION, FIRST.** The bench's first implementation traced the C+ characteristic through the lip ALL THE WAY TO THE AXIS and called that the Rao control surface. On that chain f2 = W cos(theta - alpha)/cos(alpha) drifts **2.89e-01**, the wrong-family combination drifts LESS than f2, and identification (ii) looks falsified. I reported that as a physical finding ("f2 is constant only on the design-influenced portion"), which was wrong: it is a LOCUS ERROR. The user corrected it in-session — the control surface starts at the LAST C- emitted by the circular throat arc, before the variational contour, and is the C+ from there to the lip, i.e. the C+ traced BACK from the lip and STOPPED at the kernel boundary. That is the classical definition (Rao's construction places the surface's axis end where the kernel ends), not a discovery; upstream of that boundary the fixed throat arc determines the data, so no stationarity condition can hold there and (L.5) never claimed one. The boundary is read from the march's own topology (which wall segment emits the C- through each node) and never from the invariants; as a corroboration it falls 3 nodes out of 87 from the wall-angle maximum on the chain. The wrong number is kept in the record next to the right one because it is the one a reader repeating the experiment would otherwise reproduce. **CARRIER MECHANICS.** [X-TOCV] gained an additive, default-off `return_field` export of the march columns with BOTH characteristic connectivities recorded AS BUILT (the C- chain must never be rebuilt from the column array by index arithmetic — columns also carry void-region points); the C+ walk then follows genuine grid nodes with no interpolation, giving an 87-node surface from the axis to the lip, of which **60 nodes are the control surface**, from (x=1.8782, y=0.5790) to the lip. **ROWS, ALL MEASURED.** R1/R2 (Rao's two first integrals on the control surface): f2 mean **2665.3808**, drift **9.4797e-03**; the second integral f3* = 2 pi y rho W^2 sin^2(theta) tan(alpha) of (L.13) — the one carrying the LENGTH multiplier our instance actually owns — drift 1.2347e-01; wrong-family u - v tan(alpha) drift **8.1550e-02**, i.e. 8.6x worse than f2, so pre-registration item (c) fires as intended. R7 (CROSS-DESIGN, the check the user demanded): GENO's own type-2 Rao contour marched by OUR engine — instance only, every relation still ours — gives f2 mean **2666.0260** with drift **7.9902e-03**; the two Rao CONSTANTS agree to **2.420e-04**, an order of magnitude inside either drift, so the residual is the instance's discretization, common to both designs, and NOT a property of either. THE LIP READING AGREES TOO, which is the second half of what the classical side must reproduce: the transversality ratio of Rao Eq. [14], pa/p_E = p_E - (1/2) rho W^2 sin(2 theta) tan(alpha) over p_E, comes out **0.55762 (ours) vs 0.56042 (GENO), a difference of 2.803e-03** — inside the f2 drift level. It is NOT zero, and it is not supposed to be: our instance CONSTRAINS the exit ordinate (the eps equality), so the free-endpoint condition is replaced by the constraint's multiplier, which is exactly what row R3 tests. Consuming GENO's profile output surfaced TWO DATA DEFECTS, both recorded because both silently poison any fit at the lip: the exit wall point is emitted TWICE, the copies 3.8e-6 apart in x (a node interval ~2e4 times smaller than its neighbours; an unguarded cubic fit then returns an exit slope of -74 deg instead of 7.86 deg), and the ATTACHMENT angle is NOT the maximum wall angle — GENO leaves the arc at thB = 16.9036 deg and its wall angle keeps RISING to a peak of 18.83 deg, so the S18 seed recipe (arctan of the max slope) took the peak, and feeding the peak as the attachment over-turns the wall until the axial-margin rejector refuses the design. Attachment is now found geometrically (last point still on the throat circle) and the represented wall is gated against GENO's own points at **1.867e-05**. R4/R5 (adjoint compatibility, the sharpest rejector in the bench): in the Prop. A3 gauge (lambda1 = y rho v, lambda2 = u) the corpus relation d(lambda1) -/+ y rho cot(alpha) d(lambda2) = 0, measured as a dimensionless cancellation fraction, gives **1.6468e-04** on the C+ family with its sign and **2.3426e-04** on the C- family with its sign, while the WRONG sign saturates at **exactly 1.0000** on both — the two terms add instead of cancelling. GUARD (§S6): the constant-flow invariants (33)-(34) are REFUSED on the live surface (relative flow spread 2.415e-01 against a derived uniformity tolerance of 1.932e-12) and ADMITTED on a synthetic uniform patch — the pin is enforced by code, not by discipline. R3 (the corner row = identification (iii)): with the exit ordinate CONSTRAINED, the free-endpoint transversality (L.14) becomes "augmented density at E = multiplier of that constraint", and by the envelope theorem applied to the value function that the control-surface and wall formulations SHARE, that multiplier is the AD derivative dJ/dy_lip. Measured: AD **1.054906326e+07** (which reproduces the S18 optimizer's own lambda_eps to six digits — an internal consistency check of record) against the classical corner density **9.893197135e+06**, i.e. **6.6295e-02** relative. It is NOT inside the two-resolution Richardson band (4.1756e+03) and it is **MESH-INDEPENDENT** (6.6295e-02 at r=1 vs 6.6368e-02 at r=2), so it is not discretization. TWO-KNOB CONVERGENCE TEST, which is the row's verdict of record: the mismatch falls to **2.6198e-02** (N=20) and **1.9978e-02** (N=60) on a faithful Rao wall at the same mesh, and to **1.4875e-02** when that near-continuum design is marched at r=2 — an identity of the CONTINUUM optimum, converging in both limits that separate our instance from it, and not confirmable at a Richardson band on any finite-dimensional instance. [C-O33] therefore stays OPEN with its residual QUANTIFIED and its dominant term IDENTIFIED as the DESIGN CLASS. **R6, THE PRIMARY KILL CRITERION, PASSED**: on the pre-registered perturbed-contour family around the optimum (P2_outline §5 verbatim, "AD boundary gradient vs Rao condition residuals"), dJ/dt crosses zero at t = -5.061918e-04 and the Rao residual's V-vertex sits at t = -7.308913e-03, so |t_grad - t_Rao| = **6.8027e-03 <= the derived bar 3.5612e-02**; the family discriminates (dJ/dt changes sign, +1.34e+06 to -1.40e+06) and the f2 drift is a clean V with its minimum at the design where the gradient vanishes (2.82e-02, 2.42e-02, 1.79e-02, **1.77e-02**, 2.10e-02, 2.80e-02, 3.75e-02). **ONE EXPECTATION OF MINE REFUTED AND WITHDRAWN, NOT QUIETLY DROPPED**: the carrier also asserted that the corner mismatch must be MINIMISED at the optimum. It is not — it varies monotonically across the family (7.847e-02 -> 5.690e-02) because it is dominated by the design-class representation gap, which is a smooth function of the design and has no reason to be stationary where the gradient vanishes. The assertion is removed, the refuting values are printed by the carrier, and the falsifiable statement about the corner identity is the two-knob test instead. |
| 6 | **R4 — THEORY BACK-PROPAGATION, SAME SESSION** (CLAUDE.md R4: nothing may live only in code, commit messages or a session log). Rigor class of the whole campaign delta: **PRACTICE** (measurement) — it discharges or sharpens NUMERIC conditionals and moves no proof class by itself, which is why no THEOREM* was upgraded. Written this session: (a) **M0 Part VI**, new block "O3.2/O3.3 CAMPAIGN OF RECORD" — the locus correction stated FIRST (with the wrong 2.9e-01 number kept beside the right 9.5e-03 one), the four things the bench established, and the four honest residues. (b) **`docs/rde_nozzle_P2_lemmaA.md` §3.7 NUMERIC STATUS OF RECORD**, six items (a)-(f): the locus correction; identification (ii) measured on two independent designs with the constants agreeing to 2.4e-04; the compatibility coordinate with its wrong-sign control; identification (iii)'s corner identity SUPPORTED-and-converging but not band-confirmed, so [C-O33] stays OPEN with the residual quantified; the primary criterion passed; and P-A3's status bounded honestly — the Hoffman-E evaluation proper is NOT executed because it needs the pointwise adjoint FIELD, whose extraction procedure (a source tau injected through the custom_vjp solver's p argument, so dJ/dtau is the discrete multiplier of that cell's compatibility row) is now named so the next session does not re-derive it. (c) **P2_outline §5 ADDENDUM**, five items, explicitly changing NO band and NO criterion of the pre-registered protocol: the control surface by its classical definition, the norms unchanged, (L.13) carried as the second optimality coordinate because our instance fixes the LENGTH, the corner identity with its envelope-theorem justification and its honest limits, and the X1 capturing control declared vacuous-on-shock-free-twins and deferred. The addendum also names the DESIGN-CLASS residue's procedure per the standing generality directive: the discharge is an adaptive, error-indicator-driven knot construction — NOT more uniform nodes, which would repeat the same instance-tuned mistake at higher cost. (d) **`docs/rde_nozzle_LBML.md` §5**, the EXECUTED note: the primal exponent lands where §2(i) says, the apparatus is validated to 6e-04 by the first-order control, and the ADJOINT exponent is not measurable on a refinement ladder because refinement cannot hold the topology fixed — clause LB-c2 biting, with the fixed-topology experiment declared owed and the paper required to say so; plus the mirrored-constant accuracy floor on the ideal twin. (e) **D3 theorem ledger**, P-2 entry: numeric status + the locus correction; classes of (i)-(iii) UNCHANGED (THEOREM*), which is the honest outcome — the campaign SHARPENED the conditional instead of discharging it. (f) **D6 item 9**: campaign annotation + FOUR residues queued, each with its lever named (richer adaptive design class; the lambda3/length twin of the corner row, needing L traced through the replay; a fixed-topology experiment for the adjoint exponent; adjoint-field extraction by residual injection). (g) **hypothesis ledger**: the C-O33 row moves from "PRICED" to "PRICED, RESIDUAL NOW QUANTIFIED" with the number, the cause and the discharge. (h) **claims registry**: two new carriers **[X-O32]** and **[X-O33B]** (122 -> 124 entries), each with scope, statement and its own falsifiers; lint (xv) green on the exit code at every commit of this session. |
</content>
</invoke>
