# S20 — Total-order session log (2026-08-07; CLOSING [C-O33] VIA THE
# DESIGN CLASS — adaptive, error-indicator-driven knot construction)

Session: S20, per the S19 step-8 handoff and PROGRESS NEXT-1 of
2026-08-06 (a USER decision among four residues; the user's order
opening this session RESOLVES it: residue (a) — "re-optimize in an
ADAPTIVE, error-indicator-driven knot class and re-measure the corner
row — the one that closes [C-O33]"). Mandate (user order, this
session's header): (1) S10+ opening; (2) pre-execution GATE; then

T1 [SURVEY + DECISION] — per the standing directives
`sota-library-survey-directive` + `generality-nonhardcoded-procedures`:
BEFORE writing code, survey with SOTA tools the standards for ADAPTIVE
knot/node construction and basis classes for goal-oriented shape
problems (free-knot splines, a-posteriori indicator-driven knot
insertion, DWR/goal-oriented refinement, hierarchical B-splines,
NURBS-in-shape-opt); READ THE SOURCE of whatever is adopted
(termination criteria, state across iterations, hidden tolerances);
adopt-or-declare-why-not, decision RECORDED in the record doc.
Constraint: the procedure must be GENERAL (not tuned to this
eps/L/throat), SOTA and ECONOMICAL — refine only where the residual
lives. The S19-derived candidate indicator (the classical f2 residual
per segment on the control surface, mapped back to the emitting wall
station via the `owner` array of `cplus_chain` in
validation/o33_bench.py) is to be ADJUDICATED against the survey, not
assumed.

T2 [EXECUTION] — implement the adaptive class and RE-OPTIMIZE. Cycle:
march -> per-segment residual density -> knot insertion ->
re-optimization TR-SQP under DIR-RKG -> repeat until the residual
stops falling (stopping criterion on MEASURED quantities, never fixed
trip counts). Every iteration keeps the gates of record: per-cell
Newton certification, axial-margin rejector, P4 floor, replay-fidelity
monitor, O3.1 on the gradient. MANDATORY STAGING and a DECLARED
BUDGET (S18: ~400 s per re-optimization run); if abandoned, R3 with
honest state + named lever (general-vision-nondivergence).

T3 [RE-MEASURE + VERDICT] — re-run [X-O33B] on the new optimum: row
R3 (the corner identity) MUST fall if the S19 diagnosis is right; the
two-knob test stays the row's verdict form. If it does NOT fall, the
"design class" diagnosis is FALSIFIED and the verdict is a verdict —
no band adjustment. Also re-verify R1/R7 (f2 constant, Rao constant
vs the GENO contour) on the new design: they must hold, not degrade.
Then R4 SAME SESSION (M0/D-doc delta, declared rigor classes) +
registry + R3 closure.

TERMS OF REFERENCE (binding, from the session header): anchor (P) of
D2.6; the S14-S19 verdicts are NOT re-litigated — in particular: the
Rao control surface is the C+ traced back from the lip and STOPPED at
the kernel boundary (the last C- of the arc; on the full chain f2
drifts 2.89e-01 and (ii) looks falsified — that error is NOT
repeated); the binding O3.2 falsifier is ONE-SIDED ([S-LBML] audit
line); the two-knob test is the corner row's verdict form; no numbers
without a committed carrier + a rejector that can reject; R5:
pre-registered protocol = no post-hoc band amendment without an
independent diagnosis DECLARED BEFORE the decisive run; R4 same
session; query-bounded novelty; L-X-H 2022 in method quarantine;
environment numpy 2.5.1 / jax 0.11 / scipy 1.18, persistent XLA
cache, on-demand jax + GENO WSL env. GENO DATA NOTE (found in S19,
already handled in the carrier): the profile emits the exit point
TWICE 3.8e-6 apart, and the attachment is NOT the slope maximum
(16.90 deg vs peak 18.83). QUEUED AFTER (not executed; relative order
to be decided with the user at session end): (b) the lambda3/length
twin of the corner row; (c) the fixed-topology experiment for the
adjoint exponent; (d) adjoint-field extraction by residual injection;
census-lemma session; PAP-RIM; G0/T2 review; rigor bench.

| # | Step |
|---|------|
| 1 | SESSION OPEN: HEAD = 442b652 (S19 closed at total order 1-8), branch `rde-nozzle-program`, no foreign commits, single-session rule verified. Tree state declared: the known untracked set unchanged (GENO/ = independent repo, NEVER committed here; literature/; validation/ADR_panel_2026-07-16.md + validation/PANEL_topology_census_2026-07-22.md = advisories by decision; user shell artifacts `er.name`, `mailmap.txt`, `current_commit_messages.txt`, `t --count HEAD:q`) plus the two modified data/q_mapping.* files inherited from before S15 — none is touched by this session and none enters any commit (path-limited commits per the reinforced protocol; lint (xv) gated ON THE EXIT CODE at every commit — the S18 9-bis lesson stays binding). OPENING PROTOCOL S10+ EXECUTED IN ORDER: (a) project memory — the six MANDATORY notes read in full: `s19-o33-campaign` (the S19 verdicts; the locus error NOT to repeat; the four residues with levers named), `generality-nonhardcoded-procedures` (the directive GOVERNING this session: general, SOTA, economical — issued precisely over the 8-uniform-node class this session replaces), `sota-library-survey-directive` (survey + read-the-source + toolchain-currency), `s18-brick2-closed` (do-not-redo P1-P4; the driver findings the re-optimization reuses), `moc-critical-independent-invariants` (GENO agreement != truth; the bench is the GENO-independent oracle), `general-vision-nondivergence` (decide forks from the program picture; honest state + named lever if a local fight is abandoned); plus `python-env-cantera` (env of record) and `gate-pre-esecuzione` (the gate's own text). (b) L0 = SCAFFOLD §1 (six layers + registry; the ANCHOR is (P) of M0 D2.6; this session's product is measurement + engine-class work: an L5/L6 artifact that discharges a NUMERIC conditional, not a new formulation). (c) `docs/claims_registry.yaml` — 124 entries; rows this session touches identified up front: [C-O33] (SCHEMA, declared-NUMERIC residual; falsifier "O3.3 run with component mismatch beyond derived tolerance"; inherited by [T-LEMA-i]/[T-LEMA-iii]), [X-O33B] (the bench re-run in T3), [X-TOCV] (the engine whose wall class T2 extends). (d) D6 item 9 — the S19 annotation read: four residues queued, residue (a) named verbatim ("re-optimize in a RICHER, ADAPTIVE design class (error-driven knot construction, not more uniform nodes) and re-measure the corner row"). (e) PROGRESS ORA (S19 closure) + NEXT-1 = a user decision among four residues — RESOLVED by this session's user order to residue (a). (f) the S19 log read whole (steps 1-8), including: the locus correction (control surface = C+ stopped at the kernel boundary, read from march topology via the `owner` array); the one-sided verdict-rule correction of step 4; the two-knob test as R3's verdict form; the numbers of record 6.6295e-02 (8-node, MESH-INDEPENDENT: 6.6368e-02 at r=2) / 1.9978e-02 (N=60 Rao wall) / 1.4875e-02 (there at r=2); R6 PASS |t_grad - t_Rao| = 6.80e-03 vs bar 3.56e-02. (g) `docs/rde_nozzle_P2_outline.md` §5 READ WHOLE INCLUDING THE S19 ADDENDUM — addendum item (iv) names this session's procedure: "the discharge is therefore NOT 'more uniform nodes': it is an adaptive, error-indicator-driven knot construction (free-knot / a-posteriori insertion, basis class fixed by the regularity the downstream estimate consumes), so that the class refines where the Rao residual actually lives and nowhere else". (h) `docs/rde_nozzle_P2_lemmaA.md` §3.7 (numeric status (a)-(f); (d) = the corner row this session must move). (i) `docs/rde_nozzle_LBML.md` §5 (the EXECUTED note; LB-c2 mechanism; the fixed-topology experiment stays OWED and is NOT this session's task). ENGINE STATE VERIFIED at open: env numpy 2.5.1 / jax 0.11.0 / scipy 1.18.0 (matches the record); the wall class to be replaced sits in `a1_toc_variational_jax.py::wall_geometry` (xs UNIFORM on [xB, L], M_NODES = 8, natural cubic spline clamped left); the candidate indicator's carrier objects exist ([X-O33B] `cplus_chain` returns (chain, owner); `locus_split` reads the kernel boundary from owner >= n_fan + n_arc). DECLARED RESTART POINT: PROGRESS NEXT-1 resolved to residue (a), phase [F1/P-2] (the conditional [C-O33] belongs to P-2's numeric half) executed by extending the [F2/A1] engine's design class — commit tag `[F1/P-2][F2/A1][PIANO/S20]`. |
| 2 | GATE (pre-execution, standing directive `gate-pre-esecuzione`) — **(A) PLAN ADHERENCE.** This session is NOT an orphan step (R1): it is residue (a) of the S19 closure, named in THREE places of record with the same words (PROGRESS NEXT-1 option (a); D6 item 9 S19 annotation, "re-optimize in a RICHER, ADAPTIVE design class (error-driven knot construction, not more uniform nodes) and re-measure the corner row"; P2_outline §5 addendum (iv), the design-class residue with its procedure named), and the user's order opening this session selects it explicitly. Phase binding: the GOAL is [F1/P-2] (closing [C-O33], the numeric conditional of Lemma A's identification (iii)); the MEANS is [F2/A1] engine-class work (the wall class of [X-TOCV]); both tags carried. Task mapping: T1 = the survey duty that `sota-library-survey-directive` + `generality-nonhardcoded-procedures` impose BEFORE any complex algorithmic construction; T2 = the adaptive class + re-optimization under the UNCHANGED policy stack (DIR-RKG segmentation, P1-P4 levers, all S18 gates); T3 = [X-O33B] re-run = the falsifier of record for [C-O33]. EXCLUDED by declaration, no work done on them: residues (b)/(c)/(d), census-lemma, [PAP-RIM], the G0/T2 review, the rigor bench, G5 and every user lock. **(B) UPSTREAM RIGOR AUDIT** (each point this session stands on, re-read now, not assumed): (i) THE DIAGNOSIS BEING ACTED ON IS MEASURED, NOT ASSUMED: S19 established the corner mismatch 6.6295e-02 is MESH-INDEPENDENT at fixed design (6.6368e-02 at r=2) and falls to 1.998e-02 purely by enriching the class toward a faithful Rao wall at the SAME mesh, 1.4875e-02 there at r=2 — the dominant term is the design class. This session TESTS that diagnosis by construction: if the adaptive optimum does not pull the row down, the diagnosis is falsified (kill semantics in (C)). (ii) THE ENGINE IS CERTIFIED AND ITS GATES TRAVEL: [X-TOCV] gates (per-cell Newton certification, axial-margin rejector with the P4 instance floor 0.1202 m/s for converged-design audits and 0.0 for in-optimization records, RK-G monitor (i) replay fidelity, O3.1) are design-class-agnostic — they gate every march regardless of knot placement; nothing in P1-P4 is re-opened (do-not-redo, `s18-brick2-closed`). (iii) THE VERDICT FORM IS SETTLED AND IS NOT RE-LITIGATED: R3's verdict is the TWO-KNOB convergence form (S19; a Richardson band on one finite-dimensional instance cannot confirm a continuum identity — adjudicated in-session S19 with the refuted-stationarity note printed by the carrier); the R6 primary criterion stays as passed; the locus of the control surface is the S19-corrected one (kernel boundary from march topology, `owner`-based, NEVER re-derived from invariants); the O3.2 falsifier is ONE-SIDED (LBML audit line) — none of these is touched by this session. (iv) THE CLASS CHANGE DOES NOT TOUCH THEORY CLASSES: [C-O33] is SCHEMA/declared-NUMERIC (SCAFFOLD §3 rule 4), so closing it is a MEASUREMENT; (i)-(iii) stay THEOREM* unless and until the measured residuals support more — this session can at most discharge the numeric conditional, never upgrade a proof class by itself. (v) U1/C_geo REGULARITY CONTRACT: the current wall class (natural cubic spline, clamped left) is the U1 C_geo class of record (C^2); an adaptive KNOT construction changes node PLACEMENT, not the spline class — the class regularity statement survives knot insertion (a cubic spline on any strictly increasing knot set is C^2); if the survey (T1) recommends a different BASIS, its regularity vs the U1 contract is adjudicated THERE, in writing, before adoption. (vi) GENO-INDEPENDENCE: unchanged — the indicator candidate is OUR f2 residual on OUR field; GENO supplies only the second design instance in R7 (cross-DESIGN, not cross-code) and the two S19 data defects are already guarded in `geno_design`. (vii) ANCHOR (P) of D2.6: the product is certificate-grade evidence on the (S*, delta) machinery's gradient at a richer instance class — no hardware claim, no design claim beyond the declared instance. (viii) QUARANTINE: L-X-H 2022 not consulted; no unverified page enters (survey sources are read directly, and anything adopted gets its source READ per the directive). **(C) PRE-DECLARATIONS BEFORE ANY DECISIVE RUN (R5 pre-registration integrity):** [D1] KILL SEMANTICS OF T3, fixed now: the S19 baseline for row R3 is 6.6295e-02 (8-node class, r=1). If the re-optimized adaptive-class design's corner mismatch does NOT fall below that baseline, the "design class is the dominant term" diagnosis is FALSIFIED and the session records that verdict — no band adjustment, no re-run shopping. The QUANTITATIVE success reading (how far it falls) is reported against the S19 two-knob ladder (1.998e-02 at N=60 uniform-Rao; 1.4875e-02 at r=2) but the PASS/FAIL of the diagnosis is the one-sided fall-below-baseline test, declared here before any adaptive run exists. [D2] R1/R7 NON-DEGRADATION, fixed now: on the new optimum f2 must remain constant on the control surface at the instance's discretization level (drift < 3%, the [X-O33B] band of record) and the two-design Rao-constant agreement (R7) must survive re-measurement; a new optimum that degrades R1/R7 is a FAIL of the session's execution (not of the diagnosis) and is reported as such. [D3] STOPPING RULE for the enrichment cycle (T2), fixed in form now, constants derived in T2 from measured quantities: the cycle stops when the MEASURED objective indicator (the R3 corner mismatch on the re-optimized design, the quantity this session exists to move) stops improving by more than its own two-resolution noise floor, or when the declared budget is exhausted — never a fixed trip count. [D4] BUDGET, declared in T2 before the first re-optimization with the measured per-run cost in hand (S18 precedent ~400 s/run); abandonment = R3 honest state + named lever. [D5] NO NEW BANDS: every tolerance used in T3 is the [X-O33B] derived-band machinery unchanged; the adaptive-class carrier adds no tolerance that is not derived and printed. **(D) VERDICT: gate pre-esecuzione PASS** — proceed to T1 (survey + decision). |
| 3 | T1 [SURVEY + DECISION], per `sota-library-survey-directive` (survey with SOTA tools + read-the-source + adopt-or-declare-why-not) and `generality-nonhardcoded-procedures` (general, SOTA, economical) — executed BEFORE any code. **ROADS SURVEYED (web, current sources).** (1) FREE-KNOT SPLINES (knots as joint optimization variables): the classical obstruction stands in the current literature — Jupp 1978's lethargy theorem (the objective has many spurious stationary points on faces of the feasible region, knots coalesce; the standard mitigation is Jupp's logarithmic knot transform) and persistent ill-conditioning reported across the free-knot literature into 2024-era work (arXiv 2003.03847, NPL CMSC 4/8 testing report, optimization-online L1-free-knot). (2) A-POSTERIORI RESIDUAL-DRIVEN KNOT INSERTION: the reference open implementation is FITPACK's fpcurf/fpknot (Dierckx), whose modern READABLE form ships in OUR pinned scipy 1.18 as `scipy/interpolate/_fitpack_repro.py` (`generate_knots`/`add_knot`, versionadded 1.15) — source READ this session, findings below. (3) DWR / GOAL-ORIENTED REFINEMENT (Becker-Rannacher, Acta Numerica 2001): weight local residuals by the ADJOINT sensitivity of the goal quantity; marking by Dörfler bulk criterion — the standard in adaptive FEM and current DWR practice (arXiv 2511.09333, 2404.01738). (4) HIERARCHICAL/THB B-SPLINES (Vuong et al. CMAME 2011, Giannelli et al.): local refinement machinery for >=2D tensor-product bases in IGA. (5) NURBS-IN-SHAPE-OPT practice: B-spline control-point design spaces with progressive design-space enrichment; knot insertion is geometry-preserving (Boehm) so enrichment warm-starts from the incumbent optimum. **READ-THE-SOURCE FINDINGS (directive's read half, on the adopted skeleton's reference implementation, scipy 1.18 `_fitpack_repro.py`):** insertion logic = split data into knot intervals, find the interval with MAXIMUM residual sum, insert the new knot AT A DATA SITE inside that interval (never an averaged abstract position — "the knots are a subset of x"); possibly SEVERAL knots per pass (the FITPACK nplus doubling heuristic, visible in the generator's documented output 8,9,10,12,16,24,...); HIDDEN CONSTANTS surfaced: TOL = 0.001 and MAXIT = 20 hardcoded from curfit.f, and termination on the smoothing criterion fp <= s or the nest cap. NONE of those constants is adopted (they belong to the data-smoothing problem, not ours); what IS adopted is the STRUCTURAL skeleton. **DECISION OF RECORD (registered in D6 item 9, S20 annotation).** ADOPTED: the AFEM/FITPACK adaptive skeleton — solve -> per-interval GOAL indicator -> Dörfler bulk marking (theta = 1/2 declared as the canonical bulk fraction; marking sets for theta in {0.3, 0.5, 0.7} printed for auditability) -> knot insertion at the residual-carrying station -> re-optimize -> repeat under the gate-[D3] measured stopping rule. INDICATOR ADJUDICATED (the mandate's explicit question — the S19 candidate judged against the survey, not assumed): the f2 drift per control-surface segment, mapped to the emitting wall interval via the march topology (`owner`), IS the DWR-conformant choice, not merely the classical one — by Prop. A3, f2 = -lambda2 is the ADJOINT variable itself, so its segment drift is an adjoint-weighted optimality residual of the goal J (a multiplier-compatibility defect), which is precisely the object Becker-Rannacher weighting constructs; a plain data-misfit indicator (FITPACK's own residual) has no goal orientation and is REJECTED for the outer loop. BASIS CLASS UNCHANGED, adjudicated per generality rule 3 (class from the regularity the downstream estimate consumes): the clamped-left natural cubic interpolating spline is the U1 C_geo class of record (C^2 on any strictly increasing knot set — knot insertion changes placement, not class), and the downstream consumers (march curvature, U1 contract, [X-THC1] closure) are placement-agnostic. REJECTED with reasons: (i) joint free-knot optimization — Jupp lethargy + the RK-G interaction (continuous knot motion perturbs wall-search decisions; S18 measured one decision flip per accepted step ALREADY, and knot dofs would double the measured-Hessian cost per segment base) + non-economy; (ii) THB/hierarchical bases — they solve a >=2D tensor-product locality problem that a 1D wall graph does not have (1D knot insertion is already local and exact); (iii) control-point B-spline basis switch — IGA-standard, but it would force re-validation of the entire certified stack ([X-TOCV] representation checks, GENO twin comparisons, the traced spline solve) while the S19 measurement locates the defect in knot PLACEMENT, not in parametrization conditioning, which the driver already handles (measured Jacobi + full segment Hessian); named as a lever if adaptive placement proves insufficient. ADOPTED FROM NURBS-SHAPE-OPT PRACTICE: progressive design-space enrichment with warm start — each cycle starts from the incumbent optimum re-interpolated at the enriched knot set; for an interpolating spline the warm start reproduces the incumbent wall EXACTLY at all previous knots and to the class's own O(h^4) between them, and the warm-start representation deviation is MEASURED and printed each cycle (it is not claimed zero — interpolation is not Boehm insertion; declared). GENERALITY AUDIT of the adopted procedure: no constant depends on this eps/L/throat — the indicator is defined on any march (owner exists for every design), Dörfler marking is scale-free (a fraction of measured mass), insertion sites are march stations (case-derived, not hand-placed), the stopping rule compares the goal against its own measured noise, and the budget is declared in wall-clock, not tuned counts. |
| 4 | T2 CARRIER WRITTEN + PRE-DECLARATIONS, **committed BEFORE the decisive run** (R5; the S19 step-3 precedent). NEW CARRIER `validation/adaptive_knot_optimize.py`, registry ID **[X-AKNO]** (124 -> 125 entries). ENGINE KNOB (additive, bit-identical default — the S19 `m_stop` pattern): [X-TOCV] gains module constant `KNOT_XI` (normalized interior knot abscissae in (0,1], last = 1.0); `None` = the uniform class, reproducing every pre-S20 record path unchanged; the knots move affinely with xB exactly as the uniform class does, are NOT design dofs (free-knot rejected, step 3), and are frozen during each optimization. WHAT THE CARRIER DOES (the adopted skeleton, step 3): (i) INDICATOR — per-segment |Delta f2| on the classical control surface (S19 locus of record, `owner`-based), attributed to the EMITTING wall station via the march topology (`cols[k].cline[0]`, never index arithmetic), accumulated per knot interval; NO exclusion mask, declared (the registered norms bind field-level CHECKS; the indicator is a refinement driver, not a verdict row). (ii) MARKING — Doerfler bulk, theta = 1/2 declared canonical, sets for {0.3, 0.5, 0.7} printed; insertions per cycle CAPPED at 3 (a declared BUDGET constraint: each dof adds n+1 gradient evals to every segment-base Hessian). (iii) INSERTION — at the residual-mass MEDIAN station inside the marked interval (equidistribution / FITPACK data-site rule read at source); a site closer than one local station spacing to a knot is SKIPPED (the GENO double-point lesson). (iv) WARM START — incumbent optimum re-interpolated at the enriched knots; NOT claimed geometry-preserving; the wall deviation is measured and printed per cycle. (v) RE-OPTIMIZATION — `run_trsqp` UNCHANGED (DIR-RKG segmentation, measured Jacobi, measured full segment-base Hessian, carried radius), derived tolerances by the [X-TOCV] recipe evaluated at each warm start. (vi) GATES PER CYCLE, all of record: per-cell Newton certification + axial-margin rejector inside every march; P4 audit of each converged design against delta_inst = min_margin(baseline W*)/K_RICH (the P4 recipe with the incumbent optimum as the certified base design, measured at run time); RK-G monitor (i) inside the driver; O3.1 dot-product spot check at each warm start AND each cycle optimum. (vii) STOPPING (gate [D3] instantiated): tol_stop = K_RICH x |rel(r=1) - rel(r=2)| MEASURED at the baseline (the goal metric's own mesh dependence — S19 found the mismatch mesh-independent, so the floor is honest); stop on improvement < tol_stop, on a rise (best kept, reported), on an empty admissible marked set, or on budget. (viii) MACHINERY REJECTORS (executed, PASS before this commit): known-answer single-jump chain concentrates >= 99% and Doerfler marks exactly that interval; a flat-drift chain marks > 1 interval; a zero-mass chain yields the empty set; on the live design the marking must be a STRICT subset. **BUDGET DECLARED (gate [D4] discharged):** A1_AKN_MAXCYC = 4 enrichment cycles, hard wall-clock cap A1_AKN_BUDGET_S = 5400 s for the whole carrier run, <= 3 insertions/cycle; measured anchor = the S18 run of record ~400 s per re-optimization from a 1.5% perturbed start, warm starts expected cheaper (declared expectation, not a number of record); if the budget exhausts before the stopping rule fires, the stop is HONEST (printed) and session closure follows `general-vision-nondivergence` (state + named lever). T3 WIRING, also committed BEFORE the decisive run: [X-O33B] gains env `A1_O33_DESIGN` (loads the [X-AKNO] JSON design of record: W, xi, provenance; default unset = bit-identical S18/S19 instance), `march_design`/`geno_design` now set/restore KNOT_XI too (GENO comparison designs stay in the uniform class), `perturbation_direction` samples the bump at the ambient class's own knots, and the R3 two-knob check selection is INSTANCE-CONDITIONAL, fixed now: in design-loaded mode the directional check "N=60 beats our instance by 2x" (which encodes the 8-NODE baseline's ladder position and is NOT a claim of record for an adaptive instance) is replaced by the pre-declared [D1] kill test — adaptive mismatch < live-marched 8-node baseline, both sides in the same process — while the instance-independent mesh-limit check at N=60 runs in both modes. No band, no tolerance, no verdict rule of the pre-registered protocol is touched. |
| 5 | T2 EXECUTION, ATTEMPT 1 — DECLARED, and it found a DRIVER DEFECT OF RECORD before it found an optimum. **WHAT HAPPENED.** Selftest 3/3 PASS; baseline PASS and it REPRODUCES the S19 number of record exactly: corner mismatch 6.6295e-02 (r=1) / 6.6368e-02 (r=2) -> tol_stop = 2.9411e-04; the indicator is INFORMATIVE (S2 strict-subset PASS): 41.8% of the f2-residual mass in the first knot interval after the attachment, 18.9% in the second, 17.6% in the lip interval — the residual lives where the wall turns hardest, the qualitative confirmation of the S19 diagnosis. Doerfler(0.5) marked [0, 1]; two knots inserted (x = 0.3146, 0.7026), class 8 -> 10 dofs, warm-start wall deviation 4.4e-16, O3.1 at the warm start PASS. Cycle-1 TR-SQP: segment 0 accepted 2 iterations (J already 2.7771718e+07 > the S18 J* = 2.7761688e+07, the richer class gaining thrust), ended on a decision flip — then **CRASH at the segment-1 base: "P4 gate: record at segment base not certified (worst 1.170e+00)"**. All attempt-1 optimization numbers are DISCARDED as results (S18 practice); they are kept here as the defect's evidence. **THE DEFECT, mechanism-level (read at source, not guessed).** DIR-RKG P3(ii) of record declares "per-cell certification of the record at every ACCEPTED iterate" — but `run_trsqp`'s callback re-recorded accepted iterates WITHOUT checking cert_worst (the check lived only at segment bases), so scipy accepted an iterate whose record was marginally uncertifiable (worst 1.170; the while-Newton cell metric is bounded by N_NEWTON trips and the post-hoc step_norm at the returned z can sit just above the floor on a hard cell — the enriched wall's tighter first interval produced exactly such a cell); the segment then ended on a flip, and the next segment base honestly refused the point. Worse, the callback's record-failure path PROMISED "restart from last W" in its comment but restarted from `res.x` — exactly the failed iterate — a guaranteed-crash recovery path that S18 never exercised because no record ever failed there. **THE FIX, policy-conformant, no gate touched** (committed with this step, BEFORE the decisive re-run): (i) the callback now checks cert_worst at every accepted iterate — the code brought INTO conformance with the P3(ii) text; an uncertified accepted iterate ends the segment; (ii) the segment loop gains REJECT-AND-SHRINK (standard trust-region semantics, Conn-Gould-Toint): a base whose record fails ANY record gate is rejected, the driver reverts to the last CERTIFIED base and halves the carried radius (floor 1e-3, at which the failure is genuine and raises honestly); (iii) the carrier ends the enrichment loop honestly on a genuine driver raise, keeping the incumbent best (nondivergence). **A PROCESS ERROR OF MINE, declared**: attempt 1 was launched piped through `tee`, so the notified exit code was tee's 0 while python had CRASHED — the S18 9-bis lesson (never gate on a piped exit code) recurring in monitoring form; re-runs use direct redirection and PYTHONUNBUFFERED, exit code from python itself. |
| 6 | T2 EXECUTION, ATTEMPT 2 — the step-5 fix HELD, and the run went further before exposing a SECOND defect and a physical boundary. Run stopped BY USER ORDER mid-cycle-1; all numbers below are from the printed log, and NO optimization result is claimed of record. **WHAT THE FIX BOUGHT (measured).** Baseline reproduced bit-for-bit (6.6295e-02 / 6.6368e-02, tol_stop 2.9411e-04); indicator identical (41.8% / 18.9% / 17.6% of the f2-residual mass in the first, second and lip intervals; Doerfler(0.5) = [0, 1]); the same two knots inserted (x = 0.3146, 0.7026; class 8 -> 10 dofs; warm-start deviation 4.4e-16; O3.1 PASS). Cycle-1 TR-SQP then ran through the point that killed attempt 1: at segment 1 the base was REJECTED (worst 1.170e+00), the driver reverted to the last certified base and halved the radius, and the walk CONTINUED — segments 2-6 accepted with **J rising monotonically 2.7771718e+07 -> 2.7773170 -> 2.7774918 -> 2.7775368 -> 2.7775635e+07** (the last is +1.39e+04 over the S18 8-node J* = 2.7761688e+07) and **KKT falling 1.726e+06 -> 1.176e+05 -> ... -> 9.050e+04**. So the enriched class does buy objective, and the reject-and-shrink path works. **DEFECT 2, MEASURED AS A LIVELOCK.** From segment 7 the run entered a bit-identical cycle: REJECT (worst **2.458e+00**, the same value every time) -> revert -> accept -> J, KKT and cert_worst IDENTICAL (2.7775635e+07, 9.050e+04, 2.458e+00) over segments 7-17, radius reported 0.05 -> 0.025 -> 0.0125 -> 0.01005 -> 0.005 -> **back to 0.01005**. Mechanism, read off the driver: setting `tr0` after a rejection shrinks NOTHING, because the next segment's callback re-captures scipy's OWN grown trust radius and the clip `[1e-3, 0.25]` lets it grow straight back; the proposed step (shorter than the radius) is therefore identical forever. The S18 driver never exposed this because no record ever failed there. **FIX 2 (written, syntax-checked, machinery selftest 3/3 PASS — NOT yet exercised on a decisive run; declared):** (i) the radius bound RATCHETS — `tr_cap` only ever decreases on a certification rejection and clips every later carry, so a forced shrink cannot be undone; (ii) when reject-and-shrink is EXHAUSTED at the floor, `run_trsqp` no longer raises: it returns the last CERTIFIED base with `certifiability_limited=True` and the KKT reported OPEN — the honest reading being that the design sits at the CERTIFIABILITY BOUNDARY of its class, not at a stationary point; (iii) the carrier's per-cycle verdict is split into two DECLARED outcome classes, fixed here before the decisive run: either "converged in-stratum" (the S18 check, unchanged) or "certifiability-limited", the latter still requiring a CERTIFIED objective improvement over its own warm start — a check that can fail, so this is a different verdict, not a weakened one. **PRE-DECLARED DIAGNOSTIC, ARMED AND NOT YET RUN (R5 — declared here, before the number exists).** cert_worst > 1 has exactly two candidate causes and one measurement separates them: (a) the cell is genuinely non-convergent on that wall (geometry/physics — then the rejection IS the verdict and the certifiability boundary is a real property of the enriched class), or (b) the while-Newton hit its TRIP CAP `N_NEWTON = 30` — a COMPUTE BUDGET, not a tolerance — and returned with the step still above the certification floor `NEWTON_TOL_FACTOR * eps * scale`. TEST (env `A1_RKG_CERTDIAG=1`, default OFF, purely reporting, committed with this step): re-record the SAME rejected design with the cap raised 10x and **the floor untouched**; cert_worst dropping below 1 => trip-cap artifact, staying above => genuine non-convergence. NOTHING is acted on from this diagnostic in this session: no cap is raised, no floor is moved, no band is amended. **HONEST STATE OF T2 AT THE STOP:** the enrichment loop is at cycle 1, no goal metric has been measured on a re-optimized adaptive design, so **[D1] IS UNTESTED and [C-O33] IS NEITHER DISCHARGED NOR FALSIFIED** — the session ends with the discharge procedure BUILT and its first obstruction diagnosed, not with the verdict it set out to get. |
| 7 | CRITICAL AUDIT (user order: resume with extreme rigor, judging the uncommitted changes) — **FOUR DEFECTS FOUND IN MY OWN UNCOMMITTED M0 DRAFT AND IN THE STEP-6 SEMANTICS, all corrected BEFORE any of it was committed.** [A] FALSE CERTIFICATION CLAIM: the draft called J = 2.7775635e+07 "the certified objective". Traced against the attempt-2 log it is the OPPOSITE: segment 6 ended with the flip counter UNCHANGED at 4 — i.e. on the new P3(ii) callback check — and its accepted point is exactly the design segment 7 rejects at worst 2.458. The last CERTIFIED accepted design is segment 5's, J = 2.7775368e+07 (+1.27e+04 over the S18 J*), with the certified-prefix KKT descent 1.726e+06 -> 5.056e+04. The corrected M0 block splits the numbers by certification status and claims no J from the uncertifiable design. [B] BOUNDARY OVERCLAIM: the draft asserted "a certifiability boundary EXISTS in the enriched class". What is measured is ONE uncertifiable target design, re-proposed bit-identically BECAUSE the shrink was defective — the walk never tested shorter steps, so boundary-further-out / genuine-boundary / trip-cap-artefact are three OPEN hypotheses, each with its pre-declared discriminating measurement (the ratcheted walk; the certdiag). [C] the draft's "lesson for the program" asserted as established what [B] leaves open — downgraded to a WORKING HYPOTHESIS, explicitly marked NOT established. [D] THE DEEPEST ONE, formal: the step-6 outcome semantics let a certifiability-limited cycle count toward the goal measurement. But identification (iii) is DERIVED FROM OPTIMALITY — [X-O33B]'s own R3 control REQUIRES the identity to break at non-optimal designs — so a corner mismatch measured at a KKT-open design confounds class error with non-stationarity and CANNOT test [D1]. **FORMALIZATION OF RECORD (now in M0 Part VI):** the engine's optimization is max J over the class INTERSECTED WITH THE CERTIFIABLE SET K (all record gates pass); K never bound the S18 8-node walk, the enriched class made its boundary reachable; K = K_phys intersect K_budget (the trip-cap cut), and only K_budget may ever be enlarged — the certification floor is a tolerance and does not move. Termination is two-class: outcome I (in-stratum converged, the S18 gate) licenses the corner-row measurement; outcome II (certifiability-limited, KKT open) licenses ONLY the certified-objective report. **[D1] VALIDITY CONDITION, fixed here (R5, before any decisive run):** the [D1] comparison is DEFINED over outcome-I designs only; outcome-II goals are printed as information and excluded; a campaign with no outcome-I cycle leaves [D1] UNTESTABLE (neither confirmed nor falsified — exit 1 by construction, the [X-O32] precedent) and the obstruction goes to adjudication. CODE BROUGHT TO THE SAME STANDARD (committed with this step): the carrier excludes outcome-II cycles from best/[D1], stops enrichment at an obstruction, and computes every outcome-II J/goal from the design's OWN P4-audited record (the draft replayed W_new on the warm start's plan — replay infidelity, fixed); the driver's exhausted branch now RAISES when the failing base IS the last certified base (returning it as "certified" after it just failed would be a contradiction; deterministic-replay violations must surface); the [X-O33B] design loader REFUSES non-[D1]-eligible artifacts (the JSON now carries d1_eligible). **PRE-DECLARATIONS FOR THE DECISIVE ATTEMPT 3 (R5):** driver = the ratcheted reject-and-shrink of step 6 + this step's corrections; env A1_RKG_CERTDIAG=1 ARMED for the run — it is REPORTING-ONLY (re-records any rejected design with N_NEWTON x10 and the floor UNTOUCHED, prints trip-cap-artifact vs genuine, acts on nothing); budget unchanged (4 cycles / 5400 s / <= 3 insertions); every gate, band and gtol recipe unchanged. Possible outcomes, all declared in advance: (i) outcome-I cycle(s) -> [D1] tested (kill semantics of the gate, unchanged); (ii) all cycles outcome-II -> [D1] UNTESTABLE, obstruction adjudicated (certdiag verdict + user decision on K_budget); (iii) driver contradiction raise -> honest fail. |
| 8 | T2 EXECUTION, ATTEMPT 3 — THE DECISIVE RUN OF RECORD (exit 1 BY CONSTRUCTION, the [X-O32] precedent: the campaign did not reach its verdict; carrier log preserved). **THE FIXES HELD.** Ratchet monotone 0.10 -> 0.05 -> 0.025 -> 0.0125 -> 6.25e-03 -> 3.125e-03 -> 1.563e-03 -> 1.000e-03 (floor) with NO livelock; 23 segments, 39 re-records, 1046 s, every executed check PASS except the final [D1]-untestable row. **CERTDIAG EXECUTED 8/8, ALL "GENUINE"**: every rejected design re-recorded with N_NEWTON 30 -> 300 and the floor UNTOUCHED returns a BIT-IDENTICAL cert_worst (1.170, 2.458 x4, 1.060, 1.455, 1.698) — the trip-cap hypothesis (K_budget) is DEAD, eight times over; the frozen damped-Newton mechanism (TRIALS includes t=0: the iterate freezes when no trial fraction reduces the residual norm) is the stalling mode. **BOUNDARY CRAWL MEASURED, the run's central finding**: rejections at FIVE DISTINCT nearby designs (distinct worst values) while the CERTIFIED walk advanced J = 2.7771718 -> 2.7775702e+07 (+1.40e+04 over the S18 8-node J* = 2.7761688e+07) and the KKT fell 1.726e+06 -> 3.795e+02 (seg 21; still ~400x above gtol = 9.338e-01) — the ascent direction leaves the certifiable set from every certified base near the frontier: an ACTIVE-CONSTRAINT signature, not an isolated bad point. **OUTCOME II DECLARED** (per the step-6/7 semantics): the driver returned the last CERTIFIED base; its J OF RECORD = 2.7775702e+07 from its OWN P4-audited record (the seg-20 replay print 2.7775619e+07 is the plan-FROZEN replay of a flipped decision — a 3e-05 relative plan-staleness nuance, declared); P4 audit PASS with min axial margin 0.4809 m/s UNCHANGED from the baseline — global causality-margin erosion is EXCLUDED as the mechanism; O3.1 PASS at the returned design; corner mismatch printed 6.6826e-02 as INFORMATION ONLY ([D1]-ineligible, non-stationary design). Enrichment loop STOPPED at the obstruction per the pre-declared rule; artifact `validation/s20_adaptive_design.json` written with d1_eligible = false (the [X-O33B] loader will refuse it — wired in step 7). **VERDICT: [D1] UNTESTABLE on this campaign; [C-O33] NEITHER discharged NOR falsified; the obstruction is characterized and adjudicated in step 9.** |
| 9 | ADJUDICATION + THE CLASSICAL CONNECTION (all sources READ this session, page-verified) + THE GENERAL FORMALIZATION + DECLARED AUDIT GAPS. **(a) MECHANISM STATUS, honest**: what is ESTABLISHED is that the walk hits the boundary of the DISCRETE certifiable set K at genuinely stalling cells (certdiag 8/8) with the global causality margin NOT eroding; the IDENTIFICATION of that boundary with the physical fold/coalescence boundary is a HYPOTHESIS with two alternatives not yet excluded (local spline-curvature/wall-search conditioning near the inserted knots; knot-vs-station representation mismatch) and with named discriminating measurements: argmax-cell localization (instrumentation gap — cert tracks the worst RATIO, not its location), adjacent SAME-family characteristic crossing distances near the stalling cell, and the classical validity monitor below. **(b) THE CLASSICAL CONNECTION (Rao & Beck, AIAA 94-3264, pp. 1-4 read; GENO `Profile_m.f90` DEF branch read)**: the classical corpus ALREADY NAMES our forbidden zone — Sternin 1962, "The Boundary of the Region of Existence of Optimal Nozzles Free of Shock Waves" (Sov. Phys. Doklady; cited as Ref. 3), with Rao-Beck Eq. (4) as the closed-form boundary at the kernel/control-surface junction D, their Eq. (1) being LITERALLY our f2 invariant; minimum-length optimum-thrust nozzles sit ON that boundary; and the DEF construction goes BEYOND it by an isentropic Prandtl-Meyer compression coalescing EXACTLY AT the control-surface point D' (zero interior shock extent, flow isentropic upstream of D'DE, "the downstream flow has shocks compatible with the downstream pressure" — the shock exists only OUTSIDE the design domain), Shmyglevskii 1981 giving the variational treatment of such surface discontinuities; measured classically: -22% length at -0.3% thrust, slight thrust ADVANTAGE at equal length. GENO implements exactly this in PROD (type-2 flagdef; the DE<->BD mass-flow equality IS the DEF design constraint — Profile_m.f90 A4 block). This answers the coalescence-shock objection (position/strength of an accumulating shock are NOT deducible from envelope geometry): Rao-Beck AVOID the interior shock by construction (inverse method, coalescence point = a design choice on the boundary); the GENERAL rigorous treatment makes front position/strength UNKNOWNS of a fitted-front solve. **(c) THE GENERAL FORMALIZATION OF RECORD (written to M0 Part VI, rigor classes declared there)**: the design problem is posed on a LADDER of certified solution classes — max J s.t. g = 0, Sigma in C_m INTERSECT A_t(mu_0), where A_t(mu_0) = designs whose flow problem has a solution in tier t's class with the uniform MARGIN VECTOR m(Sigma) >= mu_0 (fold margin = distance from same-family coalescence; causality margin u_x - c; the D(delta, L_x, C_geo, C_dat, h_min) constants); S_0 (shock-free) SUBSET S_1 (finitely many fitted fronts with RH + entropy + Lax + Lopatinskii certificates) trivially nests; first-order optimality at a boundary-active optimum is KKT WITH THE MARGIN MULTIPLIER (grad J = lambda grad g + mu grad m, mu >= 0) — the term the S18/S20 formulation lacked, which is WHY outcome I was impossible in principle; and the measured multiplier at a margin-constrained optimum PRICES shock-freeness, deciding the tier-1 transition (our G12/F2 line; DEF = the zero-interior-extent limiting case and the cross-code validation anchor). Rigor classes: nesting + constrained-KKT structure = THEOREM (standard); the ladder/A_t definitions = SCHEMA; "the S20 instance optimum is boundary-active" = measured-supported HYPOTHESIS (the crawl + 8/8 genuine); "K_disc approximates A_0" = CONJECTURE with falsifier = the Eq.(4)/Sternin monitor evaluated along the walk (approaches the boundary as certification degrades, or the bridge is dead). Convention trap of record for any implementation: Rao's C+/C- naming is INVERTED vs Zucrow/GENO (GENO CLAUDE.md). **(d) SOTA METHOD ADJUDICATION for the discharge (recorded for the S21 decision)**: tier-0 = margin-CONSTRAINED re-optimization with the fold margin as the constraint (KS/log-sum-exp aggregation — the standard for min-type constraints, conservative side correct — gradient free through AD; floor derived, never tuned; Eq. (4) as the GENO-independent classical cross-check monitor, NOT the general constraint); tier-1(i) DEF-style coalescence-on-the-surface = thin-set special case, kept as validation anchor, not the general method; tier-1(ii) fitted-front optimization with front position/strength as unknowns under the certificate stack = the general rigorous method (adjoint jump conditions native to the characteristic-aligned fitted march, P2_outline §5(e); captured-shock adjoints rejected for rigor per Giles-Ulbrich/Lozano, already in the citation stack). **(e) AUDIT GAPS, declared**: (i) the T1 survey did NOT weigh the candidate-dof-gradient indicator (progressive design-space refinement lineage in aerodynamic shape optimization; costs ONE record+gradient per cycle for ALL candidates via AD) — an A/B against the f2-drift indicator is queued, not assumed either way; (ii) the user issued the standing agnostic-milestone-review directive mid-session (saved to memory `agnostic-milestone-review-directive`): audits must state scope, cover committed AND uncommitted work, anchor to the milestone, name the rabbit-hole boundary in advance — applied here: the driver-surgery sequence is adjudicated NOT-a-rabbit-hole (each round defect-driven, policy-conformant), and the pre-named boundary holds: NO further driver work; next moves are measurement (localization + monitor) and formulation (margin constraint), or the standing S19 fallback. **QUERY-BOUNDED NOVELTY NOTE**: the certification-gate <-> Sternin-boundary bridge would be OUR contribution IF the monitor confirms it; Sternin/Shmyglevskii/Rao-Beck become mandatory citations of this line either way. **(f) CLOSURE STATUS OF THE CLASSICAL/SOTA SIDE, on the user's direct challenge, honest**: CLOSED = Rao-Beck AIAA 94-3264 read IN FULL (4 pp. + references) and GENO's DEF implementation read at source; NOT CLOSED and DECLARED = Sternin 1962 and Shmyglevskii 1981 are cited VIA Rao-Beck's reference list only, NOT independently page-verified — both added to the D6 acquisition queue, and M0 marks their citation status explicitly; the tier-1 (fitted-front) method's own SOTA survey is QUEUED to the G12/F2 session, not claimed done. **SOTA FRAME FOR THE GENERAL PROBLEM, surveyed with web tools this step**: the canonical taxonomy for constraints of this kind is Le Digabel-Wild ("A Taxonomy of Constraints in Simulation-Based Optimization" — the KARQ/NUSH classification): our certifiability constraint is TODAY Known-Unrelaxable-Simulation-NONQUANTIFIABLE (pass/fail known only after paying a march; a failed record yields no usable J), the class the taxonomy identifies as the hardest tractable one; the recognized remedy, when the physics exposes one, is to QUANTIFY a margin — which is precisely what the margin-constrained reformulation does (m quantifiable BEFORE violation, AD gradient), with Kreisselmeier-Steinhauser aggregation confirmed by the survey as the standard for min/max constraint fields under adjoint sensitivities (conservative side correct; adaptive-parameter variants = the current refinement). The formalization is thereby anchored to the recognized frame instead of ad-hoc language. |
| 10 | **SESSION CLOSURE (R3).** Commits: apertura+gate = f2be862, T1 survey = 7cfb49b, carrier+knob+wiring = 1d93719, fix P3(ii) = ed84f6e, fix livelock+outcome-II+certdiag = cd0e204, audit critico+[D1] validity condition = f33e813, attempt-3+R4 = 5ece8d3, chiusura = (this). Lint (xv) green ON THE EXIT CODE at every commit (124 -> 125 entries: +[X-AKNO]). **CLOSURE SUITE OF RECORD: 15/15 groups PASS in 150 s** (exit 0). **SESSION VERDICT, honest.** The session set out to close [C-O33] via the adaptive design class and DID NOT: [D1] is UNTESTABLE (no cycle converged in-stratum) and [C-O33] is NEITHER discharged NOR falsified. What the session DID produce, all of record: (1) the general adaptive-knot machinery ([X-AKNO], survey-adjudicated, selftested, rejector-bearing) with the spatially resolved indicator finding (41.8% of the optimality residual in the first post-attachment interval — the first direct evidence for the S19 diagnosis beyond the two-knob ladder); (2) TWO driver defects of record found and fixed policy-conformantly, both unexercisable from the uniform class; (3) the OBSTRUCTION characterized to the limit of what the stopped runs license: an active-constraint signature at the certifiability frontier, trip-cap hypothesis dead 8/8, causality-margin erosion excluded, mechanism NOT yet localized (instrumentation gap named); (4) the FORMULATION GAP adjudicated and the GENERAL FORMALIZATION written to M0 (tier ladder of certified solution classes, margin-constrained KKT, multiplier-priced tier transitions) with the classical anchor page-verified (Rao-Beck IN FULL; Sternin/Shmyglevskii declared cited-via-only, acquisition queued) and the SOTA anchor surveyed (Le Digabel-Wild taxonomy; KS aggregation); (5) the [D1] validity condition (optimality-derived identities are measurable only at in-stratum optima) — a rigor rule that outlives this session; (6) one new standing user directive received and saved (`agnostic-milestone-review-directive`), applied in-session (steps 7, 9). ERRORS OF MINE IN THE RECORD, not buried: the four draft defects of step 7 (false certification claim, boundary overclaim, lesson overclaim, the [D1] validity gap in my own step-6 semantics) and the attempt-1 tee-pipe exit-code mask (step 5) — each found, declared, corrected before landing. **NEXT-1 IS A USER DECISION** (PROGRESS ORA, order included): (E) localization + validity monitor (the K<->Sternin bridge test), (A') margin-constrained re-optimization with the constrained [D1] variant pre-derived, the indicator A/B, or (B) the standing S19 fallback for P-2. Pre-named boundary: NO further driver work. Queued behind, unchanged: S19 residues (b)/(c)/(d), census-lemma, [PAP-RIM], G0/T2 review, rigor bench; user locks unchanged (G5, preprint decision — recommendation of record unchanged: arm the arXiv trigger, decision stays the user's). |
