# CONFIRM-ON-REPAIRS — INSTANCE 2 (wave 3 + doc1 rev-10 retro-scope)
# S-FOUNDATIONS-C3, 2026-08-20. BASE = validation/sfoundations_raws_2026-08-13.
# Mandate: BRIEF_wave3_refuter_judge.md § CONFIRM-ON-REPAIRS (instance 2).
# Scope (a): the 21 repair texts sustained by VERDICT_wave3.md — verbatim
# fidelity vs their refuter sources and consumers + minted-defect hunt
# (restatement class). Scope (b): the FOUR doc1 rev-10 carried repairs
# applied at the C2 landing (phaseD_stop_proof.md, mandate =
# VERDICT_doc1_rev10.md §4 items (1)-(4)) + the leg-3 hash command pin,
# verified against the named consumers. IDs CR-W3-* / CR-W3-R10-*.
# Stage status of record: ALWAYS-ON (instance-1 catch CR-W2-1; sunset
# clause not fired — declared in the brief and in VERDICT_wave3 §0.2).
# This lens wrote ONLY this file. Every anchor below re-measured in MY
# window (Read/grep/sed/md5 on the working tree, 2026-08-20).

==============================================================================
## §1 SCOPE (a) — THE 21 SUSTAINED WAVE-3 REPAIRS

Method: for each repair, (i) the refuter's named-repair text was read at
its file and compared clause-by-clause against the judge's adopted text
in VERDICT_wave3 §§1-6; (ii) every load-bearing witness anchor inside
the adopted text was independently re-verified at the source file in
this window (not trusted from §0.1); (iii) judge additions/paraphrases
were hunted for minted defects (wrong operand, inverted direction,
phantom anchor, over-claim).

### 1.1 Verbatim-fidelity verdicts (per repair)

FAM (4):
- R3FAM-1 (C34, M=10 immateriality window): adopted text = the
  refuter's either/or repair (declare M=10 sufficient-not-optimized
  with the direction argument, or derive from the rho-histogram support
  width) — FAITHFUL. K_RICH = 4 re-verified (a1_toc comment :2145;
  refuter's margin_governor.py:28 witness consistent). CONFIRMED.
- R3FAM-4 (C35, xtol_u form): adopted formula xtol_u =
  floor_W/(K_RICH × max(Dv)) with extremal (max-Dv) direction, safety
  DIVIDING, median-form declared vacuous — FAITHFUL VERBATIM to the
  refuter's named repair. Witness re-verified: a1_toc:1741
  `Dv = Dv/np.median(Dv)` (median(Dv) ≡ 1 — the vacuity leg holds);
  W = Dv∘u extremal derivation re-checked (u-radius r leaves W-steps up
  to max(Dv)·r ⇒ safe scalar xtol ≤ floor_W/max(Dv); premature-stop
  risk ⇒ K divides). Judge insert "(the C20-window Newton floor)"
  glossing floor_W is consistent with the §7.2 seam (C18/C20 Tier-1
  owner, C35 rider) and findings :636 (actual floor ~4e-14). Duty homes
  into findings :633-641 owner window — row re-read, owner text intact.
  CONFIRMED.
- R3FAM-9 (C41 P3 dominance bound): adopted text carries K·r/(r+1) +
  declared erosion or eps-budget threshold — FAITHFUL. Arithmetic
  re-derived independently: at r = K = 4, coverage 16/5 = 3.2× vs 4×
  intended = 20% erosion; r ≥ (1−eps)/eps gives r = 19 at eps = 0.05;
  coverage at r=19 is 3.8× = 95% — the 19:1 D-prime datum sits exactly
  at the 5% line. All three numbers in the adopted text CORRECT.
  CONFIRMED.
- R3FAM-11 (C42 interim validity restatement): adopted verbatim incl.
  the roles-4/5 inverted-direction clause (delta_inst = min_margin/
  K_RICH, thr = m_ref/K_RICH — formula corroborated at a1_toc:270
  comment) and the VERDICT_wave1 §2.4 [X-O32] mechanism as the role-1
  checkable clause. Judge's census splice (10 + 3 notified, C8 added)
  is a declared cross-slot reconciliation consuming the same-wave
  PANEL_REMMARCH §4.3 notification — legitimate, not a distortion of
  the repair. CONFIRMED.

C50 (5):
- R3C50-1 (S1′ criterion): adopted S1′ text = the refuter's, minus the
  redundant head "certified first-order bound whose" (semantics
  identical). Judge addition "calibrated-L² solo fails A2" verified as
  panel content at PANEL_C50:283 — consumption, not minting. CONFIRMED.
- R3C50-2 (A1 scoped to flux-integral class): adopted = refuter form
  (i), with form (ii) (product metric) handed to the D1 closure judge
  as the alternative — a declared modification consistent with the
  refuter's own "the judge may prefer (ii) ... not my call" and with
  the D1 structural escalation. Anchor nit logged at CR-W3-2(ii).
  CONFIRMED.
- R3C50-3 (seam conditionalized): adopted text = refuter's repair
  clause verbatim (Λ ∈ L∞(A) imposed dual regularity, G ∈ L∞ on smooth
  complement, finite front sensitivities, FS-1/FS-2 live);
  "impossible by construction" struck. CONFIRMED.
- R3C50-4 (rigor clause): adopted verbatim. theorem_ledger :232-239
  re-read at source: "rigorous only in 1-D [Bressan-Marson 1995;
  Ulbrich SICON 2002] ... 2-D is practice-without-theorem [Baeza et
  al. 2009]" — the :235 anchor names the multi-D gap line. CONFIRMED.
- R3C50-5 (FA-2 formula): adopted Σ_k |[q]_k|·δ_k with O(jitter×slope)
  demoted to the finite-slope-representation reading — FAITHFUL (the
  refuter's parenthetical gloss "slope integrated over the front width
  recovers the jump" dropped; immaterial). The [DATA]-context origin of
  O(jitter×slope) re-verified at findings :2319 (amplitude masquerade
  at shocks). CONFIRMED.

CONV (2):
- R3CONV-1 (C4 full-tree sweep): the adopted parenthesis's census
  RE-MEASURED in my window: `grep -in "knot"` over the four FULL trees
  = exactly 9 hits at opt :367/:430/:432/:467/:480/:1653, var :972,
  prop :778/:786; `jupp|free knot|free-knot|curvature-based|abscissa`
  = ZERO hits on all four. The adopted text's numbers are TRUE ON DISK
  TODAY. CONFIRMED.
- R3CONV-7 (C38 Q10 census + consumption-notice extension): the grep
  RE-RUN in my window: `grep -inE "critical cone|B-stationar"
  docs/*.yaml` = 8 hits / 4 files exactly as adopted (ledger
  :421/:534/:539; claims :1350/:1874; findings :1136/:1947; glossary
  :1126). The judge's correction of the refuter's "five registries" to
  4 files/8 hits is MEASURED-TRUE. Pre-wave-1 carrier quotes verified
  verbatim at claims :1350 ("B-stationarity qualifier until O1", S22)
  and :1874 ("B-STATIONARITY qualifier (O1 undischarged)", S24).
  CONFIRMED.

REMMARCH (2):
- R3REMMARCH-1 (C14 metric re-anchor): the struck sentence and the
  re-anchored metric adopted; the false premise re-verified at source
  (a1:1082-1085 — every row-j2 extension cell carries the imposed
  uniform state `qq`); the mdot closure companion residual verified
  accumulated at a1:1086-1106. The interval "(Nv, j2], computed via
  solver_int" verified CORRECT for the refuter's anchor, the arc-column
  interior sweep a1:958 `for j in range(Nv + 1, j2 + 1)`. ONE
  restatement defect found — CR-W3-1 below. CONFIRMED-WITH-AMENDMENT.
- R3REMMARCH-2 (C12 mdot-channel term): adopted = refuter option (a)
  preferred with (b) as declared floor — exactly the refuter's own
  preference order; exclusion sentence carried with "the mdot channel"
  → "the mdot→Me common mode" (same object, sharper). Common-mode
  witness re-verified at a1:853-862 (Simpson mdot on the Sauer IVL) +
  :864-879 (Me from leggeAree(mdot, eps)). The judge's closing gloss
  (F-C12-2 covers the net channel; the mdot term/exclusion covers the
  other) matches the refuter's F-C12-2 vacuousness-control caveat.
  CONFIRMED.

REMENG (5):
- R3REMENG-2 (F-C16-3 demoted to instrument): adopted verbatim; the
  "forces challenger regardless of cost" overturn clause struck as
  named. CONFIRMED.
- R3REMENG-4 (C19 laxity census): adopted verbatim incl. laxity_i =
  sc/max(1,|z_i|), GAP-29 headroom threshold on the
  laxity-vs-band-dominance axis, duty homed into the F8 row. F8 row
  re-read at findings :608-616: "~100 eps |u| — about 3 decades
  looser", "no verdict compromised (contour bands
  Richardson-dominated)" — the adopted rationale sentence quotes the
  row correctly. CONFIRMED.
- R3REMENG-5 (F-C22-1 probe admissibility): adopted verbatim
  (in-class = predicate falsified + bearer flips — consistent with the
  panel's pinned consequence; out-of-class routed to C10/C5). Anchor
  fix verified: ledger :370 says "compiled the incumbent scan
  (gate-verified)"; "bitwise" lives in the findings GAP-28 delta text
  (3 hits in findings, 0 in the ledger note). CONFIRMED.
- R3REMENG-6 (C37 merge-free proxy): adopted verbatim ((S,xi)
  label-invariance and/or lip-only discarded-Hessian columns; license
  on proxy-flagged subset only; disagreement retires the proxy).
  CONFIRMED.
- R3REMENG-13 (C45 rejector family): adopted verbatim; witness
  re-verified at a1:872 (`q0 = qs[idx[-1]] if idx.size else
  float(as_)*1.8`); the homing target re-read at findings :599-607 —
  the owner prescription in the adopted text ("derive the scan window
  from the table box (q_max from h0 − h(T_TAB_LO)) + assert bracket
  found + supersonic-root rejector") is VERBATIM the F7 row's owner
  field. Judge tightening "M(qe) > 1 + margin" → "1 + derived margin"
  is R5-conformal (direction: stricter), declared in-line. CONFIRMED.

REMPOL (3):
- R3REMPOL-1 (C7 band binding): adopted verbatim. Both bindings
  re-verified at source: adaptive_knot:50-54 carries tol_stop =
  K_RICH * |rel(r=1) - rel(r=2)| exactly; :26 "the indicator is a
  refinement driver, not a verdict row"; :29-34 Doerfler + budget cap;
  :46-48 deviation MEASURED and printed. The phantom-band premise
  (no insertion band exists) re-confirmed: the per-cycle gate list
  (:61-68) contains no representation-deviation band. CONFIRMED.
- R3REMPOL-4 (C26 edge threshold): adopted = refuter's repair with
  "0 + probe-excursion allowance" rendered as "margin < 0 + the
  derived probe-excursion allowance" (same threshold). Transplant
  witness re-verified: ADVISORY_S24_thermo_closure_survey :250-252 —
  the ~4ΔT clause is the INTERNAL-JOINT mollification statement, not
  an edge band. R3REMPOL-3's KAT-B premise fix re-verified at findings
  :576 (lam_ad −0.216 vs −1.185 at M ~ 4.7, CONFIRMED LIVE S21).
  Anchor nit logged at CR-W3-2(i). CONFIRMED.
- R3REMPOL-9 (C47 residual re-declaration): adopted verbatim
  (multiset upgrade SHRINKS, does not close; role-permutations of
  in-file values = the declared residual; SARIF positional
  fingerprints = named-not-adopted path). The permutation argument
  re-derived: swapping two values already present preserves count AND
  multiset — the original closure claim was false as stated, the
  repair is the honest form. CONFIRMED.

### 1.2 Findings minted by this lens on scope (a)

**CR-W3-1 [AMENDMENT — carry to the landing splice, no re-open]**
Row C14 / R3REMMARCH-1. The judge's restatement (VERDICT_wave3 §4 C14)
reads "over the FINAL MARCHED COLUMN (interior cells G[(j, i_exit)],
j ∈ (Nv, j2], computed via solver_int)" and DROPS the refuter's
disambiguating anchor: refute_REMMARCH.md :68-69 pins candidate (i) as
"the last ARC column at exit fire ... (:958 loop)". Two loops exist:
(1) the arc-column interior sweep a1:958 `range(Nv+1, j2+1)` — where
j ∈ (Nv, j2] via solver_int is EXACT; (2) the mach-extension loop
a1:1079-1108 — where the (j2, icol) cell is the IMPOSED uniform state
(the exact false premise this repair struck) and solver_int computes
only j ∈ [Nv, j2-1]. A consumer reading "final marched column" as the
last extension column would re-include the imposed cell and mis-read
the interval. The text is self-disambiguating only for a reader who
checks that the "(Nv, j2] via solver_int" signature matches loop (1)
alone. REPAIR TEXT (one line, landing window): after "FINAL MARCHED
COLUMN" insert "(= the final ARC column at exit fire, the a1:958
interior sweep — NOT the :1079 mach-extension columns, whose j2 cell
is the imposed state)". No consumer moves; the repair's substance is
untouched.

**CR-W3-2 [NOTE — opportunistic anchor corrections, no repair owed]**
Anchor imprecisions minted by refuters and carried into adopted texts;
row identity unambiguous in every case (ids cited alongside), no
consumer misled:
 (i) "findings :579" for the FD-probes-at-P±h near-edge consumers
     (C26 delta / R3REMPOL-4; also VERDICT_wave3 §0.1): the clause
     lives at findings :581 (the row's trigger line; row
     engine-core:F3-table-clamp-silent spans :573-581).
 (ii) "findings :1650-1659" (C50 delta item 2 / R3C50-2): row
     contract:datum-internal-rh-entropy-audits-missing spans
     :1651-1659 (:1650 is the PREVIOUS row's trigger line).
The landing window may fix both while splicing; SR-12 re-measurement
at landing supersedes these numbers anyway.

No other minted defect found: every judge addition traced to a source
(panel, refuter, or registry row) and verified there; no direction
inversion, no phantom object, no silent scope widening in any of the
21 adopted texts. The judge's two refuter-side corrections
(R3CONV-7 registries count; R3FAM-13 anchor :1182) both re-verified
correct.

==============================================================================
## §2 SCOPE (b) — THE FOUR DOC1 REV-10 APPLIED REPAIRS (retro)

Mandate texts: VERDICT_doc1_rev10.md §4 items (1) R10-3, (2) R10-1,
(3) R10-2, (4) R10-4 + the leg-3 hash-command note. Application
carrier: phaseD_stop_proof.md (4964 lines on disk; §13 block + the two
mandated body sites). All four applied texts located and compared
verbatim; hash re-executed.

- **R10-1 (member-gloss split) — CONFIRMED.** Applied at the (H-G8-4)
  member, stop_proof :2246-2251: "— equivalently the hull's (v, S)-
  image is COMPACT; root-map continuity on the compact hull is the
  member's DELIVERED conclusion (derived at (D3) from the full strong
  set; imposed directly, together with image compactness, in the
  fallback branch) [R10-1 landing, VERDICT_doc1_rev10 §4(2)]" — EXACT
  match of the mandate text. The entailed harmonization ("two
  equivalent conclusions" → "two conclusions", :2262-2263) is DECLARED
  in the landing annotation (:4950-4952) and is mandate-entailed by
  the judge's one-directionality ruling — legitimate, no defect.
- **R10-2 (sweep-sentence annotation) — CONFIRMED.** Applied at
  :4790-4797 as an in-place bracket preserving the prior "18 token
  hits" (append-only rule honored): line-vs-token correction + the
  wider staged-epithet-family sentence, VERBATIM per §4(3).
- **R10-3 (edit-site-list corrections) — CONFIRMED.** Both mandated
  corrections applied as annotations: "l.31-38 [R10-3 landing
  correction: l.31-39 — l.39 carries the recital tail]" (:4813-4814)
  and "ECL0-1 fallback re-base l.4402 [R10-3 landing addition: ...]"
  (:4827-4830), plus the adjudication note (:4832-4834). Prior text
  preserved.
- **R10-4 (citation resolution) — CONFIRMED at BOTH carriers.** Grant
  head :2594-2597 and §13 block :4716-4719 now cite "(ratified
  strategy of record, ADVISORY_SfoundationsC2_prompt_2026-08-19 §
  PRIMA AZIONE PARALLELA; VERDICT_doc1_closure §4 = pass-ownership
  authority only [R10-4 landing])" — conformal with "may be cited only
  as pass-ownership authority, never as a non-optimization carrier".
- **Leg-3 hash pin — CONFIRMED AND REPRODUCED.** Pinned at :4841-4844
  with the strip-final-newline convention. Re-executed in MY window:
  section anchors re-measured (§4 = :1087, §5 = :1440);
  `sed -n '1087,1439p' phaseD_stop_proof.md | head -c -1 | md5sum` =
  6ef096f238ef76bc0ea8965cd066ef96 = the recorded value. The rev-10
  body edit (R10-1, :2246) and both R10-4 sites sit AFTER the span;
  leg 3 remains byte-identical.

Consumers verified:
- **MASTER [S-T0P] site** (docs/rde_nozzle_MASTER.md :573-626): the
  landed G8 route accounting carries the repaired form — member names
  exactly the four of record, "SUFFICIENT-BUT-UNOPTIMIZED of record
  (gap accounting, not load-bearing theory; every granted item derived
  from the FULL set, revision-10 pen-grade (D1)-(D7))", gap-graph
  edges G5→G8 / G4→G8 (g4-d) / G8-falsifier→G7-instrument, route-level
  OPEN. NO stale biconditional gloss; NO VERDICT_doc1_closure
  non-optimization citation. CLEAN.
- **claims C-XBVP-aprime** (docs/claims_registry.yaml :2087-2098): the
  statement uses the one-directional fallback form ("or, fallback
  branch, whose root-map continuity / image compactness is imposed
  directly as a member") — R10-1-consistent; falsifier = the (f1)-(f5)
  list (see CR-W3-R10-2). CLEAN.
- **claims [T-XWS] retro-annotation** (:1646): present, dated
  2026-08-19, consumes the hull/segment form via row C-XBVP-aprime,
  "(transfer doc §5/§6 annotated)". CLEAN.
- **transfer doc §5/§6** (docs/rde_nozzle_cauchy_bvp_transfer.md
  :210-219 sandwich step; :249-257 §6(a')): both annotations present,
  citing the strong sufficient set "(H-G8-1)-(H-G8-4)
  (sufficient-but-unoptimized) or its fallback members, stated in
  phaseD_stop_proof.md §9 G8" — the §9 pointer is CORRECT (the
  condition of record :2159-2341 lives inside §9, :1925-2955). CLEAN.

### 2.1 Findings minted by this lens on scope (b)

**CR-W3-R10-1 [REPAIR — sustained; affected carrier only; orchestrator
adjudicates on file (both judges closed)]**
The LANDING PASS ANNOTATION (stop_proof :4947-4964) declares the
[T-XWS] retro-annotation applied at FOUR carriers: "transfer doc §5
sandwich step + §6(a) + claims row + M0 Part II site". The first three
are on disk (verified above). The FOURTH IS NOT: MASTER's only [T-XWS]
site outside Part III is the Part-II BVP-NATIVE UPGRADE block
(:202-216), which still reads "[T-XWS] (THEOREM* inheriting
[C-XBVP](a,b), ledger §1bis)" with ZERO retro-annotation — search-
proven this window: grep "(a')|aprime|RETRO-ANNOTATION|sandwich|
segment form|C-XBVP-aprime" over MASTER hits ONLY :594 (the Part-III
[S-T0P] site). Either the one-line Part-II annotation was dropped at
the C2 landing, or the landing inventory over-claims — the exact
inventory species R10-3 itself repaired. Not label-moving ((a') rides
under the C-XBVP umbrella, so :213 is not FALSE — it is silently
incomplete at a site the of-record duty names). REPAIR, two admissible
forms, orchestrator picks: (a) land the one-line annotation at MASTER
:213 ("the §5 sandwich step consumes the hull/segment form (a'), row
C-XBVP-aprime — retro-annotation 2026-08-19"), preferred under M0
primacy + navigation-first; or (b) annotate the stop_proof landing
block correcting its carrier list to three. Scope: this carrier ONLY —
the four R10 repair texts themselves are all verified faithful; no
other row re-opens.

**CR-W3-R10-2 [NOTE — reconciliation recorded, no repair owed]**
VERDICT_doc1_rev10's landing sentence says the registry row lands
"WITH its §9 falsifier pointer"; the landed C-XBVP-aprime falsifier
field points to "the revision-10 (f1)-(f5) list of record (stop_proof
§13)". Measured: "(f1)" occurs ONLY at stop_proof :4849 (the §13
block); §9 carries the falsifier-CONSEQUENT form (:2500-2574), not the
(f1)-(f5) list. The applied pointer is therefore correct-at-source;
the verdict's "§9" wording was loose. No consumer misled (the row also
carries the r1 feasibility gate and the [X-T0P] executable layer,
both §9 content). Record only.

==============================================================================
## §3 WHAT WOULD OVERTURN THIS CONFIRMATION

- Any of the re-measured witnesses failing on a clean checkout (all
  commands quoted in-line; registries are working-tree state of
  2026-08-20 — line numbers re-measure at landing per SR-12).
- A consumer reading of the C14 metric under which the extension-
  column interpretation is the intended one (would upgrade CR-W3-1 to
  REPAIR; refuted by the refuter's own :958 anchor and the solver_int
  signature).
- A [T-XWS] Part-II annotation existing under wording my sweep missed
  (would kill CR-W3-R10-1; the sweep covered (a')/aprime/
  RETRO-ANNOTATION/sandwich/"segment form"/C-XBVP-aprime and the
  full :202-229 block was read verbatim).
- A second (f1)-(f5) list inside stop_proof §9 (would invert
  CR-W3-R10-2's direction; grep-proven single-sited at :4849).

==============================================================================
## §4 MACHINE SUMMARY

```yaml
lens: confirm_repairs_wave3 (CONFIRM-ON-REPAIRS instance 2)
date: 2026-08-20
stage_status: ALWAYS-ON (instance-1 catch CR-W2-1; this instance also caught)
scope_a:
  repairs_in_scope: 21   # per VERDICT_wave3 §11 sustained.repairs
  repairs_verified: 21/21
  verbatim_faithful: 21  # all judge modifications declared + source-backed
  anchors_reverified_at_source: 34  # code 9, registries 14, panels/refuters/trees 11
  independent_remeasurements: [knot-sweep 9 hits exact, Q10 grep 8 hits/4 files exact, K_RICH=4, leg arithmetic K*r/(r+1) re-derived]
  findings:
    - {id: CR-W3-1, class: AMENDMENT, row: C14, defect: "judge restatement dropped the ':958 arc-column' anchor — 'FINAL MARCHED COLUMN' ambiguous vs the :1079 extension loop (whose j2 cell is imposed); one-line landing splice named"}
    - {id: CR-W3-2, class: NOTE, rows: [C26, C50], defect: "carried anchor nits: findings :579 -> :581 (FD-probe clause); :1650-1659 -> :1651-1659 (row start)"}
  broken_repairs: 0
  reopened_rows: 0
scope_b:
  r10_repairs_verified: 4/4   # R10-1 exact-verbatim body edit; R10-2/R10-3 append-only annotations verbatim; R10-4 both carriers conformal
  leg3_hash_reproduced: true  # sed -n '1087,1439p' | head -c -1 | md5sum = 6ef096f238ef76bc0ea8965cd066ef96 (my window)
  consumers_clean: [MASTER-S-T0P-site, claims-C-XBVP-aprime, claims-T-XWS-retro, transfer-doc-§5, transfer-doc-§6a]
  findings:
    - {id: CR-W3-R10-1, class: REPAIR, carrier: "stop_proof §13 LANDING PASS ANNOTATION + MASTER Part-II [T-XWS] site", defect: "landing block claims the [T-XWS] retro-annotation at 'M0 Part II site' — search-proven ABSENT (MASTER :202-216 still reads '(a,b)' with zero (a') tokens); fix = one-line M0 :213 annotation (preferred) or landing-list correction; affected carrier only, no row re-opens", adjudicator: orchestrator-on-file}
    - {id: CR-W3-R10-2, class: NOTE, carrier: claims-C-XBVP-aprime falsifier field, defect: "verdict said '§9 falsifier pointer', landed pointer = §13 (f1)-(f5) — correct-at-source ((f1) single-sited at stop_proof :4849); verdict wording was loose; record only"}
sustained: {breaks: 0, repairs: 1, amendments: 1, notes: 2}
escalations: 0   # CR-W3-R10-1 re-opens its carrier only, per the brief's rule
file: validation/sfoundations_raws_2026-08-13/blocco3/confirm_repairs_wave3.md
```
