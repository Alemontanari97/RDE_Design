# BRIEF — RETRO-SWEEP: arrivals vs landed-verdict premises
# S-FOUNDATIONS-C3, 2026-08-20 (user question of record: "non c'è
# nulla di retroattivo che può informare decisioni/analisi passate?"
# — instrument = census-protocol clause 9(e) delta-sweep applied at
# ARRIVAL time + the standing retroactive reading lens, point 8).
# BASE = validation/sfoundations_raws_2026-08-13. ONE agent.

## MANDATE (strictly non-relitigating)
You READ targeted sections of the newly arrived papers AGAINST the
named premises and pinned falsifiers of ALREADY-LANDED verdicts. You
NEVER propose verdict changes and never adjudicate: per checked pair
you return exactly one of:
- CONSISTENT (paper supports or does not touch the premise; one
  sentence why);
- TENSION (paper content bears AGAINST a named premise or WOULD FIRE
  a named pinned falsifier — quote the paper at page/section, quote
  the premise/falsifier at file:line, state which falsifier fires;
  the row's own pinned protocol then governs — you only map);
- ENRICHMENT (paper adds something the landed duty text should
  carry — propose the one-line delta for the landing window, no
  self-minting).
Read-depth honesty: [FULL] only on sections actually read; you may
read targeted sections, but then the marker is [PARTIAL-section] with
the sections named. NO package installs; GENO/ read-only; write ONLY
your output file. uno_paper.pdf has NO text layer — render pages
(pymupdf get_pixmap) for the parts you need.

## CHECK PAIRS (papers in literature/; verdicts in BASE/blocco3/)
1. uno_paper.pdf (+ vanaret_leyffer arXiv preprint as searchable
   copy) + byrd_hribar_nocedal_1999 vs VERDICT_wave2.md C31 sections
   (par.2.1/4.1 + falsifiers in par.5 + the par.1.1 quoted pins):
   premises to check = scipy trust-constr cold-restart premise (BHN
   is the algorithm source), A/B matched-constraint-set feasibility
   for Uno arm-B (preset semantics in the published paper), the
   engine falsifiers one/two/three, the IP warm-start census line.
2. hicken_zingg_2014 vs the F11d pin (VERDICT_C9C11_supplement.md
   par.4.2 + choice_ledger C56 row): are the dual-consistency
   criteria AS PINNED faithful to the paper's actual criteria?
3. becker_rannacker.pdf vs VERDICT_wave1.md C11 (par.2.4): the
   DWR-target architecture premises (enrichment-class expected).
4. thakur_nadarajah_2025 + huang_zahr_2022 + huang_zahr_2023_arxiv
   companion vs VERDICT_wave1.md C9 (F9a-R arm + supplement folding)
   and VERDICT_wave2.md C49 (par.2.8/4.8 entry gate): does the
   JOURNAL version (newer than the ask) contain results bearing on
   the promotion thresholds / entry-gate parameters as pinned?
5. venditti_darmofal_2000 vs the C11 estimator-form premise
   ("two-level fine-space residual, Venditti-Darmofal class",
   supplement par.4.2): is the class attribution faithful?
6. fidkowski_darmofal_2011 vs the C9/C11 landed architecture
   (canon cross-check, enrichment-class).
7. masters_etal_2017 + lauer_ansell_2025 vs the landed C1 migration
   duty items (VERDICT_wave2 par.4.5): dof-budget guidance bearing
   on the duty text (enrichment-class).
EXCLUDED (declared, with reason): deuflhard/yamamoto/nocedal_wright
(live in-session consumers: wave-3 FAM/REMENG + NTF minor);
giles_pierce_1997 (ordered consumer: Blocco-2 centerpiece, R27 leg
(ii)); breitkopf_ulbrich (no landed C3-chain verdict rests on it;
its window is D25U/F4b); JOSS paper (software context only).

## OUTPUT
BASE/blocco3/RETRO_SWEEP_arrivals.md: per-pair table (paper section
read + depth marker -> premise/falsifier at file:line -> verdict
CONSISTENT/TENSION/ENRICHMENT + quote), then a machine summary:
pairs_checked / tensions (each with the named falsifier and owner
protocol) / enrichments (one-line deltas for the landing) /
read-status promotions earned (paper -> [PARTIAL-section] or [FULL],
for the lit-registry landing update). Final return text: the machine
summary only.
