# VERDICT — C50 FULL FORM-2 ESCALATION (D1 slot 3, closure judge)
S-FOUNDATIONS-C3, 2026-08-20. Mandate = BRIEF_wave3_depth_addendum.md
§D1 slot (3) (user-ordered structural escalation). THIS FILE IS THE
C50 AUTHORITY OF RECORD FOR THE LANDING: the landing window applies
THIS delta, not the wave-3 §2.1 delta, where they differ (diff stated
explicitly in §5, per mandate). Inputs read in full this window:
advocate_C50_pde.md, advocate_C50_data.md, PANEL_C50.md, refute_C50.md,
VERDICT_wave3.md §2 (:245-341) + handoff items (:337-341, :809-814,
:848-853, :869-874), VERDICT_contract_and_L4R1.md #D-3 (:140-145) +
F-2/F-3 (:61-74), confirm_repairs_wave3.md C50 block (:62-86, repair
texts verified 21/21 faithful — the VERIFIED forms are consumed
below), docs/choice_ledger.yaml C49/C50/C55/C56 (:655-742). No
registry/doc of record edited; the delta text in §4 is FOR the landing
window. Output = this file only.

---

## §0 POSTURE — WHAT IS CONSUMED, WHAT IS ADJUDICATED

Consumed without re-litigation (wave-judge-certified, VERDICT_wave3
§2): all 13 sustained findings R3C50-1..13 with the 8 adopted
modifications, in their confirm-verified forms (confirm_repairs_wave3
:62-86): the S1′ criterion text; the seam conditionalization (Λ ∈
L∞(A), G ∈ L∞ on the smooth complement, finite front sensitivities;
"impossible by construction" struck); the multi-D
shift-differentiability rigor clause (theorem_ledger :235, G12
R-G12.2 to-verify); FA-2 := Σ_k |[q]_k|·δ_k with O(jitter×slope)
demoted to the finite-slope-representation reading; the
EARLIER-of-two-triggers arming (findings :1668 / :2324); the union
6-tuple clause (d/ε/W semantics per [PDE] D7.1, L∞ guard in the [PDE]
role); the anchor fix diff :288-293 at all sites; OT reason-1 honest
form + N-fold alignment hazard; Freitag demotion; C56
decided-of-record; the W vs W1-W4 split bullet; the F-2 row anchor
nit :1651-1659 (CR-W3-2(ii)).

Adjudicated HERE (the escalation's own question, D1(3)): the
ARCHITECTURE of the metric choice — single metric / split-by-role /
conditional — i.e. what occupies the d slot of the union tuple; plus
the A1 form choice the wave judge handed to this slot (VERDICT_wave3
item 2, :296-304).

---

## §1 CONVERGENCE MAP — WHAT THE TWO ADVOCATES AGREE ON

Before the disagreement, the record of agreement, because it bounds
what any outcome may change. Both advocates, independently:

1. **Pick the PRODUCT FORM (ii)** for acceptance (PDE T2 §1, five
   reasons with cost declared; DATA §5, three reasons with cost
   declared, fallback (i)-with-upgrade-trigger named). The A1 form
   choice handed by the wave judge is therefore resolved by
   ADVOCATE CONVERGENCE, not judge preference.
2. **Keep every wave-3 sustained element**: union 6-tuple, S1′ text,
   rigor clause, FA-2 fixed formula, EARLIER-of-triggers arming,
   L∞ guard, tie rule, OT closure, W/W1-W4 split.
3. **Keep the sharp instrument** (thrust-calibrated flux-weighted L2
   ⊕ front-parameter block) with FS-1/FS-2 as live falsifiers — the
   PDE advocate concedes it indispensable (concession 2); the DATA
   advocate concedes the acceptance role to L1 outright (§1, §3(a)).
4. **Concede G's design-dependence**: raised as T4 by PDE; raised
   INDEPENDENTLY by DATA as "the strongest unanswered weakness"
   (§4 — unnamed by panel, refuter, and wave judge), with a proposed
   discipline (declared evaluation point + validity neighborhood +
   recalibration trigger + quoted-design probes).
5. **Arm the S1′ dispute as an experiment**: PDE FL-3 (tie-vacuity
   vs FS-verified separation + priced-out sampled route) and DATA
   FL-3 (rank correlation of ‖δQ‖_* vs |δJ| on the first U-c
   ensemble) are complementary measured falsifiers, both pinned
   below.

What remains in genuine dispute: whether the sharp object is a
second CONTRACT metric (d_sens in a d-pair — split-by-role, the
wave-3 delta) or a U-slot certified-information grade + per-design
consumer instrument under C7.2 (single contract metric — the PDE
primary). That single question is adjudicated in §2.

---

## §2 THE DECISIVE ADJUDICATION — SLOT ARCHITECTURE

### 2.1 T3 (exactness) STANDS — sharpness is a property of the certified information grade, not of the datum-space metric
The PDE advocate's central mathematical claim is elementary and
self-contained: sup{|⟨Λ,δ⟩| : ‖δ‖_L1(A) ≤ ε} = ‖Λ‖_L∞(A)·ε, attained
in the limit. When the certified perturbation class IS the L1 ball,
the "worst-case-per-unit-mass envelope" and "the actual dual pairing
against the perturbation class" are the same number — the envelope is
tight for the ball. The distinction S1′ draws is non-empty only when
the certified class is strictly smaller than the ball, and what
certifies a smaller class is richer PROVENANCE information — which is
exactly what the U grade ladder ([DATA] Def 7.2's own architecture)
carries. Judge verification: the computation is one line and correct;
the [ABS] anchors (Ben-Tal–Nemirovski; Bertsimas general-norm
duality) are argument support only, nothing rests on them. The DATA
advocate — writing in parallel — does not and cannot refute the
mathematics; its §2.2 "unbounded gap" argument concerns perturbations
INSIDE the ball that physical error would not produce, and excluding
those requires certified knowledge that they cannot occur, i.e. a
grade, not a metric. **T3 is adopted.** Consequence for S1′ (adopted
criterion, consumed, its text untouched): the sensitivity constant of
record is the actual dual pairing against the CERTIFIED perturbation
class. For the product ball that pairing is
‖Λ‖_L∞(A)·ε_field + Σ_k |∂J/∂s_k|·ε_{s_k} + |∂_Ω J|·ε_Ω — exact, not
an envelope. For richer certified grades (U-a calibrated ball, U-b
covariance) the pairing against THAT class is the sharp instrument.
S1′ is thereby DISCHARGED under the single-metric architecture — not
re-litigated: the wave judge's "L1 solo fails S1′" (:293) was derived
correctly UNDER THE PANEL'S FRAMING, in which the only channel for
sharp certified information was a second d-slot metric; the union
tuple provides the U channel, and the wave-3 verdict itself
pre-authorized this supersession (:286-288).

### 2.2 The adopted tuple clause already decides the slot count
Wave-3 item 7 (R3C50-10, consumed): "the contract tuple = [DATA]
Def 7.1's six slots (Q̂, (N,Ω), χ, U, W, 𝒫) with d/ε/W semantics per
[PDE] D7.1." [PDE] D7.1 is a 4-slot with ONE metric d. The split
text put TWO metrics into a slot whose adopted semantics carry one —
a latent tension inside the wave-3 delta itself. The blind-DATA
lens's own architecture agrees: the flux-weighted norm was BORN
inside the uncertainty description (Def 7.2 grade U-a), i.e. in the
U slot, not the d slot. The single-metric reading is the
internally-consistent completion of what wave-3 already adopted.

### 2.3 G's design-dependence: the DATA advocate's own §4 is the deciding evidence
Both advocates now agree G = ∂(thrust∘solve)/∂Q at (Q̂, design) is
design-indexed. The DATA advocate's honest repair — declared
evaluation point recorded in 𝒫, declared validity neighborhood,
recalibration trigger on leaving it, probes at the quoted design —
is adopted BELOW as instrument discipline. But note what that
discipline IS: per-design, per-neighborhood, refresh-on-move
management of an object the record already re-evaluates per design
(C56 discrete AD-adjoint, decided-of-record). That is the definition
of a CONSUMER INSTRUMENT, not of datum-space geometry shipped
upstream once. Judge trim of the PDE case, owed for honesty: T4's
STRONGEST form ("acceptance ill-posed") over-claims against the
split as adopted — d_sens never governed acceptance, so no delivery
was ever accepted-for-A/rejected-for-B. What survives of T4 is the
weak form: a certificate radius expressed in a design-indexed norm
(ε_* at grade U-a-as-contract-metric) bakes the reference design into
the upstream certificate, while the datum's native provenance
(jitter bars, cycle scatter) is design-free physics — position-band-
native, as the PDE FL-4 note states. The single-metric architecture
keeps the upstream certificate design-free (ε_field, ε_{s_k}, ε_Ω,
Σ) and performs the design-indexed sharpening at consumption. This
also strengthens, rather than merely preserves, the only W1-W4
contact of record (W3 exogeneity: the datum box stays
upstream-exogenous — sustained R3C50-13 split bullet).

### 2.4 Role vs slot: nothing the calibrated lens won is lost
The wave-3 contest settled the SENSITIVITY ROLE in the calibrated
lens's favor (DATA §2.1 — correct as a role statement, affirmed
here). This verdict does not touch that: the calibrated instrument
remains MANDATORY machinery of record — built, armed, falsified
(FS-1 with near-sonic axis and quoted-design probes, FS-2, FS-3,
FS-4), and the U-b/U-c ladder remains the only propagation path
(Hilbert-native covariance calculus — DATA §2.3, affirmed: Σ itself
is design-free; the propagation "adjoint × Σ × adjoint" is
per-design consumption, which composes exactly with this
architecture). Item-by-item preservation audit of every DATA demand:
U-b path ✓ (U slot); mandatory FS falsifiers ✓ (duty §7); tie rule ✓
(one rule, C7.2 ≡ Claim 7.4); realizability of U-b/U-c sampling ✓
(adopted, §7(3)); G staleness discipline ✓ (adopted, §7(3));
near-sonic probe axis ✓ (adopted); product form ✓ (both picked it);
displacement-amplitude census convergence ✓ (Hoffman-1995 verified
[ABS] this escalation — carried, §9). The ONLY thing not preserved
is the NAME "second contract metric" and the design-indexed ε_*
radius semantics at the upstream certificate — and §2.1-§2.3 are the
on-record reasons those two must go.

### 2.5 The remaining DATA objection (tie-rule vacuity) — killed as architecture-decider, preserved as pre-registered experiment
DATA §2.2/§3(f)(2): with only L1-certified info on a small-margin
campaign, conservative intervals tie everything; "sensitivity
semantics not served." Killed as a decider, on the record, three
ways: (i) it is a forecast about magnitudes for deliveries that do
not exist (the panel's own materiality clause — immaterial at
constants today, NO-ATTACK — cuts both ways); (ii) the product form
removes the dominant inflation driver from the cheap route — the
front-jitter mass multiplies |∂J/∂s_k|, never ‖Λ‖_L∞ (PDE UC-5,
undisputed math; the DATA advocate adopts the same form); (iii) the
sampled [min,max] route over the certified ball is exact for the
ball — residual vacuity is a priced COST question, and with only-
L1-certified provenance a tie is the HONEST report (the tie rule
doing its job, F-3 :66-74). Preserved: the objection becomes F-ARCH
(§6), the S1′ dispute made an experiment with a PRE-REGISTERED flip
consequence — if it fires, the row re-opens in favor of
split-by-role as a ledger update, not a re-litigation. This is
adjudication to convergence, not a forced pick: the losing branch
survives as the armed experiment.

### 2.6 Outcome
**SINGLE-CONTRACT-METRIC, PRODUCT FORM** (PDE primary submission
adopted, with the T4-strong-form trim of §2.3; DATA role affirmation
+ all three proposed duty deltas + both measured falsifiers
adopted). The A1 form choice = PRODUCT FORM (ii), by advocate
convergence, with the scoped form (i) demoted to a declared
PER-FAMILY fallback under demonstrated front-unidentifiability
(F-PROD, §6). Not "conditional" as an outcome class: the conditional
element is ordinary falsifier discipline (F-ARCH pre-registered),
which every adjudication of record carries.

---

## §3 WHAT HAPPENED TO EACH ADVOCATE SUBMISSION

- ADVOCATE-PDE primary (single-metric product form): **ADOPTED**,
  with T4 trimmed to its weak form (§2.3) and T5 (rigor asymmetry)
  adopted as stated — the d slot carries the theorem-grade object;
  the declared-rigor sharp instrument is carried openly as an
  instrument (rigor clause verbatim).
- ADVOCATE-PDE fallback (split with product acceptance): not
  reached; preserved as F-ARCH's flip target.
- ADVOCATE-DATA (split-by-role, sensitivity = calibrated lens):
  the ROLE case **AFFIRMED** and fully re-homed (§2.4); the slot
  case does not survive §2.1-§2.3. All three proposed duty deltas
  ADOPTED (§7(3)); FL-3/FL-4 ADOPTED as FS-3/FS-4 (§6); the §4
  G-staleness weakness ADOPTED as instrument discipline AND as
  evidence for the re-homing.
- Escalation-rule check (D1): the PDE escalation note anticipated a
  surviving content objection against T3/T4 "e.g. a demonstration
  that G can be certified design-independent" — the DATA advocate
  demonstrated the OPPOSITE (its §4 concedes design-dependence and
  disciplines it). No content objection between the advocates
  survives unkilled on this record: **named residues = 0; no forced
  pick occurred** (the one live empirical dispute is pre-registered
  as F-ARCH with flip semantics). Full disposition table in §9.

---

## §4 EXACT LEDGER DELTA TEXT FOR C50 (the landing window applies THIS text)

Proposed row-note delta (docs/choice_ledger.yaml C50, :667-677;
status NEVER → adjudicated of record, enum proposal MIXED-equivalent
per landing convention — instantiation half = named F2 duty):

> C50 CLOSURE 2026-08-20 (VERDICT_C50_form2.md, Form-2 full
> escalation D1 — THE C50 authority of record; supersedes the wave-3
> §2.1 delta where they differ, diff declared in that file §5):
> SINGLE-CONTRACT-METRIC of record, PRODUCT FORM. Contract tuple =
> [DATA] Def 7.1's six slots (Q̂, (N,Ω), χ, U, W, 𝒫) with d/ε/W
> semantics per [PDE] D7.1; the d slot carries ONE metric:
> d = L1(A) on phase-aligned fields ⊕ declared front positions/speeds
> as finite-dimensional components with their own bars (ε_field;
> ε_{s_k}; ε_Ω), inside the L∞(K) realizability guard (adopted in the
> [PDE] role: class side condition, converged-on-object with [DATA]'s
> norm-pair member), TV-bounded side condition. Acceptance =
> product-ball membership; excursion from W = NO DATUM (rejected,
> not error-barred). The thrust-calibrated flux-weighted L2 ⊕
> front-parameter block is NOT a second contract metric: it is the
> U-slot grade-U-a certified sensitivity INSTRUMENT — per-design
> consumer instrument under C7.2, delivered by the C56 discrete
> AD-adjoint of record (a fired F11d re-opens its delivery route) —
> with mandatory arming and falsifiers FS-1 (probe family incl.
> near-sonic axis, probes at the quoted design)/FS-2/FS-3
> (rank-correlation)/FS-4 (G validity-neighborhood stability); G
> carries a declared evaluation point (datum nominal + reference
> design/family, recorded in 𝒫) + declared validity neighborhood +
> recalibration trigger; U-b covariance / U-c sampling live in the
> same slot with sampling laws intersected with the L∞ guard.
> Sharpness discipline (S1′ discharged, text untouched): the
> certified sensitivity constant is the actual dual pairing against
> the CERTIFIED perturbation class — for the product ball,
> ‖Λ‖_L∞(A)·ε_field + Σ_k|∂J/∂s_k|·ε_{s_k} + |∂_Ω J|·ε_Ω, exact for
> that ball (sup-attainment, one-line duality), under the seam
> regularity declarations of record (Λ ∈ L∞(A) imposed dual
> regularity; G ∈ L∞ on the smooth complement; finite front
> sensitivities — verified at instantiation, FS-1/FS-2 live); richer
> certified grades sharpen through U, never through a second d. The
> wave-3 seam clause is replaced by the grade statement: coarser
> certified grade ⇒ coarser certified interval; grades ordered; ONE
> tie rule ([PDE] C7.2 ≡ [DATA] Claim 7.4; ties reported as ties).
> Front-block sensitivities at the record's declared rigor:
> shift-differentiability rigorous in 1-D, practice-without-theorem
> in multi-D (theorem_ledger :235; G12 R-G12.2 to-verify); FS-2 the
> live empirical guard. Fronts are a DELIVERABLE: declared front set
> (positions/speeds with bars) is a tuple component; the F-2
> datum-side moving-RH/entropy audits (findings :1651-1659, same F2
> window) read the DECLARED components and are continuous in the
> product metric; a family with demonstrably unidentifiable fronts
> falls back PER-FAMILY to the scoped form (A1 restricted to the
> flux-integral audit class + representation-level RH/entropy
> declaration) — declared, never silent (F-PROD). Alignment branch
> pinned by (N,Ω) + delivered front positions (N-fold hazard closed
> structurally); FA-2 predicted drop = Σ_k |[q]_k|·δ_k over the
> declared jump set (O(jitter×slope) only as the finite-slope-
> representation reading). ARCHITECTURE FLIP PRE-REGISTERED (F-ARCH
> — the S1′ dispute as experiment): at the first imported real-data
> family, IF the cheap product-form intervals tie a majority of
> compared design pairs that the U-a instrument (FS-1 passing, FS-2
> non-vacuous) separates AND the sampled [min,max] route over the
> certified ball is priced infeasible at campaign scale by measured
> cost, THEN this row re-opens in favor of SPLIT-BY-ROLE (sharp
> metric into the d pair) as a ledger update, not a re-litigation.
> Wasserstein/OT family stays CLOSED (honest form of record: the
> only datum-space optimization in the contract is the 1-D phase
> alignment, exhaustively solvable on S¹; OT convexity targets
> high-dimensional misfit landscapes; signed multi-component data
> needs artifact-prone normalization) — re-enters only at any future
> upstream assimilation layer. Instantiation + ALL numeric constants
> = duty F2-C50-CONTRACT-METRIC (superseding form, VERDICT_C50_form2
> §7), armed at the EARLIER of the two named triggers (findings
> :1668 F2-freeze/first-real-family; :2324 first unsteady-generator
> ingestion); sequenced with contract:datum-uncertainty-contract-
> missing and contract:phase-gauge-jitter-alignment-unpinned. Census
> of the escalation: Hoffman et al. 1995 (Mon. Wea. Rev.
> 123:2758-2770, distortion = displacement ⊕ amplitude ⊕ residual)
> VERIFIED [ABS] — upgrades the R3C50-11(3) [MEMORY] lead;
> Ben-Tal–Nemirovski + Bertsimas dual-norm/set-geometry anchors
> [ABS] ride this note (argument support; dedup vs
> docs/literature_registry.yaml owed at landing); Fidkowski-Darmofal
> AIAA J. 49(4) 2011 [ABS] = the output-weighted calibration
> principle's SOTA home (same line as the Giles-Pierce anchor, diff
> :288-293); Métivier arXiv:2101.00904 stays the optional
> non-blocking OT-closure enrichment.

---

## §5 EXPLICIT DIFF VS THE WAVE-3 DELTA (mandate requirement)

The wave-3 delta (VERDICT_wave3 §2.1 items 1-8) is superseded ONLY
where stated; everything else is carried verbatim.

- **Δ1 (ARCHITECTURE — supersedes).** Wave-3: split-by-role, d =
  (d_acc, d_sens) pair. Here: ONE contract metric (L1 product form)
  in the d slot; the wave-3 d_sens object is re-homed VERBATIM as
  the U-slot grade-U-a instrument + C7.2 consumer obligation. No
  object is deleted; the slot assignment changes (§2.1-§2.3).
- **Δ2 (A1 FORM — resolves the handed choice).** Wave-3 item 2
  adopted scoped form (i) with form (ii) handed to this judge. Here:
  PRODUCT FORM (ii) adopted (advocate convergence); scoped form (i)
  demoted to declared per-family fallback under F-PROD.
- **Δ3 (SEAM — supersedes).** Wave-3 item 3's conditionalized seam
  clause between two contract metrics is replaced by the grade
  statement (coarser grade ⇒ coarser interval; one tie rule); the
  regularity declarations (Λ ∈ L∞(A), G ∈ L∞ on smooth complement,
  finite front sensitivities) are CARRIED UNCHANGED — they now
  condition the cheap route and the instrument, same falsifiers.
- **Δ4 (NEW DUTY CLAUSES — additive).** From the advocates: G
  evaluation point + validity neighborhood + recalibration trigger +
  quoted-design probes (DATA §4); near-sonic probe axis (DATA §3c);
  U-b/U-c sampling respects the L∞ guard (DATA §2.4); front-set
  deliverable clause (both advocates).
- **Δ5 (NEW FALSIFIERS — additive).** FS-3, FS-4, F-PROD, F-ARCH,
  F-GEOM (§6). FA-1/FA-2/FS-1/FS-2/F-TIE unchanged in semantics;
  FA-2 in its confirm-verified fixed form.
- **Δ6 (CENSUS — additive).** Hoffman-1995 [MEMORY]→[ABS] verified
  (closes the R3C50-11(3) verification gate at [ABS]); two
  robust-duality anchors [ABS] ride the note; Fidkowski-Darmofal
  [ABS] named. Dedup vs literature_registry owed at landing.
- **Unchanged (carried verbatim):** S1′ criterion text (item 1 —
  discharged, not modified, §2.1); rigor clause (item 4); FA-2
  formula (item 5); EARLIER-of-triggers arming (item 6); union
  6-tuple clause (item 7); all item-8 text amendments (anchor
  :288-293 at all sites, OT honest form + N-fold hazard, Freitag
  supporting-by-analogy, C56 decided-of-record, W vs W1-W4 split);
  OT-family closure; C55 disjointness; materiality clause.

---

## §6 PINNED FALSIFIERS PER METRIC ROLE (mandate requirement)

**Acceptance role (d = L1 product form):**
- **FA-1** (d-continuity): FD sampling of the QoI over an ε-net of
  the product ball at fixed numerics; QoI oscillation not → 0 with ε
  rejects d-continuity of the pipeline ([PDE] C7.1 TEST, verbatim).
- **FA-2** (gauge quotient, verified fixed form): unaligned-vs-
  aligned d_acc drop = Σ_k |[q]_k|·δ_k over the declared jump set;
  O(jitter×slope) only as the finite-slope-representation reading;
  alignment branch pinned by (N,Ω) + delivered front positions.
- **F-PROD** (product-form viability, = PDE FL-2): a first imported
  family whose fronts are genuinely unidentifiable (no declared
  front set survives the F-2 RH/entropy audits at any admissible
  tolerance) ⇒ that family falls back to the scoped form (i),
  declared; if the representation-level declaration ALSO fails, the
  acceptance architecture re-opens entirely.
- **Overturn conditions carried** (PANEL §4.4): a stage-A flux-class
  audit demonstrated not L1-continuous on the L∞∩TV class (= PDE
  FL-1 — L1 loses the acceptance slot outright); a measured family
  whose dominant error mode is NOT front-position jitter (breaks the
  A2 weighting).

**Sensitivity role (U-a instrument + C7.2 consumer obligation):**
- **FS-1** (calibration): adjoint first-order bound violated by
  direct FD probes outside probe error bars, persisting after
  implementation fixes; probe family INCLUDES the near-sonic axis;
  probes run at the QUOTED design, never only at the reference ⇒
  calibration rejected; instrument demotes to the cheap
  product-dual route.
- **FS-2** (front block, anti-vacuity): pure front-displacement
  probe (jump shifted by δ, smooth part frozen) must be reproduced
  by the front-motion adjoint term within bars; smooth-only passes
  declared VACUOUS for this data class.
- **FS-3** (sharpness has an empirical referent, = DATA FL-3): on
  the first imported family with a U-c ensemble, rank correlation
  between ‖δQ‖_* and measured |δJ| below a declared threshold
  (constant derived at instantiation) ⇒ the calibration premise is
  empirically empty for that family; demotion as FS-1.
- **FS-4** (design stability, = DATA FL-4): re-evaluation of G at a
  quoted design INSIDE the declared validity neighborhood reordering
  perturbation rankings beyond declared tolerance ⇒ the neighborhood
  claim is rejected; per-design recalibration becomes mandatory or
  demotion as FS-1.

**Architecture level:**
- **F-ARCH** (pre-registered flip, = PDE FL-3; decisive, measured):
  at the first imported real-data family — (i) cheap product-form
  intervals tie a majority of compared design pairs that the U-a
  instrument (FS-1 passing, FS-2 non-vacuous) separates, AND (ii)
  the sampled [min,max] route over the certified ball is priced
  infeasible at campaign scale by measured cost ⇒ the single-metric
  architecture has failed its well-posedness obligation; the row
  re-opens in favor of SPLIT-BY-ROLE (sharp metric into the d pair)
  as a ledger update.
- **F-GEOM** (information grade, = PDE FL-4): a delivery whose
  provenance CERTIFIES a structured error geometry (U-b covariance
  or G-weighted band) as its native error model ⇒ the d slot carries
  the certified geometry FOR THAT FAMILY; L1 demotes to the outer
  admissibility check.
- **F-TIE** (consumer): two designs declared distinct with
  overlapping certified intervals reject the comparison claim
  (of record, C7.2 ≡ Claim 7.4 — cited, unchanged).

---

## §7 F2 CONTRACT-WINDOW DUTY TEXT (superseding form of record)

**F2-C50-CONTRACT-METRIC** (supersedes PANEL §4.3 + wave-3 items
6-7): in the same F2 contract window that consumes
contract:datum-uncertainty-contract-missing (radius ε semantics +
extrapolation rule) and contract:phase-gauge-jitter-alignment-
unpinned (the alignment-operator spec FA-2 needs, branch pinned by
(N,Ω) + delivered front positions), instantiate at the contract site
(D2.6 / problem-book contract slot):
1. the union 6-tuple (Q̂, (N,Ω), χ, U, W, 𝒫) with the SINGLE
   product-form d = L1(A) on phase-aligned fields ⊕ declared front
   positions/speeds with bars; ε = (ε_field, ε_{s_k}, ε_Ω); W
   semantics: excursion = NO DATUM; L∞(K) guard as class side
   condition; TV-bounded side condition;
2. the front-set DELIVERABLE clause: declared positions/speeds with
   bars as tuple components; the F-2 moving-RH/entropy audits read
   the declared components (continuous in the product metric);
   missing components rejected administratively per [DATA] Def 7.1;
   per-family scoped-(i) fallback ONLY on demonstrated
   front-unidentifiability (F-PROD), declared in the Verdict;
3. the U grade ladder: U-a = thrust-calibrated flux-weighted L2 ⊕
   front-parameter block, with G carrying a DECLARED evaluation
   point (datum nominal + reference design/family, recorded in 𝒫),
   a DECLARED validity neighborhood, a recalibration trigger on
   leaving it, and FS-1 probes at the quoted design incl. the
   near-sonic axis; U-b covariance and U-c sampling with sampling
   laws intersected with the L∞ guard; every grade ships with its
   reject clause;
4. the C7.2 consumer obligation with BOTH interval routes (cheap
   product-dual pairing, exact for the certified ball; sampled/
   optimization [min,max] over the ball; the U-a instrument where
   FS-1 passes and FS-2 is non-vacuous) and ONE tie rule (ties
   reported as ties); the grade statement in place of a seam
   inequality; the seam regularity declarations verified at
   instantiation (Λ ∈ L∞(A); G ∈ L∞ on the smooth complement;
   finite front sensitivities);
5. ARMING: FA-1/FA-2/F-PROD/FS-1/FS-2/FS-4 at the EARLIER of the
   two named triggers (findings :1668 = F2 contract freeze / first
   imported real-data family; findings :2324 = first unsteady-
   generator ingestion — FA-2's natural arming point); FS-3 at the
   first family carrying a U-c ensemble; F-ARCH at the first
   campaign-scale design comparison on imported data;
6. front-block sensitivities carried at the record's declared rigor
   (rigor clause verbatim: 1-D rigorous, multi-D practice-without-
   theorem, theorem_ledger :235, G12 R-G12.2 to-verify; FS-2 the
   live guard); ALL numeric constants (ε floors, net spacings,
   thresholds, tolerances, cost prices) derived there — none minted
   here.

---

## §8 DEPENDENCIES (mandate requirement)

- **contract:datum-uncertainty-contract-missing** (findings registry
  :1660-1668): this verdict supplies the METRIC half of the missing
  contract; radius/extrapolation semantics stay with the finding's
  F2 window — sequenced together (the row's own owner field already
  binds the sequencing). The finding's trigger (:1668) is one arm of
  the EARLIER-of-triggers clause.
- **contract:phase-gauge-jitter-alignment-unpinned** (:2316-2324):
  supplies the alignment operator FA-2 consumes; its own trigger
  (:2324) is the other arm; the N-fold branch hazard is closed
  structurally by (N,Ω) + delivered fronts.
- **F-2 datum-side RH/entropy audits** (findings :1651-1659, same F2
  window): under the product form these read the declared front
  components and become continuous in the contract metric (the
  R3C50-2 counterexample is void); they are also F-PROD's instrument.
- **C55 ν-datum** (ledger :721-731): DISJOINT slots affirmed — C50's
  d governs only the Û-component distance; ν (ambient weight) is a
  finite-dimensional contract datum riding the operating box W with
  provenance discipline identical to μ; the day P_amb goes
  multi-point, ν's acceptance is finite-dimensional (bars, no
  metric dispute) and its uncertainty rides the same U-grade
  discipline. Named, not decided; C55's single-point default and
  sequencing clause untouched.
- **M0 W1-W4** (:167-175): distinct object from the datum window W
  (wave-3 split bullet carried). Only contact = W3 exogeneity, and
  the single-metric architecture STRENGTHENS it: the upstream
  certificate is design-free; no W1-W4 hypothesis is fed by the
  metric choice; W1-W4 monitors (specified-not-armed) untouched.
- **C56** (ledger :733-742): decided-of-record (discrete AD-adjoint,
  all three roles closed) — the delivery route for Λ, the
  front-motion kernel, and per-design G; a fired F11d re-opens C56
  and with it the U-a instrument's delivery route (not the d slot).
- **C49** (ledger :655-665): fitted tier = sole certificate bearer;
  the front-motion adjoint anchor is phaseB_tree_diff.md :288-293
  (fixed anchor of record).

---

## §9 ESCALATION-RULE ACCOUNTING + CENSUS DISPOSITIONS

Disposition of every content-class position (D1 rule: a surviving
content objection the judge cannot kill on the record = named
residue, owner F2 contract window, never a forced pick):

| # | Position | Disposition |
|---|---|---|
| 1 | PDE T3 (exactness / sharpness = grade) | ADOPTED — unrefuted one-line mathematics; decisive (§2.1) |
| 2 | PDE T4 strong form (acceptance ill-posed under split) | KILLED by the judge — d_sens never governed acceptance; only the weak form adopted (§2.3) |
| 3 | PDE T4 weak form + DATA §4 G-staleness | CONVERGENT between advocates — adopted as instrument discipline AND slot evidence (§2.3) |
| 4 | PDE T5 (rigor asymmetry) | ADOPTED with the rigor clause verbatim |
| 5 | DATA §2.1 (S1′ = the lens's construction; role settled in its favor) | ROLE AFFIRMED; slot question decided separately — no loss audit item open (§2.4) |
| 6 | DATA §2.2/§3(f)(2) (unbounded tie vacuity) | KILLED as decider (3 on-record grounds); PRESERVED as F-ARCH pre-registered flip (§2.5) |
| 7 | DATA §2.3 (U-ladder Hilbert-native) | AFFIRMED — and compatible: U slot is the home under both architectures; Σ design-free, propagation = consumption |
| 8 | DATA §2.4 (realizability of U-b/U-c sampling) | ADOPTED verbatim into the duty |
| 9 | A1 form choice | RESOLVED — product form (ii), advocate convergence; scoped (i) = declared per-family fallback |
| 10 | PDE P-6 (framing vs wave judge) | SUSTAINED as framing clarification; S1′ text untouched, discharged under the adopted architecture (§2.1) |

**Named residues requiring the F2 forced-pick escape: 0.** The one
live empirical dispute (sharp-vs-cheap at campaign scale) is
pre-registered as F-ARCH with flip semantics — an experiment with an
owner and a consequence, not an unresolved objection.

Census dispositions: Hoffman et al. 1995 VERIFIED [ABS] this
escalation (upgrades R3C50-11(3) from [MEMORY]; Keil-Craig/CRA
remain [MEMORY], not load-bearing, verification owed if ever
adopted); Ben-Tal–Nemirovski + Bertsimas [ABS] ride the row note as
argument support (nothing rests on them beyond the self-contained
computation); Fidkowski-Darmofal [ABS] named as the calibration
principle's SOTA home; Métivier arXiv:2101.00904 stays the OPTIONAL
non-blocking OT enrichment; Hoffman full-text = optional [ABS]→[FULL]
procurement. Dedup of all ridden anchors vs
docs/literature_registry.yaml owed at the landing window (none is a
new registry row by this verdict; the landing decides).

Papers needed: NONE BLOCKING.

---

## MACHINE SUMMARY

```
role: C50-CLOSURE-JUDGE (D1 slot 3, Form-2 full escalation)
output: validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_C50_form2.md
authority: "THE C50 authority of record for the landing — supersedes the wave-3 §2.1 delta where they differ (diff declared §5)"
verdict: SINGLE-CONTRACT-METRIC-PRODUCT-FORM
verdict_text: "d slot = ONE metric: L1(A) on phase-aligned fields ⊕ declared front positions/speeds with bars (ε_field, ε_{s_k}, ε_Ω), inside L∞(K) guard, TV-bounded side condition, on the union 6-tuple (Q̂,(N,Ω),χ,U,W,𝒫); thrust-calibrated flux-weighted L2 ⊕ front block RE-HOMED as U-slot grade-U-a certified instrument + C7.2 per-design consumer obligation (C56 discrete AD-adjoint delivery route) — every wave-3 sustained element carried, none dropped"
a1_form_choice: "PRODUCT FORM (ii) — resolved by advocate convergence; scoped form (i) = declared per-family fallback under F-PROD (front-unidentifiability)"
decisive_grounds: [T3-exactness (envelope = exact dual pairing for the certified ball; sharpness = information grade, not metric — S1′ discharged, text untouched), tuple-clause consistency (adopted item 7 gives d/ε semantics per PDE D7.1 = ONE metric slot; DATA Def 7.2 born in U), G-design-indexing (convergent between advocates — DATA §4 concedes and disciplines it; per-design refresh = consumer instrument, upstream certificate stays design-free, W3 exogeneity strengthened)]
diff_vs_wave3: {Δ1: "split-by-role → single metric + U-home (supersedes)", Δ2: "A1 scoped (i) → product form (ii); (i) demoted to per-family fallback", Δ3: "seam clause → grade statement (regularity declarations carried unchanged)", Δ4: "adds G evaluation-point/neighborhood/recalibration + near-sonic probe axis + U-b/U-c sampling-respects-guard + front-set deliverable clause", Δ5: "adds FS-3, FS-4, F-PROD, F-ARCH, F-GEOM", Δ6: "Hoffman-1995 [MEMORY]→[ABS] verified; robust-duality anchors [ABS] ride the note", unchanged: "S1′ text, rigor clause, FA-2 fixed formula, EARLIER-of-triggers, union tuple clause, all item-8 amendments, OT closure, materiality"}
ledger_delta: "§4 blockquote = the exact C50 row-note delta text; enum proposal MIXED-equivalent per landing convention (instantiation half = named F2 duty)"
falsifiers: {acceptance: [FA-1, FA-2 (Σ_k|[q]_k|·δ_k), F-PROD, FL-1-carried (flux-class audit not L1-continuous), A2-weighting-overturn], sensitivity: [FS-1 (+near-sonic axis, quoted-design probes), FS-2, FS-3 (rank-correlation, =DATA-FL-3), FS-4 (G-neighborhood stability, =DATA-FL-4)], architecture: [F-ARCH (pre-registered flip to split-by-role, =PDE-FL-3, decisive+measured), F-GEOM (=PDE-FL-4), F-TIE]}
duty: "F2-C50-CONTRACT-METRIC superseding form (§7, 6 numbered clauses); arming = EARLIER of findings :1668 / :2324; FS-3 at first U-c ensemble; F-ARCH at first campaign-scale comparison; all constants derived at instantiation"
dependencies: [contract:datum-uncertainty-contract-missing (:1660-1668, metric half supplied, sequenced), contract:phase-gauge-jitter-alignment-unpinned (:2316-2324), F-2 RH/entropy audits (:1651-1659, continuous under product form), C55 ν-datum (disjoint affirmed; ν finite-dim datum, U-grade discipline; named-not-decided), M0 W1-W4 (only contact W3 exogeneity — strengthened), C56 (decided-of-record; F11d re-opens the instrument delivery route, not the d slot), C49 (fitted tier, anchor :288-293)]
residues_forced_pick: 0  # no content objection between advocates survived unkilled (disposition table §9, 10 rows); the live empirical dispute is pre-registered as F-ARCH with flip semantics
census: "Hoffman-1995 verified [ABS] this escalation (closes R3C50-11(3) verification gate at [ABS]); Ben-Tal–Nemirovski/Bertsimas/Fidkowski-Darmofal [ABS] ride the note; Métivier optional; dedup vs literature_registry owed at landing; papers_needed: 0 blocking"
registry_edits: 0
files_written: 1 (this file only)
```
