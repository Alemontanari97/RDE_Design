# PANEL_REMMARCH — MARCH/GEOMETRY REMAINDER (C5, C6, C8, C10, C12, C13, C14)
# S-FOUNDATIONS-C3 WAVE 3, 2026-08-20. One panel, per-row verdicts.
# Brief: BRIEF_wave3_panels.md §REMMARCH + §0 (wave-1 FRAME, wave-2
# §0-bis/§0-ter, LOAD-CLASS VALVE AG-1, authorities-not-relitigated).
# BASE = validation/sfoundations_raws_2026-08-13. Census dated 2026-08-20.
# No registry/doc of record edited; all deltas PROPOSED for the landing.

## 1. FROZEN FORMAL STATEMENT

Shared sub-problem (frozen): the certified characteristic march
(axisymmetric Euler, fitted fronts, JAX custom_vjp discrete adjoint,
Verdict/certificate discipline with derived bands and firing rejectors)
carries a discrete infrastructure whose unit choices were inherited
from the GENO mirror or fixed single-author: (i) the spline design
class's knot bookkeeping under design motion (C5, C6, C8), (ii) the
refinement operator feeding the estimator ladder (C10), and (iii) the
march's start/axis/exit unit processes (C12, C13, C14). Each row is
adjudicated against its SOTA alternative set to convergence, with
measurement-gated splits pinned as protocol+falsifier and measured
halves named as F2 duties. Landed authorities consumed, not re-opened:
VERDICT_wave1.md (C9 mesh law, C11 estimator), VERDICT_C9C11_supplement.md
(moving-mesh family closure, DWR weight of record), VERDICT_wave2.md
(§2.9/4.10 C21 seed policy; §4.12 C42 notification discipline).

Per-row SINGLE MAIN QUESTIONS + PRE-REGISTERED DECISION CRITERIA
(frozen BEFORE the census; the refuter is invited to attack post-hoc
rationalization against this section):

- **C5 (knot anchoring across thB updates).** Q: what anchoring of the
  interior knots keeps the design class well-defined and certificate-
  nested while the attachment point xB moves? Criteria: the incumbent
  (normalized xi, affine with xB) WINS if measured drift is immaterial
  against the interval scale it can bias AND every alternative costs
  class nesting or trace stability; an alternative WINS if drift is
  material in production without a monitor, or if an anchoring exists
  that preserves nesting at equal cost with zero drift.
- **C6 (insertion degeneracy guard).** Q: what OBJECT must the guard
  bound so that knot insertion never degrades the interpolation
  operator the certificate rides on? Criteria: the candidate that
  bounds the object which MEASURABLY degrades (operator conditioning /
  cardinal amplitude), mesh-independently, WINS; a guard bounding a
  proxy that weakens under refinement LOSES by that measurement.
- **C8 (warm-start deviation handling).** Q: does the enriched-class
  warm start need handling beyond a print? Criteria: if warm-start
  exactness is theorem-grade with a constructible derived floor, an
  ARMED rejector beats print-only by R5 (tests must reject);
  print-only wins only if no derived floor is constructible.
- **C10 (refinement operator).** Q: must refine() separate the three
  error directions for certificate bands to be attributable? Criteria:
  per-direction refinement takes the PRODUCTION role only if joint
  refinement provably masks attribution at certificate sites above the
  program noise floor; otherwise joint stays production and
  per-direction is the diagnostic instrument.
- **C12 (start line — accuracy-axis PROTOCOL + rejector ONLY; the
  higher-order branch on rejector fire is F4b/F5-owned and NOT
  adjudicated here, per the brief's scope clause).** Q: what protocol
  can certify that the first-order Sauer start's model error stays
  below band at production geometries, given that shared-start
  Richardson is blind to it BY CONSTRUCTION? Criteria: a protocol WINS
  iff it breaks the shared-start error correlation (displacement or an
  independent instrument) and can FIRE; any protocol that only
  re-exercises the shared start LOSES by construction.
- **C13 (axis unit process).** Q: what verification makes the axis
  (y→0) unit process certificate-grade at march level, given that
  Me_ach — the verdict-load-bearing exit read — IS the axis-cell
  value? Criteria: instruments win by demonstrated ability to REJECT
  at the axis cell (observed order or parity defect, derived bands);
  the incumbent process is retained unless an instrument shows a
  defect; a replacement unit process is adopted only on a fired
  instrument, never on taste.
- **C14 (exit-condition read point).** Q: is the axis-cell-only Me
  read a certificate-grade exit decision? Criteria: a read is
  certificate-grade iff its uncertainty is carried as a band and an
  independent identity can reject it; a single-point read with no band
  LOSES by construction unless the exit-line spread is measured
  immaterial — in which case the band costs one max() and is adopted
  anyway as the cheap sufficient form (LOAD-CLASS VALVE).

LOAD-CLASS VALVE declaration (AG-1, user-ratified 2026-08-20): C5, C8,
C10, C14 are gap-accounting rows (arming/bookkeeping of already-
measured defects, owners already named in findings rows); they are
converged EARLY with strong trivially-checkable sufficient hypotheses,
sufficient-not-optimized, declared as such below. C6, C12, C13 carry
genuine adjudication content and get the full treatment.

## 2. SOTA CENSUS (dated 2026-08-20)

### 2.1 Query protocol table

| # | Query (verbatim) | Where | Date | Hits/Screened/Included |
|---|---|---|---|---|
| Q1 | "Schoenberg-Whitney condition knot insertion B-spline collocation conditioning knot ratio bound" | WebSearch | 2026-08-20 | 10/10/2 |
| Q2 | "Eca Hoekstra multi-parameter grid refinement uncertainty estimation observed order anisotropic" | WebSearch | 2026-08-20 | 9/9/1 |
| Q3 | "transonic throat start line method of characteristics nozzle Sauer Kliegel-Levine Hall accuracy comparison initial data line supersonic" | WebSearch | 2026-08-20 | 9/9/2 |
| Q4 | "axis singularity axisymmetric method of characteristics r=0 treatment L'Hopital series expansion supersonic nozzle" | WebSearch | 2026-08-20 | 8/8/2 |
| Q5 | "method of manufactured solutions verification method of characteristics supersonic flow code" | WebSearch | 2026-08-20 | 8/8/2 |
| Q6 | "shape optimization spline parameterization knot placement invariance design domain deformation reparameterization drift" | WebSearch | 2026-08-20 | 10/10/2 |
| G1-G9 | measured greps for dedup and incumbent verification (findings/choice/literature registries, cava, diff, trees, code) | repo | 2026-08-20 | see §2.3/§4 |

Russian classical school: the classical-adjacent territory here is the
transonic-start line (Sauer/Hall/Kliegel-Levine + Klopfer-Holt per the
Q3 census); the Russian nozzle school enters via Kraiko rows already
of record in the literature registry (wave-agnostic; no C12-specific
Russian start-line line surfaced in Q3 — closure by stated search, not
by assertion of absence).

### 2.2 Corpus recency

Span 1944-2025. Newest items touching these rows: Lauer-Ansell 2025
(PAS parameterization review, on disk) [TITLE]; Thakur-Nadarajah 2025
(on disk, C49/C9 territory, not consumed here) [TITLE]; evolutionary
knot placement 2025 (Q6, tandfonline) [ABS]; Fernandes 2023 (MoC shape
optimization, of-record read) [REC]. Classical core: Sauer 1944, Hall
1962, Kliegel-Levine 1969, Klopfer-Holt 1975 [ABS via Q3]; Eça-Hoekstra
2014 [ABS + WANTED row]. Depth markers: [FULL] = read in this window;
[REC] = of-record READ-INTEGRAL by a prior session, consumed via
docs/literature_registry.yaml row + expert report (declared extra
marker: my direct depth this window is the registry row, not the PDF);
[ABS] = abstract/snippet only; [TITLE] = existence only.

### 2.3 Census entries (per-source one-liners, depth-marked)

**Splines / knot infrastructure (C5, C6, C8):**
- Floater, "The Schoenberg-Whitney theorem and total positivity" (UiO
  notes) [ABS, Q1]: S-W = nonsingularity of the collocation matrix iff
  t_i < x_i < t_{i+d+1}; uniqueness, NOT numerical well-conditioning.
- Q1 corpus consensus [ABS]: S-W near-violation ⇒ solution sensitivity;
  sampling/collocation matrices become severely ill-conditioned as knot
  distribution degrades; B-spline BASIS stability constant depends on
  degree only (knot-insertion proof route) — the operator that degrades
  is the INTERPOLATION operator, not the basis. This matches the
  repo's own measured object exactly (GAP-23: cond(A) saturates ~6
  while cardinal amplitude grows ~linearly 2.2/17.5/170 at ratio
  10/100/1e3 — docs/findings_registry.yaml:1310-1318 [FULL]).
- O-F8 (phaseA_tree_optimization_CONDENSED.md:71-82 [FULL], full tree
  :548): certified branch-and-bound by de Casteljau subdivision —
  operator-level guard mechanism, converges to sharp.
- Q6 corpus [ABS]: knot-placement lines (curvature-driven, learned,
  evolutionary 2025) are FITTING-optimality lines; none addresses
  certificate-class nesting under domain motion — the C5 object. The
  repo's free-knot rejection of record (D6 item 9, S20 survey; cited in
  a1_toc_variational_jax.py:110-117 [FULL]) stands unchallenged by Q6.
- Lauer-Ansell 2025 parameterization review [TITLE, on disk]: modern
  parameterization families census exists on disk for the C1 window;
  not consumed here (C5 is knot BOOKKEEPING inside a fixed family, not
  family choice) — stated reason for non-consumption.
- Boehm 1980 / Oslo knot-insertion nestedness [TITLE]: named by the
  findings row parametrization:warmstart-exactness-unarmed:1319-1326
  [FULL] as the THEOREM source for C8's exactness promotion.

**Refinement/estimator (C10):**
- Eça & Hoekstra 2014, JCP 262:104-130 [ABS, Q2 + WANTED row
  docs/literature_registry.yaml:850-851 [FULL]]: LSQ fit of the
  power-series error model over >=4 grids; safety factor a function of
  observed order and fit standard deviation; the census-grade
  instrument for multi-parameter (per-direction) refinement with noisy
  orders. Full text NOT needed for this row's verdict (per-direction
  stays diagnostic; see §3.4) — no procurement ask.
- Landed authorities [FULL]: VERDICT_wave1.md C9/C11 (via the ledger
  rows :221-252, which carry the adjudication text verbatim) — mesh
  LAW and ESTIMATOR are decided territory; C10 is the OPERATOR only.

**Start line (C12):**
- Q3 corpus (De-Laval transonic computation comparisons, AIAA
  small-throat-curvature paper) [ABS]: Sauer 1944 small-perturbation;
  Hall 1962 inverse-powers-of-Rt series; Kliegel-Levine 1969 series in
  (Rt+1), converges regardless of normalized throat curvature radius,
  third-order axisymmetric solutions; Sauer and Hall unstable at small
  throat wall radius; K-L sonic lines are used to start MoC.
- H-F16 (phaseA_tree_hyperbolic_CONDENSED.md:104-107 [FULL]):
  limiting-characteristic start surface COMPUTED (trace the C- family
  from the sonic-bubble edge), never assumed at the geometric throat;
  H-F15 (:100-102 [FULL]): sonic region as fitted internal boundary.
- Cava A7 (ADVISORY_litreview_confrontation_2026-08-13.md:1026 [FULL]):
  Hoffman-1987 LENGTH oracle's throat model IS Kliegel-Levine with
  C_D≈0.990 ⇒ y_e tests the THROAT MODEL — a banded oracle instrument
  for the higher-order branch, already of record. Cava C33 (:1120
  [FULL]): the pre-registered o32 norms already exclude a
  Sauer-IVL/lip/axis locus — the record's order claims live on the
  h-independent window. Cava R27 (:1154 [FULL]): near-sonic
  adjoint-weight transfer quasi-1D→axisym OPEN, non-blocking, decided
  by [X-GP01] transonic rung or Giles-Pierce 1997 (now on disk per the
  wave-3 brief (ii)) — C11/D-49 territory, named as seam only.
- hoffman_1987_ctp, fernandes_2023 [REC]: registry rows :556-570 —
  of-record reads; Fernandes 2023 = MoC-as-simulator inside FFD loop,
  no start-line accuracy content beyond standard practice.

**Axis process + exit read (C13, C14):**
- Q4 corpus [ABS]: L'Hôpital at r=0 is THE standard treatment in
  axisymmetric MoC practice (Auburn thesis; ducted-nozzle MoC papers);
  Zucrow-Hoffman is the canonical text form (the GENO mirror's own
  source lineage, of record).
- Q5 corpus [ABS]: MMS is the standard verification instrument for
  Euler-class codes incl. supersonic (Roache lineage; Wind-US
  verification); no MoC-specific MMS line surfaced — the axis POINT
  PROCESS MMS named by the GAP-8 owner is in-house work, correctly
  scoped as such.
- P-F22 (phaseA_tree_propulsion_CONDENSED.md:164-171 [FULL]): O1≡O2
  control-surface identity within discretization bar = free
  integral-identity rejector on every solve; already homed as findings
  row carriers:control-surface-invariance-rejector-missing
  (docs/findings_registry.yaml:1588-1592 [FULL], measured grep G:
  `grep -n "control-surface" docs/findings_registry.yaml`).
- H-F19 (phaseA_tree_hyperbolic_CONDENSED.md:121-128 [FULL]):
  parity-correct one-term axis series; Gamma_circ = O(r^2); swirling
  data CANNOT reach the axis with nonzero circulation — vortex-core or
  on-axis solid REQUIRED as explicit sector certificate. Homed of
  record: diff :374-376 [FULL] ("annular interface; the
  axis-circulation obstruction (H-F19) becomes the C13 enrichment") +
  swirl-f2a findings rows :1816-1834 (ids verified by measured grep).
- S-GENOAUDIT of record (memory + advisory): axis/interior unit
  processes GENO-twinned EXACT (fidelity), march-level accuracy
  verification NEVER — consistent with the GAP-8 row [FULL].

**Repo incumbents verified in-window [FULL]:** a1_toc_variational_jax.py
:110-117, :174-184 (knots normalized xi, affine with xB, not dofs);
adaptive_knot_optimize.py:195-212 (insertion_site dx_loc = median
station spacing), :418-459 (W_warm re-interpolation, dev_warm measured
+ printed only); o33_bench.py:195-200 (refine(cfg,r) joint scaling);
a1_ideal_march_jax.py:37-40 (Sauer IVL data contract), :555-566 +
:642-650 (axis residual + predictor), :845-885 (IVL build, exit Mach
from eps), :982-1003 (exit decision on axis Max vs m_stop, exit_cap
discipline).

### 2.4 §0-bis NAMED CROSS-CUTTING AXES — per-row bearing (one sentence each)

- (1) Optimizer query-level choice: bears on NO row here (march/class
  infrastructure upstream of the engine); C8's optimizer-warm-start
  half-sentence is C21/C31 territory, consumed by citation (wave-2
  §2.9), not re-opened.
- (2) Discrete vs continuous adjoint: bears on C12 only as the D-49 /
  F11d Sauer-IVL exclusion-locus seam (C11-owned of record, named in
  §3.5); for C13 the AD-adjoint differentiates the axis residual
  exactly by construction (custom_vjp of record, C56 CLOSED-CONSUMED,
  VERDICT_wave2 §2.10) so the primal verification instruments are the
  open half; does not bear on C5/C6/C8/C10/C14.
- (3) Moving-mesh / r-adaptive: CLOSED at census grade of record
  (VERDICT_C9C11_supplement par.4.1 — A36 is the family principle in
  derived static form); bears on C5 only to be DISTINGUISHED (knot
  motion = design-CLASS bookkeeping, not mesh motion; stated in §3.1)
  and on C10 only via the C9-owned r-arm (not this row's object).
- (4) Adjoint-free routes: bears on no row (no engine choice here).
- (5) Emergent sub-aspects: C6 surfaced the operator-level certified-
  subdivision guard (O-F8 de Casteljau) — closed INTO the C6 verdict
  as named fallback (§3.2); C14 surfaced the "uniform exit premise of
  the eps→Me inversion" as the actual falsifiable content of the row —
  closed inside F-C14-1; no orphan sub-aspect remains.

## 3. ADJUDICATION (per row)

### 3.1 C5 — knot anchoring across thB updates (NEVER; trees SILENT)

Trees: SILENT of record (diff :51 and §5 :398) — the census carries
the row alone, full §0-ter form, materiality valve used honestly.

- **Incumbent (genuine case):** normalized xi re-anchored to live xB
  (a1_toc:110-117, :180-181). This is what makes the class NESTED
  under design motion: knots move affinely with xB exactly as the
  uniform class, so class membership, warm-start exactness (C8
  theorem) and the certificate's design-space identity survive every
  thB update with zero bookkeeping.
- **Physical-x anchored knots (steelman):** exact site persistence —
  an insertion chosen at a physical feature stays there. LOSES by
  stated reason: under xB motion a physical-x knot can exit [xB, L] or
  cross a neighbor (class becomes ill-defined), and nesting — the
  premise of the C8 exactness theorem — breaks; the certificate cost
  is structural, the benefit is bounded by the measured drift below.
- **Arc-length anchored (steelman):** parameterization-intrinsic —
  best where wall arc length is the physical coordinate. LOSES: it
  inserts a state-dependent reparameterization into the traced class
  (recompile/adjoint complexity in the JAX stack) for the same bounded
  benefit.
- **Curvature-anchored (steelman):** the modern fitting line (Q6
  census, incl. evolutionary 2025). LOSES for THIS row: knots here are
  certificate-class markers, not fitting dofs — free-knot/fitting-
  optimal placement is rejected of record (D6 item 9, S20 survey) and
  Q6 surfaced nothing on certificate-class nesting under domain motion.
- **Materiality (measured, of record):** GAP-34 findings row
  (parametrization:knot-xi-drift-unmonitored, :1373-1381,
  DOWNGRADED low): everyday drift ~1.4% of the first m=8 interval,
  S18 event ~4.4% (verifier-corrected ~10x down from the original
  claim); material only against post-insertion LOCAL intervals or
  short-L. DECLARED IMMATERIAL at production scales ⇒ decided on
  cost/simplicity per §0-ter(c), with the monitor as insurance.
- **Outcome (LOAD-CLASS VALVE, sufficient-not-optimized):**
  CONVERGED-KEEP incumbent + arm the GAP-34 monitor as the pinned
  falsifier (protocol F-C5-1, §4.1). Sufficient hypothesis, trivially
  checkable: drift fraction per cycle below a declared fraction of the
  marked interval ⇒ anchoring choice immaterial; monitor fires ⇒ row
  re-opens with physical-x as first candidate.

### 3.2 C6 — insertion degeneracy guard (SINGLE-AUTHOR; the meta-panel single-author residue)

DECLARATION (per brief §REMMARCH): this panel + its wave-3 refuter
treatment IS the owed dual-proof half of the C6 single-author residue;
the row's single-author debt is discharged by this adjudication
surviving the refuter, and that discharge is part of the proposed
row delta.

- **Incumbent (genuine case):** dx_loc = median station spacing
  (adaptive_knot_optimize.py:195-212). Cheap, mesh-derived, DID block
  degenerate insertions on record runs; the FITPACK-read rationale was
  honest at authoring time.
- **Why it loses (measured, of record):** GAP-23 findings row
  (parametrization:insertion-guard-wrong-object, :1310-1318,
  CONFIRMED medium): the guarded object (station spacing) SHRINKS
  under march refinement while the object that actually degrades — the
  interpolation-operator cardinal amplitude — grows ~linearly with
  adjacent-knot ratio (measured 2.2/17.5/170 at 10/100/1e3; cond(A)
  saturates ~6 and is the WRONG metric too). The guard weakens exactly
  when the risk grows; the annex's own status line already recorded
  "rationale measured wrong: solve vs operator". Q1 census confirms
  the theory side: S-W gives uniqueness only; conditioning of the
  interpolation operator is the numerical object.
- **Alternative (operator-derived max adjacent-knot ratio, de Boor /
  S-W):** bounds the mesh-independent object; the GAP-23 owner recipe
  already names it as primary with the station check retained
  secondary. WINS on the pre-registered criterion (bound the object
  that measurably degrades).
- **O-F8 de Casteljau certified subdivision (tree enrichment, diff
  :52-55; optimization tree :548):** operator-level guard that
  converges to sharp — NAMED as the fallback instrument where the
  ratio bound proves too conservative on record data (exchange-refine
  the sufficient check); not adopted as primary (cost above need at
  current insertion counts — materiality honest in the other
  direction).
- **Materiality:** MATERIAL (certificate-touching: repeated insertion
  into the persistent 41.8%-mass interval produces clustering the
  incumbent guard progressively stops guarding — GAP-23 text); repair
  cost trivial (guard edit at next [X-AKNO] touch).
- **Outcome:** CONVERGED-FLIP to operator-derived primary bound +
  station-spacing secondary + de Casteljau named fallback; measured
  half = F2-C6-GUARD-EDIT with the negative control (§4.2). Seam with
  C5 declared: the drift-monitor threshold (F-C5-1) and the ratio
  bound share the [X-AKNO] derivation window — one derivation, two
  consumers, no collision.

### 3.3 C8 — warm-start deviation handling (NEVER; gap-accounting)

- **State of record (the whole case, already measured):** findings row
  parametrization:warmstart-exactness-unarmed (:1319-1326): warm-start
  exactness across knot insertion is a provable THEOREM (nested-space
  uniqueness ⇒ re-interpolation reproduces the incumbent EXACTLY),
  measured P3 = 6.7e-16; but the header claims NOT-geometry-preserving
  and dev_warm only prints (adaptive_knot_optimize.py:435-451) — a
  defect DETECTOR that today would print and pass: the R5 violation
  class the alternative names.
- **Trees (ENRICHING, diff :60-65):** V-F26 (variational condensed
  :220-224) continuation-from-classical-optima backbone; V-F7 (:61-69)
  fold-margin monitor; O-F32 (optimization tree :1549-1564, full
  block) warm-start as first-class fork with a periodic cold-start
  audit falsifier. All three support ARMING; none defends print-only.
- **Seam with C21 (consumed, not re-opened):** VERDICT_wave2 §2.9/4.10
  — C21 = march SEED validity at fixed topology + fixed seed set
  (Tier-0 monitors, Kantorovich radius, replay-ball coverage); C8 =
  DESIGN-VECTOR re-representation exactness at class enrichment. The
  objects are disjoint; seed validity under topology change is F9b's
  (landed C9). O-F32's cold-start audit maps to C21's fresh re-record
  fallback, NOT to C8 — declared so the same advocacy is not counted
  twice.
- **K_RICH NOTIFICATION (binding, RC27-8(iv)/wave-2 precedent):** the
  arming floor consumes K_RICH (dev_warm ≤ K_RICH · eps · measured
  cardinal amplitude · scale) — a K_RICH ROLE for the FAM slot's C42
  role census this same wave. Notified here; FAM owns the census.
- **Materiality:** the floor sits at machine-eps scale — the row is
  bookkeeping of an arming duty. LOAD-CLASS VALVE applied: converge
  with the sufficient hypotheses already in the findings-row recipe.
- **Outcome:** CONVERGED-ADOPT the alternative (armed derived-floor
  rejector + THEOREM row) exactly per the findings-row owner recipe:
  arm dev_warm check + near-degenerate negative control + M0 THEOREM
  promotion for nestedness (Boehm 1980 / Oslo) in the same session
  (R4). Measured half = F2-C8-ARM-DEVWARM (§4.3).

### 3.4 C10 — refinement operator (NEVER; trees PARTIAL)

- **Incumbent (genuine case):** refine(cfg, r) joint global scaling
  (o33_bench.py:195-200) — one knob, certificate-simple, and the
  ladder it feeds is now RESTRUCTURED by landed authority anyway (C11:
  observed-p GCI interim, K=4 in-loop only at p_obs ≥ 0.415 per site;
  C9: A36 static arm + adaptive arms under falsifier). Under those
  landings the operator's certificate role is narrower than when the
  row was minted.
- **Alternative (per-direction refinement, multi-parameter GCI,
  Eça-Hoekstra) at its best:** the census-grade instrument for
  separating error directions with noisy observed orders (Q2 [ABS] +
  WANTED row :850-851). The repo's own GAP-35 row
  (mesh-amr:joint-refine-confounds-directions, :1382-1390, DOWNGRADED
  low) measured the honest state: joint refine confounds the three
  directions (IVL rows / arc angle / contour-exit stations); the
  floor-never-measured claim was REFUTED of record; the surviving
  lever is per-direction ATTRIBUTION.
- **Trees:** PARTIAL only (diff :73-75) — adaptivity doctrine, no tree
  addresses the joint operator explicitly; no 4/4 burden shift.
- **Materiality:** LOW today (bands shrink; the lever bites only at a
  band-shrink attribution question of record — the GAP-35 trigger).
  Decided on cost per §0-ter(c): production flip unjustified; the
  diagnostic arm is nearly free.
- **Outcome (LOAD-CLASS VALVE):** KEEP joint refine(r) as production
  operator; ADOPT the per-direction refinement FLAG as the mandatory
  DIAGNOSTIC instrument at attribution questions (the GAP-35 owner
  recipe), with the pre-registered masking/promotion rule as falsifier
  (F-C10-1, §4.4). Composition with landed C9/C11 declared: mesh LAW
  (C9) and ESTIMATOR (C11) are untouched; this row governs only the
  OPERATOR that builds estimator ladders. Eça-Hoekstra full text not
  blocking (diagnostic arm only) — no procurement ask.

### 3.5 C12 — start line (SINGLE-AUTHOR; accuracy axis NEVER; PROTOCOL + rejector only)

SCOPE CLAUSE HONORED: the higher-order branch (if the rejector fires)
is F4b/F5-owned and is NOT adjudicated here; this panel adjudicates
the accuracy-axis PROTOCOL + rejector.

- **Incumbent (genuine case):** Sauer first-order, gamma=const,
  GENO-mirrored +1e-6 offset (a1:37-40, :845-860). It is the DATA
  CONTRACT of the twin comparison (fidelity adjudicated: 7.6e-9
  cross-code agreement), deliberately not a solver step; the march is
  EOS-general. GENO-mirror provenance caveat honored (MoC memory row:
  cross-code agreement ≠ accuracy — and the GAP-6 row itself already
  says exactly this: "proves fidelity, not accuracy").
- **The structural defect (of record):** GAP-6 findings row
  (mesh-amr:start-line-accuracy-rejector-missing, :1155-1164): both
  resolutions share the same start ⇒ IVL model error invisible to e(x)
  BY CONSTRUCTION (correlated-error mechanism); corrected magnitudes:
  NET wall perturbation +0.17 a* at production, r=0.1 extreme net 2.5
  (series meaningless there).
- **Census (accuracy instruments):** Q3 [ABS]: Kliegel-Levine
  converges regardless of normalized throat curvature radius and gives
  third-order axisymmetric solutions; Sauer/Hall unstable at small
  throat wall radius — this is the F4b/F5 branch's presumptive
  candidate ranking, RECORDED here, not decided. Cava A7 [FULL]:
  the Hoffman-1987 length oracle is a BANDED instrument whose y_e
  tests the K-L throat model — an oracle already of record for that
  branch. Trees (diff :82-86): H-F16 limiting-characteristic start
  surface + H-F15 fitted sonic region — a THIRD instrument class
  (compute the start surface, don't model it), also F4b/F5-owned;
  proposed as a named addition to the branch's candidate list in the
  row note (note-delta, no new row — the branch is homed in GAP-6's
  owner text).
- **Adjudication of the PROTOCOL (the only decidable half here):** the
  pre-registered criterion selects the displaced-start-line invariance
  rejector (the GAP-6 owner recipe: displace the IVL by +3 columns,
  restart, contour must move < band) because it BREAKS the shared-start
  correlation with zero new modeling; a shared-start re-march cannot
  fire by construction and loses. The rejector is completed with a
  vacuousness negative control (F-C12-2) per the cava D-47 discipline
  (a rejector that cannot fire is not a rejector).
- **Materiality:** MATERIAL at compact-throat instances (declared OUT
  of the first-order start's validity domain regardless — the row's
  trigger already names rtu→0.5 / r=0.1); at production rtu the
  +0.17 a* net perturbation vs the composed band is exactly what the
  rejector measures — the protocol IS the materiality measurement.
- **Seams declared, not double-adjudicated:** D-49 (Sauer-IVL DWR
  exclusion-locus probe) is C11-owned of record (findings :1471; lands
  in the F2 window in both F11a branches); R27 (adjoint-weight
  transfer) is its input; C12's rejector is a PRIMAL instrument and
  does not overlap it.
- **Outcome:** ADJUDICATED-PROTOCOL (split): protocol + falsifier
  pinned now (F-C12-1/2, §4.5); measured half = F2-C12-STARTLINE-
  REJECTOR (the GAP-6 owner, next engine window); higher-order branch
  stays F4b/F5 with the census ranking recorded (K-L presumptive
  first candidate; H-F16 limiting-characteristic named alongside;
  A7 oracle = the branch's banded instrument).

### 3.6 C13 — axis unit process (SINGLE-AUTHOR)

DECLARATION: as for C6, this panel + refuter treatment is the owed
dual-proof half of the single-author residue.

- **Incumbent (genuine case):** GENO foot-ratio guard v1/y1
  (a1:555-566 axis residual, :642-650 predictor). S-GENOAUDIT of
  record: unit processes GENO-twinned EXACT and the march BC machinery
  proven at unit level — fidelity is settled; march-level accuracy
  verification is the NEVER half (GAP-8 findings row
  mesh-amr:axis-process-unverified, :1176-1183, CONFIRMED medium:
  no MMS, no axis-only refinement, and Me_ach — verdict-load-bearing —
  IS the axis-cell value; honest datum: the consistency hand-probe
  found an O(h^2) coefficient bias, NO order defect, disclaimed).
- **Alternative 1 (L'Hôpital dv/dy carried, Zucrow-Hoffman) at its
  best:** carries the limit analytically instead of guarding the
  ratio — the canonical text form (Q4 [ABS] confirms it as standard
  practice). NOT adopted now, by the pre-registered criterion: no
  instrument has shown a defect in the incumbent (hand-probe: no order
  defect), and replacing a twin-exact unit process on taste would
  spend the fidelity certificate for nothing. RETAINED as the named
  candidate that the instruments promote if they fire.
- **Alternative 2 (MMS verification) at its best:** the standard
  verification instrument (Q5 [ABS], Roache lineage). ADOPTED as
  instrument, not as process change — exactly the GAP-8 owner recipe
  (march-level gconst axis oracle at two NI + MMS per Roache /
  Zucrow-Hoffman axis point process).
- **Tree enrichment (H-F19, diff :87-92; hyperbolic condensed
  :121-128):** (a) parity-correct one-term axis series ⇒ a FREE parity
  invariant on recorded cells (adopted as F-C13-3); (b) the swirl
  obstruction — swirling data cannot reach the axis with nonzero
  circulation; vortex-core or on-axis solid REQUIRED as sector
  certificate. INTERACTION NAMED, NOT DECIDED (phase-owned): of record
  the interface is ANNULAR and the obstruction is homed as "the C13
  enrichment" (diff :374-376); consumers = swirl-f2a findings rows
  (:1816-1834) and the S-5F dossier chain (BLOCCATO 14(d)); any
  swirling-data extension makes the axis process a SECTOR-CERTIFICATE
  question owned there. Proposed as a note-delta cross-link on the
  row, no new row (dedup: measured greps G4/G5, §4 header).
- **Materiality:** MATERIAL — Me_ach is the verdict-load-bearing exit
  read and it IS the axis cell (GAP-8 text); the instruments are the
  cheap half (2 reduced marches + recorded-cell checks).
- **Outcome:** ADJUDICATED-PROTOCOL (split): incumbent retained on
  fidelity-of-record + no-defect-found; verification instruments
  BINDING with pinned falsifiers (F-C13-1/2/3, §4.6); measured half =
  F2-C13-AXIS-ORACLE (the GAP-8 probe owed, F2 engine window);
  L'Hôpital-carried form = the named promotion candidate on fire.

### 3.7 C14 — exit-condition read point (NEVER; trees PARTIAL; gap-accounting)

- **Incumbent (genuine case):** axis-cell Me only (a1:982-1003: exit
  decision on |Max − Me| < m_stop with the C2-F4 exit_cap discipline
  already surfaced honestly). Cheap, converged, and the S19/S21
  discipline around m_stop and cap exits is real prior work — the
  DECISION logic is not the defect.
- **The actual defect (formalized via the frozen question):** the read
  carries NO band and its premise — uniform exit line, so the
  axis-cell value represents the exit state that the eps→Me inversion
  (leggeAree read, a1:864-885) assumed — is never checked. GAP-8's row
  already states the read is verdict-load-bearing.
- **Alternative (multi-point exit read + band) at its best:** the
  exit-line row j2 cells are ALREADY COMPUTED (a1 header :49 — row j2
  marches along the exit Mach line): the band costs a max() over
  existing values.
- **Tree enrichment (P-F22, diff :93-94; propulsion condensed
  :164-171):** the O1≡O2 control-surface identity as free per-solve
  rejector — ALREADY HOMED as findings row
  carriers:control-surface-invariance-rejector-missing (:1588-1592,
  3/4 blind convergence recorded there). CONSUMED by citation; C14's
  verdict adds no second home (dedup grep G3).
- **Materiality:** spread expected small by construction (uniform-exit
  region built by the march) but UNVERIFIED — precisely the case where
  the LOAD-CLASS VALVE says: adopt the trivially-checkable sufficient
  form early and cheaply.
- **Outcome (LOAD-CLASS VALVE, sufficient-not-optimized):**
  CONVERGED-ADOPT the alternative in sufficient form: exit decision
  logic UNCHANGED (no re-opening of the converged march); the
  certificate additionally carries (a) the exit-line Me spread as a
  reported band with a derived threshold, and (b) the control-surface
  identity rejector per the existing findings row. Falsifiers
  F-C14-1/2 (§4.7); duty folded into the GAP-8 oracle window per the
  ledger's own owner line ("folded into GAP-8 oracle") —
  F2-C14-EXITBAND rides F2-C13-AXIS-ORACLE.

## 4. PROPOSED VERDICTS + DUTIES (deltas PROPOSED; landing executes)

Dedup for this section, measured in-window (SR-12):
- G1 `grep -c "limiting.characteristic" docs/findings_registry.yaml docs/choice_ledger.yaml docs/claims_registry.yaml` → 0/0/0 (no existing home; folded as C12 note-delta, no mint — the branch lives in GAP-6's owner text).
- G2 `grep -n "warm" docs/findings_registry.yaml` → rows :1319 (warmstart-exactness-unarmed), :1328 (insertion-only ratchet, C7 REMPOL) — C8 recipe homed, no mint.
- G3 `grep -n "control-surface" docs/findings_registry.yaml` → row :1588 exists — C14 consumes, no mint.
- G4 `grep -n "^- id: .*swirl" docs/findings_registry.yaml` → swirl-f2a rows :1816/:1825/:1834 + swirl5f rows — H-F19 obstruction homed (diff :374-376), note-delta only.
- G5 `grep -n "circulation|vortex-core|on-axis" docs/findings_registry.yaml` → no dedicated axis-circulation row; homed per G4 + diff — no mint (phase-owned).
- G6 `grep -n -i "hoekstra" docs/literature_registry.yaml` → wanted_eca_hoekstra_2014 :850 exists — cited, no mint.
- Candidate NEW rows from this panel: **0** (every instrument/duty is homed in an existing findings row: GAP-6/8/23/34/35 + carriers:control-surface-invariance-rejector-missing + parametrization:warmstart-exactness-unarmed).

### 4.1 C5 (docs/choice_ledger.yaml:178-188)
- **Proposed:** NEVER → "ADJUDICATED-CONVERGED-KEEP (wave-3 2026-08-20;
  incumbent normalized-xi/live-xB retained on class-nesting +
  measured immateriality GAP-34; monitor-armed)".
- **Falsifier F-C5-1:** per-cycle knot-drift monitor (the GAP-34 owner
  recipe armed): metric = |Δxi_phys| / marked-interval width per
  cycle; threshold = declared fraction DERIVED from the C6 guard
  margin (drift may not consume the dx_loc/ratio guard margin;
  derivation published before first use, same [X-AKNO] window as
  F-C6-1); replay control: the S18 event (~4.4% of the first m=8
  interval) must be FLAGGED if the derived threshold < 4.4%, else the
  derivation must state why it passes. Fire ⇒ re-derive xi in-window
  AND re-open C5 with physical-x anchoring as first candidate.
- **Duty:** F2-C5-KNOTDRIFT-MONITOR (rides the GAP-34 owner, next
  [X-AKNO] touch; shares the derivation window with F2-C6-GUARD-EDIT —
  seam declared, one derivation, two consumers).
- **What would overturn:** a measured production campaign (short-L or
  post-insertion-local) where drift bias exceeds the composed band —
  then materiality flips and the row re-opens.

### 4.2 C6 (docs/choice_ledger.yaml:190-198)
- **Proposed:** SINGLE-AUTHOR → "ADJUDICATED-SPLIT (wave-3 2026-08-20;
  guard-object FLIP converged: operator-derived max adjacent-knot
  ratio PRIMARY (de Boor/Schoenberg-Whitney; the GAP-23 owner recipe),
  station-spacing check retained secondary, O-F8 de Casteljau
  certified subdivision named fallback where the ratio bound is too
  conservative; single-author residue DISCHARGED by this panel+refuter
  = the owed dual-proof half; measured half gated on
  F2-C6-GUARD-EDIT)".
- **Falsifier F-C6-1 (adoption control):** negative control = synthetic
  knot vector at adjacent ratio 1e3 (the GAP-23 measured configuration,
  cardinal amplitude 170): the new guard must REJECT it while the
  incumbent guard passes it — the measured separation between the two
  objects. Acceptance bound: admitted ratio derived from the measured
  amplitude curve (amplitude ≤ K_amp × baseline; K_amp derivation
  published before first use).
- **Falsifier F-C6-2 (flip refuter):** if on recorded [X-AKNO] cycles
  the ratio bound rejects insertions whose measured cardinal amplitude
  is within the baseline band at a rate that stalls enrichment, the
  constant is re-derived once; if NO constant separates harmful from
  harmless on record data, the flip is REFUTED and the incumbent
  regains primary (with the de Casteljau sharp check as the successor
  candidate).
- **Duty:** F2-C6-GUARD-EDIT (next [X-AKNO] touch; cheap).
- **Dependencies:** C5 seam (§4.1); C42/K_RICH NOT consumed here (the
  amplitude bound uses its own derived K_amp, not K_RICH — stated to
  keep the FAM census clean).

### 4.3 C8 (docs/choice_ledger.yaml:211-219)
- **Proposed:** NEVER → "ADJUDICATED-CONVERGED (wave-3 2026-08-20;
  alternative ADOPTED per findings row
  parametrization:warmstart-exactness-unarmed: armed derived-floor
  rejector + M0 THEOREM promotion (Boehm 1980/Oslo nestedness, R4);
  LOAD-CLASS VALVE applied — sufficient-not-optimized; C21 seam
  declared disjoint per VERDICT_wave2 §2.9 (design-vector
  re-representation vs march-seed validity; topology changes = F9b);
  measured half gated on F2-C8-ARM-DEVWARM)".
- **Falsifier F-C8-1:** armed floor dev_warm ≤ K_RICH · eps ·
  A_card(measured) · scale; positive control: record P3 = 6.7e-16
  PASSES; negative control: deliberately broken re-interpolation
  (near-degenerate knot remap / off-by-one knot index) must FIRE —
  zero fire on the negative control ⇒ rejector vacuous ⇒ NOT adopted
  (cava D-47 discipline).
- **Falsifier F-C8-2:** the M0 THEOREM row carries the nestedness
  hypotheses explicitly; any future insertion path that violates them
  (non-nested remap) must be excluded by the theorem's scope line —
  refuter-checkable at the M0 touch.
- **Duty:** F2-C8-ARM-DEVWARM (next [X-AKNO] touch, rides the same
  window as F2-C6-GUARD-EDIT) + the R4 M0 promotion in the same
  session per the findings-row owner.
- **NOTIFICATION to FAM/C42 (binding, this wave):** the arming floor
  consumes K_RICH — a K_RICH ROLE for the C42 role census (RC27-8(iv)
  / wave-2 C21-omega precedent). The FAM slot must count it.
- **What would overturn:** the negative control not firing (vacuous
  rejector), or a nestedness-hypothesis violation in the production
  insertion path.

### 4.4 C10 (docs/choice_ledger.yaml:232-240)
- **Proposed:** NEVER → "ADJUDICATED-SPLIT (wave-3 2026-08-20; joint
  refine(r) RETAINED as production operator on materiality (GAP-35
  DOWNGRADED low; bands shrink of record); per-direction refinement
  FLAG adopted as the MANDATORY diagnostic at band-shrink attribution
  questions (the GAP-35 owner recipe; Eça-Hoekstra multi-parameter =
  census instrument, WANTED row :850); composition declared: C9 mesh
  LAW and C11 ESTIMATOR landed of record are untouched — this row
  governs the ladder-building OPERATOR only; measured half gated on
  F2-C10-PERDIR-FLAG)".
- **Falsifier F-C10-1 (pre-registered promotion rule):** at any
  certificate band-shrink attribution question of record: run the
  three single-direction refinements at r=2 (NI rows / da arc angle /
  Nw-Ne stations per o33_bench.refine fields), publish per-direction
  observed orders; MASKING TEST: joint band shrinks while any
  single-direction band fails to shrink beyond its own noise floor ⇒
  the joint operator LOSES its certificate role at that site and
  per-direction becomes mandatory there (promotion, converged — not
  re-gated).
- **Duty:** F2-C10-PERDIR-FLAG (next o33_bench touch, per the GAP-35
  owner; trivial code).
- **What would overturn:** a masking event (F-C10-1) — promotion is
  the pre-registered consequence, no re-adjudication.

### 4.5 C12 (docs/choice_ledger.yaml:254-264)
- **Proposed:** SINGLE-AUTHOR → "ADJUDICATED-PROTOCOL (wave-3
  2026-08-20; accuracy-axis PROTOCOL pinned: displaced-start-line
  invariance rejector (GAP-6 owner recipe) + vacuousness negative
  control; twin DATA CONTRACT untouched; validity domain declared
  (compact-throat rtu→0.5 / r=0.1 OUT regardless, per the GAP-6
  trigger); higher-order branch F4b/F5-owned NOT adjudicated —
  census ranking RECORDED: Kliegel-Levine presumptive first candidate
  (converges at small Rt, third-order axisym; Q3 census 2026-08-20),
  H-F16 limiting-characteristic start surface named alongside
  (hyperbolic tree :577), cava A7 Hoffman-1987 length oracle = the
  branch's banded instrument; D-49/R27 = C11-owned seam, not
  double-adjudicated; measured half gated on
  F2-C12-STARTLINE-REJECTOR)".
- **Falsifier F-C12-1 (the rejector):** displace the IVL by +3 march
  columns, restart the march, metric = max wall-contour displacement
  and ΔJ vs the composed band (band derivation published before first
  use); FIRE ⇒ the accuracy axis re-opens as F4b/F5 with the recorded
  candidate ranking.
- **Falsifier F-C12-2 (vacuousness control):** the rejector must FIRE
  on a deliberately corrupted start state (perturbation = 10× the
  GAP-6 net production figure 0.17 a*); if it stays within band on
  the corrupted start, the displacement protocol is VACUOUS and an
  independent instrument (H-F16 comparison surface) becomes REQUIRED —
  still F4b/F5-owned, but the protocol verdict here is overturned.
- **Duty:** F2-C12-STARTLINE-REJECTOR (the GAP-6 owner, next engine
  window) + the M0 band-blindness one-liner already owed by the same
  row.
- **Dependencies:** GENO-mirror provenance caveat honored (single-code
  evidence = fidelity only); C11/D-49 and R27 seams named.

### 4.6 C13 (docs/choice_ledger.yaml:266-275)
- **Proposed:** SINGLE-AUTHOR → "ADJUDICATED-PROTOCOL (wave-3
  2026-08-20; incumbent foot-ratio unit process RETAINED
  (GENO-twin-exact fidelity of record, hand-probe found no order
  defect); verification instruments BINDING per the GAP-8 owner:
  march-level gconst axis oracle at two NI + MMS (Roache /
  Zucrow-Hoffman axis point process) + H-F19 parity invariant adopted
  as free check; L'Hôpital-carried (Zucrow-Hoffman) = named promotion
  candidate on instrument fire; single-author residue DISCHARGED by
  this panel+refuter = the owed dual-proof half; swirl/axis-circulation
  obstruction (H-F19) NAMED-not-decided: annular interface of record
  (diff :374-376), consumers = swirl-f2a rows + S-5F dossier chain
  (BLOCCATO 14(d)) — any swirling-data extension makes the axis a
  SECTOR-CERTIFICATE question owned there; measured half gated on
  F2-C13-AXIS-ORACLE)".
- **Falsifier F-C13-1:** two-NI gconst axis oracle: axis-cell observed
  order below the march's certified order beyond derived band ⇒ axis
  process demoted, L'Hôpital-carried candidate promoted.
- **Falsifier F-C13-2:** MMS on the axis point process (manufactured
  axisymmetric field with parity-correct v = a1·y + O(y^3)): order
  defect ⇒ same demotion.
- **Falsifier F-C13-3 (free, recorded data):** parity invariant
  |v1/y1 − (dv/dy)_axis| within derived band on recorded march cells
  at two NI; violation ⇒ demotion.
- **Duty:** F2-C13-AXIS-ORACLE (F2 engine window; the GAP-8 probe
  owed; carries F2-C14-EXITBAND as its second leg, §4.7).
- **What would overturn:** any of F-C13-1/2/3 firing (promotion path
  pre-registered); or the swirl-owned windows changing the interface
  scope (then the sector-certificate question supersedes this row's
  scope — named conditionality, not re-argued here).

### 4.7 C14 (docs/choice_ledger.yaml:277-285)
- **Proposed:** NEVER → "ADJUDICATED-CONVERGED (wave-3 2026-08-20;
  alternative ADOPTED in sufficient form (LOAD-CLASS VALVE): exit
  DECISION logic unchanged (axis Max vs m_stop + exit_cap discipline
  stand); certificate additionally carries (a) exit-line Me spread
  band (row-j2 cells already computed; threshold derived from the
  certified per-cell bands, published before first use) and (b) the
  control-surface identity rejector CONSUMED from findings row
  carriers:control-surface-invariance-rejector-missing (:1588 — cited,
  not re-minted); duty folded into the GAP-8 oracle window per the
  row's own owner line)".
- **Falsifier F-C14-1:** exit-line spread: max_j |Me_j − Me_axis|
  beyond the composed band ⇒ the uniform-exit premise of the eps→Me
  inversion (a1:864-885) is REJECTED for that run; pre-registered
  fallback = mass-flux-weighted exit read (then C14 re-opens ONLY on
  the read-point definition, converged otherwise).
- **Falsifier F-C14-2:** O1≡O2 control-surface identity beyond the
  discretization bar on any solve ⇒ exit bookkeeping (or the march)
  rejected — per the existing findings row; C14 consumes its fire.
- **Duty:** F2-C14-EXITBAND (folded into F2-C13-AXIS-ORACLE window,
  per the ledger owner "folded into GAP-8 oracle").
- **What would overturn:** F-C14-1 firing (fallback pre-registered).

## PAPERS NEEDED (procurement channel — may be empty; it is)

**EMPTY.** No verdict above waits on a full text not held: Eça-Hoekstra
2014 full text is NOT blocking (per-direction stays diagnostic; the
WANTED row :850 already exists and stands); Kliegel-Levine 1969 / Hall
1962 full texts belong to the F4b/F5 higher-order branch and become
that branch's procurement ask IF F-C12-1 fires — asking now would be
inflation of the current scope (materiality clause used in the
cost-saving direction, declared).

## 5. MACHINE SUMMARY

```
cluster: REMMARCH
rows:
  C5:  {proposed: "ADJUDICATED-CONVERGED-KEEP (incumbent; GAP-34 monitor armed)", gated: true,  duty: "F2-C5-KNOTDRIFT-MONITOR"}
  C6:  {proposed: "ADJUDICATED-SPLIT (guard-object flip: operator-derived ratio primary; single-author residue discharged)", gated: true,  duty: "F2-C6-GUARD-EDIT"}
  C8:  {proposed: "ADJUDICATED-CONVERGED (armed derived-floor rejector + M0 nestedness THEOREM; valve applied)", gated: true,  duty: "F2-C8-ARM-DEVWARM"}
  C10: {proposed: "ADJUDICATED-SPLIT (joint refine retained production; per-direction flag = mandatory diagnostic)", gated: true,  duty: "F2-C10-PERDIR-FLAG"}
  C12: {proposed: "ADJUDICATED-PROTOCOL (displaced-start-line rejector + vacuousness control; higher-order branch F4b/F5, ranking recorded)", gated: true,  duty: "F2-C12-STARTLINE-REJECTOR"}
  C13: {proposed: "ADJUDICATED-PROTOCOL (incumbent retained; two-NI axis oracle + MMS + parity invariant binding; swirl obstruction named-not-decided)", gated: true,  duty: "F2-C13-AXIS-ORACLE"}
  C14: {proposed: "ADJUDICATED-CONVERGED (exit-line Me band + control-surface identity consumed; decision logic unchanged; valve applied)", gated: true,  duty: "F2-C14-EXITBAND (folded into F2-C13-AXIS-ORACLE)"}
rows_adjudicated: 7
deltas_proposed: 7 row deltas + 1 binding notification (K_RICH new role -> FAM/C42 census, from C8 arming floor) + 2 note-deltas (C12: H-F16 limiting-characteristic added to the F4b/F5 candidate list inside the GAP-6 home; C13: swirl-f2a/S-5F cross-link)
escalation_candidates: 0
papers_needed: 0 (Eça-Hoekstra full text non-blocking, WANTED row :850 stands; Kliegel-Levine/Hall = F4b/F5's conditional ask, not made now)
candidate_new_rows: 0 (dedup-verified by measured greps G1-G6, §4 header: all instruments homed in findings rows GAP-6/8/23/34/35, parametrization:warmstart-exactness-unarmed, carriers:control-surface-invariance-rejector-missing)
census_recency: "1944-2025; newest census items 2025 (Lauer-Ansell PAS review [TITLE], evolutionary knot placement [ABS]); web queries Q1-Q6 dated 2026-08-20"
alternatives_closed: 14 (C5: physical-x/arc-length/curvature-anchored = 3; C6: operator-ratio adopted + de Casteljau fallback = 2; C8: armed rejector adopted vs print-only = 1; C10: per-direction diagnostic vs production = 1; C12: displaced-rejector adopted, K-L/Hall/H-F16 recorded-not-adjudicated per scope clause = 4; C13: L'Hopital-carried named candidate + MMS adopted instrument = 2; C14: multi-point band adopted = 1)
inflation_check: done (no option closed by omission; every rejection carries a stated reason; verdicts written at the depth actually held, [REC]/[ABS]/[TITLE] declared; scope clauses honored: C12 higher-order F4b/F5, C13 swirl phase-owned)
```

Sources (web census, 2026-08-20): [Floater, Schoenberg-Whitney notes](https://www.uio.no/studier/emner/matnat/math/MAT4170/v22/undervisningsmateriale/schoenberg-whitney.pdf); [Eça & Hoekstra 2014, JCP 262](https://www.sciencedirect.com/science/article/abs/pii/S0021999114000278); [De-Laval transonic computation approaches](https://www.researchgate.net/publication/324966447_De-Laval_Nozzles_transonic_flow_field_computation_approaches); [AIAA small-throat-curvature transonic flow](https://arc.aiaa.org/doi/abs/10.2514/3.5355); [Auburn axisymmetric MoC thesis (L'Hopital axis)](https://etd.auburn.edu/bitstream/handle/10415/3133/Young_Thesis.pdf?sequence=2&isAllowed=y); [Wind-US MMS verification](https://www.researchgate.net/publication/268564664_Verification_of_the_Wind-US_CFD_Code_Using_the_Method_of_Manufactured_Solutions); [MMS for Euler/Navier-Stokes](https://www.researchgate.net/publication/244987179_Verification_of_EulerNavier-Stokes_codes_using_the_method_of_manufactured_solutions); [evolutionary knot placement 2025](https://www.tandfonline.com/doi/full/10.1080/10618600.2025.2450458); [fast automatic knot placement](https://www.sciencedirect.com/science/article/abs/pii/S0010448520300981).
