# ADDENDUM C4 to BRIEF_blocco2_phaseD.md — expert-evaluation items
# (user injection (6), 2026-08-20, landed MID-FLIGHT of the Blocco-1
# workflow; the in-flight brief is FROZEN, so this addendum is the
# carrier. Binding on: the orchestrator's judge-delivery check, any
# post-judge repair/escalation round, and the landing. Round agents
# that read it (refuters/revisers read from disk) apply it directly.)

## (b) RIGHT-SIZING EXPECTATION OF RECORD (T-RED gradient level)
The design-GRADIENT-level bound of the T-RED residual operator
(channel (vi) mandate) is EXPECTED to land as SCHEMA + named M-RED
duty, not closed form. DO NOT spend rounds forcing a closed-form
gradient bound: an honest SCHEMA (hypothesis list + bound structure +
what-derives-it: five-field content bound / route-B / M-RED gradient
measurement) at declared rigor class is an acceptable dry outcome.
Label inflation remains forbidden in the other direction.

## (c) STRUCTURAL FACT — MUST APPEAR IN THE FORCHETTA TABLE
No external published referee exists for the per-phase thrust error:
the literature carries NO unsteady c_F datum that discriminates the
2D-per-phase-averaged prediction against 3D-unsteady truth (the
Harroun 1.25-flat is quasi-cycle-averaged, verified verbatim at
source 2026-08-20 — it is the non-discrimination datum, not a
referee). CONSEQUENCE (stated in the table's header or notes): the
bracket CLOSES only via our own R22-CFD or a dedicated data
procurement; until then every cell is bound/estimate-class, never
externally refereed. This declaration is a delivery-check item.

## (a) FIELD-ATLAS INPUT (targeted, in flight)
A targeted field-atlas pass (T-RED-bearing published fields, Harroun
2021 figs 12-20 first) is being produced in parallel:
blocco3/FIELD_ATLAS_targeted_c4.md. Revisers/refuters of rounds that
find it ON DISK may cite it (ADVISORY provenance, per-figure
readings with declared pages); its absence blocks nothing. The FULL
systematic corpus field-reading pass remains with the UNRATIFIED
BLOCCATO-16 proposed session — do not expand scope here.

## (e) TEMPORAL-FORM ROW REFINEMENT (for the coverage-gate sweep)
The candidate foundation row "temporal form of the design functional
(cycle-average vs harmonic-balance adjoint vs time-spectral vs
windowed unsteady adjoint)" anchors to the WEAKENED-PIN regime only:
INSIDE the periodic-rotating-wave pin the quotient structure makes
the cycle-average CANONICAL (a theorem-backed consequence, not a
choice); the alternatives question is live only where the pin is
relaxed (multi-frequency / aperiodic / windowed regimes). Row text
at mint must carry this scoping. Anchors of record: rubino_2018,
schotthofer_2024, zahr_persson_2016 (root D, read-integral).
