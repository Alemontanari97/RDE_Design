# REFUTATION — BLOCCO 3 WAVE 2, CLUSTER C1REP
S-FOUNDATIONS-C2, 2026-08-19. Refuter of record per
BRIEF_wave2_refuter_judge.md (wave-1 refuter brief verbatim + extra
duties g/g2/h + the reconciliation-honesty duty of
BRIEF_wave2_reconcile.md). Input read IN FULL: PANEL_C1REP.md (1194
lines, reconcile rewrite of record). BASE =
validation/sfoundations_raws_2026-08-13. Every anchor this file
relies on was re-read at source IN THIS WINDOW (SR-12; the panel's
measured greps re-run, not inherited).

## 0. NULL=FAILURE CHECK — PASS

PANEL_C1REP.md exists, is complete per the panels-brief schema +
§0-bis/§0-ter additions (query table, criteria pre-registration,
materiality, steelman, read-depth markers, axis bearing, PAPERS
NEEDED, machine summary, reconciliation declaration). Adjudicable.

## 1. SOURCE-VERIFICATION RECORD (what was checked, this window)

Verified VERBATIM at source (all clean unless named in §2):
- Ledger rows at the panel's re-measured refs: C1 :135-145 (owner
  "post-solve change-of-basis certificate = S25"; note D6:915-926
  pointer), C2 :147-156 (-82% r=1, cd 8.2%, baseline 5 digits,
  alternatives = {not-a-knot, free-end control points}), C3 :158-164
  ("no genuine alternative exists" gloss), C49 :653-663 (owner =
  Form-2 genuine captured advocate + user question verbatim), C9
  :219-228 (supplement folded: "front-fitting = the family alignment
  content already exact of record (C49/Lemma B par.4.3)"; MMPDE
  non-transfer), C11 :240-250 (weight of record = discrete AD-adjoint;
  "a weight cannot referee itself"; F11d two legs), C56 :731-740
  (owner: "gradient role OWED TO wave-2 C31TRIO (forecast, not
  consumed...)"). ALL refs CORRECT at my own measurement — the
  RECONCILE-R1 re-measurement is confirmed.
- Diff anchors: :22-33 (C1 O-F4/O-F30/V-F13/H-F34(f)/P-F19), :34-37
  (C2 dissolve), :38-44 (C3 challenged), :159-167 (:164
  "derivative-free/evolutionary strictly quarantined to exploration"),
  :282-287 = item 12 / :288-293 = item 13 front-terms (RECONCILE-R2
  confirmed correct), :294-299 (item 14, |J_capture − J_fitted| (v)
  carrier + H-F8 doctrine), :330-335 ("4/4 trees land on
  piecewise-smooth fitted-front classes with capturing never a
  certificate"), :377-382 (§4.8 P-c, census-lemma agenda, F2-exit
  user pin).
- D6 :915-940: S20 RESIDUE-(a) SURVEY DECISION; B-spline control-point
  switch rejection text at :936-940 verbatim ("...requires
  re-validating the entire certified stack, while the S19 measurement
  locates the defect in knot PLACEMENT, not parametrization
  conditioning — which the driver already handles by measured Jacobi +
  full segment Hessian"); U1 C_geo at :927-929.
- Findings :1290-1299 (GAP-21: ratio -(2-sqrt(3)) EXACT, amplitude ~
  slope mismatch, growth 1/h, "independently re-derived"; owner :1297
  "post-solve change-of-basis certificate = cheap first step"; trigger
  :1298; Q2 quarantine :1299) and :223-231 (GAP-5: -82%/8.2%/5 digits;
  trigger :231 "first verdict leaning on the lip-node gradient").
- Census pins: CEN-O3 :355-356 (PINNED, Chenais form); CEN-O9 :415 —
  BUT :357-358 records CEN-O9 DISSOLVED (see RC1R-6(iv)); CEN-O11
  :423-424 (Lambda = lip circles).
- Claims: T-LEMB :485-495 (statement :489 verbatim), S-LBML :656-665
  (statement :660 verbatim, h^2 dual pairing, O3.2 self-monitoring),
  PAP-P2LB :1480-1488 (statement :1484).
- M0 :185-198 (D-S1 D2.5) and :324-327 (delta_S1 a-posteriori
  membership) verbatim.
- Cava rows: C8 :1096 (Bézier a 9 parametri, GA+RANS, exact-theory
  2.45% beats GA 2.90-3.68%, "rami paralleli"; :106 confirms the
  Kraiko-Pyankov-Tillyaeva authorship), 3.18 :590-599 (quote at
  :594-595 "hundredth fractions of per cent", KT2015 p.187 da ref [8]),
  C10 :1098 (retraction: locus = parete/bordo d'uscita, NON l'urto;
  lip/wall exclusion-norm corroboration), A18 :1037 (ACE O3.5,
  homogeneous-interior clauses (i)-(iv)), A15 :1034 (FD-on-manifold,
  derived tolerance, >=4-decade tau sweep), R14 content (fixed
  geometry, kinematics-spline dofs) — at :1142, NOT :1140 (RC1R-6(i)),
  :130 ("ha la macchina e non ha il problema").
- Literature registry: giles_ulbrich_2010_part1 :105-113 (READ-PARTIAL,
  eps=h^alpha, p.886 adjoint interior BC, capturing-vs-fitting
  page-verified), part2 :114-121 (READ-PARTIAL, "fixed-stencil
  discrete adjoint converges to a WRONG value across shocks (pp.907,
  910) — 'the trap is a theorem'" VERBATIM), lozano_ponsin_2025
  :122-128 (Eqs. (30)/(31)), lozano_2018/2019 :147-162, ancourt
  :199-208 (READ-INTEGRAL, "ACE residuals as code verification"
  VERBATIM), giles_pierce_2001 :491-498, zahr_persson_2016 :572+.
- Tree advocacy at the condensed anchors, all VERBATIM: O-F4 [L298]
  ("Bernstein-coefficient bounds give SUFFICIENT certificates ...
  WITHOUT sampling"; "spline classes with bounded control polygons are
  compact"; "cone conditions ... linear constraints on control points
  (offset conditions), giving A(c)-membership by construction";
  existence link FORK-30), O-F8 [L516] ("converges to sharp by de
  Casteljau subdivision, a certified branch-and-bound on the
  constraint itself"), O-F30 [L1488], V-F6 [L317] ("O3 only as the
  *numerical realization* of O1/O2 with an a posteriori check"),
  V-F13 [L605] (two-tier level-set discovery + spline certificate),
  H-F4 [L209] ("(c) as the exploration/fallback class with an explicit
  downgrade flag"), H-F8 [L328] ("agreement is NOT truth, only
  non-rejection"; "(c) ... sector-agnostic explorer"), H-F9 [L364]
  ("fit any front whose contribution to the DWR error estimate exceeds
  the assigned budget line"), H-F10 [L396] ("(d) as the modern upgrade
  path if front-interaction complexity grows"), H-F18 [L622] ("insert
  the exact centered solution as internal data; ... the lip-shock
  generator on overexpanded phases"), H-F28 [L874] ("(c)+(b) ... with
  (d) on the theory shelf"), H-F29 [L901] ("(a) in FF-A as the
  gradient of record, (d) as its continuous twin"), P-F12 [L474],
  P-F19 [L748] (analytic Rao spine cross-check), P-F21 [L821] ("O2
  with radius at the curvature-bound floor: buys certifiable adjoint
  regularity for negligible thrust" VERBATIM).
- C48 note :651 ("adjoint cross-lowering gradient floor ~1e-8 rel ...
  FD-amplified ~7 orders; the defect was the mixed-lowering
  formulation") verbatim — §3.4-bis item 2 accurate.
- VERDICT_C9C11_supplement.md: "STANDS, as amended" (:108),
  "front-fitting = the family's alignment content already exact of
  record" (:98/:151), "a weight cannot referee itself" (:184).
- Wave-1 consumption (duty g): VERDICT_wave1 read in full — all four
  rows CONVERGED-ON-PROTOCOL, 0 escalations; the panel's "no
  conditional branches" declaration is CORRECT; §2.3/2.4/§3
  shared-artifact quote correct; §2.1 gates-absolute correct. One
  attribution nit (RC1R-6(ii)). No advocacy source double-counted as
  two independent validations (H-F9's dual use is declared as one
  source; O-F4/O-F8 cited as one advocate family).
- Panel's measured greps RE-RUN, REPRODUCED EXACTLY: cava bilingual
  sweep hit set {32, 106, 392, 456, 457, 475, 594, 861, 1096, 1118};
  `grep -cinE "captur"` = 1 at :1098; literature-registry sweep (the
  WANTED-rider items all absent; the extra hits kraiko_2016_two_sided
  :518, efremov_kraiko_2004_augmentor :527, giles_pierce_2000 :500,
  wanted_lozano_ponsin_2023 :686, wanted_budd_huang_russell_2009 :875
  are other papers — the Huang hit is Huang-Russell MMPDE, not
  Huang-Zahr HOIST, so the panel's absence claims SURVIVE).
- Panel derivations pen-checked: r^2+4r+1=0 has decaying root
  -2+sqrt(3) = -(2-sqrt(3)) — CONFIRMED; the VD/second-difference
  y'' argument is the standard B-spline property, correctly stated
  with its instantiation honestly deferred to duty item (6);
  dimension count heights<->constrained control points consistent
  (N-1 free on both sides given attachment + tangency + natural-end
  constraints); the <= 4-dofs-per-certificate locality claim is the
  cubic local-support fact — sound.

## 2. FINDINGS

### RC1R-1 — REPAIR-NEEDED (row C1): F-C1a cannot discriminate as
### pinned — under the duty's own seed pin it is self-firing

Mechanism. The panel's central structural fact is a LINEAR BIJECTION:
exact conversion reproduces the record curve EXACTLY (§3.1; §3.2
branch A says so verbatim, "including its natural end"). Duty item
(5) pins seeds to EXACT conversion ("GENO heights-seeds convert
exactly"). But the recorded failing family's curve (taper seed /
steep-thB interpolant, GAP-21) IS an alternating-curvature curve —
the oscillation lives in the curve, not in the chart. Therefore a
"converted chart re-run of the recorded failing family" under exact
conversion carries an alternating-curvature statistic IDENTICAL to
the record's, and F-C1a FIRES regardless of the truth of the
structural-removal claim — which is a claim about the chart's
DATA->DOF map for innocent/new data (where the 1-4-1 interpolation
solve is absent), not about exactly-converted record interpolants
(exact conversion IS solving the interpolation system). The only
reading under which F-C1a tests the removal claim — seed the failing
family through the control chart's own data map (control points :=
data, the Schoenberg/variation-diminishing quasi-interpolant the W1
argument actually describes) — is unstated and contradicts item (5)'s
exactness (that map does not interpolate; geometry changes at O(h^2)).
Duty (e): as pinned, the falsifier's outcome cannot refute or confirm
the adopted choice — reading A always fires (false kill of the flip),
reading B is undefined on the page.

Witness. PANEL §4.1 F-C1a + item (5) vs §3.1 W1 ("In the
control-polygon chart THE INTERPOLATION SOLVE DOES NOT EXIST") and
§3.2 branch A ("the converted seed reproduces the record curve
EXACTLY"). Findings :1293 (the mechanism is the interpolation
recurrence's boundary-forced alternating solution — a property of the
MAP + data, carried by the curve it produces).

Repair (outcome stands with it). Pin the conversion map PER USE:
exact bijection for record-continuity and the equivalence gate (items
1/5, F-C1b — correct as written); the mechanism twin F-C1a measured
on the chart's INNOCENT-DATA RESPONSE, e.g. (a) control points taken
directly as the failing family's data (VD map) with the
sign-pattern/amplitude statistic on the resulting curvature, and/or
(b) the mechanism-driver probe: a clamped-end slope-mismatch
perturbation — in the heights chart the curvature response propagates
at ratio -(2-sqrt(3)) per knot (the :1293 form); in the control chart
a single-dof perturbation has <= 4-segment support and VD-bounded
sign changes. State explicitly WHICH object the statistic is computed
on and that the exactly-converted seed is EXPECTED to reproduce the
record statistic (it is the continuity check, not the twin).

### RC1R-2 — REPAIR-NEEDED (row C1): the S18-driver/conditioning
### revalidation leg is missing from the duty — D6's adjudicated axis
### answered only partially on its own terms

Mechanism. The frozen C1 question NAMES driver survival ("the
certified stack (U1 C_geo class, S18 driver, O3.1 identity chain)
survives with a bounded, protocol-pinned revalidation") and §1-bis
axis (1) promises conditioning "named inside C1 W1/W2, adjudicated
there as a basis property". Neither is delivered: W1 adjudicates
oscillation, W2 exactness/structure; the migration duty's
revalidation scope (item 4: "design-side linear-map audit +
constraint layer + seed conversion; solve/march/front layers
untouched") omits the DRIVER layer, and F-C1b is evaluation-level
only (J + gradient mapping at fixed designs). The driver is NOT
chart-invariant: Newton steps are affine-covariant, but trust-region
geometry and the measured-Jacobi preconditioning are chart-DEPENDENT
(H_c = A^T H_x A under the chart map A) — and "measured Jacobi + full
segment Hessian" is precisely the conditioning machinery D6:940 cites
in the incumbent's favor. Duty (h) names this failure mode: the D6
rejection must be weighed, not silently narrowed — the panel weighs
its revalidation HEADLINE (protocol named, W3 gate passed) but drops
its driver-conditioning CONTENT from the named revalidation list.

Witness. D6 :936-940 verbatim (re-read this window); PANEL §1 (C1
main question), §1-bis axis (1), §3.1 W3, §4.1 items (0)-(6) and
F-C1b (no driver item, no walk-level leg).

Repair (outcome stands with it). Add a named driver leg to
F2-C1-CONTROL-CHART-MIGRATION: EITHER derive the chart-map
conditioning bound (the clamped cubic-spline collocation system —
the 1-4-1 family — has an N-independent bounded condition number, de
Boor class; then driver deltas are provably bounded and the claim
ships with its derivation) OR re-baseline the recorded S18/S24 walk
in-chart with step-count / TR-acceptance / measured-curvature stats
inside derived bands; and add the driver (TR geometry + Jacobi
preconditioner) to item (4)'s named revalidation list. Cross-cluster
note for the judge: the engine/curvature rows (C31TRIO: C32/C33)
consume the dof vector this row defines — fresh-FD-per-segment is
chart-robust (recomputed), but any quasi-Newton carry across the
migration boundary is not; the landing should verify composition.

### RC1R-3 — AMENDMENT (row C56): the §3.4-bis mandate claim is
### unverifiable on disk and contradicted by the sibling panel's
### symmetric claim; the declared dedup rule executes with directions
### verified IDENTICAL

Mechanism. The panel asserts "the wave-2 reconcile task prompt of
record directs THIS panel to adjudicate the gradient-role adjoint
realization". No on-disk carrier supports this: the C56 owner field
of record reads "gradient role OWED TO wave-2 C31TRIO (forecast, not
consumed ...)" (choice_ledger.yaml:739, measured this window); the
wave-2 briefs of record assign no C56 row to this cluster
(BRIEF_wave2_panels §C1REP rows = C1/C2/C3/C49; the judge's row
schema = C31/C32/C33/C1/C2/C3/C49/C20/C21 — no C56); and
PANEL_C31TRIO.md §3.1-bis CONSUMES the same debt claiming it as "OWED
TO THIS CLUSTER" (:650-652) with its own proposed C56 delta
(:697-705; machine row "C56-gradient-role" :1039). Two panels each
claiming THE assignment cannot both be right. What saves the outcome:
this panel PRE-DECLARED the dedup rule (one consumption of record;
landing merges; divergence = escalation), and I verified the two
directions are IDENTICAL — both close the gradient role on the
discrete AD-adjoint of the marched scheme (custom_vjp, one-lowering
per C48), both demote the continuous characteristic-native line to
formulation frame + referee, both route re-opening through F11d to
C56. No escalation condition exists.

Amend. Restate the §3.4-bis authority claim at its actual evidence
level (off-disk task-prompt attribution; the on-disk owner text names
C31TRIO; this panel's §3.4-bis = a SECOND, convergent adjudication);
the landing records ONE consumption per the declared rule (merge
texts, verify identity — done here in first instance); the judge
notes C56 sits outside the wave-2 machine-summary row schema and
carries it as the cross-cluster consistency item it is. The
ADJUDICATION CONTENT itself is verified sound (T-LEMB :489, C48 :651,
S-LBML :660, F11d in C11 :250, referee legs = A18/A15 instruments —
all verbatim-checked; the blind tree H-F29 [L901] independently
recommends exactly this split).

### RC1R-4 — AMENDMENT (census, duty g2(a)): the query-protocol table
### is not internally consistent with the citations

Mechanism. Declared included = 24 (Q1..Q8: 3+4+5+2+3+3+1+3). Items
actually carried: (i) MDG-ICE arXiv 2311.00701 [TITLE/ABS] cited in
§2.2 C49 and in the §4.5 rider — Q3 is already saturated by five
other tracking items; no slot returns it. (ii) The ML-chart trio
(Bezier-GAN 2006.12496, diffusion 2507.23443, DCGAN AIAA J) cited in
§2.2 C1 — Q1/Q2/Q8 all saturated; no slot. (iii) Springer Fluid
Dynamics 2025 FFD+BO [TITLE] cited in §2.1 as a NEWEST item of "the
censused corpus" — appears in no §2.2/§2.3 row and no slot. (iv) The
benchmark instance "[ABS, Q2 synthesis]" carries NUMBERS (34% in 177
calls vs 25% in 599) with NO NAMED SOURCE — declared non-driving, but
R5 discipline wants numbers attributable or absent. Also §2.1's
blanket "Every row's census reaches >= 2024 on at least one axis" is
false for C2, whose census is (honestly, by declared valve) zero web
items. And the ML-chart closure leans partly on "none carries a
certificate layer" asserted about [TITLE]-depth items — above held
depth as a per-paper claim.

Amend (no outcome moves — MDG-ICE is upgrade-path census only, the
ML trio is context, Springer is recency-only, the benchmark instance
is pre-quarantined by the panel's own inflation guard): fix the incl.
counts or declare the extra items' provenance (screened-hit carries
declared as such); name the benchmark-instance source or drop its
numbers; restate the ML-chart closure on the structural reason alone
(the chart family carries no exactness mechanism; data-manifold
priors alien to the certificate discipline — depth-free); scope the
>= 2024 sentence to the adjudication-bearing rows (C1/C3/C49), C2
excluded by its declared valve.

### RC1R-5 — AMENDMENT (row C1, duty g2(b)): undeclared mid-
### adjudication refinement of pre-registered criterion W2

Mechanism. Frozen W2 awards the axis for POSSESSING exact sample-free
sufficient certificates with subdivision contraction. Under the
panel's own same-space structural fact, BOTH charts possess them
(per-segment Bernstein conversion exists from any piecewise-cubic
chart) — the panel says so itself ("exactness per se does not
separate the charts") and then decides the axis on a criterion not in
§1: locality/structure of the constraint->dof maps (<= 4 dofs local
vs dense global dependence through the interpolation inverse). The
refinement is technically sound (verified: local support 4 for cubic
B-splines; dense exponentially-decaying inverse with the same root)
and the DIRECTION does not lean on it (W1 alone carries the flip;
materiality is HIGH on W1's axis) — but §0-ter (b) requires criterion
amendments DECLARED with reason, and the refuter is mandated to
attack post-hoc rationalization. Undeclared drift = finding.

Amend. Declare the W2 refinement in §1 (one sentence: "W2 amended
mid-adjudication: exactness is chart-portable within the same spline
space, so the axis separates on certificate STRUCTURE —
locality/conditioning of the constraint->dof maps; reason: the
same-space structural fact discovered in §3.1"), or re-word the W2
outcome as "tie on exactness; separation on structure". Fallback
branch (F-C1c fires -> sampled checks + derived-gap duty) unchanged.

### RC1R-6 — AMENDMENT (mis-cite family; outcomes unmoved; all other
### ~40 anchors verified verbatim-clean)

(i) Cava R14 is at ADVISORY_litreview_confrontation:1142, not :1140
as cited in §2.3 (:1140 is R10's block). Content quoted (Zahr-Persson
2016 fixed geometry, kinematics-spline dofs NOT shape) is R14's,
verbatim-correct.
(ii) §0 attributes "a certificate is not tradeable" to VERDICT_wave1
§2.1 — the phrase lives in VERDICT_wave1 §0.1 quoting tree H-F24
:774-795; §2.1 carries the gates-absolute clause via facet :21 ("The
binary P4 gate stays as the verifier of record in every
formulation"). Substance right, locus wrong.
(iii) §3.2 branch A calls free-end control points "the row's own
third alternative" — it is the row's SECOND listed alternative (C2
:150-152; third OPTION counting the incumbent). §4.2's "the listed
'free-end control points' alternative" is the correct wording.
(iv) CEN-O9 is cited (:415) as census-owned lip-carrier anchor
without its status of record: the census panel records CEN-O9
DISSOLVED by the carrier decision (:357-358, "e -> 0 continuous; no
S-side inball requirement"). The live census anchors for the corner
axis are CEN-O11 (Lambda = lip circles, PINNED per the 2026-08-02
user pins) + diff §4.8 P-c (agenda item). The C3 verdict's
census-owned re-scope SURVIVES on those two; fix the anchor list.
(Minor, same family, no separate finding: U1 C_geo text sits at D6
:927-929, cited as :928-930.)

### RC1R-7 — NOTE (dedup + reconciliation honesty; mostly positive
### record)

(i) The panel's measured greps REPRODUCE EXACTLY in my window
(bilingual hit set, captur=1 at :1098, registry sweep) — SR-12 clean.
(ii) The §0-point-5 dedup grep of findings/claims/choice registries
for the panel's new census objects is not stated in the file; I ran
it (`grep -inE "bernstein|variation.diminish|control.polygon|de
casteljau"` over the three registries): single hit = the C1 row's own
alternative line :139 — dedup CLEAN, nothing re-minted; the panel's
"note candidate, not minted" discipline is honest. State the grep at
the landing for completeness.
(iii) kraiko_2016_two_sided EXISTS in the literature registry (:518)
and is not cross-referenced in §2.3's Kraiko-2016 discussion (the
ratified cava C8 row is the carrier used — legitimate); a one-line
cross-ref completes navigation-first.
(iv) RECONCILIATION-HONESTY DUTY: PASS. All four declared repairs
(R1-R4) are APPLIED in place and tagged; R1's re-measured ledger refs
are ALL CORRECT at my own measurement; R2's tightened diff anchor is
correct (:282-287 = item 12); R3/R4 are genuine post-partial
consumptions (C9/C11/C56 rows verified as described); the kept-§1
anchors verify at source. No repair claimed-but-not-applied, no kept
section that fails verification.

## 3. PER-ROW ATTACK RECORD (duties a-f + h, each row individually)

- **C1**: (a) formalization attacked — frozen statement carries no
  incumbent/challenger smuggle; burden allocation honest (D6
  single-author FOR the incumbent on its axis; no presumption on the
  never-adjudicated axes — matches :1299). Findings RC1R-1/2/5. (c)
  incumbent side: its genuine case is represented (conditioning
  machinery, certified-stack investment, comparison studies not
  crowning control points); the one context the panel under-serves is
  the driver-conditioning content = RC1R-2. (d) challenger side: the
  flip does NOT ride 4/4 convergence-as-verdict — the diff is
  DIVERGENT-ENRICHING (only O-F4 recommends the control chart
  directly) and the panel adjudicates on content (mechanism theorem +
  structure), R-4 clean; the RC911-3 threshold-discipline transplant
  is DECLARED with recalibration. (e) F-C1b/F-C1c are genuine
  falsifiers with derived floors; F-C1a = RC1R-1. (f) no re-mint
  (RC1R-7(ii)); D6/GAP-21 verdicts of record not contradicted —
  narrowings declared.
- **C2**: mandate compliance VERIFIED — adjudicated ONLY as the
  declared conditional; the BC itself untouched (the notaknot twin
  stays a diagnostic of record); branch A resolves to a LISTED
  alternative by structure (bijection verified sound); branch B
  criteria = the frozen §1(ii). GAP-5 numbers verbatim-correct.
  Only the RC1R-6(iii) wording nit. NO independent finding.
- **C3**: challenge honestly weighed — both challengers verified at
  their tree anchors and RE-SCOPED with a stated criterion (neither
  touches the clamped tan(thB) condition — TRUE at source: H-F18 is
  state data at the corner; P-F21 changes Lambda geometry, after
  which tangency is to the lip circle); census-owned items NAMED not
  decided (F2-exit pin respected; the pinned sharp-vs-floor twin
  falsifier is explicitly adoptable ONLY by the census window);
  external materiality scale (KT2015 3.18) verbatim. RC1R-6(iv)
  anchor-status nit only. NO independent finding.
- **C49**: the captured advocate IS at its genuine best (user
  mandate): steelman verified against V-F6/H-F4/H-F8/H-F9/H-F28/
  O-F9/P-F12 at source + production-practice census given its due;
  W1 loses by five STATED reasons all anchor-verified (certificate
  objects need explicit fronts — M0 D2.5/delta_S1 verified; the held
  Giles-Ulbrich pp.907/910 theorem with 1-D-scalar scope DECLARED —
  more honest than the registry row itself; Lozano-shock attribution
  NOT used, cava C10 honored; Q4/Q7 absence query-bounded; wave-1
  C28 gates absolute). Two-tier W2 win is measurement-gated with the
  carrier protocol + F-C49a/b/c/d pinned — all four falsifiers have
  genuine reject arms (F-C49b at two sites RE-OPENS the fitted
  class's sufficiency; F-C49c escalates the build; F-C49a re-opens
  W1 against a real precedent). Tracking = named upgrade path matches
  H-F10(d) verbatim. Build right-sized (explorer NOT scheduled;
  boundary schema rides F2-C11 leg (b) — no duplicate instrument,
  wave-1 composition preserved). NO independent finding.
- **C56**: RC1R-3 (provenance amendment; content verified sound and
  convergent with PANEL_C31TRIO).

## 4. VERBATIM-QUOTE AUDIT DECLARATION

Every quotation the panel marks as verbatim was re-read at its source
in this window; all reproduce exactly EXCEPT the four loci in
RC1R-6 (one line-ref off by 2, one attribution off by section, one
ordinal, one status omission — none changes substance). The panel's
read-depth markers were spot-checked against its claims: no claim
above held depth found except the ML-chart per-paper absence clause
(folded into RC1R-4). The panel's numbers (ratios, percentages,
record values) all trace to committed carriers verified above, except
the un-sourced benchmark instance (RC1R-4(iv), pre-quarantined by the
panel itself).

## 5. DEDUP REGISTER (this refutation vs the record)

- RC1R-1/2 are NEW (no registry row or wave-1 finding covers F-C1a's
  object ambiguity or the driver revalidation leg).
- RC1R-3 overlaps PANEL_C31TRIO's own §3.1-bis claim — by design
  (cross-panel consistency item); the judge consumes ONCE.
- RC1R-4/5/6/7 are file-local census/citation/protocol findings, no
  registry overlap; RC1R-7(ii)'s grep complements (does not repeat)
  the panel's §2.4 measured greps.
- No finding here re-mints GAP-21/GAP-5/Q2-quarantine content — they
  are cited as the carriers they are.

## MACHINE SUMMARY

```json
{
  "cluster": "C1REP",
  "findings": [
    {"id": "RC1R-1", "class": "REPAIR-NEEDED", "row": "C1"},
    {"id": "RC1R-2", "class": "REPAIR-NEEDED", "row": "C1"},
    {"id": "RC1R-3", "class": "AMENDMENT", "row": "C56"},
    {"id": "RC1R-4", "class": "AMENDMENT", "row": "census"},
    {"id": "RC1R-5", "class": "AMENDMENT", "row": "C1"},
    {"id": "RC1R-6", "class": "AMENDMENT", "row": "C1/C2/C3/cross"},
    {"id": "RC1R-7", "class": "NOTE", "row": "cluster"}
  ],
  "breaks": 0,
  "repairs": 2,
  "amendments": 4,
  "notes": 1
}
```
