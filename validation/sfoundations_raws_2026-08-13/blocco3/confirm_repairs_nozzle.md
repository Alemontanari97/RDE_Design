# CONFIRM-ON-REPAIRS — SYNTHESIS_nozzle_rde_arrivals.md revision

Campaign: S-FOUNDATIONS-C4 (2026-08-20), always-on confirm stage of
record. INPUTS READ THIS WINDOW [FULL]:
REFUTE_nozzle_rde_synthesis.md (all 9 findings NRS-1..NRS-9 + machine
summary) + revised SYNTHESIS_nozzle_rde_arrivals.md (all 649 lines,
including the (h) revision disposition table). Instruments run THIS
window (SR-12): (1) residual-phrase grep on the synthesis
(`FALSE-READING|counter-datum|eq\. 30|MERIDIONAL flow-angle|
preserves in-band|8 axes`); (2) NRS-4 dedup grep RE-EXECUTED by me on
docs/literature_registry.yaml (`yao|braun|multiple ignitions|
1125-1142|927-936`, case-insensitive); (3) full [REV-NRS] marker
enumeration (grep -n -o, 26 body markers + table); (4) git check on
the synthesis path (file UNTRACKED — no committed v1 exists, so the
no-unordered-edit check is marker-diff + residual-grep, declared).
PDF pages: NOT re-rendered — no verdict below required one (every
doubt resolved at grep/marker level; paper-truth was the refuter's
burden and its [VER] anchors are not re-litigated here).

==============================================================================
## PER-FINDING VERDICTS

### NRS-1 [REPAIR] CT-3 truncated-quote basis — VERDICT: FAITHFUL
All four ordered sub-items applied:
(i) CT-3 basis re-stated as ENRICHMENT of an accurate sentence — CT-3
    disposition (:118-132) opens "The addendum-(c) sentence of record
    is STRICTLY TRUE as written" with the restrictive clause quoted as
    part of the sentence, exactly the refuter's ground.
(ii) "FALSE-READING" and the else-read-as-false framing STRUCK:
    residual grep hits ONLY the (h) table row quoting the struck text
    (:636) — zero body occurrences.
(iii) (e).6 landing text kept AS the enrichment (:477-492): nearest
    referees P-B Fig. 15 / P-C Figs. 13+20b named with BOTH stated
    disqualifiers (companion globally averaged, not per-phase; truth
    same-family URANS, not external/experimental); "The sentence needs
    no defensive rewrite" stated; G-10 (:274-276) says "record
    sentence itself unchanged — it is true as written".
(iv) CT-3 regraded "FIRES AS ENRICHMENT-AMENDMENT (narrative
    completeness), the campaign's only firing item" (:118-119); TALLY
    (:181-184) and machine summary (:622) carry the same grade.
Sites tagged [REV-NRS-1] at :118, :183, :274, :477, :622, :625 —
matches the table's declared site list exactly.

### NRS-2 [REPAIR] RMSD_theta guard — VERDICT: FAITHFUL
All three ordered statements land, and the two defective anchors are
gone:
(i) P-C eq. 20 = total-flow-angle RMS deviation from axial,
    upper-proxy defensible: G-04 (:227-228 "[VER-class]") and (e).4
    (:426-430).
(ii) P-B prints NO RMSD formula — definition UNDER-DETERMINED, with
    the eq.-20-type form explicitly "[INF], not evidence": (a) P-B
    cell (:38), G-04 (:229-231), (e).4 (:430-433).
(iii) Operative rule unconditional — "RMSD_theta from EITHER paper is
    NEVER booked against B5 in ANY direction (neither as swirl value
    nor as certified upper bound)": G-04 (:231-234) and (e).4
    (:433-436); "the only swirl datum is V_cir" stated in both.
Removals grep-proven: "MERIDIONAL flow-angle" and "eq. 30" occur only
inside the (h) table row describing their removal (:637); the (a) P-B
cell no longer carries the meridional over-assertion. Markers at :38,
:227, :426, :625 — matches declared sites ((a) P-B cell, G-04, (e).4,
machine summary).

### NRS-3 [AMENDMENT] film-cooling caveat + band wording — VERDICT: FAITHFUL
(i) FILM-COOLING CAVEAT: G-04 (:220-225) carries the ordered cell-note
    scope VERBATIM — "film-cooled configs, cooling-modulated, no clean
    baseline" — plus no-uncooled-baseline and cooling-modulates-swirl
    facts; (a) P-B cell (:38) and (e).4 (:440-443) scoped the same
    way; "valid magnitude-class existence datum [ADV]" retained as
    ordered.
(ii) BAND WORDING: "swirl angle OVERLAPPING B5's (10-14 deg) LOW EDGE,
    not inside it (9.7 sits below the floor)" in G-04 (:218-219),
    (e).4 (:443-445), and the machine-summary "first external swirl
    datum" line (:625) — both ordered edits ride that line. The
    eps_theta 0.17-0.20 INSIDE-DISPATCH-band statement stands, exactly
    as the refuter said it could. Markers at :38, :219, :220, :440,
    :625 — matches declared sites.

### NRS-4 [AMENDMENT] WANTED-list dropped entries — VERDICT: FAITHFUL
Both entries RESTORED to Tier-3 (:599-605) with exact identities:
S. Yao, J. Wang 2016, Appl. Therm. Eng. 108:927-936 (P-B[47], wave
stability) and F.K. Lu, E.M. Braun 2014, JPP 30(5):1125-1142
(P-B[12], CTX-grade review) — the refuter's "either add to Tier-3 or
declare exclusion" satisfied on the ADD branch, with the restoration
declared in place ("silently dropped in v1"). Counts propagated:
COUNT line "17 Tier-3 context items (15 + 2 restored)" (:607-608),
machine summary "17 Tier-3 (2 restored)" (:626); my independent count
of the Tier-3 enumeration = 17. DEDUP GREP RE-RUN VERIFIED: I
re-executed the registry grep MYSELF this window
(`yao|braun|multiple ignitions|1125-1142|927-936` on
docs/literature_registry.yaml) — single hit :624 = the Ornano-Braun
2017 row (distinct identity), NEITHER restored item registry-held.
The synthesis's revision-window grep claim (:603-605, table :639) is
therefore independently CONFIRMED, not merely asserted. Markers at
:605, :608, :626 — matches declared sites.

### NRS-5 [AMENDMENT] orphan-check overstated — VERDICT: FAITHFUL
Both dropped landing targets restored in exactly the refuter's
proposed form:
(i) M0 T-T3-MAP cross-cite (P-B F-01 arc) = LANDING RIDER on G-05
    (:248-251) — the "folded into G-05's landing note" option;
(ii) FIELD_ATLAS_targeted_c4.md candidate rows (P-A figure set + P-D
    Fig. 9) = ATLAS RIDER on G-01 (:201-204).
ORPHAN CHECK sentence rewritten (:308-314) to name both restorations
and the v1 undeclared drops — the strict-reading falsity the refuter
caught is cured by restoration, not by wording games. Markers at
:201, :248, :308, :623 — matches declared sites.

### NRS-6 [NOTE] V_cir hole-type erratum quarantine — VERDICT: FAITHFUL
ERRATUM QUARANTINE attached to the G-15 P-B registry-row mint
(:295-301): slot F-22's wrong "(1.5/1.0, 150)" named, paper's correct
"a/b = 1.0/1.5 and V_sec = 150 m/s" (p. 18) carried, downstream
re-entry block stated. Synthesis body untouched numerically (carries
only the range), exactly as the refuter prescribed ("no synthesis
edit is needed" beyond the riding note). Marker at :296 — matches.

### NRS-7 [NOTE] axis-count slip — VERDICT: FAITHFUL
Machine summary now "(a) 7 axes x 4 papers" (:621). Independently
recounted this window: the (a) matrix has exactly 7 axis rows (design
method / averaging stance / truncation / choking / loss decomposition
/ deflection-swirl / pressure-gain). "8 axes" survives only in the
(h) table row describing the fix (:642). Marker at :621 — matches.

### NRS-8 [NOTE] CT-5 counter-datum overweight — VERDICT: FAITHFUL
CT-5 last sentence re-scoped (:152-156): "counter-datum FOR the pin"
DROPPED (grep: zero body occurrences; only the (h) table row :643),
replaced by "Realization instance (CFD class only)... evidences
CFD-realizability of the pure-periodic pin class (G-02's phrasing),
NOT hardware realism, which is CT-5's actual concern (R20 residue)" —
aligned to G-02 exactly as ordered. Marker at :152 — matches.

### NRS-9 [NOTE] G-05 ranking-preservation softening — VERDICT: FAITHFUL
(with one bookkeeping note, below)
G-05 P-B F-11 line softened to the ordered wording class (:243-244):
"the steady design still selects a NEAR-OPTIMAL in-band truncation —
coincidence of in-band argmax is at/below Fig. 15 resolution".
"preserves in-band" grep: zero body occurrences (only table :644).
BOOKKEEPING NOTE (named, not a deviation of substance): the refuter
ordered the softening "in G-05 and in the (e).1 provenance echo"; the
(h) table's Sites column lists G-05 only and (e).1 carries no
[REV-NRS-9] marker. Grep proves the OUTCOME is satisfied file-wide:
(e).1's P-B line reads "transient-only +0.52% invisible on the flat
steady curve" (:382-383) — already the soft form, with no
ranking-preservation claim anywhere in (e).1. With v1 unavailable
(file untracked), I cannot distinguish "echo was already soft in v1"
from "echo edited without marker"; either way no offending phrasing
survives and the operative repair is complete. Flagged for the
orchestrator's marker-discipline awareness only.

==============================================================================
## UNORDERED-EDIT CHECK (diff-mindset over the markers)

- Marker census (grep -o, this window): 26 [REV-NRS] tags in the body
  at :38(x2), :118, :152, :183, :201, :219, :220, :227, :244, :248,
  :274, :296, :308, :426, :440, :477, :605, :608, :621, :622, :623,
  :625(x3), :626 — EVERY marker lands inside a site declared in the
  (h) disposition table for its finding; NO marker in an undeclared
  location; no finding's declared site lacks its marker.
- Residual grep clean: all six struck/replaced phrases occur ONLY
  inside the (h) table rows that quote them as struck.
- Consistency of untouched content spot-checked: TALLY arithmetic
  8 = 1 ENRICHMENT-AMENDMENT + 1 rider + 6 do-not-fire holds; WANTED
  W-01..W-30 tiering (8+22) unchanged; the (a) P-C cell, CT-1/CT-2/
  CT-4/CT-6/CT-7/CT-8, D-1..D-8, (e).2/(e).3/(e).5 carry no revision
  and match the v1 content the refuter's baseline described as held.
- (h) table itself is append-only and accurately describes what was
  done (verified row-by-row above).
CATCHES: NONE. No unordered edit found. The only imperfection is the
NRS-9 (e).1 site-accounting note above, which does not alter content.

==============================================================================
## MACHINE SUMMARY
role: confirm-on-repairs, slot-6 refuter findings vs revised synthesis
verdicts: NRS-1 FAITHFUL, NRS-2 FAITHFUL, NRS-3 FAITHFUL, NRS-4
  FAITHFUL (dedup grep independently re-executed and confirmed), NRS-5
  FAITHFUL, NRS-6 FAITHFUL, NRS-7 FAITHFUL, NRS-8 FAITHFUL, NRS-9
  FAITHFUL (bookkeeping note: (e).1 echo site unlisted in table;
  outcome grep-proven satisfied)
skipped_findings: 0 of 9
unordered_edits: 0 (marker census + residual grep + spot-checks)
overall: revision ACCEPTED as verbatim-faithful to the refuter's
  finding text; synthesis fit for downstream consumption
output_file: validation/sfoundations_raws_2026-08-13/blocco3/confirm_repairs_nozzle.md
