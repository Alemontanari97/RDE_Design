# BRIEF — BLOCCO 3 WAVE 2 REFUTERS + JUDGE (delta over wave 1)

## REFUTERS
`BASE/blocco3/BRIEF_wave1_refuter.md` applies VERBATIM with these
substitutions (BASE = validation/sfoundations_raws_2026-08-13):
- Clusters: C31TRIO | C1REP | C2021 (your cluster in your task
  prompt). Panel file: `BASE/blocco3/PANEL_<cluster>.md`; your output:
  `BASE/blocco3/refute_<cluster>.md`.
- Finding ID prefixes: RC31T- / RC1R- / RC2021-.
- Panel mandate file: `BASE/blocco3/BRIEF_wave2_panels.md` (frame =
  wave-1 §0 by reference; attack compliance with BOTH).
- EXTRA DUTY (g) WAVE-1 CONSUMPTION: check the panel consumed
  `BASE/blocco3/VERDICT_wave1.md` correctly — dependency claimed but
  outcome misquoted, escalation-conditionality not declared, or the
  same advocacy source double-counted as two independent validations
  (the O-F20 C28/C20 case) = findings.
- EXTRA DUTY (g2) §0-ter ENFORCEMENT (world-class census protocol):
  query-protocol table present and internally consistent (counts vs
  citations); DECISION CRITERIA pre-registered in §1 and the
  adjudication actually decided AGAINST them (post-hoc criterion
  drift undeclared = finding); materiality estimates honest in BOTH
  directions (a material fork waved off as immaterial AND an
  immaterial fork given ritual depth are both findings); steelman
  check (rejected options at their best modern instance); read-depth
  markers present and claims within depth; axis-bearing sentences
  per-row present and correct.
- EXTRA DUTY (h) per-cluster:
  * C31TRIO: the route facts (custom_vjp/jacfwd, one-lowering
    discipline) are of RECORD — a panel proposal that contradicts
    them without declaring a route change = finding. Attack any
    re-measurement of already-recorded speed numbers.
  * C1REP: the incumbent's D6 S20 rejection (D6:915-926) must be
    weighed, not inherited and not silently dropped — both failure
    modes are findings. C2 must NOT be adjudicated beyond its
    conditionality structure. C3/C49: census-owned items (cone/lip
    pins) must be NAMED not decided; the captured-only advocate must
    be at its genuine best (strawman = finding; the user mandate).
  * C2021: qualification-vs-representation conflation (C20 vs C28)
    = finding; alternatives dismissed on cost without a cited cost
    basis = finding.

## JUDGE
`BASE/blocco3/BRIEF_wave1_judge.md` protocol applies VERBATIM with:
- Inputs: PANEL_/refute_ files for C31TRIO, C1REP, C2021 (+ their
  briefs) and `BASE/blocco3/VERDICT_wave1.md` (for dependency
  consistency).
- Output: `BASE/blocco3/VERDICT_wave2.md`.
- Per-row verdicts for: C31(IP half), C32, C33, C1, C2(conditional
  structure only), C3(challenge outcome), C49, C20, C21.
- Cross-cluster consistency paragraphs: (1) engine-vs-representation
  (C31 consumes C28 — conditionality honest?); (2) basis-vs-solution
  representation (C1 exactness axis vs C49 certificate bearer — the
  two "representation" verdicts must compose into ONE coherent
  optimizer object story); (3) qualification stack (C20/C21 vs C28
  price semantics — no double-counted advocacy).
- Same escalation rule, same machine-summary schema (rows keyed
  C31/C32/C33/C1/C2/C3/C49/C20/C21), same
  proposed-ledger-deltas-as-proposal-blocks (no ledger edits).

## CONFIRM-ON-REPAIRS (new stage, adopted 2026-08-19 on the doc1
## evidence: repaired text is the record's most defect-prone class —
## R9-B/R9-C minted by rev-9's own restatement, R10-1/-3 by rev-10's)
If the wave judge sustains REPAIRS > 0, ONE dedicated confirming lens
runs after the judge, scoped to ONLY the judge-repaired clauses (the
verdict's adopted repair texts and their carriers), PLUS —
retroactively, first instance only — the 11 wave-1 repaired clauses
of VERDICT_wave1.md §1. Duties: verify each adopted repair text
verbatim against its consumers; hunt defects MINTED BY the repairs
themselves (the restatement class); per-lens IDs CR-W<n>-*. Dry =
zero sustained breaks/repairs ON THE REPAIRS; sustained amendments
carried to the landing as usual. Output:
`BASE/blocco3/confirm_repairs_wave<n>.md` with machine summary. A
sustained break/repair here re-opens ONLY the affected row (judge
adjudicates; escalation rule applies to it).
SUNSET CLAUSE (anti-over-engineering, priced at adoption): this stage
is ALWAYS-ON for its first two instances (wave 2 incl. retro wave-1;
wave 3). If across BOTH instances it sustains ZERO breaks/repairs on
repaired text, it DEMOTES to escalation-triggered only (runs only
when a judge sustains repairs above a to-be-measured count) — the
demotion is automatic at the wave-3 landing, declared in the log. If
it catches even one real defect, it stays and has paid for itself.
Instruments earn their keep by measurement or they die.
