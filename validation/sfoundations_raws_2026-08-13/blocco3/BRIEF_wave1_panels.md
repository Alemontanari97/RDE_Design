# BRIEF — BLOCCO 3 WAVE 1 PANELS (Phase C adjudications, right-sized v2)
# S-FOUNDATIONS-C2, 2026-08-19. Clusters: C28 (solo) / C27 (solo) /
# C9+C11 (mesh/estimator pair). One panel agent per cluster; your
# cluster is named in your task prompt. Read §0 (frame) + YOUR cluster
# section. Paths relative to repo root; BASE =
# validation/sfoundations_raws_2026-08-13.

## §0 FRAME (binding machine rules, all panels)

MISSION: adjudicate the cluster's choice-ledger row(s) TO CONVERGENCE
against the SOTA alternative set, WITHOUT incumbent bias (standing
directive choice-adjudication-convergence, points 6-7 baked in below).
This wave's verdicts feed the F2 general-engine build: an F2-consumed
choice left NEVER blocks F2.

METHOD (in this order, per cluster):
1. FORMALIZE FIRST: write the cluster's shared sub-problem as a FROZEN
   formal statement (objects, hypotheses, the quantity being
   optimized/bounded, the operational context: certified marched
   axisymmetric Euler solver with fitted fronts, JAX/adjoint stack,
   certificate/verdict discipline). Per-row questions inside the
   cluster stay individuated. The literature query DERIVES from this
   statement — never from a topic label.
2. SOTA CENSUS (after formalization): census the open literature for
   THIS formalized sub-problem. MUST reach the MODERN refined
   literature (recent methods, active lines — not classics + textbook
   names only); REPORT the recency of the covered corpus (year
   histogram or explicit span + newest items). Use WebSearch/WebFetch
   (load via ToolSearch). Include the RATIFIED litreview cava:
   `validation/ADVISORY_litreview_confrontation_2026-08-13.md` — cite
   its rows PER-ROW (row IDs), never wholesale. ZERO INFLATION: no
   alternative described stronger than its literature support; no
   "SOTA" label without the census behind it; verdicts written at the
   evidence level actually held. An option loses only by STATED
   REASON, never by omission.
3. ADVOCACY = THE BLIND TREES (cite-not-regenerate): the four Phase-A
   de-novo trees are the alternative-advocates, blind and already
   paid for. Cite their positions by file:line (full trees
   BASE/phaseA_tree_{variational,hyperbolic,optimization,propulsion}.md;
   line-ref maps in the *_CONDENSED.md files). Do NOT regenerate
   advocacy; do NOT strawman the incumbent — represent its genuine
   case too (it may win).
4. PER-ROW ADJUDICATION: incumbent vs each alternative, per ledger
   row. Propose the row outcome with reasons: CONVERGED-panel
   (direction + adopted choice), or measurement-gated SPLIT: converge
   on PROTOCOL + FALSIFIER pinned NOW (exact experiment, metric,
   derived threshold, what refutes what), measured half = BINDING F2
   duty (name the duty). Where the trees challenge the incumbent 4/4,
   the incumbent carries the burden of proof — but 4/4 blind
   convergence is evidence, not a verdict; adjudicate on content.
5. DEDUP (navigation-first): before minting/claiming anything, grep
   docs/findings_registry.yaml, docs/choice_ledger.yaml,
   docs/literature_registry.yaml, docs/claims_registry.yaml. Cite
   existing rows; absence claims only search-proven (state the grep).

CONTEXT OF RECORD (cite, do not re-litigate):
- Phase-B diff verdicts: BASE/phaseB_tree_diff.md (§1 per-row, §3
  theory validations, §5 arithmetic). The wave-1 rows' diff anchors
  are in the cluster sections below.
- Seed layer: certified both directions for the current Fable-refuter
  pool (SEED_PROTOCOL_v3 PASS 2026-08-19; docs/seed_outcome_registry.md).
  Pool unchanged in this wave => NO re-seed slot runs.
- Verdicts of record are re-verifiable but not re-litigated:
  VERDICT_r2pass, VERDICT_escalation, VERDICT_confirm,
  VERDICT_doc1_closure (all under BASE/r2pass/).

OUTPUT: write exactly one file,
`BASE/blocco3/PANEL_<cluster>.md` (<cluster> = C28 | C27 | C9C11),
sections: {1 FROZEN FORMAL STATEMENT / 2 SOTA CENSUS (query trail,
corpus recency, per-source one-liners, cava rows cited per-row) /
3 ADJUDICATION (per-row: incumbent case, each alternative's case with
tree file:line advocacy + census support, stated-reason outcomes) /
4 PROPOSED VERDICTS + DUTIES (per-row proposed ledger outcome +
measurement-gated protocol/falsifier pins + F2 duty names) /
5 MACHINE SUMMARY}. Machine summary schema:
{cluster, rows: {<Cn>: {proposed: str, gated: bool, duty: str|null}},
census_recency: str, alternatives_closed: N, inflation_check: done}

RULES: R-4 anti-transplant (recalibrate any carried phrase); R5
honesty (no numbers without committed source; tolerances derived);
null=failure (your file must exist, be well-formed, end with the
machine summary); env PINNED (no installs, no pip); GENO/ read-only;
no git commands; edit NOTHING outside BASE/blocco3/.

## §C28 — CERT-FRONTIER REPRESENTATION (solo panel, HIGHEST SIGNAL)

Row: docs/choice_ledger.yaml:419-427. Incumbent: "binary outside gates
only (quantifier bridge falsified)". Listed alternative: "KS-max
surrogate inside KKT, gates kept". Status NEVER; owner F2 (census row
10); the falsified K_disc~A_0 bridge (S22) left no successor.

Diff anchor: BASE/phaseB_tree_diff.md:143-155 (+ §3.7 :347-350):
4/4 CONVERGENT — certification conditions as FIRST-CLASS priced
constraints with margins and multipliers. Tree advocacy to cite:
V-F32 ("most distinctive structural choice"), H-F24 (certified-class
boundary in the active set; multiplier PRICES certifiability), O-F20
(graded violation + per-sector solvability regions, solvability-first
doctrine — note it is ALSO advocacy for C20, wave 2; cite only its
C28-relevant half), P-F33 (gradients of certifiability or the loop
"grinds or lies").

M0 STATE (the ledger LAGS M0 — this is the crux): the S20
formalization already in M0 (docs/rde_nozzle_MASTER.md ~l.2050-2360;
grep 'certifiability' + 'KKT'; esp. l.2127 KKT-with-margin-multiplier
as the optimality statement at boundary-active optima, l.2167
taxonomy, l.2332 S20-instance) — the tier ladder + KKT with the
margin multiplier. S20/S22/S24 evidence of record: certdiag 8/8
genuine crawl on the certifiability frontier; K_disc~A_0 bridge
FALSIFIED (S22); three margin-inactive/cert-limited campaign instances
(F4-branch class). Also read the C28-relevant cava rows (grep the cava
for 'C28', 'GAP-1', 'certifiab').

QUESTION TO FREEZE: how is the certifiability frontier REPRESENTED in
the production optimization loop — (a) binary gates outside the
smooth problem (incumbent), (b) priced constraint inside KKT (margin
variable + multiplier; which surrogate: KS-max / graded violation /
exact-penalty; M0's formalization), (c) solvability-region constraints
derived a priori (O-F20), (d) hybrid gate+price tiers (M0 tier
ladder)? Census axes: nonsmooth/MPCC and semi-infinite treatment,
surrogate exactness/conservativeness, multiplier semantics
(marginal price of certifiability), production-loop cost.
NOTE the ledger's own C-ledger row "TR-SQP-with-P4-gate vs
nonsmooth/MPCC handling of the certifiability frontier" (seeded S24)
— this panel is where that seeded question gets its adjudication half;
dedup, cite, don't re-mint.

Measurement-gating expectation: the representation choice has a
build+measure half (F2). Converge on the formal representation +
protocol/falsifier pins; name the F2 duty precisely.

## §C27 — CONSTRAINT AGGREGATION (solo panel)

Row: docs/choice_ledger.yaml:407-417. Incumbent: KS-min,
rho = K_RICH ln(N)/mu0 (conservativeness-derived). Listed
alternatives: adaptive-KS (Poon-Martins), induced-exp/power
(Kennedy-Hicken), two-constant rule. Status SINGLE-AUTHOR (survey
answered accuracy axis; CONDITIONING axis NEVER); owner F2 (GAP-1,
GAP-27 on-axis).

Diff anchor: BASE/phaseB_tree_diff.md:133-142 — DIVERGENT HIGH 3/4.
Tree advocacy to cite: O-F18 (EXCHANGE method on the true 1-D inner
problem for exactness; KS only within the working set; inter-node
certification via measured Lipschitz margin; exchange multipliers ≈
the measure-valued SIP multiplier = the (ii) marginal-value
semantics), V-F23 (exchange + covering, margin as DERIVED safety
factor), P-F16 (adaptive phase refinement near convergence +
MANDATORY post-hoc fine-phase sweep falsifier: a violating phase
between nodes VOIDS the certificate).

QUESTION TO FREEZE: aggregation of the per-phase (semi-infinite in
xi) constraint family into the optimizer — smooth aggregate (which:
KS-min incumbent / adaptive-KS / induced-exp/power) vs exchange/
working-set on the true inner problem vs hybrid (exchange outer + KS
within working set). Axes: CONDITIONING (never adjudicated — the
named gap), exactness/certificate semantics (post-hoc sweep
falsifier), multiplier semantics vs the mu-averaged (ii) theory,
cost in the JAX/adjoint stack.
INTERPLAY, not adjudicated here: K_RICH reuse (C42, wave 3) and GAP-1
constants — NAME the dependency, do not decide C42.

Measurement-gating expectation: conditioning axis = measured
microbenchmark protocol (pin metric + threshold + falsifier); the
measured half = F2 duty.

## §C9C11 — MARCH MESH LAW + ERROR ESTIMATOR FOR J (cluster panel,
## per-row verdicts)

Rows: docs/choice_ledger.yaml:217-226 (C9: march mesh law; incumbent
uniform NI/da/Nw/Ne per run; alternatives column-local C- insertion,
goal-oriented AMR Venditti-Darmofal; NEVER) and :238-248 (C11: error
estimator for J; incumbent two-level Richardson K=4; alternatives
three-mesh observed-p GCI Roache/Celik, DWR Becker-Rannacher, LSQ
multigrid; SINGLE-AUTHOR at band sites).

Diff anchors: BASE/phaseB_tree_diff.md:66-72 (C9: DIVERGENT HIGH,
4/4 AGAINST the uniform incumbent — goal-oriented/DWR adaptivity as
production policy with measured effectivity and derived safety
factors; V-F28, H-F26/27, O-F24, P-F31) and :76-81 (C11: DIVERGENT
HIGH 4/4 — DWR PRIMARY, Richardson demoted to referee/cross-check at
checkpoints; INVERTED burden of proof). Cite the same fork blocks
once each where shared.

CAVA rows to consume per-row (now RATIFIED): grep the cava for 'A36'
(singularity-aware derived spacing law: log-weighted DWR
equidistribution near the sonic strip) and '3.25' (DWR bars near the
sonic throat — validity-DOMAIN caveat for DWR: cite it as a
CONSTRAINT on the DWR alternative, per the C11 ledger note).

QUESTION TO FREEZE (shared sub-problem): a-posteriori error control
of a scalar objective J (thrust-class functional) computed by a
MARCHED hyperbolic solve with fitted fronts + its mesh law
(streamwise columns + wall/exit refinement), adjoint available
(JAX), certificate discipline (bands must be DERIVED, rejectors must
fire). Per-row: C9 = the mesh LAW (uniform-per-run vs goal-oriented
adapted, incl. the derived spacing-law middle road from A36); C11 =
the ESTIMATOR (Richardson-primary vs DWR-primary with Richardson
referee; GCI; LSQ-multigrid). Axes: effectivity (measured), cost per
certified run, interaction with the fitted-front stencils and the
sonic strip (A36/3.25 caveats), K_RICH dependence (C42, name-only).
BURDEN: for C11 the diff inverts it — the incumbent defends.

Measurement-gating expectation: BOTH rows land measurement-gated
(DWR build + effectivity campaign = F2). Converge on protocol +
falsifier pins (effectivity index target band, derived thresholds,
what result refutes which choice) + the duty names.
