# BRIEF — BLOCCO 3 WAVE 1 REFUTERS (one fused refuter per cluster,
# attacks PER-ROW)

ROLE: adversarial refuter over ONE wave-1 panel (your cluster is in
your task prompt: C28 | C27 | C9C11). You WRITE exactly one file:
`validation/sfoundations_raws_2026-08-13/blocco3/refute_<cluster>.md`.
Edit NOTHING else. Paths relative to repo root; BASE =
validation/sfoundations_raws_2026-08-13.

NULL=FAILURE: if `BASE/blocco3/PANEL_<cluster>.md` is missing or
malformed, write your file declaring NULL-FAILURE (what is missing)
and stop — never dry-count a dead slot.

## INPUTS
1. `BASE/blocco3/PANEL_<cluster>.md` IN FULL.
2. `BASE/blocco3/BRIEF_wave1_panels.md` §0 + your cluster's section
   (the panel's mandate — attack compliance too).
3. The panel's cited anchors AT THE SOURCE: ledger rows, diff rows,
   tree file:line advocacy, cava rows, M0 sites. Spot-check quotes
   VERBATIM; a mis-cite is a finding.

## ATTACK DUTIES — PER-ROW, both directions
(a) FORMALIZATION: is the frozen statement the RIGHT sub-problem?
    Does it smuggle the incumbent (or the challenger) into the
    formulation? Is anything material left out of the objects/
    hypotheses (fitted fronts, sonic strip, adjoint availability,
    certificate discipline)?
(b) CENSUS: completeness vs the formalized statement (name a missing
    modern line if you can find one — you may use WebSearch via
    ToolSearch); recency claim honest (check the reported span against
    the items actually cited); INFLATION HUNT: any alternative or
    incumbent described stronger than its cited support; absence
    claims not search-proven.
(c) ADJUDICATION, INCUMBENT SIDE: attack the case FOR the proposed
    winner — is the stated reason actually load-bearing? Is there a
    context where the incumbent is right that the panel ignored
    (cost, certificate semantics, the sonic-strip caveat, the
    marched/fitted structure)?
(d) ADJUDICATION, CHALLENGER SIDE: attack the case AGAINST the
    incumbent — is the 4/4 (or 3/4) tree convergence being used as a
    verdict instead of evidence? Are tree positions transplanted
    without recalibration (R-4)?
(e) MEASUREMENT-GATED PINS: is the pinned protocol actually a
    FALSIFIER (can its outcome refute the adopted choice, or only
    confirm)? Are thresholds DERIVED or magic? Is the F2 duty named
    precisely enough to be binding?
(f) DEDUP/RECORD: does the panel re-mint anything a registry row
    already carries? Does it contradict a verdict of record without
    declaring it?

## RULES
- Findings IDs: RC28-1.., RC27-1.., RC911-1.. (your cluster's prefix).
- Class each: BREAKS-VERDICT (the proposed row outcome cannot stand) /
  REPAIR-NEEDED (outcome stands only after a named repair) /
  AMENDMENT (text/scope fix, outcome unmoved) / NOTE.
  Mechanism + witness/derivation for the first two.
- Attack EVERY row of the cluster individually (C9C11: separate
  findings per row).
- End with: verbatim-quote audit declaration, dedup register, and
  MACHINE SUMMARY: {cluster, findings: [{id, class, row}],
  breaks: N, repairs: N, amendments: N, notes: N}.
- Env pinned (no installs); GENO/ read-only; no git; write only your
  file.
