# BRIEF — BLOCCO 3 WAVE 1 JUDGE (one judge over the wave's clusters)

ROLE: wave judge of record for wave 1 (clusters C28, C27, C9+C11 —
they share objects: KKT/multiplier semantics across C28/C27,
DWR/adjoint machinery across C9/C11/C27, K_RICH dependency across
C27/C11). You WRITE exactly one file:
`validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md`.
Edit NOTHING else (ledger edits happen at the landing window, not
here). BASE = validation/sfoundations_raws_2026-08-13.

## INPUTS (read IN FULL — triage on FILE)
1. The three panels: BASE/blocco3/PANEL_C28.md, PANEL_C27.md,
   PANEL_C9C11.md.
2. The three refutations: refute_C28.md, refute_C27.md,
   refute_C9C11.md.
3. BASE/blocco3/BRIEF_wave1_panels.md (the mandate they ran under).
4. Spot-check load-bearing citations at the source (ledger rows, diff
   rows, tree lines, M0 sites, cava rows) — every quote you rely on,
   verify VERBATIM in this window.

## PROTOCOL
- NULL=FAILURE CHECK: all six files exist, well-formed, machine
  summaries present. A dead slot = that cluster is NOT-ADJUDICABLE
  this wave (name it; do not dry-count).
- Adjudicate EVERY refuter finding: SUSTAINED (class) / OVERRULED
  (reason) / RECLASSED. Re-derive divergences yourself at pen grade;
  re-check numeric witnesses.
- PER-ROW VERDICTS (the deliverable): for each of C28, C27, C9, C11
  state the adjudicated outcome at the evidence level actually held:
  * CONVERGED-panel: adopted choice + direction + the stated reasons
    that survived refutation;
  * measurement-gated CONVERGED-ON-PROTOCOL: the pinned protocol +
    falsifier (quote the pins) + the BINDING F2 duty name;
  * or NOT-CONVERGED with the named residue.
  Wording at R5 honesty; zero inflation; cite anchors per-row.
- ESCALATION RULE (binding, the right-sizing safety valve): ANY
  refuter objection you SUSTAIN as BREAKS-VERDICT or REPAIR-NEEDED
  that the panel cannot be repaired-by-amendment on the page =>
  that row AUTO-ESCALATES to full Form-2 (dedicated panel + until-dry
  loop). You do NOT run the escalation: NAME it precisely (row, the
  surviving objection, what the full form must settle). Sustained
  AMENDMENTS: adjudicate the repair text and CARRY it (applied at the
  landing window; no extra round).
- PROPOSED LEDGER DELTAS: for each row, the exact new ledger text
  (status, note additions, duty rows) as a PROPOSAL block for the
  landing window — you do not edit the ledger.
- Cross-cluster consistency: multiplier semantics (C28 price vs C27
  exchange/SIP multipliers vs the (ii) marginal-value theory) and the
  estimator/mesh duties (C9/C11 protocols must compose, not
  duplicate) — one paragraph each.

END with MACHINE SUMMARY:
{rows: {C28: {verdict, gated, escalation: null|str},
        C27: {...}, C9: {...}, C11: {...}},
 sustained: {breaks: N, repairs: N, amendments: N},
 escalations: [rows], amendments_carried: [ids],
 ledger_deltas_proposed: N,
 file: validation/sfoundations_raws_2026-08-13/blocco3/VERDICT_wave1.md}

Falsifiers section for this verdict (what evidence would overturn
each per-row verdict). Env pinned; GENO/ read-only; no git; write
only your file.
