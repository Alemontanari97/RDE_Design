# BRIEF ADDENDUM — WAVE-3 DEPTH UPGRADES (user order 2026-08-20:
# "più profondità su C50 e in generale sui punti critici")
# S-FOUNDATIONS-C3. BASE = validation/sfoundations_raws_2026-08-13.
# NEW FILE by design: the in-flight wave-3 workflow reads its
# original briefs — those are FROZEN; this addendum governs the
# POST-workflow depth chain only. Nothing here re-opens a
# certificate; everything here ADDS verification depth.

## D1 — C50 FULL FORM-2 ESCALATION (structural, user-ordered — runs
## regardless of whether the wave judge sustains objections on C50)
Sequencing: AFTER VERDICT_wave3.md exists (consumes it; never
concurrent with the wave judge).
Slots (3):
(1) ADVOCATE-PDE: live steelman of the L1(A) acceptance-metric lens.
Inputs: BASE/contract_blind_pde.md (its own blind case, cite by
file:line), PANEL_C50.md + refute_C50.md + the C50 section of
VERDICT_wave3.md (consume, do not re-litigate what the judge
certified), VERDICT_contract_and_L4R1.md#D-3. Duty: argue the L1(A)
choice AT ITS BEST against the concrete acceptance-semantics use
cases of the contract (W1-W4 windows, periodic front-bearing data,
jump positions not measured to TV accuracy); answer the panel's and
refuter's strongest points against L1 EXPLICITLY; state what
evidence would make L1 lose (falsifier). §0-ter discipline: census
additions only where the panel's census left the L1 case
under-searched (delta-census, verbatim queries, read-depth markers).
Output: BASE/blocco3/advocate_C50_pde.md.
(2) ADVOCATE-DATA: live steelman of the thrust-calibrated
flux-weighted L2 + L-inf realizability lens. Inputs: symmetric
(contract_blind_data.md + the same shared files). Same duties,
mirrored: argue flux-weighted L2 + L-inf AT ITS BEST for sensitivity
semantics (thrust-calibrated weighting, UQ propagation, realizability
guard), answer the strongest counter-points, falsifier, delta-census.
Output: BASE/blocco3/advocate_C50_data.md.
(3) C50 CLOSURE JUDGE (dedicated): reads both advocates + PANEL_C50 +
refute_C50 + the wave-judge C50 section + the D-3 mandate. Adjudicates
the metric choice TO CONVERGENCE: single metric / split-by-role (the
D-3 judge's named candidate) / conditional — with, per outcome: the
exact ledger delta text for C50 (supersedes the wave-3 delta if it
differs — state the diff explicitly), pinned falsifiers per metric
role, the F2 contract-window duty text, and dependencies
(contract:datum-uncertainty-contract-missing row, C55 nu-datum, M0
W1-W4). Escalation rule still lives: a surviving CONTENT objection
between the two advocates that the judge cannot kill on the record =
named residue with owner (F2 contract window), never a forced pick.
Output: BASE/blocco3/VERDICT_C50_form2.md — THE C50 authority of
record for the landing (the landing window applies this file's
delta, not the wave-3 one, if they differ).

## D2 — REM DEPTH PASS (unconditional, upgrades the pre-registered
## judge-strain check from conditional to MANDATORY on user order)
Sequencing: AFTER VERDICT_wave3.md exists.
ONE agent: REM-DEPTH VERIFIER. Inputs: PANEL_REMMARCH/REMENG/REMPOL
+ their three refutations + the REM sections of VERDICT_wave3.md.
Duty (verification of adjudication completeness, NOT re-adjudication):
for EVERY row of the three REM clusters (C5/C6/C8/C10/C12/C13/C14;
C16/C19/C22/C37/C39/C40/C44/C45; C7/C26/C29/C30/C47), build the
attack ledger: each refuter finding ID -> the judge's disposition
(sustained/overruled/amendment) WITH stated reason, quoted. Flag:
(i) attacks with NO disposition; (ii) blanket dismissals (one
sentence covering multiple distinct attacks); (iii) verdicts whose
reason contradicts the quoted panel/refuter text; (iv) scope-clause
violations missed (F3/F4b/F5-owned territory, census-lemma
territory); (v) escalation-rule misses (a sustained content
objection without an escalation call). Output:
BASE/blocco3/REM_depth_ledger.md with per-row verdict
COMPLETE / GAP(ids) and a machine summary. Any GAP row = that row's
verdict is NOT landable: it gets a dedicated judge re-pass (the
pre-registered split-judge escalation) BEFORE the landing window
touches it.

## D3 — DECLARED NON-UPGRADES (so depth stays honest, not ritual)
(a) FAM/CONV/C50 wave-3 chains are NOT re-run — their depth is the
Form-2 escalation (D1) and the wave judge; re-running finders without
a named open disagreement is the waste orchestration-weight rule 5
forbids. (b) No re-seed: pool unchanged (SEED_PROTOCOL_v3 per-pool
rule). (c) AG-4 (single-model-family common-cause) is NOT mitigable
inside the standing model pin (Fable-only; Sonnet mechanical-only;
Opus zero) — a cross-family refuter experiment requires an explicit
user pin exception at a boundary; priced row stands, nothing silent
happens here.
