# PANEL — BLOCCO 3 WAVE 2, CLUSTER C1REP
# S-FOUNDATIONS-C2, 2026-08-19. Rows: C1 (design basis class) +
# C2 (right-end BC, conditional) + C3 (left-end BC, challenge) +
# C49 (per-phase solution representation).
# BASE = validation/sfoundations_raws_2026-08-13. Census validity
# stamp: 2026-08-19 (§0-ter (e)). File written in protocol order:
# §1 (questions + pre-registered decision criteria) FROZEN BEFORE the
# §2 census was run; mid-census amendments, if any, are declared
# in-place with reason.
# RECONCILE REWRITE of record (2026-08-19, post quota-kill #5): the
# on-disk partial (mtime 12:55, §0/§1/§1-bis only) was consumed as
# UNVERIFIED DRAFT INPUT per BRIEF_wave2_reconcile.md; every kept
# section was re-verified at source in this window; repairs are
# tagged [RECONCILE-R#] in place; see ## RECONCILIATION DECLARATION.

## §0 WAVE-1 CONSUMPTION + CONTEXT OF RECORD

Read in full: BASE/blocco3/VERDICT_wave1.md (C28/C27/C9/C11 all
CONVERGED-ON-PROTOCOL, 0 escalations). No row of this cluster is
CONDITIONAL on an unsettled wave-1 branch — all four wave-1 rows
landed converged, so every conclusion below is stated invariant.
Consumed touchpoints:

- **C11/C9 (VERDICT_wave1 §2.3-2.4, §3):** the characteristic-native
  adjoint/DWR build is ONE shared F2 artifact (F2-C11 leg (b) builds,
  F2-C9 consumes). C49's fitted-tier-as-certificate-bearer is the
  representation that build PRESUPPOSES (DWR in characteristic-native
  form consuming Ancourt 2023 + the fitted-march adjoint). A C49
  outcome that removed the fitted tier would orphan the wave-1
  duties; a C49 outcome that keeps the fitted tier as certificate
  bearer composes with them. Stated here, weighed in §3.4 as
  compatibility evidence only (not as a verdict driver — that would
  be incumbent bias by infrastructure).
- **C28 (VERDICT_wave1 §2.1):** P4/P3(ii) binary gates KEPT absolute;
  certification is priced but never traded ("a certificate is not
  tradeable"). Whatever representation C49 adopts must expose the
  per-cell certification field the gates check — a constraint on the
  captured-only option, cited in §3.4.
- **C27 (VERDICT_wave1 §2.2):** no direct dependency; the
  per-phase constraint family aggregates over xi, not over the wall
  basis. Named, not used.

[RECONCILE-R3, ADDED — post-partial events, mandated by the wave-2
reconcile task prompt] **Now-landed C9/C11/C56 ledger rows CONSUMED
(measured this window, docs/choice_ledger.yaml):**

- **C9 :219-228 (MIXED, supplement of record folded in):** the
  moving-mesh / r-adaptive FAMILY is CLOSED at census grade
  (PANEL_C9C11_SUPPLEMENT.md axis A + VERDICT_C9C11_supplement.md §2
  "AXIS A ... STANDS, as amended"): A36 IS the family equidistribution
  principle in derived static form; front-fitting = the family's
  alignment content "already exact of record (C49/Lemma B par.4.3)";
  MMPDE map-motion does not transfer (columns = characteristic
  intersections). This DIRECTLY serves this cluster's §1-bis axis (3):
  the census-grade closure is CITED there, not re-adjudicated.
- **C11 :240-250 (MIXED, supplement folded in):** the DWR WEIGHT fork
  is CLOSED — weight of record = DISCRETE AD-adjoint of the marched
  scheme (custom_vjp, one-lowering per C48; exact at computed J, O3.1
  [X-A1IM]; "captured-smear trap bypassed on the fitted class, P2
  Lemma B par.4.4"); continuous/characteristic-native line =
  formulation frame + REFEREE only ("a weight cannot referee
  itself"); new pin F11d (two legs: Giles-Pierce oracle convergence;
  ACE/Lozano-Ponsin Eqs. (30)/(31) compatibility residuals at band
  sites). Consumed in §3.4 as direct C49 evidence and in the C56
  gradient-role adjudication.
- **C56 :731-740 (MIXED, minted 2026-08-19 at the C9/C11 landing):**
  "Adjoint REALIZATION of record for the marched stack, per role
  (gradient / DWR weight / adaptation indicator)". Owner field:
  weight role CLOSED; indicator role closed by transitivity;
  **gradient role OWED TO wave-2 (forecast, not consumed)** — the
  owner text names C31TRIO as the forecast target; the wave-2
  reconcile task prompt of record assigns the forecast's consumption
  to THIS panel as part of its engine-side question (C49). This panel
  ADJUDICATES the gradient-role adjoint realization in §3.4-bis and
  SAYS SO EXPLICITLY there, with the dedup rule for the landing
  declared (one consumption of record; if PANEL_C31TRIO adjudicates
  the same role from the engine side, the landing merges and verifies
  identity of direction).

Context of record (cite, not re-litigate): diff anchors
BASE/phaseB_tree_diff.md:22-33 (C1), :34-37 (C2), :38-44 (C3),
:288-293 (§2.13 front-terms-in-adjoint) [RECONCILE-R2: tightened from
the partial's :282-293 — :282-287 is item §2.12; the front-terms
block is :288-293, re-read this window], :294-299 (§2.14
two-instrument twin doctrine), :330-335 (§3.3 certified solution
class 4/4), :377-382 (§4.8 cone-vs-sharp-features tension P-c);
ledger rows [RECONCILE-R1: line refs RE-MEASURED this window per
SR-12 — the partial inherited the brief's pre-landing refs, stale by
+2 after the landing edits] docs/choice_ledger.yaml:135-145 (C1),
:147-156 (C2), :158-164 (C3), :653-663 (C49); D6 rejection of record
docs/rde_nozzle_development_plan.md:915-940 (S20 residue-(a) survey
decision; B-spline switch rejection text verified at :936-940);
findings rows parametrization:basis-oscillation-axis-unadjudicated
(findings_registry.yaml:1290-1299, GAP-21) and
parametrization:natural-BC-lip-bias (:223-231, GAP-5); census pins
validation/PANEL_topology_census_2026-07-22.md (CEN-O3 cone pinned
Chenais-form :355-356, CEN-O9 lip carrier :415, CEN-O11 lip circles
:423-424); claims registry fitted-adjoint identities (:489 reverse-AD
== transposed adjoint sweep [T-LEMB block :485-493]; :660 O3.2
order-h^2 tangent convergence [S-LBML]; :1484 discrete adjoint
characteristic sweep [PAP-P2LB]); M0 [D-S1] D2.5 solution class
(rde_nozzle_MASTER.md:185-198, :324-327 S1 membership certified
a posteriori with margin delta_S1).

## §1 FROZEN FORMAL STATEMENT (pre-registered BEFORE the census)

**Shared sub-problem.** The certified design loop optimizes a scalar
cycle-averaged thrust functional J over an admissible class A_gen(c)
of axisymmetric wall contours (curvature/angle/cone constraints,
Chenais-form uniform cone pinned CEN-O3), each design evaluated by a
certified MARCHED axisymmetric Euler solve in the S1 piecewise-smooth
fitted-front class (M0 D2.5), differentiated by the JAX custom_vjp
discrete adjoint of the marched scheme (claims :489/:660), under the
certificate/verdict discipline (per-cell certification, P4/P3(ii)
binary gates absolute per wave-1 C28, derived bands, armed
rejectors). This cluster adjudicates WHAT OBJECT the optimizer moves
(C1: the wall-contour dof basis; C2/C3: its end conditions at the
exit and attachment ends) and WHAT OBJECT the certificate certifies
(C49: the per-phase solution representation). The two questions
couple: the basis fixes which admissibility certificates are exact
vs sampled; the solution representation fixes which state/front
certificates are even DEFINABLE.

### C1 — MAIN QUESTION
Which dof basis for the wall contour lets the certified loop move
through innocent data without leaving the certified class (no
oscillation-triggered certification failures of the GAP-21
mechanism), while every A_gen(c) admissibility constraint stays
certifiable at the record's rigor and the certified stack (U1 C_geo
class, S18 driver, O3.1 identity chain) survives with a bounded,
protocol-pinned revalidation?

**Pre-registered decision criteria (frozen before search):**
- W1 (oscillation axis): an option WINS this axis if it carries a
  THEOREM-grade mechanism removal (e.g. a variation-diminishing /
  convex-hull property that excludes the measured clamped-end
  alternating curvature wave, ratio -(2-sqrt(3)) EXACT, amplitude
  ~ slope mismatch, growth 1/h — findings :1293) — or a measured twin
  showing the mechanism absent on the recorded failing family.
  The incumbent HOLDS the axis only if the mechanism is shown
  non-firing inside the production seed protocol of record.
- W2 (exactness axis, NEW from the trees): an option WINS if
  curvature/angle/cone admissibility admits EXACT sample-free
  sufficient certificates (per-segment algebraic bounds) whose
  conservatism contracts under refinement (subdivision), replacing
  sampled checks. The incumbent holds if its sampled checks with
  derived tolerances are shown sufficient at the same rigor tier
  (i.e. the sampling gap is bounded by a derived constant, not
  asserted).
- W3 (migration cost, D6's adjudicated axis — weighed, not
  inherited): any flip must name the change-of-basis certificate
  protocol (post-solve, zero-behavior-change first step — the S25
  evidence pointer in the C1 owner field) and bound the revalidation
  of the certified stack; a flip candidate without that protocol
  LOSES regardless of W1/W2.
- W4 (structure preservation): an option LOSES if it degrades
  A(c)-membership-by-construction or the existence link (compactness
  of bounded control polygons, O-F30) — e.g. global-support
  perturbation bases that couple local edits.
- Burden: the D6:915-940 rejection is SINGLE-AUTHOR evidence FOR the
  incumbent on conditioning+revalidation (its adjudicated axis); the
  oscillation and exactness axes carry NO incumbent verdict of
  record, so on those axes neither side inherits a presumption.

**Materiality (C1): HIGH.** The oscillation mechanism produced
uncertified cells + NaN gradients on the recorded S17 chain
(findings :1293) — above every noise floor; the exactness axis
decides whether admissibility certificates carry a sampling gap at
all. Not an immaterial fork.

### C2 — MAIN QUESTION
Under which C1 outcomes does the right-end BC question survive at
all, and for the surviving branch, what protocol adjudicates
not-a-knot adoption (the executed twin is a DIAGNOSTIC of record,
-82% corner residual, adoption NEVER adjudicated)?

**Pre-registered decision criteria:** this row is adjudicated ONLY
as the declared conditional (brief mandate): (i) the dissolve
condition is structural — if C1 flips the certified carrier to a
control-polygon basis, there is no interpolation problem and hence
no end-condition dof: C2 DISSOLVES into the flip's basis spec;
(ii) if C1 holds the interpolating basis, C2 stays a live F2
adoption question whose criteria are: corner-residual improvement
must persist at a certified optimum (not only at the S19 replay
point) AND the lip-state shift (8.2% cd of record, findings :226)
must be re-measured and land inside derived bands or be adopted as
a declared design-class correction. The BC itself is NOT adjudicated
here (brief pin).

**Materiality (C2): conditional.** Material at the corner-residual
floor (-82% of the S19 residual of record is attributable to the
natural BC), but the row's existence is contingent on C1 — depth
spent accordingly (materiality valve, §0-ter (c)).

### C3 — MAIN QUESTION
Is the left-end (attachment) boundary condition of the wall
representation genuinely forced by geometry — so that DECIDED
(constructional) stands — or do the trees' two corner-treatment
alternatives (floor-radius rounded lip, P-F21; exact centered
Prandtl-Meyer fan as internal data at Lambda, H-F18) constitute
genuine alternatives ON THE SAME CHOICE AXIS the row adjudicated?

**Pre-registered decision criteria:**
- The challengers OVERTURN the row only if they change the spline
  left-end condition itself (the clamped tangency tan(thB)).
- They instead RE-SCOPE the row if they are genuine alternatives one
  level UP (corner geometry/data at the attachment set Lambda) that
  the row's "no genuine alternative exists" gloss over-claims
  against.
- Census-owned parts (cone condition vs sharp features = diff §4.8
  P-c tension; lip carrier CEN-O9; Lambda = lip circles CEN-O11) are
  NAMED, never decided here — F2-exit pin respected (user pin of
  record; census-lemma window owns them).
- A DERIVED-condition amendment (lip radius pinned at the
  curvature-bound floor) is adoptable only with its falsifier pinned.

**Materiality (C3): LOW on thrust, HIGH on class/adjoint
regularity.** P-F21's own advocacy concedes "negligible thrust";
what is material is (i) whether the sharp corner is even IN the
pinned admissible class (the §4.8 tension: unpriced), (ii) adjoint
regularity at Lambda (the lip-node gradient is the C2 trigger's
subject), (iii) exactness of the corner expansion data (H-F18).

### C49 — MAIN QUESTION
Which per-phase solution representation BEARS THE CERTIFICATE for
the averaged-design loop — fitted-front characteristic marching
(incumbent, the S1 certified class of record), adjoint-consistent
shock capturing as sole solver, a two-tier architecture (capturing
explorer + fitted certifier, |J_capture - J_fitted| promoted to a
measured (v) carrier), or modern implicit shock tracking — i.e. the
user question of record 2026-08-17: "perche si fitta una soluzione
che potrebbe essere catturata?"

**Pre-registered decision criteria:**
- W1 (certificate definability): captured-only WINS only if the
  record's certificate layer (per-cell residual certification, front
  admissibility/Lax checks, S1 class-membership monitors with margin
  delta_S1, P4/P3(ii) gates) can be DEFINED and enforced on smeared
  representations at comparable rigor, with literature support at
  the depth actually read — not by analogy.
- W2 (discovery value): two-tier WINS its exploration half if
  capturing adds genuine discovery value (unknown front inventory /
  sector topology on arbitrary intermediate designs, where the
  marcher's fixed front topology is the binding limitation) at a
  build cost the program can name; its carrier |J_capture -
  J_fitted| must come with protocol + falsifier pinned NOW (brief's
  measurement-gating expectation).
- W3 (incumbent retention): fitted-only HOLDS as certificate bearer
  if no alternative wins W1; it holds as SOLE representation only if
  W2 also fails (discovery value speculative relative to build
  cost).
- W4 (tracking upgrade): implicit shock tracking wins the
  certificate tier only with demonstrated robustness at
  production-like counts AND a discrete-adjoint story compatible
  with the custom_vjp stack of record; otherwise it can at most be
  the named upgrade path.
- Zero-inflation guard, pre-committed: the anti-capturing case must
  NOT lean on the retracted Lozano attribution (cava C10 correction
  :1098 — divergence locus = wall/exit edge, NOT the shock);
  captured-adjoint pathologies are cited only at the evidence level
  held (read-depth markers), and the modern captured-adjoint
  production practice is represented at its genuine best (user
  mandate: genuine captured advocate).

**Materiality (C49): HIGH** (solver architecture; every downstream
certificate and duty binds to it), but adoption of any new tier is
build-gated (F2/F5) — the panel converges representation + protocol,
never a build.

### §1-bis PER-ROW AXIS BEARING (§0-bis axes, §0-ter (f))

- **Axis (1) optimizer query-level engine:** does NOT bear on C1/C2/
  C3/C49 as a decision input — the engine choice (C31TRIO cluster,
  this wave) consumes whatever dof vector and constraint set this
  cluster defines; the only interface is conditioning of the basis
  (named inside C1 W1/W2, adjudicated there as a basis property, not
  an engine property). No engine alternative can repair a basis
  whose class oscillates on innocent data.
- **Axis (2) discrete vs continuous adjoint + consistency:** BEARS
  on C49 centrally (the fitted-front discrete adjoint identity of
  record, claims :489/:660, vs the Giles-Pierce-class continuous
  adjoint with shock BC; captured discrete adjoints are exactly
  where consistency is known to be delicate) and on C1 marginally
  (a change of basis composes a LINEAR map with the existing vjp —
  adjoint consistency is invariant under smooth reparametrization of
  the design space; stated, closed). C2/C3: bears only through the
  lip-node gradient regularity (named in C3). [RECONCILE-R4,
  AMENDED — post-partial event] The axis NOW HAS a ledger home: row
  **C56** (measured :731-740, minted at the C9/C11 landing) — the
  partial's candidate-row proposal is WITHDRAWN as moot; this panel
  CONSUMES C56 instead (weight role closed of record; gradient role
  adjudicated in §3.4-bis per the task prompt; dedup coordination
  with PANEL_C31TRIO declared there).
- **Axis (3) moving-mesh / r-adaptive families:** BEARS on C49 as a
  genuine alternative family (front-fitting-as-mesh-motion:
  MMPDE/monitor-function equidistribution; implicit tracking IS
  optimization-based mesh motion). [RECONCILE-R4, AMENDED] The
  family question is now CLOSED AT CENSUS GRADE of record by the
  C9 supplement (VERDICT_C9C11_supplement.md §2 axis A; C9 row note
  :228): A36 = the family equidistribution principle in derived
  static form; "front-fitting = the family's alignment content
  already exact of record (C49/Lemma B par.4.3)"; MMPDE map-motion
  non-transferable. The partial's structural note (the
  characteristic march already IS a solution-adapted moving mesh) is
  thereby CONFIRMED at census grade — cited in §3.4, not
  re-adjudicated. Implicit tracking remains the family's one member
  that is ALSO a solution representation — adjudicated under C49 W4.
  Bears on C1/C2/C3 only via C9 (wave-1, consumed — mesh law owns
  it).
- **Axis (4) adjoint-free routes (DFO/BO/evolutionary):** BEARS on
  C1 as a census axis (low-dimensional bases are what makes modern
  BO/DFO shape optimization work — a genuine modern argument FOR
  compact parametrizations, censused at its best) and on C49's
  exploration tier (a captured explorer may be driven adjoint-free;
  the trees quarantine derivative-free strictly to exploration —
  diff :159-167). Does not bear on C2/C3 (end conditions are not a
  search-space design lever). Closed per-row in §3.
- **Axis (5) emergent sub-aspects:** one emerged during
  formalization and is carried as a named sub-question inside C1
  rather than a new row: EXACT vs SAMPLED admissibility certificates
  as a general certificate-discipline pattern (Bernstein-form bounds
  are the instance here; the same pattern recurs at the front
  admissibility checks). Named in §4.5 as a note candidate, not
  minted.

## §2 SOTA CENSUS (dated 2026-08-19; §0-ter protocol)

### §2.0 Query protocol table (§0-ter (a))

All searches run 2026-08-19, engine = WebSearch (US-index), plus
in-repo greps (measured commands quoted in §2.4). "incl." = entries
carried into §2.2/§2.3 at the stated read depth.

| # | query (verbatim) | hits ret. | screened | incl. |
|---|------------------|-----------|----------|-------|
| Q1 | "Bernstein coefficient bounds B-spline curvature constraint certified shape optimization sufficient condition" | 9 | 9 | 3 |
| Q2 | "airfoil parametrization comparison CST Hicks-Henne B-spline control points aerodynamic shape optimization benchmark" | 10 | 10 | 4 |
| Q3 | "implicit shock tracking HOIST Zahr Huang robustness high-order optimization-based shock fitting 2024 2025" | 9 | 9 | 5 |
| Q4 | "adjoint shock capturing inaccurate gradient discrete adjoint converges wrong value shock optimization aerodynamic design practice" | 10 | 10 | 2 |
| Q5 | "unstructured shock-fitting Bonfiglioli Paciorri review shock fitting classical recent developments adjoint" | 9 | 9 | 3 |
| Q6 | "plug aerospike nozzle sharp lip corner rounding radius effect thrust Prandtl-Meyer expansion design" | 8 | 8 | 3 |
| Q7 | "certified a posteriori error verification shock capturing per-cell certification design optimization loop compressible Euler production certificate" | 8 | 8 | 1 (absence probe) |
| Q8 | "Bayesian optimization surrogate derivative-free aerodynamic shape design dimensionality parametrization 2024 2025 review" | 9 | 9 | 3 |

Communities covered: AIAA/JCP/CMAME adjoint+parametrization school
(Q2/Q4), the implicit-tracking line (Q3), the Moretti/Italian
shock-fitting school (Q5), plug-nozzle MOC practice (Q6), the
validated-numerics-adjacent certification probe (Q7), the BO/DFO
modern line (Q8). The RUSSIAN CLASSICAL school enters through the
HELD corpus (Kraiko rows of the litreview cava, §2.3 — C8/KT2015
verbatim rows read [FULL] in the ratified cava), which is deeper than
any web hit for this territory. Completeness is falsifiable against
the table, not asserted.

### §2.1 Corpus recency

Span of the censused corpus: 1975 (Chenais, via tree/census pins) ->
2025. Newest items: arXiv 2504.15177 (2025, rp-adaptive implicit
tracking) [TITLE], arXiv 2505.09088 (2025, DFO competitive in
moderate dimensions) [TITLE], Progress in Aerospace Sciences
parametrization review 2025 (S0376042125000661) [TITLE], Springer
Fluid Dynamics 2025 FFD+BO [TITLE], space-time HOIST JCP 2024
(S002199912400041X) [ABS], DG-adjoint gradient study arXiv
2407.18963 (2024) [ABS]. Every row's census reaches >= 2024 on at
least one axis; C49's tracking axis reaches 2025.

### §2.2 Census per row (read-depth markers per §0-bis (b))

**C1 (basis / exact certificates):**
- Bernstein-form bounds as sufficient certificates: standard
  computational-geometry property (bounds conservative, contracting
  under subdivision); web instances: "Tight Bounds on Polynomials
  and Its Application to Dynamic Optimization" arXiv 2403.07707
  [TITLE]; "Shape-constrained Estimation ... with Bernstein
  Polynomials" arXiv 2209.04476 [ABS: necessary+sufficient
  nonnegativity conditions over an interval via Bernstein form];
  spline-based topology/shape optimization with explicit curvature
  constraints (Technion structopt 2018) [ABS]. The exactness
  MECHANISM (Bernstein coefficients bound the polynomial on the
  simplex/interval; de Casteljau subdivision contracts the bound to
  sharp) is textbook-grade and is ALSO carried by the blind tree
  O-F4/O-F8 at derivation level (condensed [L298]/[L516], read
  [FULL] this window).
- Parametrization comparisons: Masters et al., "Geometric Comparison
  of Aerofoil Shape Parameterization Methods", AIAA J [ABS: 20-25
  dofs to cover the full airfoil design space at geometric
  tolerance; SVD/modal most efficient]; "Influence of Shape
  Parameterization on a Benchmark Aerodynamic Optimization Problem",
  J. Aircraft [ABS]; "Review of Aerofoil Parameterisation Methods"
  [ABS: CST rated high on parsimony axes, then PARSEC, B-splines,
  Hicks-Henne]; Progress in Aerospace Sciences review 2025 [TITLE].
  One benchmark instance [ABS, Q2 synthesis]: B-spline control
  points 34% drag reduction in 177 calls vs Hicks-Henne 25% in 599 —
  single-instance, NOT weighed as a verdict driver (inflation
  guard); carried only as evidence that control-point charts are
  first-class modern practice.
- Modern ML/dim-reduction lines (context, not options): Bezier-GAN
  arXiv 2006.12496, diffusion-manifold-constrained ASO arXiv
  2507.23443, DCGAN filtering AIAA J [all TITLE/ABS] — all reduce TO
  low-dimensional smooth charts; none carries a certificate layer.
- Of record (in-repo, [FULL]): D6:915-940 (the S20 rejection:
  adjudicated conditioning+revalidation, explicitly NOT the
  oscillation axis — findings :1299 Q2 quarantine); findings :1293
  (the measured mechanism: alternating curvature wave, ratio
  -(2-sqrt(3)) EXACT, growth 1/h); U1 C_geo class (clamped-left
  natural cubic = the certified curve class, D6:928-930).

**C2 (end condition):** census-light BY DECLARED MATERIALITY VALVE
(conditional row): the not-a-knot vs natural question is a
textbook-grade interpolation fact (de Boor, FITPACK — the record
already carries READ-THE-SOURCE findings on scipy 1.18
_fitpack_repro.py at D6:940+ [FULL]); the decisive evidence is the
EXECUTED twin of record (findings :226: corner mismatch -82%,
lip-state cd shift 8.2%, baseline reproduces S19 to 5 digits). No
web search adds decision-relevant content above that evidence for a
row whose existence is conditional on C1. Declared, both-directions
honest.

**C3 (attachment corner):**
- Plug-nozzle lip practice: modern MOC plug/aerospike design KEEPS
  the sharp lip with a centered Prandtl-Meyer fan as THE design
  method (Angelino-method + CFD validation 2024 [ABS]; "Design of
  axisymmetric aerospike nozzle based on modified MOC" 2018 [ABS];
  MATEC 2024 contour design [ABS]); corner rounding in that
  literature is motivated THERMALLY, with "no significant effect on
  thrust" [ABS, Q6 synthesis].
- Corner-vs-smooth thrust scale, HELD [FULL via ratified cava 3.18,
  :594]: KT2015 p.187 (from its ref [8]) — the classical
  sharp-vs-smooth penalty is "hundredth fractions of per cent". This
  quantifies the C3 thrust-materiality floor externally.
- Adjoint regularity at corners: P-F21's advocacy (condensed [L821],
  [FULL]) is the genuine instance; no additional web source at
  decision depth was found within Q6's scope (absence bounded by
  Q6).

**C49 (representation):**
- HELD anchors (in-repo literature registry, statuses measured this
  window): giles_pierce_2001 :491-498 READ-INTEGRAL (analytic
  quasi-1D adjoint; adjoint continuity across a fitted shock +
  internal shock BC; sonic-throat log singularity);
  giles_ulbrich_2010_part1 :105-113 READ-PARTIAL (eps=h^alpha
  interior-smearing repair; adjoint interior BC p.886;
  capturing-vs-fitting contrast page-verified);
  **giles_ulbrich_2010_part2 :114-121 READ-PARTIAL, page-verified:
  "fixed-stencil discrete adjoint converges to a WRONG value across
  shocks (pp.907, 910) — 'the trap is a theorem'"** (1-D scalar
  conservation laws — depth-honest scope: the theorem is 1-D scalar;
  its system/multi-D transfer is the field's open concern, which is
  exactly why adjoint-consistent capturing needs special
  treatments); lozano_2018/2019 :147-162 (wall/exit-edge divergence
  — cited ONLY per the cava C10 retraction discipline);
  lozano_ponsin_2025 :122-128 READ-PARTIAL (characteristic-structure
  2-D analytic adjoints; compatibility Eqs. (30)/(31) = the F11d
  leg-2 instrument); ancourt_peter_atinault_2023 :199-208
  READ-INTEGRAL (ACE residuals as code verification);
  zahr_persson_2016 :572+ (time-periodic discrete adjoint — NOT the
  tracking line; R14 caveat respected).
- Captured practice at its genuine best: production transonic
  RANS/Euler adjoint optimization through captured shocks is
  routine, decades deep (Jameson-school; SU2/MACH-class frameworks)
  [community knowledge anchored by Q2/Q4 hits, ABS]; DG adjoint
  gradients more accurate than FV at matched cost, arXiv 2407.18963
  (2024) [ABS]; supersonic discrete-adjoint wing design MDPI 2023
  [ABS]. THE HONEST STATEMENT: this practice certifies DESCENT
  QUALITY (gradients good enough to optimize), not per-cell
  certificates/class membership — no hit in Q4/Q7 shows a captured
  production loop carrying a per-cell certification layer of our P4
  class (absence claim, query-bounded).
- Implicit tracking: HOIST robust framework JCP 2022
  (S0021999122000432 / arXiv 2105.00139) [ABS]; parametrized-shock
  BCs arXiv 2304.11427 (2023) [ABS: "parameter-dependent lead
  shocks ... commonly arise in high-speed aerodynamics" — the
  design-loop-relevant variant]; space-time HOIST JCP 2024 [ABS];
  reactive-flow HOIST AIAA J [TITLE]; rp-adaptive tracking arXiv
  2504.15177 (2025) [TITLE]; MDG-ICE hypersonic viscous arXiv
  2311.00701 (2023) [TITLE/ABS]. No source found showing a
  DESIGN-adjoint THROUGH a tracking solve at production counts
  (absence bounded by Q3).
- Unstructured fitting: Bonfiglioli-Paciorri line + Onofri-Paciorri
  book "Shock Fitting: Classical Techniques, Recent Developments"
  (Springer 2017) [ABS]; embedded/extrapolated tracking on
  unstructured dynamic grids CMAME 2021 [ABS]; "Convergence Analysis
  of Shock-Capturing and Shock-Fitting Solutions on Unstructured
  Grids" AIAA J [TITLE]. Adjoint story: NOT FOUND in this line
  (Q5's own synthesis names no adjoint work — absence bounded by
  Q5).
- Adjoint-free exploration (axis 4): "Derivative-free optimization
  is competitive for aerodynamic design optimization in moderate
  dimensions" arXiv 2505.09088 (2025) [TITLE]; design-space
  dimensionality-reduction survey arXiv 2405.13944 (2024) [ABS];
  hybrid Bayesian-adjoint AIAA 2024-3753 [TITLE].

### §2.3 Ratified-cava rows cited PER-ROW (row IDs; [FULL] — the
### cava is ratified of record)

- C1: cava **C8** (:1096) — Kraiko-Pyankov-Tillyaeva 2016 is DIRECT
  optimization with "Bézier a 9 parametri" (GA + RANS), i.e. the
  classical Russian school itself uses a CONTROL-POINT chart for
  direct optimization; AND its own table shows the exact-theory
  nozzle (Delta R_x 2.45%) BEATING all GA designs (2.90-3.68%) —
  parallel branches, quoted per the cava's own anti-inflation
  wording. Feeds C1 (control-point charts are classical-school
  practice) and P-F19's analytic-spine cross-check.
- C2: NO cava row (bilingual grep, §2.4).
- C3: cava **3.18** (:588-598) — KT2015 corner-vs-smooth penalty
  "hundredth fractions of per cent" [PDF tier]: the external
  materiality scale for the corner axis.
- C49: cava **C10** (:1098) — the Lozano fitted-front attribution
  RETRACTION (divergence locus = wall/exit edge, NOT the shock):
  binding zero-inflation guard, honored in §3.4; cava **A18**
  (:1037) — ACE residual oracle (O3.5 candidate, homogeneous-interior
  clauses (i)-(iv)): the continuous-line REFEREE instrument; cava
  **A15** (:1034) — Zahr-Persson-derived gradient-verification
  protocol (FD-on-manifold, derived tolerance, 4-decade tau sweep):
  consumed by the §3.4-bis referee legs; cava **R14** (:1140) —
  Zahr-Persson 2016 fixed-geometry caveat (kinematics-spline dofs,
  NOT shape): guards against over-claiming [54] as design-adjoint
  precedent; cava row :130 — the modern adjoint school "ha la
  macchina e non ha il problema" framing (context only).

### §2.4 Absence claims (search-proven, measured commands)

- Cava bilingual sweep for this cluster's tokens: `grep -inE
  "parametrizz|bezier|bézier|labbro\b|spigolo|raccordo|
  end.condition|condizione.+estrem" ADVISORY_litreview_confrontation`
  -> hits {32, 106, 392, 456, 457, 475, 594, 861, 1096, 1118};
  inspected: only C8 (:1096) and 3.18 (:594) bear on this cluster's
  rows (others = T7(c)/cone-form territory, C31TRIO/wave-3 rows).
  `grep -cinE "captur"` -> 1 hit = :1098 (the C10 retraction row
  itself). No cava row adjudicates basis choice, end conditions, or
  fit-vs-capture as a CHOICE.
- Literature registry sweep: `grep -n "id: " literature_registry
  | grep -iE "giles|lozano|ancourt|zahr|kulfan|masters|bonfigl|
  paciorri|huang|corrigan|kraiko_2016|efremov"` -> HELD rows as in
  §2.2; NO rows for Kulfan/CST, Masters comparison, Hicks-Henne,
  HOIST/Huang-Zahr, MDG-ICE, Bonfiglioli-Paciorri -> procurement /
  WANTED riders in §4.5, claims above [ABS] not made.
- Q7 absence probe: no source combining per-cell certification +
  captured representation + production design loop (bounded by Q7's
  literal terms).

## §3 ADJUDICATION (per row; steelman per §0-ter (d))

### §3.1 C1 — design basis class

**Incumbent's genuine case (represented, may win):** the D6:936-940
rejection stands ON ITS AXIS: the S19 defect was knot placement, not
parametrization conditioning; the certified stack (U1 C_geo class,
S18 driver, O3.1 chain) is built and certified on the incumbent
chart; a basis switch has historically meant "re-validating the
entire certified stack" — and the record's own Q2 quarantine
(findings :1299) says the D6 DECISION is not relitigated here.
Modern census adds: comparison studies do not crown control points
universally (CST rates higher on parsimony axes [ABS]).

**Structural fact the adjudication turns on (panel derivation,
pen-grade, refutable):** the incumbent's certified curve class and
the B-spline control-polygon class are THE SAME SPLINE SPACE. A
clamped-left C^2 piecewise cubic on a fixed strictly-increasing knot
set IS a cubic B-spline curve on the clamped extended knot vector;
heights-at-knots <-> control points is a LINEAR BIJECTION (the
interpolation operator and its inverse). Hence the C1 flip candidate
is a CHART CHANGE on the same certified class, not a class
migration: (i) the U1 C_geo class statement is chart-invariant;
(ii) the solve/march/front layers consume evaluated geometry at
stations — chart-invariant; (iii) the design-side vjp composes with
ONE constant linear map (axis-2 note, §1-bis); (iv) the incumbent's
"natural" right end is one linear constraint (y''(L)=0) on the last
control points, which the control chart may KEEP or RELAX (that
relaxation IS ledger row C2's third alternative — see §3.2).

**W1 (oscillation):** the measured mechanism of record (findings
:1293) is the boundary-forced alternating solution of the cubic
INTERPOLATION recurrence (1-4-1 tridiagonal; characteristic root
-(2-sqrt(3)) = the decaying root of r^2+4r+1=0; amplitude ~ the
clamped-end slope mismatch; growth 1/h) — panel re-derivation,
consistent with the row's "independently re-derived". In the
control-polygon chart THE INTERPOLATION SOLVE DOES NOT EXIST: the
dof->curve map is the direct local B-spline evaluation; the
mechanism's carrier equation is structurally absent. On top of the
structural removal, the B-spline VARIATION-DIMINISHING theorem
(Schoenberg/de Boor class; y'' coefficients = scaled second
differences of the control polygon) bounds curvature sign-changes by
the polygon's own second-difference sign-changes — innocent data
cannot alternate. VERDICT ON THE AXIS: the challenger carries the
THEOREM-grade removal W1 asks for, with the instantiation derivation
(y''-coefficient form for our clamped graph) a named duty item and
the measured twin F-C1a pinned regardless (belt and braces — the
removal claim is falsifiable on the recorded failing family). The
incumbent produced no non-firing proof inside the production seed
protocol (none exists of record; the GAP-21 trigger names
"any non-GENO-seeded start / steep-thB instance" as live). W1:
control polygon WINS.

**W2 (exactness):** Bernstein-form sufficient certificates
(curvature/angle/cone; O-F4 [L298], O-F8 [L516]: subdivision
converges to sharp — "a certified branch-and-bound on the constraint
itself"; census §2.2 instances) are available on ANY piecewise-cubic
chart by per-segment conversion to Bernstein form — so exactness per
se does not separate the charts. What separates them: in the control
chart the constraint->dof maps are LOCAL and structurally simple
(cone condition = linear sufficient conditions on control-point
offsets, O-F4; each certificate touches <= 4 dofs), while in the
heights chart every Bernstein coefficient is a global function of
all heights through the interpolation inverse (exponentially
decaying but dense — same root again). W2: control polygon WINS on
certificate STRUCTURE; the exactness pattern itself is adopted
either way (axis-5 note, §4.5). The incumbent's sampled-check
retention branch (sampling gap bounded by a derived constant) is
retained as the FALLBACK if F-C1c fires.

**W3 (migration cost — D6's axis, weighed not inherited):** the flip
candidate NAMES the protocol: post-solve, zero-behavior-change
change of basis (the C1 owner field's own S25 pointer; findings
:1297 "post-solve change-of-basis certificate = cheap first step"),
= one linear solve per design + certificate re-evaluation; the
revalidation scope is BOUNDED AND NAMED by the chart-invariance
decomposition above (design-side linear-map audit + constraint layer
+ seed conversion; solve/march/front layers untouched — itself
falsifiable by F-C1b). The D6 rejection's revalidation concern is
thereby answered on D6's own axis WITHOUT relitigating D6's decision
(which was about a different lever: knot placement). W3: protocol
named -> the flip candidate SURVIVES the gate.

**W4 (structure):** control polygon: A(c)-membership partially by
construction + compactness of bounded control polygons = the O-F30
existence link. Hicks-Henne: global-support transcendental bumps —
LOSES W4 (couples local edits) and W2 (no exact algebraic
certificates on sin-bump sums); rejected at its best instance
(census: the classical ASO workhorse, still competitive in benchmark
drag studies [ABS]) — stated reason, not omission. CST/Kulfan:
global Bernstein-basis chart — exact bounds available (it IS
Bernstein), but single-segment global support (every dof moves the
whole wall), high-degree conditioning, and the class-function
structure is airfoil-LE-specific, valueless for an attachment-
tangency wall graph; rejected at its best (census: top parsimony
ratings, wins low-dof BO benchmarks [ABS]) — its genuine strength
(few dofs) is an EXPLORATION-tier virtue, quarantined there by axis
4. Two-tier V-F13 (level-set discovery + spline certificate
[L605]): the discovery tier answers a TOPOLOGY question owned by the
census/S15 program — orthogonal to C1's carrier question; closed as
re-scoped (its certificate tier = the adopted chart family).
H-F34(f) sector-structured explicit contour [L1026]: SUBSUMED — same
family as the adopted carrier (explicit smooth graph per sector,
Chenais cone class). SVD/modal + ML charts (census): no certificate
layer, data-manifold priors alien to the certificate discipline;
closed to the exploration tier alongside axis 4.

**Outcome (proposed, gated):** DIRECTION CONVERGED — the certified
carrier moves to the CONTROL-POLYGON CHART of the same certified
spline space (B-spline control points as dofs; Bernstein-exact
admissibility certificates; heights retained as a derived output).
ADOPTION OF RECORD IS GATED on the migration campaign (duty §4.1):
the incumbent chart remains the verdict-bearing chart until F-C1a/b/c
pass. This composes with D6 (its knot-placement decision and AFEM
skeleton are chart-invariant) and with GAP-21's own owner text.

### §3.2 C2 — right-end BC (conditional structure only)

Adjudicated ONLY as the declared conditional (§1 criteria):

- **Branch A (C1 migration lands):** the interpolation end-condition
  dof CEASES TO EXIST; C2 RESOLVES-BY-STRUCTURE to the row's own
  third alternative ("free-end control points"): the converted seed
  reproduces the record curve EXACTLY (including its natural end —
  zero immediate lip-state shift, by the linear bijection), and the
  optimizer is thereafter FREE at the exit end, subject to the same
  admissibility certificates. The GAP-5 record numbers become the
  MEASURED PRIORS on what freeing the end may move: the -82% corner
  residual = the improvement ceiling attributable to the end
  condition; the 8.2% cd lip-state shift = the mechanism datum whose
  re-measurement AT A CERTIFIED OPTIMUM rides the migration campaign
  (inside derived bands, or adopted as a declared design-class
  correction — findings :226/:231 discipline unchanged).
- **Branch B (C1 migration refuted by F-C1a/b/c):** C2 stays a live
  F2 adoption question, criteria exactly as frozen in §1(ii); duty
  named §4.2. The BC itself is NOT adjudicated here (brief pin
  honored: the notaknot twin remains a diagnostic of record).

No census beyond §2.2's declared-light entry was needed: both
branches are decided by structure + the executed twin of record.

### §3.3 C3 — left-end BC (challenge)

Applying the frozen criteria to the two challengers:

- **H-F18 (exact centered PM fan as internal data at Lambda,
  [L622]):** flow-data treatment AT the corner, inside the SOLVER —
  it does not touch the wall representation's end condition at all
  (the wall still attaches at tangency tan(thB) downstream of the
  corner; the fan is state data, not a geometry dof). RE-SCOPE, one
  level up: it is genuine advocacy for the FRONT-INVENTORY /
  corner-data question (it is even "the lip-shock generator on
  overexpanded phases" — feeding C49's front topology, noted there),
  not a C3 alternative. The row's axis is untouched.
- **P-F21 (rounded lip at the curvature-bound floor, [L821]):**
  changes the CORNER GEOMETRY (the attachment set Lambda), after
  which the spline end condition is again tangency — to the lip
  circle instead of the sharp corner. RE-SCOPE, one level up: the
  corner-geometry axis is CENSUS-OWNED territory of record (CEN-O9
  carrier :415; CEN-O11 "Lambda = lip circles" :423-424 — the pinned
  formalization already carries lip CIRCLES; the sharp-vs-floor
  question is exactly diff §4.8's P-c tension :377-382, already an
  agenda item for the census-lemma window, F2-exit user pin). NAMED,
  not decided here. Materiality inputs carried to that window: the
  KT2015 external scale (corner thrust penalty "hundredth fractions
  of per cent", cava 3.18) + P-F21's own "negligible thrust"
  concession -> the corner axis is an ADJOINT-REGULARITY and
  CLASS-MEMBERSHIP question, not a thrust question; and the modern
  plug-MOC practice keeps the sharp-lip fan as the design method
  (census §2.2, [ABS]).
- **Verdict on the row:** neither challenger changes the spline
  left-end condition itself -> **C3 DECIDED stands, RE-SCOPED**: the
  gloss "no genuine alternative exists" is NARROWED of record to "no
  genuine alternative exists ON THIS AXIS (the spline end condition,
  given the attachment geometry); the attachment-GEOMETRY axis has
  two named tree alternatives (P-F21/H-F18) and is census-owned
  (CEN-O9/CEN-O11, diff §4.8 P-c)". A derived-condition amendment
  (lip radius pinned at the curvature-bound floor) is ADOPTABLE ONLY
  by the census-lemma window, and only with the falsifier this panel
  pins now for that eventuality (§4.3). The adjoint-regularity
  interest at Lambda stays live through the C2/GAP-5 trigger
  ("first verdict leaning on the lip-node gradient", findings :231).

### §3.4 C49 — per-phase solution representation

**The captured advocate at its genuine best (steelman, trees +
census):** V-F6 [L317] (O3 = the numerical REALIZATION of the class
with a posteriori class-check), H-F4(c) [L209] (exploration/fallback
class with explicit downgrade flag), H-F8(c) [L328] (sector-agnostic
explorer), H-F9 [L364] (front inventory discovery; capture-the-rest
policy), H-F28 [L874] (smooth limiter/flux class making the captured
fallback differentiable), O-F9 [L566] (robust on arbitrary
intermediate designs), P-F12 [L474] (exploration/fallback solver);
census: decades of production transonic adjoint optimization through
captured shocks; DG adjoints accurate at matched cost (2024 [ABS]).
The genuine strengths: robustness on arbitrary designs, NO front-
topology commitment, mature tooling, lower implementation cost.

**W1 (certificate definability) — captured-only LOSES, stated
reasons:** (i) the record's certificate layer is DEFINED on explicit
front objects: Lax/admissibility checks need a front unknown; S1
membership monitors (finitely many transversal fronts, margin
delta_S1, M0:185-198/:324-327) need a front inventory; per-cell
z-space certification ([X-A1IM]) is defined on the fitted march. On
a smeared representation these objects exist only after
reconstruction — a DIFFERENT (and uncertified) instrument. (ii) The
held theorem anchor: fixed-stencil discrete adjoints converge to a
WRONG value across shocks (Giles-Ulbrich part 2 pp.907/910,
READ-PARTIAL page-verified — "the trap is a theorem"; scope honesty:
1-D scalar; the repair needs eps=h^alpha interior smearing, part 1) —
while the fitted class BYPASSES the trap of record (C11 supplement
axis B, landed row :250; P2 Lemma B par.4.4). (iii) The zero-
inflation guard is honored: NO Lozano-shock attribution is used
(cava C10 :1098 — the 2019 divergence locus is wall/exit edge);
Lozano 2018/2019 enter only as the lip/wall exclusion-norm
corroboration they actually carry. (iv) Production captured practice
certifies descent quality, not per-cell certificates (absence
query-bounded, Q4/Q7). (v) Wave-1 C28 keeps P4/P3(ii) gates absolute
— the gates check the per-cell certification field, which the
captured representation cannot expose at comparable rigor today.
All four blind trees, INCLUDING the captured advocates' own
recommendations, place capturing at exploration and never at
certification (diff :330-335, 4/4).

**W2 (discovery value) — two-tier WINS its exploration half:** the
marcher's fixed front topology is the binding limitation on
arbitrary intermediate designs (H-F11-class topology-change events;
H-F18's lip-shock generation; sector hunts); a captured explorer
discovers front inventory/sector candidates and may be driven
adjoint-free (axis 4 quarantine, diff :159-167). Build cost: a
full captured axisymmetric Euler solver = a named F2/F5-class build
— NOT scheduled by this panel (right-sizing; the census-enumerated
sector program of record covers the near-term topology need). The
|J_capture - J_fitted| carrier (diff §2.14 :294-299) is PINNED NOW
with protocol + falsifier (§4.4) so the tier lands measurement-ready
the day it is built. H-F9's derived front-strength policy ("fit any
front whose contribution to the DWR error estimate exceeds the
assigned budget line") is adopted as the FIT-VS-CAPTURE BOUNDARY
SCHEMA inside the certified tier, executable once F2-C11 leg (b)
lands — this ties C49's boundary to the wave-1 estimator build
instead of minting a parallel instrument.

**W4 (implicit tracking) — named upgrade path, not certificate
tier:** HOIST/MDG-ICE robustness is an ACTIVE line (2022-2025 census
§2.2: robustness papers, parametrized-shock BCs 2023, space-time
2024, rp-adaptive 2025 — all [ABS]/[TITLE]); no demonstrated
DESIGN-adjoint through a tracking solve at production counts was
found (absence bounded by Q3); the Bonfiglioli-Paciorri unstructured
fitting line carries no adjoint story (bounded by Q5). Verdict:
tracking = the RECORD'S NAMED UPGRADE PATH exactly as the blind
hyperbolic tree already recommends (H-F10(d) [L396] "the modern
upgrade path if front-interaction complexity grows"), with W4's two
entry conditions (robustness at production counts + custom_vjp-
compatible adjoint story) as its standing entry gate. No adoption.

**W3 (incumbent retention):** fitted-front marching HOLDS as
certificate bearer (no alternative won W1); it does NOT hold as sole
representation FOREVER (W2's discovery half is genuine) — the
resolution is the two-tier ARCHITECTURE adopted at the
representation level with the explorer tier BUILD-GATED. The
moving-mesh family framing of the fitted march is closed at census
grade of record (C9 supplement axis A, consumed §0/§1-bis — the
march IS a solution-adapted moving mesh; front-fitting = exact
alignment).

**ANSWER OF RECORD to the mandate question ("perche si fitta una
soluzione che potrebbe essere catturata?"):** because the program's
unit of product is a CERTIFICATE, and the certificate's objects —
per-cell residual certification, Lax/admissibility of each front, S1
class membership with margin delta_S1, and the exact discrete
adjoint with front terms designed-in (T-LEMB :489; H-F29 advocacy
diff :288-293) — are DEFINABLE only where fronts are explicit
unknowns. Capturing approximates the same weak solution but replaces
the front by a smear on which those certificates are undefined and
the fixed-stencil adjoint is provably wrong in the held 1-D scalar
theorem (depth-honest). Capturing's genuine strengths (robustness,
topology agnosticism) are exactly the EXPLORATION virtues — hence
two-tier with the fitted tier as certificate bearer, and
|J_capture - J_fitted| as a measured honesty carrier, never a truth
claim (H-F8: "agreement is NOT truth, only non-rejection").

### §3.4-bis C56 GRADIENT-ROLE ADJUDICATION (owed forecast CONSUMED
### HERE — explicit statement per the task prompt)

**Explicit consumption statement:** ledger row C56 (:731-740) owner
field forecasts "gradient role OWED TO wave-2 ... (forecast, not
consumed)", naming C31TRIO as the forecast target at mint time; the
wave-2 reconcile task prompt of record directs THIS panel to
adjudicate the gradient-role adjoint realization as part of its
engine question (C49, the representation the gradient differentiates
— the gradient premise sits verbatim in this cluster's §1 frozen
statement). This panel therefore ADJUDICATES IT NOW and declares the
DEDUP RULE: one consumption of record at the landing window; if
PANEL_C31TRIO also adjudicates the same role from the engine side,
the landing merges the two texts and VERIFIES identity of direction
(both consume the same anchors below); a direction divergence
between the two panels = escalation, not silent pick.

**Adjudication — gradient realization of record = the DISCRETE
AD-adjoint of the marched scheme (custom_vjp, one-lowering),
CONVERGED (not gated):**
1. EXACTNESS AT COMPUTED J: T-LEMB (claims :489, verified verbatim
   this window): reverse-mode AD with implicit rules IS the
   transposed adjoint sweep along the same discrete characteristics,
   the fitted front an explicit unknown — zero consistency gap at
   finite h, front-motion terms carried BY CONSTRUCTION (the
   Giles-Pierce O(1) front term is not modeled, it is transposed).
2. ONE-LOWERING DISCIPLINE: C48 note of record (cross-lowering
   gradient floor ~1e-8 rel, FD-amplified ~7 orders — the measured
   defect was mixed lowering, not the AD-adjoint) — the realization
   is pinned WITH its lowering discipline.
3. MESH LIMIT: S-LBML (claims :660): h^2 dual-pairing convergence to
   the continuum shift-derivative under LB-c1, O3.2 self-monitoring;
   the dual-consistency residue is the P2 Lemma B par.4.5 SCHEMA —
   exactly what pin F11d executes at estimator sites (C56 note: a
   fired F11d re-opens C56, not C11/C49).
4. ALTERNATIVES: separately discretized continuous adjoint
   (Ancourt/Lozano-Ponsin characteristic-native line) LOSES the
   gradient role by the supplement's own one-role rule — it is the
   FORMULATION FRAME + REFEREE ("a weight cannot referee itself";
   the same holds verbatim for a gradient) and its discretization
   would need its own validation chain the discrete route gets for
   free from T-LEMB; dual-consistent synthesis (Hicken-Zingg
   criteria) is not a third realization here — it IS the adopted
   combination (discrete realization + continuous-structure referee
   + F11d execution of the par.4.5 SCHEMA).
5. REFEREE LEGS (armed, of record or pinned): O3.1 dot-product
   identity at derived floor + O3.2 order (of record); F11d leg 1
   (Giles-Pierce analytic oracle) and leg 2 (ACE/L-P Eqs. (30)/(31)
   compatibility residuals — cava A18's instrument, clauses (i)-(iv)
   carried); FD-on-manifold spot probes per cava A15's derived-
   tolerance protocol (never magic 1e-12).

**Consequence for the C56 row at the landing (§4.5):** gradient role
flips from "OWED" to CLOSED-CONVERGED with this panel (and
PANEL_C31TRIO if convergent) as evidence; all three roles then read:
weight CLOSED (C11 supplement), gradient CLOSED (this adjudication),
indicator closed by transitivity — row status can flip MIXED ->
ADJUDICATED-SPLIT (realization converged; F11d = the measured
residue, owned by F2-C11).

### §3.5 Cross-row consistency

C1's chart migration composes with C49/C56: the design-side vjp
gains one constant linear factor (chart Jacobian) — T-LEMB/O3.1 are
untouched (F-C1b verifies exactly this); C2 branch A rides the same
campaign; C3's corner axis feeds the census-lemma window and (via
H-F18) C49's front-inventory question; C49's fit-vs-capture boundary
schema consumes F2-C11's DWR build (wave-1 composition preserved: no
duplicate instrument). No pin in this cluster moves any wave-1 pin.

## §4 PROPOSED VERDICTS + DUTIES (proposals only — landing window
## applies; no registry edits by this panel)

### §4.1 C1 (docs/choice_ledger.yaml:135-145)

- proposed: SINGLE-AUTHOR -> "ADJUDICATED-SPLIT (direction converged
  2026-08-19 wave-2: certified carrier -> B-spline CONTROL-POLYGON
  CHART of the same certified spline space, Bernstein-exact
  admissibility certificates; adoption-of-record GATED on the
  migration campaign; incumbent chart = verdict-bearing until
  F-C1a/b/c pass)".
- **BINDING F2 duty: `F2-C1-CONTROL-CHART-MIGRATION`** — items:
  (0) check-inventory audit: MEASURE which admissibility checks of
  record are sampled vs exact (no assertion about current code
  beyond findings rows); (1) exact conversion (one linear solve) +
  equivalence gate; (2) mechanism-absence twin on the recorded S17
  failing family; (3) Bernstein certificate layer + one-subdivision
  contraction measurement; (4) constraint re-map (cone/angle linear
  sufficient conditions per O-F4/O-F8) + revalidation-scope report
  (named list; solve/march layers claimed untouched BY the
  chart-invariance argument, verified by (1)); (5) seed-protocol
  re-pin (GENO heights-seeds convert exactly; feasible restoration
  in-chart documented); (6) y''-coefficient VD instantiation
  derivation (the W1 theorem instance) committed with the campaign.
- **Falsifiers (pinned now):**
  - F-C1a (mechanism twin): converted chart re-run of the recorded
    failing family (taper seed / steep-thB, GAP-21 trigger class);
    the alternating-curvature statistic (sign-pattern run length +
    amplitude vs the :1293 mechanism form) must sit below the
    derived floor of the certificate layer. FIRES -> the structural-
    removal claim is refuted, flip DEAD, incumbent stands, GAP-21
    re-opens on a new axis.
  - F-C1b (equivalence gate): at fixed constraint set (natural end
    KEPT imposed), the converted chart must reproduce record J and
    map gradients through the constant chart Jacobian at the O3.1
    derived floor, on the recorded S18/S24 walk designs. FIRES ->
    conversion defect; no adoption until repaired; chart-invariance
    claim voided pending root cause.
  - F-C1c (exactness gate): Bernstein sufficient certificates must
    PASS on record-certified optima after at most the derived
    subdivision depth (conservatism ratio measured and reported;
    threshold derivation published BEFORE first use — RC911-3
    discipline transplanted with recalibration); a Bernstein
    certificate still rejecting a record-certified optimum at depth
    -> the exactness half re-opens, sampled checks stay with their
    derived-gap duty.
- Cross-refs: D6 decision UNTOUCHED on its axis (knot placement /
  AFEM skeleton chart-invariant); GAP-21 row = the duty's findings
  carrier; C4/[X-AKNO] unaffected (knot insertion is chart-
  invariant).

### §4.2 C2 (docs/choice_ledger.yaml:147-156)

- proposed: NEVER -> "ADJUDICATED-CONDITIONAL (2026-08-19 wave-2):
  branch A (C1 migration lands) — the end-condition dof dissolves;
  C2 RESOLVES to the listed 'free-end control points' alternative by
  structure; the GAP-5 record numbers (corner -82%, cd 8.2%) become
  measured priors, re-measured at a certified optimum INSIDE
  F2-C1-CONTROL-CHART-MIGRATION (inside derived bands or declared
  design-class correction). Branch B (migration refuted) — row
  re-opens as the live F2 adoption question with the frozen §1(ii)
  criteria; duty `F2-C2-NOTAKNOT-ADOPTION` (persistence at a
  certified optimum + lip-state bands). The BC itself: NOT
  adjudicated this wave (twin remains a diagnostic of record)."
- gated: true (conditional on the C1 campaign; no independent build).

### §4.3 C3 (docs/choice_ledger.yaml:158-164)

- proposed: DECIDED -> "DECIDED (CONFIRMED-RESCOPED 2026-08-19
  wave-2): the clamped tan(thB) end condition stands — neither tree
  challenger touches the row's axis; the gloss 'no genuine
  alternative exists' is NARROWED to this axis of record. The
  attachment-GEOMETRY axis (sharp corner vs floor-radius lip vs
  PM-fan internal data) is NAMED census-lemma territory (CEN-O9
  :415, CEN-O11 :423-424, diff §4.8 P-c :377-382 — agenda row of
  record, F2-exit user pin respected), with the external materiality
  scale carried (KT2015 corner penalty 'hundredth fractions of per
  cent', cava 3.18; P-F21's own 'negligible thrust')."
- **Falsifier pinned FOR the census window's eventual amendment
  (adoptable only with it):** if the floor-radius lip is adopted
  there, its claim ("buys certifiable adjoint regularity for
  negligible thrust", P-F21) must be tested by the sharp-vs-floor
  TWIN: (a) lip-node gradient regularity compared at matched designs
  (the GAP-5 trigger instrument, findings :231); (b) thrust delta
  BELOW the derived bands (else the 'negligible' premise is refuted
  and the amendment re-prices as a design change, not a
  regularization).
- duty: none minted here (agenda row exists of record; H-F18 feeds
  C49's front-inventory question, noted in §3.4).
- gated: false (row confirmed; the open half belongs to the census
  window, not to this row).

### §4.4 C49 (docs/choice_ledger.yaml:653-663)

- proposed: NEVER -> "ADJUDICATED-SPLIT (representation converged
  2026-08-19 wave-2): TWO-TIER architecture of record — fitted-front
  characteristic marching = THE CERTIFICATE BEARER (sole certified
  representation; S1/D2.5 class, per-cell z-space certification,
  Lax/front admissibility, delta_S1 monitors, exact discrete adjoint
  T-LEMB with front terms by construction); adjoint-consistent
  capturing = the EXPLORATION tier, NAMED and BUILD-GATED (F2/F5;
  not scheduled), adjoint-free driving admissible (axis-4
  quarantine); captured-only LOSES W1 by stated reason (certificate
  layer undefinable on smears at comparable rigor; held
  Giles-Ulbrich pp.907/910 page-verified theorem anchor; Lozano-
  shock attribution NOT used per cava C10); implicit tracking
  (HOIST/MDG-ICE; Bonfiglioli-Paciorri) = the NAMED UPGRADE PATH
  with a standing entry gate (robustness at production counts +
  custom_vjp-compatible adjoint story), census 2022-2025. Fit-vs-
  capture boundary INSIDE the certified tier = H-F9's derived
  front-strength policy (front's DWR-estimate contribution vs
  assigned budget line), executable when F2-C11 leg (b) lands."
- **Carrier protocol PINNED NOW (the measured (v) half):**
  Delta_rep = |J_capture - J_fitted| at matched design/phase-set/
  BCs/thermo-tables/J-definition, on pre-registered twin sites (the
  [X-O32]/o33 bench family + any explorer-flagged candidate
  optimum); capture side refined until its own observed-p GCI band
  < the fitted band at the site (else NON-CONCLUSIVE declared, no
  carrier claim); joint band = fitted band + captured GCI band;
  Delta_rep lands as a NAMED (v) model-residual line (diff §2.14).
- **Falsifiers (pinned now):**
  - F-C49a (census escape): a source demonstrating per-cell
    certification + class-membership monitors ENFORCED on a captured
    representation in a production design loop -> W1 re-opens
    against a real precedent (absence claim is bounded by
    Q4/Q7 + an eventual named query).
  - F-C49b (twin carrier): Delta_rep outside the joint band at any
    twin site -> FRONT-INVENTORY AUDIT mandated (captured-field
    front detection vs the marcher's declared topology) BEFORE any
    further certified claim on that design region; at two sites ->
    C49 re-opens on the fitted class's sufficiency. Agreement is
    consumed as NON-REJECTION only (H-F8 doctrine, of record).
  - F-C49c (topology-change trigger): a record instance where the
    marcher cannot instantiate a design's front topology while the
    explorer is unbuilt -> the explorer build escalates from NAMED
    to SCHEDULED (the W2 premise fired in the field).
  - F-C49d (gradient/weight realization): F11d either leg firing
    re-opens C56 (not C49) — shared pin, cited not duplicated.
- **BINDING duty (named, build-gated): `F2/F5-C49-CAPTURE-EXPLORER`**
  — the explorer build + the pinned carrier protocol + F-C49b
  execution; solver class per H-F28 (c)+(b) with (d) as the shelf
  analysis; NOT scheduled this wave (right-sizing: census-enumerated
  sector program covers near-term topology needs; F-C49c is the
  escalation trigger).
- gated: true.

### §4.5 C56 + coordination, candidates, literature rider

- **C56 (:731-740):** owner-field delta proposed at landing —
  gradient role: "OWED TO wave-2" -> "CLOSED-CONVERGED 2026-08-19
  wave-2 (PANEL_C1REP §3.4-bis; realization = discrete AD-adjoint of
  the marched scheme, custom_vjp one-lowering per C48; continuous
  characteristic-native line = formulation frame + referee only;
  referee legs O3.1/O3.2 of record + F11d two legs + cava-A15
  FD-on-manifold protocol; a fired F11d re-opens this row)". DEDUP
  RULE declared: if PANEL_C31TRIO adjudicates the same role from the
  engine side, the landing merges and verifies identity of
  direction; divergence = escalation. Status proposal: MIXED ->
  ADJUDICATED-SPLIT (all three roles closed; F11d = the measured
  residue, owner F2-C11).
- **Candidate row WITHDRAWN:** the partial's §1-bis axis-2 candidate
  (adjoint-realization row) is moot — C56 exists (measured this
  window); no mint.
- **Note candidate (not minted, landing's call):** EXACT-vs-SAMPLED
  admissibility certificates as a certificate-discipline PATTERN
  (Bernstein instance here; recurs at front-admissibility checks) —
  one-line note at the certificate-discipline site if the landing
  wants a carrier; otherwise it lives inside F2-C1 item (3).
- **Literature rider (WANTED tier per procurement discipline; all
  grep-proven absent from the registry this window, §2.4):** Kulfan
  CST (2008); Masters et al. AIAA J parametrization comparison;
  Hicks-Henne (1978); Huang-Zahr JCP 2022 (robust HOIST) + arXiv
  2304.11427 (2023); MDG-ICE (Corrigan-Kercher-Kessler line, arXiv
  2311.00701); Onofri-Paciorri (eds.), Shock Fitting (Springer
  2017); PAS parametrization review 2025 (S0376042125000661); arXiv
  2505.09088 (2025, DFO-competitive). Ancourt 2023 / Giles-Pierce
  2001 / Giles-Ulbrich 2010 I-II / Lozano-Ponsin 2025 need NO new
  rows (HELD, statuses measured §2.2).

## PAPERS NEEDED (procurement channel, §0-bis (c))

1. **Masters, Poole, Taylor, Rendall, Allen — "Geometric Comparison
   of Aerofoil Shape Parameterization Methods", AIAA Journal** —
   full text needed to verify the 20-25-dof design-space-coverage
   claim used in the C1 census at [ABS]; the C1 exploration-tier
   dof-budget guidance waits on it (no adopted claim leans on it).
2. **Huang & Zahr, "A robust, high-order implicit shock tracking
   method ...", JCP 2022 (arXiv 2105.00139) + "High-order implicit
   shock tracking boundary conditions for flows with parametrized
   shocks", arXiv 2304.11427** — full texts needed before any W4
   entry-gate re-evaluation (the upgrade-path entry conditions are
   pinned; their assessment against the actual robustness evidence
   waits on full text).
3. **"A review of parameterization methods for airfoil design",
   Progress in Aerospace Sciences 2025 (S0376042125000661)** — the
   modern refinement layer of the C1 census is represented here only
   at [TITLE]; the census-completeness claim for C1's modern layer
   upgrades from query-bounded to source-verified with it.
4. **Onofri & Paciorri (eds.), "Shock Fitting: Classical Techniques,
   Recent Developments, and Memoirs of Gino Moretti", Springer
   2017** — the C49 upgrade-path census cites the unstructured-
   fitting line at [ABS]; the "no adjoint story in this line"
   absence claim would upgrade from query-bounded with the book's
   survey chapters. Priority below 1-3.

## §5 MACHINE SUMMARY

Counting rule for `alternatives_closed` (stated here per the wave-1
RC27-8(vi) precedent): one count per named alternative CLOSED by
stated reason (rejected, re-scoped, subsumed, or folded), adopted
directions not counted. C1: Hicks-Henne; CST; two-tier V-F13
(re-scoped); H-F34(f) (subsumed); SVD/modal charts; ML-generative
charts = 6. C2: not-a-knot (branch-A structural close, conditional)
= 1. C3: P-F21 (re-scoped); H-F18 (re-scoped) = 2. C49:
captured-only; implicit-tracking-as-certificate-tier;
continuous-adjoint-as-gradient/weight (C56 one-role rule);
dual-consistent-as-third-realization (folded into the adopted
combination); adjoint-free-as-certificate-route (closed to the
exploration tier) = 5. Total = 14.

```json
{
  "cluster": "C1REP",
  "rows": {
    "C1": {"proposed": "ADJUDICATED-SPLIT: certified carrier -> B-spline control-polygon chart of the SAME certified spline space (chart change, not class migration); W1 theorem-grade mechanism removal (interpolation-solve root -(2-sqrt(3)) structurally absent + variation-diminishing bound) + W2 Bernstein-exact certificates; incumbent chart verdict-bearing until F-C1a/b/c pass; D6 untouched on its own axis", "gated": true, "duty": "F2-C1-CONTROL-CHART-MIGRATION"},
    "C2": {"proposed": "ADJUDICATED-CONDITIONAL: branch A (migration lands) -> dissolves to free-end control points by structure, GAP-5 numbers re-measured as priors inside the C1 campaign; branch B (migration refuted) -> live F2 adoption question per frozen criteria; BC itself not adjudicated", "gated": true, "duty": "rides F2-C1-CONTROL-CHART-MIGRATION; branch B = F2-C2-NOTAKNOT-ADOPTION"},
    "C3": {"proposed": "DECIDED CONFIRMED-RESCOPED: clamped tan(thB) stands (neither P-F21 nor H-F18 touches the row's axis); gloss narrowed; attachment-GEOMETRY axis named census-lemma territory (CEN-O9/O11, diff 4.8 P-c) with KT2015 hundredth-fractions materiality scale and the sharp-vs-floor twin falsifier pinned for any future amendment", "gated": false, "duty": null},
    "C49": {"proposed": "ADJUDICATED-SPLIT: two-tier architecture of record — fitted-front marching = sole certificate bearer (W1: captured-only loses by stated reason, Giles-Ulbrich pp.907/910 held anchor, Lozano attribution NOT used per cava C10); captured explorer = named build-gated tier with |J_capture-J_fitted| carrier protocol + falsifiers pinned now; implicit tracking = named upgrade path with standing entry gate; fit-vs-capture boundary = H-F9 DWR-budget policy via F2-C11 leg (b)", "gated": true, "duty": "F2/F5-C49-CAPTURE-EXPLORER (named, build-gated, not scheduled)"},
    "C56": {"proposed": "gradient role CLOSED-CONVERGED (owed forecast consumed HERE per task prompt, said explicitly): realization = discrete AD-adjoint of the marched scheme (T-LEMB :489, C48 one-lowering, S-LBML :660); continuous line = frame + referee only; F11d re-opens this row; dedup rule with PANEL_C31TRIO declared (landing merges, divergence = escalation); status proposal MIXED -> ADJUDICATED-SPLIT", "gated": false, "duty": "none new (pins live in F2-C11/F11d + O3.1/O3.2 of record)"}
  },
  "census_recency": "1975-2025; every row >= 2024 on at least one axis; C49 tracking axis reaches 2025 (arXiv 2504.15177); C1 reaches 2025 (PAS review [TITLE], arXiv 2505.09088); 8 web queries + measured in-repo greps, table in §2.0",
  "alternatives_closed": 14,
  "inflation_check": "done",
  "reconciliation": {"partial_sections": ["header", "§0", "§1", "§1-bis"], "verified_clean": "all §0/§1 anchors re-read at source (diff/findings/claims/M0/D6/census-pins/trees)", "repairs": ["R1 ledger line refs re-measured (SR-12)", "R2 diff anchor :282-293 -> :288-293", "R3 §0 extended with landed C9/C11 supplement + C56", "R4 §1-bis axes 2/3 amended (candidate row withdrawn, C56 consumed; axis-3 census-grade closure cited)"], "added": ["§2", "§3", "§4", "PAPERS NEEDED", "§5", "declaration"]}
}
```

## RECONCILIATION DECLARATION

**What was on disk at read time (partial, mtime 2026-08-19 12:55,
15,031 bytes, 268 lines):** file header; §0 WAVE-1 CONSUMPTION +
CONTEXT OF RECORD; §1 FROZEN FORMAL STATEMENT (shared sub-problem +
C1/C2/C3/C49 main questions, pre-registered criteria, materiality);
§1-bis PER-ROW AXIS BEARING. NOTHING ELSE — no census, no
adjudication, no verdicts, no machine summary. The partial was
consumed as UNVERIFIED DRAFT INPUT per BRIEF_wave2_reconcile.md.

**What verified clean (kept verbatim):** the §1 frozen questions,
all pre-registered decision criteria, materiality statements, and
§1-bis axes (1)/(4)/(5). Verification performed at source this
window: diff anchors :22-33/:34-37/:38-44/:294-299/:330-335/
:377-382/:159-167 re-read; findings :1290-1299 and :223-231 re-read
(mechanism text, -82%/8.2% numbers, Q2 quarantine verbatim); claims
:489/:660/:1484 re-read (T-LEMB/S-LBML/PAP-P2LB statements); M0
:185-198/:324-327 re-read (D2.5, delta_S1); D6 :915-940 re-read
(rejection text at :936-940 verbatim); census pins :355-356/:415/
:423-424 re-read; tree advocacy spot-checked at the condensed maps
with full-tree line anchors (O-F4 [L298], O-F8 [L516], O-F9 [L566],
O-F30 [L1488], V-F6 [L317], V-F13 [L605], V-F27 [L1207], H-F4
[L209], H-F8 [L328], H-F9 [L364], H-F10 [L396], H-F18 [L622], H-F28
[L874], H-F29 [L901], H-F34 [L1026], P-F12 [L474], P-F19 [L748],
P-F21 [L821]); wave-1 verdict quotes re-read (§2.1 gates-absolute +
"a certificate is not tradeable", §2.3/2.4, §3 shared-artifact
composition).

**What was repaired (each declared in place):**
- [RECONCILE-R1] Ledger line refs in §0 context-of-record were the
  brief's PRE-LANDING refs (stale by +2 after the C9/C11/C56 landing
  edits shifted the file). RE-MEASURED this window (SR-12): C1
  :135-145, C2 :147-156, C3 :158-164, C49 :653-663, C9 :219-228,
  C11 :240-250, C56 :731-740.
- [RECONCILE-R2] Diff anchor for §2.13 front-terms tightened
  :282-293 -> :288-293 (:282-287 is item §2.12; re-read).
- [RECONCILE-R3] §0 EXTENDED with the consumption the relaunch
  mandate added: the landed C9/C11 supplement closures
  (VERDICT_C9C11_supplement.md §2 axes A/B, §4.1-4.3) and row C56 —
  post-partial events, addition not correction.
- [RECONCILE-R4] §1-bis axis (2): the partial's candidate-row
  proposal ("no ledger home ... candidate row proposed in §4.5")
  WITHDRAWN as moot — C56 exists (minted at the landing, after the
  partial was written); consumption replaces proposal. Axis (3): the
  structural note is now CONFIRMED at census grade by the C9
  supplement — citation added, self-standing adjudication language
  removed.

**What was added (the partial had none of it):** §2 SOTA census
(8-query protocol table, recency, read-depth-marked entries,
per-row cava rows, measured absence greps); §3 per-row adjudication
incl. §3.4-bis C56 gradient-role consumption (explicit, with dedup
rule); §4 proposed verdicts + duties + pinned falsifiers
(F-C1a/b/c, C2 branches, C3 twin falsifier, F-C49a/b/c/d) +
candidate/rider dispositions; PAPERS NEEDED (4 asks); §5 machine
summary; this declaration.

**Discharge:** with the above, the on-disk partial's unverified
status is DISCHARGED for this rewritten file; every kept sentence
was re-verified, every repair is declared above and tagged in place,
and everything else is new work of this window. Null=failure: this
file exists, is complete per the panels-brief schema + §0-bis/§0-ter
additions, and ends with the machine summary (§5) followed only by
this declaration required by the reconcile brief.
