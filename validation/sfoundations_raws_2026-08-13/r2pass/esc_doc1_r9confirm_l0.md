# ESCALATION REFUTATION — DOC-1, CONFIRMING ROUND on REVISION 9, lens l0
# (hyperbolic systems / functional-analytic rigor; ID prefix R9L0-)

Date: 2026-08-19. Target: `phaseD/phaseD_stop_proof.md` AT REVISION 9 —
ONLY the revision-9 delta (the consumption of ECL0-1..3 + ECR1-1..4 per
VERDICT_confirm §1 DOC-1 / §6 R-1'). Delta inventory, grep-measured in
this window ("revision 9|revision-9" over the target): header carrier
l.26 + l.110-116; TOPOLOGY-COMPLETED slice-connectedness clause +
two-branch witness recital in the condition of record, l.2162-2215, and
the three-chart-clause summary l.2216-2219; the interval-slice gloss in
the totality clause, l.2172-2173; the parity restatement l.2178-2187;
the ECR1-2 witness-regime restatement l.2255-2266; the assembly
continuity splice l.2296-2300; the AUD-cp closure sentence l.2368-2376;
the corrected falsifier consequent l.2399-2437; the propagated grant
head + item-(i) parenthetical l.2443-2459; the honest status line
l.2505-2528; the §9 gamma-table carrier l.2738-2749; the §13
annotations (E4L0-2 falsifier l.3926-3933; ER3L1-1/E4L0-4 witness
regime l.3976-3983; E4L0-3+ER3L1-3 caveat inversion + else-branch
remainder l.4035-4050; leg-3 recital l.4101-4109; seeding count
l.4150-4157); the §13 REVISION 9 block l.4179-4459. This file is the
l0 half of the two-lens confirming round owed by VERDICT_confirm §6
R-1'. Context read in full: VERDICT_confirm.md (§1 DOC-1, §2, §3, §5,
§6, §7), esc_doc1_confirm_l0.md, esc_doc1_confirm_l1.md.

COVERAGE CHECK, declared: no esc_* file on disk targets revision 9
(grep over r2pass/: only VERDICT_confirm.md names it, as the owed
consumption) — this is the first adversarial pass on the rev-9 state;
`esc_doc1_r9confirm_l0.md` was an unclaimed filename.

DEDUP REGISTER (dispositioned objections NOT re-raised): L0-1/L1-1,
L0-2, L1-2 (r2-batch); EL0-1==ESC1-F1, EL0-2, EL0-3==ESC1-F2, EL0-4
(round 1); E2L0-1, E2L0-2 (round 2); E3L0-1, ER2L1-1, E3L0-2 (round-3
window); ER3L1-1..3, E4L0-1..4 (round-4 window); ECL0-1..3,
ECR1-1..4 (confirming round, consumed at revision 9 — discharge
adequacy examined below; where a disposition partially fails, the ID
is cited with the failure point stated, per the standing dedup rule).
Both duties executed: (a) derivation-level attack on the revision-9
repair text AS repair-spec-derived text (the standing lesson, honest
residue (2)); (b) verification that the revision-9 repairs DISCHARGE
the six judge-merged findings they answer and that standing
dispositions hold at revision 9.

Every attacked passage: verbatim quote + attack + class in
{BREAKS-THE-LEG, REPAIR-NEEDED, AMENDMENT}. Every examined delta
passage not broken: CONFIRMED with the strongest attack tried.
Finding IDs: R9L0-1..R9L0-3. Line numbers are of the revision-9 file.

==============================================================================
## §R4 BINDING RULE CHECK (VERDICT_confirm §6 R-1', "every rev-9
## clause is restated against the row's own hypotheses" — declared per
## entry in the rev-9 block). Executed clause-by-clause on the delta:

| Rev-9 repair clause | Provenance | Restated? | Verdict |
|---|---|---|---|
| Slice-connectedness clause, membership + parity re-scope (l.2187-2195, 2178-2187) | ECL0-1 repair spec + VERDICT_confirm R-1' owed spec | YES — written on the row's own objects ({S : (v(W_s), S) in dom e}, hull v-values, traversed-interval hypothesis explicit) | PASS |
| Slice-connectedness clause, CONTINUITY conclusion (l.2196-2202) + assembly splice (l.2296-2300) | Same spec's "the root map is continuous on the compact hull" line (carried verbatim into the judge's R-1' spec) | NO — the spec's continuity mechanism ("a root sequence escaping along the slice would contradict continuity of e at a fixed intermediate S") was authored on the product-domain witness picture (v-constant fibers, ECL0-1's own witness) and the conclusion was transplanted with that assumption intact; it MISFITS the row's domain-unpinned EOS class, where the fiber family moves with v | **MISFIT → R9L0-1** |
| Totality-gloss interval annotation (l.2172-2173) | ECL0-1 | YES — one qualifier on the row's own gloss | PASS |
| Witness determinant regime (l.2255-2266) | ECR1-2 spec / ER3L1-1's own text | YES — restored from the row's own (m2) family objects, regime + failure regime both printed | PASS |
| Falsifier-consequent member arithmetic (l.2399-2437) | ECL0-2/ECR1-1 | Restated from the row's four-member condition as declared, BUT the restatement mints a fresh over-count: the root-quantifier member is lumped with the geometry clauses as pin-undeliverable | **restatement defect → R9L0-2** |
| Grant-head epithet + item-(i) parenthetical (l.2443-2459) | ECR1-3's two-phrase spec ("under the STRICT + hull-theta + CHART-COMPLETE condition of record") | Transplanted AS SPECIFIED — but the spec was calibrated against the revision-8 condition while REVISION 9 ITSELF enlarged it by the topology member; the same revision's other carriers mint a separate "TOPOLOGY-COMPLETE(D)" epithet the grant head does not carry | **calibration MISFIT → R9L0-3** |
| §13 ledger annotations (l.4101-4109, 4150-4157) | ECL0-3/ECR1-4 specs | YES — arithmetic re-verified below | PASS |
| Carriers (header l.110-116, §9 l.2738-2749, status line l.2505-2528, AUD-cp l.2368-2376, label summary l.4420-4428) | — | PASS as carriers, modulo the epithet divergence consumed at R9L0-3 | PASS* |

R-4 verdict: the pattern's NEXT declared instances (doc1-local ninth
and tenth, after ECL0-1 = seventh and ECL0-2 = eighth; the cross-doc
count of record is "eight-plus", VERDICT_confirm R-1'). The rev-9
honest-residue (2) claim "every revision-9 clause above is RESTATED
against the row's own hypotheses" is TRUE of the membership/parity
restatement, the witness regime, and both ledger corrections, and
FALSE of the continuity conclusion and the grant-head epithet
calibration.

==============================================================================
## LEG 5 (E-2) — the revision-9 topology clause, consequent
## arithmetic, grant-head propagation, and ledger corrections

### 5.1 The slice-connectedness clause's CONTINUITY conclusion —
### **OBJECTION R9L0-1, REPAIR-NEEDED** (interval slices do not
### deliver root-map continuity across v; in-class witness satisfies
### EVERY revision-9 member while item (i) fails quantitatively)

Quote 1 (condition of record, l.2191-2202): "each S-slice
{S : (v(W_s), S) in dom e} over a hull v-value that the roots of
eps(W_s) = e(v(W_s), .) meet is an INTERVAL — under which the parity
bracketing runs (the traversed interval IS in the domain), the root
is unique, the (v, S)-image and the chart are single-valued, the
root map W -> S(W) is continuous on the compact hull
(single-upcrossing structure: e(v, .) - eps < 0 before and > 0 after
the unique root — the continuity the assembly's compactness step and
the bridge's uniformity clause consume), and each per-segment
continuation lands on the DATA root".

Quote 2 (the assembly's consumption splice, l.2296-2300): "continuity
of Hess_W E (EOS C^2 — a §1-implicit consumption, now stated — with
the root map W -> S(W) single-valued and continuous on the hull by
the three chart clauses, revision 9 per ECL0-1) plus compactness of
the hull's state image give the uniform lower eigenvalue bound".

Quote 3 (the substitute mode, l.2213-2215): "where
slice-connectedness is not available, root UNIQUENESS is imposed
directly as a member in its place".

Attack (derivation level). "Under which ... the root map W -> S(W)
is continuous on the compact hull" is a DERIVATION claim (interval
slices + the other members => continuity), and it is FALSE: the
single-upcrossing structure is a PER-SLICE (fixed-v) statement — it
localizes the root within one fiber and controls NOTHING across v
when the fiber family itself moves discontinuously. §1 pins no EOS
domain (established, sustained, and consumed of record: ER3L1-2,
ECL0-1 — the rev-9 ECL0-1 entry's own falsifier line repeats it), so
nothing printed constrains how the S-fibers vary with v. In-class
witness, fully explicit, satisfying EVERY member of the revision-9
condition of record while the root map JUMPS:

  dom e = ([v_min, v*] x [0, 1]) ∪ ((v*, v_max] x [10, 11]),
  e = e0(v) + h_A(S) on the first strip, e = e0(v) + h_B(S) on the
  second; e0 strictly convex (e0'' > 0); h_A(S) = S + S^2 on [0, 1]
  (h_A' = 1 + 2S ∈ [1, 3] > 0, h_A'' = 2 > 0, range [0, 2]);
  h_B(S) = (S - 10) + (S - 10)^2 on [10, 11] (identical derivative
  and range profile: h_B' ∈ [1, 3] > 0, h_B'' = 2 > 0, range [0, 2]).
  e is C^2 on its domain (each strip separately; the strips are
  disjoint). Gibbs closure holds by construction (p := -e0'(v),
  theta := e_S — the construction pattern of the accepted (m2)/(m3)
  and ECL0-1 witnesses); c^2 = v^2 e0'' > 0; theta = h_i' > 0
  EVERYWHERE ON THE DOMAIN, in particular at every S-root
  (root-quantifier member SATISFIED); Hess e = diag(e0'', 2) PD
  everywhere (strict pair + hull-theta members SATISFIED);
  SLICE-CONNECTEDNESS SATISFIED — every S-slice is a single interval
  ([0, 1] over v <= v*, [10, 11] over v > v*; no v carries both);
  choose segment endpoints with v_U < v* < v_V and arrange the
  hull's eps - e0(v) values inside (0.1, 1.9) ⊂ (0, 2) (the same
  arrangement step as ECL0-1's accepted witness) — TOTALITY member
  SATISFIED at every hull point via whichever strip covers its
  v-value, and each hull point has exactly ONE root (per-slice
  monotone: h_i' > 0), so the chart is even SINGLE-VALUED.

Then: (i) "the root map W -> S(W) is continuous on the compact hull"
FAILS — S(W) ∈ [0, 1] for v(W) <= v* and S(W) ∈ [10, 11] for
v(W) > v*: a jump of magnitude ~10 at every hull point with
v(W) = v*, which any segment with v_U < v* < v_V crosses (v = 1/rho
is continuous along W-segments). (ii) The assembly's chain (Quote 2)
dies at its printed step: E(W) = -rho S(W) is not even CONTINUOUS on
the hull, so "continuity of Hess_W E ... plus compactness ... give
the uniform lower eigenvalue bound lambda_min > 0" has no premise,
and the Bregman double integral runs through a discontinuity.
(iii) Item (i) fails QUANTITATIVELY, not just ill-posedly: for U, V
close across v = v*, E(V|U) = E(V) - E(U) - D_W E(U).DeltaW equals
the smooth branch-A Bregman remainder (O(|DeltaW|^2), small) MINUS
rho_V (S_B(V) - S_A,land) ≈ -10 rho_V < 0 — the grant
E(V|U) >= c|DeltaW|^2 is violated with EVERY printed member
satisfied. (iv) "each per-segment continuation lands on the DATA
root" fails in mode: the branch-A continuation cannot be continued
past v = v* (no nearby root); continuation failure is not landing.
(v) The substitute mode (Quote 3) inherits the same gap MORE
visibly: with root uniqueness imposed directly, even the claimed
single-upcrossing mechanism is absent (it needs the interval
hypothesis), yet the assembly (Quote 2) still cites "the three chart
clauses" for continuity — in that mode NO printed mechanism backs
the consumption at all. (vi) The parity/uniqueness/single-valuedness
conclusions of Quote 1 are NOT attacked — they are delivered per
slice and confirmed below (5.4a); the defect is one rung further
out: cross-v REGULARITY of the fiber family, which no printed member
constrains.

Where the transplant broke (R-4, declared in §R4): the confirming
l0 spec's pen-grade continuity argument — "a root sequence escaping
along the slice would contradict continuity of e at a fixed
intermediate S" — takes the intermediate S to lie in the slices over
NEARBY v-values, which holds when the fibers are (locally) constant
in v (the product domain (v-range) x (I1 ∪ I2) of ECL0-1's own
witness) and fails in the row's unpinned class, where the root
sequence lives in fibers disjoint from the limit fiber. The judge's
R-1' owed spec carried the same conclusion verbatim ("under which
the parity bracketing runs and the root map is continuous on the
compact hull"), and revision 9 transcribed it faithfully — a
spec-seeded defect (this window's seeding event, both the refuter's
clause and the judge's spec carry it) and the R-4 pattern's ninth
doc1-declared instance.

Class: **REPAIR-NEEDED** — by the loop's own severity calculus, the
exact ECL0-1/E3L0-2 shape one rung further out: a presupposition
reading of "single-upcrossing structure" rescues nothing (it is
per-slice under every reading), and the witness satisfies every
printed member while the granted item (i) is FALSE — a missing
CONDITION MEMBER, not wording. NOT BREAKS-THE-LEG: the route-level
OPEN verdict is unmoved; the repair is the SEVENTH consecutive
strengthening of the no-viable-abstract-EOS-route conclusion
(L1-2 -> EL0-3/ESC1-F2 -> E2L0-1 -> E3L0-1+E3L0-2 ->
E4L0-2+ER3L1-2 -> ECL0-1 -> R9L0-1); and the clause is FREE at the
standing gamma(T) closure: there the S-fiber over v is
[phi(T_min) + R ln v, phi(T_max) + R ln v] — the R ln v-shifted
image of the FIXED tabulated interval, endpoints continuous in v —
so the fiber family varies continuously, the fibered domain is
closed over compact v-ranges, and any root-sequence limit point is
a root of the limit fiber, forced to THE root by uniqueness: the
root map is continuous there and the AUD-cp discharge is untouched.

Named repair (one member + carrier pass, mirroring the condition's
own substitute-mode pattern): FIBER-REGULARITY written as the
topology rung's companion — "over the hull's v-range the S-fiber
family of dom e varies continuously (fiber endpoints continuous
functions of v, fibers closed intervals — equivalently dom e over
the hull v-range closed and v-fibered with continuously varying
interval fibers); under it the root map IS continuous on the compact
hull (closed fibers + per-slice uniqueness + continuity of e: a root
sequence is bounded, any limit point lies in the limit fiber and
solves e = eps, hence equals the unique root — checked at pen grade
above for the closure instance); where fiber regularity is not
available, root-map CONTINUITY on the hull is imposed directly as a
member in its place." Same-pass propagation (the ECR1-3 lesson):
Quote 1's "under which ... continuous" re-scoped to the new member;
the assembly splice (Quote 2); the falsifier-consequent enumeration
(l.2403-2404) and the grant-head epithet; header l.110-116; §9
table; honest status line; AUD-cp note (one clause: fibers
continuous at the closure, as derived above); the §13 ECL0-1 entry's
continuity line annotated in place; and the domain-ladder sentence
(l.4251-4254) — "outermost rung" is falsified by this finding and
extends: K -> hull -> theta-pin region -> chart domain (totality) ->
chart-domain TOPOLOGY (slice-connectedness) -> chart-domain
REGULARITY (fiber continuity across v).

DEDUP DECLARATION: partial-failure citation, not a re-raise.
ECL0-1's disposition FAILS at exactly its continuity conclusion (the
delivered interval member closes the multivaluedness/parity/landing
mechanism ON EACH SLICE — confirmed at 5.4a); ER3L1-2 (existence)
and E4L0-2 (root multiplicity) stay delivered; ECR1-1/ECL0-2's
consequent arithmetic inherits this finding only through the member
list it enumerates (carried here at source, not double-counted).

### 5.2 The corrected whole-domain consequent's member taxonomy —
### **OBJECTION R9L0-2, AMENDMENT** (the root-quantifier member is
### NOT domain/range geometry: the whole-domain pins-reading
### delivers it; the printed consequent over-counts the standing
### moduli — conservative direction, no false-restoration power)

Quote (l.2399-2412; §13 echo l.4274-4283): "THAT kills the
condition-claim and restores the grant's PIN-READABLE members IN THE
WHOLE-DOMAIN BRANCH — the Hessian member and hull-theta, where the
theta pin itself goes hull-wide — MODULO the chart clauses
(totality, root-quantifier, slice-connectedness): those are
domain/range GEOMETRY no reading of the pins and no concavity
derivation delivers (pins constrain e's derivatives where e is
DEFINED, never where it is defined; witness: the row's own accepted
patch-EOS class with a0 large POSITIVE holds every §1 pin on its
entire domain while a thin-S-band hull exits the range), so the
chart clauses stay STANDING members with their own §13 falsifiers".

Attack (derivation level — member-by-member audit of the "no
pins-reading delivers" claim). TRUE for totality and
slice-connectedness: both are facts about WHERE e is defined (range
coverage; fiber topology), and the printed witness kills delivery
exactly as stated. FALSE for the ROOT-QUANTIFIER member: as defined
(l.2178-2180), it reads "theta > 0 ... at EVERY S-root of
eps(W) = e(v, S) over each hull point" — a POINTWISE theta condition
at points that are, BY DEFINITION, points of dom e (a root is a
domain point where e meets the level). Under the whole-domain
branch's own proviso ("PROVIDED the pins are read on the whole EOS
domain", l.2390-2391), theta > 0 holds at every domain point, hence
at every root: the member is DELIVERED by the pins-reading, with no
geometry input. What is NOT delivered is the member's downstream
uniqueness consequence — but the condition itself attributes that to
the interval hypothesis ("which — ON AN INTERVAL S-SLICE — forces
the root to be UNIQUE", l.2180-2181), and slice-connectedness
correctly stays standing. So the honest whole-domain arithmetic is:
restores Hessian + hull-theta + root-quantifier, MODULO totality and
slice-connectedness (and, post-R9L0-1, fiber regularity). The
printed witness clause supports only the totality half; NO witness
supports pin-non-delivery of the root-quantifier member, because
none exists. A collective reading ("no pins-reading delivers the
chart clauses [as a bundle]") is available and true — named and
weighed: the sentence's own continuation ("stay STANDING members
with THEIR OWN §13 falsifiers") individuates the members, and the
row's five-times-enforced written-line standard plus the ECL0-2
precedent (where a strained benign reading of this same clause was
named and rejected one revision ago) make the distributive reading
govern member arithmetic.

Class: **AMENDMENT** — auxiliary falsifier-consequent text, the
unbroken lineage class (EL0-4, E2L0-2, E4L0-3/ER3L1-3, ECL0-2/ECR1-1
— all AMENDMENT of record). Direction is CONSERVATIVE (the standing
residue is overstated, not erased): no false-restoration power, no
proof content, statement, or label exposed — which caps severity;
the loop nonetheless prices conservative mis-statements (EL0-4
precedent: sustained and repaired despite the conservative
direction). Note this defect was MINTED BY the revision-9
restatement itself (the rev-8 text did not enumerate members at
all): the exact posture honest residue (2) mandates — attacking the
restated text as new text — is what finds it. Repair, one clause at
both sites: "restores the grant's PIN-READABLE members — the Hessian
member, hull-theta, and the root-quantifier member (theta read at
roots, which are domain points the whole-domain reading covers) —
MODULO totality and slice-connectedness: domain/range GEOMETRY no
reading of the pins and no concavity derivation delivers ...", the
uniqueness consequence staying attributed to the interval member.

DEDUP DECLARATION: not a re-raise of ECL0-2/ECR1-1 — their two
mechanisms (inverted totality caveat; else-branch under-enumeration)
are DISCHARGED (verified at 5.4c); neither of their registered §13
falsifiers covers this site (deriving totality from pins; else-branch
chart presupposition — both stand). The else-branch text
("hull-theta AND the chart clauses, both consumed or presupposed as
inputs") is NOT attacked: an s-concavity-at-hull conclusion
presupposes the chart's definedness and single-valuedness, and
keeping the underived members standing there is correct.

### 5.3 The propagated grant-head epithet vs the same revision's own
### carrier taxonomy — **OBJECTION R9L0-3, AMENDMENT** (the ECR1-3
### spec transplanted at rev-8 calibration while revision 9 itself
### enlarged the condition; one token, divergent enumerations across
### four same-revision carriers)

Quote 1 (grant head, l.2443-2445): "and under the STRICT +
hull-theta + CHART-COMPLETE condition of record".

Quote 2 (the same revision's label summary, l.4424-4428): "the
thermal-stability piece now in the STRICT + hull-theta +
chart-complete + TOPOLOGY-COMPLETE form (slice-connectedness
member)".

Quote 3 (the same revision's §9 carrier, l.2738-2744, and header
l.107-116): "CHART-COMPLETED — totality + root-uniqueness clauses
added ... in revision 8 ...; TOPOLOGY-COMPLETED — slice-connectedness
member added ... in revision 9".

Quote 4 (the §13 ECR1-3 entry's own claim, l.4354-4357): "the §9
gamma-table carrier is extended in the SAME pass (TOPOLOGY-COMPLETED
per revision 9) so no carrier of the revision-9-enlarged condition
is left stale — the ECR1-3 pattern not reproduced one revision
later."

Attack. ECR1-3's repair spec ("under the STRICT + hull-theta +
CHART-COMPLETE condition of record") was authored against the
REVISION-8 condition; revision 9 transplanted it verbatim into the
grant head while the SAME revision enlarged the condition by the
topology member — the exact calibration mechanism of
ECL0-2/ECR1-1 ("calibrated against the prior member list while the
same revision enlarged the condition") and of ECR1-3 itself,
reproduced at the very sentence ECR1-3 attacked, against Quote 4's
explicit denial. Under the document's own epithet taxonomy of
record (Quotes 2-3: CHART-COMPLETED names the rev-8 axis;
TOPOLOGY-COMPLETED names the rev-9 axis, minted as SEPARATE in the
header, §9, and label summary), the grant head enumerates three of
four axes; a consumer importing that taxonomy reads "CHART-COMPLETE"
as the rev-8 two-clause bundle and claims the grant on ECL0-1's
two-branch witness — the enumeration exposure surviving at exactly
the member the round added. Honest residue (4) prints a FOURTH
variant of the same name ("hull-theta + chart-total-and-topology-
complete STRICT joint thermal-stability condition", l.2513-2519 has
a fifth: "the revision-8/9 clauses incl. slice-connectedness") —
one condition, four printed enumerations in one revision, the
executor-facing single-token/two-contents species prosecuted at
AMENDMENT in this same round's batch (doc2 CR-2≡R4L1-1 precedent,
VERDICT_confirm §1).

Mitigations, declared (why this sits at the BOTTOM of the AMENDMENT
band and not higher): the grant head's own item-(i) parenthetical
cites ECL0-1 among the genuine additions (l.2455-2459), the body
labels slice-connectedness "the chart's THIRD clause" (l.2187-2188),
and the header calls topology-completion "slice-connectedness added
as the chart's third clause" (l.110-112) — so a benign defined-term
reading exists ON THE PAGE under which CHART-COMPLETE = all three
chart clauses and every carrier denotes the same defined singular
object; no consumer that follows the definition moves. That is the
ECR1-3 precedent's exact rescue, which held IT at AMENDMENT — the
class carries over unchanged.

Class: **AMENDMENT**. Repair, one word plus harmonization: grant
head "under the STRICT + hull-theta + CHART-COMPLETE +
TOPOLOGY-COMPLETE condition of record" (or, alternative route:
retire the separate TOPOLOGY epithet everywhere and let
CHART-COMPLETE denote the three-clause bundle in header/§9/§12/§13
alike — either route, applied to ALL five printed variants in the
same pass; the R9L0-1 repair will touch the same sentences and
should carry this with it).

DEDUP DECLARATION: not a re-raise of ECR1-3 — its two demanded
phrases were delivered verbatim and its vacuity mechanism is closed
(totality is inside every reading of the epithet); the defect is the
transplant's CALIBRATION against the same-revision enlargement, a
new fact of revision 9, prosecuted per the R-4 posture.

### 5.4 CONFIRMED delta passages (strongest attacks tried and failed)

**(a) The slice-connectedness clause's per-slice deliveries + the
restated parity line (l.2178-2195, 2213-2215)** — attacks:
(i) parity mechanism re-derived at pen grade: two roots S1 < S2 in
one interval slice with theta > 0 at both — e - eps > 0 just after
S1 (increasing through zero), < 0 just before S2; continuity on
[S1, S2] ⊂ dom gives a first downward crossing S*, where
e - eps > 0 on (S1, S*) and = 0 at S* forces e_S(S*) <= 0 for C^1
e: an intermediate root with e_S <= 0 — EXACT, and the
traversed-interval hypothesis is now EXPLICIT as demanded;
(ii) uniqueness => single-valued image and chart: immediate;
(iii) single-upcrossing PER SLICE: sign pattern anywhere else in the
slice would mint a second root by IVT — exact; (iv) the substitute
mode delivers uniqueness by fiat — well-formed as a member. The
cross-v continuity conclusion is the ONLY broken clause (R9L0-1);
everything per-slice survives. CONFIRMED (minus R9L0-1's clause).

**(b) The two-branch witness recital in the body (l.2204-2211)** —
attacks: every number re-checked (h_1 = 5^S - 1: h_1' = 5^S ln 5 > 0,
h_1'' > 0, range [0, 4]; h_2: range [1, 3.1] ⊂ (0, 4), h_2' ∈
[2, 2.2], h_2'' = 0.2; Hess diag PD; totality via the h_1 branch;
two-valued chart at S_V = 10.5) — matches the judge's §5(i)
verification; the recital transcribes ECL0-1's witness faithfully.
CONFIRMED.

**(c) The corrected consequent's OTHER two mechanisms
(l.2399-2437)** — attacks: (i) inverted-caveat fix: "no reading of
the pins ... delivers [totality]" with the a0-POSITIVE patch witness
— the witness checks (theta = a0 + a1 v + bS > 0 domain-wide for a0
large positive on a bounded patch; c^2 = v^2 e0'' > 0; Gibbs by
construction; a thin S-band's range misses mid-segment eps values):
delivery claim correctly RETRACTED, matching ECR1-1's demand;
(ii) else-branch enumeration: an s-concavity conclusion at hull
states presupposes s defined and single-valued there — concavity of
an undefined or multivalued function is not a statement; the grant
correctly stays conditional on hull-theta AND the chart clauses;
(iii) the §13 rev-8 echo annotated in place with prior text
preserved (l.4035-4050) — verified verbatim. The residual taxonomy
defect is R9L0-2 only. CONFIRMED (minus R9L0-2's clause).

**(d) The witness-regime restatement (l.2255-2266; §13 annotation
l.3976-3983)** — attacks: (i) arithmetic: strict pair on the patch =
e0'' > 0 AND e0'' b > a1^2 (e_vv = e0'', e_SS = b, e_vS = a1) —
holds in the printed OPPOSITE regime, fails at (m2)'s printed
exhibit (e0'' b = 2 < 9 = a1^2, the judge's numbers) — both halves
exact; (ii) b > 0 follows inside the regime (e0'' b > a1^2 >= 0 with
e0'' > 0, and b = e_SS > 0 is forced when a1 != 0; at a1 = 0
directly); (iii) theta = a0 + a1 v + bS < 0 everywhere on a bounded
patch for a0 large negative, so c_v = theta/e_SS < 0 — the
separating witness is valid; (iv) "everywhere" read pointwise — the
strict pair is a pointwise condition, no uniform form is consumed
here. CONFIRMED — the ECR1-2 discharge is clean.

**(e) The interval-slice gloss on the totality clause (l.2172-2173)
and the AUD-cp closure sentence (l.2368-2376)** — attacks: (i) the
gloss "theta licenses uniqueness ... only on an interval S-slice" is
exactly the corrected scope; (ii) push BOTH this round's witnesses
through the closure (strongest): the tabulated range is a fixed
interval, theta = T > 0 globally, and (per the derivation recorded
under R9L0-1) the S-fibers are R ln v-shifts of that fixed interval
— connected AND continuously varying: neither the disconnected-slice
witness nor the two-strip witness has any purchase; the AUD-cp
sentence as printed claims only what revision 9 added and remains
true; R9L0-1's repair adds one clause there rather than changing
any. CONFIRMED.

**(f) The leg-3 recital correction (l.4101-4109)** — attacks:
(i) round arithmetic: l0 rounds 2-4 = three rounds, l1 rounds 2-3 =
two — matches "the escalation judge's three consecutive dry l0
rounds + two consecutive dry l1 rounds" and VERDICT_confirm §1
ECL0-3; (ii) the corrected sentence no longer counts round 1, whose
sustained EL0-1/EL0-2/ESC1-F1 the same sentence's "unchanged since
revision 5" presupposes — internal consistency restored; (iii) prior
text preserved per the supersession convention. CONFIRMED — the
ECL0-3 discharge is clean. Leg 3 itself: ZERO revision-9 edits to
the leg-3 body (grep-verified: no rev-9 marker below §13's leg-3
text other than the ledger annotation) — the thread stays DRY of
record at this lens; held OPEN by the per-doc rule only.

**(g) The seeding-count correction (l.4150-4157)** — attacks:
(i) independent regeneration from the on-file declarations: four
rev-7-declared events + this window's four (bridge E4L0-1,
image-parenthetical E4L0-2, consequent E4L0-3, c_v clause
ER2L1-1-residual) = EIGHT events; the printed SIX + the two named
l0-only drops (E4L0-1, E4L0-3) = eight — the annotation's own
partition closes; TEN seeded findings matches the l1 refuter's
file-measured count adopted by the judge; (ii) scope: the corrected
sentence stays scoped to "four consecutive rounds" (the rev-8
statement's scope), so this window's NEW seeding events (R9L0-1's,
declared above; ECL0-1's and ECL0-2's, declared at revision 9) do
not falsify it — the forward ledger now lives in the R-4
instance count, which prints "eight-plus" (absorbing). CONFIRMED —
the ECR1-4 discharge is clean.

**(h) The §13 REVISION 9 block's bookkeeping (l.4179-4459, minus
the entries' clauses consumed above)** — attacks: (i) counts:
"seven raised, judge-merged to SIX distinct ...: 1 REPAIR-NEEDED
(ECL0-1 ...) + 5 AMENDMENT, 0 BREAKS" — matches VERDICT_confirm
§1/§2 exactly; (ii) the sixth-strengthening chain has six links —
verified; (iii) discharge recital (l.4377-4398) cross-checked
against VERDICT_confirm §1 verdict-by-verdict (E4L0-1 clean at both
lenses; ER3L1-1+E4L0-4(b) residual = ECR1-2's clause; E4L0-4(a)
clean; ER3L1-2 discharged with the three named one-rung-out
residuals; E4L0-2 and E4L0-3+ER3L1-3 in part, failure points named)
— faithful, including the honest recording that the l0 lens's own
5.4(c) check MISSED the regime clause l1 caught; (iv) residues
(1)-(4): the until-dry status, the R-4 count, the Blocco-2 held-out
set (matches R-5': legs 3/5 items only), and the open-mathematics
list are each verified against VERDICT_confirm §2/§3/§6;
(v) LABEL SUMMARY: no label motion, none in prospect — matches §3.
One item examined and left below finding grade, declared: the §13
ER3L1-2 entry's preserved text "theta > 0 licenses the chart's
UNIQUENESS" (l.3941-3943) carries no rev-9 interval annotation while
the body gloss does — but the rev-9 discharge recital names the
inheritance explicitly ("residuals one rung out = ECL0-1 ...
inherited by the 'licenses uniqueness' gloss", l.4385-4386) and the
entry text is attack-description, not a load-bearing falsifier or
witness clause (the two entry types revision 9 annotated): the
supersession convention as practiced is met; no action owed.
CONFIRMED (minus the entry clauses cited at R9L0-1/R9L0-2/R9L0-3).

==============================================================================
## §D DISCHARGE VERDICTS (the six judge-merged findings revision 9
## consumes)

- **ECL0-1 (REPAIR-NEEDED): DISCHARGED IN PART** — the
  slice-connectedness member is written as the chart's third clause
  with the parity line correctly re-scoped, the substitute mode
  named, the §13 falsifier corrected in place, and every per-slice
  delivery verified (5.4a); the disposition FAILS at the
  transplanted CONTINUITY conclusion (R9L0-1): interval slices do
  not deliver root-map continuity across v, and the assembly's
  compactness step + the bridge's uniformity clause consume exactly
  that — the judge's own R-1' spec seeded the clause.
- **ECL0-2 ≡ ECR1-1 (AMENDMENT, merged): DISCHARGED IN PART** — the
  inverted totality caveat is retracted and the else branch
  completed exactly as demanded (5.4c); the restated member
  arithmetic OVER-counts in the conservative direction (R9L0-2: the
  root-quantifier member is pin-deliverable under the whole-domain
  proviso and is not domain/range geometry).
- **ECL0-3 (AMENDMENT): DISCHARGED CLEAN** — recital corrected by
  in-place annotation with exact arithmetic (5.4f). No residual.
- **ECR1-2 (AMENDMENT): DISCHARGED CLEAN** — the determinant regime
  is restored with both regimes printed and arithmetically verified
  (5.4d). No residual.
- **ECR1-3 (AMENDMENT): DISCHARGED WITH RESIDUAL** — both demanded
  phrases delivered and the §9 carrier extended in the same pass;
  the residual is the spec's own calibration against the rev-8
  condition (R9L0-3): the grant-head epithet lags the same-revision
  enlargement under the document's own TOPOLOGY-COMPLETE taxonomy,
  and the entry's "pattern not reproduced" claim does not hold.
- **ECR1-4 (AMENDMENT): DISCHARGED CLEAN** — count corrected;
  partition independently regenerated and closed (5.4g). No
  residual.
- Standing dispositions (r2-batch; rounds 1-4; confirming round)
  re-verified against the revision-9 state at this lens: the
  revision-9 edits reopen NONE of them; no orphan objection remains
  in any input file on disk (every ID has a standing disposition;
  the three partial failures above are cited by ID with failure
  points stated).

The round is NOT dry at this lens: 3 findings sustained
(1 REPAIR-NEEDED + 2 AMENDMENT, 0 BREAKS-THE-LEG). Under the dry
criterion of record (VERDICT_confirm preamble) the sustained
REPAIR-NEEDED blocks dryness; legs 3 and 5 stay OPEN pending
consumption of R9L0-1..3 and the next confirming round.

==============================================================================
## SUMMARY

| ID | Leg | Passage | Class | One-line |
|---|---|---|---|---|
| R9L0-1 | 5 | slice-connectedness clause's continuity conclusion (l.2196-2202) + assembly splice (l.2296-2300) + substitute mode (l.2213-2215) + §13 entry echo (l.4222-4227) + ladder sentence (l.4251-4254) | REPAIR-NEEDED | "under which ... the root map W -> S(W) is continuous on the compact hull" is FALSE: single-upcrossing is per-slice and controls nothing across v; explicit in-class two-strip EOS (half-open v-split, fibers [0,1] then [10,11], every fiber an interval) satisfies EVERY rev-9 member — Gibbs, c^2 > 0, theta > 0 at every root, Hess PD, totality, slice-connectedness, single-valued chart — while S(W) jumps by ~10 at v = v* and item (i) goes NEGATIVE across the jump; in the substitute mode no printed mechanism backs the consumption at all; spec-seeded (the confirming l0 clause + the judge's R-1' spec, authored on the product-domain picture) — R-4 misfit instance nine; free at the gamma(T) closure (R ln v-shifted fixed fibers, continuous); repair = fiber-regularity member (else root-map continuity imposed directly) + same-pass carrier propagation; ECL0-1's disposition fails in part (cited) |
| R9L0-2 | 5 | corrected whole-domain consequent (l.2399-2412) + §13 echo (l.4274-4283) | AMENDMENT | the root-quantifier member is lumped with totality/slice-connectedness as "domain/range GEOMETRY no reading of the pins ... delivers" — false for it: roots are domain points by definition and the whole-domain proviso reads theta > 0 there, delivering the member (its uniqueness CONSEQUENCE correctly stays with the interval member); conservative over-count, no false-restoration power; minted by the rev-9 restatement itself; ECL0-2/ECR1-1 dispositions otherwise verified |
| R9L0-3 | 5 | grant-head epithet (l.2443-2445) vs same-revision carriers (l.4424-4428, l.2738-2744, l.110-116, l.2513-2519, residue (4)) + entry claim (l.4354-4357) | AMENDMENT | ECR1-3's spec epithet "STRICT + hull-theta + CHART-COMPLETE" transplanted at rev-8 calibration while revision 9 itself enlarged the condition; the same revision mints TOPOLOGY-COMPLETE as a SEPARATE axis in header/§9/label-summary and prints five enumeration variants of one condition — the single-token/two-contents species (doc2 CR-2 precedent); the entry's "ECR1-3 pattern not reproduced one revision later" claim fails; benign defined-term reading on the page (chart's THIRD clause; parenthetical cites ECL0-1) holds it at the bottom of the AMENDMENT band; repair = one word (+ TOPOLOGY-COMPLETE) or taxonomy harmonization across all five sites |

CONFIRMED (cannot break; strongest attacks recorded in-section): the
per-slice deliveries of the topology clause — restated parity line
(re-derived exact at pen grade with the traversed-interval hypothesis
now explicit), uniqueness, single-valuedness, per-slice
single-upcrossing, the substitute mode's well-formedness (5.4a); the
two-branch witness recital, every number re-checked (5.4b); the
consequent's inverted-caveat retraction with the a0-positive witness
verified, and the completed else branch (5.4c); the witness-regime
restatement with both determinant regimes arithmetically verified
(5.4d); the interval-slice totality gloss and the AUD-cp closure
sentence, with both of this round's witnesses pushed through the
closure and absorbed (5.4e); the leg-3 recital correction (5.4f);
the seeding-count correction with the partition independently
regenerated (5.4g); the §13 REVISION 9 block's counts, chain,
discharge recital, residues, and label summary (5.4h).

Counts: 3 findings total = 1 REPAIR-NEEDED + 2 AMENDMENT;
**BREAKS-THE-LEG: 0**. No statement, rigor label, or route-level
conclusion is touched by any finding: R9L0-1's repair adds the
domain ladder's next written rung and STRENGTHENS the
no-viable-abstract-EOS-route conclusion a seventh time; all three
findings are discharged automatically at the standing gamma(T)
closure (continuously varying interval fibers; the other two are
textual). The [T-T0P] main statement stays SCHEMA; the G8/r2 OPEN
verdict, two-piece residue, and gamma-table conclusions stand. Leg
3's dry status is UNTOUCHED (zero rev-9 leg-3 body edits; ledger
annotation verified as transcription).

## SELF-FALSIFIERS (what kills each finding)

- R9L0-1: a derivation of root-map continuity on the compact hull
  from the printed members alone (refuted by the two-strip witness:
  every member satisfied, jump exhibited); or a printed line of
  record making v-discontinuous fiber families out-of-class (none —
  §1 pins no EOS domain, established/sustained/consumed at
  ER3L1-2/ECL0-1, both falsifier lines of record cite it); or a
  break of the two-strip witness at any checked member (each
  verified by construction above: strips disjoint, e C^2 per strip,
  every fiber a single interval, one root per hull point); or a
  ruling that "is continuous on the compact hull" is an IMPOSED
  member rather than a derived claim — refuted by the printed
  "under which", and an unwritten imposition would contradict the
  document's own five-times-enforced written-line standard.
- R9L0-2: a reading of record under which some S-root of
  eps(W_s) = e(v(W_s), .) lies OUTSIDE dom e (none: a root is by
  definition a domain point meeting the level), or under which the
  whole-domain proviso does not cover theta at such roots (none:
  "the pins are read on the whole EOS domain" is the branch's own
  printed hypothesis); or a ruling that the collective bundle
  reading governs member arithmetic against the sentence's own
  per-member falsifier individuation and the ECL0-2 precedent's
  rejection of strained benign readings.
- R9L0-3: a printed line of record defining CHART-COMPLETE as the
  three-clause bundle FOR ALL CARRIERS (the header's "chart's third
  clause" clause comes closest but the same header separately mints
  TOPOLOGY-COMPLETED as its own event-epithet, and the label summary
  + residue (4) print it as a separate axis of the condition's
  FORM); or a demonstration that no consumer-facing enumeration
  divergence exists across the five quoted sites (refuted by the
  quotes themselves).
- Any CONFIRMED verdict above: a derivation-level break of the
  quoted passage that the recorded strongest attack missed.

END — confirming round on the revision-9 delta, lens l0 (the l0 half
of the VERDICT_confirm §6 R-1' owed round). Machine summary:
{objections: 3, breaks: 0, repairs: 1, amendments: 2,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc1_r9confirm_l0.md}
