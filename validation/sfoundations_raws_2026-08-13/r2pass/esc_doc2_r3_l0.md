# ESCALATION REFUTATION — DOC-2 (phaseD_meanswirl_formalization.md), ROUND 3, LENS 0
# (hyperbolic-systems / functional-analytic rigor)

Date: 2026-08-19 (S-FOUNDATIONS-C escalation window, until-dry round 3).
TARGET: the r6 escalation-repair text ONLY — every passage carrying an
"(r6)" marker per the revision-log r6 block (lines 140–181) and the
§6-quinquies ROUND 3 table (2691–2738): the (w1) exemplar correction
(1546–1562 inside the 1536–1572 witness block), the JUMP-SET PIN + CLASS
PIN (1729–1744), the restated conclusion containment + weakened
Ṽ-regularity (1744–1768), the assembly step-(1) scoping + INSENSITIVITY
NOTE (1877–1911), the step-(3)/REGULARITY rewrite with the two structure
clauses (1918–1956), the (⟸) re-read (1957–1961), the r6 RETRACTION in
the class block (2012–2024, 2030–2037), the gamma-status Γ_G line
(2044–2048), the falsifier (a) re-bind (2070–2086), the falsifier (d) r6
note (2099–2101) and falsifier (e) (2105–2116), the register-row ROUND 3
clause (2368), the G-f entry ROUND 3 item (2427–2430), the ROUND 3
ledger (2691–2738). Leg-7 / A-1 scope: grep-verified NO r6 marker
touches D.2; the A-1 landing (357–371) is re-checked for regression only
(Part B, C-11). Adjudications of record consumed:
`r2pass/VERDICT_r2pass.md` §2.1/§4(b)/§4(c) (E-3 spec, A-1,
J-r2p-2/3); full r2batch texts `refute_r2batch_l0.md`/`_l1.md` at legs
6–7; BOTH round-1 escalation files (ESC-L0-1..4, ESC-1..4) and BOTH
round-2 escalation files (`esc_doc2_r2_l0.md` ESC2-L0-1..4,
`esc_doc2_r2_l1.md` R2L1-1..3), read in full.

DEDUP RULE APPLIED: no dispositioned objection re-raised unless the
disposition itself fails — where that is claimed (F-3 below, on the
adopted R2L1-3/ESC2-L0-3 repair wording) the prior IDs are cited and the
failure of the adopted text is derived. All quotes verified verbatim
against the r6 file (line reads).

MANDATE CHECK (discharge verification): for each of the seven round-2
IDs answered by r6 I verified (a) the round-2 finding's own repair spec,
(b) the landed r6 text, (c) the §6-quinquies ROUND 3 row. Verdicts:

| Round-2 ID | r6 delivery | DISCHARGE VERDICT |
|---|---|---|
| R2L1-1 (BREAKS) | per-pair JUMP-SET PIN + CLASS PIN; containment restated int{n_φ=0} ∪ N_F; step (1) scoped to F ∖ N_F; step (3) on the a.e. grade; "proved as written" retracted, counter at three; falsifier (e) minted; witness recorded as PASSING the r6 form | DISCHARGED as to the coincidence-boundary witness and the pointwise clause — the r6 containment is honest and the witness passes it. BUT the pin's PRICE is asserted, not delivered: "under this pin N_F is front-H²-null" is FALSE in-class (F-1 below) and four r6 lines consume it |
| ESC2-L0-1 (REPAIR) | "Ṽ piecewise-C¹ meridional" WEAKENED to C¹-off-a-closed-rotation-invariant-set; front-free-section derivation written; the two structure clauses derived directly per the objection's own salvage; downstream non-consumption recorded | DISCHARGED — the weakened clause is exactly what the front-free-section route proves; residual: the transcription of salvage clause (iii) drops the a.e./off-N_F grade qualifier on structure clause (i) (F-2 below) |
| ESC2-L0-2 (AMEND) | merged into the N_F scoping of step (1); H²-null INSENSITIVITY NOTE written | DISCHARGED as specified — the intersection set rides N_F; residual: the note's "which holds with no exceptional set at all" is true only on the essential-jump reading of [V], while step (1) pins [V] per-pair (rides F-1) |
| ESC2-L0-3 (AMEND) | certificates replaced by the operative ones (φ-dependence off fronts + H-NC + H-WR); smooth instance = compact restriction inside the marching domain clear of the CJ surface; provenance recorded; r5 text preserved in quotation | DISCHARGED as to the false certificates — residual universal in the adopted rider ("ANY subdomain"), F-3 below |
| ESC2-L0-4 (AMEND) | falsifier (a) trigger re-bound to connected-adiabat H-CVX (r5 form) and "the n_m = 0 closure"; vacuous-satisfaction mechanism recorded in-statement | DISCHARGED (Part B, C-7 — the re-bound trigger re-read against the detached-branch exhibit: now correctly out-of-hypothesis). One same-class omission elsewhere in the same executor-facing list: F-4 |
| R2L1-2 (AMEND) | Γ_G = v(∂p/∂e)_v = R_g/c_v = γ(T) − 1 > 0 > −2 WRITTEN at both anchors (hypothesis-line discharge 1666–1679; gamma status 2044–2048); connectedness re-cited to G_fund > 1 PLUS Γ_G > 0; internal contradiction recorded | DISCHARGED (Part B, C-9 — identity independently re-derived; both anchors verified present) |
| R2L1-3 (AMEND) | same merged edit as ESC2-L0-3 (repair option (i): local/restriction instance); (w2)/(α) noted as carrying the witness role independently | DISCHARGED as to the non-member exemplar — the residual universal in the restriction rider is F-3 (disposition-adequacy, adopted wording carried it) |
| A-1 (leg 7) | NO r6 edit (correct: both round-2 lenses re-confirmed it) | STANDS — verified lines 357–371 carry the judge's A-1 wording verbatim, no r6 marker, no regression; the r6 Γ_G work cites D.2's closed form without editing it; the arc-vs-connected-adiabat divergence remains DECLARED in D.18's hypothesis line |

Label discipline verified: both iff clauses at SCHEMA per J-r2p-2/J-r2p-3
at all four sites (class block 1993–1996, register row 2368, G-f entry,
ROUND 3 ledger 2732–2734); no self-upgrade — the r6 certification is
"proved ... AT EXACTLY THE STRENGTH OF ITS STATED CLAUSES" with labels
explicitly held; layer/gate caveat carried forward verbatim (177–181).
The over-certification counter honestly recorded at THREE (2019–2023).

==============================================================================
## PART A — OBJECTIONS (4 findings: 0 BREAKS-THE-LEG, 1 REPAIR-NEEDED, 3 AMENDMENT)

------------------------------------------------------------------------------
**F-1 — REPAIR-NEEDED. The r6 CLASS PIN does not deliver the nullness it
prices: "under this pin N_F is front-H²-null" is FALSE in the declared
class, because the pin controls only pairwise front INTERSECTIONS while
N_F's third clause (adjacent-piece-pair structure changes) and
front-on-front ACCUMULATION sets are uncontrolled — and the printed
justification (edges "locally H¹-rectifiable") is false at C¹ front
grade, by the same Whitney-grade mechanism the r6 text itself absorbed
at ESC2-L0-1. Four r6 lines consume the false nullness claim.**

Verbatim quotes (all r6-new):

Pin (1737–1744):
> "CLASS PIN (r6, priced): distinct declared fronts have pairwise
> H²-null intersections — patches of full 2-D coincidence are identified
> as one declared front there; under this pin N_F is front-H²-null
> (finitely many C¹ objects: crossings, triple curves, coincidence
> boundaries and edges are locally H¹-rectifiable). Without the pin only
> the a.e./measure-grade clauses below are asserted."

Conclusion gloss (1751–1753):
> "hence every front's jump set is contained in int{n_φ = 0} ∪ N_F —
> i.e. in int{n_φ = 0} UP TO a front-null set"

Step (1) (1900–1902):
> "{g ≠ 0} ∩ {n_φ ≠ 0} is dense in the relatively open set {n_φ ≠ 0}
> (n_φ continuous on a C¹ front; density unharmed by removing the
> H²-null N_F)"

(⟸) re-read (1959–1961):
> "[F_φ,rel] = 0 off the jump set, n_φ = 0 on the jump set minus N_F,
> and N_F is front-null under the class pin (r6 re-read)."

Derivation-level attack. N_F is DEFINED (1735–1737) as the closed set of
points of F "lying on another declared front, on F's own edge, or where
the local adjacent-piece-pair structure changes". The pin's operative
clause bounds ONLY the first component (pairwise H²-null intersections).
The third component is unbounded by anything printed, and it can be FAT
in-class under the pin:

IN-CLASS WITNESS (exact solution; the round-2 witness plus one phantom
front). Take the R2L1-1 coincidence-boundary witness exactly as recorded
in-statement (1702–1728): axisymmetric normal-shock solution Ṽ, declared
fronts F (coinciding with the shock disc Σ on the closed patch P,
helical beyond) and C2' = closure(Σ ∖ P). Add ONE more declared front G:
by Whitney there is a C∞ function k ≥ 0 on the meridional/parameter
domain vanishing exactly on a FAT Cantor-type compact set E (empty
interior, H² (E-lift) > 0 as a subset of F's parameter range, e.g.
E ⊂ int P); declare G := graph of (front-offset by) k restricted to the
OPEN set {k > 0}, so that G is a single C¹ (indeed C∞) hypersurface,
jump-free (V is smooth across it — jump-free declared fronts are
in-class of record: the document's own mixed-front witness has jump-free
portions, and §0 requires only "V C¹ off the front set with one-sided
limits"), DISJOINT from F, C2', Σ, and ACCUMULATING on F exactly over
the fat set E (cl(G) ∖ G = E-lift ⊂ F). Then:
- The CLASS PIN HOLDS: all pairwise intersections are H²-null (F ∩ G =
  ∅; F ∩ C2' = ∂P, a curve; G ∩ C2' = ∅). The front-set UNION stays
  closed (G's missing limit points lie ON F), so §0's "open complement
  of the front set" reading is intact.
- N_F ⊇ E-lift: at each q over E the local adjacent-piece-pair
  structure changes — arbitrarily near q the region above F is split by
  G into a pinching sliver and an outer piece, both of whose closures
  reach q, while over nearby non-E parameter points only the sliver
  adjoins F. H²(N_F) ≥ H²(E-lift) > 0: **N_F is NOT front-H²-null**,
  with the pin satisfied, on an in-class EXACT SOLUTION.
- The printed justification is refuted in the same stroke: cl(G) ∖ G is
  the "edge"-type set of a C¹ hypersurface and it is a fat Cantor set —
  NOT locally H¹-rectifiable, not even H²-null. At C¹ (a fortiori C∞
  graph-over-open-set) front grade, edges and accumulation sets of
  declared fronts have NO rectifiability whatsoever — this is the same
  class-grade phenomenon as ESC2-L0-1's Cantor-parametrized φ-circle
  family, which the r6 text ACCEPTED two paragraphs below (1938–1941):
  the document cannot consistently absorb that mechanism for S and
  assert rectifiability for N_F's analogous exceptional sets.

CONSUMPTION MAP (why this is proof-content, not wording): (i) the
conclusion gloss "i.e. in int{n_φ = 0} UP TO a front-null set" restates
the displayed containment as an up-to-null statement — with fat N_F the
restatement does not follow from the display (the "i.e." claims an
equivalence the pin does not deliver); (ii) the (⟸) re-read consumes
"N_F is front-null under the class pin" LITERALLY — on the witness above
that premise is false and the printed argument does not close (the
conclusion still holds, via the essential-jump route: [V] = 0 a.e. on
{n_φ ≠ 0} kills the atom's density a.e. — but that route is what round-2
l1 C-7 wrote, not what the r6 line prints); (iii) step (1)'s density
parenthetical labels N_F "H²-null" (the density conclusion happens to
survive fat N_F — {n_φ ≠ 0} ∖ N_F is relatively open and the a.e.-full
set is dense in it — but the printed reason is wrong); (iv) step (1)
pins [V] as the PER-PAIR jump and claims it vanishes "a.e. on
{n_φ ≠ 0}" — over a fat N_F ∩ {n_φ ≠ 0} the case chains prove only the
ESSENTIAL jump zero a.e. (the atom sees approximate limits; the pinching
sliver has density 0), while the per-pair jump at structure-change
points is uncontrolled: the a.e. claim at per-pair type is UNPROVEN
exactly where N_F is fat, and step (3)'s "(any H²-null exceptional set
is invisible to the atom)" (1928–1929) again assumes the nullness.
Structure clause (ii)'s "up to the H²-null N_F geometry priced in the
display" (1950–1951), the register-row clause "N_F = crossing/edge/
coincidence-boundary set, front-null" (2368), and the ROUND 3 ledger's
"making N_F front-null" (2708) inherit the same false price.

HONEST SCOPE: no CONCLUSION clause is falsified on any witness I could
build — on the phantom-G witness the containment, the up-to-null gloss
(vacuously: its jumps avoid the fat part), ∂_φV = 0, and both structure
clauses all HOLD; a solution witness whose jump set actually fills a
fat N_F is blocked by the class (the lateral discontinuity set of a
fat-patch shock is not finitely-many-C¹) — recorded in the
self-falsifier. The defect is a FALSE printed supporting claim (the pin
parenthetical), a false-as-printed premise in the (⟸) re-read, and an
unproven per-pair a.e. claim, all r6-new, sitting under the r6
certification "proved ... AT EXACTLY THE STRENGTH OF ITS STATED
CLAUSES" (2010–2012) — which therefore needs the same edit, lest the
over-certification counter (honestly recorded at THREE) go to four.

NAMED REPAIR (transcription-grade, two independent options; either
suffices): (A) extend the CLASS PIN to price what it actually needs —
"N_F is H²-null" AS the pin (honest, checkable per configuration), or
equivalently "declared fronts are properly embedded up to H¹-rectifiable
edges, and no declared front accumulates on another outside an H²-null
set" — and keep the chain as printed; or (B) keep the pin as printed and
re-route: state step (1)'s a.e. clause at ESSENTIAL-jump type (which the
atom argument of step (3) is the only consumer of — no exceptional set
needed, as the INSENSITIVITY NOTE already says), keep per-pair [V] only
for the pointwise geometry on F ∖ N_F, restate the gloss as "up to N_F
(H²-null under pin (A); in general a closed set carrying no essential
jump mass on {n_φ ≠ 0})", and rewrite the (⟸) re-read through the a.e.
grade ("n_φ[F_φ,rel] = 0 H²-a.e.: off the jump set [·] = 0; on the jump
set minus N_F, n_φ = 0; on N_F ∩ {n_φ ≠ 0} the essential jump vanishes
a.e. by (1)").

Class: **REPAIR-NEEDED** (per the ESC2-L0-1 precedent: false/underived
supporting claims inside the conclusion apparatus with a completeness
certification attached, conclusion clauses not counterexampled — NOT
BREAKS: the core ∂_φV = 0, the displayed containment, and the witness
adjudications all survive, and the essential-jump re-route is already
half-written in the document's own INSENSITIVITY NOTE).

NOVELTY/DEDUP: the CLASS PIN, N_F, the nullness claim, and every
consuming line are r6-minted (no prior ID touches front accumulation or
the pin's adequacy; R2L1-1 asked for "coincidence boundaries" to be
priced and its named repair ASSUMED the exceptional set null — the
adopted repair's price is what fails, which is exactly the
disposition-adequacy ground the brief allows).

------------------------------------------------------------------------------
**F-2 — AMENDMENT. Conclusion structure clause (i) is stated ungraded —
"one-sided limits on the locally φ-invariant front patches are
φ-independent" — while the step-(3)(i) derivation delivers it only at
a.e. grade / off N_F; the ESC2-L0-1 salvage being transcribed carried
the grade qualifier ("a.e.-determination") and the conclusion display
dropped it.**

Verbatim quotes: conclusion display (1756–1760):
> "with the two structure clauses derived in assembly step (3) WITHOUT
> any global Ṽ-regularity claim: one-sided limits on the locally
> φ-invariant front patches are φ-independent, and the ESSENTIAL jump
> set is a rotation-invariant (meridional-curve × S¹) set"

step (3)(i) (1942–1949):
> "on each locally φ-invariant front patch the step-(2) local product
> structure ... makes the one-sided traces rotation-covariant, and
> V = Ṽ∘π a.e. then forces them φ-INDEPENDENT (one-sided traces of a
> piecewise-C¹ field are a.e.-determined by its equivalence class along
> the patch); on {n_φ ≠ 0} strata ... the clause holds there at a.e.
> grade"

Attack. The printed mechanism proves: (a) on each open piece adjoining
the patch, V = Ṽ∘π a.e. plus per-piece C¹ regularity forces V
φ-independent ON THE PIECE (continuous functions equal a.e. on a
connected open set are equal), hence the trace THROUGH A FIXED
PERSISTING PIECE is φ-independent wherever that piece adjoins — i.e.
EVERYWHERE on (patch) ∖ N_F; (b) at patch points IN N_F the adjoining
piece changes along the φ-arc and the (multi-valued) one-sided limits
can differ pair-by-pair — nothing printed controls them (per-pair jumps
on N_F are exactly what the containment ALLOWS). So the honest grades
are "everywhere off N_F, a.e. on the patch" — the {n_φ ≠ 0} half of the
sentence carries its explicit "at a.e. grade" qualifier and the
φ-invariant half carries none: the asymmetry reads as an
everywhere-grade claim the derivation does not reach. This is the
identical species to the ESC-2/ESC2-L0-1 grade slippages this loop has
prosecuted three times; the fix is four words. Repair: "one-sided
limits on the locally φ-invariant front patches are φ-independent OFF
N_F (a.e. on the patch)". Class: **AMENDMENT** (no conclusion motion;
no counterexample constructed — a genuine one needs per-pair trace
divergence at an in-class structure-change point of an exact solution,
which I could not close; recorded in the self-falsifier).

NOVELTY/DEDUP: the ungraded clause is r6-minted (the r5 display hung
both clauses on the retracted "piecewise-C¹ Ṽ" instead); ESC2-L0-1 is
dispositioned FIXED and its weakening demand IS fixed — this attacks
the grade fidelity of the transcribed salvage, zero prior coverage.

------------------------------------------------------------------------------
**F-3 — AMENDMENT. The r6 witness-(w1) smooth-instance rider mints a
fresh universal: "its restriction to ANY subdomain compactly inside the
marching domain and clear of the CJ surface" instantiates the class
"genuinely φ-dependent off fronts" — but genuine φ-dependence on EVERY
such subdomain is not of record and not derivable: a locally
axisymmetric pocket of the standing flow (consistent with everything
[T-NSW] certifies) would make that restriction a NON-member. Third
consecutive instance of a defective transcribed exemplar spec
(ESC-L0-4 → R2L1-3 → this), and the disposition-failure clause is
invoked to that extent: the adopted R2L1-3 repair-option (i) wording
("the standing flow on any compact smooth subregion of its marching
domain clear of the CJ surface") carried the same residual universal.**

Verbatim quote (1536–1546):
> "(w1) an exact wave-frame solution genuinely φ-dependent OFF FRONTS
> and satisfying H-NC + H-WR has K ≠ 0 BY THE A.C. LEG of the second
> iff — e.g. the standing RDE wave-frame flow of record, [T-NSW] (...),
> or, for a SMOOTH instance, its restriction to any subdomain compactly
> inside the marching domain and clear of the CJ surface, where
> [T-NSW](a) supplies |w_rel| bounded away from c"

Attack. The exemplar clause instantiates the class predicate "genuinely
φ-dependent off fronts". For the FULL standing flow this is carried by
the recorded pumping (∂_φp ≠ 0 of record). For a RESTRICTION the
predicate must hold ON THE SUBDOMAIN, and "any" quantifies over all of
them: the claim is that every compact subdomain of the marching domain
clear of the CJ surface contains a point of genuine φ-dependence —
i.e. that the standing flow is NOWHERE locally axisymmetric on its
marching domain. Nothing cited certifies that ([T-NSW](a) certifies
|w_rel| > c there, which the rider correctly consumes for
bounded-away-from-c on compacta; the pumping record certifies
φ-dependence SOMEWHERE off fronts, not everywhere-locally). The
universal is unproven; a single axisymmetric pocket falsifies it while
leaving every mechanism of the leg intact. The mechanism and the other
certificates are untouched (Part B, C-6: H-NC/H-WR inheritance and the
bounded-away-from-c compactness step both CHECK); the witness role is
in any case independently carried by (w2)/(α), as the text itself
records. Repair (one phrase): "its restriction to any such subdomain
CONTAINING A SMOOTH-REGION POINT OF GENUINE φ-DEPENDENCE (nonempty by
the pumping record)" — or the existential "to a subdomain ... on which
∂_φV ≢ 0". Class: **AMENDMENT**.

NOVELTY/DEDUP DECLARATION: ESC2-L0-3 and R2L1-3 are dispositioned FIXED
and their core (non-member exemplar certificates) IS fixed; this
finding invokes the disposition-failure clause ONLY as to the adopted
repair wording's residual universal — the same failure mode by which
r5's transcription of the round-1 (w1) spec was prosecuted at round 2.
No prior ID raises the "any subdomain" quantifier.

------------------------------------------------------------------------------
**F-4 — AMENDMENT. The consolidated G-f entry's mixed-front item still
binds its pass-criterion to "the r5 jump-set conclusion" — a display the
r6 revision RETRACTED as false-as-printed — with no r6 annotation,
while the same window annotated the in-body falsifier (d) for exactly
this and re-bound falsifier (a) for exactly this defect class
(ESC2-L0-4). Propagation incompleteness in executor-facing spec text
(L1-5 class).**

Verbatim quote (G-f named-gap entry, 2425–2427):
> "+ (r5, ROUND 2) the MIXED-FRONT instance (must PASS K = 0 and the r5
> jump-set conclusion, REFUTE the r4 per-front dichotomy display —
> ESC-L0-1)"

Attack. After r6, "the r5 jump-set conclusion" names a superseded form
whose pointwise containment clause was adjudicated FALSE (R2L1-1); the
conclusion of record is the r6 containment. The in-body falsifier (d)
(2096–2105) carries the r6 note ("on THIS witness the r5 and r6
containments coincide — its jump set avoids N_F — so the r6 conclusion
is passed identically"); the consolidated G-f entry — the named-gap
spec an executor is directed to — does not, so a verbatim transcriber
of the G-f list builds the mixed-front check against a retracted
display form with no pointer that this is deliberate and provably
equivalent on the instance. Exposure is LOW (on this one instance the
two forms coincide, and the coincidence is proved in-body), which is
why this is an amendment and not a repair — but it is the precise
defect class the r6 window itself just consumed at ESC2-L0-4, arising
in the same executor-facing list that window edited. Repair (one
parenthetical): copy the falsifier-(d) r6 note into the G-f entry, or
re-bind the entry to "the r6 containment (equivalently, on this
instance, the r5 form — jump set avoids N_F)". Class: **AMENDMENT**.

NOVELTY/DEDUP: ESC2-L0-4 is dispositioned FIXED and its enumerated
target (falsifier (a)) IS fixed; this is a NEW omission of the same
class in a list the r6 window touched (it appended the ROUND 3 item at
2427–2430 immediately after the un-annotated ROUND 2 item), not a
re-raise. Grep: no prior ID targets the G-f entry's r5-conclusion
binding.

==============================================================================
## PART B — CONFIRMED passages (strongest attack tried, each)

**C-1. JUMP-SET PIN, per-pair definition (1729–1737) — CONFIRMED.**
Strongest attacks: (i) under-inclusion — a genuine jump point escaping
the per-pair reading: at any q carrying an essential jump, some
adjoining pair has unequal limits (approximate limits are limits through
pieces of positive density), so the per-pair jump set contains the
essential one; (ii) the over-attribution twin (normal-line traces at a
transversal crossing) is exactly what the pin's "NEVER via normal-line
traces" clause excludes, with the mechanism recorded; (iii) coincidence
patches: handled by the identification clause; at coincidence
BOUNDARIES the pair persists from the coincidence side and ∂P lands in
N_F, matching the witness adjudication. Definition coherent; the priced
part of the pin is F-1, not the definition.

**C-2. Containment chain, r6-scoped step (1) (1892–1906) — CONFIRMED as
an argument (modulo F-1's mislabels).** Strongest attacks: (i) the
topology step F ∖ cl{n_φ ≠ 0} = int{n_φ = 0} re-derived (an open front
patch disjoint from {n_φ ≠ 0} is disjoint from its closure; conversely
the complement of the closure is open with n_φ = 0); (ii) the closure
upgrade within F ∖ N_F: q ∉ N_F closed ⟹ a neighborhood avoids N_F, and
per-pair traces are continuous there by per-piece C¹-up-to-the-front
regularity (one-sided limits at every front point + C¹ in the piece
make the boundary extension continuous where the pair persists) — the
continuity extension is valid; (iii) density: an H²-full subset of a
relatively open subset of a C¹ surface is dense in it, and removing the
CLOSED N_F leaves {n_φ ≠ 0} ∖ N_F relatively open — the conclusion
survives even with fat N_F (only the printed "H²-null" label fails,
F-1).

**C-3. Step (3), distributional-∂_φ closure core (1918–1933) —
CONFIRMED for ∂_φV = 0.** Strongest attack: run the closure on the F-1
phantom-G witness (fat N_F): the atom's density is n_φ times the
ESSENTIAL jump against H²; K = 0 as a measure forces the essential
[F_φ,rel] = 0 H²-a.e. with NO exceptional set (measures see no
structure changes; the pinching sliver has density 0 at its
accumulation set, so approximate limits exist a.e.); the case chains
then run at essential type and kill the essential [V] a.e. on
{n_φ ≠ 0} — the closure and V = Ṽ∘π a.e. are ROBUST to fat N_F. The
per-pair/essential type mismatch in the printed step is F-1(iv); the
mathematics of the closure itself withstands it.

**C-4. REGULARITY weakening (1934–1941) — CONFIRMED.** Strongest
attacks: (i) closedness of the front set — the ball-and-good-angle
argument needs the front-set complement open; §0 line 274–275 ("it is
asserted on the open complement of the front set") pins the front-set
UNION closed, and the F-1 witness class keeps the union closed
(accumulation points lie on another front), so S is well-defined and
closed and the argument runs; (ii) rotation-invariance and
meridionality of S are by construction; (iii) the weakened clause "Ṽ ∈
C¹ off S" is exactly what the front-free-section route proves
(re-derived: B × {φ′} front-free gives Ṽ|_B = V(·,·,φ′) ∈ C¹), and the
stronger §0-grade claim is explicitly disclaimed with a downstream
non-consumption sentence (1762–1764) — verified: no downstream user in
the document consumes Ṽ-regularity beyond C¹-off-closed.

**C-5. Structure clause (ii) (1949–1956) — CONFIRMED.** Strongest
attack: hunt an essential jump point whose φ-circle escapes the local
product structure — an essential jump circle lies (up to the priced N_F
geometry) in some front's int{n_φ = 0}, where step (2)'s graph argument
(re-verified: n_φ = 0 on an open patch of a C¹ graph front forces the
graph function locally φ-independent) delivers the C¹-curve × φ-arc
product; the clause's "locally ... WHERE it meets a front's
int{n_φ = 0} stratum" scoping honestly excludes the N_F residue. On the
R2L1-1 witness the essential jump set is Σ = curve × S¹: passes.

**C-6. Witness (w1) main correction (1536–1562) — CONFIRMED (modulo the
F-3 rider).** Strongest attacks: (i) the a.c.-leg contrapositive —
K = 0 restricted to smooth regions is classical there, and under
H-NC + H-WR the leg forces ∂_φV ≡ 0 in smooth regions, so genuine
φ-dependence off fronts forces K ≠ 0 (a nonzero a.c. part is a nonzero
distribution): valid; (ii) does the standing flow satisfy the operative
certificates BY THE DOCUMENT'S RECORD? H-NC: sonic locus = CJ
hypersurface, empty interior (1611–1620); H-WR: |w_rel| > c on the
marching domain, co-rotation locus empty interior (1622–1630);
φ-dependence off fronts: the D.19 pumping record — all three citations
check in-document; (iii) the r5-defective certificates are preserved in
quotation with the provenance sentence, never erased. The correction
does what ESC2-L0-3/R2L1-3 demanded.

**C-7. Falsifier (a) re-bind (2070–2086) — CONFIRMED; ESC2-L0-4
DISCHARGED.** Strongest attack: the phrase "an EOS satisfying
CONNECTED-ADIABAT H-CVX" read as an EOS-alone property would be a
category slip (H-CVX is a trace-pair hypothesis) — DISSOLVED on the
grammatical parse the sentence supplies: "an EOS satisfying [H-CVX]
TOGETHER WITH a nontrivial azimuthal sheet carrying [s] = 0" quantifies
the exhibit as the (EOS, sheet trace-pair) jointly satisfying the
hypothesis, which is well-typed and is the reading the recorded
vacuous-satisfaction mechanism enforces. Re-run of the ESC2-L0-4
adjudication against the new text: the detached-branch isentropic sheet
now fails the trigger (downstream trace not on the connected adiabat)
and is named out-of-hypothesis by the parenthetical — internally
consistent; "the n_m = 0 closure" replaces the stale "r4 closure".

**C-8. Falsifier (e) (2105–2116) — CONFIRMED.** Required verdicts
re-derived on the witness: per-pair jump set of F = P (full shock jump
up to ∂P); N_F ∩ jump set = ∂P (on another declared front C2' =
closure(Σ ∖ P) ⊇ ∂P); int{n_φ = 0} ⊇ int P; so jump set ⊆
int{n_φ = 0} ∪ N_F with the null set exactly ∂P — PASSES the r6 form;
∂P ⊄ int{n_φ = 0} — REFUTES the r5 pointwise clause; K = 0 checked
(axisymmetric field, every atom carries n_φ = 0 or [V] = 0). The
one-instance separator claim is exact. Queued not executed,
commit-gated: consistent with standing discipline.

**C-9. Γ_G line, both anchors (1666–1679; 2044–2048) — CONFIRMED;
R2L1-2 DISCHARGED.** Independent re-derivation: p = ρR_gT, e = e(T) ⟹
Γ_G = v(∂p/∂e)_v = v(∂p/∂T)_ρ/(∂e/∂T)_ρ = v·ρR_g/c_v = R_g/c_v =
γ(T) − 1; AUD-cp gives c_v > 0, hence 0 < Γ_G, and > −2 trivially; the
identity holds at every table state (c_v = c_v(T) > 0 pointwise).
Strongest attack on the new attribution sentence: "G_fund > 1 TOGETHER
WITH Γ_G > 0 makes the shock adiabat globally p-monotone and connected
in range" — checked against the passage's own Bethe accounting (the
r5-confirmed sentence that convexity alone does NOT deliver
single-valuedness): the r6 sentence now cites exactly the pair
(convexity + Γ_G > −2 ⊂ Γ_G > 0) the Bethe/Menikoff–Plohr package
requires, the in-range qualifier is carried by the cited D.2 closed-form
sentence ("holds at EVERY table state, hence on every Hugoniot arc
inside the operating range"), and the internal contradiction R2L1-2
named is gone. Both anchors verified textually present and consistent
with each other.

**C-10. (⟸) re-read, conclusion (1957–1961) — CONFIRMED as a
conclusion, with its printed premise inheriting F-1.** Strongest
attack: does ⟸ survive fat N_F? Yes via the a.e. route (C-3): ∂_φV = 0
kills the a.c. rows classically off fronts, and n_φ[V] = 0 as a measure
forces n_φ[F_φ,rel] = 0 a.e. (state-continuity of the fluxes), so every
atom dies as a measure with no nullness needed. The direction is true
and stays trivial; only the printed three-clause justification consumes
the false nullness premise (F-1 names the rewrite).

**C-11. Leg-7 / A-1 (D.2 parenthetical, 357–371) — CONFIRMED, no
regression.** No r6 marker in D.2 (grep); the judge's A-1 wording
("i.e. genuinely nonlinear WITH the convex (compressive-shock)
orientation") stands verbatim; the r6 Γ_G work cites D.2's closed form
without editing it; the declared D.2-arc vs D.18-connected-adiabat
divergence is unchanged and remains correctly scoped in D.18's
hypothesis line ("unlike D.2's use (conditioning a monotonicity claim)
this hypothesis must EXCLUDE a state"). Strongest attack: whether the
r6 in-model connectedness line silently obligates D.2's arc form to
carry Γ_G too — no: D.2's monotonicity use is vacuity-tolerant, so the
arc form there needs no connectedness supplement; consistent.

**C-12. Register row D.18 ROUND 3 clause (2368) + ROUND 3 ledger
(2691–2738) + header r6 block (140–181) — CONFIRMED as bookkeeping,
with two rows carrying downstream flags.** Attacks tried: (i) label
leak — none: SCHEMA held at all four sites, E-3 adjudication explicitly
left to the escalation-window judge, layer/gate caveat carried; (ii)
ledger-vs-objection fidelity — each ROUND 3 row's Item column matches
its source objection text 1:1 (checked against both round-2 files);
(iii) the two honest prices (class pin; Ṽ-weakening) are declared in
the residue paragraph. Riders: the R2L1-1 row's "making N_F front-null"
and the register row's "front-null" inherit F-1; the ESC2-L0-3/R2L1-3
rows' "FIXED" inherit F-3's residual universal — as at round 2, the
enumerated deliveries are accurate as enumerated; the defects are in
the landed text, prosecuted above, so the rows stand as written.

**C-13. r6 RETRACTION mechanics + counter (2012–2024, 2030–2037) —
CONFIRMED.** The r5 "proved as written" sentence is quoted in full and
retracted; the replacement certification is scoped to "the strength of
its stated clauses"; the over-certification counter at THREE is
arithmetically right (r4 "complete AS WRITTEN"; r5 header's premature
"FIXED at full rigor"-backed display; r5 "proved as written").
Annotate-never-erase verified across every r6 edit (all five r5
passages survive in quotation). Note for the record: if F-1 stands, the
r6 certification sentence itself requires the F-1 edit to avoid
becoming the fourth entry — named inside F-1, not double-counted here.

**C-14. Falsifier (d) r6 note (2099–2101) — CONFIRMED.** Re-derived:
the mixed-front witness's jump set is strictly inside the φ-invariant
part of a single connected front with no other declared fronts, no
edge contact, and no structure change there, so it avoids N_F and the
r5/r6 containments coincide on it — the note's claim is exact. (The
un-annotated G-f twin of this item is F-4.)

==============================================================================
## SUMMARY TABLE

| ID | Target (r6 passage) | Class | One-line |
|----|---------------------|-------|----------|
| F-1 | CLASS PIN nullness claim + "i.e. up-to-front-null" gloss + step-(1) labels/per-pair a.e. + (⟸) premise (1737–1744, 1751–1753, 1900–1902, 1959–1961) | REPAIR-NEEDED | pin controls only pairwise intersections; structure-change/accumulation sets fat in-class under the pin (phantom-front Whitney witness); edge-rectifiability justification false at C¹ grade; four consuming lines; certification rides on it |
| F-2 | conclusion structure clause (i) ungraded (1756–1759 vs 1942–1949) | AMENDMENT | derivation delivers φ-independence of patch traces only off N_F / a.e.; the {n_φ≠0} half carries its grade, the φ-invariant half lost it in transcription |
| F-3 | (w1) smooth-instance rider "ANY subdomain" (1542–1546) | AMENDMENT | universal genuine-φ-dependence over all subdomains unproven; adopted R2L1-3 wording carried it; third transcribed-exemplar defect in three rounds |
| F-4 | G-f entry mixed-front item bound to "the r5 jump-set conclusion" (2425–2427) | AMENDMENT | pass-criterion cites a retracted display with no r6 note; same executor-misguidance class the window fixed at falsifier (a); in-body twin is annotated, spec entry is not |

BREAKS-THE-LEG: 0. Total findings: 4 (0 B / 1 R / 3 A). Discharge
verdicts: ESC2-L0-1, ESC2-L0-2, ESC2-L0-4, R2L1-2 DISCHARGED; R2L1-1
DISCHARGED as to its witness and display with the pin's price
undelivered (residue = F-1); ESC2-L0-3/R2L1-3 DISCHARGED with a
residual universal in the adopted rider (residue = F-3). A-1 STANDS.
DRYNESS STATEMENT: no false conclusion display survives on this lens;
for the first time in three rounds every constructed witness PASSES the
conclusion of record. What stands between the r6 text and a closable
E-3 is one undelivered price (F-1 — a pin extension or an
essential-type restatement, both transcription-grade) and three
one-phrase amendments. The loop is near dry, not dry: F-1 is
proof-content by this document's own minted standard.

==============================================================================
## SELF-FALSIFIERS (what kills each objection)

- F-1 dies if: (i) the §0/§5 front class is shown to pin declared
  fronts properly embedded / compact-with-C¹-boundary (my read: §0
  lines 245–276 pin only "finitely many C¹ curves/hypersurfaces" with
  one-sided limits and a closed UNION via the "open complement"
  sentence — the phantom-front witness keeps the union closed and is
  not excluded); or (ii) the pin's "pairwise H²-null intersections" is
  shown of record to quantify over CLOSURES of fronts (then cl(G) ∩ F
  fat violates it and my witness is out-of-hypothesis — but the printed
  word is "intersections" of the declared fronts as sets, and the
  identification clause speaks of coincidence PATCHES, i.e. sets with
  interior); or (iii) a proof is exhibited that for finitely many C¹
  hypersurfaces with closed union, the adjacent-piece-pair
  structure-change set is H²-null — my witness refutes exactly this, so
  the proof would have to find an error in it (the pinching-sliver
  density computation and H²(E-lift) > 0 are the checkable steps); or
  (iv) all four consuming lines are shown to consume only the
  ESSENTIAL-jump a.e. clause (they do not: step (1) prints "(per-pair
  jump, full state vector)", and the (⟸) line prints the nullness
  premise verbatim).
- F-2 dies if per-pair one-sided limits on a φ-invariant patch are
  shown φ-independent AT N_F points from the printed ingredients — or
  if the display's clause is shown (somewhere of record) to be defined
  at a.e. grade throughout, making the qualifier implicit.
- F-3 dies if [T-NSW] (at citation grade, M0) certifies the standing
  flow genuinely φ-dependent on EVERY open subregion of its marching
  domain (nowhere-locally-axisymmetric) — then "any subdomain"
  type-checks and only the missing citation remains; partial kill: if
  the pumping record is shown to hold a.e. on the marching domain, the
  finding drops to a request for that one citation.
- F-4 dies if the G-f consolidated entry is shown to be
  non-executor-facing (my read of G-f's own text — "executor MUST
  transcribe", named checks, named instances — is that it is exactly
  the executor's spec), or if "the r5 jump-set conclusion" is shown
  re-bound of record elsewhere in the entry (grep: it is not).
- The Part B confirmations die by exhibiting an error in any recorded
  re-derivation (each reproducible: the F ∖ cl{n_φ≠0} topology step;
  the essential-type atom argument on the phantom-G witness; the
  falsifier-(e) verdict table; Γ_G = R_g/c_v = γ − 1; the graph
  argument; the A-1 grep).

Round-3 lens-0 totals: 4 objections (0 BREAKS / 1 REPAIR / 3
AMENDMENT), 14 passages CONFIRMED with strongest attacks recorded.
Nothing else in the r6 delta withstood less than the attacks listed.

END — machine summary: {objections: 4, breaks: 0,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc2_r3_l0.md}
