# ESCALATION REFUTATION — DOC-1, CONFIRMING ROUND on REVISION 9, lens l1
# (gas dynamics / physics, RH algebra, counterexamples)

Date: 2026-08-19. Target: `phaseD/phaseD_stop_proof.md` AT REVISION 9 —
ONLY the revision-9 delta, per the launching brief, VERDICT_confirm.md
§2 DOC-1 (NOT DRY) + §6 R-1' (revision 9 owed, then ONE two-lens
confirming round on the rev-9 delta): the consumption of ECL0-1..3 +
ECR1-1..4 (judge-merged to six distinct, ECL0-2 ≡ ECR1-1), i.e. — as
named by the §13 REVISION 9 block (lines 4179-4460) — the
TOPOLOGY-COMPLETED condition of record with the slice-connectedness
clause, its embedded witness recital and the uniqueness fallback
(lines 2162-2219, incl. the "only on an interval S-slice" totality
gloss 2172-2173 and the restated parity line 2178-2186); the ECR1-2
witness-regime restatement (lines 2256-2266); the assembly's
root-map/continuity consumption line (lines 2297-2302); the AUD-cp
slice-connectedness sentence (lines 2368-2376); the restated
falsifier consequent (lines 2399-2437); the propagated grant head +
item-(i) parenthetical (lines 2441-2459); the honest status line
(2504-2528); the header intro/audit carriers (23-31, 107-116); the
§9 gamma-table carrier (2740-2749); the label summary (4420-4428);
the five §13 in-place annotations (3926-3933, 3976-3983, 4035-4050,
4101-4109, 4150-4157); and the §13 REVISION 9 block itself. Line
numbers throughout are of the revision-9 file.

ROUND/LENS PLACEMENT, DECLARED: VERDICT_confirm §6 R-1' mandates one
two-lens confirming round on the rev-9 delta NEXT WINDOW; this
document is the lens-l1 half (ID prefix R9L1- per brief — the R-4'
per-lens-prefix lesson applied). Output file `esc_doc1_r9confirm_l1.md`
— an unclaimed slot (directory listing measured this window); no
artifact is overwritten. COVERAGE CHECK, declared: grep "revision 9"
over r2pass/ — only VERDICT_confirm names it, as OWED/UNADJUDICATED;
this is the first adversarial pass on the rev-9 state. Scope
convention of record (esc_doc1_r3_l1.md header, carried through the
loop): attacks lie on newest-revision text OR on older clauses whose
truth conditions revision 9 changed under them; the leg-3 body (zero
rev-9 edits, dry at both lenses of record) is OUT of scope except the
§13 recital annotation, verified below as transcription.

Specs consumed: `r2pass/VERDICT_confirm.md` §1 DOC-1 (all six
adjudications), §2, §5, §6 R-1' (incl. the R-4 BINDING RULE clause
"every rev-9 clause is restated against the row's own hypotheses" and
the mandate to attack the rev-9 text AS repair-spec-derived text);
FULL texts of the two consumed refutations `esc_doc1_confirm_l0.md`
(ECL0-1..3) and `esc_doc1_confirm_l1.md` (ECR1-1..4); prior rounds
r1-r4 both lenses + r2-batch files (dedup layer). Both duties
executed: (a) derivation-level attack on the revision-9 repair text,
attacked AS repair-spec-derived text per the standing lesson; (b)
verification that the revision-9 repairs DISCHARGE the six findings
they answer. Mechanical checks executed this pass (numeric, run of
record in this window, script `r9l1_check.py` in the session
scratchpad): the R9L1-1 moving-slice witness verified member-by-member
(totality both branches over the full segment; min theta over all
roots = 2 > 0; strict pair 4 > 0; every S-slice an interval) with the
cross-jump relative entropy computed at three straddle widths
(E(V|U) = -8.918, -8.9918, -8.99918 at |DeltaW|^2 = 8.0e-4, 8.0e-6,
8.0e-8 — converging to -9 < 0); the ECR1-2 regimes (corrected regime
e0''b = 4 > 1 = a1^2 HOLDS; (m2)'s printed regime e0''b = 2 < 9 = a1^2
FAILS — both as the rev-9 text now states).

DEDUP REGISTER (dispositioned objections NOT re-raised): L0-1/L1-1,
L0-2, L1-2 (r2-batch); EL0-1==ESC1-F1, EL0-2, EL0-3==ESC1-F2, EL0-4
(round 1); E2L0-1, E2L0-2 (round 2); E3L0-1, ER2L1-1, E3L0-2 (round-3
window); ER3L1-1..3, E4L0-1..4 (round-4 window); ECL0-1..3, ECR1-1..4
(confirming round, consumed at revision 9 — discharge adequacy
examined below; where a disposition partially fails, the ID is cited
and the failure point stated, per the standing dedup rule). All
findings below attack REVISION-9 text or clauses whose truth
conditions revision 9 changed — zero prior adversarial coverage — and
each declares its distinction from every ID above. Every attacked
passage: verbatim quote + attack + class in {BREAKS-THE-LEG,
REPAIR-NEEDED, AMENDMENT}. Every examined delta passage not broken:
CONFIRMED with the strongest attack tried.

BINDING R-4 RULE CHECK (VERDICT_confirm §6 R-1', "eight-plus prior
instances"; transplanted-clause misfits are findings per brief).
Executed clause-by-clause on the full rev-9 delta:

| Rev-9 repair clause | Provenance | Restated? | Verdict |
|---|---|---|---|
| Slice-connectedness member: parity/uniqueness/single-valuedness halves (l.2187-2196, 2178-2186) | ECL0-1 repair spec + VERDICT_confirm R-1' adopted spec | YES — written as a condition member on the row's own objects, traversed-interval hypothesis explicit | PASS |
| Slice-connectedness member: root-map CONTINUITY sub-claim + assembly consumption (l.2196-2201, 2297-2302) | The l0 spec's "the root map is continuous on the compact hull", carried verbatim into VERDICT_confirm R-1' and into the clause | Transplant — the spec's continuity mechanism ("single-upcrossing") was authored on a fixed/continuously-varying slice family; the row's domain-unpinned class lets the SLICES THEMSELVES jump in v, which no printed member constrains | **MISFIT → R9L1-1 (ninth declared instance)** |
| Uniqueness fallback "in its place" (l.2213-2215) | ECL0-1 repair spec tail | Restated, but inherits the same continuity hole a fortiori | carried in R9L1-1 |
| Falsifier consequent restatement (l.2399-2437) | ECL0-2/ECR1-1 merged repair | The member split and caveat de-inversion RESTATED and correct; BUT the l0 spec's bucket sentence "the chart clauses are domain geometry no pins-reading or derivation delivers" was transplanted and ENLARGED — it mis-sorts the root-quantifier member, which the whole-domain reading DOES deliver | **MISFIT → R9L1-2 (tenth declared instance)** |
| Grant-head epithet "STRICT + hull-theta + CHART-COMPLETE" (l.2443-2444) | ECR1-3's own repair phrase (my lens's spec, authored at revision 8 when the chart clauses exhausted the enlargement) | Transplanted while the SAME revision added a new stage (TOPOLOGY-COMPLETED) under the document's stage-name convention | **MISFIT → R9L1-3 (eleventh declared instance; my own spec's wording, held to the harsh standard per the ER3L1-3 precedent)** |
| ECR1-2 witness-regime clause (l.2256-2266) | ECR1-2 repair spec | RESTATED from the row's own (m2) family objects, regime written, mechanically verified | PASS |
| Ledger annotations (ECL0-3/ECR1-4, l.4101-4109, 4150-4157) | measured counts | Regenerated per SR-12, verified against the files | PASS |

R-4 verdict: 3 of 6 clause families misfit — instances nine through
eleven. The §13 rev-9 preamble claim "every repair clause below is
RESTATED against the row's own hypotheses, never transplanted
verbatim" (l.4198-4201) and honest residue (2)'s restatement claim
(l.4444-4445) are TRUE of the parity/uniqueness halves, the witness
regime, and the ledger annotations, and FALSE of the continuity
sub-claim, the geometry bucket, and the grant-head epithet — carried
inside R9L1-1/R9L1-2/R9L1-3 at source, not double-counted.

Finding IDs: R9L1-1..R9L1-3.

==============================================================================
## §A SUSTAINED FINDINGS

### A.1 The slice-connectedness clause's root-map-continuity sub-claim
### — **OBJECTION R9L1-1, REPAIR-NEEDED** (the clause constrains each
### slice's topology, not the slice FAMILY's variation in v; an
### in-class witness satisfies every rev-9 member with a discontinuous
### root map, and the granted item (i) fails at ORDER ONE:
### E(V|U) -> -9 < 0 on cross-locus pairs)

Quote 1 (the clause, lines 2191-2203): "each S-slice
{S : (v(W_s), S) in dom e} over a hull v-value that the roots of
eps(W_s) = e(v(W_s), .) meet is an INTERVAL — under which the parity
bracketing runs (the traversed interval IS in the domain), the root
is unique, the (v, S)-image and the chart are single-valued, the root
map W -> S(W) is continuous on the compact hull (single-upcrossing
structure: e(v, .) - eps < 0 before and > 0 after the unique root —
the continuity the assembly's compactness step and the bridge's
uniformity clause consume), and each per-segment continuation lands
on the DATA root".

Quote 2 (the assembly consumption, lines 2297-2302): "continuity of
Hess_W E (EOS C^2 — a §1-implicit consumption, now stated — with the
root map W -> S(W) single-valued and continuous on the hull by the
three chart clauses, revision 9 per ECL0-1) plus compactness of the
hull's state image give the uniform lower eigenvalue bound
lambda_min > 0".

Quote 3 (the §13 ECL0-1 entry's delivery claim, lines 4222-4227):
"the root map W -> S(W) is continuous on the compact hull
(single-upcrossing structure — the continuity the assembly's
compactness step and the bridge's uniformity clause consume)".

Attack (derivation level, with the witness mechanically verified this
pass). Single-upcrossing is a PER-SLICE (fixed-v) sign structure; it
cannot deliver continuity of S(W) in W, because W-variation moves v,
and NO printed member constrains how the S-slices vary WITH v — the
clause pins each slice's topology (an interval) and is silent on the
slice family. §1 pins no EOS domain (ER3L1-2 of record, sustained
through six rungs); the row's class therefore admits EOS whose
interval slices JUMP at a v-threshold. In-class witness W-A, fully
explicit, satisfying EVERY revision-9 member INCLUDING
slice-connectedness:

  dom e = ([0.5, 1) x [0, 1]) UNION ([1, 1.5] x [10, 11])  —
  every S-slice an INTERVAL ([0,1] for v < 1; [10,11] for v >= 1);
  the two components are separated (S-distance >= 9), so e is C^2 on
  dom. e(v, S) = e0(v) + h_1(S) on the first branch,
  e0(v) + h_2(S) on the second, with h_1(S) = (1 + S)^2 (range
  [1, 4], h_1' = 2 + 2S >= 2 > 0, h_1'' = 2 > 0) and
  h_2(S) = 4 + 2(S - 10) + (S - 10)^2 (range [4, 7], h_2' >= 2,
  h_2'' = 2). Endpoints at REST: U = (rho = 2, m = 0, E_U),
  V = (rho = 2/3, m = 0, E_V); along the W-segment (m == 0)
  eps(v) = alpha v + beta is LINEAR in v (exact identity for rest
  segments), and e0 is chosen so that
  F(v) := eps(v) - e0(v) = 4 + 3(v - 1) - (v - 1)^2 — concave, so
  e0'' = -F'' = 2 > 0, F increasing, F < 4 for v < 1, F in [4, 7]
  for v >= 1 (endpoint values 2.25 and 5.25, both interior).
  Member check (mechanical, this window): Gibbs closure by
  construction; c^2 = v^2 e0'' > 0; strict pair
  e_vv e_SS = 4 > 0 = (e_vS)^2 (separable — cross term identically
  zero); Hess e = diag(2, 2) PD everywhere; TOTALITY at every
  segment point (F(v) in branch-1 range for v < 1, branch-2 range
  for v >= 1); theta = h_i' >= 2 > 0 EVERYWHERE, in particular at
  every S-root (root-quantifier member) and at every hull state
  (hull-theta member); each slice's root unique (h_i strictly
  increasing); chart single-valued; data-root landings exact
  (S_U = 0.803 in [0,1], S_V = 10.5 in [10,11]);
  slice-connectedness member SATISFIED.

Then: (i) the root map S(W) = sqrt(F(v)) - 1 for v < 1,
10 + sqrt(F(v) - 3) - 1 for v >= 1, JUMPS from 1^- to 10 at the hull
point with v = 1 — "the root map W -> S(W) is continuous on the
compact hull" (Quotes 1/3) is FALSE with every printed member
satisfied; the parenthetical's own mechanism gives only the per-slice
sign structure, which is TRUE and insufficient. (ii) The hull's
(v, S)-image is NOT compact (the branch-1 image accumulates at
(1, 1), which is not in dom e), so Quote 2's "compactness of the
hull's state image" fails and the printed derivation of
lambda_min > 0 collapses; the bridge's "uniform on compacts by
continuity of C" loses its compact. (iii) The GRANT itself dies, at
order ONE, not merely in its uniform constant: E(W) = -rho S(W) is
discontinuous along the segment, so for straddle pairs
U_n = W(v = 1 - delta), V_n = W(v = 1 + delta):
E(V_n|U_n) = E(V_n) - E(U_n) - DE(U_n).(W_V - W_U) ->
-rho* (10 - 1) = -9 < 0 while |DeltaW|^2 -> 0 (mechanically
verified: -8.918 at delta = 1e-2, -8.9992 at 1e-3, -8.99918 at 1e-4,
against |DeltaW|^2 = 8.0e-4 / 8.0e-6 / 8.0e-8). Item (i)
E(V|U) >= c|DeltaW|^2 >= 0 is violated by NEGATIVE relative entropy
on in-hull comparison pairs — segment interior points are comparison
states of record (E3L0-2's own consumed sentence: "item (i)
quantifies over comparison segments, and W-segments between K-states
LEAVE K"). (iv) "each per-segment continuation lands on the DATA
root" is ill-posed at the jump (no continuous continuation crosses
v = 1); the OUTCOME is rescued by single-valuedness (the unique root
at the endpoint IS the data root), so the landing VALUE survives
while the continuation MECHANISM as printed does not. (v) The
uniqueness FALLBACK branch ("where slice-connectedness is not
available, root UNIQUENESS is imposed directly as a member in its
place", lines 2213-2215) inherits the defect a fortiori: direct
uniqueness delivers even less continuity than interval slices; the
assembly line (Quote 2) cites "the three chart clauses" as delivering
continuity in BOTH branches.

The unwritten hypothesis, located exactly: regularity of the slice
FAMILY — the map v -> {S : (v, S) in dom e} must vary continuously
(equivalently: the hull's (v, S)-image closed/compact, or the root
map continuous imposed as a member). On a fixed product domain, or on
slices [phi_-(v), phi_+(v)] with continuous endpoints, the
single-upcrossing sign structure DOES propagate to W-continuity (the
standard sign-bracketing argument needs e defined at (v', S(W) +/-
delta) for v' near v — delivered by slice-family continuity, and by
nothing less); the witness lives precisely in the gap.

Class: **REPAIR-NEEDED** — by the loop's own severity calculus, the
exact ECL0-1/E3L0-2 shape one rung further out: an in-class witness
satisfies EVERY printed member of the condition of record while the
granted item (i) breaks (here at order one, the strongest break of
the ladder so far). NOT BREAKS-THE-LEG: the route-level OPEN verdict
is unmoved; the repair is the SEVENTH consecutive strengthening of
the no-viable-abstract-EOS-route conclusion (L1-2 -> EL0-3/ESC1-F2 ->
E2L0-1 -> E3L0-1+E3L0-2 -> E4L0-2+ER3L1-2 -> ECL0-1 -> R9L1-1); and
the clause is FREE at the standing gamma(T) closure — the tabulated
domain is a fixed PRODUCT (v-range x T-interval), the slice family is
constant in v, the moving-slice witness has no purchase, and the
AUD-cp discharge is untouched (verified in §C.4). Repair, one clause
as the chart's FOURTH member (or fused into the third): "and the
slice family varies continuously over hull v-values (the endpoints of
the S-slices continuous in v — equivalently the hull's (v, S)-image
is COMPACT and the root map continuous, under which single-upcrossing
delivers the continuity the assembly consumes); else root-map
continuity / image compactness is imposed directly as a member"; the
assembly line (2297-2302), the §13 ECL0-1 entry's delivery sentence
and its falsifier list (which carries NO falsifier for the continuity
sub-claim — this witness is exactly the missing one) corrected in the
same pass.

DEDUP DECLARATION: this is a partial-failure citation of ECL0-1, not
a re-raise. ECL0-1's disposition DELIVERS its demanded halves —
parity bracketing, root uniqueness, single-valuedness, data-root
value, the two-branch witness killed (verified in §C.1: my witness
W-A has a SINGLE-valued chart; ECL0-1's two-valued mechanism is
closed) — and FAILS at the continuity/compactness sub-clause its
adopted spec carried (the spec's "the root map is continuous on the
compact hull" was authored on connected-domain intuition one
structural level up: fixed or continuously-varying slices; the judge's
§5 pen-grade list verifies the witness and the parity exactness, not
the continuity delivery — no of-record verification blocks this).
Not a re-raise of ER3L1-2 (totality delivered and satisfied by the
witness — carried at source), nor of E4L0-2 (root multiplicity —
delivered), nor of ECR1-3 (enumeration — different carrier). The
domain ladder gains its outermost rung yet: K -> hull -> theta-pin
region -> chart domain (totality) -> per-slice topology
(slice-connectedness) -> slice-FAMILY regularity (transversal
topology).

### A.2 The consequent's geometry bucket mis-sorts the root-quantifier
### member — **OBJECTION R9L1-2, AMENDMENT** (two sites, one defect:
### the whole-domain reading DOES deliver the root-quantifier member)

Quote 1 (body, lines 2400-2408): "restores the grant's PIN-READABLE
members IN THE WHOLE-DOMAIN BRANCH — the Hessian member and
hull-theta, where the theta pin itself goes hull-wide — MODULO the
chart clauses (totality, root-quantifier, slice-connectedness): those
are domain/range GEOMETRY no reading of the pins and no concavity
derivation delivers (pins constrain e's derivatives where e is
DEFINED, never where it is defined ...)".

Quote 2 (§13 rev-9 block, lines 4275-4280): "a whole-domain firing
restores the grant's PIN-READABLE members only — Hessian + hull-theta
— MODULO the chart clauses (totality, root-quantifier,
slice-connectedness), which no pins-reading and no concavity
derivation delivers".

Attack (derivation level — one member is sorted into the wrong
bucket). The root-quantifier member (lines 2178-2180) is "theta > 0
is read at EVERY S-root of eps(W) = e(v, S) over each hull point" — a
THETA-SIGN statement at domain points: every S-root is by definition
a point of the EOS's S-domain ((v(W_s), S_root) in dom e), so the
whole-domain reading of the theta pin (theta > 0 on ALL of dom e)
delivers the member VERBATIM — tautologically, roots are domain
points. It is neither "domain/range GEOMETRY" (it is first-order pin
data at named points) nor undeliverable ("no reading of the pins ...
delivers" is FALSE for it: the whole-domain reading delivers it; no
witness in the a0-positive patch class or anywhere else can violate
it under whole-domain pins). Totality and slice-connectedness are
correctly sorted (genuine domain geometry — the parenthetical's own
witness supports exactly and only totality). Consequence: a
legitimate whole-domain firing recorded under this consequent would
leave the root-quantifier member standing as an undischarged residual
that the firing's own hypothesis discharges — UNDER-restoration, the
conservative direction (no false restoration is possible), but a
false printed universal in the falsifier text, against the row's
five-times-enforced written-line standard. A strained benign reading
was hunted and is named: "root-quantifier clause" read as the whole
sentence INCLUDING its parity/uniqueness consequence (which
slice-connectedness, i.e. geometry, underwrites) — rejected: the
MEMBER as defined is the quantified sign statement; its uniqueness
consequence is priced in the adjacent clause, and even under the
strained reading the deliverable sign-half is delivered, so the
residual is mis-priced as printed.

Class: **AMENDMENT** — auxiliary falsifier-consequent text, the
unbroken lineage class (EL0-4, E2L0-2, E4L0-3, ER3L1-3,
ECL0-2/ECR1-1 — all AMENDMENT of record), and in the SAFE direction
(under-restoration); no statement, label, or route conclusion moves.
Repair, one clause at both sites: "MODULO totality and
slice-connectedness — domain/range geometry no reading of the pins
delivers; the root-quantifier member IS delivered by the whole-domain
reading (roots are domain points), so a whole-domain firing restores
Hessian + hull-theta + root-quantifier and leaves the two geometry
clauses standing with their own §13 falsifiers".

DEDUP DECLARATION: partial-failure citation of ECL0-2/ECR1-1 — their
disposition is delivered on both demanded axes (the caveat inversion
is corrected, verified §C.2; the else-branch enumeration is
corrected, verified §C.2); the failure is in the repair clause's own
sorting, text that did not exist before revision 9, and is
spec-seeded: the l0 spec's repair clause grouped "the chart clauses
are domain geometry no pins-reading or derivation delivers" when the
chart clauses were totality + root-quantifier — the mis-sort was IN
the spec, and revision 9 transplanted and enlarged the bucket (R-4
tenth declared instance, declared in the table above). Not a re-raise
of E4L0-3/ER3L1-3 (their member-arithmetic demand is satisfied — the
count is now right, one entry is in the wrong bucket).

### A.3 The grant-head epithet under the document's stage-name
### convention + the §13 entry's "pattern not reproduced" claim —
### **OBJECTION R9L1-3, AMENDMENT** (one token, two contents; the
### ECR1-3 pattern reproduced at the margin one revision later)

Quote 1 (grant head, lines 2443-2444): "and under the STRICT +
hull-theta + CHART-COMPLETE condition of record".

Quote 2 (§13 ECR1-3 entry, lines 4353-4357): "and the §9 gamma-table
carrier is extended in the SAME pass (TOPOLOGY-COMPLETED per revision
9) so no carrier of the revision-9-enlarged condition is left stale —
the ECR1-3 pattern not reproduced one revision later".

Attack (record + semantics). The document's stage-name convention of
record distinguishes CHART-COMPLETED (revision 8: totality +
root-quantifier; header lines 107-110, §9 table 2738-2740, the §13
rev-8 block) from TOPOLOGY-COMPLETED (revision 9: slice-connectedness;
header 110-115, §9 2741-2744, §13 ECL0-1 entry title, label summary
4425-4426 — which spells the full form "STRICT + hull-theta +
chart-complete + TOPOLOGY-COMPLETE form (slice-connectedness
member)"). Under that convention the grant head's epithet names the
condition THROUGH revision 8 and omits the stage the SAME revision
added — the ECR1-3 mechanism at the margin: a reader who checks
exactly the epithet's stages (strict pair + hull-theta + totality +
root-quantifier) on ECL0-1's two-branch witness — or on R9L1-1's
W-A read without its slice-connectedness check — claims the grant
with the chart two-valued (resp. discontinuous). Against this stands
a genuine competing usage INSIDE the same revision: the consequent
(lines 2403-2404) defines "the chart clauses" as THREE — totality,
root-quantifier, slice-connectedness — under which "CHART-COMPLETE"
includes the new member and the head is current. One token, two
contents in one revision — the executor-facing name-collision species
prosecuted at CR-2/R4L1-1 (doc2) this same batch. Either way Quote
2's meta-claim over-certifies: under the stage convention the grant
head IS a carrier of the revision-9-enlarged condition left on the
rev-8 epithet (the pattern reproduced); under the three-clause
usage the stage names collide. The benign defined-term reading
("condition of record" = the defined singular object at 2140-2215,
which includes slice-connectedness) exists and is why this is not
higher — the exact reason ECR1-3 itself stood at AMENDMENT.

Class: **AMENDMENT**. Repair, one phrase + one annotation: grant head
"under the STRICT + hull-theta + CHART- AND TOPOLOGY-COMPLETE
condition of record" (or "chart-complete (all three chart clauses
incl. slice-connectedness)"), and the §13 ECR1-3 entry's "not
reproduced one revision later" sentence annotated to name the
grant-head epithet as the one carrier the sweep missed (the §9,
header, status-line and label-summary carriers WERE all extended —
verified in §C.5 — so the sweep was five-of-six).

DEDUP DECLARATION: partial-failure citation of ECR1-3 — its
disposition delivered the demanded propagation for the revision-8
members at every named carrier including the grant head; the failure
is prospective (the epithet transplanted from my own spec's wording,
authored at revision 8 — R-4 eleventh declared instance, held against
my own lens's text per the ER3L1-3 precedent) and the token collision
is revision-9-new. Not a re-raise of ECL0-1 (whose mechanism is
consumed in the condition; this prices an ENUMERATION carrier only).

==============================================================================
## §B EXAMINED, ATTACK NOTED, NOT SUSTAINED (below finding grade)

- Else-branch OVER-conditioning (lines 2412-2419): a firing is said to
  leave the grant conditional on hull-theta AND "the chart clauses" —
  strictly, an s-concavity-at-hull conclusion presupposes totality +
  single-valuedness; slice-connectedness is one sufficient ROUTE to
  the latter (the fallback member is the other). Attack tried:
  over-enumeration in the residual list. NOT SUSTAINED: the direction
  is conservative (no false restoration), and the clause's own
  disjunctive membership ("in its place") absorbs the reading; no
  consumer is misled into claiming more than is granted.
- The §13 rev-9 preamble "seven raised, judge-merged to SIX distinct"
  (l.4184-4185) vs my lens's four + l0's three = seven with one merge
  = six: matches VERDICT_confirm §1 exactly. No action.
- "the repair is the SIXTH consecutive strengthening" (l.4195-4198):
  chain recount L1-2 -> EL0-3/ESC1-F2 -> E2L0-1 -> E3L0-1+E3L0-2 ->
  E4L0-2+ER3L1-2 -> ECL0-1 = six entries, ECL0-1 sixth. Accurate
  (R9L1-1 will be the seventh; no over-count as printed). No action.

==============================================================================
## §C EXAMINED DELTA PASSAGES THAT SURVIVE — CONFIRMED, with the
## strongest attack tried

### C.1 The slice-connectedness clause's parity, uniqueness,
### single-valuedness and witness-recital content (lines 2162-2215
### minus the continuity sub-claim) — CONFIRMED

Strongest attacks tried, all failed: (a) *parity sign bookkeeping on
the restated line* (2180-2186): with theta > 0 at both roots,
e(v, .) - eps is negative just before and positive just after each
root; between two such roots the sign passes + -> -, IVT at the first
downward crossing gives an intermediate root with e_S <= 0, IN the
domain because "the traversed S-interval lies in the EOS domain" is
now EXPLICIT — the hypothesis ECL0-1 demanded, written; exact.
(b) *does slice-connectedness + root-quantifier force uniqueness?*
Yes on each slice (the parity contrapositive); my W-A witness has
unique roots per slice and does NOT touch this half. (c) *ECL0-1's
own two-branch witness re-run against the rev-9 condition*: its slice
[0,1] UNION [10,11] over a single v is NOT an interval — the new
member correctly EXCLUDES it; the two-valued-chart mechanism is dead.
(d) *the totality gloss* "theta licenses uniqueness of the inversion
— and that only on an interval S-slice" (2172-2173): now exactly
calibrated. (e) *the embedded witness recital* (2204-2212): faithful
to ECL0-1's construction, member list verbatim-checked. (f) *the
fallback's uniqueness content* (2213-2215): imposing uniqueness
directly does close the two-valued mechanism (single-valuedness is
what it buys); only the continuity consumption fails (carried in
R9L1-1, not re-counted). CONFIRMED minus R9L1-1's sub-claim.

### C.2 The falsifier-consequent restatement (lines 2399-2437) minus
### the geometry bucket — CONFIRMED

Strongest attacks tried, all failed: (a) *caveat de-inversion*: "which
the whole-domain reading also delivers" is GONE; the chart clauses
"stay STANDING members with their own §13 falsifiers" — exactly the
merged spec's demand; the a0-positive patch witness is transcribed
correctly and supports the totality half. (b) *hull-theta restoration
logic*: at hull states, theta(W_s) = e_S(v_s, S(W_s)) is defined
modulo the chart and then whole-domain theta > 0 applies — "the theta
pin itself goes hull-wide ... MODULO the chart clauses" is exact.
(c) *else-branch member count*: "conditional on hull-theta AND the
chart clauses, both consumed or presupposed as inputs" — the
presupposition argument (concavity of an undefined or multivalued
function is not a statement) is exact; ECR1-1's under-enumeration is
closed. (d) *false-restoration hunt*: no branch of the restated
consequent can erase totality or slice-connectedness — the geometry
mis-sort (R9L1-2) errs in the SAFE direction only. (e) *§13 echo
annotation* (4035-4050): prior text preserved, correction accurate,
"the consequent of record is the revision-9 body text" — correct
pointer. CONFIRMED minus R9L1-2.

### C.3 The ECR1-2 witness-regime restatement (lines 2256-2266) and
### its §13 annotation (3976-3983) — CONFIRMED; discharge CLEAN

Strongest attacks tried, all failed: (a) *algebra*: e = e0(v) +
(a0 + a1 v)S + (b/2)S^2 gives e_vv = e0'', e_vS = a1, e_SS = b;
strict pair = {e0'' > 0, e0''b > a1^2} — the OPPOSITE regime to
(m2)'s printed a1^2 > e0''b, exactly as the text now says, both
regimes mechanically instanced this pass (4 > 1 holds; 2 < 9 fails).
(b) *theta sign*: theta = a0 + a1 v + bS < 0 on a compact patch for
a0 large negative; c_v = theta/b < 0; c^2 = v^2 e0'' > 0 independent
of a0 — the witness serves the member split exactly. (c) *label
fit*: "the quadratic-patch FAMILY of (m2) taken in the OPPOSITE
determinant regime" resolves ECR1-2's class-label misfit — the
family/exhibit distinction is now written. (d) *(m2) itself
untouched*: its printed regime a1^2 > e0''b remains correct for ITS
job (killing item (i) with the pins + e_SS > 0 held). CONFIRMED.

### C.4 The AUD-cp slice-connectedness sentence (lines 2368-2376) —
### CONFIRMED

Strongest attack tried (push R9L1-1 through the closure): the
tabulated domain is a fixed PRODUCT of a v-range and ONE
T-interval — the slice family is CONSTANT in v, so the moving-slice
witness has no purchase; theta = T > 0 globally makes e(v, .)
strictly increasing, the root map is a monotone inversion of a
continuous family, continuous, image compact — R9L1-1 discharges
automatically at the closure exactly as ECL0-1 did ("the
disconnected-slice witness has no purchase" extends verbatim to the
moving-slice witness). "hull evaluability inside the tabulated range
IS the totality clause" — correct identification, priced once. The
"prices the ABSTRACT-EOS accounting only" scoping survives all three
findings — part of why none is BREAKS. CONFIRMED.

### C.5 The propagation carriers — CONFIRMED as carriers (minus the
### grant-head epithet, R9L1-3)

Verbatim reads: header intro (23-31: revision-9 sentence accurate —
"1 REPAIR-NEEDED — the slice-connectedness chart member ... + 5
AMENDMENT, 0 BREAKS" matches VERDICT_confirm §2); header audit line
(110-116: TOPOLOGY-COMPLETED inventory accurate incl. the fallback);
honest status line (2504-2528: "slice-connectedness member added in
revision 9 per ECL0-1", "the revision-8/9 clauses incl.
slice-connectedness" — current); §9 gamma table (2740-2749:
TOPOLOGY-COMPLETED entry accurate, "table conclusions UNCHANGED"
verified); label summary (4420-4428: full four-stage form, G8 residue
restated correctly); §13 rev-7-echo and rev-8-echo annotations (all
five: prior text preserved per the supersession convention, verified
in place at 3926-3933, 3976-3983, 4035-4050, 4101-4109, 4150-4157).
Stale-form hunt (grep over "chart-complete", "TOPOLOGY", "condition
of record", "single-valued"): every surviving pre-revision-9 form
sits inside preserved §13 text under a supersession note or in
necessity-direction context, EXCEPT the grant-head epithet priced as
R9L1-3. All carriers inherit R9L1-1/R9L1-2 only through the source
clauses they transcribe (carried once at source). CONFIRMED.

### C.6 The two ledger annotations (ECL0-3 at 4101-4109, ECR1-4 at
### 4150-4157) — CONFIRMED; discharges CLEAN

(a) ECL0-3: "zero sustained findings across l0 rounds 2-4 and l1
rounds 2-3" + count of record = the escalation judge's 3 + 2 dry
rounds — matches VERDICT_confirm §1 and the source summary rows;
internal consistency restored (the corrected sentence no longer
contradicts "unchanged since revision 5"). (b) ECR1-4: "EIGHT
declared distinct seeding events (TEN seeded findings)" — matches my
own file's partition exactly (4 prior + 4 new events; 4 + 6 seeded
findings), with the two dropped l0-only events named (bridge,
consequent). Both annotations preserve prior text and cite the
regeneration discipline (SR-12). CONFIRMED.

### C.7 The §13 REVISION 9 block's bookkeeping (4179-4460) —
### CONFIRMED (minus the transplant-declaration claims carried in
### §R-4 above)

Counts, merge, classes match VERDICT_confirm §1/§2 exactly; the six
consumption entries transcribe mechanisms, witnesses, and judge
verdicts faithfully (checked entry-against-file for all six); the
discharge-verdicts recital (4377-4398) matches both confirming files'
§D sections including the residual placements; the leg-3 status
paragraph (4400-4405) is transcription-accurate ("held OPEN by the
per-doc rule ONLY" — matches VERDICT_confirm §3); honest residue (1)
correctly declares the rev-9 repairs unadjudicated (this document is
the l1 half of that adjudication); residue (3) keeps the landing gate
closed per R-5'; residue (4)'s open-mathematics list is unchanged and
accurate. The R-4 restatement claims of the preamble and residue (2)
are adjudicated in the table above: true at three clause families,
refuted at three (consumed as R9L1-1/2/3 — residue (2) itself
mandates exactly this round's posture, which found them). CONFIRMED
otherwise.

==============================================================================
## §D DISCHARGE VERDICTS (the six revision-9-consumed findings)

- **ECL0-1 (REPAIR-NEEDED): DISCHARGED IN PART** — the
  slice-connectedness member is written as the chart's third clause
  with the parity line restated (traversed-interval hypothesis
  explicit), the two-branch witness recited, the §13 falsifier line
  corrected, and the fallback named; the two-valued-chart mechanism
  is CLOSED (§C.1). The disposition FAILS at the continuity/
  compactness sub-clause its adopted spec carried (R9L1-1): the clause
  pins per-slice topology while the consumed continuity is a
  slice-FAMILY property, and an in-class moving-slice witness kills
  the grant at order one under every printed member.
- **ECL0-2 + ECR1-1 (AMENDMENT, merged): DISCHARGED AS DEMANDED, one
  new defect introduced by the repair wording** — the inverted caveat
  is corrected (the delivery claim withdrawn, the chart clauses
  standing members), and the else-branch remainder now names
  hull-theta AND the chart clauses (§C.2); the transplanted geometry
  bucket mis-sorts the root-quantifier member (R9L1-2, safe
  direction).
- **ECL0-3 (AMENDMENT): DISCHARGED CLEAN** (§C.6). No residual.
- **ECR1-2 (AMENDMENT, my lens's own): DISCHARGED CLEAN** — the
  determinant regime is written at both sites, mechanically verified
  this pass; the family/exhibit distinction is explicit (§C.3). No
  residual.
- **ECR1-3 (AMENDMENT, my lens's own): DISCHARGED AS SPECIFIED for
  the revision-8 members, residual at the margin** — the grant head
  and item-(i) parenthetical were propagated exactly per my spec, and
  the §9 carrier extended in the same pass; the residual is that my
  spec's epithet, transplanted while the same revision added a new
  stage, reproduces the pattern one member out under the document's
  stage-name convention (R9L1-3 — held against my own lens's wording,
  per the ER3L1-3 precedent).
- **ECR1-4 (AMENDMENT, my lens's own): DISCHARGED CLEAN** (§C.6). No
  residual.
- Standing dispositions (r2-batch; rounds 1-4; confirming round)
  re-verified against the revision-9 state at this lens: the
  revision-9 edits reopen NONE of them; no orphan objection remains
  in any input file on disk (every ID has a standing disposition; the
  three partial failures above are cited by ID with failure points
  stated).

The round is NOT dry at this lens: 3 findings sustained
(1 REPAIR-NEEDED + 2 AMENDMENT, 0 BREAKS-THE-LEG). Under the dry
criterion of record (VERDICT_confirm preamble) the sustained
REPAIR-NEEDED blocks dryness; legs 3 and 5 stay OPEN pending
consumption of R9L1-1..3 and the round's other half / successor
adjudication.

==============================================================================
## SUMMARY

| ID | Leg | Passage | Class | One-line |
|---|---|---|---|---|
| R9L1-1 | 5 | slice-connectedness clause's continuity sub-claim (l.2196-2203) + assembly consumption (l.2297-2302) + §13 entry (l.4222-4227, falsifier list) | REPAIR-NEEDED | single-upcrossing is a PER-SLICE structure and cannot deliver root-map continuity in W: the clause pins each slice's topology, nothing pins the slice FAMILY's variation in v (§1 pins no EOS domain, of record); explicit in-class witness with moving interval slices ([0,1] for v<1, [10,11] for v>=1, separable e, all members incl. slice-connectedness SATISFIED, mechanically verified) has S(W) jumping 1->10 at v=1, non-compact image, and E(V|U) -> -9 < 0 on straddle pairs — item (i) dies at ORDER ONE; fallback branch inherits a fortiori; FREE at the gamma(T) closure (fixed product domain); ECL0-1 disposition fails in part (cited); R-4 misfit instance 9; seventh strengthening of the no-viable-abstract-EOS-route conclusion |
| R9L1-2 | 5 | consequent geometry bucket (l.2400-2408) + §13 echo (l.4275-4280) | AMENDMENT | "(totality, root-quantifier, slice-connectedness): those are domain/range GEOMETRY no reading of the pins ... delivers" mis-sorts root-quantifier — a theta-sign statement at domain points that the whole-domain theta pin delivers TAUTOLOGICALLY (roots are domain points); under-restoration only (safe direction) but a false printed universal; spec-seeded from the l0 repair clause's bucket (R-4 misfit instance 10); ECL0-2/ECR1-1 dispositions otherwise delivered (cited) |
| R9L1-3 | 5 | grant-head epithet (l.2443-2444) + §13 ECR1-3 entry meta-claim (l.4353-4357) | AMENDMENT | "STRICT + hull-theta + CHART-COMPLETE" under the document's stage-name convention (CHART = rev 8, TOPOLOGY = rev 9; label summary spells both) omits the stage the SAME revision added — the ECR1-3 pattern at the margin one revision later — while the consequent's "chart clauses = three" usage makes the token collide (CR-2/R4L1-1 species); the entry's "pattern not reproduced" claim over-certifies; transplant of my own spec's rev-8 epithet (R-4 misfit instance 11); benign defined-term reading exists (why AMENDMENT) |

CONFIRMED (cannot break; strongest attacks recorded in-section): the
parity/uniqueness/single-valuedness content of the TOPOLOGY-COMPLETED
condition with the totality gloss and witness recital, ECL0-1's
two-valued mechanism closed (C.1); the falsifier-consequent
de-inversion, hull-theta-modulo-chart logic, and else-branch
enumeration (C.2); the ECR1-2 witness-regime restatement, both
regimes mechanically instanced (C.3); the AUD-cp sentence with
R9L1-1 pushed through the closure and absorbed by the fixed product
table domain (C.4); all propagation carriers as carriers minus the
grant-head epithet (C.5); both ledger annotations (C.6); the §13
REVISION 9 block's counts, merge, transcriptions, discharge recital
and honest residues minus the three R-4 claims (C.7).

Counts: 3 findings total = 1 REPAIR-NEEDED + 2 AMENDMENT;
**BREAKS-THE-LEG: 0**. No statement, rigor label, or route-level
conclusion is touched by any finding: the [T-T0P] main statement
stays SCHEMA; the route-level OPEN verdict, two-piece r2 residue,
quadruple-pinned grant, and gamma-table conclusions stand; all three
findings discharge automatically at the standing gamma(T) closure
(fixed product table domain; the other two textual). R9L1-1's repair
adds the domain ladder's outermost written rung (slice-family
regularity) and STRENGTHENS the no-viable-abstract-EOS-route
conclusion a seventh time. The leg-3 thread stays DRY at both lenses
of record (its recital annotation verified as transcription; no leg-3
body text carries any finding).

## SELF-FALSIFIERS (what kills each finding)

- R9L1-1: a printed line of record making moving-slice (or
  branch-per-v-interval) EOS out-of-class (none: §1 pins no EOS
  domain — established, sustained, and consumed at ER3L1-2/ECL0-1);
  or a derivation that the printed members force slice-family
  continuity or root-map continuity (false: the witness satisfies
  every member with a jumping root map — mechanically verified this
  window); or a demonstration that the witness violates Gibbs
  closure, c^2 > 0, the strict pair, totality, the root-quantifier,
  hull-theta, or slice-connectedness as printed (each verified by
  construction and numerically); or a ruling that segment interior
  points are not comparison states (contradicted by E3L0-2's consumed
  sentence of record); or a reading under which E(V|U) -> -9 is
  compatible with item (i) (none: (i) lower-bounds by c|DeltaW|^2 >= 0).
- R9L1-2: a §1-class EOS satisfying the whole-domain reading of the
  pins with an S-root at which theta <= 0 (impossible: roots are
  domain points and the reading pins theta > 0 on the whole domain);
  or a printed definition of the root-quantifier member as INCLUDING
  its uniqueness consequence as the member content (the condition
  text at 2178-2186 states the sign quantifier and prices uniqueness
  through the adjacent slice-connectedness clause).
- R9L1-3: a printed line of record defining "CHART-COMPLETE" as
  covering the slice-connectedness member (the stage convention at
  header/§9/label-summary says otherwise; the consequent's
  three-clause usage COLLIDES rather than defines); or a ruling that
  the defined-term reading needs no enumeration hygiene —
  contradicted by the loop's own ECR1-3 precedent, which is why this
  stands at AMENDMENT.
- Any CONFIRMED verdict above: a derivation-level break of the quoted
  passage that the recorded strongest attack missed.

END — confirming round on the revision-9 delta, lens l1 (the l1 half
of the VERDICT_confirm §6 R-1' owed round). Machine summary:
{objections: 3, breaks: 0, repairs: 1, amendments: 2,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc1_r9confirm_l1.md}
