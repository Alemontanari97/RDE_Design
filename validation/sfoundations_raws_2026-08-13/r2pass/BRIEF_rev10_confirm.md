# BRIEF — DOC-1 REVISION 10 CONFIRMING LENSES (two independent slots)

ROLE: one CONFIRMING REFUTER lens over the REVISION 10 delta of
`validation/sfoundations_raws_2026-08-13/phaseD/phaseD_stop_proof.md`.
Your lens ID (L0 or L1) is given in your task prompt. Scope = the
rev-10 delta (delta = CONTENT): every edited passage + its consumers.
You WRITE exactly one file:
`validation/sfoundations_raws_2026-08-13/r2pass/esc_doc1_r10confirm_<lens>.md`
(<lens> = l0 or l1). You edit NOTHING else. Paths relative to repo
root.

## INPUTS (read IN FULL)
1. `r2pass/VERDICT_doc1_closure.md` (same dir) — the mandate whose
   consumption you are confirming: §1 adopted repair specs (R9-A/B/C),
   §4 residues RES-DOC1-1/2, §5 falsifiers.
2. The strategy paragraph of
   `validation/ADVISORY_SfoundationsC2_prompt_2026-08-19.md` (§ PRIMA
   AZIONE PARALLELA) — the ratified sufficient-but-unoptimized grant.
3. `phaseD/phaseD_stop_proof.md`: the §13 REVISION 10 block IN FULL +
   EVERY edit site it declares (read each edited passage with enough
   surrounding context to judge consumers) + the §9 table + header +
   grant head + assembly splice + falsifier-consequent + AUD-cp note +
   domain ladder.

## ATTACK DUTIES (all of them; per-finding output)
(a) SUFFICIENCY AT PEN GRADE: does the strong hypothesis set
    (global joint convexity / slice-connected single-chart /
    in-chart hull / fiber-regularity or fallback members) actually
    DELIVER every granted item (root-map continuity on the compact
    hull; image compactness; lambda_min > 0 chain; item (i) order-one
    bound)? Re-derive the load-bearing steps yourself. Hunt the gap
    with a witness IN the new class (a witness violating a strong
    hypothesis is OUT-OF-CLASS and proves nothing — say so if the
    rev-10 text misuses one).
(b) MANDATE CONSUMPTION: R9-A spec consumed at EVERY named carrier
    (enumerate; measured grep for the condition epithet(s) in YOUR
    window; any carrier missed = the ECR1-3 species). R9-B re-sort at
    BOTH sites with the adopted scoping. R9-C route applied at ALL
    variants + the §13 ECR1-3 annotation present.
(c) NEW-MINT HUNT: rev-9's R9-B/R9-C were MINTED BY THE RESTATEMENT
    itself. Hunt defects newly minted by rev-10's restatement:
    carrier-enumeration divergence, epithet drift, meta-claims without
    measured sweeps (overcert guard), falsifier lists not matching the
    new members, "under which" scoping to less than the full set.
(d) ZERO-EDIT SCOPE: grep-verify (your own window, cite the command)
    that the leg-3 body carries zero rev-10 edits and that no
    non-mandate passage moved (compare §13 edit-site list vs your own
    sweep of the delta).
(e) LABEL/VERDICT MOTION: confirm no label moved ([T-T0P] main SCHEMA;
    route-level OPEN; gamma(T) conclusions; standing dispositions
    r2-batch/rounds-1-4/confirming-round/rev-9 NOT reopened — dedup
    register against them, no orphan objections).
(f) R-4 AUDIT: transplant-calibration misfits in the rev-10 text;
    count of record is ELEVEN — number any new instances you sustain.

## RULES
- Per-lens ID prefixes: findings named R10L0-1.. (lens L0) or
  R10L1-1.. (lens L1). NO shared IDs.
- Class every finding: BREAKS-THE-LEG / REPAIR-NEEDED / AMENDMENT /
  WORDING, with mechanism + (for the first two) an explicit witness
  or derivation. Severity calculus = the loop's own (a missing
  condition member under which a granted item fails in-class =
  REPAIR; conservative under-restoration = AMENDMENT cap).
- INDEPENDENCE: do not read the other lens's file (it may not exist
  yet; never wait for it).
- Your file ends with: scope-sweep declaration (grep commands + hits),
  dedup register (standing dispositions checked), R-4 audit
  paragraph, and a MACHINE SUMMARY:
  {lens: l0|l1, findings: [{id, class, site}], breaks: N, repairs: N,
  amendments: N, wording: N, leg3_zero_edit: true|false,
  carriers_consistent: true|false}
- Env pinned: no installs; GENO/ read-only; no git; edit only your
  output file.
