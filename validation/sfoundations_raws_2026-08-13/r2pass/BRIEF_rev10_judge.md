# BRIEF — DOC-1 REVISION 10 CLOSURE JUDGE

ROLE: closure judge of record for the revision-10 two-lens confirming
round on `phaseD/phaseD_stop_proof.md`. You WRITE exactly one file:
`validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_doc1_rev10.md`.
You edit NOTHING else. Paths relative to repo root; artifact paths
below relative to `validation/sfoundations_raws_2026-08-13/`.

## INPUTS (read IN FULL — triage on FILE, never from prompts)
1. `r2pass/esc_doc1_r10confirm_l0.md` and
   `r2pass/esc_doc1_r10confirm_l1.md` (the two lenses).
2. `r2pass/VERDICT_doc1_closure.md` — your predecessor: the mandate,
   adopted specs, residues, and the model for this verdict's form.
3. The rev-10 delta of `phaseD/phaseD_stop_proof.md` at every attacked
   passage (spot-check every quoted passage VERBATIM against the
   on-disk file in THIS window) + the §13 REVISION 10 block.
4. The strategy paragraph of
   `validation/ADVISORY_SfoundationsC2_prompt_2026-08-19.md` (§ PRIMA
   AZIONE PARALLELA) — the ratified strategy rev-10 executes, and this
   round's design (one two-lens confirm, then this judge).

## PROTOCOL
- NULL=FAILURE CHECK first: both lens files exist, non-empty,
  well-formed, one per lens with per-lens ID prefixes, machine
  summaries present, scope sweeps + dedup registers declared. A
  missing/empty/misfiled lens = the round FAILED: say so and stop
  (verdict = NOT-ADJUDICABLE, no dry grant).
- Merge cross-lens findings at the clause (declare the merge). For
  every refuter-vs-text and refuter-vs-refuter divergence: RE-DERIVE
  at pen grade yourself (the predecessor's §3 is your model).
- DRY CRITERION (refined, of record): DRY = ZERO sustained
  BREAKS-THE-LEG + ZERO sustained REPAIR-NEEDED at BOTH lenses.
  Sustained AMENDMENTS do NOT block dryness — adjudicate each, and
  list the adopted repair text for carry (applied at the landing
  window without further rounds).
- Adjudicate every finding: SUSTAINED (class) / OVERRULED (reason) /
  RECLASSED (reason). A sustained finding needs the witness/derivation
  to actually check — re-verify the arithmetic yourself where a
  witness is numeric.
- WORDING-class deltas may be confirmed single-lens; CONTENT-class
  needs both lenses concurring on the clause (they attacked the same
  delta; divergence = you re-derive).

## CONSEQUENCES TO STATE EXPLICITLY
- If DRY: legs 3 and 5 CLOSE. State the unblocked landing set
  verbatim: [T-T0P] main statement lands (SCHEMA label, gap lists
  SPLIT, t-periodic quantifier, domain cl(Omega_march), slip-free
  caveat, G12 decomposition, gap-graph edges); the G8/[C-XBVP](a')
  registry row lands WITH its §9 falsifier pointer; the [T-XWS]
  retro-annotation lands. Name the amendments carried into that
  landing pass.
- If NOT DRY: name the residue with owner (the session prompt's rule:
  named residue, the session proceeds — no further rounds under this
  mandate unless the orchestrator re-opens).
- Falsifiers section for THIS verdict (predecessor §5 is the model).

END with MACHINE SUMMARY:
{doc1_dry: true|false, legs_closed: [3,5]|[], sustained: {breaks: N,
repairs: N, amendments: N}, amendments_carried: [ids],
landing_unblocked: true|false, residues: [names],
file: validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_doc1_rev10.md}

Env pinned: no installs; GENO/ read-only; no git; edit only your
output file.
