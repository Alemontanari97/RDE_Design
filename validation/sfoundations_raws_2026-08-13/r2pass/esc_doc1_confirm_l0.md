# ESCALATION REFUTATION — DOC-1, CONFIRMING ROUND on REVISION 8, lens l0
# (hyperbolic systems / functional-analytic rigor)

Date: 2026-08-19. Target: `phaseD/phaseD_stop_proof.md` AT REVISION 8 —
ONLY the revision-8 delta (the consumption of ER3L1-1..3 + E4L0-1..4):
the CHART-COMPLETED clauses (totality + root-quantifier) in the
condition of record, lines 2150-2176; the corrected c^2 gloss, lines
2177-2182; the corrected c_v attribution with the member-wise split,
lines 2209-2219; the WRITTEN strict Legendre bridge (assembly FIRST
LINK), lines 2222-2234, and its consumption splice at lines 2238-2239;
the AUD-cp chart-clause discharge sentence, lines 2315-2320; the
member-wise RE-SCOPED falsifier consequent, lines 2344-2361; the
propagation carriers (header audit line 102-105, honest status line
2424-2440, §9 gamma table 2651-2658, §13 revision-7 echo
SUPERSEDED-IN-PART annotation 3578-3582, header intro 23-26); the §13
REVISION 8 block, lines 3768-4041. This round is the two-lens-owed
confirming round of VERDICT_escalation.md §6 R-1 (this file = the l0
half); adjudication context read in full: VERDICT_escalation.md §2
DOC-1 + §6 R-1 (and §1/§4/§5 for the standing gate state).

COVERAGE CHECK, declared: no esc_* file on disk targets revision 8
(grep "revision 8" over r2pass/: only VERDICT_escalation §2/§7 name
it, as UNADJUDICATED) — this is the first adversarial pass on the
rev-8 state; no artifact is overwritten (`esc_doc1_confirm_l0.md` was
an unclaimed slot).

Specs consumed: `r2pass/VERDICT_escalation.md` (§2 DOC-1 dryness
adjudication, §6 R-1 residue clause, §6 R-4 BINDING RULE); the two
input refutations revision 8 consumes, read IN FULL —
`esc_doc1_r3_l1.md` (ER3L1-1..3) and `esc_doc1_r4_l0.md` (E4L0-1..4);
prior rounds `esc_doc1_r1_l0/l1.md`, `esc_doc1_r2_l0/l1.md`,
`esc_doc1_r3_l0.md` (dedup layer; EL0-1/EL0-2 leg assignments
verified at source, summary-table rows "| EL0-1 | 3 |" /
"| EL0-2 | 3 |"). Both duties executed: (a) derivation-level attack
on the revision-8 repair text; (b) verification that the revision-8
repairs DISCHARGE the seven findings they answer (ER3L1-1..3,
E4L0-1..4) and that standing dispositions hold at revision 8.

DEDUP REGISTER (dispositioned objections NOT re-raised): L0-1/L1-1,
L0-2, L1-2 (r2-batch, consumed revisions 4/5); EL0-1==ESC1-F1, EL0-2,
EL0-3==ESC1-F2, EL0-4 (round 1, consumed revision 5); E2L0-1, E2L0-2
(round 2, consumed revision 6); E3L0-1, ER2L1-1, E3L0-2 (round-3
window, consumed revision 7); ER3L1-1..3, E4L0-1..4 (round-4 window,
consumed revision 8 — adequacy examined below; where a disposition
partially fails, the ID is cited and the failure point stated, per
the standing dedup rule). The revision-6 shadow sentence
"e_SS > 0 (= c_v > 0 via c_v = theta/e_SS ...) then FOLLOWS from the
strict pair" (now lines 2185-2187) is NOT attacked: ER3L1-1's dedup
ruling (theta dependence visible in the printed "via" identity)
stands and its reason still holds at revision 8. Every attacked
passage: verbatim quote + attack + class in {BREAKS-THE-LEG,
REPAIR-NEEDED, AMENDMENT}. Every examined delta passage not broken:
CONFIRMED with the strongest attack tried.

Finding IDs: ECL0-1..ECL0-3. Line numbers are of the revision-8 file.

==============================================================================
## §R4 BINDING RULE CHECK (VERDICT_escalation §6 R-4, six prior
## instances): was each revision-8 repair clause RESTATED against the
## row's own hypotheses, or transplanted verbatim from a refutation
## file? Executed clause-by-clause on the full delta:

| Rev-8 repair clause | Provenance | Restated? | Verdict |
|---|---|---|---|
| Totality zeroth clause (l.2153-2162) | ER3L1-2 repair spec | YES — written as a condition MEMBER with the range condition spelled on the row's own objects (eps(W_s), e(v(W_s), .)) | PASS (fits the row) |
| Root-quantifier + parity clause (l.2162-2172) | E4L0-2 repair spec | FORM yes (condition member), CONTENT no — the parity MECHANISM was carried over with the spec's connected-S-domain assumption intact, which MISFITS the row's own domain-unpinned EOS class | **MISFIT → ECL0-1** |
| Strict Legendre bridge (l.2222-2234) | E4L0-1 repair line + attack-body matrix | Near-verbatim transplant, BUT verified against the row's hypotheses (theta > 0 + EOS C^2 are row members; every entry re-derived below) | PASS (no misfit) |
| c^2 gloss fix (l.2177-2182) | E4L0-4(a) suggested wording | YES — restated with the row's own domain-limitation reasoning attached | PASS |
| c_v attribution fix (l.2209-2219) | ER3L1-1/E4L0-4(b) | YES — member-wise split written from the row's identity c_v = theta/e_SS, witness named in place | PASS |
| Falsifier consequent re-scope (l.2344-2361) | E4L0-3 repair clause + ER3L1-3 attack text | PARTIAL — the l1 attack's "modulo ER3L1-2's totality" was transplanted AND INVERTED into a delivery claim; the else branch was not restated against the rev-8-ENLARGED member list | **MISFIT → ECL0-2** |

R-4 verdict: 2 of 6 clause families misfit — the pattern's seventh
and eighth instances, both consumed as findings below. The rev-8
honest-residue item (2) claim "this revision RESTATED each repair
against the row's own hypotheses" is TRUE of the totality clause, the
bridge's hypothesis-anchoring, and both gloss fixes, and FALSE of the
parity mechanism and the consequent's member arithmetic.

==============================================================================
## LEG 5 (E-2) — the revision-8 chart clauses, bridge, attribution
## repairs, and consequent re-scope

### 5.1 The root-quantifier clause's parity inference — **OBJECTION
### ECL0-1, REPAIR-NEEDED** (the condition's uniqueness chain fails
### on an in-class family; the entry's own registered falsifier fires)

Quote 1 (condition of record, lines 2162-2172): "(root-quantifier
clause — E4L0-2) theta > 0 is read at EVERY S-root of
eps(W) = e(v, S) over each hull point, which forces the root to be
UNIQUE (two theta > 0 roots of e(v, .) = eps would bracket a
theta <= 0 root by level-crossing parity: e(v, .) passes from above
the level just after the first root to below it just before the
second, so an intermediate root with e_S <= 0 exists), makes the
(v, S)-image and the chart single-valued, and lands each per-segment
continuation on the DATA root — closing the in-class landing
mechanism of the per-branch reading".

Quote 2 (the summary it feeds, lines 2173-2176): "theta > 0 AND
Hess e(v, S) positive DEFINITE on (the (v, S)-image of) the
comparison-segment hull — the image total and single-valued by the
two chart clauses above".

Quote 3 (the §13 E4L0-2 entry's own falsifier, lines 3831-3835):
"falsifier: exhibit a §1-class EOS with two theta > 0 roots of
e(v, .) = eps and no theta <= 0 root between them — impossible for
continuous e_S (the parity argument): a counterexample kills the
parity line itself."

Attack (derivation level). The parity inference silently consumes
CONNECTEDNESS of the S-slice of the EOS domain: the bracketing needs
e(v, .) DEFINED on the whole interval between the two roots. The
row's own class does not deliver that — the record of this loop
established, sustained, and consumed the fact that §1 pins NO EOS
domain (ER3L1-2, whose self-falsifier cites it; the (m2)/(m3) patch
witnesses are accepted in-class as partially-defined EOS). In-class
witness, fully explicit, satisfying EVERY member of the revision-8
condition of record while the chart stays MULTIVALUED:

  e(v, S) = e0(v) + h_i(S) on dom = (v-range) x (I1 ∪ I2),
  I1 = [0, 1], I2 = [10, 11], e0 strictly convex (e0'' > 0);
  h_1(S) = 5^S - 1 on I1 (h_1' = 5^S ln 5 > 0, h_1'' > 0,
  range [0, 4]); h_2(S) = 1 + 2(S - 10) + 0.1 (S - 10)^2 on I2
  (h_2' >= 2 > 0, h_2'' = 0.2 > 0, range [1, 3.1] ⊂ h_1(I1)).
  Gibbs closure holds by construction (p := rho^2 e_rho = -e0'(v));
  c^2 = v^2 e0'' > 0; theta = h_i' > 0 EVERYWHERE ON THE DOMAIN —
  in particular at every S-root (root-quantifier member SATISFIED);
  Hess e = diag(e0'', h_i'') PD everywhere (strict pair + hull-theta
  members SATISFIED); take endpoints U (S_U = 0.5 ∈ I1) and V
  (S_V = 10.5 ∈ I2, eps_V - e0(v_V) = h_2(10.5) = 2.025 ∈ h_1(I1));
  arrange the hull's eps - e0 values inside h_1's open range —
  TOTALITY member SATISFIED via the I1 branch at every hull point.

Then: (i) "two theta > 0 roots ... would bracket a theta <= 0 root"
is FALSE — at V itself, eps_V has an I1-root AND the data root
S_V ∈ I2, both with theta > 0, and NO intermediate root exists
because NOTHING between them is in the domain (e_S is continuous ON
THE DOMAIN; the bracket interval is outside it). This is verbatim
the counterexample Quote 3 registers as the kill condition of the
parity line — the entry's own falsifier FIRES. (ii) "forces the root
to be UNIQUE" and "makes the (v, S)-image and the chart
single-valued" (Quotes 1-2) FAIL: the chart is two-valued at V and
at every hull point whose eps lies in both branch ranges.
(iii) "lands each per-segment continuation on the DATA root" FAILS:
the continuation from U's I1-root stays on the I1 branch (the
I1-root exists and moves continuously at every segment point) and
lands at s = 1 on S_land = h_1^{-1}(2.025) ∈ I1 ≠ S_V — E4L0-2's
landing mechanism, ALIVE at revision 8 under the fully-satisfied
printed condition: item (i)'s Bregman route runs on the continued
object, which differs from the data relative entropy by
-rho_V (S_V - S_land) != 0; indeed S(W) is not even a function, so
the granted item (i) for E = -rho S is ill-posed as printed. The
parity argument is exact for CONNECTED slices (verified: with both
roots in an interval slice, take S* = the first root after S1; e
approaches the level from above at S*, so e_S(S*) <= 0 for C^1 e —
the printed mechanism is right THERE); the unwritten hypothesis is
that each S-slice {S : (v, S) ∈ dom e} met by the roots is an
INTERVAL.

The same unwritten clause is load-bearing at a SECOND site: the
assembly's "compactness of the hull's state image" and "continuity
of Hess_W E" (lines 2247-2249) need the root map W -> S(W)
continuous on the hull; with interval slices, totality + universal
root-quantifier DELIVER that continuity (single-upcrossing structure:
uniqueness + IVT force e(v, .) - eps < 0 before and > 0 after the
root; a root sequence escaping along the slice would contradict
continuity of e at a fixed intermediate S — checked at pen grade),
and the bridge's "uniform on compacts by continuity of C" then gets
its min-theta > 0. With disconnected slices the root map is not even
single-valued. So slice-connectedness underwrites BOTH the parity
line and the continuity/compactness consumption.

Class: **REPAIR-NEEDED** — by the loop's own severity calculus. The
benign-presupposition escape that held ER3L1-2 at AMENDMENT does not
exist here: a presupposition reading of "passes from above the level
... to below it" (e defined on the traversed interval) rescues the
parity SENTENCE but not the condition's SUFFICIENCY — the witness
satisfies every printed member under every reading while the grant
dies, exactly the E3L0-2 shape (missing condition member + in-class
break), which was REPAIR-NEEDED of record. NOT BREAKS-THE-LEG: the
route-level conclusion is OPEN and unmoved; the repair once more
(SIXTH time: L1-2 -> EL0-3/ESC1-F2 -> E2L0-1 -> E3L0-1+E3L0-2 ->
E4L0-2+ER3L1-2 -> ECL0-1) STRENGTHENS the no-viable-abstract-EOS-
route conclusion; and the clause is FREE at the standing gamma(T)
closure (the tabulated range is an interval — the AUD-cp discharge
below is untouched). Repair, one clause in the chart clauses:
"with each S-slice of the EOS domain over hull v-values an INTERVAL
(slice-connectedness — under which the parity bracketing runs and
the root map is continuous on the compact hull); else root
UNIQUENESS is imposed directly as a member". The §13 E4L0-2 entry's
falsifier line (Quote 3) must be corrected in the same pass
("impossible for continuous e_S" -> "impossible for continuous e_S
ON AN INTERVAL SLICE").

DEDUP DECLARATION: this is a partial-failure citation, not a
re-raise. E4L0-2's disposition FAILS at exactly the parity clause's
domain assumption (the spec's witness family h was authored on a
connected S-domain; the transplant carried the assumption — R-4
instance, declared in §R4); ER3L1-2's disposition fails at the same
point one step removed (its totality clause is delivered and sound,
but the "theta licenses uniqueness of the inversion" gloss inside it,
lines 2156-2158, is true only modulo the same connectedness — carried
here at source, not double-counted). Not a re-raise of E3L0-2
(theta positivity on the hull — delivered), nor of ER3L1-2's
existence axis (delivered), nor of E4L0-2's root-multiplicity axis
AS RAISED (the universal quantifier is delivered; the defect is one
rung further out: the domain topology under the quantifier). The
domain ladder gains its outermost rung yet: K -> hull -> theta-pin
region -> chart domain (totality) -> chart-domain TOPOLOGY
(slice-connectedness).

### 5.2 The re-scoped falsifier consequent's member arithmetic —
### **OBJECTION ECL0-2, AMENDMENT** (two sites, one defect: the
### re-scope was written against the rev-7 two-member condition, not
### the rev-8 four-member one)

Quote (lines 2344-2361): "THAT kills the condition-claim and
restores the unconditional grant IN THE WHOLE-DOMAIN BRANCH, where
the theta pin itself goes hull-wide (modulo the totality clause,
which the whole-domain reading also delivers); in the ELSE branch
the derivation has CONSUMED theta > 0 on the hull as an input, so a
firing there kills the HESSIAN member ONLY and leaves the grant
conditional on hull-theta — which is first-order data no concavity
derivation controls and retains its OWN falsifier (the E3L0-2
disposition row's ...) (consequent RE-SCOPED member-wise in revision
8 per E4L0-3/ER3L1-3, both lenses: revision 7 made the condition
two-membered under a consequent written when it had Hessian members
only — a Hessian-side firing must not erase the hull-theta member it
either consumed as input or never touched)".

Attack (derivation level — consequent arithmetic against the
revision-8 condition, which the SAME revision made FOUR-membered:
Hessian + hull-theta + totality + root-quantifier):

(a) Whole-domain branch: "(modulo the totality clause, which the
whole-domain reading also delivers)" is FALSE. The whole-domain
reading re-scopes the §1 PINS (theta > 0, c^2 > 0) to the whole EOS
domain; totality (hull ⊆ chart domain, i.e. eps(W_s) in the RANGE of
e(v_s, .)) is DOMAIN GEOMETRY, not a pin, and no reading of where
pins hold can deliver it: witness = any partially-defined in-class
EOS with theta > 0 on its ENTIRE domain whose range misses
mid-segment eps values — ER3L1-2's own consumed witness class, which
satisfies the whole-domain reading of every pin while totality
FAILS. The clause is a transplant-corruption of record: ER3L1-3's
attack text reads "where the theta pin itself goes hull-wide, modulo
ER3L1-2's totality" — flagging totality as NOT delivered; revision 8
transcribed the transplant and APPENDED "which the whole-domain
reading also delivers", inverting the l1 refuter's caveat into its
negation (§R4, eighth instance). A whole-domain-branch firing
recorded under this consequent would erase the totality member
undischarged — the exact false-restoration defect class this
consequent has now minted at its FOURTH member (lineage: revision-4
scalar-shadow over-claim -> E2L0-2 domain -> E4L0-3/ER3L1-3
hull-theta -> this). Note also: even with totality granted, the
whole-domain branch restores root-UNIQUENESS only via per-slice
monotonicity, i.e. modulo ECL0-1's slice-connectedness — the branch
arithmetic inherits 5.1.

(b) Else branch: "kills the HESSIAN member ONLY and leaves the grant
conditional on hull-theta" UNDER-ENUMERATES. A derivation concluding
strict s-concavity AT HULL STATES presupposes s(v, eps) DEFINED and
single-valued there — i.e. it consumes the totality and
root-quantifier members exactly as it consumes hull-theta. An
else-branch firing therefore leaves the grant conditional on
hull-theta AND the two chart members; the printed consequent names
one of three. The re-scope's own annotation concedes the mechanism
("revision 7 made the condition two-membered ... a Hessian-side
firing must not erase the ... member it either consumed as input or
never touched") and then applies it to the rev-7 member list while
the SAME revision enlarged the condition to four members — the
ER3L1-3 pattern ("condition enlarged under an unchanged consequent")
reproduced one revision later at the two new members.

Class: **AMENDMENT** — auxiliary falsifier text guarding the
condition-claim, the exact lineage class (EL0-4, E2L0-2, E4L0-3,
ER3L1-3 — all AMENDMENT of record); no proof content, statement, or
label exposed; the chart members retain independent falsifiers in
their §13 entries, so no route-level conclusion is at risk. A
strained benign reading of (a) exists ("whole EOS domain" read as
"all states the analysis meets, hull included") — named and
rejected: it is not what "the pins are read on the whole EOS domain"
says, and the row's own five-times-enforced written-line standard
forecloses unwritten rescues. Repair, one clause covering both
sites: "... restores the grant's PIN-READABLE members in the
whole-domain branch (Hessian + hull-theta; the chart clauses are
domain geometry no pins-reading or derivation delivers — they stay
standing members with their own falsifiers); in the else branch a
firing kills the Hessian member ONLY, leaving the grant conditional
on hull-theta AND the chart clauses."

DEDUP DECLARATION: this is a partial-failure citation of
E4L0-3/ER3L1-3 — their dispositions fail at exactly the two sites
above (the member-wise re-scope they demanded was delivered against
the wrong member count, and the totality caveat was inverted in
transcription); everything else in their delivered repair is
verified (5.4 below). Not a re-raise of E2L0-2 (restoration domain —
intact) nor of the PROVIDED/else split itself (E3L0-2's demand —
intact and confirmed of record).

### 5.3 The §13 leg-3 dryness recital — **OBJECTION ECL0-3,
### AMENDMENT** (bookkeeping over-count against the record)

Quote (lines 3975-3981): "The leg-3 escalation thread remains DRY AT
BOTH LENSES; the E-1 spec text (three-step periodization: single-
window D'-pairing, Sum chi_k == 1 on field terms, Sum chi_k' == 0
killing cutoff-derivative terms; (EU-x) equality pairing riding the
same identity) is unchanged since revision 5 with zero sustained
findings across l0 rounds 1-4 and l1 rounds 1-3."

Attack (record arithmetic). Round 1 sustained TWO leg-3 findings at
lens l0 — EL0-1 (§13 E-1 falsifier bracket) and EL0-2 (the (EU-x)
equality rider, the very rider text this recital names) — and one at
lens l1 (ESC1-F1 == EL0-1), all consumed AT revision 5; that is WHY
the text is "unchanged since revision 5" (verified at source:
esc_doc1_r1_l0.md summary rows "| EL0-1 | 3 |", "| EL0-2 | 3 |").
"Zero sustained findings across l0 rounds 1-4 and l1 rounds 1-3" is
therefore false as printed: it either counts rounds that DID sustain
leg-3 findings (rounds 1) or counts, toward the revision-5 text's
dryness ledger, rounds that never examined that text (it did not
exist at round 1) — the over-certification pattern this loop prices.
The count of record is the final judge's (VERDICT_escalation §2
DOC-1 special finding): dry for THREE consecutive l0 rounds
(revisions 5/6/7 re-tested) and TWO consecutive l1 rounds. The
recital's first half ("ZERO revision-7 edits and ZERO findings at
both round-window passes", lines 3968-3969, and the fresh-attack
inventory) is accurate against both input files.

Class: **AMENDMENT** — log-block bookkeeping only; the leg-3 dry
STATUS itself is true of record and untouched (no leg-3 body text is
attacked; none is delta). Repair, one clause: "with zero sustained
findings across l0 rounds 2-4 and l1 rounds 2-3 (the round-1
findings EL0-1/EL0-2/ESC1-F1 were consumed AT revision 5 and are why
the text dates from it)". Internal-consistency note: the same
block's dedup-adjacent claim is self-refuting as printed — a text
"unchanged since revision 5" presupposes revision-5 repairs, i.e.
round-1 findings.

DEDUP DECLARATION: attacks REVISION-8 log text only (the recital did
not exist before revision 8); no prior ID covers it; the leg-3
mathematics is not touched (dry at both lenses of record, per
VERDICT_escalation §2 — this finding does not reopen the thread, it
corrects its ledger).

### 5.4 CONFIRMED delta passages (strongest attacks tried and failed)

**(a) The strict Legendre bridge (lines 2222-2234)** — the
mathematical heart of the delta; attacked hardest, survives. Quote:
"FIRST LINK (the strict Legendre bridge, WRITTEN in revision 8 per
E4L0-1 ...): given theta > 0, strict PD of Hess e(v, S) is
EQUIVALENT to strict concavity of s(v, eps) — congruence
Hess_{(v,eps)} s = -(1/theta) C^T Hess_{(v,S)} e C with
C = [[1, 0], [-e_v/theta, 1/theta]] invertible (quadratic-form
identity Q_s(dv, deps) = -(1/theta) Q_e(dv, dS) at
dS = (deps - e_v dv)/theta), rank-faithful, uniform on compacts by
continuity of C". Attacks: (i) entry-by-entry re-derivation from the
implicit relation e(v, s(v, eps)) = eps: s_v = -e_v/theta,
s_eps = 1/theta; differentiating e_v + theta s_v = 0 and
theta s_eps = 1 gives s_vv = -(e_vv + 2 e_vS s_v + e_SS s_v^2)/theta,
s_veps = -(e_vS - e_SS e_v/theta)/theta^2, s_epseps = -e_SS/theta^3 —
each MATCHES -(1/theta) C^T Hess e C evaluated at (dv, deps) =
(1, 0), polarization, (0, 1) respectively: the congruence is an
IDENTITY, no remainder. (ii) sign/direction: the scalar -(1/theta)
is negative for theta > 0, so PD of Hess e transfers to strict
NEGATIVE definiteness of Hess s = strict concavity, and the converse
holds through C^{-1}: the printed EQUIVALENCE is exact, rank
preserved by congruence + nonzero scaling. (iii) uniformity chase
(strongest): "uniform on compacts by continuity of C" needs
1/theta bounded on the compact, i.e. min theta > 0, i.e. continuity
of theta along the chart — which lands exactly on the root-map
continuity carried in ECL0-1 (delivered by slice-connectedness +
totality + the universal quantifier; not by the bridge text itself).
The bridge is sound as written GIVEN the chart clauses; no separate
defect. (iv) hypothesis fit (R-4): "given theta > 0" + EOS C^2 are
the row's own members (hull-theta clause; the §1-implicit C^2
consumption stated at line 2247) — the transplant fits. (v) splice
check (lines 2238-2239): "STRICT s-concavity, delivered by the
bridge above, transfers to strict slice positivity" — the assembly
now consumes the bridge's output at the correct site; the E4L0-1
chain (l1 steps (2)-(4) plus the previously-missing step (1)) is
complete in the row. CONFIRMED.

**(b) The totality zeroth clause (lines 2153-2162)** — attacks:
(i) well-formedness: v(W_s) = 1/rho_s is defined on the grant's
convex physical region {rho >= rho_min}; the range condition is
stated on the row's own objects. (ii) does it close ER3L1-2's
witness? Yes: the thin-S-band patch EOS now VIOLATES the printed
zeroth clause at the exiting mid-segment states — the vacuous-
satisfaction mechanism is dead. (iii) the "abstract-EOS counterpart
of the table-domain precondition" naming is exact (the AUD-cp
sentence prices the same object at the closure). The gloss "theta
licenses uniqueness of the inversion, not surjectivity" inherits
ECL0-1 at source (uniqueness holds only modulo slice-connectedness)
— carried there, not double-counted. Otherwise CONFIRMED.

**(c) The c_v attribution fix (lines 2209-2219)** — attacks:
(i) identity re-derivation: c_v = (de/dtheta)|_v = theta/e_SS since
(dtheta/dS)|_v = e_SS — the row's identity is exact; (ii) member
split: e_SS > 0 from the strict pair (e_vv > 0 AND
e_vv e_SS > (e_vS)^2 >= 0 force e_SS > 0), SIGN of c_v from
hull-theta — exactly as now printed; (iii) the witness recital (the
(m2)-class quadratic patch, a0 large negative: strict pair
everywhere, c_v < 0) matches ER3L1-1's witness verbatim-faithfully;
(iv) the ideal-gas caveat ("where the superseded attribution was
harmlessly true") is correct — theta > 0 automatic there. The
conclusion clause ("exactly why the printed condition self-suffices
at the certified quadruple and at no strongly-convex g") survives
unchanged and was confirmed at both prior lenses. CONFIRMED — the
ER3L1-1/E4L0-4(b) discharge is clean.

**(d) The c^2 gloss fix (lines 2177-2182)** — attacks: (i) identity
c^2 = v^2 e_vv re-verified (p = -e_v, p_rho|_S = v^2 e_vv);
(ii) internal consistency restored: the row no longer claims hull
inheritance from §1 — "the §1-pinned QUANTITY, here imposed
hull-wide by the condition itself" is exactly the true statement,
with the domain-limitation cross-reference accurate; (iii) hunt for
residual "§1 pin" hull-attributions in the carriers — none found.
CONFIRMED — the E4L0-4(a) discharge is clean.

**(e) The AUD-cp chart-clause sentence (lines 2315-2320)** —
attacks: (i) push ECL0-1 through the closure (strongest): the
tabulated range is an INTERVAL and theta = T > 0 globally on it —
slices connected, roots unique by monotone inversion, exactly as
printed; the disconnected-slice witness has no purchase; (ii) "hull
evaluability inside the tabulated range IS the totality clause" —
correct identification, priced once for all consumers of record.
The "prices the ABSTRACT-EOS accounting only" scoping survives all
three of this round's findings — which is part of why none is
BREAKS. CONFIRMED.

**(f) Propagation carriers** — header audit line (102-105:
"CHART-COMPLETED — totality clause (S(W) defined hull-wide) +
universal root-quantifier added to the condition, strict Legendre
bridge and member-wise falsifier consequent written — per revision
8, E4L0-1..4/ER3L1-1..3"); honest status line (2424-2440, including
the new "(sign via hull-theta, ER3L1-1)" scalar-shadow annotation
and "chart total and single-valued there (the revision-8 clauses)");
§9 gamma table (2651-2658, "table conclusions UNCHANGED" — verified);
§13 revision-7 echo (3578-3582, ATTRIBUTION SUPERSEDED-IN-PART with
prior text preserved per the document's supersession convention);
header intro (23-26). Strongest attack: hunt a stale carrier of the
superseded strict-pair attribution or a chart-clause-free condition
form — grep over "strict pair", "single-valued", "CHART", "already
contains": every surviving superseded form sits inside preserved §13
text under a supersession note. All carriers inherit ECL0-1/ECL0-2
only through the source clauses they transcribe (carried once at
source). CONFIRMED as carriers.

**(g) The §13 REVISION 8 block's bookkeeping (3768-4041, minus the
leg-3 recital consumed as ECL0-3)** — attacks: (i) counts: "seven
findings, ALL leg 5, ALL class AMENDMENT (0 REPAIR-NEEDED, 0
BREAKS)" — matches the two input files exactly (4 + 3, all
AMENDMENT); (ii) merges: E4L0-4(b)+ER3L1-1 MERGED-AT-THE-CLAUSE and
E4L0-3+ER3L1-3 merged — both supported by the files' own dedup
declarations (same clause, independent mechanisms); (iii) the seven
consumption entries transcribe mechanisms, witnesses, provenance
(including ER3L1-3 as the round's sole non-seeded finding and the
l1 refuter's self-catch) faithfully — checked entry-against-file;
(iv) discharge-verdicts recital (3944-3966) matches both files'
discharge sections including the named residuals; (v) honest residue
(1) correctly declares the revision-8 repairs unadjudicated (this
document is the l0 half of that adjudication) and (3) keeps the
Blocco-2 held-out set closed of record; (vi) residue (2)'s
restatement claim is adjudicated in §R4 above: true at four clause
families, refuted at two (consumed as ECL0-1/ECL0-2 — residue (2)
itself mandates exactly this round's posture, which found them).
One item examined and left below finding grade, declared: the
round-number note's "each declared in the refuter's own header"
(3777-3780) — the l1 file's header declares its ROUND/LENS PLACEMENT
rather than a brief-said-revision-4 mismatch; the declarations chain
as a whole was verified by the final judge (VERDICT_escalation
NULL=FAILURE check), and no artifact was overwritten: no action
owed. Otherwise CONFIRMED.

==============================================================================
## §D DISCHARGE VERDICTS (the seven revision-8-consumed findings)

- **E4L0-1 (AMENDMENT): DISCHARGED CLEAN** — the bridge is written
  as the assembly's first link with hypotheses anchored to the row's
  own members, verified exact entry-by-entry this pass (5.4a); the
  splice consumes it at the right site. No residual.
- **ER3L1-1 + E4L0-4(b) (AMENDMENT, merged): DISCHARGED CLEAN** —
  attribution moved to the condition of record with the member-wise
  split and the in-class witness in place (5.4c); the §13 echo
  carries the supersession annotation; the status-line shadow clause
  is annotated. No residual.
- **E4L0-4(a) (AMENDMENT): DISCHARGED CLEAN** — the gloss is
  internally consistent with the row's own domain accounting (5.4d).
  No residual.
- **ER3L1-2 (AMENDMENT): DISCHARGED AS SPECIFIED, one residual one
  rung out** — totality is written as the zeroth member and kills
  the vacuous-satisfaction mechanism (5.4b); the residual is NOT in
  the delivered clause but in its companion's domain topology
  (ECL0-1), which the "licenses uniqueness" gloss inherits.
- **E4L0-2 (AMENDMENT): DISCHARGED IN PART** — the universal
  quantifier is explicit and the parity line is written as demanded;
  the disposition FAILS at the parity mechanism's transplanted
  connected-domain assumption (ECL0-1): the delivered clause closes
  the connected-slice mechanism only, and the entry's own registered
  falsifier fires on the disconnected-slice witness.
- **E4L0-3 + ER3L1-3 (AMENDMENT, merged): DISCHARGED IN PART** — the
  branch split and the hull-theta non-erasure principle are
  delivered; the disposition FAILS at the member arithmetic (ECL0-2):
  the re-scope enumerates the rev-7 members while revision 8 itself
  enlarged the condition, and the transplanted totality caveat was
  inverted into a delivery claim.
- Standing dispositions (r2-batch; rounds 1-3 window) re-verified
  against the revision-8 state at this lens: the revision-8 edits
  reopen NONE of them; no orphan objection remains in any input file
  on disk (every ID has a standing disposition; the two partial
  failures above are cited by ID with failure points stated).

The round is NOT dry at this lens: 3 findings sustained
(1 REPAIR-NEEDED + 2 AMENDMENT, 0 BREAKS-THE-LEG). Per
VERDICT_escalation §6 R-1, doc1 therefore does NOT close on this
round; legs 3 and 5 stay OPEN pending consumption of ECL0-1..3 and
the next confirming round.

==============================================================================
## SUMMARY

| ID | Leg | Passage | Class | One-line |
|---|---|---|---|---|
| ECL0-1 | 5 | root-quantifier parity clause (l.2162-2172) + summary (l.2174-2176) + §13 entry falsifier (l.3831-3835) | REPAIR-NEEDED | the parity bracketing silently consumes S-slice CONNECTEDNESS of the EOS domain — unwritten; explicit in-class two-branch EOS (e0(v) + h_i(S) on I1 ∪ I2, theta > 0 everywhere, Hess PD, totality via one branch) satisfies EVERY printed member with a two-valued chart, revives E4L0-2's off-data-root landing, and IS the §13 entry's own registered falsifier firing; also underwrites the root-map continuity the assembly's compactness step and the bridge's uniformity consume; free at the gamma(T) closure (interval domain); E4L0-2/ER3L1-2 dispositions fail in part (cited); R-4 misfit instance 7 |
| ECL0-2 | 5 | re-scoped falsifier consequent (l.2344-2361) | AMENDMENT | member arithmetic written against the rev-7 two-member condition while rev 8 made it four-membered: whole-domain branch claims the reading "also delivers" totality (false — domain geometry no pins-reading delivers; ER3L1-3's "modulo totality" caveat transplanted and INVERTED, R-4 misfit instance 8); else branch leaves the grant "conditional on hull-theta" omitting the two chart members its own firing consumes; E4L0-3/ER3L1-3 dispositions fail in part (cited) |
| ECL0-3 | 3/5 (log) | §13 leg-3 dryness recital (l.3975-3981) | AMENDMENT | "zero sustained findings across l0 rounds 1-4 and l1 rounds 1-3" over-counts: round 1 sustained leg-3 findings at both lenses (EL0-1/EL0-2/ESC1-F1, consumed AT revision 5 — which the same sentence's "unchanged since revision 5" presupposes); the count of record is the judge's 3 l0 + 2 l1 dry rounds |

CONFIRMED (cannot break; strongest attacks recorded in-section): the
strict Legendre bridge with its splice (5.4a — congruence
entry-by-entry, equivalence both directions, rank-faithfulness,
uniformity chase resolved into ECL0-1's carried clause, R-4
hypothesis fit); the totality zeroth clause (5.4b — kills ER3L1-2's
vacuous-satisfaction mechanism as demanded); the c_v attribution fix
(5.4c — identity, member split, witness recital all exact); the c^2
gloss fix (5.4d — identity + restored internal consistency); the
AUD-cp chart-clause discharge (5.4e — ECL0-1 pushed through the
closure and absorbed by the interval table domain); all five
propagation carriers (5.4f — no stale superseded form of record);
the §13 REVISION 8 block's counts, merges, transcriptions,
discharge recital, and honest residues minus the leg-3 recital
(5.4g).

Counts: 3 findings total = 1 REPAIR-NEEDED + 2 AMENDMENT;
**BREAKS-THE-LEG: 0**. No statement, rigor label, or route-level
conclusion is touched by any finding: ECL0-1's repair adds the
domain ladder's outermost written rung and STRENGTHENS the
no-viable-abstract-EOS-route conclusion a sixth time; all three
findings are discharged automatically at the standing gamma(T)
closure (interval table domain; bookkeeping textual). The [T-T0P]
main statement stays SCHEMA; the G8/r2 OPEN verdict, two-piece
residue, and gamma-table conclusions stand. Leg 3's dry status is
UNTOUCHED (ECL0-3 corrects its ledger, not its mathematics).

## SELF-FALSIFIERS (what kills each finding)

- ECL0-1: a printed line of record making disconnected-S-slice EOS
  out-of-class (none: §1 pins no EOS domain — established, sustained,
  and consumed at ER3L1-2, whose own self-falsifier cites it); or a
  derivation that totality + universal root-quantifier + Hess PD
  force slice-connectedness or root uniqueness WITHOUT it (false:
  the explicit two-branch witness satisfies all three with two
  roots); or a demonstration that the two-branch witness violates
  Gibbs closure, c^2 > 0, theta > 0 at roots, PD, or totality as
  printed (each verified above by construction); or a ruling that
  "e(v, .) passes from above the level to below it" presupposes an
  interval domain AND that presupposed hypotheses need no written
  statement — the second half contradicts the document's own
  five-times-enforced "the line must be WRITTEN" standard.
- ECL0-2: (a) a derivation of totality (a range-coverage fact) from
  any reading of the §1 pins (impossible: pins constrain e's
  derivatives where e is DEFINED, never where it is defined —
  ER3L1-2's witness class satisfies whole-domain pins with totality
  false); (b) a reading of record under which an else-branch
  s-concavity conclusion AT hull states does not presuppose s
  defined and single-valued there (none: concavity of an undefined
  or multivalued function is not a statement); or a printed line
  making "the condition-claim" denote the Hessian member alone
  (none: lines 2129-2187 define the condition as the four-member
  conjunction).
- ECL0-3: a demonstration that EL0-1/EL0-2 were not leg-3 findings
  (refuted at source: esc_doc1_r1_l0.md summary rows) or that they
  were not sustained (refuted: consumed at revision 5 of record);
  or a reading of "across l0 rounds 1-4" that excludes round 1
  (none available in ordinary arithmetic).
- Any CONFIRMED verdict above: a derivation-level break of the
  quoted passage that the recorded strongest attack missed.

END — confirming round on the revision-8 delta, lens l0 (the l0 half
of the VERDICT_escalation §6 R-1 owed round). Machine summary:
{objections: 3, breaks: 0, repairs: 1, amendments: 2,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc1_confirm_l0.md}
