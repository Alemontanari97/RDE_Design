# ESCALATION REFUTATION — DOC-3 r3.5 repair text, CONFIRMING ROUND (until-dry ROUND 5), lens l1

Lens: gas-dynamics / physics, RH algebra, counterexamples. Date: 2026-08-19.
Target: `validation/sfoundations_raws_2026-08-13/phaseD_L4_implies_R1.md`,
ATTACK SCOPE STRICTLY = the REVISION r3.5 delta only (until-dry ROUND 4
consumption, 2026-08-19): the ESC4-2 repair with its honesty clause
(Gamma_in uniform-noncharacteristicity generically VIOLATED in the
motivating class; Status-(a) H^1 leg carries the residual there), the
ESC3-4/ESC3-5/ESC4-1 consumptions (pair-relative slogan tail; third-bullet
certification-relative rewrite; fifth/sixth wording-universal annotations),
the ESC3-6 consumption (Status-(a) premise time cylinder), the W^{1,1}
route "no debt on ANY face" extension, the NG-3 register mirror extension,
and the r3.5 revision-log block + disposition table. Machine sweep of
scope (`grep -n "r3\.5"` on the target): lines 22, 242-280 (log block +
disposition table), 921 (slogan tail), 975 (third bullet), 1023 (Status-(a)
premise), 1051 (Status-(a) H^1 discharge clause), 1067 (W^{1,1} route
annotation), 2064 (NG-3 register row). NO r3.5 anchor exists inside the
two-face proof body (Claim, (cl-C)/(cl-F), (D-coll), (H-UP-fam), eps_1',
STEPs 1-3, UNION — byte-level r3.2/r3.3 text): the log's "NO step of the
two-face proof moved this round" is TRUE of the diffs. Remark 1.5.5 carries
no r3.5 marker (the log's "A-2: NO EDIT NEEDED this round" is true of the
diffs).

Adjudication context consumed FIRST, per the launching brief:
`r2pass/VERDICT_escalation.md` §2 DOC-3 (round trajectory verified;
NOT-DRY-AT-CAP confirmed; "the loop is one two-lens confirming round per
document away from dry"), §6 R-3 (r3.5 repairs UNADJUDICATED, one two-lens
round owed; NG-3 owns the variable-type residual on Gamma_mid AND
Gamma_in's grazing loci; the W^{1,1} Gauss-Green route named-not-consumed),
and §6 R-4 (BINDING for confirming rounds: every repair clause restated
against the row's own hypotheses, never transplanted verbatim from a
refutation file — six-plus declared spec-seeding instances; the rule check
is executed in §0 below). Prior rounds consumed IN FULL for dedup:
`esc_doc3_r1_l0.md` (ESC-L0-1..7), `esc_doc3_r1_l1.md` (F-1..F-6),
`esc_doc3_r2_l0.md` (ESC2-1..5), `esc_doc3_r2_l1.md` (ESC2-6..9),
`esc_doc3_r3_l0.md` (ESC3-1..3), `esc_doc3_r3_l1.md` (ESC3-4..6, this
lens's own prior round), `esc_doc3_r4_l0.md` (ESC4-1..2).

DEDUP DECLARATION: no dispositioned objection is re-raised at its own
content; every disposition of the r3.5 table is engaged only through
verification, and ALL FIVE r3.5 dispositions are ADEQUATE as dispositions
(none is attacked as failed). Both objections below target r3.5-NEW text
with zero prior adversarial coverage: ESC5-1 attacks a conditional that the
r3.5 SLOGAN parenthetical drops while the same window's third bullet and
the ESC4-1 disposition row both carry it (no prior ID raised it — the
slogan parenthetical did not exist before r3.5); ESC5-2 attacks the
depth-scope of the r3.5-NEW boundary-enumeration parenthetical inside the
Status-(a) discharge sentence (ESC2-8 and ESC4-2 raised missing FACES,
never the depth-family naming; checked against all seven prior files). ID
namespace continues this lens's sequence after ESC3-4..6 into the round-5
window: **ESC5-1, ESC5-2**.

==============================================================================
## §0 BINDING R-4 RULE CHECK (VERDICT_escalation §6 R-4), executed per
## disposition: restated-against-the-row vs transplanted-verbatim

- **ESC3-6 disposition (premise cylinder): PASS.** The landed fix
  "W^{1,infinity} on all of Omega_up x [0, T_f] WITH (G3)-BOX VALUES" is
  the document's OWN convention, verified verbatim against (H1.1) (line
  ~450: "W^{1,infinity}(Omega x [0, T_f]), values in the (G3) box") and
  (H1.1') (line ~723: "W^{1,infinity}(C_h x [0, T_f])"). The rationale
  clauses (del_t S in the E'(t) volume term; the C^0 \ W^{1,infinity}
  counter-instance) originate in ESC3-6's text but FIT the row exactly —
  the row's own (EI)/Gronwall run is the consumer they describe. No misfit.
- **ESC3-5 + ESC4-1 disposition (third bullet): PASS.** The landed bullet
  RESTATES the refuters' clauses against the row's own hypotheses: the
  kernel branch carries the class-delegation conditional "if NG-9's
  instantiation of the class admits it" (which ESC3-5's proposed one-clause
  fix, authored before ESC4-1 existed, did not contain), the kernel
  condition is spelled with its loci and mechanism, and the accidental-glue
  branch keeps unconditional entry — correct at the (H1.4)/C^1 grade floor
  of the Status's own scoping. Fitted, not transplanted.
- **ESC3-4 + ESC4-1 slogan-half disposition (slogan tail): FAIL — one
  misfit, minted as ESC5-1.** The escape-clause SHAPE is ESC3-4's proposed
  parenthetical (authored for the accidental-C^1-glue branch alone, where
  unconditional class entry is in-floor); r3.5 extended it to ESC4-1's
  kernel-jump branch WITHOUT restating it against the row's class-delegation
  hypothesis — the slogan asserts unconditional entry for a pair that is
  not H^1 and enters only if NG-9 widens the class. Exactly the defect
  class R-4 exists to catch: same window, three sites (disposition row,
  bullet, slogan), the transplant-shaped site is the one that misfits.
- **ESC4-2 disposition (Gamma_in condition + honesty clause): PARTIAL —
  the residue is minted as ESC5-2.** The condition is restated in the
  row's own vocabulary ("uniformly noncharacteristic in the same sense",
  bounded-below quantities), NOT the refuter's weaker phrase ("outside the
  loci an explicit construction covers") — a genuine strengthening-by-
  restatement, PASS on that clause. The boundary enumeration is also
  restated against the row's own depth-family form (the refutation wrote
  "Gamma_in ∪ walls ∪ Gamma_mid" at fixed h/2; the row writes "Gamma_in
  union walls union Gamma^coll_s") — but the restatement was NOT
  propagated to the mid-face condition the parenthetical justifies, which
  stays pinned to Gamma_mid: ESC5-2.
- **NG-3 register mirror disposition: PASS.** Register-form compression,
  row-appropriate; prose↔row fidelity verified clause-by-clause (§3 C7).

==============================================================================
## §1 DISPOSITION VERIFICATION (the r3.5 repairs vs the five objections
## they answer; pen grade, this lens)

**ESC3-4 — FIXED as dispositioned** (the pair-relative main clause is
correct; the kernel-branch parenthetical is ESC5-1, a NEW defect the fix
introduced, not a failure of the ESC3-4 disposition: ESC3-4's own two
failing readings are both repaired — the structure-of-the-proof reading no
longer self-contradicts at Gamma_in^coll because the predicate now tests
the PAIR's membership, and the pair-supplies reading's accidental-glue
counterexample is absorbed by the escape clause). The FIFTH-defect
annotation records both failed readings and the adopted-phrase provenance
faithfully against ESC3-4's text. ADEQUATE.

**ESC3-5 — FIXED as dispositioned.** The bullet's exclusion is now
certification-relative ("the bare COUPLING certifies no such cross-face
solvability for its pairs"), the accidental-glue and kernel-jump escape
branches present, the concluded uniqueness declared honest, "STEP 3 has
nothing the COUPLING supplies to consume" — the refuter's own true
statement, landed with the NG-9 conditional added. ADEQUATE.

**ESC3-6 — FIXED as dispositioned.** Cylinder printed; convention match
verified verbatim (§0); rationale and counter-instance recorded. ADEQUATE.

**ESC4-1 — FIXED as dispositioned** (combined rewrite with ESC3-5; the
kernel-jump subfamily scoped in the bullet WITH the NG-9-instantiation
conditional, the SIXTH-defect annotation records both refuting subfamilies
and that the conclusion held in every printing). The slogan half of the
same combined rewrite carries the ESC5-1 misfit — again a new defect of
the fix's third site, not an inadequacy of the disposition, whose row text
itself states the conditional correctly ("where NG-9's instantiation
admits it"). ADEQUATE.

**ESC4-2 — FIXED as dispositioned, branch (r-i') landed and STRENGTHENED
relative to the demanded clause** (uniform noncharacteristicity on
Gamma_in vs the refuter's "outside the loci an explicit construction
covers"), with the honesty clause the objection did not even demand: in
the motivating class the Gamma_in condition is generically VIOLATED, so
the H^1 leg carries the residual there, not the discharge — this is the
single most load-honest sentence of the round (it prices the repair's own
condition as generically unavailable rather than declaring victory). NG-3
residual (ii) extended to Gamma_in's loci; W^{1,1} route annotated
debt-free on ANY face, still named-not-consumed; the upstream 1.5.3/row-1
silence declared out of scope with owner NG-3 (F2). ADEQUATE.

**Revision-log block + disposition table — VERIFIED.** Counts: 5 IDs =
2 (ESC4-1..2 = 0B + 1R + 1A) + 3 (ESC3-4..6 = 0B + 0R + 3A) — match both
round files' summary tables exactly. The quoted discharge verdicts of
`esc_doc3_r4_l0.md` §1/§4 (ESC3-1 modulo ESC4-1; ESC3-2, ESC3-3, ESC2-9
clean; ESC2-6/ESC2-7 discharged; ESC2-8 as raised modulo ESC4-2; second
consecutive zero-inadequate round; proves-too-much 7 instances) and of
`esc_doc3_r3_l1.md` §1 (all seven r3.4 dispositions adequate; L0-7/L1-7
remain discharged; 7 instances) are faithful. The dedup notes
(ESC4-1/ESC3-5 same universal distinct subfamilies; ESC3-4 same-paragraph-
different-sentence; ESC3-6 same-sentence-different-clause) are true
against the files. Edit-site enumeration ("all five findings live in...")
is complete against the 8-anchor machine sweep. "NO step of the two-face
proof moved" TRUE. "No gas-model content in any r3.5 edit" TRUE (the
kernel-jump algebra is EOS-generic through Lemma 0.2's eigenstructure).
The declared BRIEF/ROUND DEVIATION (third instance) preserves the audit
chain — `esc_doc3_r3_l1.md` is identified by name + ID range + attacked
state, so calling it a "round-4 refutation" in the grouping sense creates
no ambiguity (the judge's own §2 groups the same two files as one
two-lens window). ADEQUATE.

==============================================================================
## §2 OBJECTIONS (r3.5 text; verbatim quote + attack + class)

------------------------------------------------------------------------------
**ESC5-1 — class AMENDMENT. The r3.5 slogan parenthetical asserts
UNCONDITIONAL (H-UP-fam)-class entry for the kernel-jump pair ("it enters
(H-UP-fam)'s delegated class"), dropping the NG-9-instantiation
conditional that the SAME r3.5 window prints in the third bullet ("if
NG-9's instantiation of the class admits it") and in the ESC4-1
disposition row itself ("where NG-9's instantiation admits it") — and
that the document's own grade scoping (the retained ESC2-5 annotation)
makes mandatory, since a piecewise-C^1 pair with a genuine jump is not
H^1 (its gradient carries a surface delta) and so sits inside the class
floor of record only if NG-9 instantiates the class wider.**

Quote (Section 1.6, proves-too-much slogan, r3.5, line ~921):
> "and a bare-coupled PAIR belongs to no structure of the proof that
> crosses its face (r3.5, repairs ESC3-4 and the slogan half of ESC4-1:
> the predicate is PAIR-RELATIVE, not face-absolute — where a glued pair
> does happen to solve across its face on its own merits, by accidental
> C^1 glue or by a kernel jump A(n)[U] = 0 on the face's characteristic
> loci, it enters (H-UP-fam)'s delegated class and the concluded
> uniqueness is that hypothesis's honest content, not an over-proof; see
> the third bullet)"

Attack (class-membership derivation under the document's own scoping).
Split the escape clause by branch. (i) Accidental C^1 glue: the pair's
members are C^1 solutions of the interior problem on all of
Omega_int^{h/2} — (H1.4)-grade, inside the instantiation floor Status (a)
itself quantifies over; unconditional entry is TRUE. (ii) Kernel jump: the
pair is piecewise C^1 with a genuine jump [U] != 0 in ker A(n) across the
face — a distributional solution (ESC4-1, judge-re-derived), but NOT C^1
and NOT H^1 (the distributional gradient of a jump across a codimension-1
surface contains a surface measure; no H^1 representative exists). The
document's class floor of record is exactly "(H1.4)-grade (C^1) pairs ...
H^1 members via the 1.5.3 mollification extension" with the retained
ESC2-5 annotation stating in terms: "if NG-9 instantiates the class wider,
e.g. L^2-weak, case (a) beyond these grades is unproven". So kernel-jump
entry is CONDITIONAL on NG-9's instantiation — which is precisely how the
same window prints it twice elsewhere: the third bullet ("or, if NG-9's
instantiation of the class admits it, a jump satisfying the kernel
transmission condition...", line ~969) and the ESC4-1 disposition row
("such a pair belongs to NG-9's slip-line territory, and where NG-9's
instantiation admits it the concluded uniqueness is (H-UP-fam)'s honest
hypothesized content"). The slogan — the sentence pointing at that bullet
with "see the third bullet" — is the ONE of the three r3.5 sites that
drops the conditional: as printed it asserts, for every kernel-jump pair,
membership in a class whose extent is delegated and undetermined, and
asserts "concluded uniqueness" content for a pair the proof may conclude
nothing about (if NG-9 instantiates the class at the document's own floor,
the pair is NOT in it — the exclusion holds, which is FINE for the
paragraph's conclusion but makes the printed entry claim false). The
paragraph's CONCLUSION (no over-proof) survives on both horns: in-class →
honest hypothesized content; out-of-class → excluded, nothing concluded.
No proof step consumes the paragraph. The defect is the SEVENTH
wording-universal of the block-family by the document's own count — and an
R-4 instance of record (§0): ESC3-4's escape-clause shape, authored for
branch (i) where it is right, extended to branch (ii) without restatement
against the class-delegation hypothesis. Fix (one clause, mirroring the
bullet): "...by accidental C^1 glue or — where NG-9's instantiation of the
class admits it — by a kernel jump A(n)[U] = 0 on the face's
characteristic loci, it enters (H-UP-fam)'s delegated class...". Not
REPAIR: honesty commentary only, conclusion true on both horns, the
correct conditional already printed at the two sites that carry weight.
**Class: AMENDMENT.**

------------------------------------------------------------------------------
**ESC5-2 — class AMENDMENT. The r3.5-NEW justification premise inside the
Status-(a) discharge sentence enumerates the boundary of Omega_int^s at
GENERAL depth ("whose boundary is Gamma_in union walls union
Gamma^coll_s") while the two type conditions that premise justifies stay
pinned to the fixed h/2 face Gamma_mid — so for a given (D-coll)-class
split at depth s != h/2 (over which the Status's own headline "at every
depth with a given (D-coll)-class split" quantifies) the printed
discharge condition constrains a surface INTERIOR to Omega_int^s and
leaves the actual data face Gamma^coll_s type-unconstrained.**

Quote (Section 1.6, Status of (H-UP-fam), case (a), r3.5, lines
~1048-1053):
> "The H^1 leg is therefore DISCHARGED as cited only for splits with
> Gamma_mid uniformly noncharacteristic (|u-bar.n_mid| and
> |u-bar.n_mid -+ c-bar| bounded below) AND with Gamma_in uniformly
> noncharacteristic in the same sense (|u-bar.n| and |u-bar.n -+ c-bar|
> bounded below on Gamma_in; r3.5, repairs ESC4-2, refuter branch (r-i')
> — the 1.5.3 mollification must be justified on EVERY smooth boundary
> portion of Omega_int^s, whose boundary is Gamma_in union walls union
> Gamma^coll_s: ..."

Attack (the sentence's own premise turned on its condition list; geometry
at pen grade). The Status headline claims theoremhood "at every depth with
a given (D-coll)-class split"; (H-UP-fam)'s printed family form quantifies
over depths s with Gamma^coll_{h/2} = Gamma_mid as the one consumed
instance (line ~759). The r3.5 parenthetical states — correctly, and for
the first time in this sentence — the depth-GENERAL justification premise:
every smooth boundary portion of Omega_int^s, boundary = Gamma_in ∪ walls
∪ Gamma^coll_s. But the condition list it justifies names |u-bar.n_mid|
bounds on Gamma_mid = Gamma^coll_{h/2} specifically. Take a given
(D-coll)-class split at depth s < h/2: then Gamma_mid lies at distance
h/2 > s from Gamma_I, hence in the INTERIOR of Omega_int^s — the printed
condition constrains a surface where no mollification happens — while the
actual face Gamma^coll_s, on which the sentence's own premise demands
justification and on which nothing in (H1.1)-(H1.4)/(D-coll)/the Status
constrains u-bar.n (the ESC2-8 mechanism verbatim), carries no printed
condition. Read literally, the sentence discharges the H^1 leg at depth s
for splits whose IRRELEVANT interior surface is uniformly
noncharacteristic. Mitigations, honestly stated: the proof consumes ONLY
s = h/2 (printed, line ~758), no depth-s != h/2 split is given anywhere in
the document, and the generic reading "n_mid = the split's own mid-face
normal" restores the right condition — so no consumed conclusion is
touched and the mechanism is right at the only exercised depth. But the
Status-(a) claim is printed at every-depth scope, the r3.4 Gamma_mid
naming was adequate ONLY while the sentence was silent about general
depth, and it is the r3.5 enumeration itself that makes the misfit live
inside one sentence (the same
first-text-to-consume-on-a-domain-while-naming-it standard ESC4-2 applied
to Gamma_in). R-4 residue of record (§0): the boundary enumeration was
restated to the row's depth-family notation, the condition naming was
carried over from the refutation's fixed-depth phrasing unpropagated. Fix
(one naming clause): "...only for splits with the split's mid face
Gamma^coll_s (= Gamma_mid at the consumed depth h/2) uniformly
noncharacteristic (|u-bar.n| and |u-bar.n -+ c-bar| bounded below on
Gamma^coll_s) AND with Gamma_in ..."; NG-3 residual (ii) reads "Gamma_mid"
twice and inherits the same one-word generalization. Not REPAIR: at the
only consumed depth the condition, the honesty clause, and the register
mirror are exactly right; the over-claim exists only for hypothetical
never-given splits and dissolves under the generic reading of n_mid.
**Class: AMENDMENT.**

==============================================================================
## §3 CONFIRMED passages (strongest attack tried, each)

**C5L1-1 — the ESC4-2 repair: Gamma_in uniform-noncharacteristicity
condition + HONESTY CLAUSE + "(BOTH data faces), mirrored in NG-3":
CONFIRMED (modulo ESC5-2's depth naming, which does not touch the Gamma_in
clause).** Strongest attacks tried, all failed: (α) measure-zero evasion
of the honesty clause — can the motivating class satisfy the uniform
condition despite 1.5.5's generic grazing, e.g. grazing confined to a
curve on the rim? NO: Status (a)'s base is W^{1,infinity}, hence
continuous, so ANY grazing point x_0 (u-bar.n(x_0) = 0) forces
inf_{Gamma_in} |u-bar.n| = 0 — the uniform lower bound dies on a single
point, measure irrelevant; 1.5.5 states grazing generic FOR the motivating
RDE injection class, so "generically VIOLATED" is exact, not conservative.
(β) Vacuity — is the honesty clause about an empty intersection (device
class fails (H1.1) anyway)? NO: the motivating-class members at stake are
SMOOTH injection bases with an injector rim (the sanity-reduction class
Status (a) exists for); the clause is non-vacuous and prices exactly them.
(γ) Sufficiency at the consumed depth — with BOTH data faces uniformly
noncharacteristic, is the 1.5.3 route now fully justified portion-wise?
YES: Gamma_in and Gamma_mid noncharacteristic → Friedrichs (A(n)
uniformly invertible recovers the normal derivative); slip walls
u-bar.n = 0 identically, kernel multiplicity 3 constant (Lemma 0.2) →
Rauch 1985; corner curves → NG-3, inventory extended r3.3/r3.4. No fifth
uninventoried portion exists: the boundary enumeration Gamma_in ∪ walls ∪
Gamma^coll_s is verified against the geometry (Omega_int^s = Omega_up \
closure(C_s); Gamma_I is buried inside C_s; the axis is interior in R^3).
(δ) "characteristic but neither uniformly nor of locally constant
multiplicity" at grazing/sonic loci — verified: ker A(n) has dim 3 at
u-bar.n = 0, dim 1 at u-bar.n = ∓c-bar, dim 0 elsewhere, so type
transitions kill both uniformity and local constancy. (ε) Sonic-inflow
loci (u-bar.n = -c-bar, choked-injector feed — physically standard in RDE
injection): also excluded by the printed condition (the "-+ c-bar" bounds
cover them); the honesty clause needs only the grazing genericity to fire
and does not under-price by not naming sonic loci as also-generic. (ζ)
Does the honesty clause force label motion? NO: the moduli are printed in
the label sentence itself (THEOREM* modulo NG-3 AND modulo the
variable-type theory), and the leg is load-bearing only for the
smooth-base sanity reduction — case (b)/NG-9 and the two-face proof
consume nothing of it.

**C5L1-2 — the pair-relative slogan MAIN clause + FIFTH-defect
annotation (ESC3-4 consumption): CONFIRMED (the kernel-branch
parenthetical is ESC5-1).** Strongest attack: hunt for a reading under
which the NEW predicate fails as its predecessor did. Structure-of-the-
proof reading at Gamma_in^coll: the face IS crossed by (H-UP-fam)'s
hypothesized class, and the bare-coupled PAIR does not belong to it —
no self-contradiction remains (the r3.4 contradiction needed the
face-absolute form). Pair-supplies reading on the accidental-C^1-glue
pair: the pair DOES belong to a crossing structure, and the escape clause
says so in-sentence — the universal-plus-carved-exception form is
coherent. The annotation records both failed readings of the superseded
tail and the adopted-phrase provenance ("l1's own round-2 wording;
adoption does not immunize") faithfully — verified against this lens's
own ESC3-4 text.

**C5L1-3 — the third-bullet certification-relative rewrite
(ESC3-5 + ESC4-1 consumption): CONFIRMED.** Strongest attacks tried:
(α) the "only if" direction — is cross-face solvability NECESSARY for
(H-UP-fam)-class entry? YES: the hypothesis's printed quantifier is "two
solutions of the same interior problem on Omega_int^s"; whatever grade
NG-9 instantiates, class members are SOLUTIONS on the whole interior
domain, hence distributional solutions across every interior face —
delegation touches the grade, not solutionhood. (β) The kernel-jump
physics, re-derived at this lens: at u-bar.n = 0 the kernel of A(n) is
{p' = 0, u'.n = 0} — entropy + two shear modes, the slip-line class; a
jump there is a frozen contact/vortex sheet, and stationarity holds
where the face is a base-flow streamsurface portion (u-bar tangential),
so the subfamily is nonempty exactly on positive-measure characteristic
portions — the bullet's "on the face's characteristic loci ... nowhere
constrained on interior faces" is the right scoping; at u-bar.n = ∓c-bar
the kernel is the acoustic mode of vanishing NORMAL characteristic speed
(u-bar.n ± c-bar = 0), which likewise propagates tangentially to the
face — the bullet's parenthetical instantiates only the u-bar.n = 0 case
and claims nothing false about the others. (γ) "nothing in a bare
coupling FORBIDS smooth gluing; it merely fails to REQUIRE it" — exact,
and it is what makes the escape clause necessary rather than decorative.
(δ) The SIXTH-defect annotation's arithmetic: document counted four at
r3.4; r3.5 assigns FIFTH = the slogan tail (ESC3-4), SIXTH = the
third-bullet universal (ESC3-5/ESC4-1, shared sentence) — consistent,
and both round files' own "fifth" self-counts are correctly superseded by
the two-defect outcome of the merged window. (ε) "refuted on two
independent subfamilies of the judge's §5 test class" — attacked via
ESC4-1's own hedge (a kernel-jump glue satisfies A(n)[U] = 0, which IS a
transmission condition, arguably placing the pair outside "coupling with
NO transmission condition"): fails to bite — the test class quantifies
over COUPLINGS (which impose nothing; the bare coupling ADMITS the pair
without requiring its condition), and the refuted sentence quantified
over bare-coupled PAIRS, on which both subfamilies are genuine.

**C5L1-4 — the Status-(a) premise time-cylinder fix (ESC3-6 consumption):
CONFIRMED.** Strongest attacks tried: (α) convention fidelity — verified
verbatim against (H1.1) ("W^{1,infinity}(Omega x [0, T_f]), values in the
(G3) box") and (H1.1'); the fix is the document's own convention, not the
refuter's invention (R-4 PASS, §0). (β) FOURTH-missing-conjunct hunt at
the same premise sentence (the pattern of ESC3-2/ESC2-8/ESC3-6): domain
Lipschitz-ness = (D-coll)-class split + (D'') (cited in the (EI) run);
same forcing = printed; walls = slip in (H-UP-fam)'s own problem
definition; solution grade = the (H1.4)/C^1 + H^1-via-1.5.3 scoping;
box values now on the cylinder (the premise's WITH-clause binds to the
cylinder-quantified base); time regularity now printed; S, SA_j Lipschitz
= (H1.1-grade base) + (G1)-(G3), the document's standing gas hypotheses
(consumed by Theorem 1's own (EI) line "by (H1.1), (G2)" and in force
throughout Section 1) — NOTHING further found. (γ) The recorded
counter-instance W_0(x) + a(t)V(x), a in C^0 \ W^{1,infinity}: genuine —
box-compatible for small a, spatially Lipschitz slice-wise, and E'(t)
picks up <(del_t S)U, U> with del_t S unbounded, killing C_0.

**C5L1-5 — the W^{1,1} route annotation extension ("no characteristic-type
debt on ANY face, Gamma_mid or Gamma_in"): CONFIRMED.** Strongest attack:
is the no-debt claim face-uniform in truth? YES — the route mollifies
nothing at the boundary: <S A_j U, U> is W^{1,1}(Omega_int^s) slice-wise
for H^1 members with Lipschitz coefficients (product rule in L^1), and
the W^{1,1} -> L^1(del) trace + Gauss-Green on the (D'')-Lipschitz domain
consume NO invertibility of A(n) anywhere — characteristic type never
enters; the extension from "Gamma_mid" to "ANY face" is the route's
actual property, not an analogy. The clause stays honest: "not verified
in this document, so not consumed" — no rigor weight rides on it, and the
disposition's "(branch (r-ii') — verifying that route — NOT taken this
round)" states the debt truthfully.

**C5L1-6 — the r3.5 revision-log block + disposition table: CONFIRMED.**
Attacks tried: count/verdict fidelity against both consumed round files
(§1 — exact); dedup-note truth (verified sentence-by-sentence); edit-site
enumeration vs the 8-anchor machine sweep (complete); "NO step of the
two-face proof moved" (TRUE — no r3.5 anchor in the proof body);
"A-2: NO EDIT NEEDED" (TRUE — no r3.5 marker in Remark 1.5.5, and this
lens's round-3 spectrum recomputation stands: adjacent gaps c-bar,
pairwise separations >= c-bar(x) > 0 pointwise via the (G3) box,
multiplicities (1,3,1) constant); the declared brief/round deviation
(third instance, house precedent, chain intact); "no gas-model content in
any r3.5 edit" (TRUE — the kernel algebra is Lemma-0.2-generic, no
gamma(T) content moved).

**C5L1-7 — the NG-3 register mirror r3.5 extension: CONFIRMED.**
Strongest attacks tried: (α) prose↔row fidelity — residual (ii) carries
Gamma_in's loci, the genericity citation to 1.5.5, the both-faces
discharge condition, the honesty clause ("generically VIOLATED on
Gamma_in in the motivating class, where the leg carries this residual,
not the discharge"), and the r3.5-tagged W^{1,1} alternative — all
clause-matched to Section 1.6. (β) Consumer-navigation test re-run: a
consumer discharging NG-3 against its own row hits the live Gamma_in loci
and cannot close — the ESC4-2-demanded property holds. (γ) NEW attack,
this round's strongest: does the trigger column under-cover the
r3.5-extended content for a DIRECT Theorem-1 H^1 consumer (the first
trigger clause reads "first consumption of Theorem 1 at H^1 regularity on
a domain with corners" — corner-conditioned, while the Gamma_in
variable-type residual lives on a smooth portion)? FAILS to bite: in the
document's own geometry class the wall-interface corner curves (Gamma_w
meets Gamma_I) exist for EVERY admissible Omega_up — the corner qualifier
is satisfied by every in-scope consumption, so the trigger fires on every
Theorem-1 H^1 consumption of record; and the Status-(a)/Prop-1'' clause
covers the consumption site the residual was minted at. (ESC5-2's
one-word generalization of "Gamma_mid" in residual (ii) rides that
finding, not a separate row defect.)

**C5L1-8 — the two-face proof core at r3.5 state: CONFIRMED (unchanged
text; no new angle found).** The proof body (Claim with (cl-F)/(cl-C),
(D-coll), (H-UP-fam) restricted quantifier, eps_1', STEPs 1-3, UNION) is
byte-stable since r3.2 with no r3.5 anchor. Fresh attacks tried at this
lens this round: (α) feed STEP 3 a kernel-jump pair ACROSS Gamma_mid
itself (the new subfamily aimed at the old face) — excluded by STEP 1
exactly as bullet 2 prints it: Lemma 1.4's frusta straddle Gamma_mid and
need a single C^1 solution on C_h, which a jumped pair is not; (MID-TRACE)
never forms, and no over-proof arises. (β) A grazing INJECTOR-RIM base
(the honesty clause's own class) pushed through the C^1 leg of Status (a)
hunting for a hidden Gamma_in noncharacteristicity consumption — none:
the C^1 (EI) run integrates by parts with Lipschitz integrands on a
Lipschitz domain and uses only the SIGN structure on Gamma_in ((ii)/(ii')
zero or good-sign trace), never invertibility of A(n); the discharge
condition is an H^1-leg debt only, exactly as printed. (γ) The
proves-too-much test re-run on this round's instances (kernel-jump at
Gamma_mid, kernel-jump at a deep face, accidental glue at a deep face,
grazing-rim base): no over-proof — every exclusion or honest-uniqueness
outcome lands per the r3.5 taxonomy.

==============================================================================
## §4 Summary table

| ID | Passage (r3.5 text) | Class |
|---|---|---|
| ESC5-1 | slogan parenthetical: "it enters (H-UP-fam)'s delegated class" asserted unconditionally for the kernel-jump branch — the NG-9-instantiation conditional printed in the same window's third bullet and ESC4-1 disposition row is dropped at the slogan site; kernel-jump pairs are not H^1, entry is class-instantiation-conditional by the document's own ESC2-5 scoping; conclusion (no over-proof) survives on both horns | AMENDMENT |
| ESC5-2 | Status-(a) discharge sentence: r3.5-new premise enumerates del Omega_int^s = Gamma_in ∪ walls ∪ Gamma^coll_s at general depth while the mid-face condition stays pinned to Gamma_mid = Gamma^coll_{h/2}; at a given depth-s != h/2 split the condition constrains an interior surface and leaves Gamma^coll_s unconstrained; right at the only consumed depth; one-word family-naming fix + NG-3 residual (ii) mirror | AMENDMENT |

Totals: **2 objections = 0 BREAKS-THE-LEG + 0 REPAIR-NEEDED +
2 AMENDMENT.** All five r3.5 dispositions ADEQUATE (§1); the R-4 binding
rule check executed per disposition (§0: three PASS, one PARTIAL =
ESC5-2, one FAIL = ESC5-1 — both residues minted, neither inadequates its
disposition). No finding touches a proof step, a hypothesis of record, or
the honesty clause's content; both are one-clause wording fixes in the
honesty commentary and the discharge sentence's naming — strictly below
the r3-window's residue class (which still carried one REPAIR-NEEDED).
No label motion is proposed: SCHEMA per J-r2p-1 stands; restoration = the
E-4 successor adjudication only. Dryness reading for this lens: the ESC4-2
honesty clause — the round's centerpiece — is CONFIRMED at its strongest
reading; the residue is two naming-grade amendments, zero
derivation-content defects. Whether that meets the until-dry criterion is
the caller's and the successor judge's call, not this refutation's.

==============================================================================
## §5 Self-falsifiers (what kills each finding)

- ESC5-1 dies if piecewise-C^1 pairs with genuine kernel jumps are shown
  to lie in H^1 (they are not: the distributional gradient of a jump
  across a codimension-1 surface contains a surface measure, so no H^1
  representative exists), or if a document clause of record is exhibited
  instantiating (H-UP-fam)'s delegated class wide enough to contain them
  UNCONDITIONALLY (the opposite is printed: the class is NG-9's burden,
  and the retained ESC2-5 annotation declares case (a) unproven beyond
  the C^1/H^1 grades), or if "enters (H-UP-fam)'s delegated class" is
  shown of record to be elliptical for the bullet's conditional (the
  slogan cites the bullet but PRINTS the unconditional form — the
  block-family's six recorded defects are precisely such ellipses).
- ESC5-2 dies if "Gamma_mid" is shown defined of record as the generic
  mid face of an arbitrary-depth split (it is not: (D-coll) defines it as
  the FIXED face at h/2, and (H-UP-fam) names the general face
  Gamma^coll_s with Gamma^coll_{h/2} = Gamma_mid as an equation, not a
  definition), or if the Status-(a) H^1 discharge sentence is shown scoped
  to the consumed depth h/2 only (its headline quantifies "at every depth
  with a given (D-coll)-class split"), or if Gamma_mid is shown to lie on
  del Omega_int^s for every admissible s (false: for s < h/2 it is
  interior to Omega_int^s at distance h/2 - s from the boundary face).

END — machine summary: {breaks: 0, repairs: 0, amendments: 2,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc3_confirm_l1.md}
