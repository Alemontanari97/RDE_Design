# BRIEF — DOC-1 REVISION 10 REVISER (S-FOUNDATIONS-C2, 2026-08-19)

ROLE: author REVISION 10 of
`validation/sfoundations_raws_2026-08-13/phaseD/phaseD_stop_proof.md`
(4460 lines at rev 9), executing the closure-judge mandate PLUS the
user-ratified sufficiency strategy. You edit ONLY this file. All paths
relative to the repo root
`c:\Users\amont\Claude\Projects\Presentazione RDE CVA\rde-lecture-code`.

## AUTHORITY CHAIN (read IN FULL, in this order)
1. `validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_doc1_closure.md`
   — the mandate. §1 carries the ADOPTED repair specs verbatim (R9-A
   spec + propagation list; R9-B repair text; R9-C repair text); §3(c)
   fixes the R-4 instance count at ELEVEN; §4 names the residues
   RES-DOC1-1 / RES-DOC1-2 you are consuming.
2. The strategy paragraph of
   `validation/ADVISORY_SfoundationsC2_prompt_2026-08-19.md` (§ PRIMA
   AZIONE PARALLELA): RATIFIED STRATEGY — re-establish the G8 granted
   half as a SUFFICIENT-BUT-UNOPTIMIZED condition on STRONG,
   trivially-checkable hypotheses (global joint convexity +
   slice-connected single-chart domain + in-chart hull); the row is
   gap ACCOUNTING, not load-bearing theory; over-generality has bought
   5 rounds — stop buying.
3. `validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_confirm.md`
   preamble + §6 R-1'/R-5' (dry criterion of record; held-out set).

## TASKS (one pass, all together)
(A) RES-DOC1-1 (R9-A REPAIR). Baseline = the adopted spec in
VERDICT_doc1_closure §1 R9-A: add the FIBER-REGULARITY member (fourth
chart member or fused into the third; fallback branch: where fiber
regularity is not available, root-map continuity / image compactness
is imposed directly as a member), with the SAME-PASS propagation to
every named carrier: the clause's "under which ... continuous"
(re-scoped), assembly splice (rev-9 l.2296-2302), falsifier-consequent
enumeration (l.2403-2404, jointly with R9-B), grant head (jointly with
R9-C), header (l.110-116), §9 table, honest status line, AUD-cp note
(one added clause: fibers continuous at the gamma(T) closure), §13
ECL0-1 entry delivery sentence + falsifier list (ADD the moving-slice
witness falsifier for the continuity sub-claim), and the domain-ladder
sentence (l.4251-4254; new outermost rung: ... -> chart-domain
TOPOLOGY (slice-connectedness) -> slice-FAMILY regularity).

(B) STRATEGIC RESTATEMENT (ratified, the load-bearing move of rev 10).
Restate the G8 granted half as **SUFFICIENT-BUT-UNOPTIMIZED**:
- Hypothesis set (strong, each trivially checkable on a given EOS):
  (H-G8-1) GLOBAL JOINT CONVEXITY: dom e convex and e C^2 with
  Hess e >= 0 on dom e, STRICT (lambda_min > 0) on the hull's
  compact neighborhood in-chart; (H-G8-2) slice-connected
  SINGLE-CHART domain (every S-slice an interval; one chart);
  (H-G8-3) hull IN-CHART (the comparison hull's states lie in dom e,
  its (v,S)-image contained in-chart); plus the R9-A member
  (H-G8-4) fiber regularity — state it, then NOTE where the strong
  set already implies it (a convex dom e has closed-interval
  S-fibers with endpoints continuous in v on the open v-range;
  handle hull-boundary v-values explicitly) and keep the judge's
  fallback branch for the remainder.
- DERIVE AT PEN GRADE that this strong set delivers the granted
  items (root-map continuity on the compact hull; image compactness;
  the lambda_min > 0 assembly chain; item (i) E(V|U) >= c|DeltaW|^2).
  If ANY step of the derivation does not close from the strong set,
  do NOT weaken the claim — impose the missing conclusion directly
  as a member (the fallback), and say so.
- DECLARE NON-OPTIMIZATION explicitly, in the grant head and §13:
  the condition is SUFFICIENT; minimality/necessity NOT claimed; the
  row is gap accounting for the abstract-EOS route (which stays OPEN
  at route level); weaker sufficient sets are possible and NOT
  sought (cite the closure verdict's orchestrator note).
- The restatement must kill the over-generality attack surface: no
  sentence may claim that weaker printed members deliver the
  conclusions; every "under which" scopes to the full strong set.

(C) RES-DOC1-2 (carried amendments, same pass):
- R9-B: falsifier-consequent member re-sort at BOTH sites (l.2403-2408
  and §13 echo l.4274-4283) with the judge's adopted text: the
  root-quantifier member is PIN-DELIVERED in the whole-domain branch;
  "domain/range GEOMETRY no reading of the pins delivers" scopes
  MODULO totality + slice-connectedness + fiber regularity ONLY; the
  uniqueness consequence stays with the interval member.
- R9-C: choose ONE route and apply consistently across ALL printed
  variants of the condition epithet (sweep them ALL in one measured
  pass — the six carriers; the ECR1-3 five-of-six lesson): either
  extend the grant head to "... + CHART-COMPLETE + TOPOLOGY-COMPLETE
  condition of record", or redefine CHART-COMPLETE as the full bundle
  and harmonize every variant. NOTE: with the (B) restatement the
  taxonomy may simplify (the strong set replaces the staged
  epithets) — if you retire the staged names, retire them at EVERY
  carrier in the same pass, keep the stage names in the §13 history
  entries (history is append-only), and add the ECR1-3 annotation:
  one sentence on the §13 ECR1-3 entry's "not reproduced" meta-claim
  naming the grant head as the carrier the rev-9 sweep missed.

(D) §13 REVISION 10 block (append; history append-only): per-finding
disposition (R9-A CONSUMED with mechanism, R9-B/R9-C CONSUMED), the
strategy declaration (sufficient-but-unoptimized, ratified), edit-site
list with line anchors, honest residues (anything you could not close,
named), falsifier list for the NEW grant (must include the
moving-slice witness class: an EOS with every per-slice member holding
and a fiber-family jump — under the strong set this witness must be
OUT-OF-CLASS, show where the hypothesis excludes it, e.g. W-A's
disconnected two-strip domain violates H-G8-1 convex dom), label
summary refresh, R-4 instance count ELEVEN (adopt judge §3(c)).

## HARD CONSTRAINTS
- LEG-3 BODY: ZERO EDITS. The leg-3 thread is DRY of record and held
  by the per-doc rule only. grep-verify your edit sites against the
  leg-3 body span before and after; declare the grep in §13.
- Touch ONLY mandate-named sites + the (B) restatement's carriers.
  Every OTHER byte of the file is out of scope.
- Verify every verdict quote VERBATIM against the on-disk file BEFORE
  editing (verdict line numbers are rev-9-calibrated; re-locate by
  content if drifted; declare any drift found).
- No edits to any other file. No label motion: [T-T0P] main stays
  SCHEMA; route-level OPEN verdict on the abstract-EOS route stands;
  gamma(T)-closure conclusions stand (all three findings discharge
  automatically at the closure — keep that sentence true).
- R-4 BINDING RULE: any phrase carried from a prior spec/verdict is
  recalibrated to the rev-10 condition before printing; audit your
  own output for transplant misfits (the CR-2/R4L1-1 species).
- OVERCERT GUARD (DOC-2 lesson): no meta-claim ("pattern not
  reproduced", "all carriers extended") without a measured full sweep
  cited in the same block.
- Env pinned: no package installs; GENO/ read-only; no git commands.

## RETURN (compact; the file is the deliverable)
{edit_sites: N, carriers_swept: N (measured grep), leg3_zero_edit:
grep-proof one-liner, fallback_members_imposed: [list or none],
residues_declared: [list or none], r4_instances: 11}
