# ESCALATION REFUTATION — DOC-3 r3.5 repair text, CONFIRMING ROUND, lens l0

Lens: hyperbolic-systems / functional-analytic rigor. Date: 2026-08-19.
Target: `validation/sfoundations_raws_2026-08-13/phaseD_L4_implies_R1.md`,
ATTACK SCOPE STRICTLY = the REVISION r3.5 delta (until-dry ROUND 4 consumption,
2026-08-19): the pair-relative slogan rewrite (ESC3-4 + the slogan half of
ESC4-1), the certification-relative third-bullet rewrite (ESC3-5 + ESC4-1,
combined), the Status-(a) premise time cylinder (ESC3-6), the Status-(a) H^1
discharge extension to Gamma_in with the ESC4-2 HONESTY CLAUSE (Gamma_in
uniform noncharacteristicity generically VIOLATED in the motivating class —
the Status-(a) H^1 leg there carries the residual), the (r-i) debt-free-on-ANY-face
annotation, the NG-3 register-mirror extension, and the r3.5 revision-log
block + disposition table itself. Machine sweep of scope (measured THIS
window: `grep -n "r3\.5" phaseD_L4_implies_R1.md` → 11 anchors): lines 22
(header), 242/248/271/274 (log block), 921 (slogan), 975 (third bullet),
1023 (premise), 1051 (H^1 discharge), 1067 ((r-i) annotation), 2064 (NG-3
row). NO r3.5 anchor inside the two-face proof body (Claim, (cl-C)/(cl-F),
(D-coll), (H-UP-fam), eps_1', STEPs 1-3, UNION — lines ~735-912, byte-level
r3.2/r3.3 text) — the log's "NO step of the two-face proof moved this round"
is TRUE of the diffs. Remark 1.5.5 carries no r3.5 marker (A-2 standing text,
"NO EDIT NEEDED" verified).

Adjudication context consumed FIRST, per brief: `r2pass/VERDICT_escalation.md`
§2 DOC-3 (round trajectory verified through r3.5; NOT-DRY-AT-CAP; the five
r3.5-consumed findings live in honesty commentary / Status-(a) premise /
register mirror), §3 LEG 14 (label SCHEMA per J-r2p-1 KEPT; ceiling
"THEOREM modulo (H-UP)" not grantable at cap; the ESC4-2 Gamma_in residual
judge-verified genuine), §6 R-3 (one two-lens round owed on the r3.5 delta;
NG-3 (F2) owns the variable-type trace theory on Gamma_mid AND Gamma_in's
grazing loci; W^{1,1} Gauss-Green = named debt-free alternative, not yet
verified) and §6 R-4 (the BINDING spec-seeding rule — §0 below). Prior
rounds consumed IN FULL for dedup: `esc_doc3_r1_l0.md` (ESC-L0-1..7),
`esc_doc3_r1_l1.md` (F-1..6), `esc_doc3_r2_l0.md` (ESC2-1..5),
`esc_doc3_r2_l1.md` (ESC2-6..9), `esc_doc3_r3_l0.md` (ESC3-1..3),
`esc_doc3_r3_l1.md` (ESC3-4..6), `esc_doc3_r4_l0.md` (ESC4-1..2).

DEDUP DECLARATION: no dispositioned objection is re-raised at its own
content; ESC3-4..6 and ESC4-1..2 are engaged ONLY through discharge
verification (§1) — all five r3.5 dispositions are ADEQUATE as dispositions,
none is attacked as failed. Every objection below targets r3.5-NEW text with
zero prior adversarial coverage: ESC5-1 attacks the register/ownership reach
of the r3.5 NG-3 extension (a state that could not exist before the r3.5
body edit); ESC5-2 attacks an internal inconsistency BETWEEN two r3.5-new
sentences (the slogan's escape clause vs the third bullet's own conditional);
ESC5-3 attacks a clause transplanted from ESC4-1's text into the r3.5 bullet
(an R-4 instance — attacking it does not fail ESC4-1's disposition, whose
counterexample role survives on a nonempty subfamily). ID namespace
continues per-round: **ESC5-1..ESC5-3**.

==============================================================================
## §0 BINDING R-4 RULE CHECK (spec-seeding pattern; VERDICT_escalation §6 R-4)

Rule verified against every r3.5 repair clause: "every repair clause is
restated against the row's own hypotheses, never transplanted verbatim from
a refutation file". Per-repair audit:

- **ESC4-2 clause (line 1051): PASS — restated, and STRONGER than the
  refuter's spec.** ESC4-1's file offered "(r-i') ... Gamma_in
  noncharacteristic outside the loci an explicit construction covers"; the
  document instead pinned the exact same uniform form it already used for
  Gamma_mid ("|u-bar.n| and |u-bar.n -+ c-bar| bounded below on Gamma_in"),
  re-derived the boundary enumeration against (H-UP-fam)'s own domain
  ("Omega_int^s, whose boundary is Gamma_in union walls union Gamma^coll_s"),
  and added the HONESTY CLAUSE beyond the spec. Conservative under-claim
  direction — correct.
- **ESC3-6 clause (line 1023): PASS — the three-word fix restated with an
  in-row rationale derived from the row's own (EI)/Gronwall run** (the
  del_t S volume term named; the C^0 \ W^{1,infinity} counter-instance
  recorded; the box-clause orthogonality argued from ESC3-2's own dichotomy).
- **ESC3-4 slogan clause (line 921): the core predicate ("belongs to no
  structure of the proof that crosses its face") is the refuter's own
  proposed wording, but the escape clause was RESTATED AND EXTENDED**
  (mechanism named: "it enters (H-UP-fam)'s delegated class"; kernel-jump
  subfamily added; provenance of the superseded tail declared). The
  EXTENSION, however, restates the escape clause UNCONDITIONALLY where the
  row's sibling sentence (the third bullet) is conditional — a misfit minted
  by the restatement itself: **ESC5-2**.
- **ESC3-5 + ESC4-1 combined bullet (line 975): PASS on the exclusion
  mechanism — genuinely merged and improved** (the certification-relative
  form, the FORBIDS/REQUIRE parenthetical, and — beyond both refuter fixes —
  the "if NG-9's instantiation of the class admits it" conditional on
  kernel-jump entry, which neither refutation file demanded). **FAIL on one
  clause**: the persistence gloss "the jump persists because those modes
  advect tangentially to the face" is transplanted near-verbatim from
  ESC4-1's attack text ("such a stationary jump persists on Sigma because
  those modes advect with u-bar, tangent to Sigma there") without
  restatement against the bullet's own generality (u-bar.n unconstrained,
  loci not flow-invariant in general) — the R-4 misfit pattern, SEVENTH-plus
  declared instance: **ESC5-3**.
- **NG-3 register mirror (line 2064): body PASS — residual (ii) restated in
  the row's own inventory grammar, honesty clause mirrored, alternative
  route mirrored. The TRIGGER column was NOT restated against the extended
  residual's own consumer set**: **ESC5-1**.

==============================================================================
## §1 DISCHARGE VERIFICATION (r3.5 repairs vs ESC3-4..6 / ESC4-1..2)

**ESC3-4 (face-absolute slogan tail) — DISCHARGED.** The tail now reads the
pair-relative predicate of the refuter's fix, the superseded tail is
retained as the FIFTH wording-universal defect with BOTH failing readings
recorded faithfully against the ESC3-4 text (structure-of-the-proof:
self-contradiction at Gamma_in^coll, with the bullet-3 generalization;
pair-supplies: accidental-C^1-glue tagged ESC3-4, kernel-jump tagged ESC4-1
— attribution exact), and the adopted-phrase provenance is declared ("l1's
own round-2 wording; adoption does not immunize"). Strongest attack tried: a
SEVENTH-universal reading of the new main clause ("belongs to no structure
of the proof that crosses its face" is false on the exceptional pairs, which
enter (H-UP-fam)'s class — a crossing structure per the slogan's own first
half). Fails to break AS A DISCHARGE: the escape clause is substantive text
inside the same sentence and carves exactly that subfamily, which is the
shape the refuter's own fix prescribed. The residue is the escape clause's
internal fit with the bullet — ESC5-2. CHECK (modulo ESC5-2).

**ESC3-5 (bullet universal, accidental-C^1-glue subfamily) — DISCHARGED.**
The bullet now does the excluding work certification-relatively: "the bare
COUPLING certifies no such cross-face solvability for its pairs — it
supplies only piecewise uniqueness ... with the trace-feed across its face
uncontrolled"; entry to the delegated class is "only if it happens to solve
across the face ON ITS OWN MERITS"; "so STEP 3 has nothing the COUPLING
supplies to consume". Verified at pen grade: (i) "only if" is the correct
NECESSARY direction (class entry additionally needs the members to solve
the full interior problem — Gamma_in data/BC, walls, forcing — which the
proves-too-much test's feed supplies by construction); (ii) the
FORBIDS/REQUIRE parenthetical is exact; (iii) on the refuter's own instance
(restrictions of global interior solutions declared a coupling at
{x = x_I - 2h}) the concluded uniqueness is honest and the rewritten
sentence is TRUE. The refuted universal is retained in annotation as the
SIXTH wording-universal defect with both subfamilies and IDs recorded
faithfully. CHECK.

**ESC3-6 (missing time cylinder) — DISCHARGED clean.** The premise now
reads "W^{1,infinity} on all of Omega_up x [0, T_f]" — verified against the
document's own convention at its source: (H1.1) is
"W^{1,infinity}(Omega x [0, T_f])" (line 450), (H1.1') the collar cylinder
— the r3.5 parenthetical's convention claim is factually faithful. The
rationale is correct as derivation: d/dt E(t) for
E(t) = Int <S(W-bar) U, U> produces <(del_t S) U, U>, whose uniform bound
is the time half of the hypothesis and is not deliverable from the box
clause; the recorded counter-instance W_0(x) + a(t) V(x),
a in C^0 \ W^{1,infinity}, box-valued, kills C_0 while satisfying the r3.4
premise. Attacks tried: (α) is anything MORE than time-Lipschitz needed —
no: the spatial halves (S, S A_j Lipschitz in x; div terms) were already
covered by the spatial half, and del_t S is the only new consumer; (β) does
the box clause need its own cylinder statement — no: "WITH (G3)-BOX VALUES"
now grammatically inherits the cylinder domain of the same subject W-bar;
(γ) mirror scan for a stale spatial-only copy of the premise
(`grep "W^{1,infinity} on all of Omega_up"`) — the only live occurrence is
the repaired line 1022-23; the disposition-table copies are historical
records of past rounds, allowed. CHECK.

**ESC4-1 (kernel-jump subfamily, bullet + slogan halves) — DISCHARGED.**
Both halves consumed in the combined rewrite; the kernel algebra verified
independently this round: for the linear symmetric system a piecewise-C^1
glue across a fixed spatial face is a distributional solution iff
A(n)[U] = 0 on the face (time part contributes no surface delta for a
stationary face); ker A(n) is nontrivial exactly at u-bar.n in {0, ±c-bar};
at u-bar.n = 0 the eigenvalue-0 eigenspace has multiplicity 3 = entropy +
two shear modes (register row 0.2's 1+3+1 structure) — the slip-line
subspace, exactly as printed. The doc's addition "if NG-9's instantiation
of the class admits it" is an IMPROVEMENT on the refuter's fix (kernel-jump
pairs are piecewise-C^1 with a genuine jump, hence outside both Status-(a)
grades (C^1 and H^1 — a codimension-1 jump is not H^1), so their class
membership is genuinely NG-9's call; the doc declines to assert it).
Residues: the unconditional slogan-side mirror (ESC5-2) and the transplanted
persistence gloss (ESC5-3). CHECK (modulo ESC5-2/ESC5-3).

**ESC4-2 (Gamma_in inherited inventory) — DISCHARGED via branch (r-i'), with
the honesty clause landed BEYOND the demanded repair.** Verified at pen
grade: (i) the boundary enumeration "Gamma_in union walls union Gamma^coll_s"
is complete for Omega_int^s (Gamma_I is not a face of the interior domain;
Gamma_mid ∩ Gamma_in = empty since d_0 > 0 and the collar is
Gamma_I-adjacent); (ii) the factual claim about 1.5.3's inventory is exact
against 1.5.3's own text (line 645-653: beyond plain Friedrichs it names
exactly the uniformly characteristic wall — Rauch 1985 — and the corner
curves — NG-3; SILENT on Gamma_in); (iii) uniform noncharacteristicity of
Gamma_in (the three quantities bounded below ⇔ det A(n) bounded below) is
sufficient for the Friedrichs regime there — and it is the conservative
choice vs the refuter's weaker "outside the loci an explicit construction
covers"; (iv) the honesty clause is faithfully sourced: Remark 1.5.5 (line
684) records grazing (u-bar.n = 0) as "generic on an injector rim in the
motivating RDE injection class", and a case-(a) SMOOTH base in the
motivating geometry can graze, so "generically VIOLATED" is coherent with
case (a)'s own premise (no contradiction with (H1.1)-failure of the DEVICE
class, which is case (b)); (v) load profile honest: the C^1-grade main
scoping needs no mollification and is untouched; the two-face proof consumes
nothing of this; no consumer in the document consumes the Status-(a) H^1
leg in the motivating class (consumer scan: Section 1.6 + register rows 1/1'
+ Section 5). Attacks tried on the repair text: (α) face-naming — the
discharge condition says "Gamma_mid" while the r3.5 boundary enumeration
says Gamma^coll_s (family form); fails to break: the proof consumes ONLY
s = h/2 with Gamma^coll_{h/2} = Gamma_mid of record (line 758-759), and
"for splits" distributes the condition per given split; (β) corner
completeness — Omega_int^s has wall-inflow corners Gamma_in ∩ Gamma_w;
under the operative generic reading of 1.5.3(b)/NG-3 ("the corner curves"
of the domain; NG-3's trigger says "a domain with corners", generic) these
were always inside the NG-3 modulo, and only Gamma_mid ∩ Gamma_w was a NEW
face needing explicit r3.3/r3.4 inventory — no break (the narrow
"wall-interface-only" reading would be an r1-text defect in 1.5.3(b) itself,
outside this delta per the same honest split ESC4-2 declared); (γ) the
(r-i) annotation "no characteristic-type debt on ANY face, Gamma_mid or
Gamma_in" — correct: the W^{1,1} Gauss-Green route needs no boundary
mollification, and the uniqueness run needs only the a.e. SIGN of the
boundary quadratic forms, not trace-space theory; the route stays
named-not-consumed. Register residue: ESC5-1. CHECK (modulo ESC5-1 on the
register reach).

**r3.5 revision-log block + disposition table — VERIFIED.** Counts: 5 IDs
= 2 (ESC4-1..2, 0B + 1R + 1A) + 3 (ESC3-4..6, 0B + 0R + 3A) = 0 B + 1 R +
4 A — matches both round files' summary tables exactly. Discharge-verdict
quotes faithful against `esc_doc3_r4_l0.md` §1/§4 (ESC3-1 modulo ESC4-1,
ESC3-2/ESC3-3 clean, ESC2-6/ESC2-7 discharged, ESC2-8 as raised modulo
ESC4-2, ESC2-9 clean; second consecutive zero-inadequate round; seven-instance
proves-too-much re-run) and `esc_doc3_r3_l1.md` §1/§4 (all seven r3.4
dispositions ADEQUATE; L0-7/L1-7 remain DISCHARGED; seven instances). Dedup
notes verified at sentence level: ESC4-1/ESC3-5 same bullet universal,
distinct subfamilies, one rewrite + two disposition rows ✓; ESC4-1's slogan
half credited separately at the slogan site ✓; ESC3-4 same paragraph
different sentence ✓; ESC3-6 vs ESC3-2/ESC2-8 same-sentence-different-clause
✓. A-2 "NO EDIT NEEDED — re-verified FULLY LANDED by BOTH round files"
faithful (l0 C4-9 spectra; l1 C3L1-10 two-loci-coincidence attack with the
pointwise (G3) bound). Edit-site enumeration complete against this window's
11-anchor machine sweep. "no gas-model content in any r3.5 edit" TRUE (the
kernel-jump text is structural linearized-Euler, gamma-general). The
BRIEF/ROUND DEVIATION declaration (third instance) matches the two round
files' own declared brief-vs-disk offsets. Label of record SCHEMA per
J-r2p-1 carried unchanged at every consumer site. CHECK.

==============================================================================
## §2 OBJECTIONS (r3.5 text; verbatim quote + attack + class)

------------------------------------------------------------------------------
**ESC5-1 — class AMENDMENT. The r3.5 NG-3 extension enlarges residual (ii)'s
BODY to Gamma_in's variable-type loci but leaves the row's TRIGGER column
corner-scoped for Theorem-1 consumption — so the disposition's ownership
claim ("the residual owned by NG-3 (F2)") under-delivers for exactly one
consumer family: an H^1 consumption of the 1.5.3 route on a CORNER-FREE
domain whose Gamma_in carries grazing/sonic loci fires NO trigger clause
while the debt is live; register row 1's own pointer ("corners = NG-3")
cannot reach residual (ii) either.**

Quotes (Section 7, NG-3 row, r3.5, and the r3.5 disposition table):
> "(ii) VARIABLE-TYPE trace theory on Gamma_mid AND on Gamma_in's
> grazing/sonic loci (**r3.5 extension, ESC4-2**: ...)"
with the row's trigger column (unchanged in r3.5):
> "first consumption of Theorem 1 at H^1 regularity on a domain with
> corners; any replacement of hypothesis (D'') by a transversality
> condition; any multi-D R2 consumption; any Status-(a)/H^1 consumption of
> (H-UP-fam) at Prop 1'' (r3.4)"
and (log, ESC4-2 disposition row):
> "The upstream SILENCE itself (1.5.3 / register row 1, r1 text) is outside
> this escalation's scope; its consumption site and register mirror are
> repaired, the residual owned by NG-3 (F2)."

Attack (register-discipline accounting — the exact consumer-navigation
standard ESC2-9 set and the r3.4 trigger extension satisfied for the
Gamma_mid half). Before r3.5, every NG-3 residual was reachable by its
trigger: corners ⊂ "domain with corners"; Gamma_mid variable-type ⊂ "any
Status-(a)/H^1 consumption at Prop 1''" (Gamma_mid exists only there). The
r3.5 body extension changes that: Gamma_in is a face of BOTH Omega_int^s
(Prop 1'' — covered by the fourth trigger clause) AND Omega_up itself
(Theorem 1's own H^1 extension via 1.5.3, register row 1 "H^1 extension
THEOREM* on smooth portions, corners = NG-3"). The variable-type debt on
Gamma_in applies to the second consumer verbatim (1.5.3 mollifies near
every boundary portion of Omega_up; its inventory is SILENT on Gamma_in —
the r3.5 text's own finding), and grazing loci live on SMOOTH portions of
Gamma_in, so: a consumer of Theorem 1 at H^1 on a corner-free (D'') domain
with a grazing injector rim (a) fails the first trigger clause ("with
corners"), (b) fails the other three (no (D'') replacement, no multi-D R2,
no Prop-1'' consumption), and (c) coming FROM row 1 is routed by "corners
= NG-3" to a corner inventory, not to residual (ii). The residual is
therefore owned-in-body but unreachable-by-trigger for that family — the
same failure shape ESC2-9 exhibited for the corner curves ("a consumer
discharging NG-3 against its own row cannot close the gap while these
curves/loci remain live" — here it CAN, by never being sent to the row).
The upstream silence itself (1.5.3/row-1 r1 text) stays outside this
round's scope per the declared split; what is attacked HERE is the
r3.5-of-record ownership claim plus the r3.5 row edit that establishes
ownership one clause short. Fix (one trigger clause + optionally two words
in row 1): extend the trigger's first clause to "first consumption of
Theorem 1 at H^1 regularity on a domain with corners OR with variable-type
(grazing/sonic) loci on a data face", and let row 1's pointer read
"corners + Gamma_in variable-type = NG-3". Not REPAIR: the discharge
sentence and the row BODY are honest of record (nothing over-claims a
delivered discharge — the ESC4-2/REPAIR shape is absent); the defect is
navigational completeness of the mirror, ESC2-9's class, consumed there as
AMENDMENT. **Class: AMENDMENT.**

------------------------------------------------------------------------------
**ESC5-2 — class AMENDMENT. The r3.5 slogan's escape clause asserts
UNCONDITIONAL class entry — "it enters (H-UP-fam)'s delegated class" — for
BOTH exceptional subfamilies, while the r3.5 third bullet (same window, same
block) correctly conditions kernel-jump entry on "if NG-9's instantiation of
the class admits it"; the ESC3-5 disposition row repeats the unconditional
form. Two r3.5-new sentences of the same block-family state the same fact at
different strengths — the stronger one over-claims what the document's own
NG-9 delegation withholds.**

Quote (Section 1.6, slogan escape clause, r3.5):
> "where a glued pair does happen to solve across its face on its own
> merits, by accidental C^1 glue or by a kernel jump A(n)[U] = 0 on the
> face's characteristic loci, it enters (H-UP-fam)'s delegated class and
> the concluded uniqueness is that hypothesis's honest content, not an
> over-proof"
versus (Section 1.6, third bullet, r3.5):
> "or, if NG-9's instantiation of the class admits it, a jump satisfying
> the kernel transmission condition A(n)[U] = 0 on the face's
> characteristic loci"

Attack (the document's own delegation discipline). The delegated solution
class is part of (H-UP-fam)'s formulation and is DELEGATED TO NG-9 of
record ((cl-C), r3.2 disposition; Status (a) proves the hypothesis only for
(H1.4)-grade pairs of that class, H^1 members by extension). A kernel-jump
pair is piecewise C^1 with a genuine codimension-1 jump — outside BOTH
Status-(a) grades (a jump across a surface is not H^1) — so whether it "is
in the class" is exactly what NG-9's unwritten instantiation decides; the
bullet says so ("if NG-9's instantiation of the class admits it"), the
slogan asserts it outright ("it enters"). The slogan also compresses the
C^1-glue half: solving ACROSS THE FACE is necessary-not-sufficient for
class entry (the members must solve the full interior problem — Gamma_in
data/BC, walls, same forcing; true of the proves-too-much test's feed by
construction, but the slogan states the implication bare). Consequence-side
honesty is unaffected either way (if NG-9's instantiation EXCLUDES the
pair, STEP 3 concludes nothing for it — even safer; if it admits it, the
uniqueness is the hypothesized content), so no derivational content moves —
but this is the block-family with SIX recorded wording-universal defects,
and the r3.5 window minted a strong/weak pair about the same membership
fact, the strong member being the sentence the adjudicator reads first.
Fix (one clause in the slogan, mirrored in the ESC3-5 row): "...it enters
(H-UP-fam)'s delegated class — for the kernel jump, where NG-9's
instantiation admits it — and the concluded uniqueness is that hypothesis's
honest content". Not REPAIR: no proof step consumes the slogan; the bullet
(the load-bearing taxonomy) is already correct; the conclusion is true on
both subfamilies in every printing. **Class: AMENDMENT.**

------------------------------------------------------------------------------
**ESC5-3 — class AMENDMENT (R-4 instance, declared per the binding rule).
The r3.5 bullet's persistence gloss "and the jump persists because those
modes advect tangentially to the face" is transplanted near-verbatim from
ESC4-1's attack text without restatement against the bullet's own
generality: tangential advection AT a characteristic locus confines the
transported jump to the face only where the locus is INVARIANT under the
base flow (e.g. u-bar.n ≡ 0 on the whole face, steady base); at an isolated
grazing locus of a generic base the material derivative of u-bar.n along
the flow is nonzero, the carried mode leaves the face, and the pair exits
the piecewise-C^1-across-THIS-face solution subfamily at t > 0.**

Quote (Section 1.6, third bullet, r3.5):
> "(u-bar.n in {0, ±c-bar}, nowhere constrained on interior faces; at
> u-bar.n = 0 the kernel is the entropy+shear subspace — the slip-line
> class, NG-9's territory — and the jump persists because those modes
> advect tangentially to the face)"
Source clause (esc_doc3_r4_l0.md, ESC4-1): "such a stationary jump persists
on Sigma because those modes advect with u-bar, tangent to Sigma there."

Attack (derivation level). Distributional solvability across the fixed face
Sigma requires A(n)[U] = 0 pointwise IN TIME; the entropy/shear jump is
transported by the ODE x' = u-bar. "Advect tangentially" is an instantaneous
statement AT the locus {u-bar.n = 0}: it guarantees trajectories through
the locus do not cross Sigma AT that instant, not that they remain in the
locus. If u-bar.n ≡ 0 on all of Sigma (steady base), trajectories starting
on Sigma stay on Sigma and persistence holds — the subfamily is NONEMPTY,
and nothing in the Claim/(D-coll)/the bullet forbids such a base on an
interior face, so ESC4-1's counterexample role and its disposition STAND
(this objection does not re-open ESC4-1; raised because the disposition's
justification clause was carried over unrestated, the R-4 pattern with six
prior declared instances). But the r3.5 sentence attaches the persistence
claim to the GENERAL characteristic loci of an arbitrary face of an
arbitrary in-class base — where it is unproven and generically false as a
mechanism (the jump's support would have to follow the moving locus, which
the fixed-face piecewise structure cannot). The enclosing clause is already
guarded ("if NG-9's instantiation of the class admits it"), so no
conclusion of the document consumes the gloss — the defect is the
transplanted justification's over-generality, the same
true-mechanism/over-general-statement shape as the six recorded
wording-universal defects. Fix (one parenthetical restatement): "...and on
a flow-invariant locus (e.g. a face with u-bar.n ≡ 0, steady base) the
jump persists — the slip-line configuration; persistence in general is
NG-9's territory with the rest of the transmission theory". **Class:
AMENDMENT.**

==============================================================================
## §3 CONFIRMED passages (strongest attack tried, each)

**C5-1 — the ESC4-2 discharge extension + HONESTY CLAUSE (lines 1048-1062):
CONFIRMED as derivation** (the register reach is ESC5-1). Attacks tried:
(α) boundary enumeration completeness for Omega_int^s — exact (Gamma_I not
a face; Gamma_mid ∩ Gamma_in empty by the collar geometry); (β) the
factual inventory claim about 1.5.3 — verified verbatim against 1.5.3's
text (wall/Rauch + corners/NG-3, silent on Gamma_in); (γ) sufficiency of
uniform noncharacteristicity for the Friedrichs regime on Gamma_in — holds
(det A(n) uniformly bounded below ⇔ the three printed quantities are);
(δ) coherence of "generically VIOLATED" with case (a)'s smooth-base premise
— coherent (a W^{1,infinity} base in the motivating geometry still grazes
the injector rim generically; the DEVICE-class (H1.1) failure is case (b),
a different clause); (ε) hunt for a consumer of the Status-(a) H^1 leg in
the motivating class that the honesty clause would strand — none exists in
the document (the leg is load-bearing only for the smooth-base sanity
reduction; the two-face proof consumes (H-UP-fam) as hypothesis); (ζ) the
Gamma_mid-vs-Gamma^coll_s face naming across the same sentence — mild
per-split abuse of notation covered by the of-record pin
Gamma^coll_{h/2} = Gamma_mid and the "proof consumes ONLY s = h/2" clause;
(η) wall-inflow corners Gamma_in ∩ Gamma_w — inside the NG-3 modulo under
the operative generic reading of "the corner curves" (the narrow reading
would locate the defect in 1.5.3(b)'s r1 text, outside this delta per the
declared split). The honesty clause itself is the strongest kind of repair:
it prices the motivating class OUT of the discharge rather than defending
it.

**C5-2 — the ESC3-6 time-cylinder premise (line 1022-1031): CONFIRMED.**
Attacks: convention verification at source ((H1.1) line 450 spells the
cylinder); the del_t S term is the unique new consumer (spatial halves
pre-covered); the counter-instance is genuine (box-valued, C^0-in-time,
Gronwall constant lost); mirror scan negative (no live spatial-only copy
remains; historical disposition rows exempt); "Third clause repaired at
the same premise sentence" bookkeeping matches ESC3-2/ESC2-8/ESC3-6
same-sentence-different-clause of record.

**C5-3 — the pair-relative slogan main clause + FIFTH-defect annotation
(lines 920-935): CONFIRMED (modulo ESC5-2 on the escape clause).**
Strongest attack: mint the SEVENTH universal against "belongs to no
structure of the proof that crosses its face" using the exceptional pairs
— fails as a break: the escape clause is substantive in-sentence text
carving exactly those pairs, the construction both refuters prescribed;
annotation fidelity verified (both failing readings recorded with correct
per-ID attribution; provenance of the adopted tail declared; the
bullet-3-generalization argument carried faithfully from ESC3-4's
Reading-A).

**C5-4 — the combined third-bullet rewrite (lines 962-990): CONFIRMED
(modulo ESC5-3 on the persistence gloss; the NG-9 conditional is an
improvement beyond both refuter fixes).** Attacks: the "only if" direction
(necessary, correctly not sufficient); the certification-relative exclusion
re-run on the judge's seven-instance test class including both exceptional
subfamilies — no over-proof anywhere (kernel-jump pairs: either NG-9's
instantiation excludes them — STEP 3 concludes nothing — or admits them —
the uniqueness is hypothesized content); the kernel algebra at u-bar.n = 0
(multiplicity-3 entropy+shear eigenspace, register row 0.2's 1+3+1)
verified; the SIXTH-defect annotation quotes the superseded r3.4 universal
exactly and tags both subfamilies with their IDs.

**C5-5 — the (r-i) annotation extension "no characteristic-type debt on
ANY face, Gamma_mid or Gamma_in" (line 1066-67): CONFIRMED.** Attack: does
the W^{1,1} Gauss-Green route smuggle a type condition through the
boundary-term SIGNS at grazing loci — no: the uniqueness run needs only
a.e. pointwise nonnegativity of the boundary quadratic forms ((ii')'s
measurable-field mechanism), not trace-space theory; and the route stays
NAMED-NOT-CONSUMED with branch (r-ii') explicitly not taken, so no rigor
weight rides on it.

**C5-6 — the NG-3 row BODY extension (line 2064): CONFIRMED as mirror
(the trigger is ESC5-1).** Attack: line-by-line fidelity against the
Section-1.6 prose — residual (ii)'s two faces, the BOTH-faces discharge
condition, the generically-VIOLATED honesty clause, the debt-free-on-ANY-face
alternative — all mirrored without strengthening or weakening; provenance
markers (r3.4/r3.5) match the actual edit history.

**C5-7 — the r3.5 revision-log block + disposition table (lines 242-280):
CONFIRMED.** Re-counted against both round files (5 = 0B + 1R + 4A; per-file
2 = 0+1+1 and 3 = 0+0+3); discharge-verdict and test-pass quotes faithful;
dedup notes verified at sentence level; the ESC4-1 two-half consumption
(bullet + slogan) correctly credited at two sites under one ID; A-2
NO-EDIT-NEEDED verified against both files' C-blocks and the absence of any
r3.5 anchor in §1.5; the BRIEF/ROUND deviation declaration (third instance)
matches the precedents declared inside the two round files themselves;
edit-site enumeration complete against this window's measured 11-anchor
sweep; "every changed statement keeps its hypothesis list and falsifier"
verified (the Status block remains inside Prop 1''/row-1' falsifier
coverage; the commentary carries the proves-too-much test as its
falsifier).

**C5-8 — the two-face proof core at r3.5 state: CONFIRMED (unchanged text;
no new attack found).** Fresh attack tried this round: feed STEP 1 a
kernel-jump pair glued across GAMMA_MID itself on a base with
u-bar.n_mid ≡ 0 (the flow-invariant configuration ESC5-3 legitimizes) —
fails to reach the proof: STEP 1's printed hypothesis requires a single
C^1 solution on ALL of C_h, which a jump-carrying glue is not, so Lemma 1.4
has nothing to act on and (MID-TRACE) never becomes available — bullet 2's
exclusion operates verbatim on the new subfamily too. STEPs 1-3/UNION and
the Claim's clauses are byte-stable r3.2/r3.3 text (anchor-verified).

**C5-9 — leg-17 Remark 1.5.5 / A-2 chain (r3.5-untouched): CONFIRMED.**
Spot re-verification per the standing mandate: no r3.5 marker in §1.5; the
grazing-genericity sentence the honesty clause cites is on disk verbatim at
line 684 with its F-6 gap wording intact ("ADJACENT spectral gaps equal to
c-bar ... extreme acoustic pair ... 2 c-bar"); the r3.5 edits cite it
without altering it; spectrum facts re-checked against register row 0.2's
1+3+1 multiplicity structure.

==============================================================================
## §4 SUMMARY TABLE

| ID | Passage (r3.5 text) | Class |
|---|---|---|
| ESC5-1 | NG-3 row: residual (ii) body extended to Gamma_in but TRIGGER still corner-scoped for Theorem-1 H^1 consumption; row 1's "corners = NG-3" pointer cannot reach residual (ii); disposition's "residual owned by NG-3" under-delivered for corner-free grazing-Gamma_in consumers | AMENDMENT |
| ESC5-2 | slogan escape clause "it enters (H-UP-fam)'s delegated class" unconditional where the same window's third bullet conditions kernel-jump entry on NG-9's instantiation; strong/weak pair about the same membership fact | AMENDMENT |
| ESC5-3 | bullet persistence gloss "the jump persists because those modes advect tangentially" transplanted from ESC4-1 unrestated (R-4 instance); persistence holds only on flow-invariant loci (e.g. u-bar.n ≡ 0, steady base); subfamily nonemptiness and ESC4-1's disposition survive | AMENDMENT |

Totals: **3 objections = 0 BREAKS-THE-LEG + 0 REPAIR-NEEDED + 3 AMENDMENT.**
Discharge verdicts (§1): ESC3-4 DISCHARGED (modulo ESC5-2 in its own escape
clause), ESC3-5 DISCHARGED clean, ESC3-6 DISCHARGED clean, ESC4-1 DISCHARGED
(modulo ESC5-2/ESC5-3, both in commentary clauses of the combined rewrite),
ESC4-2 DISCHARGED as raised via branch (r-i') with the honesty clause landed
beyond spec (modulo ESC5-1 on the register reach). R-4 rule check (§0): four
of five repairs restated against their rows' own hypotheses; the two
carried-over clauses are exactly ESC5-2/ESC5-3. No finding touches a proof
step; no finding is REPAIR-grade (no of-record discharge or accounting
over-claim stands — the r3.5 window's signature is honesty clauses, and they
check out); no label motion is proposed — SCHEMA per J-r2p-1 stands,
restoration = the E-4 adjudication only (VERDICT_escalation §3 LEG 14, §6
R-3). Dryness signal for this lens: the ESC4-2 REPAIR of the last
adjudicated round is CONSUMED AND VERIFIED at derivation level; the residue
class this round is register-navigation + two commentary wording clauses —
zero derivation-content defects remain in the r3.5 delta. Whether three
amendments meet the until-dry stopping criterion is the successor judge's
call, not this refutation's.

==============================================================================
## §5 SELF-FALSIFIERS (what kills each finding)

- ESC5-1 dies if a printed NG-3 trigger clause is exhibited that fires on
  an H^1 consumption of Theorem 1 via 1.5.3 on a corner-free domain with
  grazing Gamma_in loci (the four clauses are quoted above; none does), or
  if such a configuration is shown outside the document's class (nothing
  pins u-bar.n on Gamma_in — the r3.5 text's own finding — and (D'') admits
  smooth corner-free domains), or if register discipline of record is shown
  to bind only row BODIES and not triggers (ESC2-9's consumed standard
  says the opposite: the row must stop a consumer from closing the gap
  while the loci are live).
- ESC5-2 dies if the delegated class is shown of record to CONTAIN all
  piecewise-C^1 distributional solutions independent of NG-9's
  instantiation (the (cl-C)/NG-9 delegation says the class is part of
  (H-UP-fam)'s own formulation, delegated — the opposite), or if the
  slogan's "it enters" is shown scoped by an in-force convention to
  "enters where admitted" (no such convention is printed), or if the third
  bullet's "if NG-9's instantiation of the class admits it" is shown
  vacuous (it is the document's own r3.5 wording).
- ESC5-3 dies if trajectories of a steady in-class base through an
  isolated grazing locus {u-bar.n = 0} ∩ Sigma are shown to remain in the
  locus generically (they do not: d/dt(u-bar.n) along x' = u-bar is
  (u-bar . grad)(u-bar.n), nonzero for generic bases), or if the r3.5
  sentence is shown scoped to flow-invariant loci as printed (it is
  attached to the general "face's characteristic loci"), or if a
  construction is exhibited making the jump-support follow a MOVING locus
  while the pair stays piecewise-C^1 across the FIXED face (the jump lives
  on the face by definition; where the locus moves off any fixed point,
  A(n)[U] = 0 forces [U] = 0 there — the two are incompatible).

END — machine summary: {objections: 3, breaks: 0, repairs: 0, amendments: 3,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc3_confirm_l0.md}
