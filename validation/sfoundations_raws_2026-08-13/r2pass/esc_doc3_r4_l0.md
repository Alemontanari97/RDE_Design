# ESCALATION REFUTATION — DOC-3 r3.4 repair text, until-dry ROUND 4, lens l0

Lens: hyperbolic-systems / functional-analytic rigor. Date: 2026-08-19.
Target: `validation/sfoundations_raws_2026-08-13/phaseD_L4_implies_R1.md`,
ATTACK SCOPE STRICTLY = the NEWEST revision-log entry's edited passages —
the r3.4 (until-dry ROUND 3, 2026-08-19) repairs consuming ESC3-1..3
(lens l0 on the r3.3 state) and ESC2-6..9 (lens l1 on the same r3.3
state): the corrected proves-too-much slogan + its annotation (ESC2-6),
the third exclusion bullet for faces already interior to Omega_int^h
(ESC3-1), the "never an EXOGENOUS input" scoping in the "Which (H-UP)
form" block (ESC2-7), the completed Status-(a) premise ("WITH (G3)-BOX
VALUES", ESC3-2) and its H^1-leg variable-type clause + discharge
condition (ESC2-8), the corrected falsifier contaminant labeling + row-1'
cell (ESC3-3), the NG-3 register mirror (ESC2-9), and the r3.4
revision-log entry itself — plus the mandated re-verification of the
leg-17 amendment A-2 chain (Remark 1.5.5; r3.4 did not touch it, standing
text re-verified). Machine sweep of scope: `grep -n "r3\.4"` on the
target = lines 18, 204, 226, 234 (log), 733 (ESC2-7), 875 (ESC2-6), 898
(ESC3-1), 942 (ESC3-2), 952 (ESC2-8), 1007 (ESC3-3), 1912 (row 1'), 1962
(NG-3); no r3.4 anchor inside the two-face proof body; all examined
below.

**FILENAME/ROUND DEVIATION, DECLARED.** The launching brief named
"until-dry ROUND 3" and output file `esc_doc3_r3_l0.md`. On disk, that
round is already consumed: `esc_doc3_r3_l0.md` (ESC3-1..3, 2026-08-19,
mtime 05:44) exists and is cited IN FULL — with its exact objection
counts and discharge verdicts — by the target's r3.4 revision-log entry
(target mtime 05:55), the very repair text this brief mandates attacking
("the newest revision-log entries name the edited passages"; the newest
entry is r3.4, which consumes ESC3-1..3 and ESC2-6..9). Overwriting the
cited of-record round-3 carrier would falsify the audit chain (SR-6/SR-10
class violation; identical precedent twice over: the round-2 and round-3
slots each received a brief naming the prior round and deviated the same
way, declared in their own headers). This refutation is therefore the
ROUND-4 lens-l0 output, written to `esc_doc3_r4_l0.md`; nothing else in
the brief is deviated from. Objection IDs minted here continue the
per-round namespace as ESC4-*.

Specs consumed: `r2pass/VERDICT_r2pass.md` §2.1 (L0-7/L1-7
SUSTAINED-BREAKS, L1-8 SUSTAINED-AMENDMENT), §3 (A-2), §4(c) (E-4), §5
(falsifiers incl. the proves-too-much test). Full objection texts
consumed: `refute_r2batch_l0.md` L0-7, `refute_r2batch_l1.md` L1-7/L1-8;
prior escalation rounds `esc_doc3_r1_l0.md` (ESC-L0-1..7),
`esc_doc3_r1_l1.md` (F-1..F-6), `esc_doc3_r2_l0.md` (ESC2-1..5),
`esc_doc3_r3_l0.md` (ESC3-1..3), `esc_doc3_r2_l1.md` (ESC2-6..9) — the
two rounds whose r3.4 dispositions this round verifies. Statements of
Theorem 1', Lemma 1.4, Remark 1.5.3, Remark 1.5.5, (D-coll), (H-UP-fam),
the Claim, register rows 1/1', and the NG-3/NG-9 rows read against every
application the r3.4 text makes of them.

DEDUP DECLARATION: no dispositioned objection is re-raised at its own
content. L0-7/L1-7/L1-8, ESC-L0-1..7, F-1..F-6, ESC2-1..9, and ESC3-1..3
are engaged ONLY through discharge verification (§1); no disposition is
attacked as failed this round — all seven r3.4 dispositions are ADEQUATE
(second consecutive round with zero inadequate dispositions on this
lens). Both objections below target r3.4-NEW text with zero prior
adversarial coverage: ESC4-1 attacks a universal the ESC3-1 repair itself
introduced; ESC4-2 attacks the discharge claim the ESC2-8 repair itself
introduced (the inherited-inventory question it opens was raised by no
prior round — checked by search across all five prior refutations).

==============================================================================
## §1 DISCHARGE VERIFICATION (r3.4 repairs vs the objections they answer,
## chained back to the sustained originals)

**ESC3-1 (uncovered third face class) — DISCHARGED.** The face-dependent
taxonomy now carries the third bullet exactly as the finding demanded:
for a bare coupling across a face already interior to Omega_int^h (the
named instance {x = x_I - 2h}) the text states that STEPs 1-2 RUN, that
NO enlargement is needed, and that the operative exclusion is
(H-UP-fam)'s class quantification itself — verified at pen grade: (i)
the coupling face lies outside C_h, so the difference restricted to C_h
is a single C^1 solution of the homogeneous system and STEP 1's printed
hypothesis holds; (ii) the face is interior to Omega_int^{h/2}, so any
NG-9 instantiation of (H-UP-fam)'s class — pinned by the hypothesis's
own printed text to "two solutions of the same interior problem on
Omega_int^s" — must solve the PDE across it, which the generic
bare-glued pair does not (arbitrary jumps fail the distributional
equation); (iii) the closing scoping sentence correctly reassigns bullet
1's enlargement mechanism to Gamma_in^coll. The disposition row matches
the body and honestly credits the refuter's own verification of the
conclusion on the missing case. One universal in the new bullet's
contrast clause has an unhandled subfamily: ESC4-1 below — it does not
re-open ESC3-1 (the taxonomy's coverage and the conclusion both hold).
CHECK (modulo ESC4-1).

**ESC3-2 ((G3)-box conjunct) — DISCHARGED clean.** The premise now reads
"W^{1,infinity} on all of Omega_up WITH (G3)-BOX VALUES" — the exact
three-word fix — and the appended rationale is correct as derivation:
the (EI)/Gronwall run consumes s_min I <= S <= s_max I on all of
Omega_int^s, a pointwise range condition delivered by box membership and
not by Lipschitz regularity. Attacks tried: (α) bounded-domain reading
of "a Lipschitz base drifting to rho-bar -> 0 ... in the far interior" —
the counterexample survives on bounded Omega_up (inf rho-bar = 0 without
attainment already kills the uniform coercivity constant); (β) hunt for
a FURTHER consumed-but-unlisted hypothesis in case (a)'s (EI) run
(the ESC-L0-6 defect class, third pass): domain Lipschitz-ness =
(D-coll)-class split (cited, r3.2), wall structure = "slip walls" in
(H-UP-fam)'s own problem definition (round-2 disposition, re-verified),
same forcing = printed, solution grade = the (H1.4)/C^1 scoping of
record — nothing further found at C^1 grade; the H^1 grade is ESC2-8's
clause, handled below. CHECK.

**ESC3-3 (contaminant labeling) — DISCHARGED clean.** The corrected
sentence names the STANDING DOWNSTREAM PULSE (pre-existing at t_p,
outside Omega_up, unconstrained by the trigger) plus any POST-t_p growth
in the roughened UPSTREAM exterior, with the roughening's location of
record pinned ("upstream of Gamma_in^coll ... not downstream of
Gamma_I") — both mislabels of the r3.3 sentence repaired exactly as
demanded, superseded sentence retained with both defects recorded. The
arithmetic re-derived for the standing pulse: the trigger covers
closure-of-collar content at t_p, so above-bound influence enters C_h no
earlier than t_p; the terminal collar segment (length >= d_0, collar
speed bound lambda_max theorem-grade) gives arrival
>= t_p + d_0/lambda_max = t_p + 2x the probed window — unchanged, and
Gamma_I-entry paths are covered because d_0 is the distance to
Gamma_in^coll UNION Gamma_I (no circular use of Theorem 1'). Row 1''s
cell now carries the standing-pulse contaminant with "pre-existing at
t_p outside Omega_up" explicit — the arrival-time clarification the
disposition promised. CHECK.

**ESC2-6 (slogan overclaim of "certified") — DISCHARGED.** The slogan
now reads the demanded split form: crossed by a NAMED structure —
CERTIFIED at Gamma_mid (Lemma 1.4 on the (M-c)-monitored collar),
HYPOTHESIZED at Gamma_in^coll ((H-UP-fam), the NG-9 modulo, "certified
by nothing"). Verified against the reserved-word standard: the
monitored-hypothesis accounting block and the NG-9 row separate exactly
these two grades, and the new slogan no longer overclaims the NG-9 gap.
Strongest attack tried: "CERTIFIED at Gamma_mid" itself, since STEP 1
also consumes (H1.4) (solution C^1 on C_h), which no monitor certifies —
fails to bite: the parenthetical pins the certified object (Lemma 1.4,
theorem-grade, base hypotheses = (M-c)'s declared domain), and (H1.4) is
the uniqueness statement's own solution-class quantifier, not a
device-certifiable condition — the ESC2-6 standard distinguishes
proved-theorem structures from NG-hypothesized ones, and Lemma 1.4 is on
the right side of that line. The annotation retains the superseded
slogan with the reserved-word rationale. CHECK.

**ESC2-7 ("never an input") — DISCHARGED.** The block now reads "never
an EXOGENOUS input" with the downstream consumption sites named
explicitly (STEP 2 = Theorem 1' hypothesis (ii) supplied by (MID-TRACE);
STEP 3 = (H-UP-fam)'s same-trace clause), the derived/exogenous split
stated as the distinguishing content vs (r-b)'s lens geometry, and the
"fourth wording-universal defect" recorded of record — beyond the
demanded fix. Verified against the proof's data flow (STEPs 2-3 do
consume the Gamma_mid trace, only as STEP 1's established result) and
against the Claim's data set (initial data + Gamma_in data/BC + forcing;
the Gamma_mid trace is absent). No contradiction remains with the r3.3
annotation's "take the Gamma_mid trace as DATA". CHECK.

**ESC2-8 (H^1 leg variable-type gap) — DISCHARGED as raised, via the
refuter's own branch (r-ii); one inherited-inventory defect in the NEW
discharge clause: ESC4-2 below.** The clause now reads "THEOREM* modulo
NG-3 ... AND modulo the VARIABLE-TYPE trace theory on Gamma_mid", with
the mechanism stated correctly (1.5.3's two regimes — Friedrichs
noncharacteristic, Rauch 1985 uniformly characteristic of constant
multiplicity — cover no regime containing Gamma_mid's transition loci;
nothing constrains u-bar.n_mid), the discharge condition exactly as
(r-ii) demanded (|u-bar.n_mid| and |u-bar.n_mid -+ c-bar| bounded
below), the alternative route (r-i) NAMED-NOT-CONSUMED, and both
residuals mirrored in NG-3. Attacks tried on the repair itself: (α) is
uniform noncharacteristicity the right condition — yes (det A(n) bounded
away from 0 iff those three quantities are); it is even conservative
(a split with u-bar.n_mid == 0 identically would fall under Rauch's
regime, and the clause declines to claim it — under-claim, not a
defect); (β) the (r-i) description's mathematics — <S A_j U, U> is
W^{1,1} slice-wise for H^1 members with W^{1,infinity} coefficients
(product rule in L^1), the W^{1,1} -> L^1(del) trace and Gauss-Green
hold on Lipschitz domains — correct as stated; its retained
"wall-specific Rauch citation" is mildly over-cautious for a route with
no boundary mollification, but the route is declared not verified and
not consumed, so no rigor weight rides on it — noted, not a finding.
The NEW discharge sentence inherits an uninventoried condition on the
OTHER data face of Omega_int^s: ESC4-2. CHECK (modulo ESC4-2).

**ESC2-9 (NG-3 register mirror) — DISCHARGED clean.** The NG-3 row now
carries the r3.4 additions block: (i) the corner curves Gamma_mid
intersect Gamma_w with their Prop-1''/Status-(a) consumption named, (ii)
the variable-type trace residual with the discharge condition and the
named-not-consumed alternative, and the trigger clause extended by "any
Status-(a)/H^1 consumption of (H-UP-fam) at Prop 1'' (r3.4)" — a
consumer discharging NG-3 against its own row can no longer close the
gap while the curves/loci are live, exactly the demanded repair.
Strongest attack tried: the row's NAME ("Wall-interface corner
compatibility...") now under-describes the enlarged content — declined
as a finding: the row body and trigger carry the full inventory, the
name is a label, and the register discipline ESC2-9 invoked is about
where inventories LIVE, which is now satisfied. CHECK.

**Chain to the sustained originals — re-verified this round.** L0-7(a)
(quantifier order): eps_1 = d_0/(2 lambda_max) fixed by (D-coll) + the
collar base, eps_1' = min(eps_1, T_f - t*) > 0, R-window nonempty at
every probe time — still DISCHARGED. L0-7(b)/L1-7(ii) (trace on the
source face): the only concluded trace is on Gamma_mid at distance
>= d_0 from both uncontrolled-data faces — still DISCHARGED. The
proves-too-much test (VERDICT §5) re-run against the THREE-bullet r3.4
taxonomy on seven instances: Gamma_in^coll coupling (enlargement),
Gamma_mid coupling (STEP 1's printed C^1-on-C_h hypothesis), deep
interior face (class quantification, the new bullet), a face in the OPEN
OUTER HALF COLLAR at {x = x_I - 3h/4} (NEW instance this round: interior
to C_h, so bullet 2's printed hypothesis excludes it — and the
enlargement ALSO internalizes it, see C4-1(γ) for why the taxonomy
survives), a transversal face partly inside C_h (bullet 2 on the C_h
portion), a coupling whose glue is a genuine distributional solution
(kernel-jump subfamily: no over-proof — see ESC4-1 — the concluded
uniqueness is (H-UP-fam)'s honest slip-line content), and a coupling
across Gamma_I (the theorem's own claim, not a transmission coupling).
No instance yields an over-proof. L1-8/A-2 (leg 17): r3.4 did not touch
Remark 1.5.5 (no r3.4 anchor in §1.5); the standing text re-verified by
direct spectrum computation (u.n = 0: {-c, 0(x3), +c}; u.n = -c:
{-2c, -c(x3), 0}; adjacent distinct gaps = c-bar, all pairwise
separations of distinct eigenvalues >= c-bar, extreme pair 2c-bar;
multiplicities (1,3,1) constant; A(n) depends on u.n only through the
(u.n)I shift so Lemma 0.2's eigenvectors and projectors are regular
through both loci; the flipping object is the mode CLASS at a regular
eigenvalue's sign crossing) — A-2 remains fully landed. CHECK.

**r3.4 revision-log entry — verified.** The quoted counts match both
round files exactly (ESC3-1..3 = 0B + 0R + 3A incl. the §1 discharge
verdicts; ESC2-6..9 = 0B + 1R + 3A incl. the partially-adequate ESC2-5
reading); "7 objection IDs, no dedup collisions" is true (same paragraph
=/= same sentence for ESC3-1/ESC2-6; same sentence =/= same clause for
ESC3-2/ESC2-8); "every finding FIXED" matches the seven dispositions;
"NO step of the two-face proof moved this round" is TRUE by the machine
anchor sweep (no r3.4 marker inside STEPs 1-3/UNION/Claim/(H-UP-fam)/
(D-coll)); the edit-site enumeration is complete against the sweep (the
row-1' cell edit rides the ESC3-3 disposition, which names Section 6
explicitly). Label of record SCHEMA per J-r2p-1 carried unchanged at
every consumer site re-checked (Section 1.6 rigor block, register row
1', NG-9, the log). CHECK.

**Residue:** the two-face proof (STEPs 1-3 + UNION with
(cl-C)/(cl-F)/eps_1') again survives this lens's strongest attacks — no
proof step moved (byte-level r3.2/r3.3 text, anchor-verified) and no new
attack on the unchanged steps succeeded (§3). The two surviving defects
live in the honesty paragraph's contrast universal and the Status-(a)
H^1 discharge clause's inherited inventory — neither touches a proof
step; the SCHEMA-per-J-r2p-1 label discipline is intact.

==============================================================================
## §2 OBJECTIONS (r3.4 text; verbatim quote + attack + class)

------------------------------------------------------------------------------
**ESC4-1 — class AMENDMENT. The third bullet's contrast universal "while
the bare-coupled pair does not [solve the PDE across the face]" — and
the slogan's "a bare coupling's face is crossed by nothing" — fail on
the KERNEL-JUMP subfamily: on characteristic portions of the coupling
face (u-bar.n in {0, ±c-bar}, unconstrained anywhere), glued pairs with
jumps in ker A(n) ARE piecewise-C^1 distributional solutions across the
face.**

Quotes (Section 1.6, proves-too-much paragraph, r3.4):
> "the operative exclusion is (H-UP-fam)'s class quantification ITSELF:
> the states it quantifies over solve the interior problem on all of
> Omega_int^{h/2}, hence the PDE across EVERY face interior to that
> domain, while the bare-coupled pair does not — the coupling supplies
> only piecewise uniqueness on the two pieces with the trace-feed across
> its face uncontrolled"
and (same paragraph's slogan, r3.4):
> "and a bare coupling's face is crossed by nothing."

Attack (derivation level). For a linear symmetric hyperbolic system, a
piecewise-C^1 function with a jump across a fixed spatial surface Sigma
is a distributional solution iff A(n)[U] = 0 on Sigma. ker A(n) is
nontrivial exactly where u-bar.n in {0, ±c-bar} — at u-bar.n = 0 it is
the 3-dimensional entropy+shear subspace (p' = 0, u'.n = 0), i.e. the
slip-line modes, and such a stationary jump persists on Sigma because
those modes advect with u-bar, tangent to Sigma there. Nothing in
(D-coll), the Claim, or the bullet constrains u-bar.n on the named face
{x = x_I - 2h} (the same unconstrained-normal-Mach observation ESC2-8
made for Gamma_mid). A bare coupling — quantified by the judge's §5 test
over ANY coupling "with no transmission condition" — therefore admits
glued pairs that DO solve the PDE across the face: the printed universal
"the bare-coupled pair does not" is false on that subfamily, and the
slogan's "crossed by nothing" is false with it (the face is crossed by a
genuine weak solution). What survives: for such a pair the glue
satisfies A(n)[U] = 0, which IS a transmission condition (the natural
one for the singular part), so the pair is a slip-line-class state —
exactly the class NG-9 owns — and the "uniqueness" the proof would
conclude for it is (H-UP-fam)'s honest hypothesized content about
slip-line transmission, not an over-proof; the paragraph's CONCLUSION
(the derivation does not prove too much) holds on the subfamily. So the
defect is the fifth wording-universal minted at this paragraph-family
(the document itself counts four at r3.4), not a mechanism failure. Fix
(one scoping clause, rides the existing bullet): "...while the
bare-coupled pair does not (a glue can solve across the face only by
satisfying the kernel transmission condition A(n)[U] = 0 on its
characteristic loci — the slip-line class, NG-9's territory, where the
concluded uniqueness is (H-UP-fam)'s hypothesized content, not an
over-proof)". Not REPAIR: no proof step consumes the paragraph, the
taxonomy's mechanisms are correct on the generic (non-solution) bare
pair, and the conclusion is true on the exceptional subfamily through
exactly the clause above — verified this round. AMENDMENT.

------------------------------------------------------------------------------
**ESC4-2 — class REPAIR-NEEDED. The Status-(a) H^1 discharge clause
"DISCHARGED as cited only for splits with Gamma_mid uniformly
noncharacteristic" silently inherits 1.5.3's UNINVENTORIED variable-type
loci on Gamma_in — a data face of Omega_int^s on which grazing points
are GENERIC per the document's own Remark 1.5.5, the very sentence the
r3.4 repair quotes as its analogy — so the completed rigor accounting is
still incomplete by one named condition, one face over.**

Quote (Section 1.6, Status of (H-UP-fam), case (a), r3.4):
> "The H^1 leg is therefore DISCHARGED as cited only for splits with
> Gamma_mid uniformly noncharacteristic (|u-bar.n_mid| and
> |u-bar.n_mid -+ c-bar| bounded below); otherwise it carries this named
> residual, mirrored in NG-3."

Attack (hypothesis accounting under the document's own 1.5.3 framework —
the identical defect shape the judge sustained as REPAIR-NEEDED at
ESC2-8, per the L1-2 precedent "the accounting text must change: repair,
not wording"). The H^1 (EI) run of the 1.5.3-cited route must mollify
near EVERY smooth boundary portion of Omega_int^s, whose boundary is
Gamma_in ∪ walls ∪ Gamma_mid. The r3.4 clause conditions on Gamma_mid's
type and consumes 1.5.3 for the rest — but 1.5.3's own inventory names
exactly two features beyond plain Friedrichs (the uniformly
characteristic wall, Rauch 1985; the corner curves, NG-3) and is SILENT
on Gamma_in's type, while nothing in (H1.1)-(H1.4), (D-coll), or the
interior-problem definition constrains u-bar.n on Gamma_in, and Remark
1.5.5 states of record that grazing points (u-bar.n = 0) are "generic on
an injector rim in the motivating RDE injection class". On those loci
Gamma_in is characteristic but neither uniformly nor of locally constant
multiplicity — outside BOTH named regimes, exactly the ESC2-8 mechanism
on the other data face. Friedrichs mollification genuinely needs
noncharacteristicity there (A(n) invertible is what recovers the normal
derivative from the equation), so the debt is real, not notational. The
r3.4 sentence's "DISCHARGED as cited" therefore asserts, for
uniformly-noncharacteristic-Gamma_mid splits in the motivating class, a
discharge the cited machinery does not deliver on Gamma_in — and the
NG-3 mirror's new residual (ii) is scoped to Gamma_mid only, so a
consumer discharging NG-3 against its row would close the gap while the
Gamma_in loci remain live (the exact consumer failure ESC2-9 repaired
for the corner curves). Honest split of the burden, stated: the SILENCE
originates in 1.5.3/register-row-1 (r1 text, outside this round's scope,
never adversarially covered — checked across all five prior refutation
files); what is attackable HERE is the r3.4-NEW discharge sentence,
which is the first text to consume 1.5.3 on a domain while quoting
1.5.5's generic-grazing sentence and still leaving that face's condition
unnamed. Repair (either branch, one clause + one row edit): (r-i') pin
the discharge condition to BOTH data faces ("...only for splits with
Gamma_mid uniformly noncharacteristic AND Gamma_in noncharacteristic
outside the loci an explicit construction covers") and extend NG-3's
residual (ii) to "variable-type trace theory on Gamma_mid AND on
Gamma_in's grazing loci (generic per 1.5.5)"; or (r-ii') route the H^1
leg through the named-not-consumed W^{1,1} Gauss-Green path (which has
no mollification debt on ANY face) and verify it, retiring the 1.5.3
citation for this run. Load profile identical to ESC2-8: load-bearing
only for the smooth-base sanity reduction; case (b)/NG-9 untouched; the
two-face proof consumes nothing of this; no label motion beyond the
Status-(a) H^1 clause itself. But a discharge claim of record
over-claims until one branch lands. REPAIR-NEEDED.

==============================================================================
## §3 CONFIRMED passages (strongest attack tried, each)

**C4-1 — the third exclusion bullet (ESC3-1 repair), mechanism and
coverage: CONFIRMED** modulo ESC4-1 (a contrast clause of the same
bullet). Attacks tried: (α) "STEPs 1-2 RUN" — verified: the coupling
face lies outside C_h, so the difference is a single C^1 solution on
C_h, and Theorem 1' on C_{h/2} is untouched by the coupling; (β) can the
glued pair sneak into (H-UP-fam)'s NG-9-delegated class — no legitimate
instantiation admits it: the hypothesis's own printed text quantifies
over "solutions of the same interior problem on Omega_int^s", and the
generic bare glue is not a distributional solution across its face (the
exceptional kernel-jump subfamily is ESC4-1, resolved without
over-proof); (γ) the closing sentence "scoped to the one face the
enlargement internalizes (Gamma_in^coll)" attacked via faces in the OPEN
outer half collar (e.g. {x = x_I - 3h/4}), which the enlargement also
absorbs — fails to break: such faces are interior to C_h, so bullet 2's
printed STEP-1 hypothesis (a single C^1 solution on ALL of C_h) already
excludes them as printed, and "internalizes" reads correctly as
boundary-to-interior promotion, which applies to Gamma_in^coll alone;
the taxonomy's coverage is exhaustive (every coupling face is
Gamma_in^coll, interior to C_h, interior to Omega_int^h, transversal
across the regions — mechanisms compose — or downstream of/across
Gamma_I, which is the theorem's own claim).

**C4-2 — Status-(a) premise with the (G3)-box conjunct (ESC3-2 repair):
CONFIRMED.** Attacks tried: the bounded-domain counterexample reading
(survives — inf-not-attained still kills uniform coercivity); the
box-to-symmetrizer-bounds inference (S smooth on the compact (G3) box →
s_min, s_max uniform; correct); a third pass of the
consumed-but-unlisted-hypothesis hunt at C^1 grade (nothing found:
domain, walls, forcing, grade all pinned by printed clauses of record).

**C4-3 — falsifier contaminant sentence + window arithmetic + row 1'
cell (ESC3-3 repair): CONFIRMED.** Attacks tried: (α) the trigger/pulse
geometry (pulse injected downstream of Gamma_I = outside Omega_up;
roughening of record = interior layer in the upstream exterior —
verified against the falsifier's own construction sentence); (β)
Gamma_I-entry shortcut for the standing pulse — covered, d_0 is the
distance to the UNION of both faces and Theorem 1' is not assumed; (γ)
pre-t_p collar content — excluded by the pinned trigger; post-t_p
intra-collar growth — dedup, absorbed per the F-5/round-2 record; (δ)
the row cell's condensed "arrival >= t_p + d_0/lambda_max = 2x window"
read literally as an equation is elliptical (the DELAY, not the arrival,
equals 2x the window) — declined as a finding: the cell mirrors a body
derivation that is exact and cited in the same cell, and the round-3
record already accepted condensed mirrors at this caliber.

**C4-4 — corrected proves-too-much slogan + r3.4 annotation (ESC2-6
repair): CONFIRMED** modulo ESC4-1's "crossed by nothing" tail (same
subfamily, same fix). Strongest attack: "CERTIFIED at Gamma_mid" vs the
unmonitored (H1.4) — fails (§1: the certified object is the theorem
Lemma 1.4 with (M-c)-monitored base hypotheses; (H1.4) is the
statement's own quantifier). Annotation fidelity checked against
ESC2-6's text: the reserved-word rationale and the superseded slogan are
carried faithfully; "The bullets' mechanisms were and are correct; only
the slogan moved" is true within the ESC2-6 repair (the third bullet is
a separate r3.4 edit under ESC3-1, correctly attributed).

**C4-5 — "never an EXOGENOUS input" block (ESC2-7 repair): CONFIRMED.**
Attacks tried: consistency with the Claim's data set (Gamma_mid trace
absent — verified); consistency with bullet 2's "(MID-TRACE) never
becomes available" (the input STEP 3 lacks for a bare coupling is STEP
1's established result — no contradiction remains); the falsifier's
mid-face PROBE is a measurement, not an input; the fourth-defect
bookkeeping matches the three prior printings of record plus this one.

**C4-6 — Status-(a) H^1 clause, variable-type mechanism + discharge
condition + (r-i) description (ESC2-8 repair): CONFIRMED as to
everything except the inherited inventory (ESC4-2).** Attacks tried:
condition correctness (det A(n) bounded below iff the three printed
quantities are — exact); the uniformly-characteristic-Gamma_mid split
(Rauch-coverable but unclaimed — conservative, not defective); the
(r-i) mathematics (W^{1,1} products, trace, Gauss-Green on Lipschitz
domains — correct; "wall-specific Rauch citation" over-cautious but
named-not-consumed carries no rigor weight); corner-clause attribution
(Gamma_mid ∩ Gamma_w introduced r3.3, mirrored r3.4 — matches the
revision trail).

**C4-7 — NG-3 register row r3.4 additions + trigger extension (ESC2-9
repair): CONFIRMED.** Attacks tried: mirror completeness against the
Section-1.6 prose (both residuals present, consumption named,
alternative route named); trigger clause now fires on "any
Status-(a)/H^1 consumption of (H-UP-fam) at Prop 1''" (the consumer
failure ESC2-9 exhibited is closed); the row NAME's under-description —
declined (label vs body). Note: ESC4-2, if consumed, lands partly in
this row (residual (ii) extended to Gamma_in's loci).

**C4-8 — r3.4 revision-log entry + disposition table: CONFIRMED.**
Re-counted against both round files (3 + 4 IDs, classes match, discharge
verdicts quoted faithfully, no silent re-mint); dedup-collision note
verified sentence-by-sentence; edit-site enumeration complete against
the 15-anchor machine sweep; "NO step of the two-face proof moved" true;
"gamma(T) status of every touched statement unchanged" true (no
gas-model content in any r3.4 edit).

**C4-9 — leg-17 Remark 1.5.5 (A-2 chain; r3.4-untouched, re-verified per
brief): CONFIRMED.** Strongest attack: direct spectrum recomputation at
both loci plus the projector-regularity argument via the (u.n)I-shift
structure of A(n); the retired r2 mechanism and the retired r3 "mutual
= c-bar" phrase both remain annotated of record; no consumer cites
either unannotated.

**C4-10 — the two-face proof core at r3.4 state: CONFIRMED (unchanged
text; no new attack found).** The proof body (Claim with (cl-F)/(cl-C),
(D-coll), (H-UP-fam) restricted quantifier, eps_1', STEPs 1-3, UNION) is
byte-level r3.2/r3.3 text with no r3.4 anchor, already confirmed by the
round-2/round-3 C-blocks; this round's new instances (outer-half-collar
face, kernel-jump glue) produced no new angle on the steps themselves —
both resolve in the commentary layer (ESC4-1) or the Status text
(ESC4-2), outside the proof.

==============================================================================
## §4 Summary table

| ID | Passage (r3.4 text) | Class |
|---|---|---|
| ESC4-1 | third bullet + slogan: "the bare-coupled pair does not [solve across]" / "crossed by nothing" — false on the kernel-jump (slip-line) subfamily A(n)[U] = 0 at characteristic loci of the coupling face; conclusion survives (no over-proof) | AMENDMENT |
| ESC4-2 | Status-(a) H^1 discharge clause: "DISCHARGED as cited" inherits 1.5.3's uninventoried variable-type loci on Gamma_in (grazing generic per 1.5.5) — accounting incomplete by one named condition on the second data face; NG-3 residual (ii) is Gamma_mid-scoped only | REPAIR-NEEDED |

Totals: 2 objections = 0 BREAKS-THE-LEG + 1 REPAIR-NEEDED + 1 AMENDMENT.
Discharge verdicts (§1): ESC3-1 DISCHARGED (modulo ESC4-1's new
universal), ESC3-2 DISCHARGED clean, ESC3-3 DISCHARGED clean, ESC2-6
DISCHARGED, ESC2-7 DISCHARGED, ESC2-8 DISCHARGED as raised (modulo
ESC4-2 in the same clause), ESC2-9 DISCHARGED clean; L0-7(a)/(b) =
L1-7(i)/(ii) remain DISCHARGED; the proves-too-much test passes on seven
instances; L1-8/A-2 remains fully landed. Zero inadequate dispositions
for the second consecutive round. No finding touches a step of the
two-face proof; no label motion is proposed — SCHEMA per J-r2p-1 stands,
restoration = the E-4 adjudication only. Dryness signal for this lens:
the residue has narrowed to one inherited-accounting repair (whose
origin predates the escalation) and one wording universal; whether that
meets the until-dry stopping criterion is the seed-protocol gate's call
(LG-1/E-5), which this refutation does not and cannot discharge.

==============================================================================
## §5 Self-falsifiers (what kills each finding)

- ESC4-1 dies if piecewise-C^1 gluings with jumps in ker A(n) on
  characteristic portions of a fixed spatial face are shown NOT to be
  distributional solutions of the linear symmetric system (they are:
  the jump condition is exactly A(n)[U] = 0, and stationarity holds
  because the kernel modes at u-bar.n = 0 advect tangentially), or if
  some printed hypothesis of the Claim/(D-coll)/the bullet pins
  u-bar.n away from {0, ±c-bar} on faces interior to Omega_int^h (none
  does), or if the judge's §5 test class is shown to exclude couplings
  whose glue happens to solve across the face (its printed quantifier is
  "ANY transmission coupling with NO transmission condition"; the
  subfamily is then arguably outside it — but the printed SENTENCE
  quantifies over bare-coupled pairs, not over the test class, and dies
  or survives on its own quantifier).
- ESC4-2 dies if a document clause in force at Status (a) is exhibited
  that constrains u-bar.n on Gamma_in away from {0, ±c-bar} (search
  performed: (H1.1)-(H1.4), (D-coll), (H-UP-fam), the interior-problem
  definition, and register row 1 carry none), or if 1.5.3's mollification
  is shown not to need boundary treatment at Gamma_in for the H^1 (EI)
  identity (it does: the identity is justified portion-by-portion
  regardless of the term's later value), or if Remark 1.5.5's
  generic-grazing sentence is shown scoped away from the motivating
  class of Status (a)'s consumers (it is stated FOR the motivating RDE
  injection class), or if the H^1 leg of Status (a) is re-routed of
  record through the W^{1,1} Gauss-Green path and verified (branch
  (r-ii') — then the finding reduces to demanding the route be written,
  which is its own repair).

END — machine summary: {objections: 2, breaks: 0,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc3_r4_l0.md}
