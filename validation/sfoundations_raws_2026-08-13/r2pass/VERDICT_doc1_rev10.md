# VERDICT — DOC-1 REVISION-10 CLOSURE JUDGE (two-lens confirming round)

Date: 2026-08-19 (S-FOUNDATIONS-C2 window). Closure judge of record for
the revision-10 two-lens confirming round on
`phaseD/phaseD_stop_proof.md`, mandated by the ratified strategy
(`validation/ADVISORY_SfoundationsC2_prompt_2026-08-19.md` § PRIMA
AZIONE PARALLELA: ONE two-lens confirm on the rev-10 delta, then this
judge) executing VERDICT_doc1_closure §4 RES-DOC1-1/RES-DOC1-2. Paths
relative to `validation/sfoundations_raws_2026-08-13/`.

Inputs read IN FULL: `r2pass/esc_doc1_r10confirm_l0.md` (R10L0-1..2),
`r2pass/esc_doc1_r10confirm_l1.md` (R10L1-1..4),
`r2pass/VERDICT_doc1_closure.md` (predecessor: mandate, adopted specs
R9-A/B/C, residues, verdict model), the strategy paragraph, and the
rev-10 delta of `phaseD/phaseD_stop_proof.md` at every attacked
passage, spot-checked VERBATIM on-disk THIS window (header l.31-39 +
audit line l.124-132; condition of record + members (H-G8-1) l.2187,
(H-G8-2) l.2202, (H-G8-3) l.2223, (H-G8-4) l.2242-2267 + derivation
(D1)-(D7) l.2268-2321; sufficiency attributions l.2343-2350; momentum
block l.2384-2387; assembly splice l.2391-2401; AUD-cp note
l.2449-2496; falsifier consequent l.2496-2574; grant head l.2578-2607;
honest status l.2646-2685; §9 carrier l.2860-2913; §13 annotations
l.4384-4450 / 4481-4496 / 4569-4580; §13 REVISION 10 block
l.4687-4917). Every numeric witness re-measured in MY window (grep/
sed/md5 — §3); every refuter-vs-text and refuter-vs-refuter divergence
re-derived at pen grade (§3), predecessor §3 as model.

NULL=FAILURE CHECK: both lens files exist on disk, non-empty (434 and
363 lines), well-formed, one per lens with per-lens ID prefixes
(R10L0-*, R10L1-*), each ending in a machine summary, each declaring
its scope sweep (grep-measured in its own window), dedup register, and
R-4 binding-rule audit. Independence declared at both (other lens not
read). Neither dry-counted; neither misfiled. PASS.

DRY CRITERION APPLIED (refined, of record — this brief + the ratified
strategy + VERDICT_doc1_closure preamble): DRY = ZERO sustained
BREAKS-THE-LEG + ZERO sustained REPAIR-NEEDED at BOTH lenses.
Sustained AMENDMENTS (and a fortiori WORDING) do NOT block dryness —
each is adjudicated here and its adopted repair text listed for carry,
applied at the landing window without further rounds.

==============================================================================
## §1 PER-FINDING ADJUDICATION (cross-lens merge declared)

The two lenses raised 2 + 4 findings. MERGE AT THE CLAUSE (declared,
same rule as the predecessor's §1): R10-1 = R10L0-1 ≡ R10L1-3 (the
(H-G8-4) "equivalently" clause); R10-2 = R10L0-2 ≡ R10L1-2 (the R9-C
measured-sweep sentence). R10-3 = R10L1-1 and R10-4 = R10L1-4 are
single-lens. Distinct findings adjudicated: 4.

### R10-1 (R10L0-1 ≡ R10L1-3) — (H-G8-4)'s "equivalently" gloss
### extended beyond the adopted spec (l.2242-2248; §13 R9-A entry
### meta-claim l.4725-4731): **SUSTAINED-WORDING** (both lenses
### concurring on the clause and the class).

Verified verbatim: the member prints "fiber endpoints continuous in v,
fibers closed intervals — equivalently the hull's (v, S)-image is
COMPACT and the root map W -> S(W) is continuous on the compact hull"
(l.2245-2248), while the judge's adopted spec (VERDICT_doc1_closure §1
R9-A, re-read this window) holds ONLY image compactness inside the
equivalence and delivers root-map continuity as a CONCLUSION ("under
which single-upcrossing DOES deliver ..."). Judge re-derivation: as a
bare biconditional of the member the printed form is false in the
reverse direction — image compactness + root-map continuity constrain
the fiber family only along a tube around the image, not away from it
(a dom e with wild fiber endpoints off the image satisfies the right
side and not the left); the same one-directionality already infects
the spec's own narrower gloss (spec-seeded; standing disposition, not
re-opened — L0 correctly firewalls this). ZERO derivational load at
every consumer, verified: in the strong class BOTH conclusions are
THEOREMS at (D3)-(D4) (l.2308-2312: "THEOREMS under the strong set,
members only in the fallback branch"); the fallback branch imposes
BOTH directly (l.2261-2267); the AUD-cp closure discharge verifies the
members DIRECTLY (l.2469-2476, 2484-2490), never through the
equivalence. Consequently the §13 R9-A entry's "WRITTEN as (H-G8-4)
with the judge's adopted spec text" (l.4725-4728, which quotes the
NARROWER gloss) is loose about the extension — L0's second site,
verified. No witness, no consumer, no granted item moves: WORDING.
Adopted repair: §4, carry item (2) — restoring the spec split also
makes the §13 entry's meta-claim exact, so one repair closes both
sites.

### R10-2 (R10L0-2 ≡ R10L1-2) — the R9-C measured-sweep sentence:
### counting unit misnamed; cited pattern narrower than the certified
### claim (l.4778-4794): **SUSTAINED-WORDING** (merged; L0's
### under-coverage half folded into the clause).

Judge re-measurement (MY window, §3): the block's own pattern
(`grep -n -i -E "chart-complete|topology-complete"`) yields **18 LINE
hits** at exactly the lines both lenses list (115, 118, 2165, 2167,
2887, 2890, 4186, 4288, 4561, 4567, 4574, 4575, 4648, 4649, 4679,
4720, 4779, 4780) and **20 TOKEN instances** under `-o` (the
pattern-quotation lines 4779-4780 carry 4 of them). The block prints
"18 token hits" (l.4781): the number is correct for the quoted
command; the word "token" is not — unit mislabel, both lenses
identical, arithmetic re-verified by me. Second half (L0): the quoted
pattern under-covers the staged-epithet family its own sentence
quantifies over ("RETIRED ... at EVERY current-condition carrier") —
the rev-6/7 stage names are never swept. Judge re-measurement of the
wider family: "DOMAIN-COMPLETED" hits 112, 2161, 2885, 3892, 4270;
"STRICT + hull-theta" hits 3977, 4288, 4552, 4560, 4574, 4648 — ALL
lineage recitals or §13 history/annotations; ZERO stale
current-condition carriers (matching both lenses' independent wider
sweeps). So the certified claim's SUBSTANCE is TRUE — no
ECR1-3-species carrier miss exists anywhere; the defect is that the
cited evidence is narrower than the claim (overcert-guard species at
its mildest) plus the unit word. WORDING. Adopted repair: §4, carry
item (3).

### R10-3 (R10L1-1, single-lens) — the §13 REVISION 10 block's
### EDIT-SITE LIST is an incomplete inventory (l.4796-4811):
### **SUSTAINED-AMENDMENT** (judge-re-derived; ledger species).

Single-lens finding on an AMENDMENT-class (content-adjacent ledger)
clause: per protocol the divergence is resolved by judge re-derivation,
executed on the page and by measured sweep. Both witnesses CONFIRMED:
(w1) the third ECL0-1 in-entry annotation "[FALLBACK RE-BASED in
revision 10 per R9-A: ...]" at l.4402-4406 is revision-10 text (the
bracket says so) and its anchor is ABSENT from the list, which
enumerates only "ECL0-1 delivery re-scope l.4384, ECL0-1 falsifier
addition l.4419, domain ladder l.4443" for that region and closes "NO
OTHER BYTE TOUCHED"; my whole-file H-G8 marker sweep (89 hits, §3)
maps every hit into a declared anchor range EXCEPT l.4405 — exactly
and only the L1 witness. (w2) the header range "l.31-38" excludes the
recital's final tokens on l.39 ("conclusions again UNCHANGED —
2026-08-19))." — read verbatim this window; l.39 is a mixed line,
rev-10 tail + prior text). Why AMENDMENT, not REPAIR (L1's mitigation
confirmed): both edits are exactly the MANDATED repairs
(mandate-conformal); the fallback re-base is separately DECLARED in
the same block's R9-A mechanism entry (l.4731-4733: "the revision-9
uniqueness-only fallback re-based, annotation at the ECL0-1 entry") —
the benign one-logical-edit/three-brackets reading exists, the
ECR1-3-style rescue; the species is the ledger class with
ECL0-3/ECR1-4 precedent (both AMENDMENT); no content defect, no
consumer moves, leg-3 hash unaffected (reproduced, §3). L0's silence
explained structurally, not as contradiction: L0's containment sweep
bucketed the §13 annotations as self-defined RANGES (4384-4450),
which absorb l.4402-4406 — it audited containment, not the block's
anchor list. Adopted repair: §4, carry item (1).

### R10-4 (R10L1-4, single-lens) — the NON-OPTIMIZATION secondary
### citation "VERDICT_doc1_closure §4 orchestrator note" does not
### resolve (grant head l.2590-2591; block l.4710-4711):
### **SUSTAINED-WORDING** (search-proven by this judge).

WORDING-class, confirmable single-lens per protocol; judge
verification executed anyway: VERDICT_doc1_closure re-read in full +
measured sweep — "orchestrator" appears ONLY at its l.269 (RES-DOC1-1
owner clause) and l.273 ("any re-opening is a NEW user/orchestrator
decision"); ZERO hits for unoptimized/non-optimization/minimality/
"weaker sufficient" anywhere in that file. Neither locus carries the
cited content ("weaker sufficient sets possible and NOT sought"). The
load-bearing authority is real and correctly cited FIRST: the ratified
strategy paragraph ("condizione SUFFICIENTE-NON-OTTIMIZZATA ... la
riga è contabilità di gap, non teoria portante"), sufficient on its
own. The surplus citation dangles at both carriers. WORDING. Adopted
repair: §4, carry item (4).

==============================================================================
## §2 SUFFICIENCY, MANDATE CONSUMPTION, SCOPE — VERDICTS ADOPTED

**(a) Sufficiency at pen grade: the strong set DELIVERS — ADOPTED,
with judge re-derivation of the load-bearing steps.** Both lenses
independently re-derived (D1)-(D7) from the printed members and
concur (L0 "VERIFIED COMPLETE", L1 "SOUND" at every step). This judge
re-derived: (D1) uniqueness = strict monotonicity ON AN INTERVAL slice
(the interval clause load-bearing exactly as both lenses state — the
ECL0-1 two-branch witness shows per-component monotonicity does not
give injectivity); (D3) in full — the box argument (image point
interior to N by (H-G8-3); closed box B ⊂ int N ⊂ dom e; theta ≥
theta_min > 0 on B; vertical-segment displacement ≥ theta_min·delta;
continuity of W -> (v, eps) on {rho ≥ rho_min}; the sandwich + strict
monotonicity on the WHOLE interval slice placing THE root) delivers
continuity, and the IFT on F(W,S) = e(v(W),S) − eps(W), F_S = theta >
0, with (D1) uniqueness on ALL of dom e patching the local branches,
delivers C^2 — the step that broke at revision 9 is now a THEOREM of
the printed members; (D4) compact image = continuous image of the
compact hull; (D5)-(D7) bridge congruence (rev-8 discharged CLEAN,
mechanism unchanged) + momentum block 1/(rho·theta) > 0 ((H-G8-2)
theta at image points, in dom e by (H-G8-3)) + boost affinity +
uniform lambda_min > 0 (continuous positive Hess on the compact hull)
+ Bregman double integral = item (i) at c = lambda_min/2; item (ii)
through the C^2 chart. No unwritten hypothesis found by either lens or
by me; both lenses hunted the in-class witness one rung further out
and closed every route (fiber-endpoint discontinuity killed by
N-interiority; root escape killed by interval + theta > 0 on ALL of
dom e; degenerate domains out-of-class via (H-G8-3)).

**(b) W-A and the necessity chains, verified:** W-A's three
out-of-class showings re-verified by both lenses and re-derived by me
on the predecessor's witness data (two-strip domain non-convex —
violates (H-G8-1); fibers jump [0,1] -> [10,11] at v = 1 — violates
(H-G8-4); branch-1 image accumulates at (1,1) ∉ dom e since the v=1
slice is [10,11], with S(W) = sqrt(F(v)) − 1 -> sqrt(4) − 1 = 1 as
v -> 1^- — violates (H-G8-3)); (m1)/(m2)/(m3) correctly framed at (f3)
as OUT-of-class necessity witnesses, each violating essentially the
member it prices. The AUD-cp honest accounting re-derived: the
closure's (v,S)-image is the R·ln v-sheared strip whose lower boundary
S = phi(T_min) + R ln v is CONCAVE in v, so chords exit the strip from
below — (H-G8-1)'s domain half genuinely FAILS there; its only
in-derivation loads (interval slices, fiber structure) are verified
DIRECTLY at the closure (fibers [phi(T_min)+R ln v, phi(T_max)+R ln v],
v-continuous endpoints), so the granted items discharge through the
same (D1)-(D7) chain — the sufficient-not-necessary display is correct
and does not violate (f4).

**(c) Mandate consumption: FULLY CONSUMED — ADOPTED.** Both lenses
verified carrier-by-carrier, with matching lists: R9-A at all ten
judge-named propagation sites (member text + fallback l.2242-2267;
splice l.2391-2401 with "three chart clauses" retired — zero
single-line hits, phrase survives only line-wrapped in its own
retirement sentence; consequent l.2528-2531; grant head; header;
§9; honest status; AUD-cp fiber clause per predecessor §3(b) — l0's
description adopted as ruled; §13 ECL0-1 delivery re-scope + the
previously-missing continuity falsifier ADDED l.4419-4430 +
fallback re-base l.4402-4406; domain ladder l.4443-4450);
"single-upcrossing" survives only at l.4381 (history) — my sweep
concurs. R9-B at BOTH sites with the adopted scoping; the DECLARED
RECALIBRATION (geometry bucket enlarged by (H-G8-1)'s domain half, a
member the rev-9-calibrated list could not name) is adjudicated SOUND
— pins constrain derivatives where e is defined, never where it is
defined (the row's own a0-positive patch witness), so omitting the
domain half would have made the whole-domain consequent FALSE; the
declaration is printed per the R-4 rule (l.4757-4765); the two body
enumerations and (f5) are three-way consistent (four geometry
members), verified at both lenses and spot-checked by me. R9-C by the
harmonization route (within the judge's stated alternatives): all six
current-condition carriers print the (H-G8) taxonomy (header l.125,
condition l.2180-2186, grant head l.2581-2582, honest status l.2661,
§9 l.2895-2896, block label summary l.4860 — each verified this
window), stage names survive only in lineage/history; ECR1-3
annotation present naming the grant head as the missed sixth carrier.

**(d) Zero-edit scope: CONFIRMED.** Leg-3 anchors 1087/1440, span 353
lines, MD5 re-measured by me: stripped-final-newline
6ef096f238ef76bc0ea8965cd066ef96 = the block's recorded value (raw
variant 0a186e9d252fc01bedb657e83013bc58 — the convention both lenses
identified; the landing pass should PIN the exact command, L1's
recommendation adopted as a note, not a repair). Marker sweep inside
the span: ZERO hits. Whole-file marker containment: every hit inside
declared edit sites except the adjudicated R10-3 witness. LEG-3
CARRIES ZERO REVISION-10 EDITS; no non-mandate passage moved to the
resolution these instruments give (declared caveat as at L1: a
markerless edit evades a marker sweep; coverage rests on the byte-
exact leg-3 hash + verbatim re-reads at every declared site + the
predecessor's rev-9-verified quotes, no drift found).

**(e) Label/verdict motion: NONE — ADOPTED.** [T-T0P] main statement
SCHEMA of record, unmoved; route-level abstract-EOS verdict OPEN,
unmoved (l.2677-2681); gamma-table conclusions UNCHANGED (l.2906);
quadruple-pinned grant and two-piece r2 residue stand; §13 rev-9
annotation sites preserve prior text (verified at all five). No
orphan objection: the predecessor's three findings were the only open
items against revision 9 and all three are consumed; standing
dispositions from all prior rounds checked at both lenses' dedup
registers — none reopened.

==============================================================================
## §3 DIVERGENCES RE-DERIVED + JUDGE MEASUREMENTS

(a) **L0 silent on the inventory defect (R10-3):** structural, not a
contradiction — L0 audited containment against self-defined ranges
(4384-4450) that absorb the unlisted annotation; L1 audited the
block's own anchor list. Judge re-derivation sustains L1 (§1 R10-3):
my measured sweep isolates l.4402-4405 as the ONLY marker hit outside
declared anchors, and l.39 carries the recital tail.
(b) **R-4 candidate twelfth instance (L1 flags R10L1-3; L0 declines):**
both lenses' bottom lines agree (below threshold); judge RULES: NOT an
R-4 instance — the extension misfits no context into an operative
false claim (both conclusions are separately derived in-class at
(D3)-(D4) and separately imposed in the fallback; no consumer reads
the biconditional as load-bearing — checked at every reader by L1 and
spot-checked by me). The transplant-AND-EXTEND species is hereby NAMED
for the ledger, but the count of record STAYS **ELEVEN** declared
instances; the absorbing "eight-plus" stays true. New R-4 instances
sustained this round: ZERO (concurring with both lenses' audits).
(c) **"ORDER-TWO" (L0) vs "ORDER ONE" (L1) on item (i):** nominal
only, no re-derivation conflict — both print the identical bound
E(V|U) ≥ (lambda_min/2)|DeltaW|^2 from the identical Bregman
mechanism; L1's phrase refers to the witness-failure order the repair
closes (item (i) was negative at order one on rev-9 straddle pairs),
L0's to the bound's own order. The formulas agree; the document is
untouched by this wording difference between lens files.
(d) **Leg-3 MD5 convention:** both lenses reproduce the recorded hash
under strip-final-newline extraction; judge re-measured both values
(§2(d)). No conflict, no finding.
(e) **Judge measurements of record (MY window, Git Bash, on the
4917-line / 282293-byte on-disk file):** R9-C pattern 18 line hits /
20 tokens (line list identical to both lenses); DOMAIN-COMPLETED hits
112, 2161, 2885, 3892, 4270 and "STRICT + hull-theta" hits 3977, 4288,
4552, 4560, 4574, 4648 — all lineage/history; "under which" hits 2352
(witness-regime clause inside the (m2)-family parenthetical, read in
context — not a granted-item claim), 3569/4136/4377/4500/4537/4542
(history), 4715/4846/4907 (block meta-claims) — the f4 discipline
holds on the measured set; leg-3 anchors/span/hashes as §2(d); H-G8
89 hits with containment as §2(d); "single-upcrossing" 4381 only;
"three chart clauses" 0 line-hits; closure-verdict orchestrator/non-
optimization sweep as §1 R10-4.

==============================================================================
## §4 DRY ADJUDICATION AND CONSEQUENCES

Sustained at this round: **0 BREAKS-THE-LEG + 0 REPAIR-NEEDED at both
lenses; 1 AMENDMENT (R10-3) + 3 WORDING (R10-1, R10-2, R10-4).**

Under the dry criterion of record: **DOC-1 REVISION 10 IS DRY.**
Consequences, stated explicitly per mandate:

- **LEGS 3 AND 5 CLOSE.** The per-doc hold on leg 3 (dry of record
  since the r2 batch, held open by the per-doc rule) is released by
  this round's dry verdict; leg 5's residue chain (R9-A/B/C -> rev-10
  -> this round) is exhausted with zero sustained BREAKS/REPAIR.
- **THE HELD-OUT LANDING SET UNBLOCKS** (VERDICT_confirm §6 R-5', the
  strategy's landing sentence), verbatim: **[T-T0P] main statement
  lands (SCHEMA label, gap lists SPLIT, t-periodic quantifier, domain
  cl(Omega_march), slip-free caveat, G12 decomposition, gap-graph
  edges); the G8/[C-XBVP](a') registry row lands WITH its §9 falsifier
  pointer; the [T-XWS] retro-annotation lands.**
- **NO FURTHER ROUNDS under this mandate** (the round design was ONE
  two-lens confirm + this judge). Adjudication authority for applying
  the carried repairs at the landing window = this verdict; any
  re-opening is a NEW user/orchestrator decision.
- **[T-T0P] stays SCHEMA; route-level abstract-EOS verdict stays
  OPEN;** gamma-table conclusions, quadruple-pinned grant, two-piece
  r2 residue all stand (no label motion was in prospect at either
  lens).
- **RESIDUES: NONE.** (The carried repairs below are adjudicated
  amendments/wording, not residues in the mandate's sense.)

**AMENDMENTS CARRIED INTO THE LANDING PASS (adopted repair texts —
applied at the landing window without further rounds; §13 repairs by
in-place annotation, prior text preserved, per the append-only rule):**

 (1) **R10-3 (AMENDMENT)** — annotate the §13 REVISION 10 block's
     EDIT-SITE LIST: add the anchor "ECL0-1 fallback re-base l.4402"
     to the §13-annotation enumeration and correct "header status
     paragraph l.31-38" to "l.31-39" (both edits were mandate-conformal
     and separately declared at the R9-A mechanism entry; the
     inventory instrument alone under-enumerated).
 (2) **R10-1 (WORDING)** — restore the adopted spec's split at the
     (H-G8-4) member (l.2246-2248): "— equivalently the hull's (v, S)-
     image is COMPACT; root-map continuity on the compact hull is the
     member's DELIVERED conclusion (derived at (D3) from the full
     strong set; imposed directly, together with image compactness, in
     the fallback branch)". This single repair also makes the §13
     R9-A entry's "with the judge's adopted spec text" claim exact —
     no second-site annotation owed.
 (3) **R10-2 (WORDING)** — annotate the R9-C measured-sweep sentence:
     "18 token hits" -> "18 line hits (20 token instances; the
     pattern-quotation lines carry 4)"; append: "wider staged-epithet
     family (DOMAIN-COMPLETED; STRICT + hull-theta; bare STRICT form)
     swept at both rev-10 lenses and re-measured by the rev-10 closure
     judge: zero stale current-condition carriers."
 (4) **R10-4 (WORDING)** — at the grant head (l.2590-2591) and the
     block's strategy declaration (l.4710-4711), replace the citation
     with the resolving one: "(ratified strategy of record,
     ADVISORY_SfoundationsC2_prompt_2026-08-19 § PRIMA AZIONE
     PARALLELA)"; VERDICT_doc1_closure §4 may be cited only as
     pass-ownership authority, never as a non-optimization carrier.

Note carried without repair status: the landing pass should PIN the
exact leg-3 hash command (strip-final-newline convention:
`sed -n '<a>,<b>p' FILE | head -c -1 | md5sum`) next to the recorded
value, per L1's reproducibility note.

R-4 count of record after this round: ELEVEN (unchanged, §3(b)).

==============================================================================
## §5 FALSIFIERS FOR THIS VERDICT

- A sustained BREAKS-THE-LEG or REPAIR-NEEDED in either lens file this
  verdict failed to adjudicate (none: 2 + 4 findings, machine
  summaries 0/0 at both, every finding adjudicated in §1).
- An EOS satisfying the FULL (H-G8-1)-(H-G8-4) with a discontinuous
  hull root map, non-compact hull (v,S)-image, or item (i) failing on
  a comparison segment — kills the dry grant's mathematical basis
  (refuted by (D1)-(D7), re-derived independently at both lenses and
  by this judge at §2(a); the document's (f2)/(f3) are its standing
  forms).
- A revision-10 byte range outside the block's edit-site list AND
  outside the two adjudicated R10-3 witnesses (l.4402-4406, l.39) —
  would reopen R10-3's benign AMENDMENT classing (my whole-file marker
  sweep isolates exactly those; leg-3 byte-exact hash reproduced).
- A consumer of record for which the (H-G8-4) "equivalently" gloss
  carries derivational load (would upgrade R10-1 above WORDING; none —
  (D3)-(D4) derive both conclusions, the fallback imposes both
  directly, the closure verifies members directly: checked at every
  reader).
- A passage of VERDICT_doc1_closure §4 stating non-optimization
  content (would overturn R10-4; search-proven absent: orchestrator
  mentions only at its l.269/273, zero unoptimized/minimality/
  weaker-sufficient tokens in the file).
- A stale staged-epithet current-condition carrier under ANY pattern
  (would overturn the R9-C consumption verdict and R10-2's
  substance-holds ruling; refuted by three independent measured
  sweeps — both lenses' and mine — over the block's pattern AND the
  wider family).
- For the DRY verdict itself: a reading of the dry criterion of
  record under which sustained AMENDMENT or WORDING blocks dryness
  (refuted by the criterion's own text: this round's brief, the
  ratified strategy's refined criterion, and the predecessor's
  preamble all print AMENDMENTS-do-not-block).

END — DOC-1 revision-10 closure verdict. Machine summary:
{doc1_dry: true, legs_closed: [3, 5], sustained: {breaks: 0,
repairs: 0, amendments: 1, wording: 3},
amendments_carried: [R10-3, R10-1, R10-2, R10-4],
landing_unblocked: true, residues: [],
file: validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_doc1_rev10.md}
