# ESCALATION REFUTATION — DOC-2 (phaseD_meanswirl_formalization.md) r6
# escalation text, ROUND 3, LENS 1 (gas dynamics / physics, RH algebra,
# counterexamples)

Date: 2026-08-19 (S-FOUNDATIONS-C escalation window, until-dry round 3).
TARGET: the r6 REVISION ONLY — every passage carrying an "(r6)" / "r6"
marker per the revision-log r6 block (lines 140–181) and the §6-quinquies
ROUND 3 table (2691–2738): the (w1) exemplar correction (1536–1562); the
Γ_G in-model line at BOTH anchors (hypothesis-line discharge 1666–1679;
gamma-status block 2038–2048); the JUMP-SET PIN + CLASS PIN (1729–1744);
the r6 conclusion display (containment + weakened Ṽ-regularity + structure
clauses, 1744–1768); assembly step (1) r6 scoping + INSENSITIVITY NOTE
(1877–1911); step (3) r6 grade-honesty text + REGULARITY paragraph + the
two structure-clause derivations (1918–1956); the ⟸ re-read (1957–1961);
the class/proof-state block with the r6 RETRACTION (1987–2037); falsifier
(a) re-bind (2070–2086), falsifier (d) r6 note (2099–2101), falsifier (e)
(2105–2116); register row D.18 r6 additions (2368); G-f entry r6 addition
(2427–2430); §6-quinquies ROUND 3 table + residue paragraph (2682–2738).
Leg-7 / A-1 scope: grep-verified NO r6 marker touches D.2 (nearest
markers: header block, then 1546) — the A-1 landing (D.2 parenthetical,
lines 360–369) is re-checked for regression only (Part B, C-13).
Adjudications of record consumed: `r2pass/VERDICT_r2pass.md` §2.1/§4(b)/
§4(c) (E-3 spec, J-r2p-2/3, A-1); full round-2 objection texts
`r2pass/esc_doc2_r2_l0.md` (ESC2-L0-1..4) and `r2pass/esc_doc2_r2_l1.md`
(R2L1-1..3) read in full; round-1 escalation files and the r2batch leg-6/7
objections consulted for dedup.

DEDUP DISCIPLINE: no dispositioned objection re-raised unless the
disposition itself fails — two findings below invoke exactly that clause
(IDs cited, failure derived); the other two target r6-minted text with
zero prior coverage (grep of all esc_doc2 files and both r2pass lenses
declared per finding). All quotes verified verbatim against the r6 file
(line reads this window).

==============================================================================
## §1 Discharge audit — do the r6 repairs discharge the seven round-2
##    objections (and does A-1 still stand)?

| Round-2 obj | r6 delivery | DISCHARGE VERDICT |
|---|---|---|
| R2L1-1 (BREAKS: r5 pointwise containment + printed "equivalently" false on the coincidence-boundary witness; step-(1) lemma false at crossings; "proved as written") | JUMP-SET PIN (per-pair, never normal-line) + priced H²-null CLASS PIN; containment restated as int{n_φ = 0} ∪ N_F with the witness recorded PASSING; step (1) scoped to F ∖ N_F; step (3) re-run at a.e. grade; "proved as written" RETRACTED, counter at THREE; falsifier (e) + G-f instance minted | DISCHARGED as to display, lemma, equivalence and certification retraction — every element of the named repair re-verified below (C-1..C-4, C-9). The REPLACEMENT certification sentence ("AT EXACTLY THE STRENGTH OF ITS STATED CLAUSES") rides on the structure clauses' printed grade — residue = R3L1-1 |
| ESC2-L0-1 (REPAIR: "Ṽ piecewise-C¹ MERIDIONAL" underived; C¹-Sard obstruction; certification rides) | Clause WEAKENED to Ṽ ∈ C¹ off a closed rotation-invariant meridional set, front-free-section derivation WRITTEN; the two dependent structure clauses "derived directly" per the objection's own salvage; stronger claim recorded as unconsumed downstream | PARTIALLY DISCHARGED — the weakening and the REGULARITY paragraph are correct and verified (C-5); but the salvage was transcribed HOT: the conclusion prints the structure clauses at pointwise/global grade while the adopted salvage argument (a.e.-determination) delivers a.e./up-to-N_F grade (R3L1-1 below; disposition-failure clause invoked) |
| ESC2-L0-2 (AMEND: blanket trace-continuity false at in-class crossings) | Merged into the R2L1-1 step-(1) scoping; H²-null INSENSITIVITY NOTE written | DISCHARGED (C-4) — the scoped sentence is true on F ∖ N_F and the note correctly prices what step (3) consumes. One r6-new citation defect inside the scoped sentence's justification parenthetical (R3L1-4) |
| ESC2-L0-3 (AMEND: (w1) certificates refuted by in-document record) | Certificates replaced by the operative ones (φ-dependence off fronts + H-NC + H-WR, D.19 pumping cited); smooth instance = restriction to a subdomain compactly inside the marching domain | PARTIALLY DISCHARGED — the operative-condition rewrite and the a.c.-leg payload are correct (C-12); but the transcribed smooth-instance clause again certifies predicates its citation does not deliver ("any subdomain"; smoothness from a sonic bound) — R3L1-3 (disposition-failure clause invoked; third consecutive generation of a defective (w1) exemplar spec) |
| ESC2-L0-4 (AMEND: falsifier (a) ran retracted arc H-CVX vs "the r4 closure") | Trigger re-bound to connected-adiabat H-CVX and "the n_m = 0 closure"; mechanism recorded in-statement | DISCHARGED (C-8) — both natural executor readings of the re-bound trigger now yield a CORRECT rejector; the detached-branch exhibit is classified out-of-hypothesis on either reading |
| R2L1-2 (AMEND: connectedness attributed to G_fund > 1 alone; Γ_G line unstated) | Γ_G = v(∂p/∂e)_v = R_g/c_v = γ(T) − 1 > 0 > −2 WRITTEN at both anchors; connectedness re-cited to G_fund > 1 PLUS Γ_G > 0 | DISCHARGED (C-7) — algebra independently re-derived, variable-c_v(T) safe, both anchors consistent |
| R2L1-3 (AMEND: (w1) exemplar not a class member) | Same merged edit as ESC2-L0-3 (repair option (i): restriction instance); (w2) noted as carrying the witness role | PARTIALLY DISCHARGED — same residue as ESC2-L0-3 (R3L1-3) |

A-1 (leg 7, D.2 parenthetical): UNCHANGED at r6 (grep: no r6 marker below
line 144 touches D.2; lines 360–369 carry the r4 judge wording verbatim).
STANDS — see C-13 for the cross-consistency attack tried.

Label discipline verified at all sites (class block 1994–1996, register
row 2368, G-f entry, ROUND 3 ledger 2732–2734): both D.18 iff clauses at
SCHEMA per J-r2p-2/J-r2p-3, E-3 adjudication pending, layer/gate caveat
of the r4 block carried forward verbatim (177–181, 2736–2738). No label
self-upgrade. The ROUND 3 ledger rows match the round-2 objection texts
1:1; the two declared prices (class pin; Ṽ-regularity weakening) are
real and honestly stated.

==============================================================================
## §2 FINDINGS (4: 0 BREAKS-THE-LEG, 1 REPAIR-NEEDED, 3 AMENDMENT)

------------------------------------------------------------------------------
**R3L1-1 — REPAIR-NEEDED. The r6 conclusion prints the two structure
clauses at pointwise/global grade while the adopted ESC2-L0-1 salvage
argument transcribed into step (3) delivers only a.e./up-to-N_F grade;
the a.e.→pointwise upgrade (per-piece boundary-extension continuity +
propagation across N_F-separated arc components) is nowhere written, and
the clauses carry no N_F price although the containment clause in the
SAME display got exactly that price; the replacement certification
sentence "AT EXACTLY THE STRENGTH OF ITS STATED CLAUSES" is therefore
not yet true — sustained, this is the FOURTH consecutive round in which
the conclusion block carries a certification above its written
derivation.**

Verbatim quotes. Conclusion display (1754–1762):

> "and V coincides a.e. with a meridional field Ṽ(x, r) ∈ C¹ OFF A
> CLOSED ROTATION-INVARIANT MERIDIONAL SET, with the two structure
> clauses derived in assembly step (3) WITHOUT any global Ṽ-regularity
> claim: one-sided limits on the locally φ-invariant front patches are
> φ-independent, and the ESSENTIAL jump set is a rotation-invariant
> (meridional-curve × S¹) set — locally a C¹-curve × S¹ product where it
> meets a front's int{n_φ = 0} stratum, by the step-(2) graph structure."

Step (3), clause (i) derivation (1941–1949):

> "(i) on each locally φ-invariant front patch the step-(2) local
> product structure (front = C¹ meridional curve × φ-arc) makes the
> one-sided traces rotation-covariant, and V = Ṽ∘π a.e. then forces
> them φ-INDEPENDENT (one-sided traces of a piecewise-C¹ field are
> a.e.-determined by its equivalence class along the patch);"

Certification (2010–2012):

> "is proved under H-NC + H-WR (+ connected-adiabat H-CVX on the
> n_m = 0 strata) AT EXACTLY THE STRENGTH OF ITS STATED CLAUSES"

Derivation-level attack. (a) Clause (i) is printed FLAT ("one-sided
limits on the locally φ-invariant front patches are φ-independent" —
every patch point, no exceptional set), but the printed justification is
a.e.-determination: rotation invariance of the a.e. class V = Ṽ∘π
determines one-sided traces only H²-a.e. along the patch — at a FIXED
patch point (in particular at patch ∩ N_F points: a front crossing
INSIDE a φ-invariant patch is in-scope physics — an axisymmetric Mach
stem foot, a shock–shock crossing circle) an a.e. statement forces
nothing. The pointwise upgrade exists in-class, but its proof consumes
machinery the step never invokes: (u1) per-piece boundary-extension
continuity — a piece that is C¹ with unrestricted one-sided limits at
every front point has a CONTINUOUS extension to its closed piece, so the
trace at an N_F point is the limit of nearby patch ∖ N_F trace values;
(u2) propagation across N_F-separated arc components — a closed
H²-null set CAN disconnect a patch along a circle (a crossing curve has
positive H¹), so a φ-circle may meet several components of patch ∖ N_F,
and matching the per-component meridional profiles across the cut needs
(u1) plus the fact that helical crossing fronts are jump-free up to
their own N-sets (itself an application of the containment just proved).
Neither line is in the document. (b) Clause (ii)'s HEAD is printed flat
("the ESSENTIAL jump set is a rotation-invariant (meridional-curve ×
S¹) set") while the step-(3)(ii) argument prices its own middle step
("that circle lies — UP TO the H²-null N_F geometry priced in the
display — in a front's int{n_φ = 0} stratum") and derives the local
curve structure only where the circle meets an int-stratum; for a
hypothetical essential-jump circle inside N_F the head clause has no
written support (my construction attempts are killed by (u1) — see the
self-falsifier — but (u1) is exactly the unwritten line). (c) The
containment clause in the SAME display was repaired at r6 by attaching
its price ("⊆ int{n_φ = 0} ∪ N_F — i.e. ... UP TO a front-null set");
the structure clauses got no analogous price and no upgrade proof: the
display is internally two-grade. (d) Consequence for the certification:
"AT EXACTLY THE STRENGTH OF ITS STATED CLAUSES" is a truth-claim about
clause grades; clause (i) as stated (flat) and clause (ii)'s head as
stated (global) exceed the strength of the written derivation, so the
sentence is refuted as printed — after "complete AS WRITTEN" (r4),
"proved as written" (r5, twice-defective display), this is the fourth
consecutive certification above the written proof, exactly the counter
the document itself now tracks at three.

NAMED REPAIR (transcription-grade, two options): (i) SCOPE — print the
clauses at the grade the salvage delivers, mirroring the containment:
"one-sided limits on the locally φ-invariant front patches are
φ-independent H²-a.e. on the patch and at every point of
cl(patch ∖ N_F); the essential jump set is rotation-invariant, locally
a C¹-curve × S¹ product where it meets an int{n_φ = 0} stratum — i.e.
up to the same front-null N_F geometry as the containment"; or
(ii) UPGRADE — write lines (u1)/(u2) (three sentences: closed-piece
continuity of the boundary extension from §0's everywhere one-sided
limits; the limit-of-φ-independent-values argument at N_F points; the
jump-free gluing across helical crossings) and keep the flat clauses.
Either way the certification sentence becomes true; as printed it is
not. CLASS: **REPAIR-NEEDED** (per the L0-1/L1-1 precedent — "one line,
but the line must be WRITTEN", held there to be proof-content repair —
and the ESC2-L0-1 precedent: underived clause grade + attached
completeness certification; NOT BREAKS: I could not construct an
in-class counterexample — every candidate dies on (u1), recorded below
— and the theorem's core (∂_φV = 0, containment, Ṽ ∈ C¹ off S) is
untouched).

NOVELTY/DEDUP (disposition-failure clause invoked): ESC2-L0-1 is
dispositioned FIXED with "the two dependent structure clauses derived
DIRECTLY per the objection's own salvage" — the salvage route (iii) of
ESC2-L0-1 itself claimed the clauses "follow from V = Ṽ∘π a.e. plus
a.e.-determination of one-sided traces"; that suggestion delivers the
a.e. grade and was transcribed with the flat wording kept. The failure
is in the transcribed salvage's grade, not in the weakening (which is
correct and stands) — the same transcribed-spec-runs-hot mechanism as
ESC2-L0-3 (round 2) and ESC-L0-4 (round 1), third instance of the
class. No prior ID attacks the r6 clause grades (text minted at r6).

------------------------------------------------------------------------------
**R3L1-2 — AMENDMENT. The CLASS PIN's justification parenthetical
"crossings, triple curves, coincidence boundaries and edges are locally
H¹-rectifiable" is false in the pinned class: the pin buys H²-nullity
of pairwise intersections and NOTHING about their dimension or
rectifiability — two C¹ fronts can meet, pin-compatibly, on a closed
H²-null set of Hausdorff dimension strictly between 1 and 2, which is
not locally H¹-rectifiable; the operative claim (N_F front-H²-null)
holds directly from the pin and needs no such gloss.**

Verbatim quote (1737–1743):

> "CLASS PIN (r6, priced): distinct declared fronts have pairwise
> H²-null intersections — patches of full 2-D coincidence are
> identified as one declared front there; under this pin N_F is
> front-H²-null (finitely many C¹ objects: crossings, triple curves,
> coincidence boundaries and edges are locally H¹-rectifiable). Without
> the pin only the a.e./measure-grade clauses below are asserted."

Derivation-level attack. Transversal crossings of C¹ hypersurfaces are
locally C¹ curves (implicit function theorem) — but the class carries
NO transversality hypothesis, and TANGENTIAL contact is exactly the
configuration this round's own witness family lives in (C¹ contact at a
coincidence boundary). For tangential contact the intersection of two
C¹ fronts is an essentially arbitrary closed set of the contact
surface: by the Whitney zero-set construction, for ANY closed E in the
parameter domain there is a C¹ (indeed C^∞) function f ≥ 0 with
{f = 0} = E; take front F = {x = 0} and front G = {x = f(r, φ)} with
E = C₁ × C₂ a Cantor dust of Hausdorff dimension 3/2 (H²-null —
pin-compatible; both surfaces C¹). Then F ∩ G = E is compact, H²-null,
and NOT locally H¹-rectifiable (H¹(E) is non-σ-finite; no rectifiable
covering exists). The same mechanism breaks the "coincidence
boundaries" item: ∂P of a closed coincidence patch is only constrained
by the pin to... nothing (∂P ⊆ the pairwise intersection with the
continuing front, H²-null), and can likewise be non-rectifiable.
"Finitely many C¹ objects" is a count of the FRONTS, not a regularity
of their intersections. HONEST SCOPE: nothing downstream consumes
H¹-rectifiability — the conclusion, step (1), step (3), the ⟸ re-read
and falsifier (e) consume ONLY front-H²-nullity of N_F, which follows
from the pin directly (pairwise intersections null by pin; edges of C¹
hypersurfaces-with-boundary null; the piece-pair-structure-change set
is contained in the union of the previous two for a front-induced
piece decomposition). Repair (one edit): delete the rectifiability
gloss, or downgrade it to the transversal case ("locally H¹-rectifiable
WHERE the intersections are transversal; in general only H²-null, which
is all the assembly consumes"). CLASS: **AMENDMENT** (unconsumed
justification gloss; no conclusion motion — the L0-5 precedent: a
stated support that does not exist as stated).

NOVELTY/DEDUP: the class pin is r6-minted; no prior ID (L0-*, L1-*,
ESC-*, ESC2-*, R2L1-*) touches intersection regularity. Grep of all
esc_doc2 files for "rectifiable": zero prior hits.

------------------------------------------------------------------------------
**R3L1-3 — AMENDMENT. The r6 (w1) smooth-instance clause — "its
restriction to any subdomain compactly inside the marching domain and
clear of the CJ surface" — again certifies class predicates its cited
record does not deliver: [T-NSW](a) supplies ONLY |w_rel| > c on the
marching domain, not front-freeness of it (the standing flow's oblique
shock and slip line are in-scope objects that can extend into the
hyperbolic region — §0's own triple-point physics), and not genuine
φ-dependence of an ARBITRARY subdomain (the D.19 pumping certificate is
for the flow, not for every compact piece of it); the round-2 repair
spec said "any compact SMOOTH subregion" with a pointwise ∂_φV ≠ 0
predicate in the class statement — the r6 transcription dropped both
qualifiers. (Disposition-failure clause invoked on ESC2-L0-3/R2L1-3;
third consecutive generation of a defective (w1) exemplar spec.)**

Verbatim quote (1542–1545, inside the r6-corrected (w1)):

> "or, for a SMOOTH instance, its restriction to any subdomain
> compactly inside the marching domain and clear of the CJ surface,
> where [T-NSW](a) supplies |w_rel| bounded away from c"

Derivation-level attack, from the document's own lines. (a) SMOOTH:
the clause derives smoothness of the restriction from being "compactly
inside the marching domain" — but the only marching-domain fact of
record in this document is the H-NC block's "the marching domain of
record sits in the hyperbolic region |w_rel| > c" (1618–1620): a SONIC
bound, not a front census. The standing flow's front system (detonation
+ oblique shock + slip line — the triple-point configuration §0 names
as generic in-class, lines 263–265) is not certified to avoid the
hyperbolic region; an oblique shock with supersonic relative flow on
BOTH sides sits compactly inside {|w_rel| > c} and makes the printed
restriction non-smooth. If the marching domain is front-free BY
CONSTRUCTION of [T-NSW], the clause must SAY so and cite it — the
printed certificate ([T-NSW](a) = the sonic bound) does not carry it.
(b) φ-DEPENDENCE: membership in the (w1) class requires "genuinely
φ-dependent OFF FRONTS"; the D.19-pumping citation certifies ∂_φp ≠ 0
somewhere of record for the flow — it does not certify it on EVERY
compact subdomain, and nothing in the class forbids the standing flow
being exactly axisymmetric on some patch; under the printed universal
"ANY subdomain", a φ-independent patch gives K ≡ 0 and the exemplar
fails its K ≠ 0 role. (c) Provenance: R2L1-3's own repair option (i)
read "the standing flow on any compact smooth subregion of its marching
domain clear of the CJ surface" with the class predicate "one
smooth-region point where ∂_φV ≠ 0" — the r6 transcription kept "any"
but dropped "smooth" from the subregion qualifier and did not re-assert
the φ-dependence predicate for the restriction: the transcribed spec
runs hot for the third generation (r4 false universal → r5 non-member
exemplar → r6 over-quantified restriction). HONEST SCOPE: the payload
is untouched — the operative-condition rewrite of (w1)'s head is
correct (C-12), and the witness role is carried by (w2)/example (α)
independently, as every round has recorded. Repair (one phrase):
"its restriction to a front-free subdomain compactly inside the
marching domain meeting the D.19 pumping region (∂_φp ≠ 0 somewhere on
it); there [T-NSW](a) supplies |w_rel| bounded away from c". CLASS:
**AMENDMENT** (exemplar clause only — same classing both round-2
lenses used for the same-genus defect).

NOVELTY/DEDUP: ESC2-L0-3/R2L1-3 are dispositioned FIXED; their core
(the r5 certificates "smooth"/"bounded away" on the FULL flow) IS
fixed. This finding attacks the adequacy of the adopted replacement
under the brief's disposition-failure rule: the restriction clause is
r6-minted text. No prior ID touches the restriction's own predicates.

------------------------------------------------------------------------------
**R3L1-4 — AMENDMENT. The r6-scoped step-(1) justification parenthetical
cites "per-piece C¹ regularity up to the front" — a regularity §0 does
not grant: the declared class is "C¹ OFF the fronts with one-sided
limits at each front, and NO other discontinuities"; the trace-continuity
conclusion on F ∖ N_F is true, but its actual one-line ingredient (the
boundary extension of a C¹ function with unrestricted one-sided limits
at every front point is continuous on the closed piece) is consumed
unstated — the same consumed-unstated species as R2L1-2, and the very
line (u1) whose absence drives R3L1-1.**

Verbatim quote (1895–1900):

> "ON F ∖ N_F — off the closed front-null exceptional set of the r6
> jump-set pin — each across-the-front adjacent piece pair persists
> locally and its one-sided traces ARE continuous (per-piece C¹
> regularity up to the front, no interfering front or piece-structure
> change), so [V] is continuous on F ∖ N_F"

Against §0 (253–256, 271–273):

> "finitely many C¹ curves, off which V is C¹ with one-sided limits at
> each curve, and NO other discontinuities. ... 3-D wave-frame fields
> (§5) are the same class on D × S¹_φ, fronts = finitely many C¹
> hypersurfaces steady in φ (both types admitted)."

Attack. "C¹ regularity up to the front" (derivatives continuous up to
the boundary of the piece) is strictly stronger than "C¹ off the fronts
with one-sided limits" (values only), and §0's class statement grants
the latter. The step's CONCLUSION needs neither: existence of the
unrestricted one-sided limit at every front point already forces the
piece-side boundary extension to be continuous (for q, q′ on the front
with the same piece adjoining, every approximating value near q′ is
within ε of the extension at q once |q′ − q| is small — the standard
extension argument), which delivers per-pair trace continuity on
F ∖ N_F exactly as the step asserts. So the sentence is true with the
wrong supporting citation: a G-f executor checking the lemma against
the §0 class will find the cited ingredient absent. Repair (one
phrase): replace "per-piece C¹ regularity up to the front" with
"existence of unrestricted one-sided limits at every front point (§0),
whose piece-side boundary extension is continuous", or pin C¹-up-to-
the-piece-boundary into the §0 class explicitly (which would ALSO
supply line (u1) of R3L1-1 — the two repairs naturally merge). CLASS:
**AMENDMENT** (true conclusion, mis-cited support; the L0-5 "declared
normalization" precedent).

NOVELTY/DEDUP: the parenthetical is r6-minted (the r5 step (1) carried
no justification clause). ESC2-L0-2 attacked the r5 sentence's blanket
scope, not its regularity citation; no prior ID raises it.

==============================================================================
## §3 CONFIRMED passages (strongest attack tried, each)

**C-1. JUMP-SET PIN (1729–1737) — CONFIRMED.** Strongest attacks:
(i) well-posedness at T-junctions (a front G terminating on F: several
pairs adjoin across F at the junction) — the "SOME pair unequal"
quantifier handles multiplicity correctly, and jump-freeness of each
pair is what "off the jump set" needs for ⟸; (ii) one-front duct
geometry where the SAME piece adjoins from both sides (complement of a
disc-like front is connected) — the per-pair definition reads the two
approach SIDES, not distinct piece labels, and stays well-defined;
(iii) the normal-line exclusion — re-ran R2L1-1's over-attribution twin
(transversal crossing of a jump-free helical front with an axisymmetric
shock): under the pin the helical front's pairs adjoin ACROSS the
helical front only and carry zero jump; the shock's jump is never
attributed to it. The pin does exactly what the round-2 repair spec
demanded.

**C-2. CLASS PIN operative content + fallback (1737–1744) — CONFIRMED
(modulo the R3L1-2 gloss).** Strongest attack: hunt a pin-compatible
configuration with N_F NOT front-H²-null. Pairwise intersections are
null BY the pin; edges of C¹ hypersurfaces-with-boundary are null; the
piece-pair-structure-change set for a front-induced decomposition is
contained in (F ∩ other fronts) ∪ (edges) — could not be broken.
Closedness of N_F: the structure-change set is the complement in F of
the (open) locally-constant-structure set; union of three closed sets.
The fallback sentence ("Without the pin only the a.e./measure-grade
clauses below are asserted") is honest and matches the ⟸ re-read's
needs (the distributional core consumes no pin).

**C-3. The r6 containment display + derivation (1744–1754, 1900–1906)
— CONFIRMED.** Strongest attacks: (i) set algebra re-derived:
F ∖ cl{n_φ ≠ 0} = int{n_φ = 0} (both inclusions checked); A ∖ N ⊆ B ⟹
A ⊆ B ∪ N; (ii) the closure step: (cl{n_φ ≠ 0}) ∖ N_F ⊆
cl({n_φ ≠ 0} ∖ N_F) because N_F is closed and missed by a neighborhood
of any point outside it — so the continuity extension legitimately
reaches every point it is applied to; (iii) density after removing
N_F: relatively open subsets of a C¹ front have positive H², removing
an H²-null closed set preserves density — checks; (iv) the R2L1-1
witness re-run against the r6 form: F's jump set = P,
int{n_φ = 0}_F = int P, F ∩ C2′ = ∂P ⊆ N_F, so jump set ⊆
int P ∪ ∂P = int{n_φ = 0} ∪ N_F with the null set EXACTLY ∂P — the
in-statement "the witness PASSES the r6 containment" is correct as
recorded.

**C-4. Step (1) r6 scoping + INSENSITIVITY NOTE (1877–1911) —
CONFIRMED as an argument (modulo the R3L1-4 citation).** Strongest
attack: does the scoped lemma still over-reach anywhere in the
declared class? On F ∖ N_F the adjacent pair persists by definition of
N_F (structure changes are IN N_F), no other front interferes (points
on other fronts are in N_F), and trace continuity follows from the
extension argument — the round-2 counterexamples (Mach-reflection
crossing; coincidence boundary ∂P) are now both inside N_F, where
nothing is claimed. The insensitivity note is exact: the atom is an
H²-a.c. measure on the front, [V] = 0 H²-a.e. on {n_φ ≠ 0} kills it
there with NO exceptional-set bookkeeping, and n_φ = 0 kills it on
int{n_φ = 0} — the distributional closure consumes only the a.e.
grade, as stated.

**C-5. Step (3) grade honesty + REGULARITY paragraph (1918–1940) —
CONFIRMED.** Strongest attacks: (i) the front-free-slice identity
"Ṽ|_B = V(·, ·, φ′) ∈ C¹" — hunted a null-set-slice pathology (the
a.e. identity V = Ṽ∘π does not restrict to a fixed slice): killed
in-class because V is an honest piecewise-C¹ function — on the
front-free tube-component through B × {φ′}, ∂_φV = 0 holds CLASSICALLY
(C¹ + vanishing distributional derivative on an open set), V(x, r, ·)
is constant along each front-free fiber arc, and the arc has positive
measure in the fiber, pinning the constant to Ṽ(x, r) for a.e.
(x, r) ∈ B — the slice equality holds as an a.e. identity, which is
all "Ṽ ∈ C¹ off S" needs; (ii) S closed: its complement is open by
the product-neighborhood argument (front set closed); rotation
invariance and meridionality are by construction; (iii) hunted a
downstream consumer of the retired "piecewise-C¹ Ṽ" claim: the second
iff's in-document consumers are the S.22 identification paragraph and
the G-f falsifier instances — neither consumes Ṽ-regularity; the
"neither claimed nor consumed" sentence (1762–1764) survives.

**C-6. The ⟸ re-read (1957–1961) — CONFIRMED.** Given the r6 clause
list: a.c. K rows die by φ-independence off fronts; each front atom
n_φ[F_φ,rel]·H² dies as a measure ([F_φ,rel] = 0 off the jump set via
equal pair limits and flux continuity in state and geometry; n_φ = 0
on jump set ∖ N_F; N_F front-null). Strongest attack: run ⟸ WITHOUT
the class pin — the fallback a.e.-grade clauses still kill the atom
([V] = 0 a.e. on {n_φ ≠ 0} directly), so the direction does not
secretly depend on the pin. Consistent.

**C-7. The Γ_G in-model line, BOTH anchors (1666–1679, 2038–2048) —
CONFIRMED; R2L1-2 DISCHARGED.** Independently re-derived: for
p = ρR_gT, h = h(T): e = h − R_gT, (∂e/∂T)_v = c_p − R_g = c_v > 0 by
AUD-cp; (∂p/∂T)_v = R_g/v·... per unit mass p = R_gT/v so
(∂p/∂T)_v = R_g/v; hence Γ_G = v(∂p/∂e)_v = v·(R_g/v)/c_v = R_g/c_v =
γ(T) − 1 > 0 > −2 — pointwise at every table state, no γ = const
assumption consumed (c_v = c_v(T) throughout). Strongest attacks:
(i) variable-γ leak — none (the identity is state-local); (ii) the
sufficiency citation — connectedness/single-valuedness from convexity
PLUS Γ_G > −2 is the Bethe/Menikoff–Plohr weak-condition package the
hypothesis line already cites, and the r6 wording now attributes
connectedness to "G_fund > 1 TOGETHER WITH Γ_G > 0", resolving the
internal contradiction R2L1-2 named; (iii) anchor consistency — the
hypothesis-line parenthetical and the gamma-status block state the
same identity with the same consequence; no divergence.

**C-8. Falsifier (a) re-bind (2070–2086) — CONFIRMED; ESC2-L0-4
DISCHARGED.** Strongest attack: the residual type-slip "an EOS
satisfying CONNECTED-ADIABAT H-CVX" (H-CVX is now a trace-pair/
configuration hypothesis, not an EOS property) — prosecuted both
executor readings: (reading 1, per-configuration) the sheet's own
trace pair must satisfy connected-adiabat H-CVX — then an isentropic
nontrivial sheet under it refutes the closure, correct; (reading 2,
EOS-wide, i.e. a convex EOS) — every RH pair of a convex EOS satisfies
the hypothesis, so the exhibit again correctly refutes. On EITHER
reading the detached-branch exhibit is out-of-hypothesis (its
downstream trace is not on the connected adiabat), exactly as the
parenthetical says — the r5 vacuous-firing defect cannot recur. No
misguidance survives; the stale "r4 closure" reference is gone.

**C-9. Falsifier (d) r6 note + falsifier (e) (2099–2116) —
CONFIRMED.** (d): the mixed-front witness's jump set is strictly
inside the φ-invariant part of a SINGLE declared front (N_F = edges
only) — it avoids N_F, so the r5 and r6 containments coincide on it
and "passed identically" is exact. (e): re-derived the witness's
required verdicts — PASS distributional K = 0 (axisymmetric field:
a.c. part zero; atoms carry n_φ = 0 on P and C2′, [V] = 0 on the
helical flap), PASS the r6 containment with null set exactly ∂P (C-3
attack (iv)), REFUTE the r5 pointwise clause (∂P ⊄ int{n_φ = 0}) — a
well-posed one-instance separator of the r5 and r6 forms; the witness
declaration satisfies the class pin (F ∩ C2′ = ∂P, H²-null).
Commit-gated queueing consistent with standing discipline.

**C-10. Register row D.18 r6 additions (2368) + G-f entry (2427–2430)
— CONFIRMED as bookkeeping.** The row's ROUND 3 sentence matches the
body point-for-point (pin, N_F containment, step-(1) scoping, a.e.
grade, weakened Ṽ-regularity, (w1) exemplar, falsifier (a) re-bind,
Γ_G at both anchors, "proved as written" retraction); the G-f
coincidence-boundary instance matches falsifier (e). One nit recorded,
not minted: the row abbreviates N_F as "crossing/edge/coincidence-
boundary set", omitting the structure-change clause of the definition
— a paraphrase, and the row points at the body for the definition.
SCHEMA held at all sites; no self-upgrade (the "at exactly the
strength" prose is prosecuted at R3L1-1 as a certification defect, not
a label leak).

**C-11. §6-quinquies ROUND 3 table + header r6 block + residue
paragraph (140–181, 2682–2738) — CONFIRMED as bookkeeping, with three
rows inheriting qualifications.** Strongest attack: hunt a ledger row
claiming more than the body delivers. Rows R2L1-1, ESC2-L0-2,
ESC2-L0-4, R2L1-2: enumerated deliveries verified accurate and
complete. Rows ESC2-L0-1 ("structure clauses derived DIRECTLY") and
ESC2-L0-3/R2L1-3 ("certificates replaced by the OPERATIVE ones"):
accurate as enumerated, but the landed text carries the R3L1-1/R3L1-3
residues — per the round-2 precedent the rows stand as written and the
residue rides this round's findings. The over-certification counter
arithmetic checks (r3 "strengthened and made true" tag, r4 "complete
AS WRITTEN", r5 "proved as written" = THREE at the r6 retraction; if
R3L1-1 is sustained the counter goes to FOUR). The two declared prices
are real; the interruption-free r6 slot needs no reconciliation note.

**C-12. (w1) head + first exemplar (1536–1542, 1546–1562) — CONFIRMED
(modulo the R3L1-3 restriction clause).** The r6 class head ("exact
wave-frame solution genuinely φ-dependent OFF FRONTS and satisfying
H-NC + H-WR has K ≠ 0 BY THE A.C. LEG") re-derived: the a.c. leg runs
per smooth region, so its contrapositive is valid for FRONT-CARRYING
fields — one point of φ-dependence off fronts forces a nonzero a.c.
K row on an open set, and a.c./singular mutual singularity gives
K ≠ 0 regardless of atoms. The full-flow exemplar's certificates are
now the operative ones (sonic locus = CJ hypersurface, co-rotation
locus empty interior, D.19 pumping for φ-dependence) — matching the
H-NC/H-WR hypothesis lines. Attacks tried: (i) a kernel collision at
the CJ locus — irrelevant to the contrapositive (the pumping point is
off the loci by empty interior + continuity); (ii) the co-rotation
empty-interior certificate scope (the printed in-scope justification
covers the marching domain only) — NOT raised: that justification is
r2-era hypothesis-line text adjudicated through six rounds, outside
this round's target, and the physical fill-region argument
(w_rel ≈ −Ωr ≠ 0) leaves no constructible in-scope violation; recorded
here as attempted. (iii) The r6 annotation's quotation of the r5
clause verified verbatim; annotate-never-erase satisfied.

**C-13. Leg-7 / A-1 (D.2 parenthetical, 360–369) — CONFIRMED, no
regression.** Grep: no r6 marker below the header touches D.2; the
wording is the judge's A-1 verbatim ("i.e. genuinely nonlinear WITH
the convex (compressive-shock) orientation"), with the false-
equivalence mechanism recorded in-statement. Strongest attack: does
the r6 Γ_G addition in D.18 retroactively obligate D.2's "the arc
condition is FREE in-model" discharge to co-cite Γ_G > 0? No — D.2's
H-CVX conditions an entropy-MONOTONICITY claim along an arc whose
existence D.2 does not assert (Hugoniot well-definedness is carried by
the cited Menikoff–Plohr weak conditions); the D.2-vs-D.18 divergence
(arc vs connected-adiabat) remains DECLARED in D.18's hypothesis line
with the correct reason (D.18's use must EXCLUDE a state; vacuity
there is failure). No re-open.

**C-14. Proof-state block retraction mechanics (1998–2037) —
CONFIRMED as retraction mechanics; the replacement certification is
prosecuted at R3L1-1.** The r5 sentence is quoted and retracted with
the two witnesses named; the assembly-repair counter ("r6 is the
THIRD") is accurate; prior text preserved in quotation throughout the
r6 edits (spot-checked at all five annotation sites). Labels at
SCHEMA with the restoration path unchanged.

==============================================================================
## §4 Summary

| ID | Target (r6 passage) | Class |
|---|---|---|
| R3L1-1 | conclusion structure clauses printed flat/global vs step-(3) a.e.-grade salvage; no N_F price; unwritten a.e.→pointwise upgrade (u1)/(u2); certification "AT EXACTLY THE STRENGTH OF ITS STATED CLAUSES" rides — would be the fourth consecutive over-certification | **REPAIR-NEEDED** |
| R3L1-2 | class-pin gloss "crossings, triple curves, coincidence boundaries and edges are locally H¹-rectifiable" — false for tangential C¹ contact (Whitney zero-set, Cantor-dust intersection, H²-null but not rectifiable); operative H²-nullity unaffected | AMENDMENT |
| R3L1-3 | (w1) smooth-instance clause "restriction to any subdomain compactly inside the marching domain" — smoothness certified by a sonic bound only (in-scope oblique shock/slip line can enter the hyperbolic region); φ-dependence not re-certified under "any"; round-2 spec's "smooth subregion" + ∂_φV ≠ 0 predicate dropped in transcription | AMENDMENT |
| R3L1-4 | step-(1) justification cites "per-piece C¹ regularity up to the front" — not granted by §0 (values-limits class); true one-line ingredient (continuous boundary extension) consumed unstated | AMENDMENT |

BREAKS-THE-LEG: 0. Total findings: 4 (1 R / 3 A). Discharge audit:
R2L1-1, ESC2-L0-2, ESC2-L0-4, R2L1-2 DISCHARGED clean; ESC2-L0-1 and
ESC2-L0-3/R2L1-3 PARTIALLY DISCHARGED (residues R3L1-1 and R3L1-3 —
both transcribed-spec-runs-hot defects, third instance of the class).
A-1 stands. NO false printed display was found this round: every r6
mathematical statement I could test survived (containment, set algebra,
Γ_G identity, witness verdicts, ⟸ re-read) — the loop is CLOSE to dry
on this lens; what stands between the r6 text and a clean adjudication
is one grade-alignment repair (with its certification sentence) and
three one-phrase amendments. Labels already at SCHEMA per J-r2p-2/3 —
no label motion implied by any finding here.

==============================================================================
## §5 Self-falsifiers for THIS refutation

- R3L1-1 dies if: (i) the a.e.→pointwise upgrade is shown ALREADY
  written somewhere in the document (my grep for the boundary-extension
  argument found none: "extension" hits nothing relevant; step (1)
  asserts continuity only ON F ∖ N_F and only for traces, not the
  propagation across N_F-separated arc components); or (ii) the
  structure clauses are shown to be INTENDED at a.e. grade by the
  document's own conventions (then the finding drops to stating the
  grade in the display — but the certification sentence still needs the
  edit, since "at exactly the strength of its stated clauses" turns on
  what the clauses state); or (iii) my claim that the upgrade is
  nontrivial is refuted by a two-line in-class proof from the printed
  ingredients alone — note my own best attempt (T-junction and
  tangency-shock witnesses) FAILED to falsify the clauses precisely
  because the unwritten line (u1) is true: a constructed in-class
  counterexample to clause (i) would simultaneously kill this
  finding's REPAIR classing and re-open the leg at BREAKS, in the
  document's favor in neither case.
- R3L1-2 dies if the §0 class or the pin is shown to carry a
  transversality/finite-tangency ingredient (my read of §0 253–276 and
  the pin 1737–1743 found none), or if a proof is exhibited that
  pin-compatible C¹ intersections are always locally H¹-rectifiable —
  contradicted by the Whitney construction above (any closed H²-null
  set arises as a tangential intersection).
- R3L1-3 dies if [T-NSW] (at M0 citation grade) defines the marching
  domain as front-free of record AND certifies the D.19 pumping on
  every compact subregion (or the flow φ-dependent on every open set)
  — then "any subdomain" type-checks and only the missing citation
  remains; partial kill: front-freeness alone drops the finding to the
  φ-dependence half.
- R3L1-4 dies if §0's "piecewise C¹ on the closure" is adjudicated to
  mean C¹ up to each piece's boundary (the reading under which the
  cited regularity IS granted) — the repair then becomes a §0
  clarification instead; the finding's citation-mismatch core survives
  either way until one of the two texts is pinned.
- The §3 confirmations die by exhibiting an error in any recorded
  re-derivation (each reproducible: the F ∖ cl{n_φ≠0} = int{n_φ=0}
  decomposition; the closure-vs-N_F inclusion; the witness verdicts
  for falsifiers (d)/(e); Γ_G = R_g/c_v = γ − 1 with variable c_v(T);
  the two executor readings of falsifier (a); the front-free-slice
  argument; the a.c.-leg contrapositive for front-carrying fields).

Round-3 lens-1 totals: 4 objections (0 B / 1 R / 3 A), 14 passages
CONFIRMED with strongest attacks recorded. Nothing else in the r6
delta withstood less than the attacks listed.

END — machine summary: {objections: 4, breaks: 0,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc2_r3_l1.md}
