# ESCALATION REFUTATION — DOC-1 E-1/E-2 repairs, until-dry ROUND 4, lens l0
# (hyperbolic systems / functional-analytic rigor)

Date: 2026-08-19. Target: `phaseD/phaseD_stop_proof.md` AT REVISION 7 —
ONLY the escalation-repair text for legs 3 and 5 named by the NEWEST
revision-log entries (the §13 REVISION 7 block, lines 3447-3670, and
the body passages it edits — all on the G8/r2 accounting row of leg 5:
the DOMAIN-COMPLETED condition of record + itemized consumptions,
lines 2122-2150; the scoped-and-WRITTEN sufficiency (quadruple pin,
g-condition clause, written assembly), lines 2151-2193; the AUD-cp
note's revision-7 clause, lines 2249-2256; the RE-CALIBRATED falsifier
parenthetical, lines 2268-2283; the re-headed grant clause + theta
annotation, lines 2284-2293; the four propagation carriers: header
audit line 88-98, honest status line 2338-2354, §9 gamma table
2546-2569, §13 revision-6 entries SUPERSEDED-IN-PART note 3365-3386.
Leg 3 received ZERO edits at revision 7 — examined and handled in its
own section below).

FILE-NAME / ROUND-NUMBER DEVIATION, DECLARED (FIFTH occurrence of the
same off-by-one; same handling as the prior rounds, which the
revision log ratifies as "declared"): the launching brief named this
slot "until-dry ROUND 3" with output `esc_doc1_r3_l0.md`. That file
EXISTS, is the CONSUMED round-3 record (its IDs E3L0-1/E3L0-2 are
cited of record in the target's REVISION 7 block, lines 3447-3449),
and overwriting it would destroy the artifact the revision log cites.
The brief's operative targeting clause ("the newest revision-log
entries name the edited passages") resolves to the REVISION 7 block,
whose honest-residue item (1) states these revision-7 repairs "are
themselves UNADJUDICATED and await the next escalation-refutation
round" — this document IS that round. Output therefore goes to
`esc_doc1_r4_l0.md`; the round-3 record is untouched.

Specs consumed: `r2pass/VERDICT_r2pass.md` §2.1 rows L0-1/L1-1, L0-2,
L1-2 and §4(c) E-1/E-2 (the standing escalation specs); full ROUND-3
objection text `r2pass/esc_doc1_r3_l0.md` (E3L0-1, E3L0-2) and full
round-2 lens-l1 text `r2pass/esc_doc1_r2_l1.md` (ER2L1-1) — the three
findings revision 7 answers — read IN FULL; prior rounds
`esc_doc1_r1_l0.md`/`esc_doc1_r1_l1.md`/`esc_doc1_r2_l0.md` per the
dedup layer; original r2-batch texts `refute_r2batch_l0.md`/
`refute_r2batch_l1.md` at the attacked legs (dedup layer). Both
duties executed: (a) derivation-level attack on the revision-7 repair
text; (b) verification that the revision-7 repairs DISCHARGE the
sustained findings they answer (E3L0-1, E3L0-2, ER2L1-1).

DEDUP REGISTER (dispositioned objections NOT re-raised): L0-1/L1-1,
L0-2, L1-2 (r2-batch — consumed revisions 4/5); EL0-1==ESC1-F1,
EL0-2, EL0-3==ESC1-F2, EL0-4 (round 1 — consumed revision 5,
dispositions verified at both lenses of record); E2L0-1, E2L0-2
(round 2 — consumed revision 6, dispositions re-verified at revision
7 by both round-3-window refuters); E3L0-1, E3L0-2, ER2L1-1 (round 3
window — consumed at revision 7, adequacy examined below). All four
round-4 findings attack REVISION-7 text that did not exist before
revision 7 — zero prior adversarial coverage — and each declares its
distinction from every ID above; where a finding shows a disposition
partially failing, the ID is cited and the failure point stated.
Every attacked passage: verbatim quote + attack + class in
{BREAKS-THE-LEG, REPAIR-NEEDED, AMENDMENT}. Every examined passage
not broken: CONFIRMED with the strongest attack tried.

STANDING-LESSON COMPLIANCE (revision-7 honest residue item (2), the
spec-seeding pattern): all revision-7 repair text was attacked AS
repair-spec-derived text. Result: ALL FOUR findings below are
spec-seeded — the fourth consecutive round of the pattern (the
round-3 specs' own clauses seeded each defect; provenance declared
per finding).

Finding IDs: E4L0-1..E4L0-4. Line numbers are of the revision-7 file.

==============================================================================
## LEG 5 (E-2) — the revision-7 G8/r2 domain-completion + scoped
## sufficiency rewrite

### 5.1 The written assembly's missing FIRST link (strict Legendre
### bridge) — **OBJECTION E4L0-1, AMENDMENT**

Quote 1 (the assembly's opening, lines 2175-2181): "The assembly,
written: given the condition of record, at a rest state the W-Hessian
of -rho S is BLOCK-DIAGONAL — the (rho, E) block is
tau B^T Hess(-s) B (the (m2) perspective congruence run in the
sufficiency direction; strict s-concavity transfers to strict slice
positivity because B is invertible)".

Quote 2 (the condition of record it starts from, lines 2143-2144):
"theta > 0 AND Hess e(v, S) positive DEFINITE on (the (v, S)-image
of) the comparison-segment hull".

Quote 3 (the only printed equivalence between the two coordinate
systems, lines 2117-2122): "FULL thermodynamic stability = JOINT
concavity of s as a function of (specific volume, internal energy) —
the Bethe-Weyl class ... equivalently (theta > 0, partial Legendre
inversion) JOINT convexity of e(v, S)."

Attack (derivation level). The condition of record is stated on
Hess e(v, S); the written assembly's first operative object is
"strict s-concavity" — Hess(-s) positive definite in (v, eps). The
bridge — strict PD of Hess e(v, S) implies STRICT concavity of
s(v, eps), rank-faithfully and with uniformity preserved on compacts
— is consumed silently between "given the condition of record" and
"strict s-concavity transfers". The only printed equivalence (Quote
3) is the NON-strict class description; the strict, rank-faithful
version is a genuine one-line lemma (via the congruence
Hess_{(v,eps)} s = -(1/theta) C^T Hess_{(v,S)} e C with
C = [[1, 0], [-e_v/theta, 1/theta]] invertible — I verified the
congruence entry-by-entry: s_epseps = -e_SS/theta^3;
s_veps = -(e_vS - e_SS e_v/theta)/theta^2; s_vv as forced; the
quadratic-form identity Q_s(dv, deps) = -(1/theta) Q_e(dv, dS) with
dS = (deps - e_v dv)/theta) and it is nowhere written in the row.
The delivered assembly is exactly the l1 chain of record
(esc_doc1_r2_l1.md §5.1, steps (1)-(4)) MINUS its step (1): the
revision-7 log itself cites that chain as "the adversarial check of
record on the written assembly's content" (lines 3529-3532), i.e. it
acknowledges the check lives in the refutation file — steps (2)-(4)
were transcribed into the row, step (1) was not. Held to this
document's own repeatedly-enforced standard ("one line, but the line
must be WRITTEN" — the exact standard under which E3L0-1 was
sustained one round ago), the E3L0-1 disposition's sentence "The
assembly is now WRITTEN in the row" (line 3489) is true of steps
(2)-(4) and false of the bridge.

Class: **AMENDMENT** — the bridge is TRUE (congruence verified above;
independently pen-verified by the l1 refuter of record), the assembly
built on it is sound, and no in-class object separates anything: the
defect is one unwritten load-bearing line inside delivered repair
text, with the benign reading (the apposition "STRICT joint
s-concavity ... — positive-definite Hess e(v, S)" in the honest
status line, lines 2345-2348, asserting the identification) available
and used by every consumer. Repair, one line in the assembly: "strict
PD of Hess e(v, S) is equivalent, given theta > 0, to strict
concavity of s(v, eps) — congruence Hess s = -(1/theta) C^T Hess e C,
C invertible, rank-faithful, uniform on compacts by continuity of C".

DEDUP DECLARATION: NOT a re-raise of E3L0-1 — that priced the
sufficiency note's false quantification and the TOTAL absence of the
assembly; revision 7 delivered the assembly, and THIS finding prices
the one link the delivery omitted (text that did not exist before
revision 7). The E3L0-1 disposition fails at exactly the sentence
"The assembly is now WRITTEN" and nowhere else. PROVENANCE, declared
(spec-seeded, instance 1 of 4 this round): E3L0-1's own repair spec
named the assembly elements as "slice congruence + m-block + cross
terms + boost + compactness" — the B-congruence, NOT the Legendre
bridge; revision 7 transcribed the spec's element list faithfully and
inherited its omission. Repair-spec text is not exempt.

### 5.2 The "(v, S)-image" root-quantifier — **OBJECTION E4L0-2,
### AMENDMENT**

Quote (condition of record, lines 2143-2144): "theta > 0 AND
Hess e(v, S) positive DEFINITE on (the (v, S)-image of) the
comparison-segment hull".

Attack (derivation level — well-definedness of the object the
condition quantifies over). §1's EOS primitive is e = e(rho, S)
(lines 194-198); at an interior segment point W (off K — the row's
own central point) the pair (v, S) is obtained by ROOT-SOLVING
eps(W) = e(v, S) in S. theta > 0 AT an image point licenses only the
LOCAL inversion (IFT); it does not make the root unique on the
S-line: for a §1-class EOS non-monotone in S off the pin region,
e(v, .) = eps can have several roots, and "(the (v, S)-image of) the
hull" is then SELECTION-DEPENDENT — the condition as printed does not
say which root(s) it is evaluated at.

The ambiguity is not free. Under the per-segment-CONTINUATION reading
(continue the S-root from the U-endpoint along each comparison
segment — the only computable single-branch reading), there is an
in-class separating mechanism at the schema level: take
e(v, S) = e0(v) + h(S) with e0'' > 0 and h increasing-decreasing-
increasing (theta = h' > 0 at the two outer monotone pieces), with
the data values S_U, S_V on the two outer pieces at the same level
band. Gibbs closure holds by construction; c^2 = v^2 e0'' > 0 is
S-independent; theta > 0 and Hess e = diag(e0'', h'') PD hold ALONG
THE CONTINUED BRANCH (h convex on the first piece). But the
continuation from U lands, at s = 1, on the FIRST-piece root — NOT on
V's data root S_V: the Bregman integral then bounds the relative
entropy of the CONTINUED function, which differs from the DATA
relative entropy by -rho_V (S_V - S_landed) != 0, and printed item
(i) — a statement about the data object — is not delivered although
the printed condition, read on the continued image, is satisfied.
(Whether the §1 pin's vague domain ("the state region of interest")
independently excludes the middle theta < 0 zone is exactly the
domain question E3L0-2 established the record does NOT settle.)

The benign reading exists and SELF-REPAIRS: read "the (v, S)-image"
as the set of ALL S-roots over hull points (the image of a
multivalued map = union over branches). Under that reading the
theta > 0 clause forces root UNIQUENESS by level-crossing parity: if
two roots S1 < S2 of e(v, .) = eps both carry theta > 0, then h
passes from above the level (just after S1) to below it (just before
S2), so an intermediate root with theta <= 0 exists in the image and
the condition FAILS — contrapositive: the condition implies exactly
one root per hull point, the chart is single-valued, the continuation
lands on the data root, and the whole assembly closes. This parity
line is itself unwritten.

Class: **AMENDMENT** — by the loop's own calibration (E2L0-2,
ER2L1-1 precedents: ambiguous clause with a benign reading, one-
clause fix): the universal reading is available, closes the gap
completely, and is free at the standing gamma(T) closure (theta = T
> 0 on the whole table domain — roots unique by global monotonicity).
Under the per-branch reading alone this would be REPAIR-NEEDED
(separating mechanism above); the repair therefore must make the
universal reading EXPLICIT, not merely available. Repair, one clause:
"theta > 0 at EVERY S-root of eps(W) = e(v, S) over each hull point —
which forces the root to be unique (two theta > 0 roots bracket a
theta <= 0 root by level-crossing parity), makes the (v, S)-image and
the chart single-valued, and lands each segment continuation on the
data root".

DEDUP DECLARATION: NOT a re-raise of E3L0-2 — that priced theta's
POSITIVITY being unpinned on the hull (pin-domain axis) and is
discharged as delivered (5.5 below); this prices the IMAGE's
single-valuedness (quantifier-over-roots axis), i.e. the delta
between "theta > 0 at the (selected) image" and the monotone
inversion the chart needs — text "(the (v, S)-image of)" that did not
exist before revision 7. Not a re-raise of the E3L0-2 itemization's
chart clause (which consumed theta != 0 pointwise "before any Hessian
can be read there" — the pointwise IFT reading). PROVENANCE, declared
(spec-seeded, instance 2 of 4): the parenthetical "(the (v, S)-image
of)" was transplanted VERBATIM from E3L0-2's own repair spec
(esc_doc1_r3_l0.md §5.2 repair clause) — the spec introduced the
image without pinning the branch. The E3L0-2 disposition fails at
exactly this transplanted parenthesis and nowhere else.

### 5.3 The re-calibrated falsifier's unrescoped consequent —
### **OBJECTION E4L0-3, AMENDMENT**

Quote (lines 2268-2283): "derive joint s-concavity IN THE STRICT
FORM on the comparison-segment hull (equivalently: wherever the §1
pins hold, PROVIDED the pins are read on the whole EOS domain — else
on the hull itself; parenthetical RE-CALIBRATED in revision 7 per
E3L0-2 ...) from Gibbs closure + c^2 > 0 + theta > 0 ALONE, with no
K-geometry input — THAT kills the condition-claim and restores the
unconditional grant".

Attack (derivation level — consequent arithmetic against the
revision-7 condition). The condition of record is now a CONJUNCTION:
hull-theta AND Hess e PD. The falsifier's two branches deliver
different restorations, but the printed consequent is one:

(a) Whole-domain branch ("PROVIDED the pins are read on the whole EOS
domain"): the pins themselves cover the hull, so a successful
derivation discharges BOTH members — "restores the unconditional
grant" is correct there.

(b) Else branch ("else on the hull itself"): a derivation concluding
ON THE HULL from "theta > 0 ALONE" must take theta > 0 ON THE HULL as
an input — i.e. it ASSUMES the hull-theta member of the condition it
is supposed to kill. A successful else-branch derivation kills only
the Hessian member and restores the grant MODULO hull-theta — not
unconditionally. The printed consequent therefore has residual
false-restoration power on the hull-theta member: a firing in the
else branch would of-record delete a condition member the firing did
not discharge. This contradicts the row's OWN revision-7 grant
annotation ("that region carries NO theta clause, which is why the
hull-theta member of the condition is a genuine addition, E3L0-2",
lines 2291-2293): a member the row insists is a genuine addition
cannot be erased by a derivation that consumed it as a hypothesis.

Class: **AMENDMENT** — the falsifier is auxiliary text guarding the
condition-claim (the exact text class of E2L0-2, AMENDMENT of
record); the whole-domain branch's reading is benign and correct; the
hull-theta member separately carries its OWN falsifier in the
revision-7 log entry (lines 3565-3571), so no route-level conclusion
is exposed. Repair, one clause: "... and restores the unconditional
grant IN THE WHOLE-DOMAIN BRANCH; in the else branch it kills the
Hessian member only, leaving the grant conditional on hull-theta
(which retains its own falsifier: a printed line placing the hull in
the theta-pin region, or a proof that endpoint K-membership forces
theta > 0 along W-segments)".

DEDUP DECLARATION: NOT a re-raise of E2L0-2 (restoration DOMAIN, "on
K" vs hull — discharged, and its no-K-input clause survives intact)
nor of the parenthetical gap E3L0-2 carried (hull ⊆ pin-region
EQUIVALENCE — fixed by the PROVIDED/else split, verified in 5.5):
this prices the CONSEQUENT's strength across the split's two
branches — the branch structure is revision-7 text. PROVENANCE,
declared (spec-seeded, instance 3 of 4): E3L0-2's repair spec
rewrote the parenthetical and left the pre-existing consequent
"restores the unconditional grant" untouched next to a condition it
had just made conjunctive; revision 7 executed the spec faithfully
and inherited the mismatch.

### 5.4 Two provenance glosses in the condition/sufficiency text —
### **OBJECTION E4L0-4, AMENDMENT** (two sites, one defect class)

Site (a) quote (lines 2145-2147): "the hull-theta clause PLUS the
strict pair e_vv > 0 (= c^2 > 0, already a §1 pin) AND the strict
Grueneisen cross-term bound e_vv e_SS > (e_vS)^2 (unpinned by §1)".

Site (b) quote (lines 2172-2175): "at g = S it reads 0 < 1/c_v, i.e.
c_v > 0, which the strict pair already contains — exactly why the
printed condition self-suffices at the certified quadruple and at no
strongly-convex g."

Attack (derivation level — both glosses mis-attribute provenance, and
the row's own revision-7 accounting refutes them):

(a) "already a §1 pin": §1 pins c^2 > 0 only "on the state region of
interest" (line 197) — the SAME domain limitation the revision-7 text
itself invokes for theta two lines above ("§1 pins theta > 0 only 'on
the state region of interest', segments LEAVE K", lines 2138-2139).
On the hull, e_vv > 0 is IMPOSED by the condition of record (it is a
minor of "Hess e PD on the image"), not inherited from §1: the gloss
survived from revision 6, but at revision 7 it became INTERNALLY
inconsistent — the row now argues in one sentence that §1 pins do not
reach the hull and glosses in the next that a hull-imposed member is
"already a §1 pin". (The identification c^2 = v^2 e_vv itself is
exact — verified: p = -e_v, c^2 = p_rho|_S = (-e_vv)(dv/drho) =
v^2 e_vv — only the domain attribution is wrong.)

(b) "which the strict pair already contains": c_v = theta/e_SS. The
strict pair delivers e_SS > 0 ONLY; the SIGN of c_v additionally
consumes theta > 0 — the hull-theta member, not the strict pair. An
in-class state with e_vv > 0, e_vv e_SS > (e_vS)^2 (strict pair
satisfied) and theta < 0 has c_v < 0. Such a state is excluded by the
FULL condition — which is exactly the point: the sentence attributes
to the strict pair what the CONJUNCTION delivers, at the very
revision that made the conjunction's second member load-bearing. (At
the ideal-gas instance — where the kept iff lives — theta > 0 is
automatic and the attribution is harmlessly true; the row's scope is
abstract EOS, where it is not.) The conclusion the gloss argues FOR
("the printed condition self-suffices at the certified quadruple")
remains TRUE — it is delivered by the written assembly, which
correctly consumes hull-theta for the m-block AND for c_v's sign —
and the flanking claim "and at no strongly-convex g" is also correct
(attacked and survived: for any g with g''/g' >= m/M > 0 on the
range, an ideal gas with c_v > M/m defeats the kept iff, so the
strict condition cannot self-suffice for that g).

Class: **AMENDMENT** (both sites) — explanatory glosses with the
delivered mathematics correct around them; no statement, condition
strength, or route-level conclusion moves; benign readings exist
(site (a): "the same quantity §1 pins, here imposed hull-wide"; site
(b): true at the ideal-gas instance named in the same sentence).
Repair, two wordings: (a) "(= c^2 > 0 — the §1-pinned quantity, here
imposed hull-wide by the condition itself)"; (b) "which the condition
of record already contains (e_SS > 0 via the strict pair, the sign of
c_v = theta/e_SS via the hull-theta clause; at ideal gas theta > 0 is
automatic)".

DEDUP DECLARATION: NOT a re-raise of E3L0-2 (theta member ABSENT from
the condition — now present; this prices attribution GLOSSES inside
the completed condition, a defect that crystallized only when
revision 7 made hull-theta load-bearing) nor of ER2L1-1 (quadruple
scope of the sufficiency — delivered; this prices the WHY-clause
appended to the delivered scoping). Site (a) was inside a passage my
round-3 5.3 CONFIRMED — the recorded strongest attacks there
(Sylvester, lattice, necessity re-scope) did not test provenance
attributions; per my own self-falsifier clause a CONFIRMED verdict
yields to a new derivation-level attack, and the internal
inconsistency did not exist before the revision-7 sentence was
inserted two lines above. PROVENANCE, declared (spec-seeded, instance
4 of 4): site (b) is transplanted VERBATIM from ER2L1-1's own repair
clause ("at g = S it reduces to c_v > 0, which the strict pair
already contains", esc_doc1_r2_l1.md §5.1) — the l1 spec's derivation
ran at ideal gas where theta > 0 is free and the clause silently
generalized. The ER2L1-1 disposition fails at exactly this
transplanted clause and nowhere else.

### 5.5 The DOMAIN-COMPLETED condition + itemized consumptions —
### CONFIRMED (modulo E4L0-2/E4L0-4 above)

Quote (lines 2127-2150, the revision-7 insertion): "DOMAIN-COMPLETED
in revision 7 per E3L0-2 (the transfer from 'Hess e(v, S) PD' to item
(i) consumes theta > 0 AT HULL STATES three separate times — the
chart W -> (v, S) inverts eps = e(v, S) in S and needs e_S = theta
!= 0 along the hull before any Hessian can be read there; the partial
Legendre step above is flagged '(theta > 0)' and runs pointwise where
applied, i.e. hull-wide; and the momentum block of the sufficiency
assembly below is (1/(rho theta)) I, NEGATIVE at a hull state with
theta < 0 even with Hess e(v, S) PD — theta is FIRST-order EOS data
no Hessian condition controls — while §1 pins theta > 0 only 'on the
state region of interest', segments LEAVE K, {theta > 0} is NOT
convex in W (theta composes the non-affine S(W)), and the grant's
convex physical region below carries no theta clause: the consumption
was real, named, and unpinned): theta > 0 AND Hess e(v, S) positive
DEFINITE ..."

Strongest attacks tried, all failed:
(a) *Itemization fidelity*: the three consumptions transcribe
E3L0-2(a)/(b)/(c) faithfully (chart, Legendre, m-block), each with
the correct mechanism; the domain accounting (pin region, K-escape,
non-convexity of {theta > 0}, theta-free grant region) matches the
sustained finding clause-for-clause. No distortion found.
(b) *Is hull-theta + Hess-PD now SUFFICIENT for the assembly's
inputs?* Yes at pen grade (modulo the written-line gaps E4L0-1/
E4L0-2): theta > 0 at image points + EOS C^2 gives the local chart
and Legendre step; the m-block sign is exactly theta's sign; no
FOURTH theta consumption found (searched the assembly and the grant
text for hidden inversions — the boost map is EOS-free, the Bregman
integral consumes only Hess_W E along segments).
(c) *Over-completion check* (does the added clause exclude legitimate
EOS?): no — at the standing closure theta = T > 0 is free, and any
Bethe-Weyl-class EOS (theta > 0 on its domain) satisfies it wherever
the chart exists. The clause prices exactly the pathological class it
must.

### 5.6 The written assembly's computational content — CONFIRMED

Quote (lines 2175-2193): "... at a rest state the W-Hessian of
-rho S is BLOCK-DIAGONAL — the (rho, E) block is tau B^T Hess(-s) B
..., the momentum block is (1/(rho theta)) I > 0 by the hull-theta
clause (d^2(-rho s)/dm_i dm_j = (s_eps/rho) delta_ij =
delta_ij/(rho theta) at m = 0), and the m-(rho, E) cross terms VANISH
at m = 0 (each carries a factor m_i); boost affinity transports
positive-definiteness to all velocities by congruence; continuity of
Hess_W E (EOS C^2 — a §1-implicit consumption, now stated) plus
compactness of the hull's state image give the uniform lower
eigenvalue bound lambda_min > 0, and the Bregman double integral
gives item (i) with c = lambda_min/2."

Strongest attacks tried, all failed (every displayed identity
re-derived from scratch):
(a) *The perspective congruence*: for f(rho, E) = -rho s(1/rho,
E/rho), direct computation gives Hess f = tau [[-(tau^2 s_vv +
2 eps tau s_veps + eps^2 s_epseps), tau s_veps + eps s_epseps],
[sym, -s_epseps]], and B^T Hess(-s) B with B = [[-tau, 0], [-eps, 1]]
reproduces it entry-by-entry; det B = -tau != 0. Exact.
(b) *The momentum block and cross terms*: dE/dm_i = (s_eps/rho) m_i
(from d eps/dm_i = -m_i/rho^2), so d^2 E/dm_i dm_j|_{m=0} =
(s_eps/rho) delta_ij = delta_ij/(rho theta), and every m-(rho, E)
second derivative retains the factor m_i, vanishing at m = 0. Exact.
(c) *Boost transport*: the Galilean boost is affine on W (rho' = rho,
m' = m + rho u0, E' = E + m.u0 + rho|u0|^2/2, u0 fixed) and -rho S is
boost-invariant ((v, eps) invariant), so Hess E at a moving state is
congruent to Hess E at its rest companion via a constant invertible
matrix; the rest companion shares the state's (v, S), so the
condition read on the (boost-invariant) image covers it. Exact.
(d) *Compactness of the hull's state image* (strongest attack: an
open realized-pair set would make the hull non-closed and lambda_min
a possibly-vanishing infimum): fails against the of-record reading —
the row's hull is the segment hull over the CERTIFIED BOX (cf. line
2080 "the segment hull of the certified box"; H5' pairs range in the
fixed compact K), i.e. the image of K x K x [0, 1] under
((a, b), t) |-> (1 - t) F(a) + t F(b) with F the (continuous away
from vacuum) U -> W map — a continuous image of a compact set, hence
compact. Under that reading the printed compactness is automatic; no
non-compact reading of record survives.
(e) *The Bregman constant*: E(V|U) = Int_0^1 (1 - s) <Hess_W
E(W_s) DeltaW, DeltaW> ds >= lambda_min |DeltaW|^2 Int_0^1 (1 - s) ds
= (lambda_min/2)|DeltaW|^2 — c = lambda_min/2 is exact, and the
Hessian is consumed exactly on the segment, which lies in the hull by
definition. Exact.
(f) *"The necessity-direction chains (m1)-(m3) are untouched (they
only ever consume the slice)"*: re-checked — each kill needs only a
null/negative direction with zero m-component at a rest state, whose
Hessian quadratic form reads the slice block alone. Checks.

### 5.7 The quadruple pin + general-member clause — CONFIRMED
### (modulo E4L0-4 site (b))

Quote (lines 2164-2175): "item (i) is granted FOR THE CERTIFIED
QUADRUPLE g(S) = S, E = -rho S — the member §4 actually consumes
(H7', 'Take the certified entropy quadruple g(S) = S'; the kill
chains (m1)-(m3) also instantiate it). For a general Harten member
the g-condition enters ADDITIONALLY (the kept ideal-gas iff is its
instance; its abstract-EOS form is unstated and NOT needed — one
convex member suffices for the device); ..."

Strongest attacks tried, all failed:
(a) *H7' cross-reference*: the §4 proof pins g = S of record (the
H7' quote is verbatim); (m1)/(m2)/(m3) all run on -rho S — checks.
(b) *"one convex member suffices for the device"*: the Dafermos
device needs ONE entropy with the sandwich, and E = -rho S is that
candidate — the general-member clause honestly declines to certify
the rest of the family instead of quantifying over it (the E3L0-1/
ER2L1-1 defect, now closed). Checks.
(c) *"and at no strongly-convex g"*: verified as a true side claim
(5.4 attack record) — for every strongly convex g an in-class ideal
gas with large c_v defeats the kept iff while satisfying the strict
condition everywhere. Checks.

### 5.8 The AUD-cp revision-7 clause — CONFIRMED

Quote (lines 2249-2256): "and the revision-7 hull-theta clause and
quadruple pin are FREE there: theta = T > 0 wherever the closure is
DEFINED (T is the table coordinate; hull evaluability inside the
tabulated range is the table-domain precondition already priced once
for all consumers in this row), and g = S is the certified quadruple
of record — the condition prices the ABSTRACT-EOS accounting only."

Strongest attacks tried, all failed: (a) push all four round-4
findings through the closure — E4L0-1's bridge is the diagonal
separable-Hessian computation there (trivial); E4L0-2's roots are
unique (theta = T > 0 globally on the table domain — monotone
inversion); E4L0-3's else branch coincides with the whole-domain
branch (pins hold on the whole closure domain); E4L0-4's attributions
are harmless (theta > 0 automatic). The scoping sentence "prices the
ABSTRACT-EOS accounting only" survives ALL FOUR findings — which is
exactly why none is BREAKS. (b) table-range coverage: priced once for
all consumers, unchanged, correct.

### 5.9 Propagation carriers — CONFIRMED (all four verified on disk)

Examined verbatim: header audit line (lines 88-98: "... and
DOMAIN-COMPLETED — theta > 0 on the hull added to the condition,
granted half pinned to the certified quadruple g(S) = S — per
revision 7, E3L0-1/E3L0-2/ER2L1-1"); honest status line (lines
2338-2354: "theta > 0 AND STRICT joint s-concavity ... granted half
pinned to the certified quadruple g(S) = S ... discharged IN FULL,
strict form and hull-theta included ... separable s and theta = T > 0
there" — the appended "e_SS > 0 = c_v > 0 its necessary scalar
shadow" is exact GIVEN theta > 0, present in the same clause); §9
gamma table (lines 2546-2569: "DOMAIN-COMPLETED — hull-theta clause
added, grant pinned to the certified quadruple g = S — in revision 7
per E3L0-1/E3L0-2/ER2L1-1 ... theta = T > 0 free there); table
conclusions UNCHANGED"); §13 revision-6 entries SUPERSEDED-IN-PART
note (lines 3365-3386 — transcribes all three round-3 findings
faithfully, prior text preserved per the document's supersession
convention). Strongest attack tried: hunt a stale carrier of the
revision-6 theta-free condition or an unpinned-grant sentence — grep
over "STRICT", "hull", "quadruple", "pinned", "unconditional": every
surviving theta-free form sits inside preserved superseded §13 text
or necessity-direction context; NO stale carrier of record found. All
four carriers inherit E4L0-1..4 through the condition text they
transcribe — carried at the source clauses once, not counted per
carrier.

==============================================================================
## LEG 3 (E-1) — ZERO revision-7 edits; dry status TESTED

REVISION 7 names no leg-3 passage, and the on-disk leg-3 text is
verbatim the revision-5 state adjudicated dry at both lenses: the
three-step periodization block (lines 1186-1224) and the (EU-x)
rider's two written consumptions (lines 1226-1248) MATCH the round-2
l1 quotes character-for-character (compared this pass); the §13 E-1
falsifier bracket is untouched. Dedup: EL0-1==ESC1-F1, EL0-2 are
dispositioned with dispositions verified at both lenses; the round-3
l0 well-definedness attack and the l1 satisfiability/mollification/
term-inventory/sign/a-fortiori attacks are on file; none re-raised.

Fresh attack tried on the dry verdict (owed under the brief's "legs 3
and 5" scope): *the (p2) unfolding interchange* — "Int_R h chi_0 dt =
Int_0^T h (Sum_k chi_k) dt = Int_0^T h dt" requires exchanging the
sum over translates with the integral: Int_R h chi_0 = Sum_k
Int_0^T h(s + kT) chi_0(s + kT) ds (h T-periodic pulls back), and
Sum_k chi_0(s + kT) = Sum_k chi_{-k}(s) = 1; the interchange is
licensed because chi_0 has compact support (the k-sum is locally
FINITE — no dominated-convergence subtlety, no conditional
convergence), and h in L^1_loc suffices exactly as printed. The field
terms fed to (p2) (quadruple composed with V; (d_t D_W E(U)) psi) are
L^inf on the slab, hence in L^1_loc with room; D^2_W E is continuous
at the K-values U takes because §1's pins hold on the state region of
interest, whose referent CONTAINS K (H5': the solutions' values ARE
the states of interest — the hull-escape mechanism of E3L0-2 has no
purchase at K itself). Attack failed at every joint. **LEG 3 remains
DRY at lens l0 as of revision 7.**

==============================================================================
## THE REVISION-7 LOG BLOCK ITSELF (bookkeeping) — CONFIRMED

Examined (lines 3447-3670) against the two input files: the consumed
IDs, classes, and counts ("3 distinct defects across 2 files (2
REPAIR-NEEDED + 1 AMENDMENT, 0 BREAKS-THE-LEG)") MATCH
esc_doc1_r3_l0.md + esc_doc1_r2_l1.md exactly; the ROUND-NUMBER NOTE
correctly declares the stale brief (fourth occurrence — this document
is the fifth); the MERGED-AT-THE-CLAUSE recording of ER2L1-1 with
E3L0-1 ("consumed once, recorded at both IDs") matches both files'
own dedup declarations; the two-lens coverage restoration claim
matches the disk state (esc_doc1_r2_l1.md exists, closing revision
6's declared asymmetry); the discharge-verdict recital (E2L0-1
discharged at both lenses with residuals exactly E3L0-1/ER2L1-1;
E2L0-2 discharged with residual exactly the parenthetical inside
E3L0-2; r2-batch and round-1 dispositions re-verified; "no orphan
objection remains in any input file on disk") matches §D of the l1
file and the discharge section of the l0 file; the leg-3 dry-at-both-
lenses statement matches; "NO label motion" is consistent with
VERDICT_r2pass §4(a) ([T-T0P] main already SCHEMA); the strengthening
arithmetic ("FOURTH time in a row: L1-2 -> EL0-3/ESC1-F2 -> E2L0-1 ->
E3L0-1+E3L0-2") is right; honest residue (1) correctly declares the
revision-7 repairs UNADJUDICATED (this document is that
adjudication), (2) correctly names the spec-seeding pattern and
mandates exactly the attack posture this round executed (and which
found four further spec-seeded defects — the pattern is now four
consecutive rounds), (3) keeps the Blocco-2 gate CLOSED, (4) states
the open mathematics unchanged. Strongest attack tried: hunt a
mis-transcribed verdict, an over-claim of dryness, or premature gate
motion — none found. One loose gloss noted, below finding grade: the
ER2L1-1 entry's witness recital "lambda > 1/c_v (or >= for the
degenerate (m3)-style break)" means "or = 1/c_v" (the boundary case);
">=" includes the non-degenerate range — referentially unambiguous in
context, no action owed.

==============================================================================
## DISCHARGE VERDICTS (round-3-window sustained findings vs the
## revision-7 repairs)

- **E3L0-1 (REPAIR-NEEDED): DISCHARGED AS SPECIFIED, with two named
  residuals in the delivered text** — both demanded elements landed:
  the grant and the sufficiency are PINNED to the certified quadruple
  g(S) = S with the general-member clause verbatim-plus (5.7); the
  assembly is WRITTEN with every element the spec named, each
  verified exact at pen grade (5.6), the necessity chains untouched.
  Residuals: the delivered assembly omits its first link — the strict
  Legendre bridge the spec's element list never named (E4L0-1) — and
  the delivered why-clause mis-attributes c_v > 0 to the strict pair
  (E4L0-4 site (b), transplanted from the companion spec). The
  disposition fails at "The assembly is now WRITTEN" (true minus one
  line) and nowhere else.
- **ER2L1-1 (AMENDMENT): DISCHARGED, with the spec's own clause
  carrying a residual** — the demanded scoping clause landed nearly
  verbatim and the quadruple pin is of record everywhere the grant
  speaks; but the transplanted "which the strict pair already
  contains" silently generalized an ideal-gas fact to the abstract-
  EOS attribution (E4L0-4 site (b)). The disposition fails at exactly
  that clause and nowhere else.
- **E3L0-2 (REPAIR-NEEDED): DISCHARGED AS SPECIFIED, with two
  spec-seeded residuals** — the condition is DOMAIN-COMPLETED exactly
  as demanded, the three consumptions itemized faithfully (5.5), the
  falsifier parenthetical re-calibrated with the PROVIDED/else split,
  the grant annotated, all four carriers propagated (5.9). Residuals:
  the spec's own "(the (v, S)-image of)" parenthesis left the root
  quantifier open (E4L0-2), and the spec left the falsifier's
  consequent "restores the unconditional grant" unrescoped against
  the now-conjunctive condition (E4L0-3). The disposition fails at
  exactly those two transplanted clauses and nowhere else.

The round is NOT dry: 4 findings sustained-by-this-refuter
(0 REPAIR-NEEDED + 4 AMENDMENT, 0 BREAKS-THE-LEG).

==============================================================================
## SUMMARY

| ID | Leg | Passage | Class | One-line |
|---|---|---|---|---|
| E4L0-1 | 5 | assembly opening (l.2175-2181) vs condition (l.2143-2144) | AMENDMENT | the written assembly consumes "strict s-concavity" from a condition stated on Hess e(v, S) with the strict, rank-faithful Legendre bridge (Hess s = -(1/theta) C^T Hess e C, C invertible) unwritten — the delivered chain is the l1 record's steps (2)-(4) minus step (1), against the document's own "the line must be WRITTEN" standard |
| E4L0-2 | 5 | "(the (v, S)-image of)" (l.2143-2144) | AMENDMENT | the S-root quantifier is open: theta > 0 AT a selected image point gives only local inversion; per-branch reading admits an in-class landing mechanism (continuation misses the data root), universal-over-roots reading forces uniqueness by level-crossing parity and closes everything — the universal reading and the parity line must be written |
| E4L0-3 | 5 | falsifier consequent (l.2268-2283) | AMENDMENT | "restores the unconditional grant" over-claims in the else branch: a pins-on-the-hull derivation ASSUMES the hull-theta member, so firing kills the Hessian member only — residual false-restoration power on the member the row's own annotation calls "a genuine addition" |
| E4L0-4 | 5 | two glosses (l.2145-2147, l.2172-2175) | AMENDMENT | "(= c^2 > 0, already a §1 pin)" — §1's pin does not reach the hull (the row's own point, two lines above); "c_v > 0, which the strict pair already contains" — c_v = theta/e_SS: the sign consumes the hull-theta member, not the strict pair (true only at the ideal-gas instance) |

CONFIRMED (cannot break, strongest attacks recorded in-section): the
DOMAIN-COMPLETED condition + itemized consumptions (5.5 — fidelity,
sufficiency-of-inputs, over-completion all tested); the written
assembly's computational content (5.6 — congruence entry-by-entry,
momentum block, cross terms, boost transport, hull compactness under
the of-record reading, exact Bregman constant, slice-only necessity
chains); the quadruple pin + general-member clause incl. "at no
strongly-convex g" (5.7); the AUD-cp revision-7 clause with all four
findings pushed through and absorbed (5.8); all four propagation
carriers (5.9 — zero stale theta-free or unpinned-grant form of
record); leg-3 dry status re-tested with a fresh unfolding-
interchange attack (failed — leg 3 stays DRY at lens l0); the
revision-7 log block's bookkeeping (IDs, classes, counts, merge
recording, discharge recital, declared round-number staleness, honest
residue).

Counts: 4 findings total = 0 REPAIR-NEEDED + 4 AMENDMENT;
**BREAKS-THE-LEG: 0**. No statement, rigor label, or route-level
conclusion is touched by any finding: all four are one-line/one-
clause written-form and attribution defects inside delivered repair
text whose mathematics is verified sound; all four discharge
automatically at the standing gamma(T) closure (5.8), so the
abstract-EOS-only scoping of the row is confirmed yet again, and the
OPEN verdict, the two-piece residue, and the no-viable-abstract-EOS-
route conclusion all stand. ALL FOUR findings are spec-seeded
(fourth consecutive round of the pattern the revision-7 honest
residue itself names) — the next revision should again restate each
repair clause against the row's own hypotheses rather than
transplanting this document's repair wordings.

## SELF-FALSIFIERS (what kills each finding)

- E4L0-1: a printed line in the ROW (not in a refutation file or the
  §13 log) stating the strict Legendre equivalence or its congruence
  (grep for "C^T", "rank-faithful", "-(1/theta)": no occurrence in
  the row's body text); or a demonstration that the assembly's
  "strict s-concavity" is itself the condition of record's stated
  form (it is not: lines 2143-2144 state Hess e(v, S) PD; the honest
  status line's apposition asserts, not proves, the identification).
- E4L0-2: a printed line pinning the S-root (uniqueness clause,
  branch-continuation rule for S, or the universal-over-roots
  quantifier — none found: the row's branch-continuation rule at
  lines 2380-2390 governs M-space supersonic preimages, a different
  object); or a proof that theta > 0 at ONE root forces the root to
  be unique for every §1-class EOS (false: the increasing-decreasing-
  increasing h above is a counterexample family); or a proof that the
  §1 pin's domain covers all intermediate S-values at hull
  v-coordinates (that inclusion is exactly what E3L0-2 established
  the record does not contain).
- E4L0-3: a reading under which "the condition-claim" in the
  falsifier's consequent denotes only the Hessian member (none of
  record: "CONDITION OF RECORD" is defined at lines 2142-2150 as the
  conjunction); or a demonstration that the else branch's derivation
  can conclude theta > 0 on the hull from inputs that do not include
  it (impossible: theta is first-order EOS data, and Gibbs + c^2 > 0
  constrain it nowhere off the pin region — the row's own argument).
- E4L0-4: (a) a printed §1 line pinning c^2 > 0 on a region
  containing the hull (none: line 197 names "the state region of
  interest", and lines 2138-2139 deny the inclusion for the identical
  domain); (b) a proof that the strict pair alone forces c_v > 0 at
  abstract EOS (false: c_v = theta/e_SS and the strict pair does not
  constrain theta's sign — the row's own "theta is FIRST-order EOS
  data no Hessian condition controls").
- Any CONFIRMED verdict above: a derivation-level break of the quoted
  passage that my recorded strongest attack missed.

END — until-dry round-4 escalation refutation, lens l0. Machine
summary: {objections: 4, breaks: 0,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc1_r4_l0.md}
