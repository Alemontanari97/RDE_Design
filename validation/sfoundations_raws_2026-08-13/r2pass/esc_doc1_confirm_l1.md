# ESCALATION REFUTATION — DOC-1, CONFIRMING ROUND (post-VERDICT_escalation), lens l1
# (gas dynamics / physics, RH algebra, counterexamples)

Date: 2026-08-19. Target: `phaseD/phaseD_stop_proof.md` AT REVISION 8 —
ONLY the revision-8 delta, per the launching brief and VERDICT_escalation
§2 DOC-1 + §6 R-1: the consumption of ER3L1-1..3 + E4L0-1..4, i.e. (in
the G8/r2 accounting row of leg 5) the CHART-COMPLETED condition of
record (totality zeroth clause + universal root-quantifier clause +
parity line, lines 2150-2176), the E4L0-4(a) gloss rewording (lines
2177-2182), the ER3L1-1/E4L0-4(b) attribution correction with the
member-wise split and patch witness (lines 2209-2219), the strict
Legendre bridge as the written assembly's FIRST LINK (lines 2222-2234)
and its connective (lines 2238-2239), the AUD-cp chart-clauses addition
(lines 2315-2320), the member-wise re-scoped falsifier consequent
(lines 2344-2361), the honest-status-line additions (lines 2424-2426,
2431-2432, 2435), the header audit-line addition (lines 102-105), the
§9 gamma-table addition (lines 2651-2653), the §13 revision-7 echo's
ATTRIBUTION SUPERSEDED-IN-PART annotation (lines 3578-3582), and the
§13 REVISION 8 block (lines 3768-4041). Line numbers throughout are of
the revision-8 file.

ROUND/LENS PLACEMENT, DECLARED: VERDICT_escalation §2 records revision
8 as carrying ZERO adversarial coverage and §6 R-1 mandates "one
two-lens confirming round owed on the rev-8 delta". This document is
the lens-l1 half of that round. Output file `esc_doc1_confirm_l1.md` —
an unclaimed slot; no artifact is overwritten. Under the loop's own
scope convention of record (esc_doc1_r3_l1.md header: attacks lie on
newest-revision text OR on an older clause "whose truth conditions
[the newest revision] CHANGED under it"), passages unedited by
revision 8 but whose truth conditions the revision-8 condition
enlargement changed are IN scope; the leg-3 body (zero revision-8
edits, dry at both lenses per VERDICT_escalation §2/§3, "no further
work on the leg-3 text itself is owed") is OUT of scope except for the
§13 rev-8 block's leg-3 recital, which is revision-8 text and is
verified below as transcription.

Specs consumed: `r2pass/VERDICT_escalation.md` §2 DOC-1, §3 LEG 3/LEG
5, §5(i)-(ii), §6 R-1 + R-4 (the BINDING rule for this round);
`r2pass/VERDICT_r2pass.md` §2.1 rows and §4(c) E-1/E-2 (standing
specs); FULL texts of the two consumed refutations
`r2pass/esc_doc1_r3_l1.md` (ER3L1-1..3) and `r2pass/esc_doc1_r4_l0.md`
(E4L0-1..4); prior rounds `esc_doc1_r1_l0/l1.md`, `esc_doc1_r2_l0/
l1.md`, `esc_doc1_r3_l0.md` and the r2-batch files (dedup layer). Both
duties executed: (a) derivation-level attack on the revision-8 repair
text, attacked AS repair-spec-derived text per the standing lesson;
(b) verification that the revision-8 repairs DISCHARGE the seven
findings they answer. Mechanical checks executed this pass (sympy/
numeric, run of record in this window): the Legendre-bridge congruence
residual Hess_{(v,eps)} s + (1/theta) C^T Hess_{(v,S)} e C == 0
symbolically on a generic quadratic-family EOS (zero matrix); the
quadratic-patch strict-pair regimes ((m2)'s printed regime a1^2 >
e0''b FAILS the strict pair; the witness regime e0''b > a1^2 HOLDS it
with theta < 0 on the patch, theta_max = -96 at the test parameters);
the parity line (h = S^3 - 3S at level 0: outer roots +/-sqrt(3) both
carry h' = 6 > 0, intermediate root 0 carries h' = -3 <= 0).

DEDUP REGISTER (dispositioned objections NOT re-raised): L0-1/L1-1,
L0-2, L1-2 (r2-batch); EL0-1==ESC1-F1, EL0-2, EL0-3==ESC1-F2, EL0-4
(round 1); E2L0-1, E2L0-2 (round 2); E3L0-1, ER2L1-1, E3L0-2 (round-3
window); ER3L1-1..3 + E4L0-1..4 (round-3-l1/round-4-l0 window,
consumed at revision 8 — discharge adequacy examined below; where a
disposition partially fails, the ID is cited and the failure point
stated, per the brief's dedup rule). All findings below attack
REVISION-8 text, or a clause whose truth conditions revision 8 changed
under it — zero prior adversarial coverage — and each declares its
distinction from every ID above. Every attacked passage: verbatim
quote + attack + class in {BREAKS-THE-LEG, REPAIR-NEEDED, AMENDMENT}.
Every examined delta passage not broken: CONFIRMED with the strongest
attack tried.

BINDING R-4 RULE CHECK (VERDICT_escalation §6 R-4, six-plus prior
instances): each revision-8 repair clause was checked for restatement
against the row's own hypotheses vs verbatim transplant. Results,
clause by clause: (1) Legendre bridge — RESTATED-PLUS (the spec's
wording carried, WITH the row-specific C-matrix and quadratic-form
display added; verified to FIT the row's hypotheses theta > 0 + EOS
C^2 — congruence re-derived symbolically, zero residual): PASS. (2)
root-quantifier clause — RESTATED as a condition member in the row's
own objects (eps(W) = e(v, S) over hull points), parity mechanism
written and verified: PASS. (3) totality clause — RESTATED with the
explicit range form (stronger than the spec's parenthetical; the
empty-range reading correctly handles v-domain exit — attack recorded
in §C.1): PASS. (4) E4L0-4(a) gloss — RESTATED, internal consistency
restored: PASS. (5) attribution correction (b) — RESTATED member-wise,
BUT the in-class witness was restated with its load-bearing
determinant regime DROPPED and a class label that misfits the (m2)
regime of record: FAIL → finding ECR1-2. (6) falsifier consequent —
the l1 spec's honest parenthetical "modulo ER3L1-2's totality" was
transplanted AND MUTATED into the affirmative claim "which the
whole-domain reading also delivers", false against the row's own
in-class witnesses, and the else-branch remainder was calibrated
against the revision-7 TWO-member condition while revision 8 itself
made the condition three-membered: FAIL → finding ECR1-1. The R-4
pattern therefore RECURS at revision 8 (two of six clauses), including
one instance of the new sub-type "transplanted-then-mutated".

Finding IDs: ECR1-1..ECR1-4 (Escalation Confirming Round, doc 1).

==============================================================================
## §A SUSTAINED FINDINGS

### A.1 The re-scoped falsifier consequent's totality mis-claim —
### **OBJECTION ECR1-1, AMENDMENT** (three sites, one defect)

Quote 1 (body, lines 2343-2350): "THAT kills the condition-claim and
restores the unconditional grant IN THE WHOLE-DOMAIN BRANCH, where
the theta pin itself goes hull-wide (modulo the totality clause,
which the whole-domain reading also delivers); in the ELSE branch the
derivation has CONSUMED theta > 0 on the hull as an input, so a
firing there kills the HESSIAN member ONLY and leaves the grant
conditional on hull-theta".

Quote 2 (§13 rev-8 block, lines 3924-3928): "(consequent RE-SCOPED
member-wise: full restoration IN THE WHOLE-DOMAIN BRANCH only, where
the theta pin itself goes hull-wide (modulo totality, which that
reading also delivers)".

Attack (derivation level — the relative clause "which the
whole-domain reading also delivers" is FALSE, and the else-branch
remainder under-counts the members). The whole-domain branch of the
falsifier reads the §1 pins (Gibbs closure + c^2 > 0 + theta > 0) "on
the whole EOS domain" and — per the revision-7 recalibration's own
operative sentence, "a pins-only derivation concludes only where the
pins hold" — pins the conclusion domain to dom(EOS). The totality
clause (the condition's revision-8 ZEROTH member) is a statement
about the HULL versus the chart's range: eps(W_s) in the range of
e(v(W_s), .) at every hull point. Pins holding on the whole EOS
domain constrain NOTHING about hull points lying inside that domain's
chart image. In-class witness — the row's OWN accepted construction
class: the quadratic patch e = e0(v) + (a0 + a1 v)S + (b/2)S^2 on a
compact patch with a0 large POSITIVE (theta > 0 on the WHOLE patch
domain), e0'' > 0, e0''b > a1^2 (Hess e PD on the whole domain).
This EOS satisfies the whole-domain proviso's hypothesis — every §1
pin holds on its entire domain, and strict joint s-concavity holds
wherever s is defined (verified via the bridge congruence) — so a
pins-only derivation concluding on dom(EOS) FIRES the falsifier in
the whole-domain branch as printed. Yet with endpoints in K carrying
a thin S-band, mid-segment hull states have eps_s OUTSIDE
[e(v_s, S_-), e(v_s, S_+)] (ER3L1-2's consumed mechanism, untouched):
S(W_s) is UNDEFINED, the Bregman integral of item (i) traverses
undefined territory, and the grant is NOT restored — the totality
member was neither derived nor deliverable from the pins. The printed
consequent nevertheless declares totality DELIVERED by the reading:
residual false-restoration power on the totality member, the exact
defect mechanism E4L0-3/ER3L1-3 were sustained for on the hull-theta
member one round ago, recurring one member up because the member-wise
re-scope was calibrated against the revision-7 TWO-member condition
while REVISION 8 ITSELF made the condition three-membered (the §13
entry even records the awareness: "and revision 8 chart-completed
it", line 3922 — then hands the third member to a reading that does
not deliver it). Second site, same defect: the ELSE branch's
remainder "leaves the grant conditional on hull-theta" under-counts —
a derivation concluding strict s-concavity ON THE HULL presupposes s
DEFINED on the hull (the f(A)-presupposition convention this row
itself adopted at ER3L1-2), so an else-branch firing consumed BOTH
hull-theta AND totality as inputs and leaves the grant conditional on
BOTH; as printed, an else-branch firing would of-record erase the
totality member it silently presupposed — precisely what the row's
E4L0-3 repair forbade for hull-theta ("a member the row insists is a
genuine addition cannot be erased by a derivation that consumed it as
a hypothesis").

Distinction from the transplant source, for the record: the l1
round-3 text (esc_doc1_r3_l1.md §5.4) wrote "where the theta pin
itself goes hull-wide, modulo ER3L1-2's totality" — leaving totality
OPEN. Revision 8 transplanted the parenthetical and mutated "modulo
X" (X remains a condition) into "modulo X, which [the reading] also
delivers" (X claimed discharged). The mutation is the defect; no
refutation file on disk asserts the delivery.

Class: **AMENDMENT** — auxiliary falsifier text, the exact lineage
class (r2b-F4 / EL0-1 / E2L0-2 / E4L0-3 / ER3L1-3, all sustained
AMENDMENT); no statement, rigor label, or route-level conclusion
moves (the grant head conditions on the full condition of record; the
totality clause retains its own §13 falsifier). A benign reading was
hunted and does NOT survive the branch's own domain semantics: the
presupposition rescue ("concluding on the hull presupposes the chart
there") attaches to the ELSE branch and to the primary on-the-hull
demand, not to the whole-domain branch, whose conclusion domain the
revision-7 parenthetical explicitly re-pinned to "wherever the pins
hold". Repair, one clause at both body sites + the §13 echo: "...
restores the grant's HESSIAN AND HULL-THETA members IN THE
WHOLE-DOMAIN BRANCH (the theta pin goes hull-wide there MODULO the
totality clause, which no pins-only derivation delivers — totality
stays a standing member with its own falsifier); in the ELSE branch a
firing kills the HESSIAN member ONLY, leaving the grant conditional
on hull-theta AND the chart clauses, both consumed or presupposed as
inputs".

DEDUP DECLARATION: not a re-raise of E4L0-3/ER3L1-3 — those priced
the consequent against the revision-7 two-member condition and their
demanded member-wise split IS delivered; their disposition fails only
at the new parenthetical's false relative clause and the remainder's
member count, text that did not exist before revision 8 (IDs cited
per the brief's dedup rule). Not a re-raise of ER3L1-2 (totality as a
missing condition member — delivered as the zeroth clause, §C.1
CONFIRMED) nor of E2L0-2/E3L0-2 (restoration/condition domains —
dispositions intact).

### A.2 The attribution repair's witness restated without its
### determinant regime — **OBJECTION ECR1-2, AMENDMENT** (two sites)

Quote 1 (body, lines 2212-2216): "the strict pair ALONE does not
contain it (theta is first-order data the pair does not constrain:
the (m2)-class quadratic patch with a0 large NEGATIVE satisfies the
strict pair everywhere with c_v = theta/e_SS < 0".

Quote 2 (§13 rev-8 block, lines 3874-3877): "ER3L1-1 supplies the
in-class witness (the (m2)-class quadratic patch, a0 large negative:
strict pair everywhere, c_v < 0)".

Attack (derivation level — the witness as printed is not verifiable
and is FALSE under the most proximate instantiation). For the
quadratic-patch family e = e0(v) + (a0 + a1 v)S + (b/2)S^2, the
strict pair (e_vv > 0 AND e_vv e_SS > (e_vS)^2) reads e0'' > 0 AND
e0''b > a1^2 — a PARAMETER condition. The (m2) exhibit of record
(lines 2265-2273) is printed with the OPPOSITE regime, "a1^2 >
e0'' b" (its whole point is a violated determinant bound), and with
a0 large POSITIVE (it holds theta > 0 among its pins). A reader who
instantiates "the (m2)-class quadratic patch" at (m2)'s printed
parameters gets a patch on which the strict pair FAILS (mechanically
checked this pass: e0'' = 2, a1 = 3, b = 1 gives e0''b = 2 < 9 =
a1^2) — the sentence "satisfies the strict pair everywhere" is false
there. The witness ER3L1-1 actually supplied carries the regime
IN ITS TEXT ("e0'' b > a1^2 strict", esc_doc1_r3_l1.md §5.2) and is
valid (checked: e0'' = 2, a1 = 1, b = 2, a0 = -100: strict pair holds,
theta <= -96 on the patch, c_v = theta/e_SS < 0, c^2 = v^2 e0'' > 0
independent of a0, Gibbs closure by construction). The revision-8
restatement DROPPED the load-bearing parameter clause and attached a
class label whose printed regime contradicts the claim — against this
document's own repeatedly-enforced standard ("the line must be
WRITTEN") and against the R-4 mandate this very revision claims to
have executed ("restated each repair against the row's own
hypotheses", §13 residue (2)): the restatement lost one of the
witness's own hypotheses. The benign reading — "(m2)-class" = "the
quadratic-patch construction class of (m2)/(m3)", the loose usage the
row itself employs at line 2158 — leaves the claim UNVERIFIABLE as
printed rather than false, since the verifying parameter condition
appears nowhere in the row.

Class: **AMENDMENT** — the witness family exists and the corrected
attribution it supports is true and verified (the member-wise split
itself is CONFIRMED, §C.3); one clause repairs both sites: "the
quadratic-patch family of (m2) in the OPPOSITE determinant regime —
e0'' > 0, e0''b > a1^2 (strict pair holds on the patch) — with a0
large NEGATIVE (theta < 0 there)". No statement, label, or
route-level conclusion moves.

DEDUP DECLARATION: not a re-raise of ER3L1-1/E4L0-4(b) — the
attribution those priced is CORRECTED of record; their disposition
fails only at the witness restatement's dropped regime, revision-8
text (IDs cited per the brief's dedup rule). Not a re-raise of E3L0-2
(whose patch witness is a different regime serving a different
member) nor of E2L0-1/(m3) (determinant-boundary family — distinct
regime, e0''b = a1^2).

### A.3 The grant head and item-(i) annotation not propagated against
### the chart-completed condition — **OBJECTION ECR1-3, AMENDMENT**

Quote (body, lines 2365-2374, ZERO revision-8 edits, truth conditions
changed by revision 8): "Granted, FOR THE CERTIFIED QUADRUPLE
g(S) = S (revision 7 per E3L0-1/ER2L1-1; a general Harten member
additionally needs the g-condition) and under the STRICT + hull-theta
condition of record: (i) E(V|U) >= c|DeltaW|^2 with all comparison
segments inside the convex physical region ({rho >= rho_min} IS
convex in W — this half of the pricing was correct; that region
carries NO theta clause, which is why the hull-theta member of the
condition is a genuine addition, E3L0-2)".

Attack (derivation level — the operative granting sentence enumerates
two of three member groups, and the omission retains ER3L1-2's
vacuity mechanism at the grant itself). Revision 8 enlarged the
condition of record to totality (zeroth clause) + hull-theta (read
universally over roots) + the strict Hessian pair; every other
carrier was updated in the same window (header line 102-105
"CHART-COMPLETED", honest status line 2431-2432 "chart total and
single-valued there (the revision-8 clauses)", §9 table 2651-2653,
§12-echo "STRICT + hull-theta + chart-complete form" at line 4003) —
but the grant head, the one sentence that OPERATIVELY grants item
(i), kept the revision-7 epithet "STRICT + hull-theta". The exposure
is not decorative: WITHOUT the totality member, the universal
root-quantifier clause is VACUOUSLY satisfiable at a hull point with
NO S-root ("theta > 0 at every root" of an empty root set holds), so
a reader who checks exactly the members the grant head names — strict
pair + hull-theta-universal — on the partial image would claim the
grant on a totality-violating patch EOS while the Bregman integral
traverses S(W)-undefined states: ER3L1-2's consumed mechanism,
surviving in the grant head's enumeration. Same under-propagation in
the item-(i) parenthetical: the convex physical region carries no
theta clause AND no chart clause — the annotation names the first as
"why the hull-theta member is a genuine addition" and is silent on
the second, though the identical argument makes the chart clauses
genuine additions (the §13 ER3L1-2 entry says so explicitly: "the
clause is a genuine addition", line 3863). This is the E4L0-3/
ER3L1-3 defect pattern verbatim — "the condition enlarged under an
unchanged [carrier]" — one revision later, on the granting sentence.

Class: **AMENDMENT** — "condition of record" is a defined singular
object (defined at lines 2129-2187 WITH the chart clauses), so a
benign referential reading exists and no consumer that follows the
definition moves; the loop's precedent for exactly this pattern
(ER3L1-3) is AMENDMENT. Repair, two phrases: "under the STRICT +
hull-theta + CHART-COMPLETE condition of record" and "(that region
carries NO theta clause and NO chart clause — why the hull-theta
member and the revision-8 chart clauses are genuine additions,
E3L0-2/ER3L1-2)".

DEDUP DECLARATION: not a re-raise of ER3L1-2 (the totality member is
DELIVERED in the condition — this prices the grant head's stale
enumeration of the enlarged condition, a truth-conditions change
revision 8 made under an unedited sentence, the loop's own in-scope
convention); not of ER3L1-3/E4L0-3 (falsifier consequent — a
different carrier; their repair did not name the grant head); not of
E3L0-1 (grant-half quantification — the quadruple pin stands
CONFIRMED). The revision-8 carrier sweep (§13 block, "all four
propagation carriers") listed header/status/§9/§13-note — the grant
head was never in that four-carrier list at any round, so no
disposition claims it; this is new-coverage text, not a failed
disposition.

### A.4 Honest residue (2)'s instance count contradicts the on-file
### declarations — **OBJECTION ECR1-4, AMENDMENT**

Quote (§13 rev-8 block, lines 4019-4021): "SPEC-SEEDING PATTERN, now
SIX declared instances across four consecutive rounds (one finding of
this round, ER3L1-3, is the sole non-seeded item)."

Attack (bookkeeping at the record level — the count is measured
against the files and fails). Declared instances on disk: FOUR at
revision 7 (both round-window refuters quote the revision-7 residue's
"four named instances"); the round-3-l1/round-4-l0 window then
declared FOUR NEW DISTINCT seeding events — (i) E3L0-1's element list
seeding the bridge omission (E4L0-1, "spec-seeded, instance 1 of 4",
esc_doc1_r4_l0.md); (ii) E3L0-2's "(the (v, S)-image of)"
parenthetical seeding the root/totality gaps (E4L0-2 ≡ ER3L1-2 — one
event, found at both lenses; ER3L1-2 self-numbered "sixth"); (iii)
E3L0-2's spec leaving the consequent unrescoped (E4L0-3, "instance 3
of 4"); (iv) ER2L1-1's own clause seeding the c_v attribution
(E4L0-4(b) ≡ ER3L1-1 — one event, both lenses; ER3L1-1 self-numbered
"fifth"). Deduplicated cumulative total: 4 + 4 = EIGHT distinct
events (TEN seeded findings if counted per-finding: 4 prior + 6 of
this round's 7). The printed "SIX" is consistent only with adopting
the l1 file's cumulative endpoint (its "fifth"/"sixth"
self-numbering, assigned before/without r4_l0's four) while dropping
the two l0-only events (bridge, consequent) — both of which the SAME
§13 block records as "spec-seeded" in their own entries (lines
3811-3815, 3938-3940): the residue contradicts the block's own
provenance recordings three hundred lines above it. The count is
load-bearing for the R-4 standing lesson's severity (the judge's own
§6 R-4 hedges to "SIX-plus", and this round found the pattern
recurring — A.1/A.2 — so understatement matters).

Class: **AMENDMENT** — honest-residue bookkeeping, no mathematics.
Repair, one phrase: "now EIGHT declared distinct seeding events (ten
seeded findings) across four consecutive rounds" — regenerated from
the files, per the SR-12 discipline (counts cited from a measured
sweep in the same window, never inherited).

DEDUP DECLARATION: no prior objection prices this count (the residue
text is revision-8-new); not a re-raise of any round's declared
off-by-ones (those are round-NUMBER declarations, all verified
accurate).

==============================================================================
## §B EXAMINED, ATTACK NOTED, NOT SUSTAINED (below finding grade)

- §13 rev-8 header sentence "the launching brief again named the
  target 'revision 4'" (lines 3779-3781) vs the refuter headers'
  own descriptions (r4_l0: brief named the SLOT "until-dry ROUND 3"
  with output `esc_doc1_r3_l0.md`; r3_l1: round/lens placement note).
  Attack tried: mis-transcription of the declared deviation. NOT
  SUSTAINED: the launching briefs are not on disk to quote against,
  a brief can carry both a round and a revision descriptor, and the
  operative content (fifth/sixth occurrences, no artifact
  overwritten, resolution by the until-dry clause) matches the files
  exactly. Recorded as unverifiable, no action owed.

==============================================================================
## §C EXAMINED DELTA PASSAGES THAT SURVIVE — CONFIRMED, with the
## strongest attack tried

### C.1 The CHART-COMPLETED condition: totality zeroth clause +
### universal root-quantifier + parity line (lines 2150-2176) —
### CONFIRMED

Strongest attacks tried, all failed: (a) *parity-line sign
bookkeeping* (the one place a flipped inequality would poison the
uniqueness claim): with f(S) = e(v, S) - eps, f'(S1) > 0 puts e ABOVE
the level just after S1 and f'(S2) > 0 puts e BELOW it just before S2
— exactly as printed ("passes from above the level just after the
first root to below it just before the second"); IVT + first downward
crossing yields an intermediate root with e_S <= 0, in the root set
the universal clause quantifies over, so the condition FAILS there —
contrapositive exact. Mechanically instanced (h = S^3 - 3S, level 0:
outer roots -sqrt(3), +sqrt(3) both carry h' = 6 > 0; intermediate
root 0 carries h' = -3). Tangential (double) roots carry theta = 0
and are excluded by the same clause; continuity of e_S is covered by
the stated EOS C^2 consumption. (b) *totality clause vs v-domain
exit* (hunting a residual existence gap the clause misses): at a hull
point with v(W_s) outside the EOS's v-domain, e(v(W_s), .) is
undefined and its range EMPTY — "eps(W_s) lies in the range" is then
FALSE and the condition correctly FAILS; no vacuous pass survives.
(c) *data-root landing*: endpoints' data (v, S) are roots by
definition; totality + universal-clause uniqueness + IFT continuation
(theta != 0 at every root) make S(W) single-valued and continuous
along each segment, so the s = 1 landing IS V's data root — the
E4L0-2 separating mechanism is closed, not just fenced. (d) *the
forward gloss "theta licenses uniqueness of the inversion, not
surjectivity"* (line 2157): read against the clause order, "licenses
uniqueness" could over-credit a single-root theta sign — but the
universal root-quantifier clause two lines below is the referent and
is what the condition actually imposes; within the same sentence
block the gloss has no independent consumer. Attack fails.

### C.2 The strict Legendre bridge as written FIRST LINK (lines
### 2222-2234) and its connective (lines 2238-2239) — CONFIRMED

Strongest attacks tried, all failed: (a) *entry-by-entry congruence*:
re-derived independently via the implicit second-derivative identity
(differentiate e(v, s(v, eps)) = eps twice: 0 = C^T Hess e C +
theta Hess s with C = [[1, 0], [-e_v/theta, 1/theta]]), and checked
SYMBOLICALLY this pass on a generic quadratic-family EOS: residual
Hess_{(v,eps)} s + (1/theta) C^T Hess_{(v,S)} e C = the zero matrix.
The quadratic-form identity at dS = (deps - e_v dv)/theta is the same
identity read on vectors; det C = 1/theta != 0. (b) *equivalence
direction and strictness leak*: congruence by invertible C plus the
NEGATIVE scalar -(1/theta) (theta > 0) maps PD exactly onto ND,
rank-faithfully by Sylvester inertia — no leak in either direction
(re-verifies my own round-2/round-3 chain in the row's coordinates).
(c) *uniformity clause* ("uniform on compacts by continuity of C"):
on a compact where theta > 0 and continuous, theta is bounded below,
C and C^{-1} = [[1, 0], [e_v, theta]] are bounded continuous, and
eigenvalue bounds transfer with uniform constants — the compressed
clause is complete given the stated hypotheses (hull-theta + EOS
C^2). (d) *placement*: the connective "STRICT s-concavity, delivered
by the bridge above" (lines 2238-2239) closes exactly the E4L0-1 gap
— the assembly now consumes s-concavity FROM the written bridge, not
from an unwritten identification. The E4L0-1 discharge is complete.

### C.3 The attribution correction's member-wise split (lines
### 2209-2212, 2216-2219) and the E4L0-4(a) gloss rewording (lines
### 2177-2182) — CONFIRMED (modulo ECR1-2's witness clause)

Strongest attacks tried, all failed: (a) *the split's algebra*:
e_SS > 0 from the strict pair (e_vv > 0 AND e_vv e_SS > (e_vS)^2 >=
0); c_v = theta/e_SS re-derived (T = e_S; at fixed v, c_v = de/dT =
theta/e_SS); sign via hull-theta exactly as printed. (b) *the
ideal-gas caveat* ("at the ideal-gas instance theta > 0 is automatic,
which is where the superseded attribution was harmlessly true"):
exact — and honestly placed. (c) *site (a) internal consistency*:
"the §1-pinned QUANTITY, here imposed hull-wide by the condition
itself" now agrees with the domain-limitation sentence above it
(lines 2145-2146); the identity c^2 = v^2 e_vv unchanged and correct.
(d) *the honest status line's annotation* "(sign via hull-theta,
ER3L1-1)" at line 2435: exact given theta > 0 in the same
parenthetical chain. The ER3L1-1/E4L0-4 discharge is complete except
at the witness restatement (ECR1-2).

### C.4 The AUD-cp chart-clauses addition (lines 2315-2320) —
### CONFIRMED

Strongest attacks tried, all failed: (a) *closure uniqueness*:
theta = T > 0 globally on the table domain makes e(v, .) strictly
increasing in S — at most one root, monotone inversion, exactly as
printed. (b) *closure totality*: hull evaluability inside the
tabulated range IS the existence statement — "the SAME precondition
there" is the correct identification, already priced once for all
consumers. (c) *push ECR1-1 through the closure* (the strongest): at
the closure the totality member is priced SEPARATELY (table-domain
precondition), so the closure discharge does NOT lean on the false
whole-domain-delivers claim — which is exactly why ECR1-1 is
abstract-EOS-side only and AMENDMENT-grade. Attack fails.

### C.5 The four updated carriers (header 102-105; honest status
### 2424-2440; §9 table 2651-2658; §13 rev-7 echo annotation
### 3578-3582) — CONFIRMED

Strongest attacks tried, all failed: verbatim reads of all four; the
header's "CHART-COMPLETED — totality clause (S(W) defined hull-wide)
+ universal root-quantifier added ..., strict Legendre bridge and
member-wise falsifier consequent written" is an accurate one-line
inventory of the revision-8 delta; the status line names the chart
clauses at the condition ("chart total and single-valued there (the
revision-8 clauses)"); the §9 table's "CHART-COMPLETED — totality +
root-uniqueness clauses added, strict Legendre bridge written" is
accurate with table conclusions unchanged; the §13 revision-7 echo's
ATTRIBUTION SUPERSEDED-IN-PART annotation preserves prior text per
the document's supersession convention and restates the corrected
attribution faithfully. Stale-form hunt (grep over "strict", "hull",
"quadruple", "chart", "unconditional", "totality"): every surviving
pre-revision-8 form sits inside preserved superseded §13 text under a
supersession note, in necessity-direction context, or at the TWO
sites already priced as ECR1-1/ECR1-3 — no FURTHER stale carrier
found.

### C.6 The §13 REVISION 8 block's bookkeeping (lines 3768-4041) —
### CONFIRMED (modulo ECR1-4)

Strongest attacks tried, all failed: the consumed IDs, classes, and
counts ("seven findings, ALL leg 5, ALL class AMENDMENT, 0
REPAIR-NEEDED, 0 BREAKS") match esc_doc1_r3_l1.md +
esc_doc1_r4_l0.md exactly; the two MERGED-AT-THE-CLAUSE recordings
(E4L0-4(b)+ER3L1-1; E4L0-3+ER3L1-3) match both files' own dedup and
mechanism texts, including ER3L1-1's self-catch quote; every FIXED
entry's mechanism transcription is faithful to its source finding
(checked entry against file for all five entries); the discharge-
verdicts recital matches §D of the l1 file and the discharge section
of the l0 file, including the residual-placement sentences; the
leg-3 status recital (lines 3968-3981) transcribes both files' fresh
failed attacks correctly (l0: unfolding interchange; l1:
direction-of-minting/seam, representation independence,
partition-sign, rider integrability) and the round-count claim ("l0
rounds 1-4 and l1 rounds 1-3") matches the disk; the LABEL SUMMARY
(no label motion; [T-T0P] main SCHEMA; two-piece r2 residue) is
consistent with VERDICT_r2pass §4(a) and VERDICT_escalation §3; the
Blocco-2 gate sentence correctly defers to E-5/E-6. The residue-(2)
instance count is the one sustained defect (ECR1-4).

==============================================================================
## §D DISCHARGE VERDICTS (the seven revision-8-consumed findings)

- **E4L0-1 (AMENDMENT): DISCHARGED** — the bridge is written as the
  assembly's first link with the full display, verified at pen grade
  and symbolically this pass (zero residual). No residual defect.
- **E4L0-2 (AMENDMENT): DISCHARGED** — the universal-over-roots
  reading is explicit in the condition, the parity line is written
  and exact, the data-root landing follows; the separating mechanism
  is closed. No residual defect.
- **ER3L1-2 (AMENDMENT, my lens's own): DISCHARGED** — totality is
  the condition's zeroth clause in an explicit range form STRONGER
  than my spec's parenthetical (it also handles v-domain exit via the
  empty-range reading); the closure identification is correct.
  Residuals NOT in the clause itself but in two carriers of the
  enlarged condition: the falsifier consequent claims the
  whole-domain reading delivers totality (ECR1-1) and the grant
  head's enumeration omits it (ECR1-3).
- **ER3L1-1 (AMENDMENT, my lens's own): DISCHARGED** — the
  attribution is moved to the condition of record with the
  member-wise split written exactly as demanded. Residual: the
  witness restatement dropped its determinant regime and carries a
  misfitting class label (ECR1-2) — the disposition fails at that
  sub-clause and nowhere else.
- **E4L0-4 (AMENDMENT): site (a) DISCHARGED in full (consistency
  restored, verified); site (b) DISCHARGED with the ECR1-2 residual**
  (same clause as ER3L1-1's).
- **E4L0-3 (AMENDMENT): DISCHARGED-IN-PART** — the demanded
  member-wise split is delivered and its else-branch input-consumption
  logic is correct as far as it goes; the disposition fails at the
  new parenthetical's totality-delivery claim and the else-branch
  remainder's member count, both measured against the condition
  revision 8 itself enlarged (ECR1-1).
- **ER3L1-3 (AMENDMENT, my lens's own): DISCHARGED-IN-PART** — same
  disposition and same failure point as E4L0-3 (my spec's honest
  "modulo ER3L1-2's totality" was mutated in transplant; held to the
  harshest reading per the standing lesson, including against my own
  lens's wording).
- Standing dispositions re-verified against revision 8: r2-batch
  L0-1/L1-1, L0-2, L1-2; rounds 1-3 EL0-1/ESC1-F1, EL0-2,
  EL0-3/ESC1-F2, EL0-4, E2L0-1, E2L0-2, E3L0-1, ER2L1-1, E3L0-2 —
  all hold; nothing at revision 8 re-opens any of them; no orphan
  objection remains in any input file on disk.

The round is NOT dry at this lens: 4 findings sustained
(0 REPAIR-NEEDED + 4 AMENDMENT, 0 BREAKS-THE-LEG).

==============================================================================
## SUMMARY

| ID | Leg | Passage | Class | One-line |
|---|---|---|---|---|
| ECR1-1 | 5 | falsifier consequent (l.2344-2350) + §13 echo (l.3924-3928) | AMENDMENT | "modulo the totality clause, which the whole-domain reading also delivers" is FALSE — pins on the whole EOS domain constrain nothing about hull ⊆ chart range (the row's own accepted patch EOS with a0 large positive is the witness: pins hold domain-wide, totality fails); the else-branch remainder also omits the presupposed chart clauses — the member-wise re-scope was calibrated to the rev-7 two-member condition while rev 8 itself made it three-membered; my own spec's "modulo ER3L1-2's totality" was mutated in transplant (R-4 recurrence) |
| ECR1-2 | 5 | attribution witness (l.2212-2216) + §13 (l.3874-3877) | AMENDMENT | "the (m2)-class quadratic patch ... satisfies the strict pair everywhere" restated WITHOUT its determinant regime e0''b > a1^2 — at (m2)'s printed regime (a1^2 > e0''b) the strict pair FAILS; the load-bearing parameter clause present in ER3L1-1's text was dropped in the restatement |
| ECR1-3 | 5 | grant head + (i) annotation (l.2365-2374; truth conditions changed by rev 8) | AMENDMENT | the operative granting sentence still reads "STRICT + hull-theta condition of record" — totality omitted from the enumeration though the universal root-quantifier is VACUOUSLY satisfiable without it (empty root set), retaining ER3L1-2's mechanism at the grant itself; the (i) parenthetical names hull-theta as the genuine addition and is silent on the chart clauses; every other carrier was updated this revision |
| ECR1-4 | 5 | §13 honest residue (2) (l.4019-4021) | AMENDMENT | "SIX declared instances" contradicts the files: 4 declared at rev 7 + 4 new distinct seeding events this window (bridge, image-parenthetical, consequent, c_v clause — two of them recorded as spec-seeded by the same block's own entries) = EIGHT distinct events / TEN seeded findings |

CONFIRMED (cannot break; strongest attacks recorded in-section): the
CHART-COMPLETED condition — totality zeroth clause (incl. the
v-domain-exit/empty-range attack), universal root-quantifier, parity
line (sign bookkeeping re-derived and mechanically instanced),
data-root landing (C.1); the strict Legendre bridge — congruence
verified symbolically to zero residual, equivalence rank-faithful,
uniformity clause complete under the stated hypotheses — and its
assembly connective (C.2); the member-wise attribution split and both
gloss rewordings minus the witness clause (C.3); the AUD-cp
chart-clauses identification with ECR1-1 pushed through the closure
and absorbed (C.4); all four updated propagation carriers with a
stale-form hunt (C.5); the §13 revision-8 block's transcriptions,
merges, counts, discharge recital, leg-3 recital, label summary, and
gate deferral (C.6).

Counts: 4 findings total = 0 REPAIR-NEEDED + 4 AMENDMENT;
**BREAKS-THE-LEG: 0**. No statement, rigor label, or route-level
conclusion is touched by any finding: the route-level OPEN verdict,
the two-piece r2 residue, the quadruple-pinned grant, and the
gamma-table conclusions all stand; all four findings discharge
automatically at the standing gamma(T) closure (totality/uniqueness
priced there; the counts and enumerations are textual). ZERO
derivation-content defects survive in the revision-8 mathematics —
every displayed identity of the delta was re-derived at this lens,
two of them mechanically. The surviving defect class remains
wording/propagation/bookkeeping, now concentrated in carriers of the
condition revision 8 itself enlarged. The leg-3 (E-1) thread remains
DRY at both lenses (recital verified; body out of scope per brief and
VERDICT_escalation §3). DOC-1 is therefore NOT dry at this round at
this lens.

## SELF-FALSIFIERS (what kills each finding)

- ECR1-1: a derivation from Gibbs closure + c^2 > 0 + theta > 0 read
  on the whole EOS domain that CONCLUDES eps(W_s) ∈ range e(v_s, .)
  at every hull point for every §1-class EOS (cannot exist: the
  a0-positive patch EOS satisfies the pins domain-wide and violates
  totality on a thin-S-band hull — refute the patch's in-class
  membership to revive, which would simultaneously kill the row's own
  (m2)/(m3) exhibits); or a printed definition of the whole-domain
  branch that pins its conclusion domain to the HULL rather than to
  "wherever the pins hold" (none: the revision-7 recalibration
  sentence is explicit and unedited).
- ECR1-2: a printed line in the row supplying the witness's
  determinant regime (grep for "e0'' b > a1^2" / "e0''b > a1^2" in
  body text: no occurrence outside §13-quoted refutation text); or a
  reading of "(m2)-class" of record under which (m2)'s printed
  regime a1^2 > e0''b satisfies the strict pair (false by
  arithmetic).
- ECR1-3: a printed grant-head enumeration naming the chart clauses
  (none: line 2368 reads "STRICT + hull-theta condition of record");
  or a proof that the universal root-quantifier clause FAILS (rather
  than holds vacuously) at a rootless hull point under standard
  quantifier semantics — that would need the totality clause, which
  is exactly the member the enumeration omits; or a ruling that the
  defined-term reading needs no enumeration hygiene — contradicted by
  the loop's own ER3L1-3 precedent, which is why this stands at
  AMENDMENT.
- ECR1-4: a partition of the on-file declarations under which the
  distinct-event count is six WITHOUT dropping E4L0-1 or E4L0-3 —
  none exists: both are declared "spec-seeded" in r4_l0 AND in the
  rev-8 block's own entries, and the prior four are quoted by both
  round-window refuters.
- Any CONFIRMED verdict above: a derivation-level break of the quoted
  passage that the recorded strongest attack missed.

END — confirming round, lens l1, on the revision-8 delta. Machine
summary: {objections: 4, breaks: 0, repairs: 0, amendments: 4,
file: validation/sfoundations_raws_2026-08-13/r2pass/esc_doc1_confirm_l1.md}
