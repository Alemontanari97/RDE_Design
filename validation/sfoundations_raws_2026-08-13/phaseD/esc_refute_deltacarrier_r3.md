# ESCALATED REFUTATION — MINOR (c) DELTA-CARRIER, ROUND 3
# S-FOUNDATIONS-C4 escalation window, 2026-08-20. ESCALATED REFUTER
# per VERDICT_blocco2.md §3 E-3 (full-form escalation; single fused
# lens, math structure + carrier/bookkeeping contracts, right-sized by
# the orchestrator). Target: phaseD_minor_deltacarrier.md AS REVISED
# through [ESC-r3-*] (913 lines on disk this window, mtime 19:58:12).
# Inputs of record: VERDICT_blocco2 §§2.3/3/4 + refute_minor_
# deltacarrier.md + esc_refute_deltacarrier_r1.md (0 B / 1 R / 1 A /
# 2 N) + esc_refute_deltacarrier_r2.md (0 B / 1 R / 1 A / 1 N). This
# file is the ONLY file this slot writes. Findings
# ESC-DELTACARRIER-r3-<n>, classes BREAK/REPAIR/AMENDMENT/NOTE. Zero
# inflation: everything not under FINDINGS was attacked and held
# (VERIFIED-SOUND block). Probe re-run this window.

## READ-DEPTH DECLARATION (this window; all internal, no papers; CT-6
## kept — no number from the four nozzle papers consumed anywhere)

- phaseD_minor_deltacarrier.md [FULL] (whole revised file, 913 lines,
  all [ESC-r1-*]/[ESC-r2-*]/[ESC-r3-*] markers, disposition tables
  §§11-13; every r3 marker site located and read: [ESC-r3-1a] :446,
  1b :484, 1c :528, 1d :649, 1e :624, 1f :821, 1g :519, 2a :356,
  2b :295, 3 :688 — all nine present at the claimed sites);
- esc_refute_deltacarrier_r1.md [FULL] + esc_refute_deltacarrier_r2.md
  [FULL] (prior rounds: findings, named repairs, VERIFIED-SOUND
  blocks, machine summaries — prior-objection ledger built from them);
- refute_minor_deltacarrier.md [FULL] (round-0 record, 8/8 sustained);
- VERDICT_blocco2.md [FULL] (whole file; §§2.3/3/4 the mandate —
  guidance labels read as NON-ADOPTED);
- phaseD_minor_objdom.md :285-334 [FULL slice] (the consumed OBJDOM-5
  [ESC-r1-3] (H6')/ship-gate block re-read at source this window and
  compared against (c) §1 (H6'-A)/(H6'-B) + DC-5(c): substantively
  identical, ship-gate sentence verbatim-consistent) + full marker
  census re-measured this window (grep -c: [ESC-r1-*] 30, [ESC-r2-*]
  18, [ESC-r3-*] 6, [ESC-r4-*] 0 — matches the revision's §13 census
  exactly);
- esc_refute_objdom_r3.md tail [targeted]: dry: TRUE, closure-judge
  ready; NO esc_refute_objdom_r4.md exists (ls-checked) — (b) loop
  state re-established before adjudicating the consumption;
- docs/findings_registry.yaml :292-300 [FULL] (row; the :300 trigger
  "sequenced with/after [OBJ-DOM]; ships only with the sliver banded
  or repaired" re-verified character-exact this window);
- blocco3/SYNTHESIS_nozzle_rde_arrivals.md :280-283 (G-12) + :413-423
  ((e).3) [FULL slices] (graft source re-verified; grafted sentence
  faithful, [ADV], carries no paper number — the +50-60% figure of
  the source region does NOT ride the graft; CT-6-clean);
- docs/rde_nozzle_MASTER.md :250-252 [targeted] (D2.6 GIVEN line
  "constant Pa > 0" at :251-252 — the [ESC-r3-3] adjacency sentence's
  premise verified at source);
- docs/rde_nozzle_P1_sections_5_7.md :79-84 [targeted] (T7(c)
  regime-2 free-sign text re-verified for the standing [ESC-r1-7]
  dormancy clause);
- esc_probe_deltacarrier_h3breakout.py [FULL] (source re-audited: the
  [ESC-r2-4] tol_A derivation comment + line 76 match; isentrope leaf,
  sonic q*, gamma_s assertion, half [A] grid + closed u-interval,
  half [B] breakout construction all re-checked) + EXECUTED this
  window: PASS, exit 0 ([A] sup−p_lam = −4.731e-10, tol_A = 2.971e-15,
  aligned attainment exactly 0.0; [B] q = 0.888675, u = 1e-6,
  exceedance +1.607546e-1 vs required 2.971e-12). EVERY digit matches
  the revision's §2/§12/§13 quotes. Re-runnable as claimed; pinned-env
  numpy only, no install.
- File mtimes measured this window (SR-12; command: stat -c '%y %n'):
  phaseD_minor_objdom.md 19:16:17 (UNCHANGED — same mtime as every
  prior round's measurement, so the consumed (H6')/ship-gate block is
  the same bytes rounds 0-2 verified; independently confirmed by
  re-reading :285-334 at source) < esc_refute_objdom_r3.md 19:22:33 <
  esc_refute_deltacarrier_r1.md 19:38:02 <
  esc_probe_deltacarrier_h3breakout.py 19:40:06 (UNCHANGED since the
  [ESC-r2-4] repair — the revision's "probe not touched this round"
  claim reproduces) < esc_refute_deltacarrier_r2.md 19:52:13 <
  phaseD_minor_deltacarrier.md 19:58:12 (all 2026-08-20 −0400) — the
  §13 ordering claims reproduce.

## PRIOR OBJECTIONS (r1 + r2) — RESOLVED / PERSISTING (each verified
## at source this window)

- r1-1 (REPAIR, B_fam unrestricted): RESOLVED (round 2, re-verified
  standing this round — [ESC-r2-1] definition restriction intact; the
  r3 edits touch only its membership prose, which was r2-1).
- r1-2 (AMENDMENT, consumption version clause): RESOLVED (round 2;
  re-measured THIS window: (b) still at [ESC-r3-*] 30/18/6/0, mtime
  unchanged, no r4 file, its r3 refutation DRY — the corrected
  declaration remains true now; the explicit closure re-verification
  duty stands in the [ESC-r1-0] header).
- r1-3 (NOTE, dangling clause): RESOLVED (round 2; reflowed sentence
  present and grammatical at §3).
- r1-4 (NOTE, probe tol_A scale): RESOLVED (round 2; probe unchanged
  since, mtime-proven; re-run PASS this window, digits match).
- r2-1 (REPAIR, "supersonic-exit by construction, hence in W_cert"
  invalid inference): RESOLVED at ALL FOUR anchored sites plus two
  history annotations. Verified site-by-site this window: §4
  definition paragraph [ESC-r3-1a] (:446-475) — prior sentence
  RETRACTED-as-quoted, replaced by the certificate form (u_x > c
  per-cell, i.e. M cosθ > 1, a-posteriori S1 margin check, membership
  "UPON THAT CERTIFICATE", "a checkable a-posteriori certificate,
  never a construction fact"), and the uniform-exit relaxation STATE
  dropped from the instantiation list per [ESC-r1-6]; (V2) [ESC-r3-1b]
  (:484-486) reworded to the same certificate; B1^c wiring [ESC-r3-1c]
  (:528-534) — retraction quoted, certificate + report-on-fail entry
  clause, and the displayed "Hence B1^c <= J_ideal(eps) over the
  certified entries" made independent of membership prose; [DC-F2-2]
  certificate-on-entry duty [ESC-r3-1d] (:649-654); DC-6 attainment
  made conditional [ESC-r3-1e] (:624-633, SCHEMA class tolerates);
  §12 record annotated [ESC-r3-1f] (:821-828, history kept); the
  residual [ESC-r1-6]-bracket occurrence pinned as quoted-history-only
  [ESC-r3-1g] (:519-523). Grep sweep of "by construction" over the
  whole file: occurrences at :449, :515, :626, :818 — each sits
  inside a quoted retraction or an [ESC-r3-1f/1g]-annotated history
  passage; NO operative statement carries the inference. §9 landing
  pin updated (:680-684) so the retracted inference cannot land.
  Math of the repair independently re-verified: u_x = q cosθ > c ⟺
  M cosθ > 1 (exact); cos 15° = 0.9659, M_e = 2 ⇒ M cosθ = 1.93 ≥
  1.9 (the file's genericity gloss is conservative and hedged
  "generically", with the operative claim the certificate, exactly
  AG-1-shaped); the probe's half [B] (supersonic-speed cell breaking
  the bound) re-run PASS — the conflation the repair kills remains
  executable-proven.
- r2-2 (AMENDMENT, W_cert phase-quantifier unpinned): RESOLVED.
  [ESC-r3-2a] (:356-369) pins membership at the phase data in play,
  cycle-integrated displays read the mu-a.e.-all-phase subclass, the
  MIXED reading is excluded, and the (V1) asymmetry is closed;
  [ESC-r3-2b] (:295-297) makes "(H1)-(H5b) at mu-a.e. xi" explicit in
  DC-1's MU-INTEGRATED FORM. Both consistent readings re-checked
  sound this window (per-phase: DC-1 applies phase-by-phase to each
  inner sup; all-phase: smaller sets, same chains; every displayed
  inequality holds under either). One residual one-word looseness in
  the pin's own text is finding r3-1 (NOTE) — a residue of the
  repair's wording, not a persistence of r2-2 (the quantifier IS now
  pinned and the mixed reading excluded, as demanded).
- r2-3 (NOTE, Pa landing adjacency): RESOLVED. [ESC-r3-3] (:688-698)
  adds the mandatory landing-text sentence; its premise verified at
  source this window (MASTER.md :251-252 "constant Pa > 0"); the
  sentence correctly names the deliberate widening, the [OBJ-DOM] Pa
  axis, the DC-5(b) owner line, and the class-constant offset
  Pa·Int_wall n_x dA. Editorial, faithful.

## VERIFIED-SOUND (attacked, held; listed so the closure judge sees
## coverage — all in THIS window)

1. [ESC-r3-1a] certificate form vs the FULL (H1)-(H5b) definition of
   W_cert: attacked as possibly over-narrow ("in W_cert UPON THAT
   CERTIFICATE" names only the (H3) margin check while W_cert also
   requires (H1)/(H2)/(H2b)/(H5b)). HELD: for the named family
   instantiations the per-phase constrained solves are POSED inside
   the shared-data contract ((H1)/(H2) are the solve's own data, the
   legitimate sense of "construction"), (H5b) is a class-wide
   table-checkable EOS property, and (H2b) is a data-level check with
   its own named owner in the SAME duty list ([DC-F2-5], [ESC-r1-8])
   — referenced by [ESC-r3-1d] ("same pattern as the (H2b) check").
   (H3) is the only member-specific residual hypothesis, so equating
   "their W_cert certificate" with the per-cell axial-supersonicity
   check is correct in context; every other hypothesis has a named
   owner at the carrier (DC-5(b) prints eps/tables/IVL-mdot/(H2b)).
2. [ESC-r3-1c] displayed-bound independence: "the sup over a subset
   of W_cert-certified members is bounded by the {eps}-member ceiling
   regardless of any membership prose" — re-proved: A_phase({eps,L})
   ∩ W_cert ⊆ A_phase({eps}) ∩ W_cert (slot removal enlarges), DC-1
   member-by-member on W_cert bounds the larger sup by J_ideal. Under
   the repaired text a certificate-FAILING classical solve simply
   does not enter the ladder (report-on-fail, [ESC-r3-1d]) — the
   displayed chain never consumes an uncertified value. Sound.
3. [ESC-r3-1e] DC-6 conditional attainment: under the certificate the
   phase-shared classical optimum lies in the restricted family and
   attains its sup (unrestricted per-phase attainer, certified ⇒ in
   the restricted set ⇒ attains the smaller-or-equal restricted sup);
   Pi_L → 0 degeneration intact; SCHEMA class tolerates the
   conditionality per its own definition (consumes registry/M0, no
   re-derivation). Sound.
4. [ESC-r3-2a/2b] pin, math half: DC-3(a) ("S in W_cert feasible for
   all phases") reads all-phase per the pin — consistent; DC-3(c)'s
   "satisfies (H1)-(H5b) by definition ... pointwise in xi" is now
   correct under the all-phase reading (a.e.-xi hypotheses feed DC-1
   at each xi); DC-1's MU-INTEGRATED FORM carries its own explicit
   a.e. clause. The r2 refuter's mixed-reading hazard is EXCLUDED by
   name. Sound (see r3-1 for the one-word residue).
5. DC-1 proof (untouched this round): spot re-derivation — A'(q) =
   rho'q² + 2rho q − lambda(rho'q + rho) + p' with p' = −rho q,
   rho' = −rho q/c² collapses to rho(q − lambda)(1 − M²) exactly;
   (ii.c) case 2 chain B = p + rho c(c − lambda) < p − rho c q <
   p − rho c² = p(1 − gamma_s) ≤ 0 via q > c and gamma_s ≥ 1 —
   correct; assembly equality at the uniform exit exact by the
   definition of lambda. Still sound.
6. Probe (untouched this round, mtime-proven): source re-audited —
   [ESC-r2-4] tol_A = 64·eps·max(rho(lambda)·lambda², p(lambda)) at
   line 76 with the largest-intermediate derivation comment; half [A]
   u-interval closed [c(q), q] per (ii.a); half [B] asserts
   u_small < c_mid (genuine (H3) violation) and q* < q_mid < lambda;
   breakout hand-check p(q_mid) − p(lambda) ≈ 0.1723 − 0.0116 ≈
   0.1607 matches the run. Re-run PASS exit 0, every digit matches
   the revision's quotes. Two-sided as claimed.
7. (H6')/ship-gate consistency (standing adoption): (c) §1
   (H6'-A)/(H6'-B) + DC-5(c) re-compared against (b) :290-330 at
   source this window — substantively identical, ship-gate sentence
   verbatim-consistent ("[OBJ-DOM-IMPL] landed and used on both
   sides, OR the net-panel band included"); registry :300 quote
   character-exact; (b) mtime unchanged ⇒ the consumed block is
   bit-identical to what rounds 0-2 verified. Sound; the two minors
   still share one gate text.
8. Sequencing/bookkeeping state claims of §13: every checkable claim
   reproduced in MY window (census 30/18/6/0, mtimes, no r4 file, (b)
   r3 refutation dry: TRUE, (b) NOT closed, landing PROPOSED at
   [ESC-r1-9], closure re-verification duty LIVE). The MARKER
   DISAMBIGUATION note in [ESC-r3-0] is correct and useful (bare
   string counts on THIS file would double-count quoted (b) markers).
   Sound.
9. Disposition table §13: 3/3 rows, every [ESC-r3-*] marker present
   in the body at the claimed sites (grep-verified, nine markers
   1a-1g/2a/2b/3), retractions quoted-as-retracted, none contested,
   history preserved. Compliant.
10. CT-6 whole-file: the r3 delta's only new numbers are generic
    engineering glosses (M_e ≥ 2, 15°, cos 15° = 0.966) and probe
    digits — no number from the four nozzle papers anywhere; the
    G-12 graft sentence remains number-free. Clean.
11. [ESC-r3-3] Pa sentence: no-contradiction logic re-checked (a
    lemma may carry broader hypotheses than (P); the widening covers
    the executed vacuum objective; argmax untouched, offset
    class-constant) — faithful to the r2-3 finding and to MASTER :251
    at source. Sound.

## FINDINGS

### ESC-DELTACARRIER-r3-1 [NOTE] — one word of the [ESC-r3-2a] pin
overclaims: "equivalently, write W_cert(xi) inside the integral" — the
all-phase and per-phase pinnings are NOT equivalent as definitions of
B_fam (the two sups differ in general); only the immediately-following
scoped claim ("both readings agree for every named consumer") is true,
and it is what every proof uses

Anchor: revised file §3 [ESC-r3-2a] (:356-369): "in cycle-integrated
displays ... W_cert denotes the mu-a.e.-ALL-PHASE certified subclass —
equivalently, write W_cert(xi) inside the integral; both readings
agree for every named consumer ..."; the §9 landing pin (:680-684)
carries "phase-quantified per [ESC-r3-2a]" into the [T-DCRX] landing
form. Provenance: the word sits inside the r2 refuter's own named
repair text, adopted verbatim — a repair can carry a residue.

Reason: with W_cert(xi) := the (H1)-(H5b)-at-xi certified subclass and
W_cert^ap := the mu-a.e.-all-phase subclass (= a.e.-intersection of
the W_cert(xi)), one has W_cert^ap ⊆ W_cert(xi) for a.e. xi, hence
sup_{A_phase(C) ∩ W_cert^ap} F ≤ sup_{A_phase(C) ∩ W_cert(xi)} F
pointwise, with STRICT inequality possible (a design certified at xi
but not at some other phase can dominate at xi). So the two pinnings
give different B_fam VALUES in general; as set/value definitions they
are inequivalent, and an unqualified "equivalently" in a pin of record
is a set-identity claim the file does not (and need not) prove. What
IS true — and re-verified this window — is exactly the scoped sentence
that follows: every DISPLAYED statement holds under either reading
(leg (a) lower-bounds by an all-phase-certified S, which lies in both;
leg (c) upper-bounds via DC-1 applied at each xi to W_cert(xi) ⊇
W_cert^ap members; monotonicity (b) is reading-independent), and no
B_fam value is ever computed or quoted, so no false number can ship.
NOTE, not AMENDMENT: the quantifier IS pinned, the mixed reading IS
excluded (the r2-2 demand is met), both readings close every proof,
and the residue is one word whose over-reading is corrected by its own
adjacent gloss — but the §9 landing carries the pin toward M0, where a
THEOREM-adjacent "equivalently" between two inequivalent definitions
is the exact looseness class this loop polices ([ESC-r1-3]'s own
standard).

Repair (one word, no conclusion moves): "— alternatively, write
W_cert(xi) inside the integral (either pinning closes every proof;
both readings agree for every named consumer, ...)" — or keep
"equivalently" and scope it explicitly: "equivalently FOR EVERY USE IN
THIS FILE". Fold into the landing cut of [ESC-r3-2a] whenever [T-DCRX]
is extracted; nothing else moves.

## SEQUENCING CHECK (SR-C4-9, explicit)

VERDICT_blocco2 §4(1)-(3) compliance of the round-3 revision:
VERIFIED, unchanged from rounds 1-2 — the (H6') two-branch form and
the ship-gate are character-consistent with the escalated (b) source
(:290-330, re-read at source this window) and with §4(2); registry
:300 license quoted character-exact; [ESC-r1-9] + the [ESC-r2-2]
closure duty still gate the landing on (b)'s closure. Loop state
re-measured this window: (b) at [ESC-r3-*] (census 30/18/6/0, mtime
19:16:17 unchanged), its r3 refutation DRY and closure-judge ready,
NO r4 file, (b) NOT closed — the (c) closure judge's re-verification
duty on the consumed block remains LIVE and nothing may land yet. No
inconsistency between the two minors as revised.

## PAPERS NEEDED

(empty)

## MACHINE SUMMARY

findings: 0 BREAK, 0 REPAIR, 0 AMENDMENT, 1 NOTE (r3-1: the
  [ESC-r3-2a] pin's "equivalently, write W_cert(xi) inside the
  integral" overclaims set/value equivalence of the two pinnings —
  the all-phase and per-phase B_fam sups differ in general; the
  adjacent scoped claim ("both readings agree for every named
  consumer") is the true and load-bearing one; one-word repair named
  for the landing cut; no displayed statement is false under either
  reading and no B_fam value is ever quoted).
prior_objections: r1-1..r1-4 RESOLVED (standing from round 2,
  re-verified); r2-1 RESOLVED at all four sites + two history
  annotations (grep sweep: no operative "by construction" inference
  survives; repair math independently re-derived; probe half [B]
  still kills the conflation); r2-2 RESOLVED (quantifier pinned,
  mixed reading excluded, (V1) asymmetry closed — the r3-1 NOTE is a
  one-word residue of the repair wording, not a persistence); r2-3
  RESOLVED (landing sentence faithful, premise verified at MASTER
  :251 at source).
core_verdict: all three round-3 repairs are SOUND as applied; DC-1's
  proof, DC-2's restated chain, the restricted B_fam definition with
  its certificate-form membership, the (H6')/ship-gate adoption, the
  DC-4 dormancy, and every displayed inequality hold; the probe
  reproduces every digit; all §13 state/bookkeeping claims reproduce
  under independent measurement.
dry: TRUE — round 3 finds no actionable defect (the single NOTE is a
  one-word landing-cut wording item with a named repair, no statement
  false, no conclusion at risk); the escalation loop's until-dry
  condition is met at this lens. Closure-judge ready — with the
  standing duties intact: (b)-closure re-verification of the consumed
  (H6')/ship-gate block ([ESC-r1-0]/[ESC-r2-2]), landing PROPOSED
  until §4(3) is satisfied ([ESC-r1-9]).
probes: esc_probe_deltacarrier_h3breakout.py re-run this window,
  exit 0, all quoted digits reproduced ([A] sup−p_lam = −4.731e-10,
  tol_A = 2.971e-15, attainment exactly 0.0; [B] q = 0.888675,
  exceedance +1.607546e-1 vs required 2.971e-12); re-runnable,
  pinned-env numpy only, no install.
ct6: clean (this file consumes no paper number).
