# R22-F CENTERPIECE — REFUTATION, ROUND 4, LENS L0
# (variational/optimization structure: fiber lower bounds, J-variation,
# duality, channel (ii)/(vi) quantification structure — refined
# criterion: delta/mu schema = mu measured engine-curvature carrier,
# delta = design-gradient-level residual bound)

Session: S-FOUNDATIONS-C4, Blocco 1 v2 continuation, 2026-08-20.
Target: validation/sfoundations_raws_2026-08-13/phaseD/
phaseD_r22f_centerpiece.md (author draft revision 1 + reviser passes
[REV2-r1-0..21], [REV2-r2-0..8], [REV2-r3-0..10], 1828 lines incl.
§§9-12 graft + disposition tables). Numbering continues across rounds:
prior L0 IDs end at R22F-L0-18; this round starts at R22F-L0-19.

READ-DEPTH DECLARATIONS (this window):
- BRIEF_blocco2_phaseD.md (as amended): [FULL] (209 lines; CENTERPIECE
  section re-read against the round-4 duties).
- BRIEF_blocco2_phaseD_addendum_c4.md: [FULL] (46 lines).
- phaseD_r22f_centerpiece.md: [FULL] (1828 lines, incl. all
  [REV2-r3-*] blocks, the §12 round-3 disposition table, and the
  [REV2-r3-6] in-place cell edits with superseded fragments).
- ALL NINE prior v2 refutation files [FULL] this window (round-4
  orchestrator order): phaseD_r22f_refute_r1_l{0,1,2}.md,
  phaseD_r22f_refute_r2_l{0,1,2}.md, phaseD_r22f_refute_r3_l{0,1,2}.md
  — own-lens prior-objection verification duty + cross-lens site
  consistency; reviser dispositions read in the draft's §§10-12 tables
  AND verified at their edited sites for my lens's round-3 findings.
- docs/rde_nozzle_MASTER.md: NOT re-read this window. The S20-block
  anchors load-bearing below (M0:2178-2183; M0:2208-2211;
  M0:2307-2310) were source-verified in MY round-2 window (r2_l0
  READ-DEPTH block, of record) and re-confirmed by the L1 refuter's
  independent round-3 source check (r3_l1 L1-15(a), of record); the
  draft text citing them is unchanged since. No new M0 claim is made
  this round.
- swirl5f carriers / SYNTHESIS / FIELD_ATLAS / NOZZLE_RDE_STUDY
  dossiers: [NOT-READ] this window — no L0 finding leans on them.
EXECUTABLE PROBE (standing preference honored): validation/
sfoundations_raws_2026-08-13/phaseD/r22f_v2_probe_r4_l0_mu_identity_
proxreg.py — WRITTEN and RUN this window, ALL ASSERTS PASS (PART A
feeds L0-19; PART B feeds L0-20); synthetic 1-D/2-D counter-models
only, derived tolerances (CT-6 clean: no number from the four nozzle
papers used anywhere; no magic constants).

==============================================================================
## MANDATE-COVERAGE DUTY (round-4, all lenses) — RESULT

Channel (vi) with the delta/mu schema: **PRESENT** — forchetta row (vi)
:1301 as amended by the [REV2-r2-6] and [REV2-r3-6] cell edits, backed
by §2.2-bis :533-596 + [REV2-r2-3] :597-695 + [REV2-r3-1/2/3] :697-773
(mu = measured carrier with the O1 gate and H-G5 declared; delta =
SCHEMA + named derivers; the brief-:126 "never a value-level bound
silently passed off as optimum coverage" sentence carried in the WORST
cell). Fig. 18 per-term disposition: **PRESENT** (§2.3 exhibit
:775-784 + disposition table :825-831 with the [REV2-r1-9]
joint-nullity wording and [REV2-r1-10] per-instance carriage —
unchanged since round 3). Both mandated elements verified in the
deliverable text. Coverage duty: no missing-element REPAIR this round.

## PRIOR-OBJECTION VERIFICATION (my lens, rounds 1-3 → disposition check)

Rounds 1-2 (L0-1..L0-15): chain closed of record (r2_l0 and r3_l0
verification blocks; the one persisting limb L0-6 → L0-12 closed at
round 2). Round-3 (L0-16..L0-18), each verified AT ITS EDITED SITE
this window:
- R22F-L0-16 (REPAIR, H-G5 not carried at the licensing sites) —
  **RESOLVED AS FILED**: [REV2-r3-1] adds the H-G5 hypothesis line
  (:704-711) and carries it at ALL FIVE licensing sites my finding
  named — (T)'s definition (backed parenthesis, :712-718), H-G4
  (in-place edit with the superseded fragment verbatim, :718-720),
  R-13 (:720-722 + :1552-1554), the (F) hypothesis list (:685-686),
  and the (vi) cell check list ([REV2-r3-6](a), :1319-1328) — plus my
  optional fix shape (c) as the constructive branch-wise coverage
  remark (:724-733) and the WORST-cell attribution ([REV2-r3-6](c)).
  The carriage objection is closed. (The CONTENT of the new hypothesis
  line's prox-regular disjunct — text that did not exist at round 3,
  proposed verbatim by MY OWN fix shape (a) — fails under attack this
  round: R22F-L0-20, NEW finding on new text with self-origin
  declared, not a persisting count; same classification precedent as
  round-3 L2-19.)
- R22F-L0-17 (AMENDMENT, evaluation-point pin) — **RESOLVED**:
  [REV2-r3-2] (:735-742) pins g and the cone at S*_red, prints the
  bound ||S*_true − S*_red|| ≤ delta(S*_red)/mu, and adds the
  executor sentence; carried into the (vi) cell ([REV2-r3-6](a)).
  Matches the round-3 fix shape exactly.
- R22F-L0-18 was a NOTE (no action owed); limb (g) absorbed by
  [REV2-r3-7] (headline fitted-sheet qualifier verified present,
  :1480-1483); limb (d) noted for the landing editor at
  [REV2-r3-10](f). Confirmed.

**PERSISTING PRIOR OBJECTIONS RE-COUNTED THIS ROUND: 0.**

==============================================================================
## FINDINGS

### R22F-L0-19 — BREAK (executable counterexample RUN): the delta/mu
### schema never names WHICH functional's Hessian the curvature floor
### mu is — the VI chain behind the licensed gradient-route forms
### consumes the curvature floor of J_TRUE, while (D) asserts the
### measured carrier (the engine's J_red Hessian) "INSTANTIATES the
### schema object"; the curvature-level face of the reduction residual
### is silently priced at zero, and the licensed form-(T) bound fails
### unboundedly with EVERY printed check passing

ANCHORS: §2.2-bis OBJECT :540-548 — J_red and J_true are DEFINED
(:541-542) and delta is built from grad J_true − grad J_red (:544),
but the curvature side never returns to the distinction: condition (1)
:579-585 ("mu must be a curvature FLOOR (smallest reduced-Hessian
eigenvalue) holding UNIFORMLY ...") names no functional; form (T)
:614-619 ("mu_T = the curvature floor over the TANGENT
(feasible-direction) cone on the certified ball") names no functional;
form (C) :620-627 likewise; (F) :685 ("a curvature floor mu per (A)")
likewise; sign convention (C) :637-646 says "for concave J" —
generic J. Grep-verified this window: the strings "J_true"/"J_red"
occur ONLY at :541-544 and :550 (delta's definition and channel
structure) — no curvature sentence anywhere carries the functional
identity. (D) :647-653 then asserts: "The carrier INSTANTIATES the
schema object at an interior / margin-inactive S*" with the carrier =
"the segmented TR-Newton curvature in the driver's parameterization
(S18/S24 lineage, Hessians of record)" — the engine's Hessians are
Hessians of the REDUCED (coded, per-phase-2D) functional; the engine
has never computed J_true (that is the entire premise of R22).
[REV2-r3-2] :735-742 prints the bound ||S*_true − S*_red|| ≤
delta(S*_red)/mu; the (vi) BEST cell :1301 licenses it with "mu = the
MEASURED engine curvature at S*".

RIGHT-SIZING COMPLIANCE: this attacks the SOUNDNESS of the printed
instantiation sentence and the licensed form, not closure of any
gradient bound (addendum_c4 (b): "if you believe the schema itself is
UNSOUND, that IS attackable"); the asked fix is itself a SCHEMA +
named deriver — no closed form demanded. SR-C4-13 respected.

DEFECT (two horns, one hole): the form-(T) VI chain evaluated at
S*_red — my own round-3 hand-derivation of record (r3_l0 L0-18(a) and
:74-77): VI at x1 for grad J_red, VI at x2 for grad J_true, "plus the
mu_T strong-concavity line" — consumes the strong-concavity modulus of
J_TRUE along the feasible segment (that is the only line connecting
grad J_true(x2) to grad J_true(x1)). The same holds for form (C).
Therefore:
  HORN 1 (measured-carrier reading — the only executable one, and the
  one (D) + the cell print): mu = the J_red Hessian floor. The chain
  is NOT a theorem: mu_true and mu_meas differ exactly by the
  CURVATURE-LEVEL face of the same reduction residual (the
  design-Hessian gap H_true − H_red — the second-order analogue of
  delta), which NO printed hypothesis (H-G1..H-G5), condition
  ((1)-(3)), gate (O1/H-G4), or residue (R-9..R-15) prices; a small
  gradient-level residual at S*_red does not bound it.
  HORN 2 (J_true-floor reading): the chain is sound but condition
  (1)'s a-posteriori check "delta/mu ≤ the certified basin radius" is
  UNEXECUTABLE (no measured object instantiates mu — the (E) ladder
  re-measures the ENGINE's curvature, i.e. J_red's), and (D)'s
  instantiation sentence is false as printed.
Either way a load-bearing printed sentence of the user-facing schema
is unsound. Note the asymmetry with the value route: (F)'s bound is
provable from x1's OWN optimality + strong concavity of J_RED + the
eps_U premise (J_red(x1) − J_red(x2) ≥ (mu_red/2)·shift², then
0 ≤ J_true(x2) − J_true(x1) ≤ 2·eps_U − (mu_red/2)·shift²) — so the
value route IS sound with the measured carrier as-is; the gradient
route is not. The draft treats the two routes' mu as one object.

EXECUTABLE COUNTEREXAMPLE (probe r22f_v2_probe_r4_l0_mu_identity_
proxreg.py PART A, RUN, all asserts PASS; synthetic 1-D, CT-6 clean):
J_red = −(a/2)x² (a = mu_meas = 100, argmax S*_red = 0), J_true =
−(c/2)(x−b)² (c = mu_true = 1, argmax b = 1), feasible set convex
(H-G5 holds), metric M = I (condition (3) holds), Hessians CONSTANT
(the measured floor a sustains on every ball — any (E)-ladder basin
certificate passes), interior margin-inactive S* (H-G4 moot),
cone-pairing trivial (full-space cone both sides). Results:
  (A2) licensed form (T), mu = measured carrier: delta(S*_red) = c·b
  = 1, bound = 0.01; ACTUAL shift = 1 — violation 100x; basin check
  0.01 ≤ 2 PASSES (A1).
  (A3) mu = J_true's floor: bound = 1 — TIGHT (the chain is fine;
  only the mu identity is broken).
  (A4) corrected curvature-transfer form mu_eff = mu_meas − L_H with
  L_H = sup_ball |H_true − H_red| = 99: bound = 1 — EXACTLY recovers.
  (A5/A6) value route (F) with the SAME measured carrier: bound
  2·sqrt(eps_U/mu_meas) = 2.83 ≥ 1 HOLDS, and its own a-posteriori
  check honestly REFUSES to certify (2.83 > basin radius 2) — the
  defect is isolated to the gradient route.
  (A7) violation factor = mu_meas/mu_true grows without bound along
  the ladder (10x → 10000x).

WHY BREAK (L0-10 precedent, not L0-16's): the counter-model satisfies
EVERY printed hypothesis and check of the licensed form under its only
executable reading — no printed hypothesis is violated, and the
licensed bound-form is false. (L0-16 was REPAIR because its
counter-model violated a printed-but-uncarried hypothesis; here no
hypothesis exists to violate.) NOT a persisting count: no prior
finding at any lens raised the functional identity of mu. SELF-ORIGIN
PARTIAL, DECLARED (zero inflation both ways): conditions (1)-(3)
originate in MY round-1 amendment L0-8; (D)'s carrier-instantiates
sentence follows MY round-2 fix shape for L0-11; my r2/r3 hand
verifications recorded "the mu_T strong-concavity line" without naming
whose curvature it is — the sharpening is mine to ask for; the reviser
applied every prior prescription faithfully. This is a defect of the
prescriptions, not of their application.

FIX SHAPE (AG-1 valve: strong, trivially checkable where checkable,
sufficient-not-optimized; one hypothesis line + declaration touches —
no closed form demanded, addendum_c4 (b) respected):
  (a) DECLARE THE FUNCTIONAL at every curvature sentence: in forms
  (T)/(C) and condition (1), the schema object mu is the curvature
  floor of J_TRUE on the certified ball/cone (sign convention (C)
  unchanged; the [REV2-r2-3](C) mu_curv rename at landing carries the
  identity too).
  (b) NEW HYPOTHESIS LINE H-G6 (curvature transfer): the measured
  carrier instantiates the J_true floor only up to the CURVATURE-level
  residual — mu_true_floor ≥ mu_meas − L_H, with L_H := the sup over
  the certified ball of the design-Hessian residual norm
  ||H_true − H_red||_M (the second-order face of the SAME reduction
  residual operator; by the adjoint representation it decomposes along
  the same (J)/(H) channels at second order). STATUS honest: L_H is
  UNDERIVED of record — SCHEMA + named derivers, mirror of delta's own
  treatment (R-9): (1) five-field content bound at Hessian level
  (X-T3QS-5F, F2); (2) C51-route-B; (3) M-RED gradient-rider
  extension — divided differences of the MEASURED gradient gaps along
  the §3.6 design sweep = measured directional Hessian-gap (a cheap
  extension of the same rider, beside the (E) curvature ladder). New
  residue line beside R-9 (R-16 candidate).
  (c) (D) REWORDED: the carrier instantiates the schema object UNDER
  H-G6 (mu_eff = mu_meas − L_H) at an interior/margin-inactive S* —
  never flatly; until L_H lands, the gradient-route cells license NO
  number even a-posteriori from the measured carrier alone (the (vi)
  WORST cell's honesty line extends: delta AND L_H underived).
  (d) (F) DECLARED MEASURED-CARRIER-SOUND: state the J_red-side
  derivation (one sentence: x1's optimality + J_red strong concavity
  + H-G5 + the eps_U premise suffice), making the value route the
  ONLY route whose mu is legitimately the measured carrier as-is —
  carried into the (vi) BEST cell's value-route clause. Probe PART A
  legs A3-A5 witness that (a)-(d) restore exactness (tight bounds)
  and leave the schema family coherent.

### R22F-L0-20 — REPAIR (executable counterexample RUN; self-origin
### declared): H-G5's prox-regular disjunct is UNSOUND as printed —
### on a uniformly prox-regular, connected feasible set with the shift
### ball in one component, both licensed routes fail unboundedly, and
### the value route's own a-posteriori check PASSES while its bound is
### false

ANCHORS: [REV2-r3-1](a) :704-711 — "H-G5: the feasible set intersected
with the certified ball is CONVEX (or locally convex / prox-regular
with the shift ball inside ONE connected component) — declared and
checked per instance"; the licensing sites that now carry H-G5: H-G4
:663-669, R-13 :1548-1554, (F) :685-686, the (vi) BEST cell :1301,
R-14 :1561-1570.

RIGHT-SIZING COMPLIANCE: attacks the soundness of a printed hypothesis
disjunct (a licensing sentence), not bound closure. SR-C4-13
respected.

DEFECT: the VI / strong-concavity arguments behind forms (T) and (F)
consume SEGMENT FEASIBILITY between the two argmaxes. Convexity of
C ∩ ball delivers it; the printed alternative "prox-regular with the
shift ball inside ONE connected component" does NOT — prox-regularity
bounds the boundary's curvature (single-valued projection on a
neighborhood) but places no restriction on the segment between two
BOUNDARY argmaxes, and on a boundary concave toward the feasible side
the effective curvature of the objective ALONG the constraint surface
carries the second-fundamental-form correction mu − |lambda|·kappa
(lambda = active multiplier scale, kappa = boundary curvature), which
the ambient-Hessian floor mu misses entirely. The disjunct therefore
licenses the forms exactly where their proofs do not run.

EXECUTABLE COUNTEREXAMPLE (probe PART B, RUN, all asserts PASS;
synthetic 2-D, CT-6 clean): C = {||x|| ≥ 1} (complement of the open
unit disk: UNIFORMLY prox-regular with prox-parameter r = 1, smooth
boundary, CONNECTED — one component); J_red/J_true = −||x − a_i||²/2
with anchors a_1, a_2 at radius eps_off = 0.01 INSIDE the disk,
separated by angle 0.5 rad; both Hessians = −I, so mu = 1 for BOTH
functionals (deliberately NO mu-identity confound with L0-19: this
failure is pure feasible-set geometry). Both argmaxes sit on the unit
circle; the shift = 2·sin(0.25) = 0.495 is set by the ANGLE
(independent of eps_off) while the gradient gap and value gap scale
with eps_off. Results:
  (B1) the printed disjunct's checks PASS: prox-regular (r = 1),
  shift ball (radius 0.6) inside the single connected component, true
  argmax inside the ball.
  (B2) form (T): delta_T = 4.79e-3 (support-function form on the
  tangent cone at S*_red, computed exactly), bound = 4.79e-3 vs
  actual shift 0.495 — violation 103x.
  (B3) value route (F): eps_U = 4.19e-3 on the basin cap, bound
  2·sqrt(eps_U) = 0.130 < shift 0.495 — FALSE, and its a-posteriori
  check 0.130 ≤ 0.6 PASSES: under this disjunct the value route
  CERTIFIES a false bound (unlike L0-19's PART A, where it honestly
  refused — the segment-feasibility loss breaks the certification
  logic itself).
  (B4) convex control (unit disk, interior argmaxes): bound = shift
  exactly (tight) — nonconvex boundary isolated as the sole killer.
  (B5) the form-(T) violation grows without bound as eps_off → 0
  (10x → 1032x on the ladder).

WHY REPAIR, NOT BREAK: the defective text is one disjunct of a
hypothesis line, and the convexity branch of the same line is sound
and carries all currently-instantiated uses (box/linear design
constraints; the KS-margin set was ALREADY declared OPEN under R-14,
so no existing licensing silently leans on the broken branch); the
schema's licensed forms remain true under the convex branch. Same
classification class as round-2 L1-7 (a licensing hypothesis whose
stated form does not cover its exposure). SELF-ORIGIN DECLARED (zero
inflation both ways; L2-19 precedent): the disjunct's wording is
verbatim from MY round-3 fix shape (a) (r3_l0 :189-196); the reviser
adopted it faithfully — a new defect in new text, not a disposition
failure and not a persisting count. The round-3 probe could not catch
it (its two-component counter-model violated the one-component clause;
this round's one-component prox-regular model is the sharper
instrument).

FIX SHAPE (one line + one wording touch; AG-1 valve,
sufficient-not-optimized): (a) restrict H-G5's checkable branch to:
"C ∩ certified ball CONVEX (equivalently: the segment between S*_red
and every point of the shift ball feasible) — declared and checked per
instance; trivially checkable for box/linear design constraints." The
"locally convex / prox-regular + one component" disjunct is WITHDRAWN
as a licensing branch; if retained at all, it is a NAMED F2 refinement
carrying the curvature-corrected floor clause (mu_eff = mu −
|lambda*|·kappa_max > 0 with kappa_max the boundary's
second-fundamental-form bound = 1/r_prox; SCHEMA, deriver named beside
the O1/R-13 block) — never a bare disjunct. (b) R-14's wording
updates consistently: the KS-margin set's status stays OPEN, now
awaiting EITHER a convexity certificate OR the curvature-corrected
clause (strictly strengthens the existing declared-OPEN line; the
branch-wise value-dominance remark [REV2-r3-1](b) is unaffected — it
is a value-comparison argument and survives, but note per L0-19(d)
that its margin test consumes eps_U, whose measured instantiation
rides the same sweep-sup discipline).

### R22F-L0-21 — AMENDMENT (one clause): the eps_U sweep-sup
### instantiation lacks the refinement-stability discipline the file
### itself established for the floor scan — a SAMPLED sup is a lower
### estimate of the basin sup, so premise discharge is
### sampling-density-dependent as printed

ANCHORS: [REV2-r3-3](b) :760-767 — "its measured instantiation = the
SUP over the M-RED value legs (A)-(B) sampled ALONG THE DESIGN SWEEP
... (sweep coverage declared per family ...)"; check list (c)
:768-773 item (2) "sweep-sup coverage declared per family"; the (vi)
BEST cell :1301 carries the same clause; PRECEDENT IN-FILE:
[REV2-r3-5] :1151-1169 — the floor certificate was tied to the
registered refinement ladder precisely because a DISCRETE scan
statistic (there a min, over-optimistic on a coarse mesh) must be
sustained under refinement to be a certificate.
REASON: the sweep-sup is the same defect class in the opposite
direction — a finite sample UNDER-estimates the continuum sup on the
basin, and eps_U enters the bound 2·sqrt(eps_U/mu) monotonically: an
under-estimated eps_U under-states the shift bound → overclaims
optimum coverage. "Coverage declared" names the samples but states no
acceptance rule; [REV2-r3-3](b)'s own discharge sentence ("until the
sweep sup is measured, the uniformity premise is OPEN") implies a
measured sweep-sup DISCHARGES the premise — with no stability clause,
discharge would be a sampling-density-dependent event, exactly what
[REV2-r3-5] exists to prevent for the floor. Nothing false is
asserted today (the premise is OPEN and declared) — hence AMENDMENT.
FIX SHAPE (one clause, mirroring [REV2-r3-5]; rides the same
§3.6/R-15 duty): "the measured sweep-sup is a sampled LOWER estimate
of the basin sup; the uniformity premise discharges only when the
sweep-sup is SUSTAINED under the declared sweep-refinement re-check
within its own band (refinement discipline = the same registered
ladder pattern as [REV2-r3-5]); a sup that grows under refinement =
premise stays OPEN, declared."

### R22F-L0-22 — NOTE: verified-at-source items this round (positive
### record for the judge; no action)

(a) Mandate-coverage duty: both mandated elements PRESENT (header
block above).
(b) [REV2-r3-1] carriage verified at ALL FIVE sites named by my L0-16
(H-G4, R-13, (F), (T)-definition backing, (vi) cell check list) — the
round-3 fix is complete as filed; the branch-wise coverage remark
(:724-733) re-derived by hand this window: margin > 2·eps_U on branch
A implies J_true(x_B) ≤ J_red(x_A*) − margin + eps_U < J_true(x_A*)
for every x_B on branch B — sound.
(c) [REV2-r3-2] S*_red evaluation pin verified in (B) and in the (vi)
cell; consistent with the VI chain's consumption point.
(d) [REV2-r3-6] in-place cell-edit protocol verified: all superseded
fragments quoted verbatim; no silent deletion; the printed (vi) row
matches the (a)-(c) descriptions.
(e) [REV2-r3-8] count correction re-verified by row count of §11:
seven AMENDMENT-typed rows (L0-13, L0-14, L1-8, L1-9, L1-10, L2-15,
L2-16) — 7/7 correct; the L2-12 NOTE row is now present.
(f) §12 disposition table checked against the actual edit sites for
the three L0 rows (L0-16/L0-17/L0-18) — accurate.
(g) [REV2-r3-7] headline fitted-sheet qualifier verified present at
the third site (:1480-1483); the "licensed phrasing" header is now
true of the headline.
(h) L0-19/L0-20 INTERACTION, stated for the judge: the two findings
are independent (PART B uses equal curvatures — no mu-identity
confound; PART A uses a convex set — no H-G5 confound) and their
fixes compose: H-G6 (curvature transfer) + the convex-branch H-G5
restriction leave a coherent licensed family in which the value route
runs on the measured carrier (J_red-side derivation) and the gradient
route awaits L_H alongside delta — both honest-SCHEMA outcomes per
addendum_c4 (b).

==============================================================================
## PAPERS NEEDED

NONE for this refutation. (All attacks close on carriers already on
disk; no procurement would change any finding above.)

## MACHINE SUMMARY (round 4, lens L0)

breaks = 1 (R22F-L0-19)
repairs = 1 (R22F-L0-20)
amendments = 1 (R22F-L0-21)
notes = 1 (R22F-L0-22)
persisting_prior = 0 (all round-3 L0 objections verified RESOLVED at
their edited sites; rounds 1-2 chains closed of record)
probe: r22f_v2_probe_r4_l0_mu_identity_proxreg.py — RUN, all asserts
PASS (PART A: mu functional-identity, 100x violation with every
printed check passing, corrected transfer form exact, value route
sound with measured carrier; PART B: prox-regular disjunct, 103x
form-(T) violation and a value-route false-certification with its
a-posteriori check passing, convex control tight); synthetic
counter-models only (CT-6 clean).
dry_at_this_lens: NO (1 BREAK + 1 REPAIR standing)
