# ESCALATED REFUTATION — MINOR (c) DELTA-CARRIER, ROUND 1
# S-FOUNDATIONS-C4 escalation window, 2026-08-20. ESCALATED REFUTER
# per VERDICT_blocco2.md §3 E-3 (full-form escalation; single fused
# lens, math structure + carrier/bookkeeping contracts, right-sized by
# the orchestrator). Target: phaseD_minor_deltacarrier.md AS REVISED
# (all [ESC-r1-0..9] markers). Inputs of record: VERDICT_blocco2 §2.3
# (8/8 sustained) + §4 (sequencing, BINDING) + refute_minor_
# deltacarrier.md. This file is the ONLY file this slot writes.
# Findings ESC-DELTACARRIER-r1-<n>, classes BREAK/REPAIR/AMENDMENT/
# NOTE. Zero inflation: everything not under FINDINGS was checked and
# found sound (VERIFIED-SOUND block). Probe re-run this window.

## READ-DEPTH DECLARATION (this window; all internal, no papers; CT-6
## kept — no number from the four nozzle papers consumed anywhere)

- phaseD_minor_deltacarrier.md [FULL] (whole revised file, 561 lines,
  every [ESC-r1-*] marker + disposition table §11);
- refute_minor_deltacarrier.md [FULL] (whole file);
- VERDICT_blocco2.md [FULL] (whole file; §§2.3/3/4 the mandate);
- phaseD_minor_objdom.md :280-449 [FULL slice] (the consumed OBJDOM-5
  [ESC-r1-3] block + P-1/F-1/F-2 protocol + §3 duties) + grep census
  of [ESC-r2-*]/[ESC-r3-*]/(H6') markers + disposition tables SS6-SS8
  rows read (r3 delta identified);
- esc_refute_objdom_r3.md tail [targeted] (DRY verdict, closure-judge
  ready — minor (b) loop state established before adjudicating the
  (c) consumption);
- docs/findings_registry.yaml :285-309 [FULL] (row :292-300; :300
  trigger re-verified character-exact this window);
- blocco3/SYNTHESIS_nozzle_rde_arrivals.md :276-289 (G-12) +
  :408-424 ((e).3) [FULL slices] (graft source re-verified);
- docs/rde_nozzle_P1_sections_5_7.md :72-94 [FULL slice] (T7(c)
  regime-2 free-sign text re-verified for [ESC-r1-7]);
- esc_probe_deltacarrier_h3breakout.py [FULL] (source audited line by
  line) + EXECUTED this window: PASS, exit 0 ([A] sup−p_lam =
  −4.731e-10 ≤ tol, aligned attainment exactly 0.0; [B] breakout
  exceedance +1.6075e-1 ≫ threshold). Re-runnable as claimed;
  pinned-env numpy only, no install.
- File mtimes measured this window (SR-12; command: stat -c '%y %n'):
  phaseD_minor_objdom.md 19:16:17 < esc_refute_objdom_r3.md 19:22:33
  < esc_probe_deltacarrier_h3breakout.py 19:25:24 <
  phaseD_minor_deltacarrier.md 19:29:27 (all 2026-08-20 −0400).

## VERIFIED-SOUND (attacked, held; listed so the closure judge sees
## coverage — all in THIS window)

1. [ESC-r1-4] Pa ≥ 0 repair: Pa-insensitivity of the DC-1 proof
   INDEPENDENTLY re-derived — step (i) constant Pa closes over the
   closed surface (true for any constant, a fortiori Pa ≥ 0 and the
   vacuum case Pa = 0); step (ii)'s CLAIM sup G = p(lambda) never
   mentions Pa; step (iii)'s −Pa·A_e appears identically on both
   sides of C_exit[W] ≤ C_exit^id. The vacuum objective (the engine's
   only executed one) is now covered; the DC-5(b) "same Pa
   convention, declared" line with the class-constant mixing error
   Pa·Int_wall n_x dA is correct. Sound. (Pa ≥ 0 is stronger than
   the proof needs — any constant works — but a non-minimal
   hypothesis in a THEOREM* is licensed, and Pa ≥ 0 is physical.)
2. [ESC-r1-1] (H6') two-branch form: character-consistent with the
   escalated minor (b) OBJDOM-5 [ESC-r1-3] block (:314-330 there,
   re-read at source) AND with VERDICT_blocco2 §4(2). Branch (H6'-A)
   = the row's REPAIRED state, branch (H6'-B) = the row's BANDED
   state — the :300 license correctly read. The same-domain clause
   correctly demoted to necessary-only. Sound.
3. [ESC-r1-2] ship-gate re-pin: registry :300 re-read at source this
   window — "sequenced with/after [OBJ-DOM]; ships only with the
   sliver banded or repaired" — the revision's quote is
   character-exact; the retracted "(fix-A or fix-B)" branch and false
   "verbatim" flag are quoted-as-retracted, never silently deleted.
   The re-pinned gate text matches VERDICT_blocco2 §4(2) verbatim.
   Sound.
4. [ESC-r1-1a] band direction: ceiling upper edge WORSENED (raised)
   by the net-panel band ⇒ delta quoted larger ⇒ conservative —
   direction logic re-checked, correct; no sign asserted (R5-clean).
5. [ESC-r1-3] W_cert restriction IN DC-2: the restated chain re-proved
   — A(c) ∩ W_cert ⊆ A(eps) ∩ W_cert (c-slot removal enlarges),
   DC-1 member-by-member on W_cert gives sup_{A(eps) ∩ W_cert} J ≤
   J_ideal(eps); Pi_c ≥ 0 and delta_true ≥ 0 both hold with S* in
   A(c) ∩ W_cert by its own certificate (stated). The falsifier's
   exhibit class correctly restricted. DC-2's own text is now sound
   (but see finding r1-1 for DC-3/DC-6 propagation).
6. [ESC-r1-3a] probe: source audited — isentrope leaf exact (c² =
   (γ−1)h verified; gamma_s = γ identity verified analytically and
   asserted in-probe); sonic q* = sqrt(2(γ−1)/(γ+1)) correct; both
   (ii.c) case branches exercised by the grid (q + c(q) < lambda
   occurs near q*, since q*+c* ≈ 1.155 < 1.2); half [A] attainment
   check exact-0 by IEEE product commutation; half [B] counter-state
   re-derived by hand: p(q_mid) = 0.6051^3.5 ≈ 0.1723 > p(lambda) =
   0.28^3.5 ≈ 0.0116, exceedance ≈ 0.1607 — matches the run to 4
   digits. Genuinely two-sided (either half can FAIL). PASS/FAIL
   only, no number of record. Sound (see r1-4 NOTE on the tol_A
   derivation scale).
7. [ESC-r1-5] G-12 graft: source re-verified at
   blocco3/SYNTHESIS_nozzle_rde_arrivals.md (e).3 :413-423 + G-12
   :280-283 — the grafted sentence is faithful, [ADV], CT-6-clean
   (no paper number in it), present at BOTH §3 and the §9 [T-DCRX]
   landing text; artifact-connectedness discharged. Sound.
8. [ESC-r1-6] equality rewording: DC-1 statement now attributes
   equality to the relaxation's maximizer STATE with in-class
   attainment explicitly not claimed; DC-3(c) "=" → "≤ with equality
   exactly on the uniform-exit relaxation"; downstream chain
   "Hence B1^c ≤ J_ideal(eps)" re-checked intact. Sound as wording.
9. [ESC-r1-7] dormancy: T7(c) source :79-84 re-read — "K = {point}
   ... lambda = components of D with free sign (the fixed-eps
   bookkeeping in which all executed instances live)" — the dormancy
   clause and the [DC-F2-3] contingency are faithful. Sound.
10. [ESC-r1-8] (H2b) ownership: check formula mdot/A_e < rho*·c* is
    the correct existence/uniqueness condition (rho·q strictly
    decreasing from rho*c* to 0 on the supersonic branch — re-derived);
    owner now DC-5(b) line + [DC-F2-5] report-on-fail. Sound.
11. Disposition table §11: 8/8 rows, every marker present in the body
    (grep-verified), every retraction quoted-as-retracted; guidance
    labels from VERDICT_blocco2 §2.3 correctly carried as NON-ADOPTED
    ([ESC-r1-9]); no "already landed" overreach (landing stays
    PROPOSED, gated on §4(3) incl. minor (b) closure). Compliant.
12. CT-6: no number from the four nozzle papers anywhere in the
    revision (checked; the only numbers are program record numbers
    with sources and the probe's nondimensional outputs).

## FINDINGS

### ESC-DELTACARRIER-r1-1 [REPAIR] — the W_cert repair is incompletely
propagated: DC-3(c)'s and DC-6's B_fam symbol keeps its UNRESTRICTED
definition, so the THEOREM-labeled display "B_fam({eps}) ≤
J_ideal_cycle(eps)" binds a sup over possibly-uncertified members —
the exact defect class MIN-DELTACARRIER-3 established — with the
rescue living only inside a revision-annotation bracket

Anchor: revised file §4 definition "B_fam(C) := Int_Xi sup_{S' in
A_phase(C)} F[S'; s(xi)] dmu(xi)" (unrestricted) vs DC-3(c) display
"B_fam({eps}) <= J_ideal_cycle(eps)"; the W_cert scoping appears ONLY
as a parenthetical inside the [ESC-r1-6] bracket ("per [ESC-r1-3] the
B_fam-vs-J_ideal comparison, where it invokes DC-1, is over certified
(W_cert) members"); also DC-6 ("B_fam({eps, L}) is attained by one
design") and the ladder wiring "Hence B1^c <= J_ideal(eps)".

Reason: DC-1 binds only W_cert members ([ESC-r1-3], correct). With
A_phase(C) defined by the geometric c-vector alone (same source as
DC-2's A(c), M0 D2.6 :250-266 — per-cell exit supersonicity is NOT a
slot), A_phase({eps}) may contain uncertified members, and for those
the pointwise bound genuinely fails (the probe's half [B]
counter-state: u → 0+ at q in (c*, lambda) gives G → p(q) >
p(lambda)). So the (c) display over the UNRESTRICTED symbol is
unproven in exactly the MIN-DELTACARRIER-3 sense. The revision's own
standard forbids this shape: [ESC-r1-3] retracted DC-2's prior
display because "a THEOREM*-class display may not leave it implicit"
— yet DC-3 carries a THEOREM label (under V1-V2) whose (c) leg
attaches the restriction not to the symbol's definition but to a
parenthetical inside an [ESC-*] annotation bracket, the file's
convention for revision rationale/retractions. A good-faith M0
landing extraction that consolidates or strips the [ESC-*] brackets
(they are round-bookkeeping, not statement text) ships DC-3(c) and
DC-6 with the unrestricted B_fam — the MIN-DELTACARRIER-2 exposure
pattern (good-faith executor, wrong contract).

Failure/consequence: [T-DCRX] lands with a THEOREM-labeled inequality
whose displayed symbols do not entail it; the DC-2 repair is then
undone one section downstream. NOT a BREAK: the restriction IS stated
explicitly in the current text, so no false claim ships today.

Named repair (one clause, conclusion-preserving — verified before
naming): restrict the FAMILY DEFINITION itself, B_fam(C) := Int_Xi
sup_{S' in A_phase(C) ∩ W_cert} F[S'; s(xi)] dmu (or mint
B_fam^cert), and move the certification remark into statement text:
(a) then requires S certified (true for every actual consumer — the
certified S*); (b) monotonicity unaffected; (c) becomes proven by
DC-1 member-by-member; the B1^c instantiations (Rao/Guderley-
Armitage/Kraiko per-phase constrained solves) are supersonic-exit by
construction, hence in W_cert — the author's own bracket already
argues this — so B1^c ≤ J_ideal(eps), delta^c, and DC-6's attainment
all stand unchanged. No conclusion moves for any existing or named
instance.

### ESC-DELTACARRIER-r1-2 [AMENDMENT] — the consumption declaration
mis-states the on-disk state of the consumed input: minor (b) already
carried [ESC-r3-*] when the (c) revision was written, but [ESC-r1-0]
and §11 declare it "as revised through [ESC-r1-*] and [ESC-r2-*]"

Anchor: revised file [ESC-r1-0] ("phaseD_minor_objdom.md (as revised
through [ESC-r1-*] and [ESC-r2-*])") and §11 ("grep-verified
[ESC-r1-*]/[ESC-r2-*] marker census (escalated state confirmed before
consumption)") vs the measured record THIS window: phaseD_minor_objdom
.md mtime 19:16:17 (the [ESC-r3-0]/[ESC-r3-1] revision, present at
:28-38, :128, SS8) < phaseD_minor_deltacarrier.md mtime 19:29:27,
both 2026-08-20 (stat command cited in READ-DEPTH; SR-12).

Reason: the (b) file on disk at the (c) revision's write time was the
ROUND-3 state; a census declared over [ESC-r1-*]/[ESC-r2-*] patterns
either missed the r3 markers or was declared incompletely. An
of-record read-depth/census claim that mis-states the consumed
input's version is the bookkeeping defect class this loop polices
(precedent: minor (b)'s own ESC-OBJDOM-r1-3, a false status sentence
with no math affected, classed AMENDMENT and fixed).

Substance verified UNAFFECTED this window (why this is not REPAIR/
BREAK): [ESC-r3-1] touches ONLY the OBJDOM-1 (a.2) truncation-order
IFF clause (p'(0)=0 sufficiency correction) at its three anchor
sites; the OBJDOM-5/(H6')/ship-gate block (:290-330) carries only
[ESC-r1-3] and is character-consistent with what (c) adopted;
esc_refute_objdom_r3.md is DRY (closure-judge ready). Nothing adopted
is stale.

Named repair (one line): correct [ESC-r1-0]/§11 to "as revised
through [ESC-r3-*]", citing that the r3 delta does not touch the
consumed (H6')/ship-gate block — and at (c)'s closure the judge
re-verifies the consumed block against (b)'s CLOSED text (if (b)'s
closure judge amends the (H6') form, this adoption re-opens — the
condition [ESC-r1-9] already gates on (b) closed; make the
re-verification explicit).

### ESC-DELTACARRIER-r1-3 [NOTE] — §3 grammatical seam left by the
[ESC-r1-3] insertion: a dangling modifier clause after the inserted
conditionality sentence

Anchor: revised file §3, the sequence: decomposition display (ending
"delta_true := ... >= 0 (true suboptimality),") → inserted sentence
"DC-1's THEOREM* conditionality is hereby carried EXPLICITLY ... is
untouched [ESC-r1-3]." → orphaned continuation "with the split NOT
separately computable today (that is exactly the bound-ladder row's
gap)."

Reason: the "with the split..." clause grammatically attached to the
display before the insertion; it now follows a period-terminated
inserted sentence and dangles. Meaning is recoverable by a human; a
machine-read landing extraction (this file's final text = landing
source for [T-DCRX]) can mis-attach the clause. Editorial only, no
statement moves. Repair: reflow (e.g. "The split is NOT separately
computable today ...") when the landing text is cut.

### ESC-DELTACARRIER-r1-4 [NOTE] — the probe's tol_A derivation uses
the wrong scale: the float-error budget of G's evaluation chain is
set by the ~18x larger intermediate terms, not by p(lambda)

Anchor: esc_probe_deltacarrier_h3breakout.py header ("slack = 64 *
eps * p(lambda) covers the ~10-flop evaluation chain") and line 68
(tol_A = 64.0 * eps * p_lam).

Reason: near the maximizer (lambda, lambda) the chain computes
rho·lam² ≈ 0.209 and −lam·rho·lam ≈ −0.209 against p_lam ≈ 0.0116 —
a cancellation whose absolute rounding noise is O(eps·0.21) ≈
2.1·eps-units for the ~10-flop chain, vs tol_A = 64·eps·p_lam ≈
0.74·eps-units: the stated derivation UNDER-covers by ~3x at the
correct scale. Not activated in the configured run: the grid's
discretization deficit (−4.7e-10, curvature-dominated) is ~6 orders
above the noise, and the attainment check is exactly 0 by IEEE
product commutation — the PASS of record is robust. The latent
failure mode is a FALSE FAIL (spurious rejection at a much finer
grid), i.e. the conservative direction for a rejector; no conclusion
is at risk. But R5 requires tolerances DERIVED, and this derivation
is wrong-as-stated. Repair (one line, if the probe is ever
re-gridded): tol_A = 64·eps·max(|rho(lam)|·lam², p_lam), or document
the grid-deficit-dominates argument as the actual guard.

## SEQUENCING CHECK (SR-C4-9, explicit)

VERDICT_blocco2 §4(1)-(3) compliance of the revision: VERIFIED. (H6')
adopted in the judge-ratified two-branch form, character-consistent
with the escalated (b) source and with §4(2); ship-gate = the
row-:300 license correctly read (both re-verified at source this
window); [ESC-r1-9] gates landing on (b)'s closure + §4(3). Residual
exposure named in finding r1-2 (re-verify the consumed block against
(b)'s CLOSED text at this item's closure); no inconsistency between
the two minors as revised — they now share one (H6')/ship-gate text.

## PAPERS NEEDED

(empty)

## MACHINE SUMMARY

findings: 0 BREAK, 1 REPAIR (r1-1: W_cert restriction not propagated
  into the B_fam definition — DC-3(c)/DC-6 display binds the
  unrestricted symbol, scoping lives only in an annotation bracket;
  one-clause fix named, conclusion-preserving), 1 AMENDMENT (r1-2:
  consumption declaration says (b) "through [ESC-r2-*]" while the
  on-disk (b) was already at [ESC-r3-*], mtime-proven; substance
  verified unaffected — r3 does not touch (H6')/ship-gate; (b) r3 =
  DRY), 2 NOTE (r1-3 §3 dangling clause, editorial; r1-4 probe tol_A
  derived at the wrong scale — latent false-FAIL only, configured
  PASS robust).
core_verdict: all eight applied repairs are SOUND where applied —
  (H6') form and ship-gate character-consistent with the escalated
  (b) and VERDICT_blocco2 §4(2); Pa ≥ 0 insensitivity independently
  re-derived; DC-2's W_cert chain re-proved; G-12 graft
  source-faithful and CT-6-clean; dormancy and (H2b) ownership
  faithful to their anchors; probe re-run PASS exit 0 and audited.
  The single content-bearing residue is the DC-3/DC-6 propagation
  gap (r1-1).
dry: FALSE (one REPAIR outstanding) — one more author pass on r1-1
  (+ the r1-2 line) should reach DRY; nothing found touches DC-1's
  proof, DC-2's restated chain, or any adopted (H6')/gate text.
probes: esc_probe_deltacarrier_h3breakout.py re-run this window,
  exit 0, outputs quoted in READ-DEPTH; re-runnable, pinned-env
  numpy only.
ct6: clean (this file consumes no paper number).
