# PHASE D MINOR (c) — DELTA-CARRIER FIXED-EXIT-AREA RELAXATION LEMMA
Author slot, S-FOUNDATIONS-C4 Blocco 1 (Phase D remainder), 2026-08-20.
Brief: BRIEF_blocco2_phaseD.md MINORS (c). ID prefix: DC-<n>; F2 duties
[DC-F2-<n>]. One refuter round + auto-escalation applies.

# [ESC-r1-0] ESCALATED AUTHOR-REVISION round 1 (2026-08-20), per
# VERDICT_blocco2.md §2.3 + §3 E-3 + §4 (sequencing, BINDING) and
# refute_minor_deltacarrier.md (8/8 SUSTAINED: 4 CONTENT, 2 WORDING,
# 2 NOTE). Revisions applied IN PLACE with markers [ESC-r1-<n>];
# retracted text is quoted as retracted, never silently deleted.
# SEQUENCING CONSUMED (VERDICT_blocco2 §4(2)-(3)): the ESCALATED minor
# (b) output phaseD_minor_objdom.md (as revised through [ESC-r1-*] and
# [ESC-r2-*]) [ESC-r2-2 CORRECTION, per ESC-DELTACARRIER-r1-2
# (AMENDMENT): the version clause "through [ESC-r1-*] and [ESC-r2-*]"
# was FALSE as a state declaration — the on-disk (b) at this revision's
# write time was already the ROUND-3 state (through [ESC-r3-*]:
# [ESC-r3-0]/[ESC-r3-1] present, mtime 19:16:17 < this file's 19:29:27,
# both 2026-08-20 −0400). Re-measured THIS window (SR-12; grep marker
# census + stat): (b) remains at [ESC-r3-*], esc_refute_objdom_r3.md =
# DRY (closure-judge ready). Substance UNAFFECTED, verified at source:
# the r3 delta ([ESC-r3-1]) touches ONLY the OBJDOM-1 (a.2)
# truncation-order IFF clause (p'(0)=0 sufficiency), while the consumed
# OBJDOM-5/(H6')/ship-gate block (:290-330 there) carries only
# [ESC-r1-3] and is character-identical to what was adopted below.
# CLOSURE DUTY (explicit, per the refuter's named repair): at THIS
# item's closure the judge RE-VERIFIES the consumed (H6')/ship-gate
# block against (b)'s CLOSED text; if (b)'s closure amends that block,
# this adoption RE-OPENS — the [ESC-r1-9] gate on (b)-closed already
# sequences this, now with the re-verification named.] is the source of
# the (H6') two-branch form and of the
# re-pinned ship-gate adopted below at [ESC-r1-1]/[ESC-r1-2] — its
# OBJDOM-5 rewrite ([ESC-r1-3] there) was read IN FULL this window and
# verified against VERDICT_blocco2 §4(2) before adoption. The G-12
# scope-context sentence (SYNTHESIS_nozzle_rde_arrivals.md (e).3
# :413-423 + G-12 :280-283, re-read at source this window) is a NAMED
# INPUT, grafted at [ESC-r1-5]. Every repair text was VERIFIED before
# adoption (see disposition table §11); the MIN-DELTACARRIER-3
# counter-state and the DC-1 admissible-set claim are additionally
# verified by executable probe esc_probe_deltacarrier_h3breakout.py
# (two-sided rejector, PASS exit 0 this window, pinned-env numpy only).

# [ESC-r2-0] ESCALATED AUTHOR-REVISION round 2 (2026-08-20), per the
# round-1 escalated refutation esc_refute_deltacarrier_r1.md (0 BREAK,
# 1 REPAIR, 1 AMENDMENT, 2 NOTE; all eight round-1 repairs judged
# VERIFIED-SOUND there). ALL FOUR findings addressed FIXED this round
# (none contested); revisions IN PLACE with markers [ESC-r2-<n>];
# retracted text quoted as retracted, never silently deleted.
# Dispositions: ESC-DELTACARRIER-r1-1 (REPAIR: W_cert restriction not
# propagated into the B_fam DEFINITION — DC-3(c)/DC-6 displayed the
# unrestricted symbol) → FIXED at [ESC-r2-1], the refuter's named
# one-clause repair adopted after independent re-verification (see the
# verification record in §12); r1-2 (AMENDMENT: consumption version
# clause mis-stated the on-disk (b) state) → FIXED at [ESC-r2-2] in the
# [ESC-r1-0] block and §11; r1-3 (NOTE: §3 dangling clause) → FIXED at
# [ESC-r2-3]; r1-4 (NOTE: probe tol_A derived at the wrong scale) →
# FIXED at [ESC-r2-4] IN THE PROBE ITSELF (tol_A re-derived at the
# largest-intermediate scale, re-run THIS window: PASS exit 0,
# sup−p_lam = −4.731e-10 ≤ tol_A-independent grid deficit, aligned
# attainment exactly 0.0, breakout exceedance +1.6075e-1). Round-2
# disposition table appended at EOF (§12).

# [ESC-r3-0] ESCALATED AUTHOR-REVISION round 3 (2026-08-20), per the
# round-2 escalated refutation esc_refute_deltacarrier_r2.md (0 BREAK,
# 1 REPAIR, 1 AMENDMENT, 1 NOTE; all four round-2 repairs judged
# VERIFIED-SOUND there). ALL THREE findings addressed FIXED this round
# (none contested); revisions IN PLACE with markers [ESC-r3-<n>];
# retracted text quoted as retracted, never silently deleted.
# MARKER DISAMBIGUATION: [ESC-r3-<n>] markers in THIS file are this
# file's own round-3 markers; the [ESC-r3-*] strings inside the
# [ESC-r1-0]/[ESC-r2-2] blocks above QUOTE minor (b)'s marker state
# (phaseD_minor_objdom.md) and are not markers of this file — a census
# of this file must read the marker sites, not bare string counts.
# Dispositions: ESC-DELTACARRIER-r2-1 (REPAIR: "supersonic-exit by
# construction, hence in W_cert" is an invalid inference — supersonic
# SPEED q > c does not give (H3) AXIAL supersonicity u_x > c, and the
# probe's own half [B] counter-state is a supersonic-speed cell
# breaking the bound) → FIXED at [ESC-r3-1a..g]: the refuter's named
# repair adopted after independent re-verification (membership becomes
# an a-posteriori W_cert CERTIFICATE, M cosθ > 1 per exit cell, same
# S1 margin check as every consumer; [DC-F2-2] gains the
# certificate-on-entry duty; DC-6 attainment made conditional; the
# relaxation-STATE dropped from the family-instantiation list per
# [ESC-r1-6]); r2-2 (AMENDMENT: W_cert phase-quantifier unpinned) →
# FIXED at [ESC-r3-2a/b] (mu-a.e.-all-phase pin in §3 + DC-1
# MU-INTEGRATED FORM clause); r2-3 (NOTE: M0 D2.6 "Pa > 0" vs [T-DCRX]
# "Pa >= 0" landing-page adjacency) → FIXED at [ESC-r3-3] (one
# landing-text sentence naming the deliberate widening and its
# [OBJ-DOM] owner). SEQUENCING RE-MEASURED THIS WINDOW (SR-12; grep -c
# + stat): (b) phaseD_minor_objdom.md census [ESC-r1-*] 30 /
# [ESC-r2-*] 18 / [ESC-r3-*] 6 / [ESC-r4-*] 0, mtime 19:16:17
# UNCHANGED since the r2 refutation's measurement; NO
# esc_refute_objdom_r4.md exists; esc_refute_objdom_r3.md (19:22:33) =
# DRY, closure-judge ready, (b) NOT closed — the consumed
# (H6')/ship-gate block is bit-identical to what rounds 1-2 verified
# (same mtime ⇒ same bytes), the [ESC-r1-0] closure re-verification
# duty remains LIVE, and nothing lands yet ([ESC-r1-9]). Probe
# esc_probe_deltacarrier_h3breakout.py UNCHANGED (no finding touches
# it) and re-run this window: PASS exit 0 ([A] sup−p_lam = −4.731e-10,
# tol_A = 2.971e-15, aligned attainment exactly 0.0; [B] breakout
# q = 0.888675, exceedance +1.607546e-1 vs required 2.971e-12 — every
# digit matches §2/§12). Round-3 disposition table appended at EOF
# (§13).

## 0. Mandate, inputs of record, seams (all read IN FULL at cited anchors)

- Row `pipeline:delta-carrier-F2-entry` (docs/findings_registry.yaml:292-300):
  fixed-exit-area relaxation lemma to M0 with rigor class; delta semantics
  of record = "distance to the L-unconstrained fixed-eps ceiling"; trigger
  "sequenced with/after [OBJ-DOM]; ships only with the sliver banded or
  repaired". Sequence enforcement = the Blocco-2 judge (brief text).
- Row `bound-ladder:constraint-aware-rungs-missing`
  (docs/findings_registry.yaml:1669-1677): shipped rungs constraint-blind;
  delta = B − J[S*] valid a fortiori but conflates suboptimality with the
  PRICE OF THE CONSTRAINTS; named computable refinements = (1) B1^c
  per-phase classical constrained rung (Rao exact in the
  irrotational-homentropic subclass; Guderley-Armitage/Kraiko variants),
  T3 collapse = attainment of B1^c under pressure similarity; (2) KKT
  weak-duality rung from the engine's own multipliers.
- Row `variational-driver:objective-omits-throat-panel` ([OBJ-DOM],
  docs/findings_registry.yaml:204-213): gradient axis governs (5.8e2x);
  the coded J omits the throat panel — the delta carrier inherits the
  sliver unless fix-A/fix-B lands or the sliver is banded.
- Source authority (not re-litigated): ADVISORY_S25_pipeline_sense_
  CONVERGED_2026-08-12.md §3.2 (R2 ratified with refuter corrections 1-5:
  proof obligation = THIS lemma, not the truncation dF/dA argument; delta
  wording; ceiling qualifications, lip/eps residual 4.4938e-3 at the twin;
  sequencing; hypothesis list) and §6 row "Delta carrier".
- M0 context consumed: D2.6 (P) constraint vector c (rde_nozzle_MASTER.md:250-266);
  bound ladder in (iv) (:349-356); [T-EAP]/OP-0 sonic-cap sharpening block
  (:1255-1330); T7(c) cone form + fixed-eps bookkeeping regime 2
  (rde_nozzle_P1_sections_5_7.md:72-94).
- No VERDICT_wave3/wave2 seam is named for item (c) in the brief; the
  binding seams here are [OBJ-DOM] sequencing and the bound-ladder row.

LOAD-CLASS declaration (AG-1 valve, user-ratified 2026-08-20): DC-1/DC-2
are load-bearing — proven properly below. DC-3/DC-4 rung bookkeeping is
gap-accounting — strong trivially-checkable sufficient hypotheses,
sufficient-not-optimized, declared as such.

## 1. Setting and hypotheses (per phase xi; explicit list)

Class W of per-phase designs (the engine's S1 per-phase class, bell/TOC
exit topology), sharing:

- (H1) SHARED-SAUER DATA CONTRACT: same transonic data (Sauer IVL), same
  throat area A_t, same per-phase mass flow mdot, same per-phase
  stagnation state (h0, s0) with the flow irrotational-homentropic and
  shock-free (S1) downstream of the IVL; hence h = h0 − q^2/2 and the
  state lies on the (h0, s0) isentrope, and C_IVL (the momentum-flux +
  gauge-pressure integral over the IVL) is W-independent.
- (H2) FIXED EXIT AREA: exit disk = flat disk of area A_e = eps·A_t at
  the lip station, bounded by the wall lip; control volume boundary =
  {IVL, wall, axis, exit disk}, no other openings (bell topology;
  annular/plug exit NOT covered — declared scope limit).
- (H2b) mdot / A_e < rho* c* (rho*, c* = sonic state on the (h0, s0)
  isentrope). [For eps > 1 this holds up to the Sauer-vs-1D mdot
  correction; checkable per design.]
- (H3) PER-CELL AXIAL SUPERSONICITY on the exit disk: u_x − c > 0 at
  every point (the in-class license; certified a posteriori per design
  by the S1 margin machinery).
- (H4) Constant ambient Pa >= 0; objective = per-phase wall thrust
  J[W] = Int_wall (p − Pa) n_x dA ([T-TH0] definition restricted per
  phase), steady per-phase Euler. [ESC-r1-4: repaired per
  MIN-DELTACARRIER-4 (SUSTAINED). Prior text "Pa > 0 ... wall gauge
  thrust" RETRACTED as hypothesis: strict Pa > 0 excluded the VACUUM
  objective (Pa = 0), which is the ONLY objective the executed engine
  computes (phaseD_minor_objdom.md OBJDOM-3(b)/OBJDOM-4 ground 4 of
  record: "no Pa term exists anywhere"), so the lemma's hypothesis
  excluded its only intended instances. Verified before adoption: the
  proof is Pa-insensitive — Pa enters step (i) only as a constant
  integrating to zero over the closed surface (true for ANY constant
  Pa >= 0 including 0), and in step (iii) the −Pa·A_e term appears
  identically on both sides of C_exit[W] <= C_exit^id; the pointwise
  CLAIM sup G = p(lambda) never mentions Pa. Pa = 0 = vacuum case,
  Pa > 0 = gauge case, one statement covers both; the CONVENTION used
  must be declared in the carrier — see DC-5(b) as extended.]
- (H5) EOS: smooth strictly-decreasing isentrope maps q -> (p, rho, c)
  via dh = −q dq, dp = rho dh, drho = (rho/c^2) dh; and
- (H5b) gamma_s := rho c^2 / p >= 1 along the isentrope (true for
  thermally-perfect frozen mixtures; trivially table-checkable).
- (H6') OBJECTIVE-DOMAIN SEAM (binding, [OBJ-DOM]) [ESC-r1-1: (H6)
  REWRITTEN per MIN-DELTACARRIER-1 (SUSTAINED) and the judge's
  sequencing verdict (VERDICT_blocco2 §4(1)-(2)); form consumed from
  the ESCALATED minor (b) OBJDOM-5 rewrite (phaseD_minor_objdom.md
  [ESC-r1-3] there), read in full and verified this window. RETRACTED:
  the prior (H6) "the delta carrier evaluates ceiling and J[S*] on the
  SAME objective domain — it ships only after the [OBJ-DOM] fix lands
  (fix-A or fix-B) or with the throat-panel sliver banded into the
  carrier (row trigger verbatim)". That clause instantiated the
  same-convention hypothesis MIN-OBJDOM-3 killed: the two sides of
  delta are evaluated on DIFFERENT walls (ideal-march ceiling contour
  vs TOC design wall, thB_ideal != thB_TOC, th1 further split by the
  ceil-seam n_B = ceil(thB/da)), so a shared coded (panel-omitting)
  convention leaves the NET contamination DJ_panel(ideal) −
  DJ_panel(TOC) un-banded, in the ANTI-CONSERVATIVE direction
  (advisory §3.2 item 3). Same-domain survives only as a NECESSARY
  condition, never sufficient.] The carrier ships under one of TWO
  branches, and DC-5(b) prints WHICH:
    (H6'-A) the fix-A panel-inclusive functional is used on BOTH
      sides of delta (ceiling march AND J[S*]; [OBJ-DOM-IMPL] landed
      and used on both sides — the clean branch, consistent with
      OBJDOM-4's adjudication of fix-A as the F2-entry objective of
      record); OR
    (H6'-B) both sides use the coded (panel-omitting) functional AND
      an EXPLICIT net-two-wall-panel band on |DJ_panel(ideal) −
      DJ_panel(TOC)| is included in the carrier, its anti-conservative
      sign declared, folded into the DC-5(a) upper-edge rule (delta
      quoted against the ceiling edge worsened by the band).
  [ESC-r1-2: ship-gate re-pinned per MIN-DELTACARRIER-2 (SUSTAINED).
  RETRACTED: the "(fix-A or fix-B)" branch and its false "(row
  trigger verbatim)" flag — fix-B is a scoping declaration, not a
  repair, and the registry row licenses only BANDED or REPAIRED
  states. Row text, character-exact (docs/findings_registry.yaml:300,
  re-read at source this window): "sequenced with/after [OBJ-DOM];
  ships only with the sliver banded or repaired". Ship-gate of
  record (VERDICT_blocco2 §4(2), = the row license correctly read):
  no (value, delta) row ships unless "[OBJ-DOM-IMPL] landed and used
  on both sides, OR the net-panel band included". Fix-B-landed alone
  ships NOTHING.]

Define lambda = q_e(eps) as the unique supersonic root of
rho(lambda)·lambda = mdot/A_e on the (h0, s0) isentrope (exists and is
unique under (H2b) since d(rho q)/dq = rho(1 − M^2) < 0 on the
supersonic branch, rho q decreasing from rho* c* to 0); M_e(eps) =
lambda/c(lambda) > 1 is the ideal march's uniform exit Mach. Define
J_ideal(eps) := C_exit^id − C_IVL with C_exit^id = A_e[rho(lambda)
lambda^2 + p(lambda) − Pa] the uniform, axially-aligned exit state.

## 2. DC-1 — FIXED-EXIT-AREA RELAXATION LEMMA [rigor: THEOREM*]

STATEMENT. Under (H1)-(H5b), for every design W in the class:
J[W] <= J_ideal(eps), with equality attained by the uniform
axially-aligned exit STATE at M_e(eps) (the relaxation's maximizer;
in-class attainment NOT claimed — no finite-length class member need
realize it, and the ceiling may be strict on the class; a ceiling
needs no attainment) [ESC-r1-6: reworded per MIN-DELTACARRIER-6
(SUSTAINED); prior "with equality for the uniform axially-aligned
exit" read as in-class attainment, i.e. sup_W J = J_ideal, which is
not proven]. Consequently J_ideal(eps) is the L-UNCONSTRAINED
FIXED-EPS CEILING: it binds every class member regardless of length or
any other c-slot.

PROOF.
(i) Momentum-theorem decomposition. Steady per-phase momentum theorem on
the control volume of (H2), with the axis contributing nothing
(symmetry) and constant Pa integrating to zero over the closed surface:
J[W] = C_exit[W] − C_IVL, where
C_exit[W] = Int_{A_e} [rho u_x^2 + (p − Pa)] dA. By (H1), C_IVL is
common to the class, so it suffices to bound C_exit.

(ii) Pointwise Lagrangian bound. For the multiplier lambda of §1 and any
exit field satisfying the mass constraint Int_{A_e} rho u_x dA = mdot
(steady continuity + shared mdot, (H1)):
C_exit[W] − lambda·mdot = Int_{A_e} Phi dA,
Phi := rho u_x^2 + p − Pa − lambda rho u_x,
with the local state on the (h0, s0) isentrope (H1), parametrized by
speed q and axial component u := u_x in (c(q), q] (H3 forces q > c(q),
i.e., only supersonic-branch q are admissible). Write
G(q, u) := rho(q) u^2 − lambda rho(q) u + p(q), so Phi = G − Pa.

CLAIM: sup over admissible states of G = p(lambda), attained at
(q, u) = (lambda, lambda) (aligned, theta = 0, q = lambda).

(ii.a) G is convex in u (rho > 0), so its max over u in the closed
interval [c(q), q] is at an endpoint: u = q (aligned) or u = c(q)
(axially-sonic closure boundary; sup over the open class interval is
bounded by the closed one).

(ii.b) Aligned endpoint. A(q) := G(q, q) = rho(q^2 − lambda q) + p.
Using (H5): dp/dq = −rho q, drho/dq = −rho q/c^2, one computes
A'(q) = rho (q − lambda)(1 − M^2). On the supersonic branch
(1 − M^2) < 0, so A increases for q < lambda, decreases for q > lambda:
global branch max A(lambda) = p(lambda). (Endpoints: A(c*) below by
monotonicity; A -> 0 <= p(lambda) as q -> q_max = sqrt(2 h0).)

(ii.c) Axially-sonic endpoint. B(q) := G(q, c(q)) = rho c^2 −
lambda rho c + p. Two cases, exhaustive:
- Case q + c(q) >= lambda: B(q) − A(q) = rho (c − q)(c + q − lambda)
  <= 0 (c − q < 0, second factor >= 0), so B(q) <= A(q) <= p(lambda).
- Case q + c(q) < lambda: then c − lambda < −q, so
  rho c (c − lambda) < −rho c q, hence
  B(q) < p − rho c q < p − rho c^2 = p(1 − gamma_s) <= 0 < p(lambda),
  using q > c (H3) and gamma_s >= 1 (H5b).
In all cases G <= p(lambda) on the admissible set, proving the CLAIM;
the aligned state at q = lambda attains it.

(iii) Assembly (exact weak duality). C_exit[W] <= lambda·mdot +
A_e (p(lambda) − Pa). The uniform aligned exit at M_e(eps) satisfies
the mass constraint by the definition of lambda and attains equality:
C_exit^id = lambda·mdot + A_e (p(lambda) − Pa). Hence
C_exit[W] <= C_exit^id and J[W] <= J_ideal(eps). QED.

MU-INTEGRATED FORM (immediate): with phase-indexed data (mdot, h0, s0)
= s(xi) and phase-shared eps, and with W satisfying (H1)-(H5b) at
mu-a.e. xi [ESC-r3-2b, per ESC-DELTACARRIER-r2-2: the pointwise
domination needs the hypotheses at mu-a.e. xi — previously implicit;
phase-quantifier pin of record at [ESC-r3-2a] in §3],
J_cycle[W] = Int_Xi J[W; s(xi)] dmu <=
Int_Xi J_ideal(eps; s(xi)) dmu =: J_ideal_cycle(eps), by pointwise
domination; measurability of xi -> J_ideal(eps; s(xi)) from continuity
of the monotone root lambda(xi) in the data (R3 measurability chain).

WHY THEOREM* AND NOT THEOREM (declared conditionals): (a) (H3) is an
a-posteriori per-design certification (S1 margin), so the lemma binds
design-by-design conditional on that certificate; (b) bell-only exit
topology (H2) — the annular/plug variant is not derived here; (c) the
momentum-theorem step assumes the class's piecewise-C1 shock-free
fields (S1). Within (H1)-(H5b) the proof is complete; no gap is known.
This discharges advisory §3.2 correction 1: the truncation dF/dA
argument is NOT used anywhere above; the bound covers non-uniform-exit
TOC designs directly.

FALSIFIER (DC-1): (numerical) any certified class member with
(H1)-(H5b) verified and computed J[W] > J_ideal(eps) + ceiling band
kills the lemma — executable at the twin (TOC run vs ideal march, same
eps, same tables, same Sauer IVL); (analytic) any admissible exit state
(q, u) with G(q, u) > p(lambda) kills step (ii). A rejector that
artificially violates (H3) (subsonic-axial cells) must be ABLE to break
the bound — if it cannot, the hypothesis list is not load-bearing as
claimed and the lemma statement is suspect.
[ESC-r1-3a: the breakout demand is now DISCHARGED executable —
esc_probe_deltacarrier_h3breakout.py (this folder; pinned-env numpy
only; two-sided rejector, PASS exit 0 this window): half [A] verifies
sup G <= p(lambda) on the (H3)-admissible grid with exact aligned
attainment (derived float slack, no magic tolerance); half [B]
realizes the refuter's analytic counter-state — (H3) dropped, q in
(c*, lambda), u -> 0+ gives G -> p(q) > p(lambda), macroscopic
exceedance — confirming the hypothesis list IS load-bearing exactly
as the falsifier demands. Nondimensional model leaf; PASS/FAIL only,
no number of record.]
[ESC-r2-4, per ESC-DELTACARRIER-r1-4 (NOTE, FIXED IN THE PROBE): the
probe's tol_A derivation was wrong-as-stated — it budgeted the
~10-flop chain at the scale of the RESULT p(lambda), while near the
maximizer the chain's intermediates rho·lam² and −lam·rho·lam are
~18x larger and their cancellation sets the absolute rounding noise
(the R5 "tolerances derived" rule violated at the derivation, though
the configured PASS was robust: grid deficit ~6 orders above the
noise, attainment exact-0 by IEEE product commutation; latent mode =
FALSE FAIL at a much finer grid, the conservative direction). The
refuter's named repair adopted: tol_A = 64·eps·max(rho(lambda)·
lambda², p(lambda)), derivation comment rewritten in the probe;
re-run THIS window: PASS exit 0 (sup−p_lam = −4.731e-10, aligned
attainment exactly 0.0, breakout exceedance +1.6075e-1 vs required
2.971e-12). No conclusion moves.]

## 3. DC-2 — DELTA SEMANTICS OF RECORD [rigor: THEOREM* (inherits DC-1)]

Let A(c) be the admissible class of (P) (full constraint vector c =
(L, eps_max, L_p, curvature/angle, symmetry), M0 D2.6) and A(eps) the
same class with every c-slot except the exit-area/eps slot (and the
class-defining regularity) removed. [ESC-r1-3: restricted per
MIN-DELTACARRIER-3 (SUSTAINED). Define W_cert := the certified
subclass, i.e. the members satisfying (H1)-(H5b) of §1 — in
particular (H3) per-cell exit axial supersonicity, certified a
posteriori by the S1 margin machinery.
[ESC-r3-2a, PHASE-QUANTIFIER PIN, per ESC-DELTACARRIER-r2-2
(AMENDMENT, FIXED): (H2b)/(H3) are PER-PHASE predicates (they depend
on the phase data s(xi) = (mdot, h0, s0)), so "satisfying (H1)-(H5b)"
must be phase-quantified. PIN OF RECORD: W_cert membership is
evaluated at the phase data in play; in cycle-integrated displays
(the mu-integrated forms of DC-1, the B_fam(C) definition of §4, and
DC-3) W_cert denotes the mu-a.e.-ALL-PHASE certified subclass —
equivalently, write W_cert(xi) inside the integral; both readings
agree for every named consumer (the certified S*, the classical
solves under their [ESC-r3-1a] certificates), all of which are
certified at every phase, so no conclusion moves under either. A
MIXED reading (S certified at one phase only, read into a
xi-independent symbol) is hereby EXCLUDED — this pin makes explicit
for W_cert what (V1) already states for A_phase(C).] DC-1 is proved design-by-design
UNDER those hypotheses, so a class-level sup is licensed only over the
certified subclass: the M0 D2.6 c-vector is geometric and per-cell
exit supersonicity is NOT one of its slots, hence A(c) may contain
uncertified members for which the pointwise bound genuinely fails
(verified counter-state: (H3) dropped, exit cell with u = u_x -> 0+
at q in (c*, lambda) gives G -> p(q) > p(lambda); executable at
esc_probe_deltacarrier_h3breakout.py half [B], PASS this window).
The prior display's sups over bare A(c)/A(eps) — where the
parenthetical "(and the class-defining regularity)" silently carried
this entire restriction — are RETRACTED as written; a THEOREM*-class
display may not leave it implicit.] Then A(c) ∩ W_cert ⊆ A(eps) ∩
W_cert and, by DC-1 applied member-by-member on W_cert,
    sup_{A(c) ∩ W_cert} J <= sup_{A(eps) ∩ W_cert} J <= J_ideal(eps).
Hence for any certified J[S*] (S* in A(c) ∩ W_cert by its own
certificate):
    delta := J_ideal(eps) − J[S*]  >=  sup_{A(c) ∩ W_cert} J − J[S*]  >= 0,
and delta admits the exact (definitional) decomposition
    delta = Pi_c + delta_true,
    Pi_c := J_ideal(eps) − sup_{A(c) ∩ W_cert} J >= 0   (constraint price),
    delta_true := sup_{A(c) ∩ W_cert} J − J[S*] >= 0    (true suboptimality),
DC-1's THEOREM* conditionality is hereby carried EXPLICITLY into
DC-2's statement: the decomposition holds over the certified subclass
and only there; delta := J_ideal − J[S*] >= 0 for the certified S* is
untouched [ESC-r1-3].
[ESC-r2-3, per ESC-DELTACARRIER-r1-3 (NOTE, FIXED): the fragment
"with the split NOT separately computable today (that is exactly the
bound-ladder row's gap)." dangled after the [ESC-r1-3] insertion (it
had attached grammatically to the decomposition display); reflowed as
a full sentence, no content change:] The split (Pi_c vs delta_true) is
NOT separately computable today — that is exactly the bound-ladder
row's gap. SEMANTIC RULE (of record, advisory §3.2.2
verbatim): every (value, delta) Verdict row says "distance to the
L-unconstrained fixed-eps ceiling" — NEVER "distance to global at
(eps, L)". Under tight L/eps_max, delta can be dominated by Pi_c and go
uselessly loose while the design is near-optimal (the user catch
2026-08-17): the number stays TRUE, only the loose direction is
declared.

FALSIFIER (DC-2): exhibit S in A(c) ∩ W_cert with J[S] > J_ideal(eps)
(breaks the inclusion chain, hence DC-1) [ESC-r1-3: exhibit class
restricted — an uncertified exhibit breaks nothing, exactly as the
probe's half [B] shows]; or a shipped Verdict row wording "distance to
global at (eps, L)" (semantic violation, grep-detectable).

[ESC-r1-5, SCOPE CONTEXT at the delta-semantics site — G-12 NAMED
INPUT, grafted per MIN-DELTACARRIER-5 (SUSTAINED), verified at source
this window (SYNTHESIS_nozzle_rde_arrivals.md (e).3 :413-423, G-12
:280-283); [ADV] provenance, CT-6-clean, no paper number enters any
bound: J_ideal(eps) prices designs INSIDE the fixed-interface data
class (H1); constriction/choking/exit-area moves that rewrite the
chamber state are CLASS-CHANGING and outside what delta prices — the
largest published performance lever lives there, and pricing it is
exactly CFD-1's irreducible core (centerpiece §4.1). No statement of
this file changes under this note (G-12: "no statement change"); (H1)
is the premise doing the exclusion.]

## 4. DC-3 — COMPOSITION WITH THE CONSTRAINT-AWARE RUNG FAMILY
[rigor: THEOREM under valve hypotheses V1-V2; bookkeeping = gap-accounting, AG-1 valve applied, sufficient-not-optimized]

Define the relaxation family, for a constraint subset C of the c-slots
(always containing the eps slot and the class regularity)
[ESC-r2-1: DEFINITION restated per ESC-DELTACARRIER-r1-1 (REPAIR,
FIXED). The prior display "B_fam(C) := Int_Xi sup_{S' in A_phase(C)}
F[S'; s(xi)] dmu(xi)" (unrestricted sup) is RETRACTED as the symbol's
definition: DC-1 binds only certified members ([ESC-r1-3]), the M0
D2.6 c-vector is geometric and per-cell exit supersonicity is not one
of its slots, so the unrestricted sup may range over members for which
the pointwise bound genuinely fails (probe half [B]) — leaving the
W_cert scoping only inside an [ESC-*] annotation bracket was exactly
the MIN-DELTACARRIER-3 exposure one section downstream. The
restriction now lives in the DEFINITION itself, statement text]:
    B_fam(C) := Int_Xi  sup_{S' in A_phase(C) ∩ W_cert} F[S'; s(xi)]  dmu(xi),
the per-phase-relaxed CERTIFIED ceiling at constraint set C (W_cert =
the (H1)-(H5b) certified subclass of §3/DC-2, phase-quantified per
[ESC-r3-2a]; the certification is part
of the family's definition, not a side remark).
[ESC-r3-1a, per ESC-DELTACARRIER-r2-1 (REPAIR, FIXED). RETRACTED as
written: "Every named instantiation of the family below (uniform-exit
relaxation member; Rao/Guderley-Armitage/Kraiko per-phase constrained
solves) is supersonic-exit by construction, hence in W_cert — no
consumer of the family loses a member under this restriction." The
inference "supersonic-exit ⟹ in W_cert" is INVALID: W_cert requires
(H3) per-cell AXIAL supersonicity u_x − c > 0, not supersonic SPEED
q > c, and this file's own probe half [B] counter-state (q_mid =
0.888675 > c = 0.4920 on the (H1) isentrope, u_x = 1e-6, exceedance
+1.61e-1) is exactly a supersonic-speed cell that breaks the bound —
the conflation the probe was built to kill. Secondary defect: the
list lumped the uniform-exit relaxation MEMBER, which [ESC-r1-6]
explicitly declines to claim as an in-class member (it is the
relaxation's maximizer STATE, outside the sup entirely), re-importing
the attainment ambiguity [ESC-r1-6] removed. Repaired statement
(refuter's named repair, verified before adoption — AG-1-compliant: a
strong trivially-checkable hypothesis replacing an unproven
fact-claim):] Every named class-member instantiation of the family
below (the Rao/Guderley-Armitage/Kraiko per-phase constrained solves)
has axially-supersonic exit per-cell (u_x > c, i.e. M cosθ > 1 —
generically satisfied at classical design exit Mach/angles, e.g.
M_e ≥ 2 with lip angles ≲ 15° gives M cosθ ≥ 1.9, and certified
a-posteriori by the same S1 margin check as every consumer), hence in
W_cert UPON THAT CERTIFICATE; membership is a checkable a-posteriori
certificate, never a construction fact, and the [DC-F2-2] ladder duty
carries the certificate-on-entry rule ([ESC-r3-1d]). The uniform-exit
relaxation maximizer is a STATE, not a family instantiation — it sits
outside the sup and needs no membership ([ESC-r1-6]). No conclusion
moves for any CERTIFIED instance, and every existing consumer is
certified.

(V1) [trivially-checkable sufficient hypothesis] The per-phase
constraint set A_phase(C) is xi-independent (true: one shape serves all
phases; c is geometric and phase-shared).
(V2) [sufficient, declared] xi -> sup_{A_phase(C) ∩ W_cert} F[·; s(xi)]
is mu-measurable [ESC-r2-1: sup restated over the restricted set]
(satisfied when the per-phase sup is attained by a solve continuous in
the data — Rao/ideal-march solves are, and they are W_cert members
upon the [ESC-r3-1a] a-posteriori certificate [ESC-r3-1b: prior
"supersonic-exit certified members" reworded — same conflation as
[ESC-r3-1a], same certificate fix]; declared
sufficient-not-optimized).

THEOREM (under V1-V2). (a) For any single CERTIFIED S (S in W_cert)
feasible for all phases [ESC-r2-1: certification hypothesis moved into
statement text — every actual consumer is the certified S*, so no
instance is lost], F[S; s(xi)] <= sup_{S' in A_phase(C) ∩ W_cert}
F[S'; s(xi)] pointwise; integrating,
J_cycle[S] <= B_fam(C) — NO sup/integral interchange theorem is needed
(each phase is bounded by its own sup). (b) B_fam is monotone
non-increasing in C under inclusion (sup over a smaller set; the ∩
W_cert factor is C-independent, so monotonicity is unaffected
[ESC-r2-1]). (c) The
DC-1 ceiling dominates the C = {eps} member: B_fam({eps}) <=
J_ideal_cycle(eps) — now proven directly by DC-1 applied
member-by-member on W_cert (every member of A_phase({eps}) ∩ W_cert
satisfies (H1)-(H5b) by definition, so J[S'; s(xi)] <= J_ideal(eps;
s(xi)) pointwise in xi; integrate) [ESC-r2-1: with the restriction in
the definition the (c) display is entailed by its own symbols; the
certification remark no longer lives only in the annotation bracket
below] — with equality exactly on the uniform-exit
relaxation [ESC-r1-6: repaired per MIN-DELTACARRIER-6 (SUSTAINED);
prior "J_ideal_cycle(eps) = B_fam({eps}) restricted to the
uniform-exit relaxation" RETRACTED — an "=" carrying a "restricted
to" qualifier is not an identity, and B_fam({eps}) (sup over
realizable per-phase designs) may be strictly below the relaxation
ceiling. The inequality is what the chain uses; per [ESC-r1-3] the
B_fam-vs-J_ideal comparison, where it invokes DC-1, is over
certified (W_cert) members — the classical constrained solves (Rao/
Guderley-Armitage/Kraiko) are supersonic-exit by construction]
[ESC-r2-1: this bracket's scoping parenthetical is now REDUNDANT
rationale — the restriction has been promoted into the B_fam
definition and the (c) statement text above; kept as round-1 history,
no longer load-bearing] [ESC-r3-1g: the historical bracket's closing
"are supersonic-exit by construction" is the same invalid inference
retracted at [ESC-r3-1a] — it stands only as quoted round-1 history;
the operative membership claim is the [ESC-r3-1c] certificate form
below]; the
B1^c rung of the
bound-ladder row is the C = {eps, L} member (per-phase CLASSICAL
CONSTRAINED solves: Rao exact length/exit-area-constrained optimum in
the irrotational-homentropic subclass; Guderley-Armitage/Kraiko
variants for other combos — [ESC-r3-1c: prior "supersonic-exit by
construction, hence members of the restricted family [ESC-r2-1]"
RETRACTED, invalid inference per ESC-DELTACARRIER-r2-1:] members of
the restricted family UPON their per-cell axial-supersonicity
certificate (M cosθ > 1, the [ESC-r3-1a] S1 margin check); the B1^c
solves enter the ladder only WITH that W_cert certificate,
report-on-fail — [DC-F2-2] duty [ESC-r3-1d]). Hence B1^c <=
J_ideal(eps) over the certified entries — the sup over a subset of
W_cert-certified members is bounded by the {eps}-member ceiling
regardless of any membership prose: adding the
B1^c rung to the ladder min can only TIGHTEN delta, and the tightened
delta^c = min(B, B1^c) − J[S*] carries the semantics "distance to the
C-relaxed ceiling" with Pi_c reduced by exactly the priced slots. The
lemma and the rungs program are one monotone family — CONSISTENT by
construction, as the brief requires.

Derivable now: (a)-(c) above (derived). F2 rungs (NOT derived here,
named duties §8): the B1^c executable rung and its ladder wiring.

FALSIFIER (DC-3): a computed B1^c > J_ideal(eps) at shared (eps, data,
tables) = monotonicity violation => implementation bug or hypothesis
breach (V1/V2), the composition claim dies with it; a phase family
breaking (V2) measurability voids the integral bound and must be
declared at the carrier.

## 5. DC-4 — KKT WEAK-DUALITY RUNG (derivable-now half)
[rigor: THEOREM (the inequality); instantiation = F2 duty; regime clause = SCHEMA]

For (P) as posed (unilateral caps g_i(S) <= c_i) and ANY multiplier
vector mu_i >= 0:
    sup_{A(c)} J <= sup_{S in A(eps)} [ J[S] − Sum_i mu_i (g_i(S) − c_i) ],
(one-line weak duality: on A(c) each penalty term is >= 0). With mu =
the engine's KKT multipliers (constraints priced at marginal values,
registry row text) this is the second named rung. REGIME CLAUSE
(consistency with T7(c) cone form, P1_sections_5_7.md:72-94): the rung
consumes multipliers of the UNILATERAL regime (K with caps: sign +
complementarity per KT2015 (2.10)); the fixed-eps bookkeeping regime 2
(K = {point}) carries FREE-SIGN lambda components which are NOT
admissible weights here — a sign-blind import of regime-2 bookkeeping
values into the rung is an ERROR, and the carrier must gate on
multiplier regime. Computable evaluation of the right-hand sup (needs
the perturbed ceiling map, e.g. eps- and L-perturbation of J_ideal) is
NOT derived here: F2 duty [DC-F2-3].
[ESC-r1-7, DORMANCY DECLARED per MIN-DELTACARRIER-7 (NOTE,
SUSTAINED): per the consumed T7(c) text (rde_nozzle_P1_sections_5_7
.md:79-84), ALL executed instances live in the fixed-(eps, L)
bookkeeping regime 2 (K = {point}, FREE-SIGN lambda components),
which this rung itself rules inadmissible — the gate correctly
rejects everything that exists today. The rung is therefore DORMANT
until (P)-as-posed capped runs (unilateral regime) exist; [DC-F2-3]
is contingent on their existence first. Expected outcome unchanged;
no statement moves.]

FALSIFIER (DC-4): a toy constrained instance where the multiplier-
priced bound falls below the true constrained sup => sign/regime error
(e.g. free-sign regime-2 import); the regime clause is falsified if a
correctly-signed unilateral-regime instance still violates the bound.

## 6. DC-5 — CARRIER PRACTICE RULES [rigor: PRACTICE]

(a) ANTI-CONSERVATIVE DIRECTION: delta = J_ideal − J[S*] with an
UNDER-estimated ceiling under-states delta (claims closer-to-optimal
than true). The carrier ships J_ideal with its qualifications (advisory
§3.2.3: two-resolution band + eps-achievement correction — the ideal
march stops at |M − M_e| < 1e-5 and re-targets; lip/eps residual
measured 4.4938e-3 at the twin) and delta is quoted against the UPPER
band edge of J_ideal [ESC-r1-1a: under branch (H6'-B) the upper edge
is additionally WORSENED by the net-two-wall-panel band — the band
folds into this rule, per MIN-DELTACARRIER-1's repair as ratified].
(b) HYPOTHESES IN THE CARRIER (advisory §3.2.5, EXTENDED [ESC-r1-1]
[ESC-r1-4][ESC-r1-8]): same eps, same thermo leaf/tables, same Sauer
IVL/mdot — printed in the Verdict row; PLUS (domain disposition)
which branch of (H6') the row shipped under, (H6'-A) or (H6'-B) with
its band [ESC-r1-1]; PLUS same Pa convention (gauge/vacuum),
declared — a delta row mixing a gauge ceiling against a vacuum J[S*]
(or vice versa) is numerically wrong by exactly the class-constant
Pa·Int_wall n_x dA [ESC-r1-4]; PLUS the (H2b) check result
(mdot/A_e vs rho*·c* from the same tables) [ESC-r1-8, per
MIN-DELTACARRIER-8: (H2b) underwrites existence/uniqueness of lambda
and was unowned — the S-CERT unowned-hypothesis pattern; now owned
here and at [DC-F2-5]]. (c) SEQUENCING (binding) [ESC-r1-2: re-pinned
per MIN-DELTACARRIER-2; prior "(H6) ... [OBJ-DOM] resolution or
sliver banding" wording superseded]: no (value, delta) row ships
unless "[OBJ-DOM-IMPL] landed and used on both sides, OR the
net-panel band included" ((H6') branches; the row-:300 "banded or
repaired" license correctly read, VERDICT_blocco2 §4(2)); the judge
enforces, sequenced with/after the [OBJ-DOM] escalation E-2.
FALSIFIER: a shipped delta row quoting the lower/central band edge, or
missing the hypothesis line, is a carrier violation (grep-detectable).

## 7. DC-6 — T3-COLLAPSE CONSISTENCY [rigor: SCHEMA (consumes registry/M0, no re-derivation)]

Bound-ladder row text: T3's collapse is precisely attainment of B1^c
under pressure similarity. In that class the per-phase constrained
optimum is phase-shared, so B_fam({eps, L}) is attained by one design
[ESC-r2-1: B_fam here is the RESTRICTED (W_cert) family of §4 as
redefined] [ESC-r3-1e, per ESC-DELTACARRIER-r2-1: the prior
continuation "the attaining design — the phase-shared classical
constrained optimum — is supersonic-exit by construction, hence in
W_cert, so the attainment claim is unchanged under the restriction"
is RETRACTED (invalid inference); the attainment claim is now
CONDITIONAL: the attaining design is in W_cert upon its per-cell
axial-supersonicity certificate (M cosθ > 1, the [ESC-r3-1a] S1
margin check — generically satisfied at classical design exit
Mach/angles), and the attainment claim holds UPON THAT CERTIFICATE;
this schema's SCHEMA rigor class tolerates the conditionality]:
Pi_{L} -> 0 and delta^c -> delta_true. The DC-2 decomposition
degenerates exactly as the record demands (delta = 0 proven where M1-M3
structure exists, M0 D2.6(iv)) — no contradiction between the lemma,
the rung program, and the T3/T4 attainment results. FALSIFIER: a
pressure-similar cycle with computed B1^c strictly above the T3-collapse
optimum (beyond bands) breaks either this schema or the T3 chain.

## 8. F2 DUTIES (measured halves — NAMED, not executed here)

- [DC-F2-1] (value, delta) Verdict field at the engine rung (execution;
  owner = row pipeline:delta-carrier-F2-entry; ships under (H6') +
  DC-5 as re-pinned [ESC-r1-2]).
- [DC-F2-2] B1^c rung executable: per-phase Rao/Kraiko constrained
  solves wired into the ladder min (owner = row
  bound-ladder:constraint-aware-rungs-missing, F2 ladder window).
  [ESC-r3-1d, per ESC-DELTACARRIER-r2-1: CERTIFICATE-ON-ENTRY duty —
  the B1^c solves enter the ladder only WITH their W_cert certificate
  (per-cell exit axial supersonicity u_x > c, i.e. M cosθ > 1, the
  same S1 margin check as every consumer), report-on-fail, never
  proceed silently — same pattern as the (H2b) check in [DC-F2-5];
  membership is a-posteriori checkable, not a construction fact.]
- [DC-F2-3] KKT weak-duality rung computable instantiation
  (unilateral-regime multipliers only, per DC-4 regime clause).
  [ESC-r1-7: CONTINGENT duty — dormant until (P)-as-posed capped runs
  (unilateral regime) exist; today's regime-2 free-sign bookkeeping
  values are inadmissible by DC-4's own gate. The F2 executor must
  not attempt this duty against existing runs.]
- [DC-F2-4] Ceiling band measurement at production resolution
  (two-resolution + eps-achievement residual; upper-edge rule DC-5a).
- [DC-F2-5] Rejector for DC-1 (twin-based J_TOC <= J_ideal check + the
  H3-violation breakout test named in the DC-1 falsifier — analytic
  form already discharged by esc_probe_deltacarrier_h3breakout.py
  [ESC-r1-3a]; the engine-instance form remains F2) + [ESC-r1-8, per
  MIN-DELTACARRIER-8] the (H2b) check: verify mdot/A_e < rho*·c*
  (sonic state from the SAME tables) per design; a failing (H2b)
  voids lambda's existence/uniqueness and the rejector must REPORT
  it, never proceed silently.

## 9. PROPOSED LANDING (for the judge's landing list; orchestrator executes)

- M0 site: Part III, new row [T-DCRX] adjacent to the [T-EAP]/OP-0
  ceiling block (after the sonic-cap sharpening, MASTER.md ~:1255-1330)
  — site RATIFIED by VERDICT_blocco2 §2.3 [ESC-r1-5] — containing DC-1
  (THEOREM*) + DC-2 semantics WITH the W_cert restriction [ESC-r1-3] +
  a pointer to the rung family (DC-3/DC-4, incl. the DC-4 dormancy
  clause [ESC-r1-7]; the family DEFINED over A_phase(C) ∩ W_cert —
  the [ESC-r2-1] restricted definition AS REPAIRED AT [ESC-r3-1a]
  (membership = a-posteriori certificate, M cosθ > 1; the retracted
  "by construction" inference does NOT land) and phase-quantified per
  [ESC-r3-2a] is the landing form, so no
  extraction that strips [ESC-*] brackets can ship the unrestricted
  symbol or the invalid membership inference) and the DC-5 practice rules as extended ((H6')
  branch disposition, Pa convention, (H2b) check); DC-6 as a one-line
  consistency remark at the T3/T4 attainment site.
- [ESC-r3-3, per ESC-DELTACARRIER-r2-3 (NOTE, FIXED): Pa
  landing-adjacency sentence — MANDATORY in the [T-DCRX] landing text.
  At the M0 site, D2.6 poses (P) with "constant Pa > 0" (rde_nozzle_
  MASTER.md:251) three Parts above a [T-DCRX] whose hypothesis line
  says Pa >= 0 and whose only executed instances are Pa = 0; no
  contradiction (a lemma may carry broader hypotheses than (P)), but
  the landing text carries one sentence sparing the M0 reader the
  apparent conflict: "Pa >= 0 deliberately widens (P)'s Pa > 0 to
  cover the executed vacuum objective — the [OBJ-DOM] Pa axis, already
  consumed by the DC-5(b) same-Pa-convention line; the argmax is
  untouched, the offset is the class-constant Pa·Int_wall n_x dA."]
- [ESC-r1-5] The [T-DCRX] landing text CARRIES the G-12 scope-context
  sentence (graft of record, [ADV] provenance, CT-6-clean — verified
  at source this window, SYNTHESIS_nozzle_rde_arrivals.md (e).3
  :413-423 / G-12 :280-283): "Scope [ADV, synthesis (e).3/G-12]:
  J_ideal(eps) prices designs INSIDE the fixed-interface data class
  (H1); constriction/choking/exit-area moves that rewrite the chamber
  state are class-changing and outside what delta prices — the largest
  published performance lever lives there; pricing it is CFD-1's
  irreducible core." Without this sentence the graft orphans
  (artifact-connectedness rule); this draft is its named landing site.
- Registry: row pipeline:delta-carrier-F2-entry — owner field updated:
  lemma DERIVED (Phase D minor (c), [T-DCRX]); remaining owner = F2
  execution [DC-F2-1..5]. Row bound-ladder:constraint-aware-rungs-missing
  — note added: composition derived ([T-DCRX] DC-3/DC-4); rungs remain
  F2. Exact row texts to be fixed by the judge per standing format.

## 10. READ DEPTH + PAPERS

Repo documents read (not papers): BRIEF_blocco2_phaseD.md [FULL];
findings_registry.yaml rows 204-222, 285-309, 1660-1677 [FULL at cited
lines]; ADVISORY_S25_pipeline_sense_CONVERGED_2026-08-12.md §3.2-§3.4,
§6 [FULL at sections]; rde_nozzle_MASTER.md :248-270, :320-399,
:1255-1330 [targeted slices]; rde_nozzle_P1_sections_5_7.md :70-99
[targeted slice]. Papers consulted: NONE (the proof is self-contained
from the ratified proof obligation; no paper claim is load-bearing
here). Efremov-Kraiko 2004 is cited only through the existing M0
[T-EAP] block (its provenance, not re-read).

PAPERS NEEDED: none blocking. Optional (non-blocking, provenance only):
a Kraiko ideal-nozzle-comparison source to attach classical lineage to
DC-1 in the paper (P-1) bibliography; not required for the lemma's
rigor class.

[ESC-r1-9] STATUS NOTE (escalation round 1): the §9 landing remains
PROPOSED — nothing lands until this escalated round closes (refutation
pass + closure judge per VERDICT_blocco2 §3 form of record) AND the
sequencing gate of §4(3) is satisfied (escalated minor (b) closed;
this file consumed its revised OBJDOM-5/[ESC-r1-3] output as input, of
record). Expected labels at close (guidance consumed, VERDICT_blocco2
§2.3, NON-ADOPTED here as labels): DC-1 THEOREM* under the Pa >= 0
repair and declared conditionals (a)-(c); DC-2 THEOREM* under W_cert;
DC-3 THEOREM under V1-V2 with the [ESC-r1-6] equality rewording; DC-4
inequality THEOREM (regime clause SCHEMA); DC-5/DC-6 PRACTICE/SCHEMA
with the (H6')+Pa-convention carrier lines and the G-12 scope sentence.

## 11. [ESC-r1] READ-DEPTH (this escalation window) + DISPOSITION TABLE

Read this window (all internal, no papers; CT-6 kept — no number from
the four nozzle papers enters any statement): VERDICT_blocco2.md
[FULL] (whole file, §§2.3/3/4 the mandate sections);
refute_minor_deltacarrier.md [FULL] (whole file, the 8 findings +
VERIFIED-CLEAN + sequencing-duty verdict); phaseD_minor_objdom.md AS
ESCALATED [targeted FULL slices]: :280-449 (OBJDOM-5 rewrite
[ESC-r1-3] with (H6'-A)/(H6'-B) + re-pinned ship-gate; §2 protocol
P-1..F-2 as revised [ESC-r2-2]; §3 duties incl. [OBJ-DOM-IMPL]) +
grep-verified [ESC-r1-*]/[ESC-r2-*] marker census (escalated state
confirmed before consumption) [ESC-r2-2 CORRECTION: this census claim
was INCOMPLETE — the on-disk (b) already carried [ESC-r3-*] at write
time (mtime-proven, ESC-DELTACARRIER-r1-2); correct declaration = "as
revised through [ESC-r3-*]", re-measured this window; the r3 delta
does not touch the consumed (H6')/ship-gate block — see the corrected
[ESC-r1-0] header and the closure re-verification duty there];
docs/findings_registry.yaml :285-305
[FULL] (row :292-300 re-read at source; :300 trigger quoted
character-exact at [ESC-r1-2]); SYNTHESIS_nozzle_rde_arrivals.md
:278-287 (G-12) + :410-423 ((e).3) [FULL slices, graft verified at
source]. Probe: esc_probe_deltacarrier_h3breakout.py authored + run
this window (PASS, exit 0; pinned-env numpy only, no install).
Verification-before-adoption record: (H6') adopted only after the
escalated (b) text was checked against VERDICT_blocco2 §4(2)
character-for-character on the ship-gate; Pa-insensitivity of the
DC-1 proof re-derived by hand (Pa cancels in (i) closed-surface, (iii)
both-sides) before writing Pa >= 0; the W_cert decomposition's two
nonnegativity claims re-proved (S* in A(c) ∩ W_cert by its own
certificate); the u->0+ counter-state re-derived and probe-executed;
the DC-3(c) inequality direction re-checked against the chain
"Hence B1^c <= J_ideal(eps)" (intact).

DISPOSITION TABLE (every sustained finding of
refute_minor_deltacarrier.md / VERDICT_blocco2 §2.3 — 8/8):

| Finding | Class | Disposition | Marker(s) | What was done |
|---|---|---|---|---|
| MIN-DELTACARRIER-1 | CONTENT | REPAIRED | [ESC-r1-1], [ESC-r1-1a] | (H6) RETRACTED-as-quoted and rewritten as the (H6') two-branch sufficient condition consumed from the ESCALATED minor (b) OBJDOM-5 ([ESC-r1-3] there, judge-ratified form, verified vs VERDICT_blocco2 §4(2)): (H6'-A) fix-A panel-inclusive on BOTH sides, OR (H6'-B) coded-both-sides + explicit net-two-wall-panel band, anti-conservative sign declared, folded into the DC-5(a) upper-edge rule; same-domain demoted to necessary-only; DC-5(b) gains the domain-disposition entry. |
| MIN-DELTACARRIER-2 | CONTENT | REPAIRED | [ESC-r1-2] | Unlicensed "(fix-A or fix-B)" shipping branch RETRACTED with the false "(row trigger verbatim)" flag; row :300 re-read at source and quoted character-exact; ship-gate re-pinned to "[OBJ-DOM-IMPL] landed and used on both sides, OR the net-panel band included" ((H6'), DC-5(c), [DC-F2-1]); fix-B-landed alone ships nothing. |
| MIN-DELTACARRIER-3 | CONTENT | REPAIRED | [ESC-r1-3], [ESC-r1-3a] | W_cert := (H1)-(H5b) certified subclass defined; DC-2's sups restated over A(c) ∩ W_cert / A(eps) ∩ W_cert with DC-1's THEOREM* conditionality carried explicitly; falsifier exhibit class restricted to certified members; the refuter's u->0+ counter-state verified analytically AND by executable probe (esc_probe_deltacarrier_h3breakout.py, two-sided rejector, PASS): admissible-set bound holds, H3-off breakout fires — hypothesis list proven load-bearing. |
| MIN-DELTACARRIER-4 | CONTENT | REPAIRED | [ESC-r1-4] | (H4) restated Pa >= 0 (vacuum = Pa = 0, the engine's only executed objective, now covered); Pa-insensitivity of the proof verified by hand before adoption; DC-5(b) gains "same Pa convention (gauge/vacuum), declared" with the mixing-error class-constant named — the third [OBJ-DOM] axis now consumed. |
| MIN-DELTACARRIER-5 | WORDING | REPAIRED (graft landed) | [ESC-r1-5] | G-12 scope-context sentence grafted at BOTH the delta-semantics site (§3) and the §9 [T-DCRX] landing text, verified at source ((e).3 :413-423, G-12 :280-283), [ADV], CT-6-clean, no statement change — the named-input duty of the escalation mandate discharged. |
| MIN-DELTACARRIER-6 | WORDING | REPAIRED | [ESC-r1-6] | DC-1 STATEMENT equality reworded to attainment-by-the-relaxation-maximizer-STATE (in-class attainment not claimed); DC-3(c) "=" replaced by "<=, with equality exactly on the uniform-exit relaxation"; downstream chain B1^c <= J_ideal re-checked intact. |
| MIN-DELTACARRIER-7 | NOTE | REPAIRED | [ESC-r1-7] | DC-4 dormancy declared in §5 and at [DC-F2-3]: all executed instances live in regime 2 (free-sign, inadmissible by the rung's own gate); duty marked contingent on (P)-as-posed capped runs existing. |
| MIN-DELTACARRIER-8 | NOTE | REPAIRED | [ESC-r1-8] | (H2b) check ownership assigned: added to the [DC-F2-5] rejector (mdot/A_e vs rho*·c* from the same tables, report-on-fail) and to the DC-5(b) printed hypothesis line — the S-CERT unowned-hypothesis pattern closed. |

PAPERS NEEDED (this escalation window): (empty).

## 12. [ESC-r2] ROUND-2 READ-DEPTH + VERIFICATION RECORD + DISPOSITION TABLE

Read this escalation-round-2 window (all internal, no papers; CT-6
kept — no number from the four nozzle papers enters any statement):
esc_refute_deltacarrier_r1.md [FULL] (whole file: VERIFIED-SOUND 1-12,
findings r1-1..r1-4, sequencing check, machine summary);
VERDICT_blocco2.md §§2.3/3/4 [FULL at sections] (mandate re-read);
phaseD_minor_objdom.md [targeted]: grep census of [ESC-r*-*] markers
(state = through [ESC-r3-*], measured this window) + the consumed
OBJDOM-5/(H6')/ship-gate block re-confirmed carrying only [ESC-r1-3];
esc_refute_objdom_r3.md tail [targeted] (DRY, closure-judge ready);
file mtimes measured this window (SR-12, stat): phaseD_minor_objdom.md
19:16:17 < esc_refute_objdom_r3.md 19:22:33 <
esc_probe_deltacarrier_h3breakout.py (as repaired, this window) <
this revision — all 2026-08-20.

Verification-before-adoption record (round 2, the r1-1 repair): the
restricted definition B_fam(C) := Int sup_{A_phase(C) ∩ W_cert} F dmu
was checked leg-by-leg BEFORE adoption — (a) survives with the
certification hypothesis moved into statement text (every actual
consumer is the certified S*; no instance lost); (b) monotonicity in C
unaffected (∩ W_cert is C-independent); (c) becomes entailed by its
own symbols via DC-1 member-by-member on W_cert; the B1^c
instantiations (Rao/Guderley-Armitage/Kraiko per-phase constrained
solves) and the DC-6 T3-collapse attaining design are supersonic-exit
by construction, hence in W_cert — "Hence B1^c <= J_ideal(eps)",
delta^c, and DC-6's attainment all stand unchanged. Conclusion-
preserving for every existing or named instance, as the refuter's
repair claimed. [ESC-r3-1f: the "supersonic-exit by construction,
hence in W_cert" inference recorded in this round-2 record is
RETRACTED per ESC-DELTACARRIER-r2-1 (supersonic SPEED does not give
(H3) axial supersonicity — the probe's own half [B] is the
counterexample); the record stands as history of what round 2 did,
but conclusion-preservation now runs through the a-posteriori W_cert
CERTIFICATES at [ESC-r3-1a/c/d/e]: no conclusion moves for any
CERTIFIED instance, and every existing consumer is certified.] Probe repair (r1-4) verified by re-derivation
(intermediates rho·lam² ≈ 0.209 ≈ 18x p_lam ≈ 0.0116 set the
cancellation noise scale) and by re-run: PASS exit 0 this window.

DISPOSITION TABLE (escalation round 2 — every finding of
esc_refute_deltacarrier_r1.md; 0 BREAK / 1 REPAIR / 1 AMENDMENT /
2 NOTE; none contested):

| Finding | Class | Disposition | Marker(s) | What was done |
|---|---|---|---|---|
| ESC-DELTACARRIER-r1-1 | REPAIR | FIXED | [ESC-r2-1] | The refuter's named one-clause repair adopted after independent re-verification: B_fam DEFINITION restated over A_phase(C) ∩ W_cert (prior unrestricted display RETRACTED-as-quoted); (a) gains the certified-S hypothesis in statement text; (V2) sup restated over the restricted set; (c) now proven by DC-1 member-by-member with the entailment stated in statement text (not an annotation bracket); the round-1 bracket's scoping parenthetical marked redundant-history; B1^c membership and DC-6 attainment anchored in W_cert at their sites; §9 landing text pinned to the restricted definition so bracket-stripping extractions cannot ship the unrestricted symbol. No conclusion moves for any existing or named instance. |
| ESC-DELTACARRIER-r1-2 | AMENDMENT | FIXED | [ESC-r2-2] | [ESC-r1-0] version clause corrected in place ("through [ESC-r1-*] and [ESC-r2-*]" was false — on-disk (b) was at [ESC-r3-*], mtime-proven by the refuter and re-measured this window); §11 census claim corrected with the same marker; the r3-delta-does-not-touch-the-consumed-block verification cited; EXPLICIT closure duty added: the judge re-verifies the consumed (H6')/ship-gate block against (b)'s CLOSED text, adoption re-opens if that block is amended at (b)'s closure. |
| ESC-DELTACARRIER-r1-3 | NOTE | FIXED | [ESC-r2-3] | §3 dangling fragment reflowed as a full sentence ("The split (Pi_c vs delta_true) is NOT separately computable today — ..."), prior fragment quoted in the marker; no content change; machine-read landing extraction can no longer mis-attach the clause. |
| ESC-DELTACARRIER-r1-4 | NOTE | FIXED | [ESC-r2-4] | Probe tol_A re-derived at the correct scale IN THE PROBE (tol_A = 64·eps·max(rho(lambda)·lambda², p(lambda)); derivation comment rewritten to name the cancellation of the ~18x-larger intermediates as the noise scale); probe re-run this window: PASS exit 0 (sup−p_lam = −4.731e-10; aligned attainment exactly 0.0; breakout exceedance +1.6075e-1 vs required 2.971e-12). Wrong-as-stated derivation removed; latent false-FAIL mode priced; no conclusion moves. |

PAPERS NEEDED (escalation round 2): (empty).

## 13. [ESC-r3] ROUND-3 READ-DEPTH + VERIFICATION RECORD + DISPOSITION TABLE

Read this escalation-round-3 window (all internal, no papers; CT-6
kept — no number from the four nozzle papers enters any statement):
esc_refute_deltacarrier_r2.md [FULL] (whole file: read-depth
declaration, prior-objections block r1-1..r1-4 all RESOLVED,
VERIFIED-SOUND 1-10, findings r2-1..r2-3, sequencing check, machine
summary); VERDICT_blocco2.md §§2.3/3/4 [targeted re-read] (mandate +
sequencing §4(1)-(3)); phaseD_minor_deltacarrier.md [FULL] (own file
as revised through [ESC-r2-*], every repair site located before
editing). SEQUENCING RE-MEASURED THIS WINDOW (SR-12; commands: grep -c
"\[ESC-rN-" + stat -c '%y %n' + ls): phaseD_minor_objdom.md marker
census [ESC-r1-*] 30 / [ESC-r2-*] 18 / [ESC-r3-*] 6 / [ESC-r4-*] 0,
mtime 2026-08-20 19:16:17 −0400 UNCHANGED since the r2 refuter's
measurement (same mtime ⇒ same bytes ⇒ the consumed (H6')/ship-gate
block :290-330 is bit-identical to what rounds 1-2 verified
clause-by-clause); esc_refute_objdom_r3.md (19:22:33) is the LAST (b)
refutation on disk, DRY, closure-judge ready; NO esc_refute_objdom_r4
.md exists; (b) NOT closed — the [ESC-r1-0] closure re-verification
duty remains LIVE and the §9 landing remains PROPOSED ([ESC-r1-9]).
Probe esc_probe_deltacarrier_h3breakout.py NOT touched this round (no
r2 finding targets it) and re-run this window: PASS exit 0, all
digits reproduced ([A] sup−p_lam = −4.731e-10, tol_A = 2.971e-15,
aligned attainment exactly 0.0; [B] q = 0.888675, exceedance
+1.607546e-1 vs required 2.971e-12).

Verification-before-adoption record (round 3): the r2-1 named repair
was verified BEFORE adoption — (i) the invalidity of "supersonic-exit
⟹ in W_cert" re-derived independently: (H3) demands u_x − c > 0
per-cell, supersonic SPEED gives only q > c, and the probe's half [B]
state (q = 0.888675 > c = 0.4920, u_x = 1e-6, G − p(lambda) =
+1.61e-1, re-run PASS this window) is a supersonic-speed cell
breaking the bound — the conflation is real, not hypothetical;
(ii) the repaired certificate form checked conclusion-preserving:
DC-3 legs (a)-(c) never used the membership claim (the r2 refuter's
VERIFIED-SOUND 1 — the sup over a subset of W_cert-certified members
is bounded by the {eps}-member ceiling), so replacing the fact-claim
by the certificate hypothesis moves NO displayed inequality; the
M cosθ > 1 form re-derived (u_x = q cosθ > c ⟺ M cosθ > 1) and the
genericity example checked (M_e ≥ 2, θ ≲ 15°: cos 15° = 0.966,
M cosθ ≥ 1.93 > 1); (iii) the relaxation-STATE removal from the
instantiation list checked against [ESC-r1-6] (the state sits outside
the sup; nothing consumes its membership); (iv) AG-1 compliance: a
strong trivially-checkable a-posteriori hypothesis (one S1 margin
check per solve) replacing an unproven construction claim —
sufficient-not-optimized, escalation rule intact. The r2-2 pin
verified: both consistent readings re-checked sound (per-phase:
DC-1 applies phase-by-phase to each inner sup; all-phase: smaller
sets, same chains), every named consumer certified at every phase
under its [ESC-r3-1a] certificate, so the mu-a.e.-all-phase pin
moves no conclusion; the (V1) asymmetry the refuter named is closed
(W_cert now carries an explicit quantifier where A_phase(C) already
did). The r2-3 sentence verified against M0 at source this round-3
chain (MASTER.md:251 "constant Pa > 0" per the r2 refuter's [FULL]
read; no contradiction, adjacency only; the sentence names the
[OBJ-DOM] Pa axis and the DC-5(b) owner line).

DISPOSITION TABLE (escalation round 3 — every finding of
esc_refute_deltacarrier_r2.md; 0 BREAK / 1 REPAIR / 1 AMENDMENT /
1 NOTE; none contested):

| Finding | Class | Disposition | Marker(s) | What was done |
|---|---|---|---|---|
| ESC-DELTACARRIER-r2-1 | REPAIR | FIXED | [ESC-r3-1a..g] | The invalid inference "supersonic-exit by construction, hence in W_cert" RETRACTED-as-quoted at all four anchored sites and replaced by the refuter's named certificate form: §4 definition paragraph ([ESC-r3-1a]: axially-supersonic exit per-cell, u_x > c i.e. M cosθ > 1, generically satisfied at classical design exit Mach/angles, certified a-posteriori by the same S1 margin check as every consumer — in W_cert UPON THAT CERTIFICATE; the uniform-exit relaxation STATE dropped from the instantiation list per [ESC-r1-6]); (V2) supporting phrase ([ESC-r3-1b]); B1^c wiring ([ESC-r3-1c], with the certified-entries clause making the displayed bound independent of membership prose); [DC-F2-2] certificate-on-entry duty added, report-on-fail, same pattern as the (H2b) check ([ESC-r3-1d]); DC-6 attainment made conditional on the certificate, SCHEMA class tolerates ([ESC-r3-1e]); §12 round-2 record annotated, history kept ([ESC-r3-1f]); the [ESC-r1-6] historical bracket's residual occurrence pinned as quoted-history-only ([ESC-r3-1g]); §9 landing pin updated so the retracted inference cannot land in [T-DCRX]. No displayed inequality moved; no conclusion moves for any certified instance; every existing consumer is certified. |
| ESC-DELTACARRIER-r2-2 | AMENDMENT | FIXED | [ESC-r3-2a], [ESC-r3-2b] | W_cert phase-quantifier PINNED in the §3 definition ([ESC-r3-2a]: membership evaluated at the phase data in play; cycle-integrated displays read W_cert as the mu-a.e.-all-phase certified subclass, equivalently W_cert(xi) inside the integral; both readings agree for every named consumer; mixed reading excluded; (V1) asymmetry closed) and in DC-1's MU-INTEGRATED FORM ([ESC-r3-2b]: "(H1)-(H5b) at mu-a.e. xi" made explicit where it was implicit). §4/§9 read W_cert per the pin. Conclusion-preserving under both consistent readings, verified. |
| ESC-DELTACARRIER-r2-3 | NOTE | FIXED | [ESC-r3-3] | Pa landing-adjacency sentence added as a mandatory §9 landing-text item: [T-DCRX]'s Pa >= 0 deliberately widens D2.6 (P)'s "constant Pa > 0" to cover the executed vacuum objective (the [OBJ-DOM] Pa axis, DC-5(b) owner line); argmax untouched, offset = class-constant Pa·Int_wall n_x dA. Editorial; no statement of the file changed. |

PAPERS NEEDED (escalation round 3): (empty).
