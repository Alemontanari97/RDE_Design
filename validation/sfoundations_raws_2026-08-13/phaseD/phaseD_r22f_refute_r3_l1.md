# R22-F CENTERPIECE — REFUTATION, ROUND 3, LENS L1 (PDE / hyperbolic structure)

Session: S-FOUNDATIONS-C4, Blocco 1 v2 continuation, 2026-08-20.
Target: validation/sfoundations_raws_2026-08-13/phaseD/phaseD_r22f_centerpiece.md
(author draft revision 1 + reviser passes [REV2-r1-0..21] and
[REV2-r2-0..8], INCLUDING the §10/§11 disposition tables and the
[REV2-r2-6] in-place (vi)-row cell edits with superseded fragments).
Lens scope (brief LOOP spec + orchestrator refinement, round 3): quotient
validity ([T-T0P] entry contract, S.22 sub-scope uniformity),
residual-operator well-posedness, front conditions, Harroun Fig. 18 + the
four grafted exhibit instances carried per-term, G-c adjudication
soundness vs the corrugated-sonic-surface locus.
Numbering continues across rounds: round 2 closed at R22F-L1-11; this
round starts at R22F-L1-12.

READ-DEPTH DECLARATIONS (this window):
- BRIEF_blocco2_phaseD.md [FULL] (209 lines); addendum_c4 [FULL]
  (46 lines); target draft [FULL] (1537 lines, incl. §9 graft table,
  §10/§11 disposition tables, all [REV2-r1-*]/[REV2-r2-*] blocks).
- phaseD_r22f_refute_r1_l1.md [FULL] (273 lines) and
  phaseD_r22f_refute_r2_l1.md [FULL] (282 lines) — own-lens prior-
  objection verification duty; reviser dispositions read in the draft's
  §10/§11 tables (the disposition carriers of record for both rounds).
- docs/rde_nozzle_MASTER.md [SECT]: :2178-2183 (margin-multiplier KKT
  block), :2205-2215 (measured-supported boundary-active hypothesis),
  :2305-2312 (OBLIGATIONS LEDGER O1) — source spot-verification of the
  three M0 anchors the new [REV2-r2-3](C)/(D) text consumes.
- No paper re-read this window (no finding below needs one; the GP1997
  and Harroun page-verifications of round 1 stand — no [REV2-r2-*] edit
  touched the §2.3/§2.4 quotes they certified).
- Probe files r22f_v2_probe_r2_l1_interior_margin.py,
  r22f_v2_probe_r2_l2_routed_mass.py, r22f_v2_probe_r2_l0_activeset_
  shift.py, r22f_v2_probe_r2_l2_value_grad_gap.py confirmed ON DISK
  (cited by the [REV2-r2-*] blocks; not re-run — of record).

EXECUTABLE PROBE (this round, standing preference honored):
r22f_v2_probe_r3_l1_gridmin_floor.py (same folder) — WRITTEN and RUN
this window, ALL ASSERTS PASS; consumed by R22F-L1-13. Constructed
margin field only (no external-paper number: CT-6 clean; no magic
constants: every tolerance is an exact constructed value).

==============================================================================
## PRIOR-OBJECTION VERIFICATION (round-2 L1 findings vs the [REV2-r2] pass)

- R22F-L1-7 (REPAIR, entry-only margin floor does not gate interior
  near-sonic loci): **RESOLVED.** [REV2-r2-5](a) re-declares m0 as the
  MARCHED-DOMAIN margin floor (min over the marched domain of
  M_march(x) − 1 ≥ m0 per phase, a-posteriori one-scan check,
  excluded-and-booked clause "declared, never silent"), supersedes
  [REV2-r1-14]'s entry-only sentence, and propagates into H-G2 (at-site
  pointer in §2.2-bis) and R-10 ([REV2-r2-7]) — exactly the round-2 fix
  shape, all three propagation sites verified present this window.
  (A verification-timing/mesh refinement residue of the NEW check
  wording → R22F-L1-12/L1-13, NEW findings on the revised text, not
  persisting counts: the round-2 objection asked for precisely the
  domain-min hypothesis that was delivered.)
- R22F-L1-8 (AMENDMENT, sector-routing misnomer + coverage accounting):
  **RESOLVED.** [REV2-r2-5](b) creates the named MARGIN-EXCLUDED-PHASES
  bucket ("NOT members of Xi_sub"; T-T3-MAP(d)'s definition not
  overloaded — the exact defect), with the KP18-F6 mechanism citation
  retained at [ADV]; (c) adds the mu(marched)/mu(total) reporting rule
  and the §3.5 per-family marched-sector reading. Both fix-shape lines
  delivered.
- R22F-L1-9 (AMENDMENT, §2.5 SBV propagation not at site): **RESOLVED.**
  [REV2-r2-4] appends the at-site marker at §2.5 ("READ AS 'conditional
  on H-RED-2(SBV)/H-RED-3/H-RED-4'"), and [REV2-r2-2] adds the §2.2(i)
  site pointer; [REV2-r2-8](d) routes the SBV-inclusive list to the L-2
  landing. Verified at both sites.
- R22F-L1-10 (AMENDMENT, (vi) BEST-cell [SE] label without gradient-
  level basis): **RESOLVED.** [REV2-r2-6](a) strikes the plausibility
  clause (superseded fragment quoted verbatim) and replaces it with the
  value-route sqrt-bound SCHEMA ([REV2-r2-3](F)) — no [SE] tag remains
  on a gradient-level-void clause; fix branch (b) of my round-2 fix
  shape, jointly with L2-14, and sharper than asked (the replacement is
  derivable content, not a relabel).
- R22F-L1-11 (NOTE + CT-6 hygiene remark): **no action was owed**; the
  hygiene remark was nonetheless absorbed — [REV2-r2-6](d) strips the
  "P-A 4% miss" numeral from the (vi) WORST cell and confines it to
  [GRAFT-G07] (superseded fragment quoted verbatim).

**PERSISTING PRIOR FINDINGS RE-COUNTED THIS ROUND: 0** (all round-1 L1
objections were verified RESOLVED in round 2 and their sites are
unchanged; all round-2 L1 objections verified RESOLVED above).

==============================================================================
## MANDATE-COVERAGE DUTY (round-3, all lenses) — RESULT

Channel (vi) with the delta/mu schema: **PRESENT** (Part 5 table row
[REV2-r1-16] as amended by the [REV2-r2-6] cell edits; §2.2-bis
[REV2-r1-8] + [REV2-r2-3]; brief :97-134 mandate (5)(vi) satisfied in
the addendum_c4 (b) right-sized form — delta declared UNDERIVED with
the three named derivers, value-level bound never passed off as optimum
coverage). Fig. 18 per-term disposition: **PRESENT** (§2.3 disposition
table as corrected by [REV2-r1-9], per-instance carriage [REV2-r1-10]
incl. the D-4 declarations). No missing mandated element this round.

==============================================================================
## FINDINGS

### R22F-L1-12 — AMENDMENT (M-RED determinacy rule, verification
### timing): mu_routed = 0 declared "trivially checkable at synthesis
### time" while routing now includes an a-posteriori march-output
### criterion — the timing claim contradicts [REV2-r2-5](a)

ANCHOR: [REV2-r2-5](c) (draft :1011-1015): "PRIMARY RULE — mu_routed =
0 per family, a CONSTRUCTION property of the in-house synthesized
families F-a..F-d, trivially checkable at synthesis time". Same block,
(a) (draft :986-991): interior-margin exclusion is "checked A
POSTERIORI from the march output (one scan)".
DEFECT: after the (a) repair, the routed set includes phases excluded
for INTERIOR margin dips — a property of the MARCHED SOLUTION, not of
the synthesized data: it is not decidable at synthesis time (my own
round-2 probe r22f_v2_probe_r2_l1_interior_margin.py is the standing
witness: an entry-clean phase develops an interior near-sonic strip
only through its front content, discovered by marching). So "mu_routed
= 0 ... trivially checkable at synthesis time" cannot be true of the
rule as now defined; only the ENTRY-margin half is synthesis-time
checkable. WHY THE HOLE DOES NOT REOPEN (and hence AMENDMENT, not
REPAIR): the detection instrument exists and is mandatory — (a)'s
one-scan check runs per family and its exclusions are
"excluded-and-booked ... declared, never silent", so a family believed
mu_routed = 0 at synthesis that dips interior is CAUGHT post-march and
falls into the declared FALLBACK branch of (c); the silent-booking
channel closed by L2-13/L1-8(b) stays closed. The defect is purely the
mis-stated verification timing of the primary rule — but it is
landing-facing text ([REV2-r2-8](c) lands these rules), so it should
not land as printed.
FIX SHAPE (one sentence, AG-1 valve): reword the primary rule —
"mu_routed = 0 per family: AIMED at zero by construction (entry
margins checkable at synthesis time); VERIFIED per family by the (a)
post-march one-scan check (interior margins are march-output
properties); a scan-detected violation = the declared
exclusion/re-scope, which is the (c) fallback branch — never a silent
booking."

### R22F-L1-13 — AMENDMENT (B-4 licensing / floor-check instrument):
### the one-scan domain-margin check certifies a DISCRETE min; the
### floor certificate can FLIP under the registered mesh refinement.
### Executable probe RUN.

ANCHOR: [REV2-r2-5](a) (draft :986-991): "min over the marched domain
of (M_march(x) − 1) ≥ m0 > 0 per phase, checked A POSTERIORI from the
march output (one scan; the march already carries the field)"; the
license consumer is B-4 (draft :956-964 + [REV2-r1-14] as superseded):
the ln-truncation argument of §2.4 (limit L3 logic) consumes a fixed
CONTINUUM margin, and B-4's bars are quoted under the registered
pre-registered-norms mesh-convergence discipline
(validation/o32_mesh_convergence.py:24-38, cited in B-4 as of record).
DEFECT: the one-scan min of the march output at mesh h is the min of
the DISCRETE field. A post-front near-sonic strip has a PHYSICAL width
(set by front geometry / post-front expansion length), independent of
the marching mesh; when that width is below the local axial spacing,
the mesh-h scan reports min ≥ m0 while the refined march reveals the
dip — the floor certificate then FLIPS from PASS to FAIL under exactly
the refinement ladder B-4's own discipline runs. As printed, the spec
makes family inclusion/exclusion (and with it eps(F)'s marched-sector
denominator, [REV2-r2-5](c)) a mesh-dependent property with no
declared stability clause — inside a band framework whose entire point
is pre-registered mesh-independence.
EXECUTABLE COUNTEREXAMPLE (probe r22f_v2_probe_r3_l1_gridmin_floor.py,
RUN this window, ALL ASSERTS PASS): constructed margin field, floor
m0 = 1e-2, background 10·m0, one Gaussian strip of half-width h/8
centered between two nodes of the 101-point scan grid, true min =
m0/100. A1: coarse one-scan min = 9.997e-2 ≥ m0 — floor PASSES at
mesh h. A3/A4: at 16x refinement the one-scan min = 1.000e-4 < m0/10 —
certificate FLIPS. A5: coarse-scan/true-min collapse factor = 1000x.
No magic constants (attenuation exp(−8) derived from the geometry);
CT-6 clean.
WHY AMENDMENT, NOT REPAIR: the hypothesis itself (continuum domain min
≥ m0) is exactly right and is what the round-2 fix shape asked for;
M-RED is a SPEC (execution = F2), and the missing piece is one
instrument clause in a check whose companion discipline (o32
registered norms) already exists in the same band B-4 — a wording-
level completion, not a missing hypothesis. It becomes load-bearing
only at execution time, and the landing ([REV2-r2-8](c)) should carry
the completed form.
FIX SHAPE (one clause, AG-1 valve, sufficient-not-optimized): the
floor certificate is tied to the registered refinement discipline
already cited in B-4 — "the one-scan check is repeated on the
registered mesh ladder; the floor certificate = min sustained across
the ladder within its own band (flip under refinement = the declared
exclusion/re-scope, never silent); equivalently the floor check is
declared a PER-MESH property with the ladder re-check a named part of
the M-RED protocol run."

### R22F-L1-14 — AMENDMENT (seam L0 declared; (vi) row internal
### consistency): the value-route bound is quoted "available
### a-posteriori" with no a-posteriori check sentence — condition (1)'s
### own rule states the check only for the delta/mu form

ANCHOR: (vi) BEST cell as amended by [REV2-r2-6](a) (draft :1155):
"VALUE-ROUTE bound available a-posteriori [SCHEMA]: |argmax shift| ≤
2·sqrt(eps_U/mu) ..."; [REV2-r2-3](F) (draft :651-665): "under a
uniform VALUE-error premise on the certified basin ... and a curvature
floor mu per (A)"; §2.2-bis condition (1) (draft :555-561): "valid IF
delta/mu ≤ the certified basin radius, and the check is stated as part
of any cell that uses it."
SEAM DECLARATION: basin/curvature licensing is optimization structure
(L0 turf); reported here because it was found during this round's
mandated (vi)-coverage check and zero-inflation outranks turf (same
protocol as round-2 L1-10). NOT a demand for a gradient bound
(addendum_c4 (b) / SR-C4-13 respected; the value-route lemma itself is
sound — algebra re-derived by hand this window, see L1-15(d)).
DEFECT: the sqrt-form bound holds only where the curvature floor is
licensed — i.e. the shift ball must sit inside the certified basin,
exactly as for the delta/mu form. Condition (1) states that
a-posteriori check ("delta/mu ≤ the certified basin radius") for the
gradient-route form ONLY, and its own rule demands the check be
"stated as part of any cell that uses it"; the value-route clause in
the same cell carries "available a-posteriori" with no check sentence,
and [REV2-r2-3](F) states the basin premise but names no check. By the
cell's own rule the parallel check is missing: 2·sqrt(eps_U/mu) ≤ the
certified basin radius (same radius, same deriver [REV2-r2-3](E)/R-12;
with the reduced argmax inside the basin as part of the same check).
An executor reading the cell finds "a-posteriori" with nothing to
execute for this form.
WHY AMENDMENT: the premise is present in (F) ("on the certified
basin"); nothing false is asserted — the check sentence the schema's
own condition (1) rule requires is simply not printed. One-line
completion, landing-facing ([REV2-r2-8](e)).
FIX SHAPE (one line, in the cell or appended to (F)): "a-posteriori
check for the value route: 2·sqrt(eps_U/mu) ≤ the certified basin
radius (radius deriver = [REV2-r2-3](E)/R-12; reduced argmax verified
inside the basin in the same check)."

### R22F-L1-15 — NOTE (verification ledger, this round; no
### draft-structure action)

Verified at source this window, no defect found:
(a) M0 ANCHORS of the new [REV2-r2-3](C)/(D) text — spot-verified at
the file: M0:2178-2183 is the margin-multiplier KKT block with "mu"
the MULTIPLIER symbol (the rename rationale is real); M0:2205-2215
carries "the S20 instance optimum is boundary-active is a
measured-supported HYPOTHESIS" (the (D) citation faithful);
M0:2305-2312 carries OBLIGATIONS LEDGER O1 with "until discharged the
margin-active KKT is B-STATIONARITY ONLY — no multiplier mu is
defined" (the O1-gate reading and H-G4 faithful).
(b) PROPAGATION COMPLETENESS of [REV2-r2-5](a): all three declared
sites verified present (H-G2 at-site pointer in §2.2-bis; R-10
re-wording [REV2-r2-7]; B-4 licensing sentence) — no orphaned
entry-only reading remains un-superseded.
(c) [REV2-r2-6] protocol integrity: all four superseded cell fragments
are quoted verbatim in the marker block (the only non-appendable site
class handled as the brief's append-only rule intends); the (vi) row
as printed matches the [REV2-r2-6](a)-(d) descriptions.
(d) VALUE-ROUTE LEMMA algebra ([REV2-r2-3](F)) re-derived by hand this
window: J_true(x_r) ≥ J_red(x_r) − eps_U ≥ J_red(x_t) − eps_U ≥
J_true(x_t) − 2·eps_U, then strong concavity with floor mu gives
(mu/2)·||shift||^2 ≤ 2·eps_U, i.e. ||shift|| ≤ 2·sqrt(eps_U/mu) —
exact; the Theta(sqrt(eps)) tightness claim is the standard flat-top
construction and the probe r22f_v2_probe_r2_l2_value_grad_gap.py is on
disk as its executable witness.
(e) LENS-SCOPE STABILITY: no [REV2-r2-*] edit touched §2.0 (entry
contract), §2.1 (operator identities), §2.3 exhibit/per-term carriage,
§2.4 (G-c discharge + limits L1/L2/L3), or the channel-(i) forchetta
cells — the round-1/round-2 verifications of those blocks stand
unchanged; the G-c leg-(ii) DISCHARGED-citable verdict stands.
No inflation found in this round's sweep.

==============================================================================
## PAPERS NEEDED

NONE for this refutation. (All findings close on carriers already on
disk; no procurement would change any finding above.)

==============================================================================
## MACHINE SUMMARY

round: 3
lens: L1 (PDE/hyperbolic structure)
findings: 4 (R22F-L1-12 .. R22F-L1-15)
breaks: 0
repairs: 0
amendments: 3 (L1-12 mu_routed synthesis-time checkability claim vs the
  a-posteriori domain-margin criterion — timing wording, hole does not
  reopen; L1-13 one-scan floor check is a discrete min — certificate
  can flip under the registered refinement ladder, probe RUN, tie-to-
  ladder clause named; L1-14 value-route "available a-posteriori" lacks
  its check sentence per condition (1)'s own rule — seam L0 declared)
notes: 1 (L1-15 verification ledger: M0 anchors of [REV2-r2-3](C)/(D)
  spot-verified at source; propagation completeness; [REV2-r2-6]
  verbatim-supersession protocol integrity; value-route algebra
  re-derived exact; lens-scope blocks unchanged)
persisting_prior: 0 (all five round-2 L1 objections verified RESOLVED
  at their [REV2-r2] sites; all round-1 objections stood resolved)
probe: r22f_v2_probe_r3_l1_gridmin_floor.py (RUN, ALL ASSERTS PASS)
dry_at_this_lens: YES (0 BREAKS, 0 REPAIRs; 3 AMENDMENTS for the
  closure judge to adjudicate and carry to the landing per the brief's
  refined criterion — amendments do not block dry)
