# R22-F REFUTATION — ROUND 4, LENS L2 (asymptotics / measure-and-scaling)

Session: S-FOUNDATIONS-C4, Blocco 1 v2 continuation, 2026-08-20. Target:
`phaseD/phaseD_r22f_centerpiece.md` REVISION 4 ([REV2-r1-0..21] +
[REV2-r2-0..8] + [REV2-r3-0..10] passes, 1829 lines, read [FULL]
including §§9-12). Numbering continues across rounds: last L2 id =
R22F-L2-23, this round starts at R22F-L2-24. Round 4 = the brief's
rounds cap.

LENS SCOPE HONORED: 3-6% swirl-energy scaling use; M-RED band
derivations (zero magic constants); epsilon-orders of the 2-epsilon
transfer; degenerate limits; forchetta evidence-class honesty ([SE]
labels, CT-6, definitional guards). RIGHT-SIZING RULE (addendum_c4 (b)
/ SR-C4-13) APPLIED: no finding below demands a closed-form gradient
bound; the §2.2-bis SCHEMA + named derivers is accepted as delivered;
L2-25 attacks the SOUNDNESS of a printed licensing hypothesis
("if you believe the schema itself is UNSOUND, that IS attackable" —
the rule's own terms), at a degenerate-geometry/small-parameter limit
squarely inside this lens; the L0/L2 seam is declared there
(zero-inflation outranks turf, the round-2 L1-10 protocol).

READ-DEPTH DECLARATIONS (this window):
- BRIEF_blocco2_phaseD.md [FULL]; BRIEF_blocco2_phaseD_addendum_c4.md
  [FULL]; phaseD_r22f_centerpiece.md rev-4 [FULL] (1829 lines).
- ALL NINE prior v2 refutation files [FULL] this window:
  phaseD_r22f_refute_r{1,2,3}_l{0,1,2}.md (own-lens prior-objection
  duty on the L2 chain; disposition tables §§10-12 cross-checked).
- No paper read or re-read this window (no L2 finding needs one); all
  prior source verifications (FINAL_report :436-437, judgeverify,
  findings_registry :1459-1467/:2026, advisory anchors) stand from
  rounds 1-3 and are not re-litigated.
Measured commands of this window (SR-12):
- `grep -n "prox-regular" phaseD_r22f_centerpiece.md` → 4 sites:
  :707 (H-G5 line), :1301 ((vi) cell), :1562/:1564 (R-14) — feeds
  L2-25's propagation list.
- `sed -n '1802,1817p' ... | grep -c "AMENDMENT"` → 7; `| grep -c
  "REPAIR"` → 2 — §12 typed-row counts, feeds L2-24(d).
- `grep -n "until the sweep sup is measured" ...` → :1301 (the second
  site, [REV2-r3-3](b) :766-767, is split across lines; read at site)
  — feeds L2-26.

PROBE (pinned env, numpy only; run PASS this window, all asserts):
- `r22f_v2_probe_r4_l2_proxreg_annulus.py` (parts A-F feed L2-25;
  part G feeds L2-26). Synthetic counter-models only, CT-6 clean; all
  tolerances derived from grid resolutions, no magic constants.

MANDATE-COVERAGE DUTY (round 4): channel (vi) row PRESENT with the
delta/mu schema ((vi) row :1301 — a-posteriori delta/mu form, licensed
forms (T)/(C) under H-G5, O1-gated carrier, delta UNDERIVED with the
three derivers named in brief order; §2.2-bis :533-742) — brief
:112-126 satisfied; Fig. 18 per-term disposition PRESENT (§2.3 exhibit
:775-784 + disposition table :825-831 with the [REV2-r1-9] joint-
nullity readings + [REV2-r1-10] per-instance carriage, unchanged this
round). No coverage finding.

==============================================================================
## PRIOR-FINDINGS VERIFICATION (all 4 actionable L2 round-3 findings)

### R22F-L2-24 — NOTE (resolution ledger; each disposition verified at
### its edited site, not from the §12 table alone)
- L2-19 (eps_U pointwise-instantiation identity) → RESOLVED:
  [REV2-r3-3] (:744-773) withdraws the "=" identity with both
  superseded fragments quoted verbatim, states the sweep-sup
  instantiation of record with per-family coverage declared, adds the
  value-route a-posteriori check sentence (c) (jointly discharging
  L1-14), and [REV2-r3-6](b) carries all of it into the (vi) BEST cell
  (:1301, verified in the cell text). The repair branch I specified is
  adopted faithfully — including its own residual defect (the
  "until measured ⇒ discharged" implication my round-3 wording seeded:
  → L2-26, AMENDMENT, new finding on new text, self-origin declared
  again; NOT a persisting count — the round-3 objection asked for
  precisely what was delivered).
- L2-20 (headline fitted-sheet qualifier, third site) → RESOLVED:
  [REV2-r3-7] (:1492-1502) edits the §5.1 BEST bullet in place with
  the superseded fragment quoted verbatim; the headline now reads
  "(on ray-like cycles WITH A GOOD FITTED SHEET — M-RED leg (E) tests
  it)" (:1479-1481) — consistent with §2.2(i) and the (ii) BEST cell;
  all three sites now carry the licensed condition set.
- L2-21 (round-2 disposition arithmetic 6/6 → 7/7) → RESOLVED:
  [REV2-r3-8] (:1763-1783) corrects the three sites by append-only
  READ-AS (stale numerals retained as history, declared never to
  land), adds the omitted L2-12 NOTE row, and declares the inclusion
  rule (courtesy NOTE rows retained). Re-measured this window: §11
  carries exactly 7 AMENDMENT-typed rows; §12 carries 7 AMENDMENT + 2
  REPAIR rows matching its own COUNTS line and the three round-3
  machine summaries (0+1+1 breaks=0, 1+0+1 repairs=2, 1+3+3
  amendments=7, notes 4) — arithmetic now exact at every site.
- L2-22 (synthesis-time checkability wording) → RESOLVED: [REV2-r3-4]
  (:1135-1150) rewords the primary rule (aimed-by-construction at
  synthesis / VERIFIED by the post-march one-scan; joint with L1-12)
  with the superseded fragment verbatim; consistent with
  [REV2-r2-5](a) and with the [REV2-r3-5] ladder clause; the
  degenerate-limit control (m0 → 0+) still closes cleanly.
PERSISTING PRIOR FINDINGS: 0 (L2-18/L2-23 were NOTES, no action owed).

==============================================================================
## NEW FINDINGS (round 4, on revision-4 text)

### R22F-L2-25 — BREAK (degenerate-geometry limit; executable
### counterexample RUN): the PROX-REGULAR disjunct of H-G5 licenses
### forms (T) and (F) on a connected, prox-regular, nonconvex feasible
### set where BOTH routes fail UNBOUNDEDLY while every printed check —
### including H-G5 itself — passes

SEAM DECLARED: feasible-set geometry is L0 turf; filed here because
the failure is a degenerate-limit/scaling event (violation factor
diverges as eps^{-1/2}; the killer is invisible at any fixed value
level) found during this round's mandated (vi)-coverage sweep, and
zero-inflation outranks turf (round-2 L1-10 protocol). CROSS-ORIGIN
DECLARED (zero inflation both ways): the disjunct "(or locally convex
/ prox-regular with the shift ball inside one connected component)"
originates VERBATIM in the L0 refuter's round-3 fix shape (r3_l0
:189-196), adopted faithfully by the reviser ([REV2-r3-1](a)) — a new
defect in new text, not a disposition failure; not a persisting count.

ANCHORS: H-G5 line :704-711 ("the feasible set intersected with the
certified ball is CONVEX (or locally convex / prox-regular with the
shift ball inside ONE connected component) — declared and checked per
instance"); its own RATIONALE :712-716 (the licensed arguments "each
consume SEGMENT FEASIBILITY between the two argmaxes" — the rationale
names the true consumed property); licensing carriage sites that now
say "UNDER H-G5": H-G4 :663-669, R-13 :1552-1554, (F) hypothesis list
:686, (vi) cell check list :1301 ("H-G5 ([REV2-r3-1]: feasible-set
convexity/prox-regularity declared and checked per instance");
R-14 deciders :1561-1565 ("a convexity/prox-regularity certificate for
the margin set"); branch-wise coverage remark [REV2-r3-1](b)
:724-733.

DEFECT: prox-regularity within one connected component does NOT
deliver segment feasibility — the exact property the rationale
sentence says both licensed routes consume. EXECUTABLE COUNTEREXAMPLE
(probe r22f_v2_probe_r4_l2_proxreg_annulus.py, RUN, all asserts PASS;
synthetic, CT-6 clean): feasible set C = annulus {1 ≤ |x| ≤ 3} in the
plane — CONNECTED (one component), PROX-REGULAR with positive reach
exactly 1 (part D witnesses unique projections throughout the hole);
J_red = −mu|x|²/2 + eps·x1, J_true = −mu|x|²/2 − eps·x1, mu = 1,
metric M = I (condition (3) holds), Hessian CONSTANT (= −mu·I), so the
curvature floor is uniform on every ball and any basin-radius
certificate passes. Argmaxes: S*_red = (+1,0), S*_true = (−1,0) —
shift = 2 for EVERY eps > 0 (part A). Results:
  (T): at S*_red the inner constraint is active; tangent cone =
  {d1 ≥ 0}; gradient gap g = (−2eps, 0) points INTO the forbidden
  half — support-function residual delta_T = 0 exactly (part B).
  Licensed bound 0 vs shift 2: UNBOUNDED violation.
  (F): eps_U = sup|J_true − J_red| = 6eps on C; bound 2·sqrt(eps_U/mu)
  vs shift 2 → violations 40.8x / 408.2x / 4082.5x along the eps
  ladder 1e-4/1e-6/1e-8 (part C) — Theta(eps^{-1/2}), unbounded.
  Form (C)'s multiplier-margin clause is SATISFIED and blind (part F:
  lam = mu − eps ≈ 1 >> |g| = 2eps; the KKT point at (+1,0) PERSISTS
  under J_true — local shift 0 — while the global argmax sits at the
  antipode): same blindness as the L0-16 probe, now WITHOUT the
  disconnectedness that probe leaned on.
  Both a-posteriori radius checks PASS (0 and 0.049 ≤ any covering
  basin radius; reduced argmax inside the basin). Convex control
  (part E: the disk, hole removed): shift ≤ form-(T) bound
  (= 2eps/mu, analytically tight; numerically below grid resolution),
  value bound holds — the HOLE is isolated as the sole killer.
H-G5 STATUS IN THE COUNTER-MODEL, by reading dichotomy (the phrase
"with the shift ball inside ONE connected component" is ambiguous):
under every EXECUTABLE reading (the shift ball = the ball of the
licensed radius around S*_red — radius 0 resp. 0.049 here — or "both
argmaxes in one component", the anti-L0-16-probe reading) the check
PASSES: the annulus is one component and is certifiably prox-regular.
The only reading that excludes the counter-model — the ball of the
ACTUAL shift radius CONTAINED in C — is (i) not executable (the
actual shift is the unknown being bounded) and (ii) vacuous at every
boundary S*: any positive ball around a boundary point exits C, so
that reading un-licenses the constrained case entirely — exactly the
margin-active locus H-G4/H-G5 exist for. Either way the printed line
fails its function. Secondary limb: the [REV2-r3-1](b) coverage
remark offers "branch-wise application ... across feasible components
/ nonconvex lobes" — on a CONNECTED nonconvex set there is no
component decomposition to apply branch-wise, and no lobe
decomposition rule is declared: the remark's coverage story also has
no executable content there.

WHY BREAK (loop precedent, L0-10): a licensed bound-form admits a
cheap counter-model satisfying EVERY printed licensing condition —
H-G5 included, via its prox-regular disjunct. (Contrast L0-16, filed
REPAIR because its counter-model VIOLATED the printed convexity
parenthesis; here the disjunct itself admits the killer.) The (vi)
row is landing-facing: an executor with a prox-regularity certificate
would certify a false shift bound. Note the LOCALLY-CONVEX disjunct
is sound and needs no edit: for a closed connected set, locally
convex ⇒ convex (Tietze–Nakajima, classical) — the defective disjunct
is prox-regularity alone.

FIX SHAPE (one hypothesis-line edit + three wording touches; AG-1
valve — strong, trivially checkable where checkable):
(a) H-G5 line: strike "prox-regular" from the license-equivalent
disjunct list, or retain it ONLY with the explicit companion clause
the rationale already names: "prox-regular AND the segment between
S*_red and the candidate argmax feasible — segment feasibility is the
consumed property; since the true argmax is unknown, the only
a-priori-checkable sufficient form is convexity of C ∩ ball (locally
convex admissible via Tietze–Nakajima)".
(b) R-14 deciders: "a convexity/prox-regularity certificate" → "a
convexity certificate (a prox-regularity certificate does NOT license
the forms — probe of record r22f_v2_probe_r4_l2_proxreg_annulus.py)".
(c) (vi) cell parenthesis: "feasible-set convexity/prox-regularity
declared and checked" → convexity form per (a).
(d) [REV2-r3-1](b): extend the WORST-cell no-coverage attribution to
CONNECTED-nonconvex geometry (branch-wise coverage is undefined
without a declared lobe decomposition; where none exists, no
global-argmax coverage is claimed — the (vi) WORST cell's existing
sentence :1301 already says "on a NONCONVEX feasible set ... NOT
claimed by ANY form", which is TRUE and stays; the defect is that
H-G5's prox-regular disjunct contradicts it by licensing a nonconvex
instance).

### R22F-L2-26 — AMENDMENT (epsilon-premise instantiation class;
### executable): "until the sweep sup is measured, the uniformity
### premise is OPEN" implies measured ⇒ discharged — a finite sampled
### sweep-sup never certifies the uniform level; one class-label
### clause missing (the [REV2-r3-5] pattern, one instrument up)

ANCHORS: [REV2-r3-3](b) :760-767 ("its measured instantiation = the
SUP over the M-RED value legs (A)-(B) sampled ALONG THE DESIGN SWEEP
... until the sweep sup is measured, the uniformity premise is OPEN
and DECLARED"); (vi) BEST cell :1301 ("uniformity OPEN and declared
until the sweep sup is measured"); the draft's own instrument
precedent [REV2-r3-5] :1151-1169 (a discrete one-scan min is
mesh-dependent → certificate tied to the registered refinement
ladder). SELF-ORIGIN DECLARED: the "until the sweep sup is measured"
wording originates in MY round-3 repair (r3_l2 :129-131), adopted
faithfully — the sharpening is mine to ask for; not a disposition
failure.
DEFECT: the sampled sweep-sup is a FINITE-SAMPLE lower bound on the
basin sup; the sentence's "until measured" structure licenses an
executor to treat the measured sweep-sup as DISCHARGING the (U_G)
premise and plug it into 2·sqrt(eps_U/mu) — false under
sub-sample-width residual structure. EXECUTABLE COUNTEREXAMPLE (probe
part G, RUN): 1-D convex feasible interval (H-G5 clean — isolates the
sampling defect from L2-25), J_true strongly concave with floor
mu = 1, J_red = J_true + a triangular residual spike of half-width =
h_sweep/4 centered strictly BETWEEN two sweep samples: sampled
sweep-sup = 0.0 exactly (all 21 samples miss the spike) → literal
bound 2·sqrt(0/mu) = 0, while the actual argmax shift = 0.425; the
TRUE-sup bound (2·sqrt(0.12/1) = 0.693) holds — the lemma is sound,
the premise instantiation class is the defect, exactly one level up
from L2-19's pointwise version. This is the design-sweep analog of
the L1-13 mesh-min flip, and the draft already owns the fix pattern.
WHY AMENDMENT (not REPAIR): M-RED is a spec (execution = F2); the
route carries [SCHEMA] not a number; "sweep coverage declared per
family" is already printed as an acknowledged incompleteness; nothing
false is asserted TODAY — the defect is one implication in
landing-facing text, same class as L1-13 ("instrument clause
completion", AMENDMENT precedent) and as my own L2-22. Demanding a
CERTIFIED uniform sup would be demanding the (U)-class theorem — an
infinite regress the right-sizing rule forbids; the fix is a class
label + stability clause, not a stronger theorem.
FIX SHAPE (one clause at (F)/[REV2-r3-3](b), read into the cell):
"the measured sweep-sup instantiates eps_U at SAMPLED-SUP (estimate)
class, with a declared sweep-refinement stability clause — sup
sustained under refinement of the sweep sampling within its own band,
the [REV2-r3-5] pattern (named part of the §3.6 rider / R-15) — and
with sub-sample-width residual structure declared un-excluded;
certified closure of the uniformity premise remains with the
(U)/S.22-class program (R-1): measurement INSTANTIATES the premise at
declared class, it never discharges it."

### R22F-L2-27 — NOTE (verified-green ledger, round 4; zero credit
### inflation): revision-4 chains that SURVIVED L2 attack this window
(a) [REV2-r3-1](b) cross-branch dominance threshold re-derived: for
any S in branch B, J_true(S) ≤ max_B J_red + eps_U and J_true(A*_red)
≥ max_A J_red − eps_U, so a reduced-functional margin > 2·eps_U
excludes the true argmax from B — the printed 2·eps_U constant is
EXACT for the disconnected/branch-decomposable case (the connected-
nonconvex gap is L2-25's secondary limb, not a constant error).
(b) [REV2-r3-3](c) value-route check list mirrors condition (1)'s
discipline correctly; the degenerate limits still behave (mu → 0
fails the radius check rather than faking a bound; eps_U → 0 with the
premise TRUE gives shift → 0 — probe part E/G control legs).
(c) [REV2-r3-4]/[REV2-r3-5] jointly re-checked: the routed-bucket
accounting remains determinate on both branches (mu_routed = 0
verified post-march / declared fallback), the ladder clause cites the
registered o32 discipline — BAND RULE exhaustiveness holds; m0 → 0+
degenerate limit clean.
(d) §12 disposition table verified against the three round-3 files
(0 BREAKs, 2 REPAIRs, 7 AMENDMENTS, 4 NOTES; joint-fix sites
{L1-12,L2-22}, {L2-20,L0-18(g)}, {L2-19,L1-14} all consistent);
round-2 count correction [REV2-r3-8] verified exact (7 AMENDMENT
rows in §11; cross-check 2+3+2 vs machine summaries).
(e) [REV2-r3-7] headline: all three sites now carry the fitted-sheet
qualifier; the BEST bullet's "booking debit at the 1.5-3%-of-thrust
scale [SE]" is the FOLD-variant class of record (conservative in a
BEST line, consistent with [T-DISC-2](ii) and the (iii) cells) — no
overclaim.
(f) CT-6 sweep over ALL [REV2-r3-*] insertions: no P-A..P-D numeral
inside any table cell, bound, band, or schema; probe numbers
synthetic; definitional guards (G-04 stronger form [REV-NRS-2],
film-cooling caveat, RMSD_theta never-booked rule) intact and
unedited; addendum-(c) declaration [REV2-r1-15] unchanged in place —
delivery-check item still met.
(g) (vi) row honesty spine intact: WORST cell still asserts NO
argmax-shift number at any grade; [SCHEMA] labels on both routes; O1
gate named in-cell; brief :126 never-value-for-optimum sentence
carried; the S*_red evaluation pin ([REV2-r3-2]) is the executable
choice and consistent with the form-(T) chain.

==============================================================================
## COUNTS (this round, this lens)
BREAKS: 1 (L2-25). REPAIRS: 0. AMENDMENTS: 1 (L2-26).
NOTES: 2 (L2-24, L2-27).
PERSISTING PRIOR FINDINGS RE-COUNTED: 0 (all 4 actionable round-3 L2
findings verified RESOLVED at their edited sites).
DRY VERDICT AT THIS LENS: NOT DRY (1 BREAK sustained). Round 4 is the
cap: per the brief (:154-156), if the BREAK's one-line fix cannot be
adjudicated inside the closure, the prox-regular disjunct is a NAMED
RESIDUE held out per the escalation rule — never label-inflated; note
the fix is a strict NARROWING of a hypothesis line (strike/strengthen
one disjunct), which removes the false license without touching any
other statement of the file.

## PAPERS NEEDED
NONE. (No L2 finding requires a source not already on disk;
Tietze–Nakajima is cited as classical context for the SOUND disjunct,
not load-bearing for any defect claim.)
