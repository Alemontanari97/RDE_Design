# R22-F REFUTATION — ROUND 2, LENS L2 (asymptotics / measure-and-scaling)

Session: S-FOUNDATIONS-C4, Blocco 1 v2, 2026-08-20. Target:
`phaseD/phaseD_r22f_centerpiece.md` REVISION 2 ([REV2-r1-0..21] pass,
1243 lines, read [FULL] including §9 graft table and §10 disposition
table). Numbering continues across rounds: last L2 id = R22F-L2-11, this
round starts at R22F-L2-12.

LENS SCOPE HONORED: 3-6% swirl-energy scaling use; M-RED band derivations
(zero magic constants); epsilon-orders of the 2-epsilon transfer;
degenerate limits; forchetta evidence-class honesty ([SE] labels, CT-6,
definitional guards). RIGHT-SIZING RULE (addendum_c4 (b) / SR-C4-13)
APPLIED: no finding below demands a closed-form gradient bound; L2-14
attacks the SOUNDNESS/label of a printed cell clause and offers a
value-level (not gradient-level) repair — the schema-plus-named-duty
outcome of §2.2-bis is accepted as delivered.

READ-DEPTH DECLARATIONS (this window):
- BRIEF_blocco2_phaseD.md [FULL]; BRIEF_blocco2_phaseD_addendum_c4.md
  [FULL]; phaseD_r22f_centerpiece.md rev-2 [FULL] (1243 lines).
- phaseD_r22f_refute_r1_l2.md [FULL] (my prior round);
  phaseD_r22f_refute_r1_l0.md [PART :1-120 — headers + L0-1..L0-4, the
  findings sharing sites with my L2-3/L2-4];
  phaseD_r22f_refute_r1_l1.md [HEADERS via grep — L1-1..L1-6 titles].
- No paper re-read this window (no L2 finding required one); all prior
  source verifications (FINAL_report :436-437/:450-452, judgeverify
  §5.2/§3.3/ITEM 1.1, findings_registry :1459-1467/:2026, advisory
  :807/:826-834) stand from round 1 and were not re-litigated.

PROBES (pinned env, numpy only; both run PASS this window):
- `r22f_v2_probe_r2_l2_routed_mass.py` (feeds L2-13)
- `r22f_v2_probe_r2_l2_value_grad_gap.py` (feeds L2-14)

MANDATE-COVERAGE DUTY (round 2): channel (vi) row PRESENT with the
delta/mu schema ([REV2-r1-16], table row :975, delta/mu + soundness
conditions (1)-(3) referenced to §2.2-bis) — brief :112-126 satisfied;
Fig. 18 per-term disposition PRESENT (§2.3 table :589-595 with the
[REV2-r1-9] corrected cell readings). No coverage finding.

==============================================================================
## PRIOR-FINDINGS VERIFICATION (all 9 actionable L2 round-1 findings)

### R22F-L2-12 — NOTE (resolution ledger; each disposition verified at
### its edited site, not from the §10 table alone)
- L2-1 (channel (vi) + gradient schema) → RESOLVED: §2.2-bis
  [REV2-r1-8] (:475-535) delivers the SCHEMA + three named derivers +
  soundness conditions (1)-(3); (vi) row [REV2-r1-16] (:975) carries
  delta/mu with mu = measured TR-Newton curvature and the a-posteriori
  basin check. Conforms to addendum_c4 (b): no closed form claimed.
- L2-2 (addendum-(c) declaration) → RESOLVED: [REV2-r1-15] (:954-966)
  lands the record sentence verbatim-equivalent (no-external-referee +
  Harroun-1.25 quasi-cycle-averaged qualifier + CONSEQUENCE), declared
  "this paragraph IS the table's header note". Delivery-check item met.
- L2-3 (DROP sign leg) → RESOLVED: [REV2-r1-2](2) (:172-186) scopes leg
  (i) to physical-h0-fixed comparisons; uncompensated DROP routed to the
  geometry-signed formula with the r_e = r_in degeneracy named;
  [REV2-r1-3] propagates the LOWER-BOUND READING; [REV2-r1-18](b)
  propagates the (iii) rigor column. Algebra re-checked this window:
  Delta u_e^2 = lambda^2 Gamma_0^2 (1/r_in^2 − 1/r_e^2) is exact under
  h0_phys = h0_data + lambda^2 Gamma_0^2/(2 r_in^2) — matches my probe
  and the record's R-B clause.
- L2-4 (2-epsilon mis-assembly) → RESOLVED: [REV2-r1-4] (:270-312)
  reassembles into per-surrogate upper leg (claim 10 verbatim, eps_G)
  + irreducibility lower leg (premise (DR) named, midrange optimality
  osc/2, sharpness up to full oscillation) — all three sub-claims of
  leg (b) re-checked by hand this window: per-fiber Chebyshev center =
  midrange with radius eps_fib/2 (exact in R^1); sup-error ≥ osc/2 on
  any same-fiber pair; sharpness inherits claim 10's two-point
  construction. The inter-fiber unboundedness sentence is present.
  Downstream [T-DISC-4](a) re-pointed by [REV2-r1-5]. Sound.
- L2-5 (Cantor channel) → RESOLVED in substance: [REV2-r1-6] (:421-433)
  strengthens H-RED-2 to SBV (AG-1-valve form, as my repair proposed)
  and declares the propagation to §2.2(i)/§2.5. Residual notation slip
  → L2-15 (AMENDMENT, below).
- L2-6 (Richardson dof) → RESOLVED: [REV2-r1-12] (:799-806) declares a
  ≥ 4-point St-ladder; third divided difference now exists; executable
  as published. (On 4 points the two second divided differences differ
  by (x3−x0)·f[x0..x3] — the declared uncertainty IS that spread's
  carrier; no residual defect.)
- L2-7 (B-2 measure) → RESOLVED: [REV2-r1-13] (:812-817), dmdot =
  rho u_x dA, units re-checked = N, consistent with the per-streamline
  debit.
- L2-8 (fitted-sheet qualifier) → RESOLVED: [REV2-r1-7] (:450-457)
  restores the qualifier at §2.2(i); [REV2-r1-18](a) at the (ii) BEST
  cell.
- L2-9 (anchor name) → RESOLVED: [REV2-r1-17] (:977-981).
- L2-10/L2-11 were NOTES (no action owed); L2-10 correctly FORWARDED to
  the closure judge ([REV2-r1-21] :1169-1172).
PERSISTING PRIOR FINDINGS: 0.

==============================================================================
## NEW FINDINGS (round 2, on revision-2 text)

### R22F-L2-13 — REPAIR (measure accounting; executable): [REV2-r1-14]'s
### routing sentence leaves eps(F) UNDER-DETERMINED at mu_routed > 0 —
### the routed-sector term lies outside the published band algebra
ANCHORS: [REV2-r1-14] :831-840 ("Phases below the floor route to the
declared subsonic sector (T-T3-MAP(d)), never into the march"); §3.1
:744-753 (eps(F) := |J_exact − J_avg|(F), J_exact = ∫ F_true(xi) dmu
over the FULL cycle measure); BAND RULE :841-843 ("every band =
(floating-point floor ...) ⊕ (a curvature/exponent measured ...) ⊕
(declared [INF] model constants ...) — nothing else"); Part 3 header
:739 ("Zero magic constants").
DEFECT (two legs):
(a) SECTOR MISLABEL: phases with M_entry ∈ (1, 1 + m0) are SUPERSONIC —
they are NOT members of Xi_sub as T-T3-MAP(d) defines it (subsonic
entry). "Route to the declared subsonic sector" re-labels a supersonic
phase as-if-subsonic; the map's measure split mu(Xi_sub) is thereby
changed by a spec sentence, not by the data.
(b) eps UNDER-DETERMINATION: the march defines F_2D only on the marched
sector, so for a family with routed mass mu_r > 0 the published eps(F)
admits two inequivalent executor readings — (i) comparison restricted
to the marched sector, or (ii) full-measure J_avg with routed phases
booked through the mixture form (the routing sentence read literally).
Probe `r22f_v2_probe_r2_l2_routed_mass.py` (synthetic toy, Xi_sub
empty by construction, mu_r = 0.149): the two readings differ by
0.0119 = mu_r × (booking error) exactly, while reading (i) gives
0.0000 — the discrepancy term is proportional to routed mass and the
mixture-form model error, and is NOT representable as fp-floor,
measured sweep curvature, or a declared [INF] constant: it sits outside
the band algebra the spec itself declares exhaustive. Degenerate-limit
control (lens duty): at m0 → 0+ the routed bucket empties and the
ambiguity vanishes — the defect belongs to the finite-m0 routing
sentence, not to the floor idea.
REPAIR (cheap, either branch keeps the spec executable-as-published):
(i) REQUIRE mu_routed = 0 per family — for the in-house synthesized
families F-a..F-d a uniform entry margin is a construction property,
trivially checkable at synthesis time (AG-1 valve: strong,
sufficient-not-optimized); any family violating it is a DECLARED
family exclusion, never a silent booking; or (ii) define eps on the
marched sector with mu_routed REPORTED per family and the routed-sector
booking named as a declared-[INF]-class exclusion line in the BAND
RULE. Either way one sentence; the current text supports both readings
and therefore neither.

### R22F-L2-14 — REPAIR (evidence-class honesty + epsilon-order;
### executable): the (vi) BEST cell's plausibility clause rides an
### unstated rate — the derivable value-route shift scale is
### O(sqrt(eps)), which does NOT support "within the in-class
### design-delta scale"
ANCHORS: (vi) row BEST cell :975 ("on-ray, in-class, with the (J)/(H)
gradient channels at their BEST value-level readings, the shift is
plausibly within the in-class design-delta scale [SE — NO number
asserted: delta has no derived bound]"); CELL RULES :932-935 ("NO cell
above its held evidence class"); brief :126 ("never a value-level bound
silently passed off as optimum coverage"); addendum_c4 (b) :15 ("Label
inflation remains forbidden in the other direction"); channel (iv)
WORST cell :973 (in-class deltas = fractions of a point: Hoffman
0.04-0.34%, our +0.51%).
DEFECT: the clause grounds an OPTIMUM-adequacy plausibility ("shift ...
within the in-class design-delta scale") in VALUE-level readings —
note the cell's own wording concedes the (J)/(H) gradient channels
have only "value-level readings". Probe
`r22f_v2_probe_r2_l2_value_grad_gap.py` establishes both directions of
the honest rate:
(A) SOUND CONTENT, never stated in the draft: under a uniform
value-error premise on the basin (a (U)-type premise — exactly M-RED's
measured eps per family) and a curvature floor mu, the shift IS
bounded: |argmax shift| ≤ 2·sqrt(eps/mu) (three-line strong-concavity
proof, verified on adversarial and random families, 50 draws + 4
adversarial constructions, all within bound);
(B) THE RATE IS TIGHT: a perturbation of uniform size eps moves the
argmax by Theta(sqrt(eps/mu)) — probe attains shift ≥ 0.9·sqrt(eps/mu)
at value gap exactly eps; shift/value-gap = 10x at eps = 1e-2 and 100x
at eps = 1e-4 (mu = 1 units).
CONSEQUENCE: sqrt(single-digit-%) is NOT "in-class design-delta scale"
in curvature-normalized units without further premises — the clause's
inference points the OPTIMISTIC way on the user-facing bracket of
record, and its [SE] tag has no scaling derivation of record behind it
(every other [SE] in the file traces to a measured scale or a derived
order; this one traces to a rate never derived, and the only derivable
rate contradicts the conclusion's scale).
REPAIR (right-sized; two acceptable branches, both cheap, neither
demands the gradient bound):
(i) REPLACE the clause with the derivable value-route bound as SCHEMA:
"a-posteriori VALUE-route bound: |argmax shift| ≤ 2·sqrt(eps_U/mu),
eps_U = M-RED measured per-family eps, mu = the measured curvature
floor, valid under the §2.2-bis condition-(1) basin check; note the
sqrt rate — value-level adequacy alone confines the shift only to
O(sqrt(eps)), and ONLY the gradient-route delta/mu (§2.2-bis, deriver
list) can tighten optimum coverage to the in-class delta scale". This
ADDS sound, immediately-derivable content (measured mu exists;
M-RED eps is the named duty) and makes the BEST cell honest with a
stated rate; or
(ii) DELETE the plausibility clause (the cell then rests on the
delta/mu schema + basin check alone) or downgrade it to CONJECTURE
with the missing premise NAMED (a bound on the residual's
design-frequency content — the smooth-dependence premise the sqrt-tight
construction violates).
NOT a closed-form demand: branch (i) is a value-level lemma consuming
already-named quantities; the §2.2-bis gradient SCHEMA is accepted as
delivered (addendum_c4 (b)).

### R22F-L2-15 — AMENDMENT (append-only protocol consistency): declared
### propagations to §2.5 and §2.2(i) are not marked at their sites
ANCHORS: [REV2-r1-6] :421-433 states "(propagated: §2.5 reads
'conditional on H-RED-2(SBV)/H-RED-3/H-RED-4')" — but §2.5 :720-721
still prints "Two-channel reduction: THEOREM* conditional on
H-RED-3/H-RED-4 convention pins" with NO marker at the site; likewise
§2.2(i)'s "exactly TWO first-order channels" :442-443 carries no
site-adjacent pointer to the SBV clause. The file's own protocol
(:11-17) promises "superseded text ... is marked SUPERSEDED at its
site by the adjacent marker block". For a machine-read deliverable a
§2.5-only reader receives the stale conditional list, and the
[REV2-r1-6] sentence "reads" asserts an edit that was not made in
place. One-line fix: add "[see REV2-r1-6: + H-RED-2(SBV)]" at
:720-721 and a site pointer at :442-443 (or reword [REV2-r1-6] from
"reads" to "is to be read as"). No content defect — the semantic
propagation is fully declared.

### R22F-L2-16 — AMENDMENT (CT-6 bright line): the P-A numeral "4%"
### appears inside a table cell of the forchetta
ANCHORS: (vi) WORST cell :975 ("P-A 4% miss — all [ADV], no number
enters the schema"); cell-notes header :993-997 ("NO number below
enters any bound, band, or cell value of the table above");
[GRAFT-G07] :1060-1064 (the same datum already lives in the notes
block: "P-A Fig. 15b drag tail + 4% miss"). My round-1 green-ledger
item L2-11(h) certified "no P-A..P-D number enters any table cell" —
revision 2 breaks that letter (though not the rule's substance: the
numeral is a marker citation, [ADV]-labeled, and does not set the
cell's value, which is "delta UNDERIVED — NO number"). Cheap fix that
preserves the bright line the confirm-verified SYNTHESIS ceiling
(D-8/CT-6) draws: strip the numeral from the in-table cell ("P-A
design-point miss, see [GRAFT-G07]") — the number stays available in
the notes block where the evidence-ceiling banner governs it.

### R22F-L2-17 — NOTE (verified-green ledger, round 2; zero credit
### inflation): revision-2 chains that SURVIVED L2 attack this window
(a) [REV2-r1-2](2) DROP algebra exact (re-derived; matches probe r1 and
R-B); (b) [REV2-r1-4] leg (b) midrange optimality: per-fiber Chebyshev
radius = eps_fib/2, sup-error ≥ osc/2 on same-fiber pairs, sharpness
inheritance — all re-checked by hand, sound at the stated SCHEMA grade;
(c) [REV2-r1-12] ≥4-point ladder dof arithmetic correct; second-divided-
difference spread properly carried by the third difference; (d)
[REV2-r1-13] B-2 measure dimensionally exact (N); (e) §2.2-bis metric
condition (3) (delta in the dual norm of the metric in which mu is the
Hessian's smallest eigenvalue) is the correct norm-invariance
requirement for delta/mu — attack considered (norm mismatch) and
withdrawn: condition (3) already closes it; (f) basin clause (1)
(a-posteriori form, delta/mu ≤ certified basin radius) is the correct
scoping of a POINT-measured mu — degenerate limit mu → 0 correctly
fails the check rather than producing a fake bound; (g) the (vi) seam
[REV2-r1-19] (never summed with (i)-(v)) is the correct composition
statement; (h) [REV2-r1-15] declaration text faithful to addendum_c4
(c) including the qualifier and consequence; (i) CT-6 sweep over all
revision-2 insertions: no P-A..P-D number enters any bound, band, or
cell VALUE (the single in-cell numeral is L2-16, a bright-line
amendment, not a value entry); definitional guards (G-04 stronger form
[REV-NRS-2] + film-cooling caveat) intact; (j) §10 disposition table
counts match the three round-1 files (1 BREAK, 15 REPAIRs, 6
AMENDMENTS, 4 NOTES-no-action).

==============================================================================
## COUNTS (this round, this lens)
BREAKS: 0. REPAIRS: 2 (L2-13, L2-14). AMENDMENTS: 2 (L2-15, L2-16).
NOTES: 2 (L2-12, L2-17).
PERSISTING PRIOR FINDINGS RE-COUNTED: 0 (all 9 actionable round-1 L2
findings verified RESOLVED at their edited sites).
DRY VERDICT AT THIS LENS: NOT DRY (2 REPAIRS sustained; 0 BREAKS).

## PAPERS NEEDED
NONE. (No L2 finding requires a source not already on disk.)
