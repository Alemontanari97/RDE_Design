# ESCALATED REFUTATION — MINOR (c) DELTA-CARRIER, ROUND 2
# S-FOUNDATIONS-C4 escalation window, 2026-08-20. ESCALATED REFUTER
# per VERDICT_blocco2.md §3 E-3 (full-form escalation; single fused
# lens, math structure + carrier/bookkeeping contracts, right-sized by
# the orchestrator). Target: phaseD_minor_deltacarrier.md AS REVISED
# through [ESC-r2-*] (708 lines on disk this window). Inputs of
# record: VERDICT_blocco2 §§2.3/3/4 + refute_minor_deltacarrier.md +
# esc_refute_deltacarrier_r1.md (prior round: 0 BREAK / 1 REPAIR /
# 1 AMENDMENT / 2 NOTE). This file is the ONLY file this slot writes.
# Findings ESC-DELTACARRIER-r2-<n>, classes BREAK/REPAIR/AMENDMENT/
# NOTE. Zero inflation: everything not under FINDINGS was attacked and
# held (VERIFIED-SOUND block). Probe re-run this window.

## READ-DEPTH DECLARATION (this window; all internal, no papers; CT-6
## kept — no number from the four nozzle papers consumed anywhere)

- phaseD_minor_deltacarrier.md [FULL] (whole revised file, all
  [ESC-r1-*] + [ESC-r2-*] markers, disposition tables §11/§12);
- esc_refute_deltacarrier_r1.md [FULL] (whole file: VERIFIED-SOUND
  1-12, findings r1-1..r1-4, sequencing check, machine summary);
- refute_minor_deltacarrier.md [FULL] (whole file, round-0 record);
- VERDICT_blocco2.md §§2.2-2.3, 3, 4 [FULL at sections] (mandate;
  sustained findings + guidance labels re-read, labels NON-ADOPTED);
- phaseD_minor_objdom.md :285-335 [FULL slice] (the consumed OBJDOM-5
  [ESC-r1-3] (H6')/ship-gate block re-read at source and compared
  clause-by-clause with (c) §1 (H6') and DC-5(c)) + full marker census
  re-measured this window (grep -c: [ESC-r1-*] 30, [ESC-r2-*] 18,
  [ESC-r3-*] 6; r3 markers at :28, :38, :128, :520, :553, :582 — the
  r3 delta touches ONLY the OBJDOM-1 (a.2) truncation-order clause,
  exactly as [ESC-r2-2] declares; the (H6') block carries only
  [ESC-r1-3]);
- esc_refute_objdom_r3.md tail [targeted]: dry: TRUE, closure-judge
  ready — (b) loop state re-established before adjudicating the (c)
  consumption; NO esc_refute_objdom_r4.md exists (checked);
- docs/findings_registry.yaml :285-309 [FULL] (row :292-300; the :300
  trigger "sequenced with/after [OBJ-DOM]; ships only with the sliver
  banded or repaired" re-verified character-exact this window);
- blocco3/SYNTHESIS_nozzle_rde_arrivals.md :276-289 (G-12) +
  :408-424 ((e).3) [FULL slices] (graft source re-verified; the
  grafted sentence carries no paper number — CT-6-clean);
- docs/rde_nozzle_MASTER.md :250-266 [FULL slice] (D2.6 GIVEN line +
  c-vector: geometric slots confirmed; "constant Pa > 0" in the (P)
  GIVEN line — see finding r2-3);
- docs/rde_nozzle_P1_sections_5_7.md :72-94 [FULL slice] (T7(c)
  regime-2 free-sign text re-verified for [ESC-r1-7] dormancy);
- esc_probe_deltacarrier_h3breakout.py [FULL] (source re-audited line
  by line, incl. the [ESC-r2-4] tol_A re-derivation) + EXECUTED this
  window: PASS, exit 0 ([A] sup−p_lam = −4.731e-10, tol_A = 2.971e-15,
  aligned attainment exactly 0.0; [B] breakout q = 0.888675,
  exceedance +1.607546e-1 vs required 2.971e-12). Every digit matches
  the revision's §2/[ESC-r2-4] quotes. Re-runnable as claimed;
  pinned-env numpy only, no install.
- File mtimes measured this window (SR-12; command: stat -c '%y %n'):
  phaseD_minor_objdom.md 19:16:17 < esc_refute_objdom_r3.md 19:22:33
  < esc_refute_deltacarrier_r1.md 19:38:02 <
  esc_probe_deltacarrier_h3breakout.py 19:40:06 (the [ESC-r2-4]
  repaired state) < phaseD_minor_deltacarrier.md 19:43:12 (all
  2026-08-20 −0400) — the §12 ordering claim reproduces.

## PRIOR OBJECTIONS (r1) — RESOLVED / PERSISTING (each verified at
## source this window)

- r1-1 (REPAIR, B_fam unrestricted): RESOLVED. [ESC-r2-1] restates the
  DEFINITION B_fam(C) := Int_Xi sup_{A_phase(C) ∩ W_cert} F dmu in
  statement text; legs re-proved independently this window — (a) holds
  with the certified-S hypothesis in statement text; (b) monotonicity
  unaffected (∩ W_cert C-independent); (c) entailed by its own symbols
  via DC-1 member-by-member; (V2) sup restated over the restricted
  set; DC-6 and the §9 landing pinned to the restricted definition
  (bracket-stripping extraction can no longer ship the unrestricted
  symbol). Prior display quoted-as-retracted. The residual seams the
  restated text itself carries are findings r2-1 and r2-2 below — they
  are NEW residues of the repair's supporting text, not a persistence
  of r1-1 (the restriction now lives in the definition, as demanded).
- r1-2 (AMENDMENT, consumption version clause): RESOLVED. [ESC-r2-2]
  corrects [ESC-r1-0] and §11 to "through [ESC-r3-*]" with the
  mtime-proof cited; re-measured THIS window: (b) still at [ESC-r3-*]
  (mtime unchanged 19:16:17, no r4 refutation file, esc_refute_objdom
  _r3.md dry: TRUE) — the corrected declaration is true NOW, not just
  at write time; the explicit closure re-verification duty (judge
  re-checks the consumed (H6')/ship-gate block against (b)'s CLOSED
  text, adoption re-opens on amendment) is stated in the header, and I
  re-verified the consumed block character-consistent with (b) :290-330
  this window.
- r1-3 (NOTE, dangling clause): RESOLVED. §3 reflowed as a full
  sentence ("The split (Pi_c vs delta_true) is NOT separately
  computable today — that is exactly the bound-ladder row's gap."),
  prior fragment quoted in the marker; no content moved.
- r1-4 (NOTE, probe tol_A scale): RESOLVED. tol_A = 64·eps·max(
  rho(lambda)·lambda², p(lambda)) now in the probe with the derivation
  comment rewritten to the largest-intermediate scale; independently
  re-derived this window (rho·lam² = 0.20996 ≈ 18.1x p_lam = 0.011616;
  ~10-flop chain ≤ ~10·eps·max-term, 64 = next power of two with ~6x
  margin — sound); probe re-run PASS exit 0, digits match.

## VERIFIED-SOUND (attacked, held; listed so the closure judge sees
## coverage — all in THIS window)

1. [ESC-r2-1] restated chain, math half: all three legs re-proved (see
   r1-1 above). The B1^c wiring "Hence B1^c <= J_ideal(eps)" holds
   over the restricted family by (b)+(c) regardless of any membership
   claim (the sup over a subset of W_cert-certified members is bounded
   by the {eps}-member ceiling). Sound as displayed. (The MEMBERSHIP
   prose supporting "no consumer loses a member" is finding r2-1.)
2. [ESC-r2-2] state declarations: every checkable claim reproduced
   this window (marker census, mtimes, r3-delta scope, (b) dry state,
   no r4 file). Sound.
3. [ESC-r2-3] reflow: grammatical, content-identical, machine-read
   extraction can no longer mis-attach the clause. Sound.
4. [ESC-r2-4] probe repair: tol_A derivation now at the correct scale;
   re-derivation and re-run both reproduce; the breakout threshold
   1e3·tol_A = 2.971e-12 is scale-separated from the exceedance
   +1.6075e-1 by ~11 orders as claimed. Attainment exact-0 by IEEE
   product commutativity re-confirmed ((rho·lam)·lam vs (lam·rho)·lam,
   commutative factors, identical products). Sound.
5. DC-1 proof: spot re-derivation this window of (ii.b) A'(q) =
   rho(q − lambda)(1 − M²) from dp = −rho q dq, drho = −(rho q/c²) dq
   (matches), of the (ii.c) case-2 chain (B < p − rho c q < p(1 −
   gamma_s) ≤ 0 via q > c and gamma_s ≥ 1 — correct), and of the
   assembly equality at the uniform exit (C_exit^id = lambda·mdot +
   A_e(p(lambda) − Pa) by the definition of lambda — exact). Untouched
   by round 2, still sound.
6. (H6')/ship-gate consistency: (c) §1 (H6'-A)/(H6'-B) and DC-5(c)
   compared clause-by-clause against (b) :290-330 ([ESC-r1-3] block)
   and VERDICT_blocco2 §4(2) — substantively identical, the ship-gate
   sentence verbatim-consistent ("[OBJ-DOM-IMPL] landed and used on
   both sides, OR the net-panel band included"); registry :300 quote
   character-exact. Sound; the two minors still share one gate text.
7. G-12 graft: source re-verified ((e).3 :413-423 + G-12 :280-283);
   the grafted sentence present at BOTH §3 and §9, faithful, [ADV],
   no paper number (the +50-60%/1.2% figures of the source region do
   NOT ride the graft). CT-6-clean. Sound.
8. Disposition table §12: 4/4 rows, every [ESC-r2-*] marker present in
   the body at the claimed sites (grep-verified), retractions
   quoted-as-retracted; landing still PROPOSED, gated on §4(3) + (b)
   closure ([ESC-r1-9] + [ESC-r2-2] closure duty). Compliant.
9. CT-6 whole-file: no number from the four nozzle papers anywhere in
   the revision (checked; only program record numbers with sources and
   the probe's nondimensional outputs).
10. [ESC-r1-4]/[ESC-r1-7]/[ESC-r1-8] (untouched by round 2): anchors
    re-verified at source (M0 D2.6 geometric c-vector; T7(c) :79-84
    free-sign regime-2 line; (H2b) check formula) — still faithful.

## FINDINGS

### ESC-DELTACARRIER-r2-1 [REPAIR] — the r2-repaired definition
paragraph's membership claim "supersonic-exit by construction, hence
in W_cert" is an invalid inference — supersonic SPEED does not imply
(H3) axial supersonicity, and the file's OWN probe half [B]
counter-state is exactly a supersonic-speed cell that breaks the bound

Anchor: revised file §4 definition paragraph ("Every named
instantiation of the family below (uniform-exit relaxation member;
Rao/Guderley-Armitage/Kraiko per-phase constrained solves) is
supersonic-exit by construction, hence in W_cert — no consumer of the
family loses a member under this restriction"); repeated at the B1^c
wiring ("supersonic-exit by construction, hence members of the
restricted family [ESC-r2-1]"), at DC-6 ("the attaining design — the
phase-shared classical constrained optimum — is supersonic-exit by
construction, hence in W_cert, so the attainment claim is unchanged"),
and in the §12 verification record. Provenance: the clause originates
in esc_refute_deltacarrier_r1.md's own named repair ("supersonic-exit
by construction, hence in W_cert") and in the author's round-1
[ESC-r1-6] bracket — a repair can be wrong; this one carries an
invalid inference into the landing-form statement text.

Reason: W_cert requires (H3) — PER-CELL AXIAL supersonicity, u_x −
c > 0 — not supersonic speed q > c. The two are inequivalent, and the
file itself proves it: the probe's half [B] breakout state has q_mid =
0.888675 > c(q_mid) = 0.4920 (supersonic SPEED, on the (H1) isentrope)
with u_x = 1e-6 → G > p(lambda), exceedance +1.61e-1. So
"supersonic-exit ⟹ in W_cert" is exactly the conflation the probe was
built to kill. For the classical solves the true condition is M cosθ
> 1 per exit cell (u_x = q cosθ > c): generically satisfied at
realistic design exit Mach/angles (M_e ≥ 2, lip angles ≲ 15° give
M cosθ ≥ 1.9), but NOT guaranteed "by construction" — a Rao/Kraiko
exit characteristic carries non-uniform flow with nonzero angles, and
low-M_e/large-angle corners of the design space are not excluded by
the construction itself; (H2b)/(H1) membership likewise needs the
shared-data check, not construction. Secondary defect in the same
sentence: the list lumps the "uniform-exit relaxation member" —
which [ESC-r1-6] explicitly declines to claim as an in-class member
(it is the relaxation's maximizer STATE) — together with class-member
solves under one "in W_cert" claim; for the state the restriction is
irrelevant (it sits outside the sup entirely), and listing it as a
family instantiation re-imports the attainment ambiguity [ESC-r1-6]
removed from DC-1.

Failure/consequence: the [ESC-r2-1] repair's own conclusion-
preservation argument ("No conclusion moves for any existing or named
instance", §12) is underwritten by an inference the file's executable
counterexample invalidates. The DC-3 THEOREM legs (a)-(c) are NOT at
risk (they hold over the restricted family with no membership claim
needed — VERIFIED-SOUND 1), and B1^c ≤ J_ideal(eps) stands as
displayed; what fails is the IDENTIFICATION prose: if a classical
solve ever fell outside W_cert, its computed value would not be the
restricted-family sup, [DC-F2-2]'s rung wiring would silently compute
a non-member, and DC-6's attainment claim would be unproven — with
the M0 landing form ([ESC-r2-1] is pinned as the landing text)
carrying the invalid "by construction" inference into [T-DCRX].

Named repair (one clause per site, conclusion-preserving — verified
before naming, AG-1-compliant: a strong trivially-checkable
hypothesis replacing an unproven fact-claim): replace "supersonic-exit
by construction, hence in W_cert" with "axially-supersonic exit
per-cell (u_x > c, i.e. M cosθ > 1 — generically satisfied at
classical design exit Mach/angles and certified a-posteriori by the
same S1 margin check as every consumer), hence in W_cert UPON THAT
CERTIFICATE"; [DC-F2-2] gains the corresponding one-line duty (the
B1^c solves enter the ladder only with their W_cert certificate,
report-on-fail — same pattern as the (H2b) check in [DC-F2-5]); DC-6's
attainment claim inherits the conditionality (its SCHEMA class already
tolerates this); and the "uniform-exit relaxation member" is dropped
from the family-instantiation list (it is the maximizer state, outside
the sup, per [ESC-r1-6] — one parenthetical). No conclusion moves for
any certified instance; every existing consumer is certified.

### ESC-DELTACARRIER-r2-2 [AMENDMENT] — W_cert's phase-dependence is
unpinned: (H2b)/(H3) are phase-data-dependent hypotheses, but the
cycle-integrated displays use W_cert as a xi-independent symbol
without declaring which of the two consistent readings is meant

Anchor: revised file §3 ("W_cert := the certified subclass, i.e. the
members satisfying (H1)-(H5b) of §1" — §1's hypotheses are stated at
ONE per-phase data set (mdot, h0, s0)); §4 B_fam definition (sup over
A_phase(C) ∩ W_cert INSIDE Int_Xi ... dmu(xi)); DC-3(a) ("any single
CERTIFIED S (S in W_cert) feasible for all phases"); DC-3(c) ("every
member of A_phase({eps}) ∩ W_cert satisfies (H1)-(H5b) by definition"
— at which phase?); also DC-1's MU-INTEGRATED FORM (pointwise
domination silently needs (H1)-(H5b) at mu-a.e. xi).

Reason: (H2b) (mdot/A_e < rho*·c*) and (H3) certification depend on
the phase data s(xi), so "satisfying (H1)-(H5b)" is a PER-PHASE
predicate; a xi-independent W_cert must mean the all-phase (mu-a.e.)
certified subclass, or else W_cert(xi) varies inside the integral.
EITHER consistent pinning closes every proof (checked this window:
per-phase reading — each inner sup is over members certified at that
xi, DC-1 applies phase-by-phase; all-phase reading — smaller sets,
same chains), and every named consumer (the certified S*, the
classical solves under r2-1's repaired certificate) is certified at
every phase, so no conclusion moves under either. But an UNPINNED
mixed reading is unsound: S certified at one phase only, read into
DC-3(a)'s "S in W_cert", makes F[S; s(xi)] ≤ sup_{A ∩ W_cert} F
unproven at the uncertified phases. The file's own standard
([ESC-r1-3]: "a THEOREM*-class display may not leave it implicit")
applies to the symbol [ESC-r2-1] just promoted into the definition;
(V1) pins A_phase(C)'s xi-independence explicitly while W_cert's is
left silent — the asymmetry is the defect. AMENDMENT, not REPAIR:
both self-consistent readings are sound, no display is false under
either, and the fix is one definitional clause (contrast r1-1, where
the restriction was absent from the definition altogether).

Named repair (one clause, conclusion-preserving): pin in §3, e.g.
"W_cert membership is evaluated at the phase data in play; in
cycle-integrated displays W_cert denotes the mu-a.e.-all-phase
certified subclass (equivalently, write W_cert(xi) inside the
integral — both readings agree for every named consumer, all of which
are certified at every phase)"; and one clause in DC-1's
MU-INTEGRATED FORM ("(H1)-(H5b) at mu-a.e. xi") closing the same
implicitness where it pre-existed.

### ESC-DELTACARRIER-r2-3 [NOTE] — landing adjacency: M0 D2.6's GIVEN
line declares "constant Pa > 0", while [T-DCRX] lands with Pa ≥ 0
covering the vacuum instances that are the engine's only executed ones

Anchor: docs/rde_nozzle_MASTER.md :251 ("envelope E ..., constant
Pa > 0") vs revised file (H4) Pa ≥ 0 ([ESC-r1-4]) and §9 landing text.

Reason: no contradiction — a lemma may carry broader hypotheses than
(P), and the Pa-mismatch between (P)-as-posed and the executed vacuum
objective is the known third [OBJ-DOM] axis, already consumed by
DC-5(b)'s "same Pa convention, declared" line. But at the M0 landing
the two statements become PAGE-ADJACENT: D2.6 poses (P) with Pa > 0
three Parts above a [T-DCRX] whose hypothesis line says Pa ≥ 0 and
whose only executed instances are Pa = 0. One sentence in the §9
landing text ("Pa ≥ 0 deliberately widens (P)'s Pa > 0 to cover the
executed vacuum objective — the [OBJ-DOM] Pa axis; argmax untouched,
the offset is the class-constant Pa·Int_wall n_x dA") spares the M0
reader the apparent conflict and names its owner. Editorial; no
statement of the revision is wrong; unraised in all three prior
rounds (checked).

## SEQUENCING CHECK (SR-C4-9, explicit)

VERDICT_blocco2 §4(1)-(3) compliance of the round-2 revision:
VERIFIED, unchanged from round 1 — the (H6') two-branch form and the
ship-gate are character-consistent with the escalated (b) source
(:290-330, re-read this window) and with §4(2); registry :300 license
quoted character-exact; [ESC-r1-9] + the [ESC-r2-2] closure duty gate
the landing on (b)'s closure. Loop state re-measured this window: (b)
at [ESC-r3-*], its r3 refutation DRY, closure-judge ready, NOT closed
— so the (c) closure judge's re-verification duty on the consumed
block remains LIVE, and nothing may land yet. No inconsistency
between the two minors as revised.

## PAPERS NEEDED

(empty)

## MACHINE SUMMARY

findings: 0 BREAK, 1 REPAIR (r2-1: "supersonic-exit by construction,
  hence in W_cert" is an invalid inference — supersonic speed does
  not give (H3) axial supersonicity, the file's own probe half [B]
  counter-state is a supersonic-speed cell breaking the bound; the
  classical-solve membership is a checkable a-posteriori certificate,
  not a construction fact; one-clause fix named per site, plus the
  relaxation-STATE mislisted as a family member against [ESC-r1-6]),
  1 AMENDMENT (r2-2: W_cert's phase-dependence unpinned — (H2b)/(H3)
  are per-phase predicates, cycle-integrated displays use the symbol
  xi-independently; both consistent readings sound, mixed reading
  unsound, one-clause pin named), 1 NOTE (r2-3: M0 D2.6 "Pa > 0" vs
  [T-DCRX] Pa ≥ 0 landing adjacency — one sentence at landing).
prior_objections: r1-1 RESOLVED (restricted definition verified
  leg-by-leg; its supporting membership prose is the NEW r2-1);
  r1-2 RESOLVED (corrected declaration true at re-measurement: (b)
  still r3/DRY, no r4); r1-3 RESOLVED (reflow verified); r1-4
  RESOLVED (tol_A re-derivation verified analytically + probe re-run
  PASS, digits match).
core_verdict: all four round-2 repairs are SOUND as applied to their
  targets; DC-1's proof, DC-2's restated chain, the (H6')/ship-gate
  adoption, and the displayed inequalities of DC-3 all hold; the
  content-bearing residue is confined to the membership/attainment
  PROSE of the [ESC-r2-1] paragraph (r2-1) and the W_cert
  phase-quantifier (r2-2) — both conclusion-preserving one-clause
  fixes, neither touches a displayed bound.
dry: FALSE (one REPAIR outstanding) — one more author pass on r2-1
  (+ the r2-2 clause) should reach DRY; nothing found touches DC-1,
  DC-2, DC-4, DC-5, or any adopted (H6')/gate text.
probes: esc_probe_deltacarrier_h3breakout.py re-run this window,
  exit 0, all quoted digits reproduced; re-runnable, pinned-env
  numpy only, no install.
ct6: clean (this file consumes no paper number).
