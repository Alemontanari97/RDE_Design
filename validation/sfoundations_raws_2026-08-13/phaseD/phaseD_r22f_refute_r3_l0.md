# R22-F CENTERPIECE — REFUTATION, ROUND 3, LENS L0
# (variational/optimization structure: fiber lower bounds, J-variation,
# duality, channel (ii)/(vi) quantification structure — refined
# criterion: delta/mu schema = mu measured engine-curvature carrier,
# delta = design-gradient-level residual bound)

Session: S-FOUNDATIONS-C4, Blocco 1 v2 continuation, 2026-08-20.
Target: validation/sfoundations_raws_2026-08-13/phaseD/
phaseD_r22f_centerpiece.md (author draft revision 1 + reviser passes
[REV2-r1-0..21] and [REV2-r2-0..8], 1537 lines, incl. §§9-11 graft +
disposition tables). Numbering continues across rounds: prior L0 IDs
end at R22F-L0-15; this round starts at R22F-L0-16.

READ-DEPTH DECLARATIONS (this window):
- BRIEF_blocco2_phaseD.md (as amended): [FULL] (209 lines; CENTERPIECE
  section re-read against the round-3 duties).
- BRIEF_blocco2_phaseD_addendum_c4.md: [FULL] (47 lines).
- phaseD_r22f_centerpiece.md: [FULL] (1537 lines, incl. all [REV2-r2-*]
  blocks, §11 round-2 disposition table, and the [REV2-r2-6] in-place
  cell edits with superseded fragments).
- phaseD_r22f_refute_r1_l0.md / r2_l0.md: [FULL] (my lens, both rounds
  — prior-objection verification duty).
- phaseD_r22f_refute_r2_l1.md / r2_l2.md: [FULL] (282 + 262 lines —
  the round-2 findings whose fixes [REV2-r2-4/5/6] I verified at-site
  for consistency with my lens's sites).
- phaseD_r22f_refute_r1_l1.md: [PART :1-120] (headers, coverage duty,
  L1-1..L1-3 — site overlap with my L0-1..L0-3); phaseD_r22f_refute_
  r1_l2.md: [PART :1-100] (headers, L2-1..L2-3 — site overlap with my
  L0-4..L0-6). Their round-1 dispositions were independently verified
  RESOLVED by the r2_l1/r2_l2 files read [FULL] above; no round-3 L0
  finding leans on the unread tails.
- docs/rde_nozzle_MASTER.md: NOT re-read this window. The S20-block
  anchors load-bearing below (M0:2178-2183 margin-multiplier KKT;
  M0:2208-2211 margin-active measured-supported hypothesis;
  M0:2307-2310 O1 = B-stationarity only until discharged) were
  source-verified in MY round-2 window (r2_l0 READ-DEPTH block, of
  record) and the draft text citing them is unchanged since; no new
  M0 claim is made this round.
- swirl5f carriers / SYNTHESIS / FIELD_ATLAS / NOZZLE_RDE_STUDY
  dossiers: [NOT-READ] this window — no L0 finding leans on them.
EXECUTABLE PROBE (standing preference honored): validation/
sfoundations_raws_2026-08-13/phaseD/r22f_v2_probe_r3_l0_nonconvex_
feasible.py — RUN this window, ALL ASSERTS PASS; synthetic 1-D
counter-model only (CT-6 clean: no number from the four nozzle papers
used anywhere).

==============================================================================
## MANDATE-COVERAGE DUTY (round-3, all lenses) — RESULT

Channel (vi) with the delta/mu schema: **PRESENT** — forchetta row
(vi) :1155 as amended by the [REV2-r2-6] cell edits (a)-(d), backed by
§2.2-bis :509-572 + [REV2-r2-3] :573-665 (mu = measured carrier with
the O1 gate declared; delta = SCHEMA + named derivers; "where not
derivable, the cell says so" clause satisfied in the WORST cell).
Fig. 18 per-term disposition: **PRESENT** (§2.3 exhibit :667-676 +
disposition table :717-723 with the [REV2-r1-9] joint-nullity wording
and [REV2-r1-10] per-instance carriage — unchanged since round 2).
Both mandated elements verified in the deliverable text. Coverage
duty: no missing-element REPAIR this round.

## PRIOR-OBJECTION VERIFICATION (my lens, rounds 1-2 → disposition check)

Round-1 (L0-1..L0-9): all dispositions verified RESOLVED in my round-2
window (r2_l0 §PRIOR-OBJECTION VERIFICATION, of record); the one
persisting limb (L0-6 → re-filed L0-12) is closed this round, below.
Round-2 (L0-10..L0-15), each verified AT ITS EDITED SITE this window:
- R22F-L0-10 (BREAK, cone-pairing unsoundness) — **RESOLVED**:
  [REV2-r2-3](A) supersedes conditions (1)-(2) wherever they pair
  critical-cone curvature with a critical-cone-projected residual, and
  licenses exactly the two probe-verified forms: (T) tangent-cone
  (delta and mu on the SAME cone) and (C) critical-cone under the
  multiplier-margin clause (Bonnans-Shapiro regime). I re-derived the
  form-(T) VI chain by hand this window on a convex feasible set:
  with x1 = reduced argmax, x2 = true argmax, VI at x1 for grad J_red
  and at x2 for grad J_true plus the mu_T strong-concavity line give
  mu_T·||x1−x2||^2 ≤ <g(x1), x2−x1> ≤ delta_T·||x2−x1|| — sound, and
  it consumes only g at the computed point S* (see L0-17). The fix is
  faithful to my round-2 repair paths (a)/(b) — my own prescription
  carried the convex-set scope only as a parenthesis, and the reviser
  applied it faithfully; the licensing-carriage defect found this
  round (L0-16) is NEW text ([REV2-r2-3](D)/H-G4/R-13), not a
  persisting count.
- R22F-L0-11 (carrier overclaim / O1 gate) — **RESOLVED**:
  [REV2-r2-3](D) splits schema object vs measured carrier, names the
  O1 gate (H-G4), and [REV2-r2-6](b) qualifies the cell parenthesis
  with the superseded fragment quoted verbatim; residue R-13 carries
  the gate. Matches the round-2 fix shape exactly.
- R22F-L0-12 (persisting §1.3 parenthetical) — **RESOLVED**:
  [REV2-r2-1] :283-297 supersedes the parenthetical and prints the
  corrected scoped restatement (physical-h0-fixed / DROP
  geometry-signed / r_e = r_in corner) in exactly the form my round-2
  repair line specified; landing rule extended so the corrected form
  lands ([REV2-r2-8](b)).
- R22F-L0-13 (basin-radius deriver unnamed) — **RESOLVED**:
  [REV2-r2-3](E) names the curvature re-measure ladder as the deriver
  (M-RED rider extension, F2), residue R-12 carries it, cell
  references it.
- R22F-L0-14 (typing + symbol hygiene) — **RESOLVED**: [REV2-r2-3](B)
  declares the M-Riesz/Moreau support-function form; (C) orders the
  mu → mu_curv rename at landing + the sign convention (checked: for
  negative-definite H_reduced, smallest eigenvalue of −H = |largest
  eigenvalue of H| — correct as printed); [REV2-r2-8](a) carries it.
- R22F-L0-15 was a NOTE (no action owed).

**PERSISTING PRIOR OBJECTIONS RE-COUNTED THIS ROUND: 0.**

==============================================================================
## FINDINGS

### R22F-L0-16 — REPAIR (executable counterexample RUN): the convexity
### hypothesis of the licensed argmax-shift forms is not CARRIED by the
### licensing sentences — H-G4/R-13 license form (T) at a margin-active
### S* flatly, and the value-route lemma (F) omits the hypothesis
### entirely; on a nonconvex feasible set BOTH routes fail unboundedly
### while every other printed condition (incl. form (C)'s
### multiplier-margin clause) holds

ANCHORS: form (T) definition :589-595 — the convex-set hypothesis
exists ONLY as the parenthesis "(unconditional on convex feasible
sets)" (:589; the word "convex" appears nowhere else in the file,
grep-verified this window); H-G4 :639-642 — "at a margin-active S*
... until then only form (T) with the tangent-cone floor IS LICENSED
THERE" (no convexity carriage); R-13 :1363-1367 — "form (T) remains
licensed" (same); value-route lemma (F) :651-665 — hypotheses named =
uniform value-error premise + "a curvature floor mu per (A)", no
feasible-set hypothesis; (vi) BEST cell :1155 — licenses both the
a-posteriori delta/mu form ("cone-pairing + licensed forms (T)/(C)
per [REV2-r2-3](A)") and the value-route bound with no feasible-set
condition in the check list. RIGHT-SIZING COMPLIANCE: this attacks
the SOUNDNESS of the licensing sentences, not closure of the gradient
bound (addendum_c4 (b): "if you believe the schema itself is UNSOUND,
that IS attackable"); SR-C4-13 respected.

DEFECT: both licensed shift routes are theorems ONLY on convex
feasible sets — the VI/strong-monotonicity argument behind (T) and
the strong-concavity value argument behind (F) each need the segment
between the two argmaxes to be feasible (x2 − x1 must be a feasible
direction at x1, and the VI at x2 must test against x1). The draft
prints that hypothesis once, as a parenthesis inside (T)'s
definition, and then LICENSES the forms in three places that do not
carry it — including exactly the locus where it is least available:
the margin-active S*, whose KS-aggregated fold-margin constraint
surface is the record's own cusp candidate (M0:2307-2310, O1;
M0:2208-2211 margin-active = the measured-supported hypothesis —
both source-verified in my r2 window). No convexity certificate for
the admissible profile manifold ∩ {margin ≥ 0} exists anywhere of
record, and H-G4's whole purpose is that locus.

EXECUTABLE COUNTEREXAMPLE (probe r22f_v2_probe_r3_l0_nonconvex_
feasible.py, RUN, all asserts PASS; synthetic, CT-6 clean): 1-D
design space, metric M = I (condition (3) holds), feasible set
C = (−∞,−1] ∪ [+1,∞) (nonconvex, two branches — the 1-D cartoon of a
margin-type constraint x^2 ≥ 1), J_red = −x^2 + 0.002x (strict argmax
x1 = +1), J_true = −x^2 − 0.005x (strict argmax x2 = −1), curvature
−J'' = 2 = mu CONSTANT (uniform floor on every ball: any basin-radius
certificate passes condition (1)'s check). Results:
  (A) form (T): T_C(+1) = {d ≥ 0}; support-function delta_T =
  sup{−0.007·d : d ∈ [0,1]} = 0 → licensed bound 0; actual global
  shift = 2. UNBOUNDED violation.
  (B) value route (F): eps_U = 0.0105 on the certified ball; bound
  2·sqrt(eps_U/mu) = 0.145; actual shift = 2 → 13.8x violation.
  (C) form (C)'s multiplier-margin clause is SATISFIED and blind:
  the constraint is active at x1 with multiplier lam = 0.999 >> the
  perturbation (0.007), strict complementarity holds, and the LOCAL
  argmax on the +1 branch does not move at all (local shift = 0, the
  Bonnans-Shapiro regime working as designed) — while the GLOBAL
  argmax migrates to the other feasible component. The
  multiplier-margin clause protects local active-set stability only;
  global migration across disconnected (or nonconvex) feasible
  regions is structurally invisible to every printed check.
  (D) convex control [−R, R]: both bounds hold, form (T) tight
  (shift = delta_T/mu exactly) — nonconvexity isolated as the sole
  killer.

WHY REPAIR, NOT BREAK: the counter-model VIOLATES a printed
hypothesis of form (T) (the :589 parenthesis), so no licensed form is
false under its own full hypothesis set — the defect is that three
licensing sentences (H-G4, R-13, the (vi)-cell check list) and the
(F) lemma's hypothesis list fail to CARRY that hypothesis, at sites
where its truth is precisely what is in doubt. Same defect class as
round-2 L1-7 (a load-bearing licensing hypothesis whose stated form
does not cover the named exposure), one hypothesis line short. NOT a
persisting count: [REV2-r2-3] is round-2 text, and my own round-2
repair path carried the same parenthesis-only scoping.

FIX SHAPE (AG-1 valve: strong, trivially checkable where checkable,
sufficient-not-optimized; one hypothesis line + three wording
touches):
  (a) new hypothesis line H-G5 beside H-G1..H-G4: the feasible set
  intersected with the certified ball is CONVEX (or locally convex /
  prox-regular with the shift ball inside one connected component) —
  declared and checked per instance; trivially checkable for
  box/linear design constraints; for the KS-aggregated margin set at
  a margin-active S* its status is OPEN and says so, named beside
  O1/R-13 (the licensing sentence there then reads "form (T) is
  licensed under H-G5", never flatly).
  (b) bind (F) to H-G5 explicitly (its strong-concavity step consumes
  the same segment-feasibility).
  (c) constructive coverage remark (cheap, optional but closes the
  honest gap): global migration across feasible components/nonconvex
  lobes is a VALUE-comparison event, not a gradient event — it IS
  coverable by branch-wise application of the licensed forms plus a
  cross-branch value-dominance margin > 2·eps_U (M-RED's own measured
  quantity); if not added, the (vi) row must say global-argmax
  coverage on a nonconvex feasible set is NOT claimed by any form of
  this schema (which the WORST cell's honesty line almost, but not
  quite, already says — it attributes the no-coverage to delta being
  underived, not to the feasible-set geometry).

### R22F-L0-17 — AMENDMENT (one-sentence pin): the evaluation point of
### the support-function residual is left implicit — the VI argument
### needs g and the cone AT THE COMPUTED REDUCED ARGMAX S*, which is
### also the only computable choice

ANCHORS: (B) :604-612 — delta_cone := sup{<g, d> : d in cone,
||d||_M ≤ 1} names neither the point where the gradient gap g is
evaluated nor the point whose tangent/critical cone is meant; §2.2-bis
header :516-521 defines delta(S) at generic S; the a-posteriori check
(condition (1) :555-561, (vi) cell :1155) consumes "delta/mu" without
pinning S.
REASON: the form-(T) derivation (re-checked this window, see L0-18(a))
consumes EXACTLY g(S*_red) paired with directions in T_C(S*_red) —
the REDUCED (computed) optimum: the true argmax is not available, and
evaluating at any other point breaks the chain <g(x1), x2−x1> ≤
delta_T·||x2−x1||. This is good news for executability (one-point
evaluation at the point the engine already owns) and should be
PINNED, not inferred: a future M-RED gradient-rider executor reading
(B) alone could legitimately evaluate delta at a family of points or
at the wrong cone. One sentence in (B): "g and the cone are evaluated
at the computed reduced optimum S*_red; the bound then reads
||S*_true − S*_red|| ≤ delta(S*_red)/mu". Clearly-right, no new math,
judge-adjudicable without a round (brief LOOP rule).

### R22F-L0-18 — NOTE: verified-at-source items this round (positive
### record for the judge; no action)

(a) Form (T) soundness on convex sets re-derived by hand this window
(VI at both argmaxes + strong concavity along the feasible segment);
constant 1 (delta_T/mu_T) confirmed; only g at S*_red is consumed —
consistent with the probe's convex control (PART D, bound tight).
(b) Value-route constant re-derived: J_true(x2) − J_true(x1) ≤ 2eps_U
and ≥ (mu/2)·shift^2 give shift ≤ 2·sqrt(eps_U/mu) — the (F) constant
is correct, and the [REV2-r2-6](a) cell text quotes it faithfully.
(c) Sign convention in (C) checked: smallest eigenvalue of −H_reduced
= |largest eigenvalue of the negative-definite H_reduced| — correct.
(d) The measured full-space curvature floor is a CONSERVATIVE valid
instantiation of any cone floor (min over a cone ≥ min over the full
space), so (D)'s carrier split is sound at an interior S* and the
carrier remains usable for form (T) as a conservative mu_T — worth a
half-sentence at landing but not owed (no defect).
(e) [REV2-r2-6] in-place cell-edit protocol verified: all four
superseded fragments quoted verbatim; no silent deletion; the edited
(vi) row is consistent with (F)/(D)/H-G4 as printed.
(f) §11 disposition table checked against the actual edit sites for
all six L0 rows — accurate.
(g) CROSS-LENS HYGIENE REMARK (L2 territory, forwarded to the judge
per the zero-inflation rule; no L0 action): the §5.1 HEADLINE BEST
bullet (:1308-1310) prints "the gap is plausibly SINGLE-DIGIT
PERCENT" WITHOUT the good-fitted-sheet qualifier that [REV2-r1-7]
declared load-bearing and restored at §2.2(i) and the (ii) BEST cell
([REV2-r1-18](a)) — a third site carrying the shortened phrasing. The
headline is "the licensed phrasing, nothing stronger" by its own
header, so the omission is exactly the class [REV2-r1-7] repaired
elsewhere. One-line judge-adjudicable propagation.

==============================================================================
## PAPERS NEEDED

NONE for this refutation. (All attacks close on carriers already on
disk; no procurement would change any finding above.)

## MACHINE SUMMARY (round 3, lens L0)

breaks = 0
repairs = 1 (R22F-L0-16)
amendments = 1 (R22F-L0-17)
notes = 1 (R22F-L0-18)
persisting_prior = 0 (all five actionable round-2 L0 objections
verified RESOLVED at their edited sites; round-1 chain closed at
round 2 + the L0-12 limb closed this round)
probe: r22f_v2_probe_r3_l0_nonconvex_feasible.py — RUN, all asserts
PASS; synthetic 1-D counter-model only (CT-6 clean).
dry_at_this_lens: NO (1 REPAIR standing)
