# R22-F CENTERPIECE — REFUTATION, ROUND 2, LENS L0
# (variational/optimization structure: fiber lower bounds, J-variation,
# duality, channel (ii)/(vi) quantification structure — refined
# criterion: delta/mu schema = mu measured engine-curvature carrier,
# delta = design-gradient-level residual bound)

Session: S-FOUNDATIONS-C4, Blocco 1 v2 continuation, 2026-08-20.
Target: validation/sfoundations_raws_2026-08-13/phaseD/
phaseD_r22f_centerpiece.md (author draft revision 1 + [REV2-r1-0..21]
reviser pass + §9 graft table + §10 disposition table). Numbering
continues across rounds: prior L0 IDs end at R22F-L0-9; this round
starts at R22F-L0-10.

READ-DEPTH DECLARATIONS (this window):
- BRIEF_blocco2_phaseD.md (as amended): [FULL] (209 lines).
- BRIEF_blocco2_phaseD_addendum_c4.md: [FULL] (46 lines).
- phaseD_r22f_centerpiece.md: [FULL] (1243 lines incl. §§9-10).
- phaseD_r22f_refute_r1_l0.md / _l1.md / _l2.md: [FULL] (all three
  prior v2 refutations; reviser dispositions read in the draft §10).
- docs/rde_nozzle_MASTER.md: [SECTION] :2170-2320 (S20 tier-ladder /
  KKT-with-margin-multiplier block, EQ-v2 registration, OBLIGATIONS
  LEDGER O1-O4) — load-bearing for R22F-L0-11/L0-14, verified at
  source this window. Round-1 anchor verifications (M0 :74, :390-397,
  :860-919, :1077-1183) NOT re-read — no finding below re-leans on
  them beyond the round-1 verification of record (R22F-L0-9).
- swirl5f carriers: NOT re-read this window; claim-10 source
  verification stands from round 1 (swirl5f_fun.md :535-594,
  [SECTION], R22F-L0-4/L0-9) and no round-2 finding re-litigates it.
- SYNTHESIS_nozzle_rde_arrivals.md / FIELD_ATLAS_targeted_c4.md /
  the four NOZZLE_RDE_STUDY dossiers: [NOT-READ] this window — no L0
  finding leans on them.
EXECUTABLE PROBE (standing preference honored): validation/
sfoundations_raws_2026-08-13/phaseD/r22f_v2_probe_r2_l0_activeset_
shift.py — RUN this window, all asserts PASS; synthetic quadratic
counter-model only (CT-6 clean: no number from the four nozzle papers
used anywhere).

==============================================================================
## MANDATE-COVERAGE DUTY (round-2, all lenses) — RESULT

Channel (vi) with the delta/mu schema: **PRESENT** — forchetta row
(vi) :975 ([REV2-r1-16]) + §2.2-bis :475-535 ([REV2-r1-8]); the
"where not derivable, say so + name the deriver" clause (brief
:122-126) is satisfied (WORST cell "delta UNDERIVED", derivers named
in order). Fig. 18 per-term disposition: **PRESENT** (§2.3 exhibit
:539-548 + disposition table :589-595 with the [REV2-r1-9] joint-
nullity wording and [REV2-r1-10] per-instance carriage). Both
mandated elements verified in the deliverable text. Coverage duty:
no missing-element REPAIR this round.

## PRIOR-OBJECTION VERIFICATION (my lens, round 1 → disposition check)

- R22F-L0-1 (no (vi) row) — RESOLVED: [REV2-r1-16] row present with
  BEST/WORST/provenance/what-tightens-it; brief clause satisfied.
- R22F-L0-2 (no gradient-level schema) — RESOLVED: §2.2-bis present
  (hypothesis list H-G1..H-G3, channel decomposition, three named
  derivers, honest STATUS per addendum_c4 (b)).
- R22F-L0-3 (addendum-(c) declaration) — RESOLVED: [REV2-r1-15]
  lands the record sentence as the table's header note, verbatim-class.
- R22F-L0-4 (BREAK, 2-epsilon transplant) — RESOLVED: [REV2-r1-4]
  two-leg reassembly re-checked at this lens this window: leg (a) =
  claim 10 per-surrogate (premise (U_G), conclusion 2·eps_G — matches
  the round-1 source verification of the lemma); leg (b) =
  irreducibility floor with premise (DR) named, osc/2 lower bound and
  midrange-attainment both sound (one-line algebra re-checked); the
  irreducibility sentence correctly attaches to leg (b) only; the
  inter-fiber unboundedness caveat is printed. Downstream propagation
  [REV2-r1-5] correct ([T-DISC-4](a) now cites leg (b)).
- R22F-L0-5 (pressure-term fixity / actual pairs) — RESOLVED:
  [REV2-r1-2](1)(3) + [REV2-r1-3]: H2.2 re-anchor, booking-level
  label, actual-pair form routed to M-RED/B-2, probe context kept at
  plausibility grade. Correct.
- R22F-L0-6 (DROP-branch sign reversal) — RESOLVED IN THE MAIN,
  ONE LIMB PERSISTING: [REV2-r1-2](2) delivers the geometry-signed
  formula (algebra re-checked this window: Delta u_e^2 = lambda^2
  Gamma_0^2 (1/r_in^2 − 1/r_e^2) — exact), the r_e = r_in degeneracy
  is named, [REV2-r1-3]/[REV2-r1-18](b) propagate to §1.2 and the
  (iii) rigor column. NOT propagated: the §1.3 parenthetical (see
  R22F-L0-12 — persisting REPAIR, re-counted).
- R22F-L0-7 (D1.2 pin) — RESOLVED: [REV2-r1-1] pins the calibrated-
  scalars fiber and propagates to eps_fib and [T-DISC-4](a).
- R22F-L0-8 (delta/mu soundness conditions) — APPLIED as ordered
  ([REV2-r1-8] conditions (1)-(3), carried into the (vi) row). The
  round-2 refined criterion now finds the applied conditions
  INSUFFICIENT in the constrained case — see R22F-L0-10. That is a
  NEW defect in new text (my own round-1 conditions had the hole; the
  reviser applied them faithfully), not a persisting one.

Persisting prior objections re-counted this round: 1 (the L0-6 limb,
re-filed as R22F-L0-12).

==============================================================================
## FINDINGS

### R22F-L0-10 — BREAK (executable counterexample RUN): the delta/mu
### schema's printed soundness conditions (1)-(3) do NOT make the
### licensed a-posteriori form sound — critical-cone mu fails under
### active-set migration, and condition (2)'s projection makes the
### violation unbounded

ANCHORS: §2.2-bis :481-486 (delta := ||P_T[grad J_true − grad
J_red]||_{M*}, P_T = "projection on the feasible directions (critical
cone at the active set)"); condition (1) :519-525 (a-posteriori form
"valid IF delta/mu ≤ the certified basin radius"); condition (2)
:526-531 (mu = reduced Hessian on the critical cone, delta = the
PROJECTED residual); forchetta row (vi) BEST cell :975 (licenses
exactly that form). Addendum_c4 (b) :8-15: demanding a closed form is
not a break, but "if you believe the schema itself is UNSOUND, that
IS attackable" (mandate wording) — this finding attacks soundness,
not closure. SR-C4-13 respected.
DEFECT: the argmax-shift bound |shift| ≤ delta/mu with mu = the
CRITICAL-CONE reduced-Hessian floor is false when the perturbation
moves the active set — and every printed condition can hold in the
failure. EXECUTABLE COUNTEREXAMPLE (probe r22f_v2_probe_r2_l0_
activeset_shift.py, RUN, all asserts PASS): strictly concave
separable quadratic on {y ≤ 0}, curvature mu = 1 in the critical-cone
direction, nu = 1e-3 in the constrained direction, active constraint
with multiplier lambda = 0.05 and STRICT COMPLEMENTARITY at S*_red;
metric M = I (condition (3) satisfied); Hessian CONSTANT, so the
curvature floor is uniform on every ball and any finite basin-radius
certificate passes condition (1)'s check. Gradient gap (0, −2c),
c = 0.05: unprojected dual-norm delta = 0.1, licensed bound
delta/mu = 0.1; ACTUAL shift = c/nu = 50 — violation 500x. WORSE,
with condition (2)'s OWN delta (the residual PROJECTED onto the
critical cone): the gap (0, −2c) is M-orthogonal to the critical cone
{d_y = 0}, so P_T[gap] = 0, delta = 0, the licensed bound predicts
ZERO shift, and the violation is UNBOUNDED. The projection in the
delta definition removes exactly the component that drives the
migration. The two cone names in :484-485 ("feasible directions" vs
"critical cone") are DIFFERENT objects at a strict-complementarity
active point, and the schema currently takes the curvature from the
small cone and (per its own definition) the residual from the same
small cone — the unsound pairing.
WHY BREAK: a licensed bound-form of the deliverable admits a cheap
counter-model that satisfies every printed licensing condition; the
(vi) BEST cell would certify a false shift bound the moment a
measured delta lands (no number is asserted TODAY, which is why the
draft is not convicted of a false NUMBER — the licensed FORM is
false, which the schema's whole purpose is to prevent).
REPAIR PATH (both legs probe-verified in the same run; either
restores soundness — one paragraph, AG-1 valve):
  (a) TANGENT-CONE FORM (unconditional on convex feasible sets): take
  mu = the curvature floor over the TANGENT (feasible-direction) cone
  on the ball and delta = the residual dual-norm on the SAME cone;
  the strong-monotonicity/VI argument then gives |shift| ≤ delta/mu
  with no active-set hypothesis (probe repair R-a: bound 100 ≥ shift
  50, holds). Cost: mu degrades to the smallest feasible-direction
  curvature.
  (b) MULTIPLIER-MARGIN CLAUSE (keeps the sharper critical-cone mu):
  license the critical-cone form ONLY for perturbations below the
  active-set stability threshold — each active multiplier ≥ its
  named margin against delta (probe repair R-b: delta' = 0.025 <
  lambda keeps the active set, shift 0 ≤ delta'/mu, holds). This is
  the Bonnans-Shapiro-class strict-complementarity stability regime;
  state the clause beside condition (2) and in the (vi) BEST cell's
  check list.
  Either way condition (2)'s delta must pair with the cone the mu is
  taken on — never curvature on the small cone with residual
  projected onto the same small cone while the shift explores the
  large one.

### R22F-L0-11 — REPAIR (provenance overclaim in the (vi) BEST cell +
### unnamed O1 gate): the measured segmented TR-Newton curvature is
### asserted to BE the critical-cone reduced-Hessian eigenvalue "of
### record" — at the record's own margin-active hypothesis that object
### is not defined of record

ANCHORS: forchetta row (vi) BEST cell :975 ("mu = the MEASURED engine
curvature at S* (segmented TR-Newton smallest reduced-Hessian
eigenvalue on the critical cone, Hessians of record)"); §2.2-bis
condition (2) :526-531 ("multiplier/margin shifts priced per the
KKT-with-margin-multiplier formalization of record (S20 ladder)").
SOURCE VERIFIED THIS WINDOW (M0 S20 block): (i) M0:2178-2183 — the
margin-multiplier KKT (grad J = lambda grad g + mu grad m, mu ≥ 0);
(ii) M0:2208-2211 — "the S20 instance optimum is boundary-active" is
a measured-supported HYPOTHESIS of record; (iii) M0:2307-2310 —
OBLIGATIONS LEDGER O1: "Danskin/Clarke cusp derivative (until
discharged the margin-active KKT is B-STATIONARITY ONLY — no
multiplier mu is defined) -> F4b theory WP".
DEFECT: at a margin-active S* — the record's own measured-supported
hypothesis for the current engine optimum — the active-constraint
multiplier is NOT yet a defined object of record (O1 undischarged,
B-stationarity only), hence neither is the critical cone nor the
reduced Hessian ON it; and the KS-aggregated fold margin is a
cusp-candidate (nonsmooth) constraint surface, which is exactly why
O1 exists. What IS of record is the segmented TR-Newton curvature in
the driver's parameterization (S18/S24 lineage) — a real measured
carrier, but not certified as a critical-cone reduced-Hessian
eigenvalue. The cell's parenthesis asserts the identification at
"of record" grade: above held evidence class (violates the table's
own cell rule, :934-936). Condition (2)'s appeal to the S20
formalization inherits the same gate: that formalization contains no
perturbation/stability theorem to price "multiplier/margin shifts" —
it defines the stationarity structure and O1 explicitly withholds
the multiplier.
REPAIR (wording + one named gate; no new math): (a) split the cell's
mu sentence: the SCHEMA object = the curvature floor per conditions
(1)-(2) (as repaired by R22F-L0-10); the MEASURED CARRIER of record =
the segmented TR-Newton curvature (Hessians of record), which
instantiates the schema object at an interior/margin-inactive S*
(the F1/S22-S24 campaigns' margin-inactive instances are exactly
this case), and at a margin-active S* only AFTER O1 discharges —
name O1 as the gate in the cell or in §2.2-bis H-G2/H-G4. (b) delete
or qualify "on the critical cone" from the "Hessians of record"
parenthesis. This keeps the row honest at zero cost to the schema.

### R22F-L0-12 — REPAIR (PERSISTING limb of round-1 R22F-L0-6,
### re-counted per the round-2 rule): §1.3's un-superseded parenthetical
### still asserts the unconditional within-fiber positivity that
### [REV2-r1-3] itself denies

ANCHORS: §1.3 :249-251 ("Let eps_fib(P) := sup over the fiber of
|J(s) − J(s')| (the within-fiber separation, > 0 by
[T-DISC-1]+[T-DISC-2](i) wherever swirl content is admissible)");
[REV2-r1-4] :270-280 (supersedes ONLY the "if a designer optimizes
..." sentence — the eps_fib parenthetical is not inside the quoted
superseded span); [REV2-r1-3] :219-228 ("On the uncompensated DROP
branch ... ZERO separation — no strictly-positive floor is asserted
on that branch"); landing note [REV2-r1-21] :1159-1162 ("the
corrected forms land, never the superseded sentences" — an UNMARKED
sentence therefore lands).
DEFECT: on the uncompensated-DROP branch at r_e = r_in the
[T-DISC-1] family exhibits ZERO separation (round-1 probes
r22f_v2_probe_r1_l0_fiber_sign_2eps.py PART C and r22f_v2_probe_r1_
l2_drop_sign.py, both of record — no re-run needed), so "> 0 ...
wherever swirl content is admissible" is false as printed; every
other site was scoped by [REV2-r1-2/3/5/18], but this parenthetical
was left un-superseded and, per L-1/[REV2-r1-21], WOULD LAND in M0
contradicting the adjacent [REV2-r1-3] text. My round-1 repair asked
explicitly for [T-DISC-3]'s positivity to be re-derived with the
H1.3/degenerate-corner scope ("re-derive [T-DISC-3]'s positivity as:
eps_fib > 0 wherever the compensated variant is admissible", r1_l0
:194-196); the disposition table (:1216) marks L0-6 FIXED, but this
sentence shows the fix did not reach §1.3.
REPAIR (one line): mark the parenthetical superseded and restate:
"> 0 wherever the physical-h0-fixed variant is admissible (FOLD or
compensated, H1.3 headroom; [REV2-r1-2] scope) — on the
uncompensated DROP branch geometry-signed with the r_e = r_in
degenerate corner (zero separation), per [REV2-r1-3]".

### R22F-L0-13 — AMENDMENT: condition (1)'s "certified basin radius"
### has no named deriver/owner — the a-posteriori check cannot ever
### fire as specified

ANCHORS: §2.2-bis condition (1) :519-525; (vi) BEST cell :975 ("valid
IF delta/mu ≤ the certified basin radius (check stated per §2.2-bis
condition (1))"); WHAT DERIVES delta :507-512 (three named derivers —
all for delta, none for the radius).
REASON: the a-posteriori form needs TWO inputs: delta (derivers
named: X-T3QS-5F / route-B / M-RED gradient rider) and the certified
radius on which the curvature floor holds uniformly. No instrument,
duty, or owner is named for the radius anywhere in §2.2-bis, §3, or
the (vi) row — when a measured delta lands (M-RED rider), the check
as published cannot be executed. This mirrors the brief's own
discipline ("the cell says so and names what would derive it",
:122-126) applied to the schema's second input.
AMENDMENT (naming only, no new math): name the radius deriver — the
natural candidate is a curvature re-measure ladder on a ball around
S* in the engine's own parameterization (a cheap extension of the
M-RED gradient-measurement rider, §3.6, F2; or an engine duty beside
the Hessian-of-record machinery), with the radius = the largest
ball on which the measured floor sustains its value within its own
band. Add one line to §2.2-bis and the (vi) BEST cell.

### R22F-L0-14 — AMENDMENT (typing + symbol hygiene for the M0
### landing): P_T applied to a dual object needs the M-Riesz/Moreau
### identification declared; and "mu" collides with the S20 margin
### multiplier at the very site §2.2-bis will land beside

ANCHORS: §2.2-bis :481-486 (delta = ||P_T[grad gap]||_{M*}: P_T is a
primal-cone projection, the gradient gap is a dual-space object, and
the norm is the dual norm — the composition is well-defined only
through the M-Riesz map); condition (3) :532-535 (names the metric
but not the identification); L-2/[REV2-r1-21] (§2.2-bis lands in M0);
M0:2178-2183 (the S20 formalization spells "grad J = lambda grad g +
mu grad m, mu ≥ 0" — mu THERE is the margin MULTIPLIER); M0:2309
("no multiplier mu is defined").
REASON: (a) as printed the formula is type-ambiguous; the sound
reading (declare it) is: identify the gap g with M^{-1}g, project
M-orthogonally onto the cone C, measure in ||·||_M — equivalently,
by the Moreau decomposition, delta = sup_{d in C, ||d||_M ≤ 1}
<g, d> (the support-function form, which is also the form the VI
argument of R22F-L0-10(a) consumes directly). One sentence fixes it,
and the support-function form makes the cone-pairing discipline of
the L0-10 repair automatic. (b) §2.2-bis's mu (curvature) and the
S20 block's mu (margin multiplier) will sit in the same M0
neighborhood after landing (L-2 + the S20 ladder both live in the
optimality layer); at a margin-active S* BOTH objects appear in the
same sentence (R22F-L0-11). Rename the schema's curvature (e.g.
mu_curv, or kappa) at landing; also state the sign convention
(concave J: the floor is the smallest eigenvalue of −H_reduced —
"smallest reduced-Hessian eigenvalue" of a negative-definite H is
the most negative one as printed).

### R22F-L0-15 — NOTE: verified-at-source items this round (positive
### record for the judge; no action)

(a) Mandate-coverage duty result: both mandated elements PRESENT
(see header block). (b) [REV2-r1-4] reassembly re-verified at this
lens: leg (a) faithful to claim 10 (round-1 source verification
stands); leg (b)'s osc/2 lower bound, midrange attainment
(sup_P eps_fib(P)/2), full-oscillation sharpness reading, premise
(DR) + falsifier — all sound at SCHEMA grade; quantifier direction
now correct; the irreducibility sentence correctly bound to leg (b).
(c) [REV2-r1-2](2) algebra re-checked exactly (Delta u_e^2 =
lambda^2 Gamma_0^2 (1/r_in^2 − 1/r_e^2)); consistent with the B2
geometry-signed record clause. (d) [REV2-r1-13] B-2 measure:
∮ u_theta^2/(2 u_e) dmdot with dmdot = rho u_x dA — dimensionally N,
consistent with the per-streamline debit; correct. (e) The (vi)
WORST cell's honesty line ("delta UNDERIVED — NO argmax-shift number
exists at any grade") and the never-value-for-optimum clause (brief
:126) are present and correctly scoped. (f) §10 disposition table
spot-checked against the actual edits for all nine L0 rows —
dispositions accurate except the L0-6 residue filed as R22F-L0-12.
(g) The [REV2-r1-15] addendum-(c) declaration matches addendum_c4
(c) :17-26 content faithfully (no-referee sentence + Harroun
qualifier + CONSEQUENCE).

==============================================================================
## PAPERS NEEDED

NONE for this refutation. (All attacks close on carriers already on
disk; no procurement would change any finding above.)

## MACHINE SUMMARY (round 2, lens L0)

breaks = 1 (R22F-L0-10)
repairs = 2 (R22F-L0-11, R22F-L0-12)
amendments = 2 (R22F-L0-13, R22F-L0-14)
notes = 1 (R22F-L0-15)
persisting_prior = 1 (R22F-L0-12 = the un-propagated limb of round-1
R22F-L0-6, re-counted as REPAIR per the round-2 rule; all other
round-1 L0 objections verified RESOLVED)
probe: r22f_v2_probe_r2_l0_activeset_shift.py — RUN, all asserts
PASS; synthetic quadratic counter-model only (CT-6 clean).
dry_at_this_lens: NO (1 BREAK + 2 REPAIRs standing)
