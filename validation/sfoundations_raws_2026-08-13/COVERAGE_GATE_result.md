# COVERAGE GATE — arithmetic of record (S-FOUNDATIONS, 2026-08-17)
# Per COVERAGE_GATE_spec.md: (A) machine inventory, (B) three-way
# accounting reconciled to zero, (C) adversarial critic (dual-seeded),
# (D) verdict. All counts measured in-window (SR-12).

## (B) THREE-WAY ACCOUNTING PER INVENTORY CATEGORY

1. HYPOTHESES (12 items, table of record in the session transcript +
   checkpoint): 12 == 10 COVERED (hypothesis-legitimacy audit,
   verdicts CERTIFIED after dual-seed re-run: 5 LDM + 1 CONDIZIONATA,
   hypaudit/VERDICT + addendum) + 2 PARTIAL-with-owner (GEOM
   cone/attachment -> census-lemma window, F2-exit user pin; S1
   solution class -> phaseD proof labels + C49 adjudication, session
   C) + 0 NOT-COVERED.

2. CHOICE LEDGER (51 rows, machine tally):
   - REVIEW coverage: 51 == 48 diff-assessed (phaseB_tree_diff §1
     verdicts incl. SILENT-as-assessed) + 3 minted-with-provenance
     this window (C49, C50, C51) + 0 unreviewed.
   - EXECUTION status: 51 == 7 DECIDED + 44 OPEN-with-owner
     (28 NEVER + 13 SINGLE-AUTHOR + 3 MIXED, every row carries owner;
     the F2-consumed subset = the DECLARED SPLIT queue to session C)
     + 0 unowned.

3. THEORY CLAIMS (claims_registry: 140 rows, lint (xv) green this
   window): coverage BY COMPOSITION, declared: (a) the
   formulation-layer statements were re-derived/proven THIS window
   (S-T0P family: [T-T0P-E] THEOREM + lemma family; 2.5D-swirl block
   D.1-D.20 labeled; L4=>R1 THEOREM/THEOREM*; contract blind-diff 9
   re-derivations) — landings queued to session C behind the r2 pass
   (judge condition); (b) the remaining registry rides the S-CERT
   certification audit (R33, 4 days prior, MC8 8/8) + the green
   falsifier suite (this window's runs); (c) the ROAD level for all
   of it was blind-re-derived (Phase A convergences, diff §3).
   Per-theorem re-derivation of the full 140 was NEVER this
   session's mandate — composition declared, no silent claim.

4. DE-NOVO FORKS (141): row-side accounting COMPLETE (diff §5:
   48 ledger verdicts reconciled 8+10+11+6+12+1). Fork-side per-fork
   consumption ledger = PARTIAL-with-owner: session C panels consume
   forks per-cluster citing file:line; the per-fork closure artifact
   = session C close deliverable. Declared, owned.

5. NOVEL ITEMS (16, diff §2): 16 == 5 MINTED this window
   (carriers:control-surface-invariance-rejector-missing,
   engine:flow-branch-selection-unpinned,
   plume:vortex-sheet-stability-unchecked,
   thermotab:fundamental-derivative-audit-missing,
   scope-pins:frozen-model-form-bar-at-champions) + 10 MAPPED to
   existing rows (cited in §2 in place: stage-A rows ×2, swirl-f2a
   ×1, flatness ×1, existence/P7 ×1, litreview-Giles ×1,
   cycle-measure ×1, moc-critical/C49 ×1, D-GSEP ×1, oracle-battery
   ×1) + 1 AGENDA-with-owner (§2.9 base-pressure/truncation ->
   census-lemma/F4b window, ADR truncation user call adjacent).

6. WORKFLOW-JUDGE DELIVERIES: contract judge C-4 == CONSUMED
   (F-1/F-2/F-3 rows + C50/C51); C-1/C-2/C-3 == session C prompt
   (landing package). Proofs judge same-window duties == TRAVEL WITH
   THE LANDING (session C package, per LG-1 gating); LG-1 itself ==
   ROW (orchestration:until-dry-confirm-direction-unproven).
   Hypaudit monitor lists == map to existing monitor rows + M8
   noted in the session C prompt.

7. SESSION DECISIONS/CATCHES: all typed — model pins (memory),
   census-lemma/PAP-RIM (census rows unchanged, decision logged),
   constraint-ladder catch (row), topology errata (log §errata),
   spike/bell (SPEC_ideal_lengths_oracle.md, owner R13), Pa-range
   c-slot question (BLOCCATO, user call), right-sizing + escalation
   rule (checkpoint, session C prompt).

## (D) GATE VERDICT (pre-critic)
Arithmetic reconciled to zero in every category, with two PARTIAL
categories carried by named owners (hypotheses 2/12; fork-side
ledger) and the DECLARED SPLIT queue (Phase C adjudications + Phase
D landings) as the session's split-of-record. No category carries an
unowned residue. Critic + dual-seed report appended below when done.

---

## (C)+(D) CRITIC REPORT CONSUMED + FINAL VERDICT (2026-08-17)

CRITIC: COVERAGE_GATE_critic.md — verdict MATRIX-HAS-OMISSIONS
(6 findings). CONSUMPTION, same window:
- F1 (HIGH, C-4 overclaim) REPAIRED: F-4..F-10 + D-1/D-2/D-4 + the
  two §4 orphans (§4.5, §4.10) now carried as a NAMED QUEUE ENTRY in
  the committed session C prompt (Blocco 2 addition) — the C-4 bar
  ("registry rows OR named queue entry") is now genuinely met.
  Category 6 corrected: contract judge deliveries 14 == 3 minted
  (F-1/2/3) + 2 minted (C50/C51) + 9 queued-by-name (F-4..F-10,
  D-1/D-2/D-4 net of D-3/D-5); proofs judge same-window duties ==
  ALL queued-by-name (Blocco 2 list, verbatim in prompt).
- F2 REPAIRED: category 5 denominator per spec == 16 §2 items + 10
  contract demands (3 minted + 7 queued) + 6 hypaudit bundles
  (verdicts certified, monitors mapped to rows/M8-queued) — full
  arithmetic: 32 == 8 minted + 13 mapped/certified + 11
  queued-by-name + 0 uncarried.
- F3 ACKNOWLEDGED: diff §5 partition had two compensating miscounts
  (C2 double-counted SILENT+CONDITIONAL; C17/C18 merged); the
  critic's independent re-enumeration confirms DISTINCT-ROW coverage
  complete over C1-C48 — the corrected partition authority is the
  critic file; the diff §5 text stands with this correction noted
  (source untouched per SR-10 provenance).
- F4 REPAIRED: category 6 denominators above; category 7: 7 listed
  decisions/catches == 7 carriers verified (memory files ×3, rows
  ×2, SPEC ×1, BLOCCATO ×1).
- F5 REPAIRED: §4 signals 10 == 8 traced (critic-verified) + 2
  queued-by-name (§4.5, §4.10 in the session C prompt).
- F6 REPAIRED (honesty citation): category 3(b) rides the S-CERT
  audit WITH its verdict of record — NON-CERTIFICABILE, 2 P0 (one
  repaired in-window; staleness import-closure OPEN, owner F2, row
  at findings_registry) — the theory-claims certification is
  therefore "audited with named open P0", never "clean".
- SEED HONESTY: known-true control examined and NOT flagged (PASS);
  the planted canary (lit-registry growth omission) was VOID by
  seed-design defect (the session log already carried the count —
  third seed-design defect of the day, feeding the LG-1
  seed-protocol repair); detection power demonstrated instead by
  the critic's GENUINE catches (F1-F2 High/Medium). Declared.

## FINAL GATE VERDICT: PASS-WITH-CONSUMED-REPAIRS
Every category now reconciles to zero with named carriers; the
critic's residues are all consumed or queued-by-name in committed
artifacts; the declared split (Phase C/D remainder) is the
session's split-of-record with the committed prompt as carrier.
No unowned residue exists at close. (The gate itself caught real
omissions before commit — the instrument works; adopted-as-candidate
standing R3 gate, user ratification at next touchpoint.)
