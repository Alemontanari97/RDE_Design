# PHASE B — TREE DIFF OF RECORD (S-FOUNDATIONS, 2026-08-17)

Diff of the four Phase-A de-novo attack trees (141 forks: variational
34 / hyperbolic 41 / optimization 32 / propulsion 34; full texts =
phaseA_tree_*.md, condensed slices *_CONDENSED.md with line refs)
against (1) docs/choice_ledger.yaml (48 rows) and (2) the plan/M0
record. Method: every ledger row gets each tree's stance; verdicts:
CONVERGENT (independent re-derivation of our road/choice — cited as
validation), DIVERGENT (recommends against our incumbent — Phase C
agenda item), ENRICHING (new genuine alternative/criterion for the
row's set — enters the row's alternatives per the breadth directive),
SILENT (below the trees' granularity — no evidence either way; NOT
support). Novel forks with no ledger home are listed in §2; §3 =
theory-layer convergent validations; §4 = brief-ambiguity signals
mapped to the record. Authored by the Fable orchestrator (user pin:
diff = orchestrator work, no delegation). Fork refs: V=variational,
H=hyperbolic, O=optimization, P=propulsion + fork number; line refs
in the condensed files.

## §1 PER-LEDGER-ROW DIFF (C1-C48)

- **C1 design basis (SINGLE-AUTHOR)** — DIVERGENT-ENRICHING, HIGH.
  O-F4 recommends B-spline CONTROL-POINT contours as the certified
  carrier with a NEW argument absent from our record: Bernstein-form
  coefficient bounds give EXACT (sample-free) sufficient certificates
  for curvature/angle/cone constraints, and control-point classes give
  compactness + A(c)-membership by construction (existence link,
  O-F30). V-F13 wants a two-tier split (level-set discovery tier +
  spline-graph certificate tier); H-F34 (f) sector-structured explicit
  contour; P-F19 spline certificate class + analytic (Rao) spine as
  cross-check. Consequence: C1's Phase-C adjudication gains a second
  axis (admissibility-certificate exactness) beyond D6's oscillation
  axis, and a two-tier option our record never contains.
- **C2 right-end BC (NEVER)** — SILENT directly (no tree chooses
  interpolating splines, so the end-condition question dissolves in
  their formulations; O-F4's control-point route makes C2 moot —
  recorded as a CONDITIONAL consequence: if C1 flips, C2 dissolves).
- **C3 left-end BC clamped tangency (DECIDED constructional)** —
  CHALLENGED (the only DECIDED row directly challenged): P-F21
  recommends a rounded lip at the curvature-bound floor (adjoint
  regularity argument); H-F18 recommends the exact centered-corner
  (Prandtl-Meyer fan) solution inserted as internal data at Λ. Two
  genuine alternatives to "no genuine alternative exists". Phase C
  agenda: re-open C3's constructional status with these two options.
- **C4 knot placement (DECIDED survey)** — CONVERGENT-in-mechanism +
  ENRICHING: O-F6's adaptive design-space refinement driven by the
  representation-residual indicator ||(I-Pi)G|| with a derived stop
  (refine only above the (v) noise floor) parallels [X-AKNO]
  goal-indicator insertion; the projection-residual indicator and the
  derived stop criterion enrich the C4 record.
- **C5 knot anchoring (NEVER)** — SILENT (below granularity).
- **C6 insertion degeneracy guard (SINGLE-AUTHOR)** — ENRICHING:
  O-F8's certified branch-and-bound by de Casteljau subdivision is a
  new operator-level guard mechanism alongside the listed
  Schoenberg-Whitney alternative.
- **C7 dof policy insertion-only (NEVER)** — PARTIAL: O-F6 stops on a
  derived indicator (no removal); V-F13 discovery tier implicitly
  allows topology/dof reduction. No tree defends insertion-only as a
  ratchet; none demands removal either. Weak agenda support.
- **C8 warm-start deviation (NEVER)** — ENRICHING: V-F26 makes
  continuation-from-classical-optima the BACKBONE (highest-information
  start) and V-F7 adds a fold-margin monitor (inf_xi sigma_min of the
  linearized steady operator) — a principled seed-validity instrument.
  O-F32 (full block, optimization tree L1549-) treats warm-start
  policy as a first-class fork. Supports arming the C8 rejector.
- **C9 march mesh law (NEVER)** — DIVERGENT, HIGH, 4/4 CONVERGENT
  AGAINST THE INCUMBENT: all four trees recommend goal-oriented
  (adjoint-weighted/DWR) adaptivity as the production mesh policy
  (V-F28, H-F26/27, O-F24, P-F31), with measured effectivity and
  derived safety factors. Consistent with the pending litreview A36
  row. C9's Phase-C adjudication should treat uniform-per-run as the
  challenged incumbent.
- **C10 refinement operator (NEVER)** — PARTIAL (adaptivity doctrine
  implies local/anisotropic control but no tree addresses the joint
  global-scaling operator explicitly).
- **C11 error estimator for J (SINGLE-AUTHOR)** — DIVERGENT, HIGH,
  4/4: every tree makes DWR the PRIMARY J-error estimator with
  Richardson demoted to referee/cross-check at checkpoints (V-F28,
  H-F26, O-F24, P-F31) — the inverse of our incumbent (two-level
  Richardson primary, DWR never built). Phase C: C11 with inverted
  burden of proof.
- **C12 start line (SINGLE-AUTHOR)** — ENRICHING on the never-
  adjudicated accuracy axis: H-F16 computes the limiting-characteristic
  start surface (trace the C- family from the sonic-bubble edge),
  never assumes the geometric throat; H-F15 embeds the sonic region as
  fitted internal boundary. New accuracy-axis instruments for C12.
- **C13 axis unit process (SINGLE-AUTHOR)** — ENRICHING: H-F19
  parity-correct one-term axis series + a NEW obstruction our record
  lacks: swirling data families cannot reach the axis with nonzero
  circulation — vortex-core resolution or on-axis solid REQUIRED,
  stated as an explicit sector certificate. Interacts with the
  swirl-f2a rows (§2.8).
- **C14 exit read point (NEVER)** — PARTIAL via P-F22's control-
  surface identity (multi-point redundancy as free rejector).
- **C15 m_stop floor (DECIDED)** — SILENT.
- **C16 Newton damping ladder (NEVER)** — SILENT at ladder
  granularity; the universal tolerance-chain doctrine (O-F25 full
  block; V-F31) covers the derivation duty standard.
- **C17 N_NEWTON (SINGLE-AUTHOR) / C18 floor constants
  (SINGLE-AUTHOR)** — CONVERGENT ON THE DUTY: O-F25 ("every tolerance
  derived", each with derivation hash per H-F40) and V-F31 (stop when
  deltas fall below their own measured noise) independently mandate
  exactly the derivation duty our rows carry as F2-live.
- **C19 cert metric scale (SINGLE-AUTHOR)** — PARTIAL (monitor-list
  doctrine H-F23; componentwise-vs-scalar not addressed).
- **C20 certificate qualification (NEVER)** — ENRICHING: O-F20's
  "solvability-first doctrine" derives per-sector sufficient
  solvability regions dry-level (wall angles below Prandtl-Meyer
  margin, above compression-coalescence bound → marching solution
  exists and is smooth) — if the optimum is interior, the hidden
  constraint never fires and the certificate is structurally clean. A
  new qualification mechanism alongside kappa(J)/Kantorovich/interval.
- **C21 seed validity (NEVER)** — ENRICHING: continuation-defined
  branch + V-F7 fold-margin monitor as a seed-validity instrument
  (alternative family to alpha-theory/Kantorovich).
- **C22 wall-foot search (NEVER)** — SILENT.
- **C23 record-failure policy (DECIDED)** — SILENT. **C24 thermo
  closure (DECIDED KEEP)** — CONVERGENT + ENRICHING: V-F9 lands on
  certified-interpolation tables with AD-differentiable evaluation
  (our [X-THC1] road re-derived); H-F6 adds NEW mandatory audits our
  C-A..C-D conditions do not contain: h(T) monotonicity, a^2>0, and
  the FUNDAMENTAL DERIVATIVE Gamma_fund = 1+(rho/a)(da/drho)_S > 0
  over the reachable (T,p) set — the audit that underwrites Lax
  conditions and Majda hypotheses (→ candidate new registry row,
  §2.6). P-F14 demands the frozen-vs-equilibrium Isp bracket as a
  NAMED model-form (v) bar (few percent for H2-air — potentially
  larger than delta; → §2.7).
- **C25 table box (NEVER)** — CONVERGENT with our envelope-derived
  alternative: H-F6 audits over the REACHABLE set (envelope-derived
  box in our vocabulary).
- **C26 out-of-box behavior (NEVER)** — PARTIAL (traced-monitor
  doctrine implies the traced flag; no tree addresses clamp modes).
- **C27 aggregation (SINGLE-AUTHOR)** — DIVERGENT, HIGH, 3/4: O-F18
  (exchange method on the true 1-D inner problem for exactness, KS
  only WITHIN the working set for smoothness, inter-node certification
  via measured Lipschitz margin, exchange multipliers ≈ the
  measure-valued SIP multiplier = the (ii) marginal-value semantics),
  V-F23 (exchange + covering, margin as DERIVED safety factor), P-F16
  (adaptive phase refinement near convergence + MANDATORY post-hoc
  fine-phase sweep falsifier: a violating phase between nodes voids
  the certificate). Our pure-KS incumbent is challenged on the axis
  the annex called never-adjudicated (conditioning) AND on exactness.
- **C28 cert-frontier representation (NEVER)** — CONVERGENT-WITH-M0 /
  DIVERGENT-WITH-LEDGER, HIGHEST SIGNAL, 4/4: V-F32 (certification
  conditions = first-class constraints with margins and multipliers —
  "the most distinctive structural choice of the whole formulation"),
  H-F24 (certified-class boundary in the active set; its multiplier
  PRICES certifiability), O-F20 (graded violation + solvability
  regions), P-F33 (gradients of certifiability or the loop "grinds or
  lies"). This is precisely the S20 M0 formalization (tier ladder +
  KKT with margin multiplier) that the LEDGER row still lists as
  NEVER with no successor to the falsified K_disc~A_0 bridge. Phase C:
  C28 adjudication consumes this 4/4 convergence; the ledger row lags
  the M0 state and the de-novo panel independently landed on M0's
  road.
- **C29 mask granularity (MIXED)** — PARTIAL-CONVERGENT: O-F19
  regime-cells-as-certified-classes matches rung-freezing in spirit.
- **C30 mask crop (NEVER)** — SILENT.
- **C31 optimizer engine (MIXED)** — CONVERGENT on family + DIVERGENT
  on the IP half: all four converge on TR-SQP-class with derived
  constants as the certificate closer (O-F16/17 TR-filter SQP with
  radius constants derived from the measured rho-histogram; H-F36
  TR-SQP + exact penalties; V-F30 error-aware inexact TR; P-F27),
  derivative-free/evolutionary strictly quarantined to exploration.
  NONE recommends an interior-point method as the certificate closer —
  our in-use scipy trust-constr IP path (the C31 NEVER half) is now
  actively challenged, not merely unsurveyed.
- **C32 curvature policy (SINGLE-AUTHOR)** — DIVERGENT: O-F17/23
  recommend CG-truncated Newton on Hessian-VECTOR products
  (second-order adjoints) in-flight with the full spectrum only once
  at S*; V-F24 Lanczos + derived tolerance. Challenges the fresh full
  dense FD Hessian per segment (ties to C33, C48, and the N5 mapped
  row engine:vmap-hessian-adjoint-divergence).
- **C33 constraint curvature (NEVER)** — ENRICHING (same HVP family,
  O-F21 item 6).
- **C34 TR floor/caps (NEVER)** — CONVERGENT with the alternative:
  O-F17's measured rho-histogram-derived accept thresholds and radius
  constants instantiate exactly the "noise-derived floor" alternative.
- **C35 xtol (NEVER)** — CONVERGENT with the derivation duty
  (tolerance-chain doctrine, O-F25/H-F40).
- **C36 scaling policy (NEVER)** — ENRICHING: O-F7 derives the metric
  from the MEASURED reduced-Hessian symbol (fit the decay exponent of
  H's action on high-frequency shape modes; set smoothing/metric order
  to flatten it) — a principled H^s alternative to frozen Jacobi
  diagonal scaling.
- **C37 segmentation trigger (NEVER)** — ENRICHING: O-F19
  regime-cell doctrine gives the materiality question (N6 panel) its
  candidate formal frame: activity changes = certified-class
  boundaries, logged as stratification loci (H-F11 logs
  insertion/removal events as (S,xi) stratification boundaries).
- **C38 outcome-II declaration (NEVER)** — CONVERGENT with the
  B-stationarity alternative: O-F23 and V-F24 independently state the
  degenerate/nonsmooth case as a second-order condition on the
  CRITICAL CONE of the semi-infinite program, declared as weak-(iii).
- **C39 multiplier provenance (MIXED)** — ENRICHING the NEVER
  (verification) half: O-F18's exchange-multiplier ≈ binding-measure
  density + P-F26's marginal-value multiplier audit (dJ/dc vs
  multiplier, cheap falsifier) give the verification instruments.
- **C40 lip equality (NEVER)** — SILENT.
- **C41 band composition (NEVER)** — CONVERGENT with the
  declared-policy alternative: H-F39 (published budget TABLE, measured
  values + derived bands, partition itself derived), O-F24
  (mu-weighted sum with measured effectivity band), V (named-term
  budget). No tree tolerates ad-hoc mixes.
- **C42 K_RICH reuse (NEVER)** — CONVERGENT-AGAINST-INCUMBENT, 4/4:
  "no magic 1.25" (V-F28), safety factors DERIVED from measured
  effectivity distributions on the oracle suite (H-F26), derivation
  hash per tolerance (H-F40). The one-numeral-8-roles incumbent has
  zero independent defenders.
- **C43 asymptotic-range handling (NEVER)** — CONVERGENT with the
  per-site observed-order alternative (H-F26(b) measured order).
- **C44 FD steps (SINGLE-AUTHOR)** — ENRICHING the kink-aware axis:
  V-F29's special obligations (verify AT a fitted front and AT an
  active g_sep phase, not only smooth interior points) + O-F22's
  battery (complex-step on the fitted/analytic tier; Taylor-remainder
  ramp with measured slope over a derived decade). Note: complex-step
  applicability interacts with our custom_vjp route — record as
  option-with-stated-precondition.
- **C45 leggeAree bracket (SINGLE-AUTHOR)** — SILENT.
- **C46 padding (DECIDED) / C47 ratchet (SINGLE-AUTHOR) / C48
  vmap-in-engine (DECIDED)** — SILENT (below the blind trees'
  granularity; no diff evidence either way). V-F29's
  derived-tolerances-from-conditioning is adjacent context for C48's
  cross-lowering discipline, nothing more.

## §2 NOVEL FORKS WITH NO LEDGER HOME (candidate rows / agenda)

1. **Discrete control-surface invariance as always-on rejector**
   (V-F2 O3; P-F22 O1≡O2 wall-vs-control-surface equality within
   discretization bar on EVERY solve; H-F21 plume-decoupling
   certificate + per-sector control-surface placement). The continuum
   identity is in D-MU; the DISCRETE identity as a free per-solve
   rejector carrier is NEW. → candidate choice-ledger row (C49-class)
   + F2 carrier duty.
2. **Per-phase branch selection under hysteresis + fold-margin
   monitor** (V-F7: continuation-defined branch, inf_xi sigma_min
   monitor; O-F11 multiplicity probes + worst-case-over-branches
   certificate policy). No ledger/registry row covers flow-branch
   selection. → candidate row.
3. **Causal-separation escalation ladder** (P-F6: supersonic-screen
   check → impedance band → fixed-point re-measurement; H-F14 margin
   audit): enriches problem-statement:stage-a-audits-premised-not-
   implemented (R1 premise) with a concrete instrument ladder.
4. **Plume vortex-sheet stability + decoupling certificate** (H-F3:
   convective-Mach check phase-by-phase, Miles/Papamoschou-Roshko;
   H-F21: Mach-disk phases certifiable when the disk is downstream of
   the control surface). → candidate rows, owner F4b/F5-adjacent.
5. **Gamma_d placement residual accounting** (P-F5 O3: heat-release
   residual accounted always-on): enriches the same stage-A row.
6. **Fundamental-derivative audit** Gamma_fund > 0 + h(T)
   monotonicity + a^2 > 0 over the reachable set (H-F6): NEW thermo
   condition beyond C-A..C-D. → candidate registry row (thermotab).
7. **Frozen-vs-equilibrium model-form (v) bar** (P-F14): the scope
   pin (frozen) needs its named model-form bracket carrier at
   champions. → candidate registry row (scope-pin consequence).
8. **Swirl first-order mandate** (P-F9 unconditional; H-F7
   unconditional; V-F5 R1s): CONVERGENT 3/4 with the mean-swirl
   panel's F2a mandate — cite swirl-f2a:f-swirl-1/2 + angular-
   momentum rows (seeded this window, tranche (d)). Independent
   validation, no new row needed.
9. **Base-pressure / truncated-plug Euler-uncertifiable zone**
   (P-F20): connects to the ADR truncation default user call
   (BLOCCATO 9b) and plug sectors: certificate class must exclude or
   model the base region explicitly. → agenda for the census-lemma /
   F4b window (F2-exit pin respected).
10. **Phase-quadrature stratification at events** (H-F33: stratify at
    detected events, dF/dS discontinuous across events even where F
    is continuous; P-F10 kinked-data composite rules; O-F2 frozen
    nodes during outer phases + doubling carrier; V-F4 measured
    xi-modulus): enriches the mu/quadrature record (T-O2/ME-3
    hypotheses) with the event-stratified gradient point — candidate
    row for the F2 quadrature design.
11. **H-DATA monitor with adjoint-based data-sensitivity carrier**
    (O-F3: |dJ/d(interface data)| from the SAME adjoint converts the
    measured data residual into an objective-error term — an
    EVALUABLE carrier for the H-DATA line of (v); P-F11 synthetic
    two-mode injection as the monitor's own rejector calibration;
    H-F41 certificate MOVES on trip, never dies silently): major
    enrichment of problem-statement:flatness-monitor-unarmed — the
    monitor gains a principled threshold (sensitivity-derived) and a
    self-falsifier.
12. **Existence discrete-shadow split** (O-F30: finite-dimensional
    certificate NOW unconditional + function-class theory target;
    V-F15: H-STAB conditional theorem + two computable failure flags
    — oracle failing along a maximizing sequence; boundary
    oscillation at scale h0): enriches the P7/Chenais road with
    named, monitorable failure boundary instruments.
13. **Front-terms in the adjoint designed-in** (H-F29: captured-shock
    adjoints miss the O(1) front-motion delta term, Giles-Pierce;
    fitted linearized-RH adjoint from the start; H-F10(b) closed-form
    RH Jacobians as the reason to fit): F2 adjoint-design input of
    record → candidate row if no litreview row already carries the
    Giles-Pierce point (dedup against tranche (e) when it lands).
14. **Two-instrument twin doctrine** (O-F9 |J_capture − J_fitted| as
    a measured model-residual (v) carrier; H-F8 "agreement is NOT
    truth, only non-rejection" — verbatim our cross-code doctrine):
    CONVERGENT with moc-critical-independent-invariants; the
    J-capture-vs-fitted diff as a NAMED (v) carrier is the enriching
    increment.
15. **g_sep carrier = integral-BL margin** (V-F12 O3+O1 two-sided,
    O-F13 least-magic + calibrated proxy, P-F15 with derived margin
    from validation scatter + one unsteady calibration): 3/4
    convergent on IBL as the in-loop criterion class for D-GSEP's
    declared empirical closure — Phase C/F2a agenda: adjudicate the
    D-GSEP criterion class against the IBL-margin family.
16. **Averaged-system regression oracles** (H-F35 Dirac-mu Rao
    regression as standing rejector of the whole optimality
    implementation; P-F19/26 single-phase limit must recover Rao
    exactly + marginal-value multiplier audit): enriches the (ii)
    verification battery; partially covered by our classical-oracle
    program — the mandatory-regression framing is the increment.

## §3 THEORY-LAYER CONVERGENT VALIDATIONS (blind re-derivations)

1. **Steadification exactness (T-T0 road)**: H-F1(b) re-derives the
   co-rotating-frame steady-traveling-wave exactness WITH a short
   proof and axial-thrust frame-invariance; V (Proof P8) states
   J_exact = R2 steady thrust under H-DATA; P (DP-2) same. The (v)
   requirement is decomposed by H-F2 into TWO measurable terms — the
   azimuthal-flux commutator (helix angle tan(alpha_h) = Omega*r/u_x
   as "the geometric face of the marginal Strouhal") and the
   3D-vs-axisymmetric commutator — which is structurally OUR
   T-RED reduction-residual decomposition re-derived blind. DIRECT
   INPUT to the Phase D centerpiece (T-DISC/T-RED/M-RED).
2. **Rao-collapse under averaging (T7 road)**: H-F35 proves-sketch
   that the classical exit-characteristic reduction collapses under
   the mu-average with a shared wall; correct structure = per-phase
   adjoints + ONE averaged wall condition + endpoint transversality —
   our T7/(**') system re-derived blind.
3. **Certified solution class (D2.5/S1 road)**: 4/4 trees land on
   piecewise-smooth fitted-front classes with capturing never a
   certificate; H-F5 re-constructs the Li-Yu + Majda scoping and the
   class-relative uniqueness statement (our T-XWS/C-MAJDA
   architecture); "wild non-uniqueness fenced by class fiat, declared
   openly" (P-F12) = our declared-conditional honesty.
4. **Existence road (P7/Chenais)**: H-F34 cites Chenais 1975 on the
   uniform-cone class verbatim; V-F15 adds the conditional-theorem
   form with H-STAB named. Our (i) architecture re-derived.
5. **delta-mechanism / bound ladder**: V-F25 P1 (interchange bound
   B1 = Int sup_S F dmu) = our int-max bound; delta = 0 iff one shape
   is per-phase optimal mu-a.e. = the T3/T4 dichotomy criterion
   re-derived; P-F28 "any J > B is an instant solver-bug rejector" =
   bound-as-oracle doctrine; H-F37 chain-of-bounds reporting = our
   ladder reporting.
6. **Thermo road**: V-F9/H-F6 = certified gamma(T) tables with AD
   evaluation ([X-THC1] road re-derived) + new audits (§2.6).
7. **Certifiability-as-priced-constraint**: 4/4 (see C28) — the S20
   M0 formalization (tier ladder + KKT margin multiplier)
   independently reached by every lens. The strongest single
   methodological validation in the diff.

## §4 BRIEF-AMBIGUITY SIGNALS → RECORD MAP

1. **Swirl in s(xi)** (V-b, H-1, P-b): resolved OF RECORD by the
   mean-swirl panel mandate + [vorticity] contract placeholder
   (swirl-f2a rows, seeded tranche (d)); the 3-lens independent
   flag CONFIRMS priority of the F2a deltas.
2. **g_sep model layer** (V-c, O-a, P): of record = D-GSEP declared
   empirical closure (criterion class R2); trees' IBL-margin
   convergence = §2.15 agenda.
3. **mu exactness / xi-regularity / atoms** (V-d, O-b, H-5): of
   record = ME-2/ME-3 mu-hypotheses + pure-periodic scope pin; the
   regularity-as-measured-quantity instruments (O-F2 FFT probe,
   V-F4 xi-modulus) are the enriching increment (§2.10).
4. **All-phases-supersonic?** (V-a): answered of record by the
   L4-DEFAULT (certified default = axially supersonic with margin;
   subsonic = declared case-class O1-O4) — the brief deliberately
   left it open; no gap.
5. **Quiescent ambient** (P-d, H-2): of record Pa constant (D-DOM/
   D-JEX). Declaration line candidate for the contract docs (external
   coflow explicitly out of scope).
6. **Lambda: one vs two lip circles** (H-4): DECIDED of record
   (census pins 2026-08-02: O11 lip circles); cite.
7. **Annular vs full-disk Gamma_d** (H-6): of record = annular
   interface; the axis-circulation obstruction (H-F19) becomes the
   C13 enrichment.
8. **Cone condition vs sharp features (plug tips, sharp lips)**
   (P-c): GENUINE TENSION not explicitly priced in the record — the
   uniform cone condition may exclude the classical optimum's sharp
   features; interacts with C3 (lip) and the truncation question.
   → agenda item for the census-lemma window (F2-exit, user pin
   respected) + note at the A_gen definition site at next M0 touch.
9. **delta scope: joint vs per-sector** (O-e): of record = joint over
   A_gen(c) with sector decomposition (D2.6); H-F37/P-F17 "finite max
   over sector champions" matches. No gap.
10. **No target magnitude for delta/(v)** (P-e): thrust-stand-class
    (~0.5-1%) anchoring = a new external calibration reference for
    tolerance ambition; note for the (v) budget design, no row.

## §5 ARITHMETIC AND CONSUMPTION

141 forks assessed against 48 ledger rows + plan/M0. Ledger verdicts:
DIVERGENT/CHALLENGED: C1, C3, C9, C11, C27, C28(ledger-lag), C31(IP
half), C32 (8 rows). CONVERGENT (with incumbent, alternative, or
duty): C4, C17/C18(duty), C24, C25, C34, C35, C38, C41, C42, C43
(10). ENRICHING-only: C6, C8, C12, C13, C20, C21, C33, C36, C37,
C39, C44 (11). PARTIAL: C7, C10, C14, C19, C26, C29 (6). SILENT:
C2(conditional-dissolve), C5, C15, C16(mechanics), C22, C23, C30,
C40, C45, C46, C47, C48 (12). CONDITIONAL: C2. (8+10+11+6+12+1=48.)
Novel items: 16 (§2), of which candidate NEW rows: §2.1, §2.2, §2.4,
§2.6, §2.7, §2.10, §2.13 (7, minted at Phase C/R3 with dedup against
tranche (e)); the rest map to existing rows (cited in place).
Phase C consumption order (F2-consumed first, per the session
prompt): C28 → C27 → C9/C11 (mesh/estimator pair) → C31(IP)/C32/C33
(curvature trio) → C1 (+C2 conditional, C3 challenge) → C20/C21 →
C34/C35/C36/C41/C42/C43 (derived-constants family) → remainder per
owner. Phase-owned rows (F3/F4b/F5) stay at their phase entry per
the standing rule.
