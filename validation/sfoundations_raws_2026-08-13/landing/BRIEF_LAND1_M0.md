# LANDING BRIEF — LAND-1: M0 (docs/rde_nozzle_MASTER.md) — Blocco 2, S-FOUNDATIONS-C

You edit ONE file only: `docs/rde_nozzle_MASTER.md` (M0). House rules
(binding): R4 — the M0 sentence must NEVER be stated stronger than the
proof; every landed statement carries rigor class + hypothesis pointers +
falsifier pointer + gamma/EOS status; annotation style consistent with
the surrounding document; NO "armed monitor" claims anywhere (C-3 of
record: monitors are SPECIFIED, not ARMED — no run artifacts exist);
DOC-2 GUARD: the over-certification counter on D.18 material stands at
FOUR — do not mint a fifth strength claim above in-line grades.

Authorities to read first (targeted): 
- `validation/sfoundations_raws_2026-08-13/phaseD/VERDICT_phaseD_proofs1.md` §3.1/§3.2/§5 (labels + ready-to-land conditions)
- `validation/sfoundations_raws_2026-08-13/VERDICT_contract_and_L4R1.md` B.3 (labels + quotable composite) + C-1
- `validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_r2pass.md` §3/§4 (escrow + amendments A-1/A-2)
- `validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_escalation.md` §4 (released package, held-out set)
- `validation/sfoundations_raws_2026-08-13/r2pass/VERDICT_confirm.md` (leg-6/leg-14 closures, leg-14 label THEOREM modulo (H-UP-fam), amendments AM-1..AM-7, doc1 legs 3+5 STILL held out)
- Staging sources: `phaseD/phaseD_meanswirl_formalization.md` (D.13 row text §4; H-AM block §3; register §6), `phaseD_L4_implies_R1.md` (composite form), `phaseD/phaseD_stop_proof.md` §8 (delta list — for the [T-T0P-E]/[L-INC] part ONLY).

## EDITS (in document order; cite provenance "VERDICT_r2pass §3 + VERDICT_escalation §4 (+ VERDICT_confirm for legs 6/14)" at each landing site)

1. **[D-CONTRACT] / L4-DEFAULT block (lines ~116-143):**
   (a) PLANAR-ONLY REPAIR (REQUIRED; §7 item 6 of the meanswirl doc; D.4
   curved clause is CONFIRMED THEOREM): the standing certificate
   "u_x - c >= delta" is planar-only; land the meridional-normal margin
   form m_n = u.n_m - c >= delta (criterion M_n > 1, frame-invariant,
   reduces exactly to m_x on planar Gamma_d; tilted-element
   counterexample = audit-wiring rejector, D.4 r3).
   (b) C-1 RETRO-PROPAGATION (BLOCKING, VERDICT_contract B.4): restate the
   L4-DEFAULT certificate as the SPLIT certificate — (M-a)
   normal-on-surface; (M-a') axial-on-segment + box membership — or add
   planarity + segment pins; every Theorem-2/3 consumption cites (M-a').
   Compose (a)+(b) coherently: the normal-direction form IS the repair of
   both.
   (c) W1-W4 WINDOW NOTE (hypaudit R1-CAUSAL verdict, CERTIFIED): add the
   note that R1 is of record CONDIZIONATA on the validity window W1-W4
   (W1 every patch axially supersonic with measured margin; W2 design
   vector strictly downstream of the CERTIFIED interface as
   geometry-invariance — throat-area invariance necessary-NOT-sufficient,
   Liu 2022 B-vs-C; W3 operating-point exogeneity; W4 subsonic patches
   routed to the declared closure), monitors A1-A6 SPECIFIED-not-armed.
   (d) NO-COFLOW DECLARATION LINE (diff §4.5): external coflow explicitly
   out of scope; Pa constant per D-DOM/D-JEX.
2. **M0 VI.1 (the CycleFamily contract table): replace the `[vorticity]`
   placeholder with the D.13 row** (staged text in the meanswirl doc §4:
   w(y;xi)/Gamma row + h0 profile promotion + audits (a)-(c) with margin
   m_n on curved Gamma_d; recovery-uniqueness THEOREM clause). INCLUDE
   the two BLOCKING pins from `validation/swirl5f_panel_2026-08-19/DISPATCH_swirl5f.md` §4:
   (B-1) the A4 tangential-energy audit normalization PINNED — the 3-6%
   figure is KE-normalized; the h0-normalized reading is ~4x smaller
   (0.3-2.5%); discriminating test: only the KE reading gives
   Omega*r ~ D_CJ; the D.10 falsifier must never fire on a unit mismatch;
   (B-2) h0-CONVENTION provenance clause — whether h0(y;xi) includes
   u_theta^2/2 is a REQUIRED declared field of the generator (decides the
   B2 fold/drop variant; rejector = D.20 Delta-h0 = Omega*Delta-Gamma
   linkage on convention-inconsistent data). Cite the dispatch as
   advisory provenance (panel grades are not record grades).
3. **M0 Part III (mean-swirl / flux-nullity landing, §7 item 2):** land
   the H-AM hypothesis block VERBATIM from the staging doc §3 (H-AM0
   with L in AC + concentration-test audit + ASSUMED-PER-DATASET
   fallback; H-AM1..H-AM5 as staged) + D.6 [MS-T-FLUXNULL] THEOREM* with
   the two named conditionals (c1) assembled-balance symbolic check
   (queued, G-a) + (c2) H-AM0; Rmk 3.1 THEOREM; D.7 THEOREM*; D.8
   census DEFINITION + THEOREM* exhaustiveness over {not-H-AM0..not-H-AM5};
   D.9 [MS-T-MEASURE] THEOREM (i)(ii); D.10 [MS-T-SKE] THEOREM + PRACTICE
   twins (falsifier = the KE-normalized A4 per pin B-1); D.11 lands AS
   CONJECTURE; D.12 PRACTICE prohibition. Add a compact cross-reference
   to D.18 [MS-DEF-KRES] at its RECORD labels: DEFINITION
   (distributional) + SCHEMA completeness (G-f) + both iff clauses SCHEMA
   per J-r2p-2/J-r2p-3 with restoration ONLY via the G-f battery — the
   restated text of record is the staging doc r7 state (cite
   VERDICT_confirm leg-6 closure). Do NOT transcribe D.18's full content
   into M0 — pointer + labels + gap G-f owner only (DOC-2 guard).
4. **[S-T0P] site (lines ~463-498): PARTIAL upgrade only.** Land: the
   equivariance half [T-T0P-E] as THEOREM (function-space complete, with
   the lemma family [L-EQV1-3]/[L-INV]/[L-STD]/[L-SPACE]/[L-COMPAT]/
   [L-XSON3]/[L-XREC]/[L-XWALL3]/[L-XC3D] at THEOREM and the three-target
   falsifier (t1)-(t3)); [L-INC] THEOREM stratum (A) / SCHEMA-inherited
   (B); [P-HB1]/[P-HB2]/[P-HB3](i') at their §3.1 labels; the
   Cor-5.1-scoped consumption sentences: [T-T0] and [T-SLRW] fire on the
   t-periodic class ON cl(Omega_march) ONLY, slip-free instances only.
   EXPLICIT DEFERRAL NOTE (required, honest): the [T-T0P] main-statement
   upgrade (both strata SCHEMA, split gap lists) and the
   G8/[C-XBVP](a') gap-row landing are HELD OUT pending doc1's dry round
   (VERDICT_confirm: legs 3+5 open; revision 9 + one confirming round
   owed) — the proof of record remains phaseD_stop_proof.md. Do not land
   any main-statement text.
5. **L4=>R1 (site near [T-NSW]/causality material, M0 Part V or the
   D2.4-adjacent theorem area — find the right anchor):** land the
   BINDING QUOTABLE FORM of the composite from VERDICT_contract B.3,
   UPDATED by VERDICT_confirm: clause (i) linearized causal separation on
   the monitored COLLAR — THEOREM (Thm 1'), modulo PRACTICE bridges
   (H-RW)/(M-c); full-domain device-class form now **THEOREM modulo
   (H-UP-fam)** (Prop 1'' per VERDICT_confirm; NG-9 keeps the class
   instantiation; the honesty clause travels: in the motivating class
   Gamma_in uniform-noncharacteristicity is generically violated, the
   Status-(a) H^1 leg carries the residual); (ii) steady per-phase
   causal separation — THEOREM conditional on the SEGMENT certificate
   (M-a'); (iii) finite-amplitude protection priced for PLANAR NORMAL
   UPSTREAM-FACING fronts only, Euler+slip scope (viscous channel NG-5).
   "L4 => R1" WITHOUT these qualifiers is not a statement of record.
   Monitors (M-a)/(M-a')/(M-b)/(M-c): SPECIFIED, not armed (J-4).
6. Consistency sweep: any M0 line contradicted by the above (old
   "u_x - c" wording elsewhere, old S-T0P-only references) gets a
   one-line annotation pointing at the new form — never silent deletion.

Return: compressed list of edits (M0 line region -> what landed) + any
honest residue. Do NOT touch other files.
