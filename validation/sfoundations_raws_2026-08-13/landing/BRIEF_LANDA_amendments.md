# LANDING BRIEF — LAND-A: adjudicated amendments AM-1..AM-7 — Blocco 2

Files you may edit: `validation/sfoundations_raws_2026-08-13/phaseD/phaseD_meanswirl_formalization.md`
(AM-1..AM-3) and `validation/sfoundations_raws_2026-08-13/phaseD_L4_implies_R1.md`
(AM-4..AM-7). NO other files. These are the wording-class amendments
SUSTAINED and ADJUDICATED by the successor judge
(`r2pass/VERDICT_confirm.md` — read its amendment section first for the
exact grounds); apply each at its named site, annotation style (quote the
replaced text where the house style does), and append ONE compact
revision-log entry per document citing VERDICT_confirm (doc2: extend the
r7 log family; doc3: extend the r3.x log family). Do NOT touch any other
passage; do NOT change any label or grade (DOC-2 guard: over-certification
counter at FOUR — your edits must not alter any strength claim).

- AM-1 (doc2, CR-1): in the (<=) re-read third clause, replace "by step
  (1)'s unconditional clause (1b)" (K=0-fed, circular in the direction
  whose goal is K=0) with a citation of the conclusion's own without-pin
  gloss (a given) or of the step-(3) decomposition applied to the GIVEN
  del_phi V = 0.
- AM-2 (doc2, CR-2): disambiguate the "(u1)" token — rename the unclaimed
  composite upgrade at ~lines 1874-1876 (e.g. "(u1') limit-matching AT
  N_F points plus the (u2) gluing") and state "(u1) itself, the extension
  lemma, IS written and consumed only off N_F"; mirror in register row
  (~line 2562) and the ROUND-4 R3L1-1 disposition row.
- AM-3 (doc2, R4L1-2): restate (1b)'s justification — approximate limits
  "exist BY DEFINITION at every essential-jump point, and at H2-a.e.
  point of each witness geometry of record (a pinching sliver has density
  0 at density points of its accumulation set)" — dropping the underived
  universal.
- AM-4 (doc3): the slogan escape clause gains the NG-9 conditional for
  the kernel branch ("...or — where NG-9's instantiation of the class
  admits it — by a kernel jump A(n)[U]=0..."); mirror in the ESC3-5
  disposition row.
- AM-5 (doc3): persistence gloss restated to flow-invariant loci ("on a
  flow-invariant locus, e.g. a face portion with u-bar.n == 0 and steady
  base, the jump persists — the slip-line configuration; persistence in
  general is NG-9's territory").
- AM-6 (doc3): Status-(a) discharge condition names the mid face
  family-wise ("the split's mid face Gamma^coll_s (= Gamma_mid at the
  consumed depth h/2) uniformly noncharacteristic"); NG-3 residual (ii)'s
  "Gamma_mid" generalized the same one word.
- AM-7 (doc3, reduced scope): NG-3 trigger first clause extended ("...on
  a domain with corners OR with variable-type (grazing/sonic) loci on a
  data face"); register row 1 pointer reads "corners + Gamma_in
  variable-type = NG-3".

Return: the 7 applications (site -> applied text) + the two log entries.
