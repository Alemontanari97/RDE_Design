# LANDING BRIEF — LAND-3: N6 doc + D6 plan + ADVISORY_INDEX — Blocco 2, S-FOUNDATIONS-C

Files you may edit: `docs/rde_nozzle_N6_swirl.md`, `docs/rde_nozzle_development_plan.md`
(D6), `validation/ADVISORY_INDEX.md`. NO other files. Match each
document's existing style; annotation, never erasure; zero inflation.

Authorities: `validation/sfoundations_raws_2026-08-13/phaseD/phaseD_meanswirl_formalization.md`
§7 (items 3-4) + register §6; `r2pass/VERDICT_escalation.md` §4;
`r2pass/VERDICT_confirm.md`; `validation/swirl5f_panel_2026-08-19/DISPATCH_swirl5f.md`
§2/§7; `validation/sfoundations_raws_2026-08-13/phaseB_tree_diff.md` §4 item 10.

## A. N6 doc §5 (meanswirl §7 item 3)
Add the register cross-references: D.15 [MS-T-GAMONLY] (Clause 1 THEOREM
— the false-licensing theorem CONSUMES N6-3; Clause 2 SCHEMA = G-g, owner
this §5 line); D.19 [MS-T-PUMP] + D.20 [MS-T-ROTH] as the mechanism layer
above N6-3 (D.20 labels: THEOREM smooth+contacts / THEOREM* mass-crossing
fronts, carrier queued G-a); D.14 [MS-DEF-TRIPLE] as the operational
consumer of the obstruction identity — LICENSING leg explicitly
SCHEMA-gated on G-b1 (owner: this §5 line; blocking use only until
G-b1); G-b1 and G-g duties recorded ON this line if not already present.

## B. D6 plan (meanswirl §7 item 4 + diff §4 item 10)
1. Register the D.16 [MS-DEF-AMAUDIT] audit row and the D.14 G6 rejector
   as CONTRACT rows in the appropriate plan table (audits/monitors
   ledger): PRACTICE grade, rejector-gated, SPECIFIED-not-armed; note
   the A4 normalization pin (KE-normalized, per the D.13 contract-row
   pin landed this window by LAND-1).
2. Register the carrier `phaseD_meanswirl_symcheck.py`: suite-group
   assignment AT COMMIT + baseline row per lint (vii) discipline in the
   commit window (write the duty line; the actual suite wiring happens
   at commit — say so).
3. THRUST-STAND NOTE (diff §4.10): at the (v)-budget / tolerance design
   site of the plan, add the one-line external calibration anchor:
   thrust-stand-class accuracy (~0.5-1%) as the tolerance-ambition
   reference for the (v) budget design. Note only, no row.

## C. ADVISORY_INDEX.md
1. Block row (or per-file rows, matching the index's existing convention
   for raws directories) for `validation/sfoundations_raws_2026-08-13/r2pass/`
   — COUNT the files with a measured command in this window (SR-12),
   never from memory; content tag: Blocco-1 r2 batched pass + dual-seed
   v2/v3 + escalation + confirming round (2 SEED_PROTOCOL docs, briefs,
   refutations, 3 VERDICT files).
2. Block row for `validation/swirl5f_panel_2026-08-19/` — 10 files per
   its dispatch §2 BUT measure the count yourself (SR-12); tag:
   inter-session advisory panel (grades NOT of record; conversion table
   in DISPATCH; §8 verifier verdict pending at registration time if
   still absent).
3. Row for `validation/sfoundations_raws_2026-08-13/landing/` (the three
   landing briefs, measured count).
4. If the index carries the lint-(xx) bijection convention, verify your
   rows keep it (run the lint if the command is documented in the index
   header or SCAFFOLD §6; report outcome).

Return: per-file list of edits + measured counts used + lint outcome +
honest residue.
