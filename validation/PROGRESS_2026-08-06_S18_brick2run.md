# S18 — Total-order session log (2026-08-06; PRODUCTION CODE + END-TO-
# END RUN — BRICK 2 CLOSURE)

Session: S18, per the S17 step-12 handoff and PROGRESS NEXT-1 of
2026-08-06. Mandate (user order, this session's header): (1) S10+
opening; (2) pre-execution GATE; (3) the AMENDED PRODUCTION LEVERS of
the S17 adversarial pass (kickoff doc §5bis), each with its rejector
gate: P1 while-loop Newton on the certification metric (damping
retained, cap 30; licit because custom_vjp shields the primal loop
from AD) gated on X-SCANM equivalence RE-PASS; P2 bucket-per-phase +
whole-loop jit with the SAFE-WHERE guard (where-NaN-gradient trap
declared §5bis; dummy cells on a fixed problem with NO gradient path
to W) gated on the T2a clean-host RE-RUN (X-LSG0 — the PRODUCTION
GATE stays CLOSED until it passes; O3.1 = leak detector); P3
two-track C^1 closure (X-THC1 primary for the brick, linear 'nasa'
for the GENO twin; record and replay share one closure) gated on the
X-THC1 C5 band; P4 axial-margin rejector sharpening (instance floor
delta from the certified base design's min_margin, not just > 0).
THEN the end-to-end [X-TOCV] OPT run (S17 attempt died OOM on the
unjitted path — infrastructure, not method; NO inherited numbers,
R5): perturbed start -> TR-SQP (RK-G driver on trust-constr,
source-verified semantics) -> GENO type-2 contour oracle in the
double-resolution Richardson band + N3 -> transversality instance
(KKT + multiplier reading: eps = implicit p_a vs corner combination).
Mandatory run improvements: unbuffered output (verbose trust-constr +
tee to file), margin audit on the final design, T2 evaluated with the
MEASURED N_TR (constants in X-LSG0). Brick-2 Verdict + R4 same
session (kickoff doc + M0/D6 delta) -> UNLOCKS O3.3 (pre-registered
protocol P2_outline §5, intact; the O3.2/O3.3 campaign is the NEXT
session, P-2's numeric half). Bench items (B1 interval s_L, C-XBVP(b),
g-scan B3, GBE concavity) ONLY if budget survives the brick; NO
census-lemma, NO PAP-RIM (queued after, relative order decided then).

| # | Step |
|---|------|
| 1 | SESSION OPEN: HEAD = eae6bce (S17 closed at total order 1-12), branch rde-nozzle-program, no foreign commits, single-session rule verified. Tree state declared: known untracked unchanged (GENO/, literature/, ADR + census advisories, user mailmap artifacts, stray user files) — none touched. S10+ opening executed in full: project memory (MANDATORY five read: s17-brick2-kickoff — duties (a)-(d) ALL DONE with carriers, do-not-redo honored, NEXT-1 = this production mini-session then the run; moc-critical-independent-invariants — GENO is not gospel, critical list of mirrored quirks, truth rests on GENO-independent invariants (per-cell Newton certification, O3.1 transpose exactness, X-THC1 structural invariants, margin rejector); sota-library-survey-directive — survey + read-the-source + toolchain currency for EVERY complex step; general-vision-nondivergence — program picture at every fork; s15-foundations-campaign — campaign items do-not-redo; plus python-env-cantera for the live env). L0 = SCAFFOLD §1 read (six layers + registry; anchor = (P) of D2.6). claims_registry.yaml lint-verified LIVE this session: 122 entries parsed, 0 violations, 3 rejectors REJECTED (PASS) — matches the S17 closure count. D6 read (item 9 = brick STARTED annotation of record, duties bundled and executed S17). PROGRESS ORA (S17 block) + NEXT (NEXT-1 = this session) read. S17 log read COMPLETE (steps 1-12, incl. step 11 adversarial harvest). Kickoff doc docs/rde_nozzle_brick2_kickoff.md read ENTIRE (§5bis = this session's plan; adopted sequence P1 -> P2 -> P3 -> measure -> P4/P5 on numbers -> P6). ENV verified live: numpy 2.5.1, jax 0.11.0, scipy 1.18.0 (unpin of record holds); XLA persistent cache expected at TEMP/rde_jax_cache. TERMS OF REFERENCE BOUND: anchor (P) of D2.6; winner = closures NEVER hardware; novelty query-bounded; S14/S15/S16/S17 verdicts NOT re-litigated (incl. re-adjudication, the four duties, L-DoD with uniform-margin + h_min hypotheses, the T2a gate closure); L-X-H 2022 method attribution in quarantine; no numbers without committed carrier + rejector; R5: the S17 dead-run numbers DO NOT EXIST; R4 same session; one commit per task, path-limited, git log -3 + status before, hunk audit after, lint (xv) green at every commit. USER LOCKS untouched and not blocking: G5 send (time-boxed); ADR + census ratifications; literature uploads (page-verify L-X-H/Liu/Harroun); GV email; RDE dataset; PREPRINT DECISION (RK-A re-weighed S17, recommendation of record = arm the arXiv trigger when O3.3 numbers land — decision stays with the user); SDP = DECIDED (Clarabel primary, MOSEK fallback), not re-asked. |
| 2 | GATE (pre-execution, standing directive gate-pre-esecuzione) — (A) PLAN ADHERENCE: this session = D6 item 9 (A1 brick 2, F2/A1) CLOSURE leg: the production levers P1-P4 are the ADOPTED SEQUENCE of the kickoff doc §5bis (each already adjudicated in the S17 adversarial pass — this session EXECUTES, does not re-litigate); the end-to-end OPT run is the brick's own definition (D6 item 9: dJ/dSigma + TR-SQP + (**')/corner on the assembled march); the T2a re-run closes the PRODUCTION GATE opened (closed) at S17 step 9; O3.3 unlock = D6 item 9 "UNLOCKS O3.3 = P-2's numeric half" -> next session per PROGRESS NEXT. Tags [F2/A1][PIANO/S18]. Census-lemma + PAP-RIM EXCLUDED (queued after brick 2 by user pin, order decided then); bench items conditional on budget. (B) UPSTREAM RIGOR AUDIT (points this session rests on, re-read live): (i) X-SCANM equivalence 8.9e-16 vs Newton floor 1.6e-12 + O3.1 7.2e-09 vs 4.1e-08 — the P1 gate has its of-record baseline; (ii) X-LSG0 constants of record (t_record 18.0 s, t_solve 39.8 s, t_grad 119.6 s, grad/solve 3.004 T1 PASS, t_GENO 0.286 s, T2a FAIL-as-implemented, K_prac = 4) — the T2a gate is armed with derived thresholds, nothing re-derived here; (iii) §5bis R-1 amendment: while_loop is licit ONLY because every cell is custom_vjp (AD never traces the primal loop) — damping RETAINED, cap 30; (iv) §5bis R-4 trap: jnp.where does NOT shield gradients from NaN in the masked branch — dummy cells must be a fixed well-conditioned problem with NO gradient path to W; O3.1 is the leak detector; (v) §5bis R-5: the staged X-TOCV carrier still defaults to the LINEAR closure — the P3 fix is scheduled, record and replay must share ONE closure, the GENO twin regression STAYS on 'nasa' linear (bit-level data contract); (vi) X-THC1 C5 dual-route band of record: max|dT| = 9.1e-07 K inside 1.5e-05 K — the P3 gate; (vii) margin rejector as-committed checks u_x - c > 0; the S17 sharpening note binds the P4 floor to the certified base design's min_margin (instance floor, declared); (viii) trust-constr semantics source-verified S17 (equality-only Byrd-Omojokun; callback(state) with legacy (xk, state) arity fixed; fresh BFGS per segment; status 4 = failure) — reused, not re-read; (ix) L-DoD hypotheses (uniform axial margin delta + h_min) = two D2.5-U constants — the truncation in the record path is lemma-backed; (x) O3.3 protocol PRE-REGISTERED intact (P2_outline §5): this session UNLOCKS, does not run it. DISCREPANCIES: none found against the record; the §5bis plan maps 1:1 onto this session's mandate. (C) VERDICT: **gate pre-esecuzione PASS** — proceed to P1. |
| 3 | P1 [F2/A1] WHILE-NEWTON EXECUTED (kickoff §5bis R-1 amended lever) — `make_implicit_solver.newton` in validation/a1_ideal_march_jax.py moved from `lax.fori_loop(0, 30)` to `lax.while_loop` terminating on the CERTIFICATION METRIC (the undamped Newton step size at the current iterate — the same quantity `step_norm` certifies post-hoc — against the certification bound NEWTON_TOL_FACTOR x eps x scale(z)), cap N_NEWTON = 30 UNCHANGED, damping RETAINED (trial set {1, 1/2, 1/4, 1/16, 1/64, 0} untouched — bad trial points keep full robustness); licit because `solve` is custom_vjp: AD never traces the primal loop (while_loop has no reverse rule; the transpose IS the implicit rule — Lemma B untouched). GATE RE-RUN [X-SCANM] VERDICT PASS (exit 0): record march certified (cert worst 2.311e-02 over 2756 cells — slightly above the S17 fori-30 value as expected: the loop exits AT certification instead of polishing 30 trips, still two orders inside the bound); plan identical (20 fan / 25 arc (2 interp) / 40 straightening, 2684 interior cells); **equivalence scan-vs-python 8.882e-16 vs Newton-floor tol 1.630e-12 (bit-level, unchanged from S17)**; O3.1 on the scan path 9.991e-09 vs derived tol 5.032e-08; N1 mis-indexed plan DETECTED (1.552e-03 vs floor), N2 corrupted vjp DETECTED. The P1 gate condition (X-SCANM re-pass) is MET. |
| 4 | P2 [F2/A1] BUCKET-PER-PHASE + WHOLE-LOOP JIT EXECUTED (kickoff §5bis R-4 lever, the BINDING T2a remediation) — new engine `make_run_scan_jit` in validation/a1_march_scan.py: THREE lax.scan modules (fan / arc / straightening) inside ONE jitted function of P; all plan-derived arrays baked as numpy constants; per-evaluation Python re-trace REMOVED structurally. SAFE-WHERE GUARD implemented as declared in §5bis: padded lanes solve a FIXED dummy problem that is an EXACT recorded cell problem (fan carry/pt2 pair with its recorded solution as seed — certification-VERIFIED at build time with a RuntimeError rejector; numpy constants => no gradient path to P); the one genuinely new select (interp flag angle) carries its own safe-den guard. GATE 1 [X-SCANM re-run with the P2 rejectors added] VERDICT PASS: **bucketed jit == python replay at 6.217e-15 vs 1.630e-12 Newton floor (bit-level)**; whole-loop compile ONLY 4.9 s (the S17 mega-jit fear resolved by the 3-module design); **O3.1 on the jit path 4.923e-11 vs tol 2.424e-08 — the safe-where leak detector CLEAN** (a masked-branch NaN would break the transpose identity); N3 corrupted jit vjp REJECTED; all prior checks unchanged-PASS. GATE 2 [X-LSG0 T2a RE-RUN, clean host] VERDICT PASS: t_record 17.22 s; one-time compile solve 4.7 s / grad 10.3 s (persistent XLA cache); **t_solve = 0.116 s (was 39.8 s as-implemented: 343x), t_grad = 0.185 s (was 119.6 s), grad/solve = 1.593 <= 4 (T1 PASS)**; t_GENO = 0.299 s; **T2a: 0.116 s <= K_prac x t_GENO = 1.197 s — PASS, THE PRODUCTION GATE IS OPEN** (closed since S17 step 9); per-cell replay 4.15e-05 s (declared projection); t_record/t_solve = 148.5 (RK-G accounting); negative control fires. The X-LSG0 measurement-object note updated of record (production path; a T2a failure is now STRUCTURAL = D6 flip clause). Note of record: grad/solve moved from 3.004 (per-column dispatch path) to 1.593 on the whole-loop path — still consistent with the cheap-gradient theorem (the bound is an upper bound; the padded primal pays dummy-lane work the transpose partially reuses). |
| 5 | P3 + P4 [F2/A1] TWO-TRACK CLOSURE + MARGIN FLOOR EXECUTED on the brick carrier (X-TOCV, staged re-run) — (i) P3 WIRING: the brick's PRIMARY closure is now [X-THC1] quintic C^1 (state_c1 adapter + dedicated solver set, record AND replay share it — one closure per run, never mixed); the GENO twin regression stays on 'nasa' linear (two tracks, declared). (ii) TOC PRODUCTION PATH: make_run_toc_scan_jit (fan + design-wall padded scan modules in ONE whole-loop jit of W; safe-where guard with certification-verified recorded dummies for interior AND axis lanes) wired into the RK-G driver (runj per segment; jitted value_and_grad; re-record SKIPPED when the accepted iterate has not moved — semantics-preserving, declared). (iii) P4 SHARPENING: margin_floor parameter on the record rejector + DECLARED INSTANCE FLOOR delta_inst = min_margin(W0)/K_RICH for converged-design audits. STAGED VERDICT PASS (all gates): record W0 (C^1) 24.6 s, 4343 cells, cert worst 2.106e-01, **min axial margin 0.4809 m/s => delta_inst = 0.1202 m/s (the L-DoD uniform-margin instance constant)**; **P3 GATE: max|u_c1 - u_lin| on the wall = 1.022e-07 vs two-resolution Richardson band 5.660e-01 (0/98 out) — the closure switch is SEVEN ORDERS sub-resolution** (the X-THC1 C5 statement holding at march level); replay fidelity per-column 1.091e-11 AND bucketed jit 1.228e-11 vs 5.601e-10 floor (TOC whole-loop compile 6.3 s); O3.1 on the PRODUCTION path 1.188e-03 vs derived tol 1.156e-01 (J(W0) = 2.775106e+07; leak detector clean), N1 corrupted gradient REJECTED. DEVIATIONS DECLARED: (a) the P3 gate is at FLOW level (wall SPEED profile) because the wall GEOMETRY of a specified-wall march is resolution-independent — the contour-level reading of the C5 gate is vacuous here (same reason as the oracle-band fix in step 6); (b) first staged attempt KILLED by me at the record phase (~18 min in, CPU/wall 0.28 = dispatch-bound eager per-cell closure calls in the margin rejector; fix = sound-speed lookup jitted once per run — general, no semantics change; NO numbers harvested from the killed attempt); (c) record cert worst rose from 1.65e-02 (S17 fori-30) to 2.106e-01 — expected under the P1 while-Newton (exit AT certification rather than 30-trip polishing), still 5x inside the bound. |
| 6 | END-TO-END OPT RUN, ATTEMPT 1 — HONEST FAIL WITH FULL DIAGNOSIS (verbose + tee per the session mandate; 480 s wall, NO OOM: the S17 infrastructure failure mechanism is GONE — every objective/gradient call is a compiled call, t_solve 0.693 s / t_grad 0.919 s per eval on the TOC case). MEASURED OUTCOME (diagnostics, NOT results): 8 segments (the cap), 17 re-records, 25 iterations; every RK-G segment accepted ONE step then a wall-search decision (N, Nv) flipped (first_diff at columns 14/44/81/82/92 — the chord-foot indices are FRAGILE decisions over ~100 stations) => segment restart; status 3 at the cap with KKT 2.323e+05 vs gtol 1.156e-01 — NOT CONVERGED (convergence + transversality checks FAIL as they must); J rose from 2.7751060e+07 (seed) to 2.7757801e+07 — the gradient works (recovered the perturbation AND exceeded the GENO-projected seed); P4 margin audit PASS (0.4809 >= 0.1202); N3 discriminator PASS; oracle FAIL consistent with non-convergence (max|dy| 2.83e-03 vs 1.63e-03 band); T2 as-instantiated FAIL (25 x 1.613 s = 40.3 s vs 4 x 8.225 s = 32.9 s, GENO type-2 log: 10 outer iters / 96 inner marches). ADJUDICATION: driver-artifact failure, not method failure — (i) max_segments = 8 truncated the walk (the RK-G policy does NOT bound segment count; with per-flip restarts one segment ~ one productive step, so the cap IS the iteration budget); (ii) each restart re-shrank the trust radius from 0.05 (two wasted evals + capped step per segment). AMENDMENTS EXECUTED before the deciding run (all declared): max_segments -> 100; trust radius CARRIED across segments (callback-captured, clipped [1e-3, 0.25] — the RK-G excursion bound is the CURRENT radius); per-segment progress line. ORACLE AMENDMENT PRE-REGISTERED before the deciding run: the band gains the MEASURED spline-class REPRESENTATION term (W0 is by construction the projection of the GENO wall onto the M_NODES-dof design class, so |spline(W0) - GENO| is the class's own projection floor — measured, not tuned; zero on the arc sector which the class represents exactly; both band components reported separately). R5: none of the attempt-1 numbers are claimed as results. |
| 7 | END-TO-END OPT RUN — CONVERGED, VERDICT OF RECORD PASS (attempts 2-5 fully declared; the RECORD run is attempt 5 = the committed carrier executed end-to-end, exit 0). ATTEMPT 2 (segment cap 100 + carried radius): J climbed monotonically but KKT plateaued at ~1.2-1.5e+05 with decay ~5%/segment — the measured STEEPEST-DESCENT rate of a kappa ~ 30-50 problem (fresh-BFGS-per-segment is policy-bound, so conditioning IS the rate); KILLED at segment 18, diagnosis recorded, no numbers claimed. ATTEMPT 3 (+ measured JACOBI preconditioning: diagonal curvature by central differences of the exact adjoint gradient, 18 evals; diag|H| in [1.62e+07, 4.80e+08], kappa_diag 5.4, stiff direction = thB with scale 5.29): first segment did the work of 18 attempt-2 segments; KKT re-plateaued in the 4e+04-1e+05 band (off-diagonal coupling invisible to diagonal scaling); KILLED at segment 15, no numbers claimed. ATTEMPT 4 (+ R-3 ACTIVATION on numbers: MEASURED FULL HESSIAN at each segment base by forward differences of the adjoint gradient, n+1 = 10 evals, symmetrized, frozen within the segment — policy-conformant: freshly measured per base, never carried): Newton-quality steps — segment 3 entered the FLIP-FREE regime (15 iterations), KKT 3.5e+04 -> 1.465, then xtol collapse (stale base Hessian after the early large steps) => carrier FAIL as it must; TWO structural findings from its diagnostics: (i) status-2-with-KKT-open is stale-model collapse, not stationarity — driver now CONTINUES with a fresh segment, stopping only on gtol or zero progress; (ii) the single out-of-band oracle sample (x = 1.155, err/band 1.19) was located by a standalone geometric diagnostic and CAUSE-VERIFIED: the measured band proxies are |differences| with an ISOLATED ZERO CROSSING there (e_repr dips to 5e-05 vs 3e-03 elsewhere) — a pointwise band collapses at estimator zeros while the true error is smooth (neighbors pass at err/band 0.36-0.49); AMENDMENT 4 (declared): neighborhood envelope (running max over adjacent samples, edge-clamped) on the measured band terms — repairs degenerate zeros from the estimator's own neighborhood, no new constants; same envelope discipline as the X-GENOXC floor. T2 SEMANTICS adjudicated: indicator (mirroring the X-LSG0 T2a pattern of record), firing = governance event (G0 re-decision review queued), not a voider of the scientific claims. ATTEMPT 5 = THE RECORD RUN (deterministic replay of the trajectory + the two semantic fixes): **VERDICT PASS**. NUMBERS OF RECORD: TR-SQP status 1 (gtol), **KKT 7.745e-02 <= gtol 1.156e-01 — O3 TRANSVERSALITY INSTANCE PASS: the Rao stationarity reached VIA THE GRADIENT** (5 segments, 14 re-records REPORTED per DIR-RKG, 3 re-record events, 23 iterations, 67 evals, 400 s); **J(perturbed start) -> J* = 2.7761688e+07 vs J(GENO-projected seed) = 2.7751060e+07 (+1.06e+04: the optimizer recovers the 1.5% perturbation AND beats the seed in-class)**; W*: thB = 15.5527 deg (seed 18.8456 — attachment angle traded against nodes at measured J-flatness); P4 margin audit PASS (min u_x - c = 0.4809 >= 0.1202 m/s instance floor); multiplier reading lambda_eps = 1.054966e+07 => implied Pa 8.395e+05 Pa vs lip pressure 1.412e+06 Pa (ratio 0.595 — instance reading, full corner term-match = O3.3); **ORACLE: 91/91 samples inside the derived cross-code band** (components: GENO cross-res 4.1e-04, spline-class representation 3.0e-03, reference resampling 7.6e-03; max|dy| = 1.861e-03, median err/band 0.102) — the two INDEPENDENT routes (variational gradient vs classical Mrao/eps bisection) agree on the Rao contour at ~1e-03 absolute; N3 wrong-design discriminator PASS; **T2 FIRED of record** (67 x 1.814 s = 121.6 s vs 4 x 8.981 s = 35.9 s; GENO type-2 log: 10 outer iters, 96 inner marches): cost decomposition = curvature measurement + RK-G re-records (T1 = 1.593 and T2a = 0.116 s vs 1.197 s both PASS with margin => NOT language-throughput) — **consequence per the D6 flip clause: G0 re-decision REVIEW QUEUED with this evidence** (not silent, not a voider). **BRICK 2 VERDICT: CLOSED** — the variational-TOC road exists end-to-end of record (certified march + exact adjoint gradient + RK-G-conformant TR-SQP + transversality instance + cross-code oracle), with two declared open governance items (T2/G0 review; benign-flip + second-order-implicit levers named in the kickoff doc). **O3.3 IS UNLOCKED** (D6 item 9: the pre-registered protocol P2_outline §5 untouched — the O3.2/O3.3 campaign is the next session, P-2's numeric half). |
