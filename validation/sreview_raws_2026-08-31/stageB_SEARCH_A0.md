# STAGE B — item SEARCH — ADVOCATE A0 (genuine advocate of the ALTERNATIVE: explore-then-certify)

Persona (stageB_items.json id SEARCH, advocate_persona): the numerical-optimization / DFO engineer of the
de-novo trees (optimization lens S7/S8 + hyperbolic S11). Deliverable = the PRE-REGISTRATION of PILOT-CB,
never its execution (REF-13 joint clause at F2.REPR; V-C57 item 3). Scope rule honored: every argument below is
weighed only by how it changes the CREDIBILITY or the COST of answering Q0 by the search road under review,
including the road's own decisive experiment. Literature tiers [IO]/[REP]/[APERTO] and procurement asks in §6.
Files read integrally or at the cited lines: stageA_diff_SP4.md (:1-294), stageB_items.json, choice_ledger C23/
C27/C28/C31/C38/C49/C57/C60, VERDICT_engine_cluster_2026-08-31.md :58-125, PROGRESS S20 :78, S22 :73-80, S24
:106-112, M0 :3487-3560 and :4250-4299, TWIN A-1 (:148-164), RED_TEAM RT-8 (:250-268), PROGRESS_Sreview LOG-2
:265-268 (dominance criterion), the three JSON design sets, the four trees' SP4 blocks, literature_registry rows
:1108-1136 and :1180-1190; pinned env probed READ-ONLY (no install) for DFO engines.

## 0. THE FALSIFIER I PROPOSE (both sides must agree on it BEFORE arguing)

PILOT-CB as pre-registered in stageA_diff_SP4.md §6, SHARPENED on four points the refuter will otherwise attack:

F-SEARCH (one pilot, one instance, two arms, both recorders, envfp pinned; NO RUN in Stage B):
- INSTANCE of record: the S22 certifiability-limited base (validation/s22_certlim_base.json: W length 11, xi
  length 10, provenance "[X-AKNO] certifiability-limited returned base (outcome II)") + its FIVE distinct rejected
  frontier designs (validation/s22_rejected_designs.json: 8 records at cert_worst 1.170 / 2.458 x4 / 1.060 / 1.455 /
  1.698, m_nodes = 10) + the S20 W* (validation/s20_adaptive_design.json, W length 9) as multistart seeds.
- ARM I (incumbent): the segmented TR-Newton certify-then-accept walk of record from the outcome-II base, re-run
  under the closure-aware gate with the A-1 margin rule (quotable iff cert_worst <= 1/K_RICH = 0.25 under the
  pinned recorder, TWIN §9 A-1). Arm I runs FIRST and its march counter defines the budget.
- ARM E (alternative): explore-then-certify = multistart (>= 2 starts: S22 base, S20 W*, the classical seed) +
  amplitude homotopy from the classical seed (A5, 4/4 derived, SP4 diff §3) driven by a derivative-free engine on the
  SAME fitted march with the P4 gate DEFERRED (each evaluation returns J and logs cert_worst as a free row), then ONE
  terminal certification of the winner with margin (A-1 rule, BOTH recorders printed) and, on failure, ONE certified
  back-projection whose marches are CHARGED to Arm E's budget.
- EQUAL BUDGET (sharpening 1): defined in MARCH COUNT (forward + adjoint marches logged by Arm I), never in seconds —
  the S22 anchor "14 segments, 498 s" (S22 log :79) is a wall-clock at the pre-S25 solve cost (1.197 s vs 0.116 s
  post-S25, M0 :3287) and cannot be scaled honestly without the count. Back-projection marches count (sharpening 2).
- ENGINE (sharpening 3, measured this window): the pinned env carries NO BO/GP/CMA-ES engine (skopt, botorch, cma,
  nevergrad, optuna, pybobyqa, nlopt, sklearn, torch: all ABSENT; scipy 1.18.0 present with COBYQA, Powell, COBYLA,
  differential_evolution). Installing is an O5-class decision forbidden to agents. Arm E is therefore pre-registered
  on scipy-native engines: COBYQA (model-based DFO with nonlinear inequality constraints) as the local explorer with
  cert_worst <= 1/K_RICH passed as a QUANTIFIED inequality, and differential_evolution / multistart as the global
  layer. A GP/EI arm is a SECOND pilot arm only on a user O5 card (procurement ask, §6).
- OUTCOMES (each must be able to fire; thresholds from record constants, no new numerals):
  K1: Arm E's terminal design certifies with margin (cert_worst <= 0.25, pinned recorder) AND
      J_E - J_I > K_RICH |J_M - J_2M| (the registered two-level band; the S24 F1b instance of that band was
      5.3824e3 on J ~ 4.03e7, i.e. ~1.3e-4 relative, S24 log :110) -> the in-loop closer is FALSIFIED as the closer
      for this class; the pilot posture becomes the arm-P recipe of TWIN §4 by amendment.
  K2: Arm E's winner FAILS terminal certification AND its certified back-projection lands within the band of J_I ->
      the alternative is REFUTED on this class; the frontier is class-intrinsic; C38 closes on this datum with the
      outcome-II declaration explicit (rejection count + non-evaluable rule, SP4 A9).
  K2' (sharpening 4, the refuter's own point adopted as a kill): the back-projection does not reach a design with
      cert_worst <= 0.25 within the residual budget -> Arm E is NON-EVALUABLE on this class, declared; same
      consequence as K2 for the closer question.
  K3 / K3': Arm E's certified design has the margin ACTIVE (mu > 0 on the ladder) -> the C28 priced-margin hybrid is
      vindicated for the first time; INACTIVE a fourth time -> C28 declared dead weight at tier 0, ladder re-scoped
      to tier-1 entry.
  K0 (declared, not a verdict): Arm E never leaves the start by more than the band under the budget -> pilot
      INCONCLUSIVE; printed as such; no amendment either way.
- SEEDED REJECTORS (scripted): (i) a doctored Arm E winner at cert_worst 0.9 must be REFUSED as quotable (A-1 seed);
  (ii) an Arm E run whose march count exceeds Arm I's by > 0 must be REFUSED as "equal budget"; (iii) a K1 print with
  J_E - J_I inside the band must be REFUSED.
- OWNER / WINDOW: pre-registration = this item; execution + priority = the F2.REPR joint table (REF-13, V-C57 item 3),
  where Arm I's re-run is SHARED with the X-AKNO / X-LOCD re-forms already owed (M0 :4262-4267 names those runs), so
  the pilot's incremental cost is Arm E only (§3).

I accept this falsifier in full, including K2/K2' as the measurements that would make me abandon the alternative.

## 1. POSITION (one paragraph)

The record has no certified outcome-I optimum on any sector: three campaigns exited certifiability-limited with the
priced margin INACTIVE (S20 :78 "the walk hits the boundary of the DISCRETE certifiable set K ... certdiag 8/8"; S22
:79 "MARGIN INACTIVE THROUGHOUT ... mu = 0 identically on the ladder ... SEVEN rejections ALL at cert_worst 1.531";
S24 :109-110 "min DE val 7.31e-2 = 33x the tightest floor ... F4 FIRED ... third instance"). The binding constraint is
therefore NOT the physical margin the ladder prices (M0 :3487-3510) but a hidden, version-bound, discrete frontier
(M0 :4254-4271: the binary verdicts of cert-marginal designs at cert_worst 1.06-2.46 FLIP with the recorder; X-AKNO
"crawls a different frontier"). A certify-then-accept closer STOPS at the first contact with that frontier, so what
the three exits measured is the recorder's frontier, not the argmax of J. Explore-then-certify does not weaken the
certificate (the shipped design carries the SAME terminal A-1 certificate on converged forward states, M0 :4281-4287);
it removes the certificate from the PATH, where it was never quotable anyway, and lets the search cross a thin
cert-marginal band that the in-loop gate treats as a wall. At 0.116 s per solve (V-C57 item 3, M0 :3287) the pilot
that decides this is a session-fraction and it changes the credibility of the decisive number in BOTH outcomes.

## 2. THE CASE AT ITS GENUINE BEST (anchored)

2.1 The record's OWN taxonomy says the incumbent is fighting the worst kind of constraint the wrong way. M0 :3535-3545
[REP via M0; no registry row, no PDF on disk — see §6] classes the certifiability constraint as Known-Unrelaxable-
Simulation-NONQUANTIFIABLE ("the optimizer learns only pass/fail, after paying a march") and names the remedy:
QUANTIFY a margin. The record quantified the FOLD/CAUSALITY margin (m(Sigma) in A_t(mu_0)); three runs showed it is
not the binding one (mu = 0 x3). The binding quantity that DOES vary continuously across the frontier is cert_worst
itself (1.06 / 1.17 / 1.455 / 1.698 / 2.458 on the five rejected designs; 0.899-0.485 on the base, S22 :77). Arm E
passes cert_worst <= 1/K_RICH to a constrained DFO engine as a quantified inequality — this is the derivative-free
instantiation of the C28 hybrid the record adopted on paper ("KS-max on traced per-cell certification ratios",
C28 note, [P-CERTKS]) and never got to bind. K3/K3' is the first measurement of whether that representation binds.

2.2 The in-loop certificate buys nothing quotable. Every design the closer certifies on its path is discarded except
the last; the last is quotable only under A-1 (TWIN §9). Under explore-then-certify the last design carries exactly
the same certificate, printed with recorder + envfp. The NAND structural reason (M0 :4281-4287, V-C60) is orthogonal:
Arm E is reduced-space too (design variables only; state by the same march); nothing in it is a SAND iterate.

2.3 The frontier is thin and version-bound, which is the regime where local certify-then-accept is provably
start-dependent and exploration is cheap. RT-8 (RED_TEAM :256-262): "a local argmax from one start is a property of
the start"; X-AKNO reproduces the S20 outcome CLASS but "crawls a different frontier (first offender 1.916 at cell
2010)" (M0 :4265-4267). Two starts on the same instance already reach different frontier points; the in-loop gate
freezes whichever is hit first. Arm E's multistart + homotopy is the 4/4-derived answer (SP4 diff A4/A5; opt G.3/G.7,
hyp (2)+(5), var X.2/X.3, prop X.2/X.6) and its dispersion row is the optimizer-basin row RT-8 A-8(iii) demands.

2.4 J is continuous through the frontier; certification is a per-cell post-hoc flag. The P4 error text of every
rejected record is "record at segment base not certified (worst ...)" — the record EXISTS with its J; the certificate
(C17/C18 per-cell Newton residual vs the NTF floor) is evaluated on it afterwards. X-CDKAT: cert_worst moved
0.2106 -> 0.1813 (-13.9%) under env change "with the verdict STRUCTURE unchanged" (M0 :4257-4260). So Arm E consumes
the march's own J (never a surrogate value — the refuter's "no certificate class for surrogate values" does not
apply) and logs cert_worst per evaluation at zero cost.

2.5 In-class J differences large enough to fire K1 exist. S24 F7: a certified, margin-inactive design beat the DEF
representative by +0.51% = 38x the registered band, "without approaching the boundary" (S24 :110). K1's threshold
K_RICH|J_M - J_2M| (~1e-4 relative) is therefore fireable by a factor ~10 on this class — and so is K2, because the
five rejected designs sit at cert_worst 1.06-2.46, i.e. within one recorder flip of PASS: a back-projection of a few
per cent in W plausibly lands at J within the band of J_I. Both arms of the falsifier are live; neither is rigged.

2.6 Generality and the decisive sector. The decisive test is the truncated plug (TWIN §2), whose state solve
includes the H20 free plume — "the FIRST genuinely iterative outer state solve in the program" (V-C60 item 1(iii)) —
and the C61 base closure; plug sensitivity machinery does not exist (LEVERS F2-M3, cited at SP4 diff §3 A5). A
derivative-free explorer on the cheap tier is the ONLY search that runs on the plug at F3.TWIN opening without first
building plug adjoints; if K1 fires, the plug arm P has a recipe on day one of F3; if K2 fires, the record knows the
adjoint build is unavoidable BEFORE spending F2.ENGINE on it. Either outcome re-prices the critical path.

2.7 The paradigm was never adjudicated. C57's own note: the certificate-first constitution is "declared axiology,
never head-to-head adjudicated"; V-C57 item 1: "never a head-to-head vs the modern DFO/BO/evolutionary tier"; the
user ratified the one-shot pilot on 2026-08-20 for exactly this reason (C57 note). All four blind lenses put the
DFO tier where I put it: exploration + terminal certification, never the closer (SP4 diff A6, 4/4). I do not ask
the record to replace the closer; I ask it to measure whether the closer's in-loop gate is the reason no outcome I
exists.

## 3. COST, CREDIBILITY, AND THE DOMINANCE SCALES (PROGRESS_Sreview LOG-2 :265)

COST (sessions-to-first-credible-number, band declared): Arm I re-run = 0 incremental (shared with the X-AKNO /
X-LOCD re-forms owed at F2.ENGINE first act). Arm E: at m ~ 10 dofs the trees' own DFO count is O(10d)-O(50d) =
100-500 marches per local run (opt S8 :163-172); x(3 starts + ~5 homotopy steps) ~ 2-4e3 marches; at 0.116 s/solve
(T2a bench, M0 :3287) that is 4-8 minutes of compute; with a x10 safety for adjoint-free segment overheads, < 1.5 h.
Harness (COBYQA wrapper over the existing march callable, deferred-gate flag, march counter, back-projection = the
existing feasibility-restoration path of the driver, seeded rejectors): 2-3 h. Band [0.3, 0.6] session, inside the
SP4 diff's "<= 1 session". Incumbent COST to the same datum ("is the frontier the closer's or the class's"): not
priced in the record — the closer alone cannot produce it (a fourth run reproduces the exit class, X-AKNO); hence
COST_lo(I) is undefined for this question, and the criterion's cost inequality is trivially satisfied on the datum,
NOT on the whole road (I do not claim road-level dominance from a pilot).

CRED (referee's five questions): the pilot raises q4 (reproducibility / version-stability) from "recorder-bound
binary verdicts" to a printed dispersion + both-recorders row, and q3 (uncertainty budget) by removing the
class-construction artefact from SMALL (RT-9: "SMALL can be a class-construction artifact"). It does not touch q1,
q2, q5. Honest score delta: +1 to +2 on CRED for the SEARCH road IF K1 or K2 fires; 0 if K0. The incumbent's CRED on
q4 is capped by its own three exits.

Effect on Q0 through the decisive experiment: TWIN §6 branches MATERIAL/SMALL/INTERMEDIATE are readable only if
arm P reaches outcome I with margin; a fourth cert-limited exit caps the twin at INTERMEDIATE by construction (SP4
diff §4 item 3). PILOT-CB is the cheapest measurement that tells the program, before F3 opens, whether arm P's
recipe must change (K1) or whether the outcome-II declaration is the honest shape of the answer (K2) — the
"never postpone the resolvable" directive applies: cheap, in-window, with an owner and a trigger.

## 4. THE REFUTER'S ATTACKS, ANSWERED IN ADVANCE

R1 "DFO has no certificate class; surrogate values." — Arm E evaluates the march's own J (§2.4), and the shipped
design carries the SAME A-1 certificate as Arm I's. The certificate is a property of the final converged forward
state (M0 :4281-4287), not of the path. No surrogate enters; a GP arm would, and is deliberately NOT pre-registered
(§0, engine).
R2 "The captured explorer does not exist." — Conceded (grep this window: the only "captur*" hits in validation/*.py
are subprocess/callback word matches, no capturing solver; C49 explorer build is a F2.REPR decision). Arm E runs on
the fitted march with the gate deferred; the C49 vehicle is an upgrade, not a precondition.
R3 "Deferred certification = an uncertified walk that can end anywhere." — It ends where it ends and is then either
certified (K1/K3), back-projected (K2), or declared non-evaluable (K2'/K0). Compare: the incumbent's certified walk
ended three times at a frontier the recorder moves (M0 :4262-4267). A path certificate that is recorder-bound is
the walk that "ends anywhere"; the pilot measures which is which.
R4 "Back-projection into K is itself a search problem." — Conceded and charged: back-projection marches count
against Arm E's budget and K2' kills Arm E if it does not land within the residual budget. The incumbent's driver
already runs this restoration (rejected_designs + TR shrink, SP4 diff A9); I use the same code path.
R5 "Equal budget is unmeasured." — Conceded; §0 defines it as Arm I's logged march count, and rejector (ii) refuses
any Arm E overrun. Seconds are not the unit.
R6 "NAND structural reason." — Orthogonal (§2.2). Arm E is reduced-space.
R7 (PM) "The F2.ENGINE first-act window is taken by [P-IPADJ], [OBJ-DOM-IMPL], X-O32/X-LOCD re-forms." — Conceded on
scheduling: execution is at the F2.REPR joint table by REF-13 (V-C57 item 3), and Arm I is the X-AKNO/X-LOCD re-form
itself. Nobody yields a window; the pilot borrows Arm I's run and adds < 1.5 h of adjoint-free compute.
R8 "K3/K3' rides the unqualified ratio; the wrong-branch mode evades both the priced ratio and the P4 gate" (C28
wave-2 rider). — Declared: K3 reads activity of the margin multiplier, not the ratio's qualification; the wrong-branch
discrimination waits on C20 Tier-0 exactly as the ledger says, for BOTH arms equally.

## 5. CONCESSIONS (stated before the refuter asks)

C-1 No BO/GP/CMA-ES engine exists in the pinned env (measured this window); "at its genuine modern best" is NOT
    available without an O5 card. Arm E as pre-registered is scipy-native (COBYQA + differential_evolution +
    multistart + homotopy); a GP/EI arm is a procurement ask, not a claim.
C-2 No capturing explorer exists; C49 build decision at F2.REPR.
C-3 Back-projection is a search; it is charged and K2' is a kill.
C-4 The equal-budget unit is the march count, measured on Arm I first; the "498 s" figure is not a budget.
C-5 If K2 or K2' fires, I abandon explore-then-certify for this class: the closer stands, C38 closes on the datum,
    and the alternative survives only as the seeding/dispersion apparatus already in the record (C57 alternative 3).
C-6 The pilot is one instance (n = 1, m ~ 10, bell class of the S22 base); a K1 on it licenses an AMENDMENT of the
    arm-P recipe, not a theorem about the class. Generalization (q5) is untouched.
C-7 Nothing in this position touches a rigor class of M0 Parts I-V; the ladder/KKT-with-margin structure stays
    THEOREM/SCHEMA as declared (M0 :3529-3534).

## 6. [KNOWLEDGE] LEDGER (tiers; nothing above the depth held)

- Le Digabel & Wild, taxonomy of constraints in simulation-based optimization — [REP] via M0 :3535-3545 (surveyed
  2026-08-07, S20 log step 9); no registry row, no PDF on disk. Load-bearing for §2.1 only as the record's own
  classification; I cite the record's sentence, not the paper. PROCUREMENT ASK: the paper (Optimization and
  Engineering, 2015/2017 class) — needed to verify the "quantify a margin" remedy wording before the pilot
  pre-registration cites it as external; owner C28/[P-CERTKS] window.
- COBYQA / differential_evolution present in scipy 1.18.0 — [IO] measured this window on the pinned env (import
  probe, read-only). scipy 1.18.0 = the env of record (memory python-env-cantera, envfp e100f996 duty).
- Jones, Schonlau & Welch 1998 (EGO) and Hansen CMA-ES — [APERTO]: cited by the trees (opt S8 [full], prop S-7),
  NO registry row (grep jones/schonlau/cma/hansen = 0 hits this window). Not load-bearing here (the GP arm is NOT
  pre-registered). PROCUREMENT ASK only if the user issues the O5 card for a GP arm.
- "Derivative-free optimization is competitive for aerodynamic design optimization in moderate dimensions",
  arXiv:2505.09088 — registry WANTED, TITLE tier (:1183-1187), no path. NOT used as evidence.
- Conn, Gould & Toint, Trust-Region Methods — registry WANTED (:1128), no path. NOT used.
- Farrell, Birkisson & Funke 2015 (deflation) — registry WANTED (:1230), no path; it is the INCUMBENT's M4 apparatus
  (V-C57 item 1) and is cited here only to note that the incumbent's global machinery is also unread and unbuilt.
- Alexandrov et al. (TR model management) / Peherstorfer et al. 2018 — [APERTO], not used (A10 is road-(h)
  conditional, outside SEARCH).
- Every numeral above (0.116 s, 1.197 s, cert_worst values, 33x, +0.51%, 5.3824e3, -13.9%, 1.06-2.46, K_RICH = 4)
  is a record numeral with its file:line cited; none is [KNOWLEDGE].

## 7. WHAT WOULD MAKE ME ABANDON THE ALTERNATIVE

K2 or K2' of §0 on the S22 instance (Arm E's winner fails terminal certification and its charged back-projection
either lands within the band of J_I or exhausts the budget). A secondary abandon: K0 twice (two independent seeds,
no motion beyond the band) — then the class is flat at this m and the search paradigm is immaterial, which is a
result for the SMALL branch, not for me.

## 8. PRE-REGISTRATION TEXT (M0/D6-ready, class PRACTICE until executed)

PILOT-CB [PRACTICE, pre-registered 2026-09-05, execution at the F2.REPR joint table (REF-13)]: instance = S22
certifiability-limited base + five rejected frontier designs + S20 W* (validation/s22_certlim_base.json,
s22_rejected_designs.json, s20_adaptive_design.json); Arm I = segmented TR-Newton certify-then-accept of record,
closure-aware gate, A-1 rule, both recorders printed, march count N_I logged; Arm E = multistart (>= 2 starts) +
amplitude homotopy from the classical seed, scipy COBYQA with cert_worst <= 1/K_RICH as a quantified inequality +
differential_evolution global layer, P4 gate deferred, per-evaluation cert_worst logged, budget N_E <= N_I including
back-projection marches, ONE terminal certification with margin under the pinned recorder (+ second recorder
printed), ONE certified back-projection on failure; outcomes K1 / K2 / K2' / K3 / K3' / K0 as in §0 with the
registered two-level band K_RICH|J_M - J_2M| as the only threshold; seeded rejectors (i)-(iii); zero-cost rows landed
regardless of outcome: rejection count, non-evaluable declaration, multistart dispersion (A-8 iii). Falsifier of the
pre-registration itself: an Arm E harness that cannot return J from an uncertified record (then §2.4 is false and
the pilot is re-designed on the captured tier or withdrawn).
